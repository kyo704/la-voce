import StartFlow from "@/components/StartFlow";

// ============================================================================
// 着地の画面（2026-09-04）
//
//   ★QRコードの行き先です。★対面でお渡しするときに使います。
//
//   ★★`/start` は使えません。★すでに「印刷用の1枚」があります
//     （app/start/page.js・紙に刷って配る版下）。
//     ★★2026-09-04、私はそれを一度上書きしました。★戻しました。
//       ★`cat >` は、★在るかどうかを聞かずに上書きします。
//       ★★作る前に、★在るかどうかを見ること。
//
//   ★短いリンクにするかどうかは、★運営者のご判断です。
//     ★仕様は「woolsong.app の直下」と言っています。
//     ★いまの `/` は、9言語の紹介のページです。★消していません。
//     ★決まったら、次のどちらかにします。
//       ・`/` をこの画面に差し替える（★いまの紹介は別のURLへ）
//       ・`woolsong.app/hajimeru` のまま、★QRをこちらに向ける
//     ★どちらでも、この画面は変わりません。
//
//   ★ここは、判定を持ちません。★StartFlow が持ちます。
//     ★判定は navigator を読むので、★サーバでは決められません。
// ============================================================================

export const metadata = {
  title: "はじめる｜Woolsong"
};

export default function HajimeruPage() {
  return <StartFlow />;
}
