-- ============================================================================
-- ★試しの 台帳 だけ ── ★「子どもの枠」の 見比べ 用の 種（★2026-09-26）
--
--   ★★見本 `SC['子どもの枠']` は 1人 持って います ──
--     ★呼び名「はるちゃん」／ 配役「合唱（子ども）」／ 緊急の 連絡先 あり
--     ★見た 記録 1件（★舞台監督）。
--   ★★★わけは 台帳が 3つ だけ 通します（`todays_call` ／ `emergency` ／ `guardian_request`）。
--     ★はじめ `rehearsal` と 書いて 23514 で 落ちました（★2026-09-26）。
--   ★★★台帳に 配役の 列は ありません（★`koen_kids` は 呼び名 だけ）。
--     ★見本の「合唱（子ども）」は 配役 です ── ★別の 表（`koen_cast`）の 話 です。
--     ★★だから ここでは 呼び名 と 見た 記録 だけ を 入れます。
--
--   ★★★年齢・学校・写真・体調の 列は 1つも ありません。★入れる ものも ありません。
--   ★★本番に 入れません。★`la-voce-test2` だけ です。
-- ============================================================================

insert into public.koen_kids (id, koen_id, guardian_user_id, nickname)
values ('d6000000-0000-4000-8000-000000000001',
        'c3000000-0000-4000-8000-000000000001',
        'e45e50eb-ed09-402f-9dc7-73bda4852380',
        'はるちゃん')
on conflict (id) do nothing;

-- ★★見た 記録（★消せない 記録 です）。★見た方の お名前は 入れません。
insert into public.koen_kid_contact_reads
  (id, kid_id, koen_id, viewer_user_id, viewer_role_at, reason_kind, viewed_at,
   kid_name_at, koen_title_at)
values ('d7000000-0000-4000-8000-000000000001',
        'd6000000-0000-4000-8000-000000000001',
        'c3000000-0000-4000-8000-000000000001',
        'e45e50eb-ed09-402f-9dc7-73bda4852380',
        '舞台監督', 'todays_call', '2026-09-20 05:00:00+00',
        'はるちゃん', 'フィガロの結婚')
on conflict (id) do nothing;

-- ★消す とき
--   delete from public.koen_kid_contact_reads where id = 'd7000000-0000-4000-8000-000000000001';
--   delete from public.koen_kids              where id = 'd6000000-0000-4000-8000-000000000001';

-- ============================================================================
-- ★★★入って いる 公演を 1つ に します（★2026-09-26・★試しの 台帳 だけ）
--
--   ★★「子どもの 枠」の 行は、★どの 公演か が 決まって いる ときだけ 出ます。
--     ★★掛け持ちの 方に 1つを 勝手に 選ばせません ──
--       ★お子さんを 別の 公演に 出して しまいます。
--   ★★試しの 口座は 3つの 公演に 入って いました（★`test_seed.py` の 2つ ＋ こちらの 1つ）。
--     ★★見本は 1つの 公演の 世界 です。★だから 古い 2つを「抜けた」に します。
--   ★★★これは 実際の 人が する こと と 同じ です（★`left_at` を 入れる）。
--     ★行は 消しません。★運営の 記録は 残ります。
-- ============================================================================

update public.koen_members
   set left_at = '2026-09-25 00:00:00+00'
 where user_id = 'e45e50eb-ed09-402f-9dc7-73bda4852380'
   and koen_id <> 'c3000000-0000-4000-8000-000000000001'
   and left_at is null;

-- ★戻す とき
--   update public.koen_members set left_at = null
--    where user_id = 'e45e50eb-ed09-402f-9dc7-73bda4852380'
--      and left_at = '2026-09-25 00:00:00+00';

-- ★★緊急の 連絡先（★見本は「あり」）。★画面に 番号は 出ません。
insert into public.koen_kid_contacts (kid_id, contact)
values ('d6000000-0000-4000-8000-000000000001', '090-0000-0000')
on conflict (kid_id) do nothing;
