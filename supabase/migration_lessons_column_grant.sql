-- ============================================================================
-- ★レッスンの 新しい 列を、★読めるように します（★2026-09-20）
--
--   ★★★`lessons` は **列ごと** に 渡して います。
--     ★★表ぜんぶ の `select` は 取り上げて あり、★名ざしの 列 だけ です。
--     ★★★2026-09-20（★D93）に `place_id` と `kind` を 足した とき、
--       ★★渡し（grant）を 足して いません でした。
--     ★★★その ため、★`select("*")` が **要求ごと** 落ちて いました。
--       ★★0行では ありません。★`data` が null に なります。
--       ★★日程の 表は「この 日に コマは ありません」と 出て いました。
--       ★★ご本人の 画面の「次の レッスン」も 同じ です。
--
--   ★★足すのは 2列 だけ です ── ★`place_id` と `kind`。
--     ★★`created_by` は 渡しません。★画面で 使いません
--       （★呼ぶ 側も 名前で 並べる ように 直しました）。
--
--   ★★★取り上げは しません。★いま 渡して いる 列は そのまま です。
--     ★★（★表ぜんぶ の `select` は もとから 取り上げて あります。
--       ★★だから 列を 足しても、★広い 渡しが 勝つ ことは ありません。）
--
--   ★何度 流しても 同じに なります。
-- ============================================================================

grant select (place_id, kind) on table public.lessons to authenticated;

-- ---------------------------------------------------------------------------
-- ★確かめ（★読むだけ）
-- ---------------------------------------------------------------------------
--   select c.column_name,
--          has_column_privilege('authenticated','public.lessons',c.column_name,'SELECT')
--   from information_schema.columns c
--   where c.table_schema='public' and c.table_name='lessons'
--   order by c.ordinal_position;
