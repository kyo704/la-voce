# 本番の姿の写し — 2026-09-03

★`supabase/AUDIT_full_schema_vs_repo.sql` の出力を、そのまま貼ります。
★編集しません。届いた順に埋めていきます。

**取得日**：2026-09-03
**取得した人**：運営者（SQL エディタ）
**時刻帯**：★出力の時刻は UTC です（`current_setting('TimeZone')` = UTC。同日確認）

---

## ② 関数（★4件を受領・写し）

★本番は13件。うち★**4件の中身を受領しました**（残り9件は名前のみ、または未受領）。

### are_connected ★リポジトリに定義なし

```sql
CREATE OR REPLACE FUNCTION public.are_connected(viewer_id uuid, other_id uuid)
 RETURNS boolean
 LANGUAGE sql
 STABLE SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
  SELECT
    EXISTS (
      SELECT 1 FROM teacher_student_links
      WHERE status = 'active' AND (
        (teacher_id = viewer_id AND student_id = other_id) OR
        (teacher_id = other_id AND student_id = viewer_id)
      )
    )
    OR
    EXISTS (
      SELECT 1 FROM assignments
      WHERE ended_at IS NULL AND (
        (teacher_id = viewer_id AND student_id = other_id) OR
        (teacher_id = other_id AND student_id = viewer_id)
      )
    )
$function$
```

★**確かめたこと**
- 参照する表は `teacher_student_links` と `assignments` の**2つだけ**。
  ★**16組で全部**という数え方は、これで裏づけられました。
- `status = 'active'` と `ended_at IS NULL` で、★**いま続いている関係だけ**を見ます。
  解除された関係では、もう真になりません。
- ★**両向きです。** `(teacher = viewer AND student = other)` と
  `(teacher = other AND student = viewer)` の**両方**を見ています。
  ★つまり**生徒の側からも、先生の profiles の行が読めていました。**
  ★私は「先生が生徒を見られた」とだけ書いていましたが、**片側だけの話ではありません。**
  ★組の数は変わりませんが、**誰が誰を見られたか**の記述は、両向きに直す必要があります。

### can_view_ops ★リポジトリに定義なし（.md に写しあり）

```sql
CREATE OR REPLACE FUNCTION public.can_view_ops(viewer_id uuid, p_org_id uuid, p_student_id uuid)
 RETURNS boolean
 LANGUAGE sql
 STABLE SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
  SELECT
    EXISTS (SELECT 1 FROM enrollments WHERE org_id = p_org_id AND student_id = p_student_id AND status = 'active')
    AND (
      is_org_owner_or_admin(viewer_id, p_org_id)
      OR EXISTS (SELECT 1 FROM assignments WHERE org_id = p_org_id AND teacher_id = viewer_id AND student_id = p_student_id AND ended_at IS NULL)
    )
$function$
```

★`supabase/security-snapshot-2026-09-01.md:77-89` の写しと、★**論理が一字一句同じ**でした
（差はドル引用符の書き方だけ）。2026-09-01 から変わっていません。
★`is_org_owner_or_admin` を呼んでいますが、★**この関数はリポジトリにありません**
（リポジトリにあるのは `is_org_owner` という**別名**です）。

### can_view_organization ★リポジトリに定義なし

```sql
CREATE OR REPLACE FUNCTION public.can_view_organization(viewer_id uuid, p_org_id uuid)
 RETURNS boolean
 LANGUAGE sql
 STABLE SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
  SELECT
    EXISTS (SELECT 1 FROM memberships WHERE org_id = p_org_id AND user_id = viewer_id)
    OR EXISTS (SELECT 1 FROM enrollments WHERE org_id = p_org_id AND student_id = viewer_id AND status = 'active')
    -- 招待コードを確認中の人（まだメンバーでも生徒でもない）にも、教室名だけは見せる。
    -- コード自体を知っていることが前提の、限定的な開示。
    OR EXISTS (SELECT 1 FROM org_invitations WHERE org_id = p_org_id AND used_at IS NULL AND expires_at > now())
$function$
```

★★★ **3つ目の EXISTS に、`viewer_id` が1度も出てきません** ★★★

コメントは「**コード自体を知っていることが前提**の、限定的な開示」と書いていますが、
★**SQL は、その人がコードを知っているかを一切見ていません。**
引数にコードがありません。見ているのは「その教室に、使われていない・
期限内の招待が**1件でもあるか**」だけです。

★**つまり、ログインしている人なら誰でも、招待が出ている教室の行を読めます。**

★**今日の根本原因と、まったく同じ形です。**
`profiles_connected_display_name` は**名前**が実態と違いました。
こちらは**コメント**が実態と違います。★どちらも、読む人を誤らせます。

★**いまの実害は小さい**（`organizations` の列は5つだけ：
`id` / `name` / `kind` / `created_by` / `created_at`。機微な情報はありません）。
★**ただし、欠陥そのものは残ります。**
`created_by` は人と教室を結ぶ id です。そして★**列が1つ足された瞬間に、
天井が上がります。** 有料の教室プランで契約情報などが加わる可能性を考えると、
★**いま直しておく理由があります。**

### role_rank ★リポジトリに同名の定義なし

```sql
CREATE OR REPLACE FUNCTION public.role_rank(r text)
 RETURNS integer
 LANGUAGE sql
 IMMUTABLE
AS $function$
  select case r
    when 'owner' then 3
    when 'admin' then 2
    when 'teacher' then 1
    else 0
  end;
$function$
```

★`SECURITY DEFINER` ではなく `IMMUTABLE` の純粋な対応表です。**それ自体に危険はありません。**
★リポジトリにあるのは `org_role_rank(p_role text)` と `my_org_role_rank(p_org_id uuid)`
（`migration_role_rank_no_self_promotion.sql:45,63`）で、★**名前が違います。**
★**同じ判断が2か所にある形**です。どちらが効いているかを確かめる必要があります。

---

## ① ポリシー

```
（未取得）
```

---

## ③ トリガー（public と auth）

```
（未取得）
```

---

## ④ 表への権限と、RLS の有無

```
（未取得）
```

---

## ⑤ 拡張機能

```
（未取得）
```

---

## ⑥ 表と列

```
（未取得）
```

---

## ⑦ 外部キー

```
（未取得）
```

---

## 突き合わせの結果（★関数について・2026-09-03 時点）

★本番の関数は**12件**（運営者が `count(*)` で確定。当初「13件」とあったのは
照会の見出しの書き間違いで、データの欠落ではありません）。

### 本番にあって、リポジトリに無いもの

★**「無い」にも段階があります。混ぜません。**

| 何 | 段階 | どこに何があるか | 何をするか |
|---|---|---|---|
| `are_connected` | ★**どこにもなし** | — | つながっているかの判定。★今日の漏れの中心 |
| `can_view_organization` | ★**コメント1行のみ** | `migration_org_insert_policies.sql:61` | 教室の行を読めるか |
| `is_org_owner_or_admin` | ★**呼ばれているが定義なし** | `URGENT_fix_owner_self_promotion.sql:78` が**呼んでいます** | オーナーか責任者か |
| `role_rank` | ★**どこにもなし** | — | 役職を数に直す |
| `can_view_ops` | ★**監査の記録にのみ** | `security-snapshot-2026-09-01.md:77-89` | 教室のレッスンを見られるか |
| `is_org_member` | ★**作業指示書にのみ** | `docs/lavoce-作業指示-公開前の実装.md:42` | 教室のメンバーか |

★**`can_view_ops` と `is_org_member` は、中間の段階です。**
どこかに文字は残っていますが、★**移行ファイルではありません。**
- `can_view_ops` … **本番から写した監査の記録**。実物である見込みが高い
  （★実際、今日の本番の定義と**論理が一字一句同じ**でした）
- `is_org_member` … **これから作る指示書**。★書いたとおりに作られたかは分かりません

### リポジトリにあって、本番に当たっていないもの

★**こちらも同じくらい危ないです。直したつもりのものが、効いていません。**

| 何 | どこ | 何をするはずだったか | ★重さ |
|---|---|---|---|
| `org_role_rank` | `migration_role_rank_no_self_promotion.sql:45` | 役職を数に直す | ★**移行が流されていません** |
| `my_org_role_rank` | 同 `:63` | 自分の役職の数を返す | ★同上 |
| `is_org_owner` | `URGENT_fix_owner_self_promotion.sql:63` | オーナーか | ★定義したが**呼ばれてもいません** |

★**訂正（2026-09-03）**：`migration_teacher_link_reconnect.sql` を
「未適用」としてこの表に数えかけましたが、★**誤りでした。適用されています。**
本番に `teacher_student_links_active_pair_idx`（部分一意索引）が実在し、
古い全体の一意制約は消えています。★**リポジトリと本番が一致している例です。**
★9/1 のダンプ（適用前の姿）だけを見て判断したのが原因です。
★**ダンプは「その時点の姿」であって、「いまの姿」ではありません。**

★★★ **`migration_role_rank_no_self_promotion.sql` が流されていない件** ★★★

★ファイル名は `no_self_promotion` — ★**自分で自分の役職を上げられないようにする規則**です。
★**本番に、その2つの関数がありません。** つまり**流されていない見込みが高い。**

★**そして検査 `components/tests/role-rank.test.js` は通っています。**
あの検査は★**ファイルの中身**を見張るもので、★**本番に当たっているかは見ません。**
★**「検査が通っている」と「本番で効いている」が、切り離されています。**
★今日くり返し出た形の、いちばん分かりやすい例です。

★**確かめること**：`memberships` のポリシーが、いま何を呼んでいるか。
`get_student_entries` が12件に無いのは★**9/1 に消したとおり**で、食い違いではありません。
