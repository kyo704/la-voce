-- ============================================================================
-- ★学年 ── ★数で 持ち、★言い方は 学校が 決める（★裁定 その98 追補・2026-09-19）
--
--   ★★★学年は「組織の 形」では ありません。★時の 位置 です。
--     ★★学部・学科は、★その方が 属する ところ。★変わりません。
--     ★★学年は 毎年 変わります。
--     ★★★一緒に すると ── ★毎年 4月に、★全員の `division_id` を 書き換える ことに
--       ★★なります。★学部も 学科も 変わって いないのに、です。
--       ★★進級の 跡も 消えます（★「2年の ときの 記録」が 引けません）。
--
--   ★★★いまの `grade_label` は 自由に 書ける 字 です。★ゆれます。
--     ★★「2年」「2年生」「II」「2nd」── ★どれも 同じ ものです。
--     ★★★数（`grade_year`）で 持てば、★並べ替えと 絞り込みが できます。
--     ★★言い方（`grade_label`）は 残します ── ★欧州の 音楽院は 数え方が ちがいます。
--
--   ★★★進級（★毎年 4月に 誰が 上げるか）は、★まだ 決まって いません。
--     ★★10月に 要りません。★台帳 08-18 に 記しました。★ここでは 作りません。
--
--   ★何度 流しても 同じに なります。
-- ============================================================================

-- ★★`grade_label` は すでに あります（★2026-09-19 に 台帳で 確かめました）。
--   ★★だから 足すのは `grade_year` だけ です。
alter table public.enrollments add column if not exists grade_year int;

comment on column public.enrollments.grade_year is
  '学年を 数で。1 / 2 / 3 …。言い方は grade_label。裁定その98追補・2026-09-19。';
comment on column public.enrollments.grade_label is
  '学年の 言い方（学校が 決める 字）。数は grade_year。';

-- ---------------------------------------------------------------------------
-- ★確かめ
-- ---------------------------------------------------------------------------
--   select column_name, data_type from information_schema.columns
--   where table_schema='public' and table_name='enrollments'
--     and column_name in ('grade_year','grade_label');
--
--   ★2つ とも ある はず です。★`grade_year` は integer です。
--   ★★いまの 行は すべて null です。★移し替えは して いません。
--     ★★「書いて いない」と「1年」を、★見分けられる ように する ため です。
