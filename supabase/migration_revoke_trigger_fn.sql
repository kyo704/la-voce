-- ============================================================================
-- La Voce / Woolsong ── ★引き金の 関数を、★画面から 呼べなく します（★2026-09-21）
--
-- ★Supabase の SQL Editor で 実行してください（★何度 実行しても 安全です）。
--
-- ★★★見つけた こと
--   ★★`matching_report_makes_cut` に `execute` が 渡って いました。
--   ★★PostgreSQL は、★関数を 作ると 既定で みんなに `execute` を 渡します。
--   ★★★ほかの 6本は 取り上げて いました。★これだけ 抜けて いました。
--
-- ★★いま 呼ばれても 何も 起きません（★引き金の 外では `new` が ありません）。
--   ★★けれど `security definer` です。★渡した ままに しません。
--   ★★引き金から 呼ぶ ぶんには、★`execute` は 要りません。
-- ============================================================================

revoke all on function public.matching_report_makes_cut()
  from public, anon, authenticated;
