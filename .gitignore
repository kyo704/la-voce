# 依存関係とビルド成果物（リポジトリに入れない）
node_modules/
.next/
out/

# 環境変数（秘密鍵が入るため、絶対にコミットしないこと）
.env
.env.local
.env*.local

# macOS
.DS_Store

# バックアップのダンプ（A-P0-1）
# ★要配慮個人情報と、ログインのパスワードのハッシュが入ります。
#   絶対にコミットしないこと。使い終わったら消すこと。
backups/
docs/inbox/*.txt

# ★見た目の 確認に 使う テスト用の 鍵。★決して git に 入れないこと。
.env.e2e
artifacts/

# Supabase CLI のローカル一時ファイル（接続先・プロジェクト情報を含むためコミットしない）
supabase/.temp/
