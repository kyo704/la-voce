# まだ使っていない荷物（★開かないこと）

ここの zip は、**まだ使いません。開かないでください。**

## なぜ開かないのか

いま動いているのは、次の2つです。

- 着せかえ **217点** … `public/sheep/items/` ＋ `docs/assets/sheep-items-index.json`
- 内装 **19点**（壁10・床9） … `public/sheep/tiles/` ＋ `docs/assets/sheep-tiles-index.json`

ここの zip を今ひらいて `public/sheep/` に混ぜると、
**どちらが本物か分からなくなります。**
同じ名前の絵が両方に在ることもあり、片方だけ直す日が来ます。

zip のまま置いてあるのは、**「まだ使っていない」を目に見える形にするため**です。

## 中身と仕様

**中身の説明も、仕様も、zip の中の README.md がすべてです。**
それが正本です。ここには書き写しません。書き写すと、2か所になります。

## いつ開くのか

坂本さんの決めた順番（2026年9月6日）です。

1. 着せかえ217点 ＋ 歩く脚 … **実機で確かめる（いまここ）**
2. 本番モードの、最小の実装
3. 規約 第7条の差し替え
4. — ここまでで、公開できます —
5. **166点への入れ替え（＝ここの zip を開く）**

**4 を過ぎるまで、開かないでください。**

## ただし、数字だけは、もう使っています

166点の版（v4）の仕様書にある、足まわりの座標です。
**新しい靴12点は、この座標に合わせて描かれています。**
ここを合わせずに進むと、あとで12点ぜんぶ描き直しになります。

```
足の中心x   左 421 ／ 右 601
足の下端    y 1002   ← 靴の底が、ここに来ます
足の幅      114
接地線      y 1006
```

この4つは `lib/sheepWardrobe.js` の `LEGS` に入れてあります。
**絵はまだ使っていませんが、数字は先に合わせてあります。**

## 使わないもの

足の絵4点（`foot_L` / `foot_R` / `foot_shadow_L` / `foot_shadow_R`）は
**使いません。**脚はコードで描いています（案B1）。絵は0枚です。

## 置いてあるもの

| 場所 | 中身 |
|---|---|
| `assets/wardrobe-v2/` | 着せかえ 166点（v4・帽子と靴を直し） |
| `assets/interior-v2/` | おうちの内装 120点（第2〜5便） |

v3 の zip は持ってきていません。v4 に置きかわっているためです。
