# La Voce ／ 国際展開ロードマップと i18n 実装仕様

日本発の声のプロ向け体調管理アプリを、世界規模で展開するための仕様。
実装は Claude Sonnet 5 を想定。

**この文書の優先順位**

1. **§2 いま入れるべき4つの内部表現** ← あとからやると全画面をやり直すことになる。最優先
2. **§5 規制対応：3法域を同時に満たす設計** ← 機能ではなく文言と使用目的の話。全画面に適用
3. §3 音名表記 ／ §4 単位 ← 独立して進められる
4. §6 以降 データ保護・課金・ローカライズ ← 課金地域を広げる前に

---

## 1. 結論と段階

**技術的には確実に可能で、市場としては日本より有望です。** 障壁は技術ではなく、
規制・ローカライズ・サポートの人的コストの3つ。

重要な区別として、**「英語版を出す」ことと「その国で事業展開する」ことは別**です。
前者は法務なしで今日からできます。**課金地域を日本に限定したまま、UIだけ世界に開く**
のが最も現実的な入口です。

| Phase | 内容 | 課金地域 | 法務の重さ |
|---|---|---|---|
| 0 | §2 の内部表現を国際標準に直す | 日本 | なし |
| 1 | 日本で製品を完成させる（記録項目v2＋指導者プラン） | 日本 | 国内のみ |
| 2 | 英語UI＋音名/単位の切り替え。**無料で世界公開** | **日本のみ** | 軽い（規約の英訳、同意の英語化） |
| 3 | 米国＋英語圏（UK / AU / CA）で課金開始 | ＋US/UK/AU/CA | 中（州法対応、税） |
| 4 | EU | ＋EU | 重（GDPR＋MDR） |

**EU を最後に回すのは、個人〜小規模開発では正解です。** GDPR の明示的同意、
EU域内代理人の設置、MDR の使用目的の宣言——手間の割に、市場規模は米国のほうが大きい。

**日本発であることの隠れた優位**：日本はEUから十分性認定を受けており、
2023年の第一次レビューでも維持されました。つまり **EUユーザーのデータを日本のサーバーに
置ける**（標準契約条項が不要）。Phase 4 に進むとき、この差は大きく効きます。

---

## 2. いま入れるべき4つの内部表現（最優先）

**Phase 0。これだけは、他の何より先にやってください。**
あとから直すと全ファイルを触ることになります。逆に、いまなら2週間弱で終わります。

### 2.1 音名は MIDI ノート番号で保持する

```ts
// ✗ こうしない
interface VoiceEntry { pitchChest?: string }      // 'A3' のような文字列

// ✓ こうする
interface VoiceEntry { pitchChestMidi?: number }  // 57
```

表示は必ず変換関数を通す。**文字列で保持すると、ドイツ式・ラテン式に切り替えた瞬間に
過去データが読めなくなります。**

### 2.2 単位は内部 SI 固定、表示層で変換

```ts
// 内部は常に kg / cm / ℃ / ml / g·m⁻³
interface DayRecord {
  weightKg?: number
  // ...
}

// 表示は必ず format 関数を通す
formatWeight(weightKg, profile.unitSystem)   // '58.2 kg' | '128.3 lb'
```

**ユーザーが入力した単位で保存しない。** 入力時に SI へ変換して保存する。

### 2.3 日時は UTC ＋ タイムゾーンで保持

```ts
interface VoiceEntry {
  atUtc: string        // ISO 8601, UTC
  tz: string           // IANA名 'Asia/Tokyo' / 'Europe/Vienna'
  localDate: string    // 'YYYY-MM-DD' 記録上の「その日」。集計はこれを使う
}
```

`localDate` を別に持つのが要点です。海外公演で時差をまたぐと、
UTC だけでは「その日の記録」が2日にまたがります。**集計は必ず `localDate` で行う。**

タイムゾーンの記録は、のちの「移動・時差ぼけと声」の分析にもそのまま使えます。

### 2.4 文字列は全てキー化する

日本語をコンポーネントに直書きしない。`next-intl` か `react-i18next` を導入し、
**Phase 0 の時点で日本語だけの辞書を作る**（翻訳はまだしない）。

```
messages/
  ja.json
  en.json     ← Phase 2 で埋める
```

分析結果の文章は変数を含むので、**ICU MessageFormat** を使う。

```json
{
  "insight.sleepLag": "あなたの声は、睡眠不足の{lag}日後にいちばん落ちます（ρ = {rho}）",
  "insight.streak": "{symptom}が{days, plural, other {#日}}続いています"
}
```

---

## 3. 音名表記システム

**これは翻訳ファイルでは解決しません。国によって体系そのものが違います。**

### 3.1 2つの軸を独立に持つ

音名の綴りと、オクターブの数え方は**別の設定**にします。実際に混在します
（ドイツ語圏はドイツ式音名＋ヘルムホルツ番号が伝統ですが、国際式番号も併用されます）。

```ts
type NoteNameSystem = 'english' | 'german' | 'latin'
type OctaveSystem   = 'scientific' | 'yamaha' | 'helmholtz'

interface NotationPref {
  noteName: NoteNameSystem
  octave: OctaveSystem
  accidental: 'sharp' | 'flat'   // 表示の優先。内部は常にシャープで保持
}
```

### 3.2 音名の対応表

| 半音 | English | German (♯系) | German (♭系) | Latin（伊/仏/西） |
|---|---|---|---|---|
| 0 | C | C | C | Do |
| 1 | C♯ | Cis | Des | Do♯ |
| 2 | D | D | D | Re |
| 3 | D♯ | Dis | Es | Re♯ |
| 4 | E | E | E | Mi |
| 5 | F | F | F | Fa |
| 6 | F♯ | Fis | Ges | Fa♯ |
| 7 | G | G | G | Sol |
| 8 | G♯ | Gis | As | Sol♯ |
| 9 | A | A | A | La |
| 10 | A♯ | Ais | **B** | La♯ |
| 11 | B | **H** | H | Si |

**ドイツ式で最も注意すべき点：英語の B が H、英語の B♭ が B です。**
ここを間違えると、ドイツ語圏の声楽家にとって全データが半音ずれて見えます。

フラット表記も不規則です：**Es / As / B**（Ees, Aes, Hes ではない）。

### 3.3 オクターブ番号の対応表

中央ハ（MIDI 60）を基準に。**実装後、この表をそのままユニットテストにしてください。**

| MIDI | 国際式 | ヤマハ式 | ヘルムホルツ※ | ドイツ式（国際番号） | ラテン式 |
|---|---|---|---|---|---|
| 36 | C2 | C1 | C | C2 | Do2 |
| 48 | C3 | C2 | c | C3 | Do3 |
| 53 | F3 | F2 | f | F3 | Fa3 |
| 57 | A3 | A2 | a | A3 | La3 |
| 58 | A♯3 | A♯2 | ais | Ais3 | La♯3 |
| 59 | B3 | B2 | h | H3 | Si3 |
| **60** | **C4** | **C3** | **c′** | **C4** | **Do4** |
| 61 | C♯4 | C♯3 | cis′ | Cis4 | Do♯4 |
| 69 | A4 | A3 | a′ | A4 | La4 |
| 72 | C5 | C4 | c″ | C5 | Do5 |
| 79 | G5 | G4 | g″ | G5 | Sol5 |
| 84 | C6 | C5 | c‴ | C6 | Do6 |

※ ヘルムホルツ列は伝統的な組み合わせであるドイツ式音名で示しています。
英語式音名＋ヘルムホルツ番号を選んだ場合は `a♯′` のようになります。
**音名と番号は独立に選べる設計にすること。**

変換ロジック：

```
scientificOctave(midi) = floor(midi / 12) − 1
yamahaOctave(midi)     = floor(midi / 12) − 2

helmholtz(midi):
  o = scientificOctave(midi)
  o >= 3  →  小文字 + "′" × (o − 3)      // c′ = C4（中央ハ）
  o == 2  →  大文字                       // C  = C2
  o <  2  →  "," × (2 − o) + 大文字       // ,C = C1
```

### 3.4 既定値

| 地域 | noteName | octave |
|---|---|---|
| 日本 | english | scientific（ヤマハ式も選択可。現行アプリの注記どおり混乱の元） |
| 英語圏 | english | scientific |
| ドイツ語圏（DE/AT/CH） | german | scientific（helmholtz も選択可） |
| 伊/仏/西/葡/中南米 | latin | scientific |

**設定は言語から自動推定し、必ず手で変えられるようにする。**
ドイツ在住の英語話者、日本人の欧州留学生など、言語と表記の好みは一致しません。

### 3.5 声種（Fach）

日本の7分類（ソプラノ／メゾ／アルト／カウンターテナー／テノール／バリトン／バス）は、
ドイツ語圏の Fach 体系より粗いです。地域で選択肢を差し替えられる構造にします。

```ts
interface VoiceTypeOption {
  key: string            // 国際共通の内部キー
  labelKey: string       // 表示ラベルは辞書から
  rangeLowMidi: number
  rangeHighMidi: number
}
const VOICE_TYPES: Record<Region, VoiceTypeOption[]> = { /* ... */ }
```

ドイツ語圏向けには、少なくとも lyrischer / dramatischer / Koloratur の区別を足せるように。
**内部キーは国際共通にし、表示ラベルと音域の目安だけを地域で差し替える。**

---

## 4. 単位系

```ts
type UnitSystem = 'metric' | 'imperial'
```

| 量 | 内部（固定） | metric 表示 | imperial 表示 | 換算 |
|---|---|---|---|---|
| 体重 | kg | kg | lb | 1 kg = 2.20462 lb |
| 身長 | cm | cm | ft ′ in ″ | 1 in = 2.54 cm |
| 気温 | ℃ | ℃ | ℉ | ℉ = ℃ × 9/5 + 32 |
| 水分 | ml | ml | fl oz (US) | 1 US fl oz = 29.5735 ml |
| 絶対湿度 | g/m³ | g/m³ | g/m³ | 換算しない（科学単位のまま） |
| 音程差 | 半音 | 半音 | semitones | 換算しない |

既定は言語・地域から推定（US → imperial、他は metric）。手で変更可能に。

**食品データベースについて**：日本食品標準成分表ベースのデータは海外で使えません。
記録項目 v2 で**食事の詳細PFC記録を詳細モード限定に降格**した判断は、
そのまま国際化コストの削減になります。簡易モード（タンパク質 g/kg ＋ 概算kcal）なら
国が変わっても動きます。**Phase 2 では詳細モードを英語圏で無効にしてよい。**

---

## 5. 規制対応：3法域を同時に満たす設計

### 5.1 各法域の線引き

| | 何が規制対象になるか | 実務上の急所 |
|---|---|---|
| **日本** | 疾病の診断／病態の評価／**疾病の兆候の検知**／リスク表示 | 記録・統計処理・表示・共有は非該当 |
| **米国 FDA** | 一般的健康管理（general wellness）の枠を外れると医療機器 | **臨床閾値への言及／疾患名の参照／臨床行動を促すアラート／「医療グレード」「臨床的に正確」の表現**で枠を外れる |
| **EU MDR** | 診断・予防・監視・**予測**・予後・治療 を使用目的とするなら医療機器 | 「スコアを計算する／異常にフラグを立てる／パターンを解釈する」は判定の関門を通過してしまう。**救いは「使用目的が wellness/lifestyle であること」**。宣言文がすべてを決める |

### 5.2 全法域を同時に満たす、たった1つの原則

> ## 自分比だけで語る。文献の閾値をアプリから出さない。

これで日本・米国・EU を同時に満たせます。

| ✗ 出してはいけない | ✓ 代わりにこう出す |
|---|---|
| 「CPPS が 14.45 dB を下回っています（嗄声の目安）」 | 「CPPS があなたの平常値より 2.1 dB 低い日が3日続いています」 |
| 「s/z 比が 1.4 を超えています。声帯の器質的問題を疑います」 | 「あなたの s/z 比は平常 0.98、今日は 1.35 です」 |
| 「声帯のむくみが疑われます」 | 「弱声の最高音が、あなたの平常値より3半音低い日が4日続いています」 |
| 「逆流性食道炎の可能性があります」 | 「夕食から就寝まで2時間未満の日が今週5日ありました」 |
| 「低栄養の疑いがあります」 | 「摂取エネルギーが3週間、推定の必要量を下回っています」 |

**文献の閾値は「健康情報」タブの解説記事に置き、個人の記録には紐づけない。**
記事は教育コンテンツであり、規制対象になりません。

すでに設計した**「コンディション偏差値（自分の過去分布に対する位置）」が、
まさにこの原則の実装**です。中核指標は最初から国際展開に向いています。

### 5.3 コードレビューの観点（分析画面を触るPRで毎回渡す）

- [ ] 疾患名・病態名を分析結果に出していないか（dysphonia, edema, reflux, nodule …）
- [ ] 身体状態に対して「疑い」「可能性」「リスク」を使っていないか
- [ ] 文献由来の閾値を数値で出していないか
- [ ] 臨床行動を促すアラート／アラームになっていないか
- [ ] 「医療グレード」「臨床的に正確」「診断」と書いていないか
- [ ] 因果の言い方が「◯◯した日の翌日は、平均して〜と記録されています」の形か
- [ ] 件数（n）が添えられているか

### 5.4 EU向けの使用目的の宣言（Phase 4 用のドラフト）

EU では**製造者が宣言する使用目的**が判定を決めます。
アプリ内・サイト・ストア説明のすべてで、次の趣旨を一貫させます。

> La Voce is a general wellbeing and performance-tracking tool for professional voice users.
> It records self-reported and self-measured information about voice use, sleep, hydration,
> nutrition, environment and workload, and presents it as personal trends relative to the
> user's own history. **It is not intended to diagnose, prevent, monitor, predict, prognose,
> treat or alleviate any disease or medical condition**, and it must not be used as a
> substitute for professional medical advice.

**「予報（forecast）」機能の名称に注意。** EU では "prediction" が医療目的に明記されています。
機能名を *prediction / risk* ではなく **"tomorrow's outlook" / "readiness"** のような
パフォーマンス寄りの語にし、対象を「声のコンディション（自己申告）」に限定してください。
疾患の予測ではないことが、名称からも読めるようにします。

### 5.5 地域別の機能フラグ

```ts
type Region = 'JP' | 'US' | 'US_WA' | 'UK' | 'EU' | 'CA' | 'AU' | 'OTHER'

interface RegionPolicy {
  billingEnabled: boolean
  /** 既往症を登録した人向けの、疾患に紐づく分析（逆流など） */
  conditionLinkedAnalysis: boolean
  consentModel: 'jp' | 'gdpr' | 'us_state'
  /** 詳細な食品DB（日本の成分表ベース） */
  detailedFoodDb: boolean
}
```

**`conditionLinkedAnalysis` は既定 false、日本のみ true から始める。**
疾患に紐づいた個人向け分析は、米国では一般的健康管理の枠を外れ、
EU では使用目的が医療寄りに読まれるおそれがあります。
英語圏で出す場合は、**疾患名を使わず「夕食のタイミングと翌朝の喉の記録」**という
中立な枠に作り替える必要があります（分析の中身は同じ、名前と文脈だけが違う）。

---

## 6. データ保護と同意

### 6.1 方針：最も厳しい法域に合わせて1本化する

地域ごとに同意フローを分岐させると保守できません。
**GDPR の明示的同意＋ワシントン州法の分離同意**を満たす形を、全地域に適用します。

### 6.2 同意の粒度

```ts
interface Consents {
  /** 健康データの収集・処理（必須。これがないとアプリを使えない） */
  collect: { at: string; version: string }
  /** 指導者・第三者との共有（任意。共有相手ごとに取る） */
  share?: { at: string; version: string; scope: FieldGroup[] }
  /** 匿名化統計への利用（任意。いつでも撤回可） */
  statsUse?: { at: string; version: string }
}
```

**販売（sale）は行わないことを明記する。** ワシントン州法では健康データの販売に
署名入りの authorization が必要になるため、**「販売しない」と宣言して機能自体を持たない**
のが最も安い解です。日本でも要配慮個人情報のオプトアウト第三者提供は認められていません。

同意画面の要件：

- 取得する項目のカテゴリを具体的に列挙する（「健康情報」ではなく「睡眠時間、症状、体重…」）
- 目的を具体的に書く（「サービス向上のため」は不可）
- **同意しないと使えない部分と、使える部分を分ける**
- 撤回の方法を同じ画面に書く
- 同意日時とポリシーのバージョンを保存する

### 6.3 データ主体の権利（全地域で実装する）

| 権利 | 実装 |
|---|---|
| アクセス・ポータビリティ | JSON ＋ CSV の一括書き出し（機械可読、UTF-8） |
| 削除 | アカウントと全データの実削除。30日の猶予つき、猶予中は復元可 |
| 訂正 | 過去の記録の編集（すでにある） |
| 同意の撤回 | 統計利用は即時停止。収集の撤回はアカウント削除と同義 |
| 処理の制限 | 分析の一時停止（記録だけ続ける）モード |

**アカウント削除は、課金を始める前に必ず実装してください。** あとから足すのは難しく、
これがないまま海外ユーザーを受け入れるのはリスクです。

### 6.4 リージョン判定

- IP による推定は**参考値のみ**。ユーザーが自己申告で選べるようにする
- 判定結果を `Profile.region` に保存し、途中で変えられるようにする（留学・移籍がある業界）
- **迷ったら厳しいほうに倒す**（`OTHER` は GDPR 相当の扱いにする）

---

## 7. 課金の地域制御

```ts
// Stripe 側の方針
// - Checkout セッション作成時に Profile.region を確認し、billingEnabled でなければ拒否
// - 価格は通貨ごとに Price オブジェクトを分ける（実行時の為替換算はしない）
// - Stripe Tax を有効化（EU VAT は購入者所在地、US sales tax は州ごと）
```

**購買力に応じた価格差（PPP）を検討する。** 日本 ¥580 をそのまま $4.99 にすると、
新興国では高すぎ、北欧・スイスでは安すぎます。地域グループを3〜4段階に分けるだけで十分。

Phase 2 では **`billingEnabled` を日本のみ true**。海外ユーザーは全機能を無料で使えます。
これは損ではなく、**データと反応を集めるための投資**です。

---

## 8. ローカライズの範囲

### 訳すもの

- UI 文言すべて
- 分析結果の文章（ICU MessageFormat で数値・複数形を埋める）
- 健康情報の記事
- 利用規約・プライバシーポリシー・同意文（**法務レビューが必要。機械翻訳で出さない**）

### 訳すのではなく、地域で差し替えるもの

| 対象 | 理由 |
|---|---|
| **職業の選択肢** | 米国では **worship leader（礼拝の歌唱リーダー）が数十万人規模**で、日本にはほぼ存在しない。ポッドキャスター、配信者、コールセンター、教員も地域で重みが違う |
| **声種（Fach）** | §3.5 |
| **休養方法の選択肢** | 「入浴」は日本的。欧米では sauna / stretching / therapy などに置き換わる |
| **症状の言い回し** | 直訳が通じないものがある（「喉の張り感」など） |
| **食品データベース** | 廃止。簡易モードのみにする（§4） |

### 訳さないもの

- 音名（§3 の変換で対応）
- 単位記号
- 学術用語（CPPS, ACWR など。**ただし画面に出す名前は平易な言葉にする**）

---

## 9. 実装フェーズと規模

| Phase | 内容 | 規模 |
|---|---|---|
| **0-a** | 音名を MIDI 番号へ移行（マイグレーション込み） | 3〜4日 |
| **0-b** | 単位の内部SI化＋表示層の format 関数 | 2〜3日 |
| **0-c** | 日時の UTC＋TZ＋localDate 化（マイグレーション込み） | 3〜4日 |
| **0-d** | 文字列のキー化（ja.json のみ作る） | 3〜5日 |
| 1 | （日本での製品完成。別資料） | — |
| **2-a** | 音名表記の切り替え（3体系 × 3番号体系）＋ユニットテスト | 3〜4日 |
| **2-b** | 英語辞書の作成と英語UI | 1〜2週間 |
| **2-c** | 地域別の職業・声種・休養方法の差し替え | 3〜4日 |
| **2-d** | §5 の文言監査（全画面）＋使用目的の宣言文 | 3〜5日 |
| **3-a** | 同意モデルの1本化（GDPR＋州法準拠） | 5〜7日 |
| **3-b** | データ書き出し・アカウント削除 | 3〜5日 |
| **3-c** | 課金の地域制御＋Stripe Tax＋多通貨価格 | 5日 |
| 4 | EU 対応（代理人、DPA、MDR の宣言整備） | 法務主導 |

**Phase 0 だけなら2週間弱**です。ここを先にやっておけば、
国際化を「いつか」に先送りしても損をしません。

---

## 10. Sonnet 5 への依頼のコツ

- **Phase 0 は必ずマイグレーションから。** 音名・日時の変更は破壊的です。
  「読み取り互換レイヤー → マイグレーション → 書き込み側の切り替え → 旧フィールド削除」
  の4ステップに分け、**1PRずつ**進めさせてください
- 音名変換は **§3.3 の表をそのままテストケースとして渡す**。ドイツ式の H / B は
  必ずテストに入れること。ここは人間のレビューでは見落とします
- 表示変換は `formatNote()` `formatWeight()` のような**関数に必ず集約**させる。
  各コンポーネントで直接組み立てさせない。明示的に要件として書くこと
- §5.3 のチェックリストは、**分析画面を触るPRのレビュー観点として毎回渡す**
- 翻訳は**機械翻訳の下訳＋声のプロによる用語チェック**を前提にする。
  音楽・声の専門用語は一般の翻訳では誤訳されます
  （chest voice, belting, onset, passaggio, Fach など）

---

## 11. 現実的な最大の障壁について

技術でも規制でもなく、**サポートの人的コスト**だと考えています。
多言語で問い合わせが来たとき、個人では受け切れません。

対策として：

- **Phase 2〜3 は英語のみ**に絞る（他言語UIは出さない）
- UI 内で完結する設計にする（FAQ、ツールチップ、オンボーディングを厚く）
- 問い合わせフォームは英語のみと明記する
- コミュニティ（Discord など）に一次サポートを担わせる余地を残す

---

## 参考

- FDA General Wellness Policy for Low Risk Devices（2026年改訂版）
- MDCG 2019-11 Guidance on Qualification and Classification of Software（EU MDR）
- Washington My Health My Data Act（私訴権あり）
- EU–Japan mutual adequacy arrangement（2019年採択、2023年の第一次レビューで維持）
- 個人情報保護法：要配慮個人情報の取扱い
- プログラムの医療機器該当性に関するガイドライン（厚生労働省）

## 注意書き

本書は開発計画の検討材料であり、法務の助言ではありません。
各国の医療機器規制・データ保護法・消費者法の適用については、
課金地域を広げる前に、当該法域の専門家に確認してください。
使用目的の宣言文はドラフトであり、そのまま公開せず法務レビューを受けてください。
