# インシデント記録

★この文書は**足すだけ**です。書いた行は直しません。誤りが分かったときは、
**新しい行として訂正を足します**。消して直すと、訂正した事実そのものが消えます。

★**7つの見出しを、必ず全部埋めます。** 埋まらない見出しは「分かっていません」と書きます。
空欄にすると、**調べていないのか、調べて無かったのかが分かりません。**

★**人数は書きません**（2026-09-02 の凍結）。範囲は**一覧**で示します。

---

## 001 ｜ /api/advice ― 記録を外へ出す経路が、サーバ側で閉じていなかった

**発見**
2026-09-03、私（Claude）が `/api/advice` の中身を読んでいて気づきました。
きっかけは、同意の撤回が強制されていなかった件の調査です。★**同じ形を探していて出ました。**

**内容**
`app/api/advice/route.js` は `entries` を読み、`buildSummary` が喉・声・睡眠・水分・
気温・湿度・活動・公演の出来・心の余裕・喉の症状・食事メモ・メモを1行の文にまとめ、
`lib/anthropic.js` が `api.anthropic.com` へ送ります。
この経路を止めていたのは★**画面の定数だけ**でした（`VocalTracker.jsx:393` の
`AI_ADVICE_ENABLED = false`）。★**あれはボタンを隠すだけで、経路は生きています。**
ログイン済みの方が URL を直に叩けば通る形でした。

**範囲**
いつからか：この経路が書かれたときから。★**正確な日は特定していません。**
誰が：ログインできる方であれば、誰でも。★**自分の記録だけ**が対象です
（`.eq("user_id", user.id)` と RLS の二重で、他人の記録は引けません）。

**確認**
★**一度も外へ出ていません。** 出どころが2つあり、互いに独立しています。
- 送り手側：本番の Vercel に `ANTHROPIC_API_KEY` が**一度も設定されたことがない**
  （運営者が管理画面で確認）
- 受け手側：Anthropic の Console が「今月の使用額 $0.00／過去7日間にアクティビティなし」、
  新規の口座で残高の追加もなし（運営者が確認）

★**「失敗した」ではなく「一度も送っていない」です。** `route.js:105-110` が鍵の有無を
`fetch` より前に見て 500 を返すため、`lib/anthropic.js:4` の `fetch` に**到達しません**。

★**確認できなかったこと**：この経路が何回叩かれたか。
★**ただし、叩かれていたとしても外へは出ていません**（上の2つの出どころによる）。

★**正確に言うと**：記録は**読まれています**。`entries` の読み出し（`88-93行`）は
鍵を見る**前**にあります。読んだものはサーバの中に留まりました。
★**「読んでいない」と言うと嘘になります。「外へ出していない」が正確です。**

**処置**
2026-09-03、`adviceEnabled()` をサーバ側に置きました（`route.js:60-71`）。
`AI_ADVICE_ENABLED === "true"` のときだけ開き、★**認証より前**に判定するので、
閉じているあいだは記録を1行も読みません。★閉じるほうへ倒しています
（未設定・空・`"false"`・`"1"`・`"yes"` は、すべて閉）。
commit `5483cf3`。

**判断**
★**届け出ていません。** 外へ出た記録が1件も無いためです。
★この判断は「出ていないことが確かめられた」ことに乗っています。
出どころが2つあり、送り手側と受け手側の別々の系から一致しました。

**その後**
- 同じ形（画面の定数が門の代わり）を探し、★**Stripe の3ルートで見つけました**（記録 002）。
- 検査 `advice-route-gated.test.js`（16本）が、門が認証より前にあることを見張ります。
- 台帳 `lib/outboundRoutes.js` に、この経路と「一度も通っていない」記録を残しました。

---

## 005 ｜ 役職の自己昇格 ― ★穴はありませんでした（記録の欠落だけ）

★**これは「開いていた」記録ではありません。「調べて、閉じていた」記録です。**
★調べたこと自体を残さないと、次に同じ疑いが出たときに、また一から調べることになります。

**発見**
2026-09-03、TASK 1（本番とリポジトリの突き合わせ）の途中で、
`migration_role_rank_no_self_promotion.sql` が★**本番に流されていない**ことが分かりました。
ファイル名は `no_self_promotion` — 自分で役職を上げられないようにする規則です。
★そして 2026-09-02 に、同じ場所で**実際に自己昇格が起きています**
（`URGENT_fix_owner_self_promotion.sql:1-30`。+g4t3 が自分を owner に上げた）。

**内容（調べた対象）**
`memberships` のポリシーは4本でした。

| ポリシー | 操作 | 要点 |
|---|---|---|
| `memberships_delete_admin` | DELETE | オーナー/責任者、かつ owner 以外 |
| `memberships_insert_bootstrap_owner` | INSERT | 自分・owner・自分が作った教室・★**まだ誰も居ない教室** |
| `memberships_select` | SELECT | 本人、またはオーナー/責任者 |
| `memberships_update_role_management` | UPDATE | ★下記 |

```
WITH CHECK: CASE WHEN auth.uid() = user_id
              THEN role_rank(role) <= role_rank(
                     (SELECT m.role FROM memberships m WHERE m.id = memberships.id))
              ELSE role <> 'owner' END
```

★**この `role_rank` の比較は、リポジトリのどこにもありません。**
★9/2 の記録によれば、当時この条件はありませんでした（だから昇格が通りました）。
★**つまり 9/2 のあと、誰かが手でこの条件を足したことになります。**
★**2度目の手当てで、これも写されていません。**

**確認（★実地。読解ではありません）**

| 試したこと | 結果 |
|---|---|
| teacher の口座が、自分を `admin` へ上げる | ★**RLS に弾かれた**（42501） |
| teacher の口座が、自分を `owner` へ上げる | ★**RLS に弾かれた**（42501） |
| teacher の口座が、既存の教室へ `owner` として入る | ★**RLS に弾かれた**（42501） |
| owner が2人以上いる教室 | ★**0件**（9/2 の跡は直っています） |

★**上げる道と、入る道の両方を試しました。** 片方だけでは足りません。
★**私の「弾かれるはず」という読みは、採用せずに実地で確かめました。**
（PostgreSQL の「1つの文は自分自身の変更を見られない」規則から、
　副問い合わせは元の値を見るはず、と読んでいました。★結果は一致しましたが、
　今日は読解で4回外しているので、読みを根拠にはしていません。）

**★試し方の欠陥（安全上の話ではありません）**
`begin;` 〜 `rollback;` で囲んでいましたが、★**囲みが効きませんでした。**
先頭の `insert`（試験用の membership）が★**本番に残りました**（`id = 64a84bcd-…`）。
SQL エディタが1文ずつ流していると、1文ごとに確定します。
★運営者が `delete` で片づけられました。
★**決めごとにしました**（`docs/lavoce-くり返す失敗の形.md` 7-4）：
本番で試すときは `begin`/`rollback` を守りとして数えない。書き込まない形を先に考える。

**判断**
★**穴はありません。届け出の対象ではありません。**
★**ただし、守っているものが、リポジトリのどこにも書かれていません。**
次に誰かが `memberships` のポリシーを手で触ったとき、★**この守りは黙って消えます。**
そして★**検査は通り続けます**（`components/tests/role-rank.test.js` は
ファイルの中身を見張るもので、本番に当たっているかは見ません）。

**残っている作業**
1. ★`memberships_update_role_management` を、移行ファイルとして書き起こす
2. ★`role_rank` 関数も同じく書き起こす
3. ★`URGENT_fix_owner_self_promotion.sql` の RESTRICTIVE 4本を、どうするか決める
   （下記の検討を参照）

**★RESTRICTIVE 4本は、いま冗長か**

★**冗長ではありません。意味が残ります。** 理由は3つです。

- ★**PERMISSIVE は OR、RESTRICTIVE は AND です。**
  いま守っているのは PERMISSIVE 1本（`memberships_update_role_management`）です。
  ★**PERMISSIVE がもう1本足されたら、条件は足し算になり、広いほうが勝ちます。**
  RESTRICTIVE なら、何本足されても AND で絞り続けます。
- ★**いまの守りは `role_rank` の順位比較に乗っています。**
  `URGENT_fix_owner_self_promotion.sql` の案は `role <> 'owner'` を
  ★AND の直下に置く形で、★**owner を書く道が誰にも無くなります。**
  順位の比較より、こちらのほうが単純で、読み違えようがありません。
- ★**INSERT と DELETE は、UPDATE とは別の道です。**
  `memberships_restrict_role_insert` と `_owner_row_delete` が守るのは、
  ★いまの UPDATE のポリシーが触れていない範囲です。

★**ただし、そのまま流すことは勧めません。** あのファイルは
`is_org_owner` を作りますが、★**本番に無く、リポジトリでも呼ばれていません。**
★**いまの本番の姿に合わせて書き直すほうが、筋が通ります。**
★判断は運営者と Opus のものです。私は選択肢だけをお示しします。

---

## 004 ｜ 招待コードが、使っても「使用済み」にならない

★**これは今日の漏れとは別件です。** 調べの途中で見つかりました。

**発見**
2026-09-03、招待14件すべてで `used_at` が空である理由を追っていて気づきました。

**内容**
招待を受ける流れは2段になっています。

1. 画面側（`components/VocalTracker.jsx`）
   `9556` つながりを作る → `9589` 同意を記録する → `9592` **`used_at` を立てる**
   → `9606` サーバへ（`/api/enrollment/accept`）
2. サーバ側（`app/api/enrollment/accept/route.js`）
   招待を**読み**（50行）、つながりを**読み**（70-76行・書きません）、
   在籍（114行）と担当（144行）を**作ります**。
   ★**`used_at` を立てる処理は、1つもありません**（検索して確認）。

★しかもサーバ側のコメント（66-68行）は、こう書いています。

> 直後に呼ばれます。そのとき招待は**既に使用済みになっています**。
> 使用済みを弾くと、★正しい流れが通らなくなります。

★**サーバ側は「画面側が立てた」ことを前提にしていますが、自分では立てません。**
画面側が `9592` へ到達しなければ、★**誰も立てません。**

**なぜ重いか**
`9494` の関門は、こうです。
```js
if (data.used_at || new Date(data.expires_at) < new Date()) { …使用済み… }
```
★**`used_at` が空のままなら、この関門は開いたままです。**
期限（発行から7日）が来るまで、★**そのコードを持つ人なら誰でも、何度でも使えます。**

**範囲**
いつからか：★分かっていません。サーバ側の経路ができたときからです。
誰が：招待コードを知っている人。★コードは画面に出て、アプリの外で渡されます。

**確認**
14件すべてで `used_at` が空でした。★**ただし、これは「使われていない」証拠には
なりません。** 立てる処理が無いので、使われても空のままです。
★**「空である」ことから「使われていない」を導けません。**

**処置**
★**まだ直していません。** 今日の漏れの調べを優先しています。

**判断**
★届け出の対象ではないと考えます。第三者による発行が0件で、
つながりも第三者どうしが0件だからです。★**ただし、直すまでは開いたままです。**

**その後**
★**この件が、今日の調べの結論に影響しないことを確かめました。**
「第三者どうしのつながりが0件」は、`teacher_student_links` と `assignments` を
★**直接数えて**確かめたもので、招待の記録には依存していません。
★**もし `used_at` を根拠にしていたら、誤った安心を文書に書くところでした。**

---

## 002 ｜ 決済の経路が、鍵なしで通信していた

**発見**
2026-09-03、記録 001 の「同じ形を探す」作業のなかで見つけました。

**内容**
`lib/stripe.js` は `new Stripe(process.env.STRIPE_SECRET_KEY)` を作りますが、
★**鍵が未設定でも投げません。** 読み込みは通り、★**呼んだときに初めて**
`StripeAuthenticationError` になります。つまり★**通信そのものは起きます。**
そして `app/api/stripe/checkout/route.js` には鍵の見張りが**どこにもありませんでした**。
その結果、
```
stripe.customers.create({ email: user.email, metadata: { supabase_user_id: user.id } })
```
が**そのまま `api.stripe.com` へ届き、そのあと拒否されます**。
★**メールアドレスと利用者IDが外へ出てから拒否される形**でした。

★記録 001 との違い：あちらは `fetch` の**手前**で止まるので1バイトも出ません。
こちらは**出てから**止まります。★**同じ「閉じている」でも、中身が違います。**

**範囲**
いつからか：`bd10c1e`（2026-08-23、最初の一括アップロード）から。
誰が：★**画面に入口はありません。** `app/billing/page.js:25` の早期 return により、
`REQUIRE_SUBSCRIPTION` が有効でないあいだは案内だけを返し、
`<CheckoutButton />` に**到達しません**。★**URL を直に叩いた場合のみ**です。

**確認**
★**これまでに叩かれたことがあるかは、分かっていません。**
Vercel のアクセスログで数えられますが、まだ数えていません。
★**「たぶん誰も試していない」とは書きません。**

いまが閉じていることは、運営者が実地で確かめられました（2026-09-03）。
- 画面：`/billing` にボタンが1つも無い
- 経路：`fetch('/api/stripe/checkout', {method:'POST'})` → **503**

**処置**
2026-09-03、`stripeConfigured()` を `lib/stripe.js` に置き、
`checkout` / `portal` / `webhook` の★**3つすべて**で、何かをする前に見ます。
未設定なら 503。commit `3082fe7`。

**判断**
★**届け出ていません。** 出たとしても、メールアドレスと利用者ID（uuid）で、
体調の記録は含まれません。かつ、相手は決済代行として契約している Stripe Japan, Inc. です。
★**ただし「出た件数が分かっていない」ことは、この判断の弱いところです。** 明記します。

**その後**
- 台帳 `lib/outboundRoutes.js` に、実地の確認と「まだ分かっていないこと」を残しました。
- 検査 `stripe-gated.test.js`（28本）が、見張りが何かをする前にあることを見張ります。
- ★**私はこの経路について、コードの読みだけで2度まちがえました。**
  「入口は無い」→「全員に出る」→「早期 return で到達しない」。
  ★動くものを確かめた記録のほうが強いので、台帳の一次資料はそちらにしています。

---

## 003 ｜ profiles の行が、つながっている相手から読めていた

**発見**
2026-09-03、`supabase/migration_org_member_names.sql` の**確かめ方③**で見つかりました。
「`profiles` は、あいかわらず自分の行しか読めないこと」を確かめる照会です。
+g4t2 として `select count(*) from public.profiles` が★**2**を返しました（1であるべき）。

★**この照会は、別の機能を入れる前の安全確認として書いたものです。**
★**本来の目的とは違うものを見つけました。**

**内容**
ポリシー `profiles_connected_display_name`（SELECT）
```
USING: are_connected(auth.uid(), id)
```
★**名前は「display_name だけ」と読めます。** ですが RLS は**行単位**で、列は絞れません。
★**行が読めれば、その行の全部が読めます。**
`allergies` / `regular_medications` / `conditions`（既往症）/ `health_notes` /
`is_under_18` / `cycle_show_on_home` / `line_user_id` / `occupation`（自由記述）…

★**このポリシーも `are_connected` も、リポジトリのどこにもありません。**
手で当てられ、一度も写されていません。

**範囲**
いつからか：★**分かっていません。** リポジトリに無いので git に記録がありません。
★**少なくとも 2026-09-02 より後ではありません**（その日、教室のメンバーの名前が
読めなかったため、教室の在籍は対象外だと分かります）。
誰が：`are_connected()` が真を返す組。★**先生と生徒のつながり**が含まれることは、
+g4t2（先生役）が +g4s1（生徒役）の行を読めたことから確かです。

**確認**

★**ログについて**
PostgREST のログを見ました。`profiles` への読み出しは、見られる期間には出ていません。
ただし見られるのは直近24時間だけです。ポリシーが存在した期間の全部には、まったく届きません。
★これは「証拠が無い」のではなく、「証拠を取れる期間が無い」です。
**「漏れは起きなかった」ではありません。**

★**そこで、ログではなくデータから確かめました。** 読めた条件は
`are_connected(auth.uid(), id)` が真になる組み合わせです。
★その組み合わせを、1件ずつ数えました。

★**`are_connected` が見ている表（2026-09-03 確認）**
`teacher_student_links` と `assignments` の**2つだけ**です。ほかは見ていません。
★これが確かめられるまで、下の数え方は「範囲を取り違えているかもしれない」ものでした。
確認できたので、★**下の数は、読めた組み合わせの全部です。**

★**組み合わせの数（過去に解除されたものも含みます）**

| | 数 |
|---|---|
| 組の総数 | 16 |
| 判定できない（`profiles` の行が無い） | 0 |
| 運営者ご本人が片側（別枠） | 5 |
| 内部（試験用）どうし | 11 |
| 片方だけ内部 | 0 |
| ★**独立した第三者どうし** | ★**0** |

★内訳の合計（0+5+11+0+0＝16）が総数と一致します。★**分類の漏れはありません。**

★**運営者ご本人の5組について**
5組とも `kyo0703opera`（ご本人が実際にお使いの口座）×
`kyosakamoto0703@gmail.com`（ご本人の試験用の器）でした。
★**ご自身のデータを、ご自身が読める状態だった、ということです。**
★ほかの実在の方は、この5組のどこにも現れません。

★**したがって**
> ★設定の誤りにより取得が**可能な状態**にはあったが、その条件を満たす
> 組み合わせのうち、独立した第三者どうしのものは
> ★**組み合わせとして存在しなかった**。

★**「原理的になかった」とは書きません。** 原理としては可能でした。
★**存在しなかったのは組み合わせであって、可能性ではありません。**

★**いつからだったか（時間の窓）**
2026-09-01 12:05:26（JST）に取られた控え `woolsong-20260901-120526.sql` では、
`teacher_student_links` も `assignments` も★**0行**でした
（`COPY` 行と終端 `\.` の間を数えて確認。推測ではありません）。
★**いまある16組すべてが、それより後にできています。**
★つまり、ポリシーがいつからあったかに関わらず、
**実在の方が関わりうる組み合わせは、9/1 12:05 より前には1つも存在しません。**

★**行が消された可能性について**
`teacher_student_links` と `assignments` に対する `.delete()` は、
コードに**1つもありません**。解除は行を残して印をつける形です
（`status: "revoked"` / `ended_at`）。実際に「解除済み」の行が4件残っています。
利用者向けの DELETE ポリシーも**ありません**。
★`assignments_all_owner_admin`（ALL・`is_org_owner_or_admin` で絞られる）だけが
理屈のうえでは消せますが、教室の管理者の権限が要ります。
★**履歴が消された形跡はありません。**

★**数について（人数の凍結との関係）**
ここに書いた数は★**組み合わせの数**であって、利用者の人数ではありません。
★弁護士に渡す証拠として、正確な数が要るため記載しています。
人数そのものは、この文書にも書きません。

**この2つの表から、行は消せるか**
★**アプリからは消せません。** `teacher_student_links` と `assignments` に対する
`.delete()` は、コードに**1つもありません**（全ファイルを検索）。
解除は、行を残して印をつける形です。
- つながりの解除：`update({ status: "revoked", revoked_at, revoked_by })`（`VocalTracker.jsx:9642`）
- 担当の解除：`update({ ended_at })`（`VocalTracker.jsx:10279`）

★実際に `ended` の行が残っていることが、この形が使われている証拠です。
★**ただし「アプリから消せない」は「消せない」ではありません。**
SQL エディタからは `service_role` で消せます。★DELETE ポリシーの有無は、
別途 `pg_policies` で確かめる必要があります。

**処置**
2026-09-03、運営者がポリシーを落とされました。
```
drop policy if exists "profiles_connected_display_name" on public.profiles;
```
落としたあと、`profiles` に残るのは「Users can view own profile」（SELECT）と
「Users can update own profile」（UPDATE）の2本のみであることを確認済み。

置き換えとして `get_connected_names(uuid[])` を用意しました（commit `99d208e`、★未実行）。
★`are_connected` は**書き写さず、呼びます**。返す列は
`id` / `display_name` / `vocal_profession` の3つだけです。
呼ぶ側3か所（`9733` / `9937` / `10284`）も置き換え済み。

★**ポリシーを落としてから関数を入れるまでのあいだ、先生・生徒の名前は出ません。**
「先生」「名前を読み込めませんでした」と表示されます。★**内部のIDは出ません**
（2026-09-02 に塞ぎ済み）。★**壊れた画面は数分で直せます。読まれた行は戻せません。**

**判断**

```
判断: 報告なし
理由:
  1. 読める状態にあったこと(設定の誤り)は事実
  2. しかし、読める対象(独立した第三者同士の組み合わせ)は機械的な全数確認により0件と確定
  3. 招待コードは全14件が運営者ご自身の発行によるもので、第三者への到達は確認されていない
  4. 「先生は見られません」に相当する利用者向けの約束は、同意画面・アプリ内のいずれにも存在しなかったため、説明義務違反の上乗せもない
記録日: 2026年9月3日
判断の性質: 事業者自身による判断(弁護士による確認は行っていない)
```

★★★ **理由4について、実装側から異なる事実を報告します（2026-09-03）** ★★★

★**「該当する約束は存在しなかった」は、私が確かめた範囲では成り立ちません。**
★この記録は判断を書き換えるためのものではありません。**判断は上のとおりです。**
ただし、**根拠の一つが事実と食い違ったまま残ることは避けます。**

**(A) `components/VocalTracker.jsx:17822-17825` — ★画面に出ます**
```jsx
<p className="text-xs mb-3 leading-relaxed" style={{ color: C.ink }}>
  つながると、この先生とレッスンの予定を共有できるようになります。
  あなたの記録の中身は、先生には見えません。
</p>
```
★先生とつながる前に、生徒が読む文です。コメントではありません。
★しかもこの文面は版として記録されています
（`lib/linkConsent.js:32` `LINK_AGREEMENT_VERSION = "link-2026-09-01"`）。
つまり「この文に同意した」という記録が `link_consents` に残ります。

**(B) `lib/consent.js:56` — 同意画面の目的の文**
> 月経周期の開始日を保存し、あなた自身の分析にだけ使います。
> ★**先生や教室には、一切共有されません。**

**★事実関係の切り分け**
- `entries`（日々の記録）… ★渡っていません。(A) はこの範囲では正しい。
- `cycle_periods`（周期そのもの）… ★渡っていません。(B) はこの範囲では正しい。
- `profiles` の行 … ★**渡っていました。**
  `conditions`（既往症）／`allergies`／`regular_medications`／`health_notes`／
  `is_under_18`／`cycle_show_on_home`／`line_user_id` を含みます。

★**「記録の中身」を `entries` と読むなら、(A) は正しいままです。**
★**「体調の情報」と読むなら、誤りでした。** 既往症と常用薬は、`entries` より機微です。
★(B) は周期そのものではなく「周期を使っているか」（`cycle_show_on_home`）が渡りました。

★**どちらの読み方を採るかは、事業者の判断です。私は決めません。**
★**ただし「約束が存在しなかった」という前提は、採れません。**

★**参考**：私が最初に「これに近い断言がある」と申し上げた
`VocalTracker.jsx:11289` は★**JSX コメントで、画面には出ません**。
★上の (A)(B) は、それとは別の、**実際に表示される文**です。

**理由5（原文）**
```
  5. 以上により、法26条の「発生したおそれ」(具体的な可能性を要する)には該当しないと判断した
```

### ★再検討の結果（2026-09-03・最終）

```
判断: 報告なし(維持・再検討済み)
再検討の経緯: LINK_AGREEMENT_VERSION(17822-17825)およびlib/consent.js:56の発見を受け、
  運営者が理由④を再検討。
  (A) 「あなたの記録の中身は、先生には見えません」— 対象が「記録の中身」と広く、
      profiles(既往症等)は実際に露出可能だったため、広く読めば守られていなかった。
  (B) 「月経周期の開始日…一切共有されません」— 対象が「開始日」と明確で、
      該当データ(cycle_periods.start_date等)は今回の漏れの対象外。字義通り守られている。
  検討の結果、(A)についても「読める対象(第三者)が0件だった」という理由①〜③の結論
  (誰も実際に見ていない/見られない)自体は揺るがないとの判断により、報告なしを維持。
  ただし(A)の文言自体の正確性については、別途見直しが必要と認識。
```

### ★私（Claude）の側の訂正

★**私は最初、(B) も破られていたかのように書きました。不正確でした。**

列を1つずつ確かめた結果は、次のとおりです。

| どこに | 何が | 今回漏れたか |
|---|---|---|
| `cycle_periods` | `start_date` / `end_date` | ★**漏れていません** |
| `entries` | `cycle_start` | ★**漏れていません** |
| `profiles` | `track_cycle` / `cycle_show_on_home` | ★**漏れました。ただし真偽値です** |

★`profiles` に、周期の**日付を持つ列は1つもありません**。
★渡ったのは「周期の記録機能を使っているか」「ホームに出しているか」という
**真偽値だけ**で、日付でも周期の長さでもありません。

★**したがって (B) は、約束した対象（開始日）については守られています。**
★私が「(B) も誤っていた」と読める書き方をしたのは、**私の側の不正確さ**です。

**★入口は、隠れていませんでした（2026-09-03・実機で確認）**

★私は当初「教室の機能には入口が無い」と考えていました。**誤りです。**
運営者が実際にアプリを開いて確かめられました。

- 「レッスン」のタブは、ふつうの口座で**開いた直後から見えています**
- そこから「教室に参加する」で、招待コードを入れる画面に**行けます**

★**しかも、それは意図した設計です。** `lib/featureFlags.js:51-53`：
```js
export function canSeeStudentTeacherLink() {
  return true;   // ★常に true
}
```
コメント（`33-49行`）に理由が書かれています。生徒側の入口を隠していたために
「つながって初めて出るタブの中に、つながる入口がある」という
★鶏と卵になっていたのを、わざわざ解いたものです。

★**したがって「気づかれにくい隅だった」を、軽い材料に使いません。**
★**使ってもらうために開けた道**でした。9/1 までに招待が8件発行されています。

★私は `canTeachLessons`（先生側の入口）だけを見て「つながりが0件なら出ない」と
言いました。★**見ていたのは別の入口でした。** 画面のことを、コードの一部から
言い当てようとして外したのは、今日これで4回目です。

**その後**
- 常設の棚卸しを作りました：`supabase/AUDIT_policies_by_using_clause.sql`。
  ★**名前を見ない。USING 句を見る。**
- 命名の規則を決めました（`docs/lavoce-くり返す失敗の形.md` 6-2）。
  ポリシーの名前は★「誰がその行を見てよいか」を書く。★列の名前は書かない。
- ★**同じ誤解が、今日3件目です**（`lessons`・`entries`・`profiles`）。
  4件目を探すため、本番の姿を丸ごと写し取る照会を用意しました
  （`supabase/AUDIT_full_schema_vs_repo.sql`）。
- 検査 `profiles-cross-user.test.js`（23本）が、他人の `profiles` を直に読む書き方が
  戻ってこないことを見張ります。
