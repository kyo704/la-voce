# G4 通し確認｜手順書（+g4t1〜+g4t4／+g4s1・+g4s2）

**★上から順に、そのまま進めてください。** 教室の状態が積み上がっていくので、
飛ばすと後ろが成り立ちません。判定は `docs/lavoce-G4-受け入れ確認.md` の①〜⑭に対応します。

> **★2026-09-02 追記｜実際の割り当ては下の表と入れ替わりました。**
> 権限昇格の件を追ううちに、教室が先に出来上がったためです。
> **読み替えてください。**
>
> | この文書 | 実際 |
> |---|---|
> | +g4t1（オーナー） | **+g4t2**（音楽学校A のオーナー） |
> | +g4t2（講師→責任者） | **+g4t3**（責任者。昇格の確認ずみ） |
> | +g4t3（★どこにも入らない人） | **+g4t4**（★こちらを未所属のまま残す） |
> | +g4t4（1人だけの教室） | **+g4t1**（solo org を持っている） |
>
> ★「どこにも入らない人」は **+g4t4** です。⑥と共通ゲートAで要ります。
> ★+g4t1 は、⑦（教室への招待）で音楽学校A に**講師として**入ります。
> 　そのあと⑧（講師が自分を責任者にできない）が試せるようになります。

## 登場人物（★この割り当てで進めます）

| 口座 | 役 | 何のために要るか |
|---|---|---|
| **+g4t1** | 教室のオーナー | 教室を作る。招待する。予定を作る |
| **+g4t2** | 講師 → 責任者 → 講師 | 役割の上げ下げ。教室に招かれる側 |
| **+g4t3** | ★教室に入らない先生 | 「関係のない人には見えない」の確認に要る |
| **+g4t4** | ★1人だけの教室のオーナー | ⑪作りかけの教室・⑫運営者を0にしない |
| **+g4s1** | 生徒（在籍する） | 予定・出席・カレンダー |
| **+g4s2** | 生徒（2人目） | ★出席の印が、ほかの生徒に見えないことの確認 |

★**+g4t3 は、最後まで何にも参加させないでください。** 「入っていない人」が
いないと、⑥が確かめられません。

## SQL の実行について（★全体に効く注意）

- ★`service_role`（SQL Editor の既定）では **RLS を素通り**します。
  「見えるか」「書けるか」を試すときは、必ずなりすまします。
- ★`request.jwt.claims` を**先**、`set local role authenticated` を**あと**。
  逆にすると `auth.uid()` が null になり、**止まった理由を取り違えます**。
- なりすます SQL は、すべて `begin; … rollback;` で囲んであります。行は残りません。

uuid は、これで引けます（★メールの形が違うときは読み替えてください）。

```sql
select u.email, u.id
  from auth.users u
 where u.email like 'kyosakamoto0703+g4%@gmail.com'
 order by u.email;
```

---

# 段階0｜指導者ベータを付ける（★これが無いと教室の画面が出ません）

```sql
update public.profiles p
   set teacher_beta_access = true
  from auth.users u
 where u.id = p.id
   and u.email in (
     'kyosakamoto0703+g4t1@gmail.com',
     'kyosakamoto0703+g4t2@gmail.com',
     'kyosakamoto0703+g4t3@gmail.com',
     'kyosakamoto0703+g4t4@gmail.com'
   );

-- ★4行 返ることを確かめます
select u.email, p.teacher_beta_access
  from public.profiles p join auth.users u on u.id = p.id
 where p.teacher_beta_access = true
   and u.email like 'kyosakamoto0703+g4t%@gmail.com';
```

**★ついでに⑬の下ごしらえ：profiles の行が無い人が居ないこと**

```sql
select count(*) as "★profiles が無い人（0であること）"
  from auth.users u
 where not exists (select 1 from public.profiles p where p.id = u.id);
```

★0 でないなら、その人が誰かを確かめてから先へ進んでください
（`supabase/check_missing_profiles.sql` の②）。
6人とも登録直後なので、ここは 0 のはずです。

**★次に進む前に：4人とも表示名を入れてください。**
もっと → プロフィール → 表示名。**+g4t1＝「一郎」／+g4t2＝「二郎」／
+g4s1＝「花子」／+g4s2＝「桃子」** のように、見て分かる名前にします。
⑥で「名前が出るか」を見るので、空欄だと確かめられません。

★このとき、**表示名の欄の下に注意書きが出ていること**を確かめます。
```
この名前は、同じ教室のメンバーにも表示されます。生年月日や住んでいる場所など、
知られたくないことが分かる名前は避けてください。
```

---

# 段階1｜教室を作る　→【⑤ bootstrap の実地確認】

**+g4t1 でログイン**

1. 下の帯から **レッスン** を開く
2. 「教える」に切り替える
3. **「教室を作る」** を開いて、ボタンを押す

**確かめること**
- 教室が一覧に出る（「マイ教室（オーナー）」）
- ★エラーが出ないこと

**失敗の見え方**
- 「教室の準備が、途中で止まっています。同じ操作を繰り返しても直りません。」
  → ★bootstrap のポリシーが壊れています。**ここで止めてください。**
  これは受け入れ確認⑤の失敗そのもので、**誰も教室を作れない**状態です。
- 何も起きない／反応がない → コンソールを見て、403 が出ていないか確かめます。

```sql
-- ★1行 返ること（オーナーとして入れている）
select o.name as "教室", m.role as "役割"
  from public.organizations o
  join public.memberships m on m.org_id = o.id
 where o.created_by = '<+g4t1 の uuid>';
```

---

# 段階2｜生徒をつなぐ　→【⑧ 在籍】

**+g4t1**
1. レッスン → 教える → **「招待コードを発行する」** を押す
2. 出たコードを控える

**+g4s1 でログイン**
3. **もっと** → **「先生とつながる」**
4. コードを入れる → ★**先生の名前が出ること**（「一郎」）
5. 共有する項目を選び、参加する

**確かめること**
- ★手順4で先生の名前が出る（出ないなら `get_invitation_teacher` が入っていません）
- +g4s1 のレッスンタブに、教室と担当の先生が出る

```sql
-- ★1行 返ること。2026-09-01 まで、この表は全体で0行でした
select count(*) as "在籍の行数"
  from public.enrollments where status = 'active';
```

**失敗の見え方**
- 「コードが見つかりませんでした」→ コードの確認経路が通っていません
- つながったのに `enrollments` が0行 → ★今朝直した不具合の再発です

**+g4s2 でも同じことをします**（コードは+g4t1 が再発行）。
★生徒が2人になります。⑤の「ほかの人の印が見えない」に要ります。

---

# 段階3｜先生を教室に招く　→【⑦ 教室への招待】

**+g4t1**
1. レッスン → 教える → 自分の教室を開く
2. **「講師を招待する」** → 「招待コードを発行する」
3. コードを控える

**+g4t2 でログイン**
4. レッスン → 教える → **「教室に参加する」**
5. コードを入れる → ★**教室名が出ること**
6. 「参加する」

**確かめること**
- +g4t2 の一覧に、その教室が出る

**失敗の見え方**
- ★「参加に失敗しました。もう一度お試しください。」
  → **古い版です。** この文言は消してあります。デプロイを確かめてください。
- 「コードが見つかりませんでした」→ lookup の経路が通っていません
- ★この経路は 2026-09-02 まで**一度も成功していませんでした**。
  ここが通ることが、今回いちばん確かめたいことの1つです。

```sql
select u.email, m.role
  from public.memberships m join auth.users u on u.id = m.user_id
 where m.org_id = '<教室の uuid>';
-- ★2行（+g4t1 = owner、+g4t2 = teacher）
```

---

# 段階4｜名前が見えること　→【⑥ メンバーの名前】

**+g4t1**（教室を開いたまま）

**確かめること**
- メンバー欄に **「一郎」「二郎」** と出る
- 在籍している生徒に **「花子」「桃子」** と出る
- ★`53ef27ed…` のような**文字列が出ていないこと**

**失敗の見え方**
- 404 がコンソールに出る → `supabase/migration_org_member_names.sql` が未実行
- 「名前を読み込めませんでした」→ 関数を呼べていません
- 「名前未設定」→ その人が表示名を入れていません（段階0に戻る）
- ★uuid の断片が出る → **古い版です**

**★関係のない人には見えないこと（+g4t3 でなりすまし）**

```sql
begin;
select set_config('request.jwt.claims',
  '{"sub":"<+g4t3 の uuid>","role":"authenticated"}', true);
set local role authenticated;
select auth.uid() as "★+g4t3 になっているか";
select count(*) as "★0 であること" from public.get_org_member_names('<教室の uuid>');
rollback;
```

**★profiles そのものは、あいかわらず読めないこと**

```sql
begin;
select set_config('request.jwt.claims',
  '{"sub":"<+g4t2 の uuid>","role":"authenticated"}', true);
set local role authenticated;
select count(*) as "★1 であること（自分の行だけ）" from public.profiles;
rollback;
```

★ここが2以上なら、profiles が開いています。**同じ行に allergies・
regular_medications・周期・is_under_18 があります。止めてください。**

---

# 段階5｜教室の予定　→【④⑤⑨⑩】

## 5-1　予定を作る（+g4t1）

教室を開く → 「教室の予定」→ 日付・種類（本番）・名前を入れて **「足す」**

## 5-2　生徒に見えること（+g4s1）　→【⑨-1】

ノート → カレンダー

**確かめること**
- 「教室の予定」の一覧に出る
- 升目の**下辺に細い線**が出る
- ★**種類ごとに色が変わっていないこと**（§7-5：値で塗り分けない）

## 5-3　出席の印　→【⑨-4】

+g4s1 が **「出る」** を押す → **「出ます」** に変わる

## 5-4　★ほかの人の印が見えないこと　→【⑤】

```sql
begin;
select set_config('request.jwt.claims',
  '{"sub":"<+g4t1 の uuid>","role":"authenticated"}', true);
set local role authenticated;
select count(*) as "見える印", count(*) filter (where user_id <> auth.uid())
       as "★他人の印（0であること）"
  from public.org_event_participants;
rollback;
```

★**オーナーにも見えません。** +g4s2 でも同じく 0 です。

## 5-5　★日付を変えたら、生徒に届くこと　→【⑩ 今日いちばん見落としやすい所】

1. **+g4s1**：ノート → カレンダーを**開いたままにする**
2. **+g4t1**：予定の日付を、別の日に変える
3. **+g4s1**：★**ホーム → ノート → カレンダー** とタブを移って戻る
4. 新しい日付になっていて、
   **「◯月◯日から◯月◯日に変わりました」** が出ること

**失敗の見え方**
- 古い日付のまま → ★2026-09-02 に直した不具合の再発です
- ★**見ている最中に変わらないのは、正常です**（購読は1つもありません）。
  確かめるのは「タブを移って戻ったら新しくなる」ことだけです。

## 5-6　取り下げ　→【⑨-3】

+g4t1 が「取り下げる」→ +g4s1 がタブを移って戻る

**確かめること**
- **「この予定は取り下げられました」** と **「表示から消す」** が出る
- ★予定が**黙って消えていないこと**
- 「表示から消す」を押すと、そこで初めて消える

## 5-7　★生徒は予定を作れないこと　→【④】

```sql
begin;
select set_config('request.jwt.claims',
  '{"sub":"<+g4s1 の uuid>","role":"authenticated"}', true);
set local role authenticated;
insert into public.org_events (org_id, event_date, kind, title)
values ('<教室の uuid>', current_date, 'その他', '権限の確認');
rollback;
```
**★42501 で止まること。** 成功したら、書き込みのポリシーが開いています。

---

# 段階6｜役割の上げ下げ　→【③ 8場面】

## 6-1　画面で（+g4t1）

メンバー欄で **+g4t2 を「教室の責任者」に**

**確かめること**：変わること（★これが通らないなら締めすぎです）

## 6-2　SQL で8場面

`supabase/check_owner_role_protection.sql` を開き、置き換えます。

| 差し込み | 値 |
|---|---|
| `<教室>` | +g4t1 の教室 |
| `<オーナーA>` | +g4t1 |
| `<責任者>` | +g4t2 |
| `<講師>` | ★+g4t2 を講師に戻してから使うか、③のために +g4t3 を一時的に招く |
| `<オーナーB>` | ③の中で一時的に作られます |

| | 場面 | 期待 |
|---|---|---|
| ① | 責任者が自分を owner に | **0行** |
| ② | 責任者がオーナーを降格 | **0行** |
| ③ | 共同オーナーAがBを降格 | **0行** |
| ④ | オーナーが自分で降りる | **1行** |
| ⑤ | 最初の1人（教室を作る） | **1行** |
| ⑥ | オーナーが講師を責任者に | **1行** |
| ⑦ | 責任者を講師に戻す | **1行** |
| ⑧ | 講師が自分を責任者に | **0行** |

★**④⑤⑥⑦が0行なら、締めすぎです。**
　とくに⑤が0行だと、**誰も新しい教室を作れません**。
　①②③⑧だけを見て通すと、この壊れ方は**絶対に見つかりません**。

---

# 段階7｜運営者を0にしない　→【⑫】

**+g4t4 でログイン** → レッスン → 教える → 「教室を作る」

これで +g4t4 は**自分ひとりの教室のオーナー**です。

## 7-1　最後のオーナー

メンバー欄で、自分を「講師」にしようとする
→ ★**止まること**。
```
この教室を運営できる人が、いなくなってしまいます。
先に、ほかの方を「教室の責任者」にしてください。
```

## 7-2　★オーナーが居ない教室（今回広げたところ）

```sql
-- ★+g4t4 を admin にして、owner を居なくします（あとで戻します）
update public.memberships set role = 'admin'
 where org_id = '<+g4t4 の教室>' and user_id = '<+g4t4 の uuid>';
```
★これは service_role で行います（UPDATE で owner を書く道は塞いであるため）。

画面を開き直して、自分を「講師」にしようとする
→ ★**止まること**。前の版では**通っていました**（owner だけ数えていたため）。

```sql
-- 戻します
update public.memberships set role = 'owner'
 where org_id = '<+g4t4 の教室>' and user_id = '<+g4t4 の uuid>';
```

---

# 段階8｜記録とレッスンが漏れないこと　→【①②】

```sql
-- ① +g4s1 のセッション
begin;
select set_config('request.jwt.claims',
  '{"sub":"<+g4s1 の uuid>","role":"authenticated"}', true);
set local role authenticated;
select count(*) as "見える行数",
       count(*) filter (
         where not exists (select 1 from public.teacher_student_links l
                            where l.id = lessons.link_id and l.student_id = auth.uid())
           and (student_id is null or student_id <> auth.uid())
       ) as "★自分のものでない行（0であること）"
  from public.lessons;
rollback;

-- ② +g4t1 のセッション
begin;
select set_config('request.jwt.claims',
  '{"sub":"<+g4t1 の uuid>","role":"authenticated"}', true);
set local role authenticated;
select count(*) as "★見える生徒の記録行数（0であること）"
  from public.entries where user_id <> auth.uid();
rollback;
```

★②が1以上なら、**生徒の記録が先生に読めています**。
　`select("*")` は全列を返すので、「先生に決して渡さない11列」も渡ります。

---

# 段階9｜作りまえの教室　→【⑪】

```sql
-- ★わざと「オーナーの居ない教室」を作ります（service_role）
insert into public.organizations (name, kind, created_by)
values ('確認用・作りかけ', 'solo', '<+g4t4 の uuid>');
```

**+g4t4** でレッスン → 教える を開く

**確かめること**
- **金色の枠**で「確認用・作りかけ」が出る
- 「この教室は作られましたが、あなたが『オーナー』として入るところで
  止まっています。」と出る
- ★**押せるものが1つも無いこと**（ボタン・入力欄・選択欄）

**失敗の見え方**
- ★一覧に**出てこない** → 持ち主から見えず、直しようがありません
- ★ボタンが**ある** → 押すと必ず 403 になります

```sql
-- 片づけます
delete from public.organizations where name = '確認用・作りかけ';
```

---

# 段階10｜共通ゲート　→【⑭】

## A｜白い画面が出ないこと

**+g4t1・+g4t2・+g4t3・+g4s1 の4人**で、
ホーム／今日／分析／おうち／ノート（4つのサブタブ）／もっと／レッスン
を**全部**開きます。

★**+g4t3 が大事です。** どこにも属していない先生なので、
　権限で囲まれた画面に**いちばん当たりやすい**口座です。

**失敗の見え方**：押せるボタンの先が**何も描かれない**。
★条件が偽のとき、React は黙って何も描きません。

## B｜禁じた言い回しが出ていないこと

- 「まだ」（★全面禁止）
- 「確認してください」
- ★「もう一度お試しください」が、**通らない経路**に出ていないこと
- 「あなたの出席登録は取り消されました」
- 断定的な相関文／他の利用者との比較・平均・順位

## C｜戻る道があること

権限で止まる画面に、**押せる選択肢**があること。
★ボタン1つ＋文章での案内、という形になっていないこと。

## D｜実機（iPhone）

A と同じ道を歩きます。
- 入力欄に触れて**拡大しない**（16px 以上）
- 下端が安全領域に隠れない
- ★**機内モードで白い画面にならない**

---

# 段階11｜開発機で

```bash
npm run build
npm run lint
for f in components/tests/*.test.js; do node "$f" >/dev/null || echo "失敗: $f"; done
```

★検査は**リポジトリの中しか見ていません**。
　①②のような穴は、ソースを読んでいるかぎり永久に見つかりません。
　**段階8を省かないでください。**

---

# 片づけ（★通ったあとで）

★**+g4 の口座は消さないでください。** 退会の経路は 2026-09-02 に直したばかりで、
まだ実地で確かめていません。次の確認のために、そのまま残します。

段階9で作った教室だけ、消してあります。
