# La Voce 引き継ぎ資料（2026年9月12日）

対象：次の担当チャット／セッション
正：`Woolsong全てのデータ/00-統合-いまの全体像（9月11日・統合版）.md`（以下「統合ファイル」）
本書は、統合ファイルとリポジトリの実態を照らし合わせた結果だけを記す。

---

## 1. 現在のリポジトリ状態

```
リポジトリ : https://github.com/kyo704/la-voce
ブランチ   : main
最新コミット: b6ecd86（origin/main と同期済み）
未コミット変更: なし（作業ツリーはクリーン）
未追跡ファイル: HANDOFF_VERCEL_DEPLOY_2026-09-12.md（リポジトリ直下・情報メモ、未add）
```

---

## 2. 今回完了したコミットと機能

直近のコミット（新しい順）：

```
b6ecd86 fix: restore docs/ to deployment (JSON assets required at build time),
         move tailwindcss/postcss/autoprefixer to dependencies
c23f85d fix: remove invalid nodeVersion and env secret refs from vercel.json
dbe1c6a fix: remove nonexistent vercel/action workflow, convert lib/email* to
         ESM, fix test format
31119ab docs: add handoff, deployment fix, and github status resources
ff03d76 fix: github actions vercel deployment config - add VERCEL_TOKEN to env
1c0ee94 feat: add vercel deployment, UI components, security layer
```

完了した機能・修正：

- **メール制限層**：`lib/emailRestrictions.js`／`lib/emailAuthGuard.js`（ESM形式）。許可メール2件のみ実行権限を持つ判定。テスト `components/tests/email-restrictions.test.js`（本リポジトリ独自の readFileSync＋動的import＋自作 `ok()` 形式）。
- **UIコンポーネント追加**：`components/ReviewPanel.jsx`／`NotePanel.jsx`／`MorePanel.jsx`／`ResponsiveLayoutCheck.jsx`（VocalTracker.jsx への統合はまだ）。
- **Vercelデプロイ導線の是正**：存在しない `vercel/action` を使う `.github/workflows/deploy.yml` を削除し、Vercelの「GitHubリポジトリを取り込むだけの標準連携（push毎に自動デプロイ）」に一本化。
- **`vercel.json` の修正**：スキーマ上存在しない `nodeVersion` と、Secrets未登録のまま書いていた `env`（`@secret名` 形式）を削除。
- **`.vercelignore` の修正**：`docs/` を丸ごと除外していたのを解除。`lib/secondColorChoice.js` ほか複数ファイルが `@/docs/assets/*.json` や `@/docs/opus/items.json` をビルド時に直接importしているため、除外すると本番ビルドが壊れる。
- **`package.json` の修正**：`tailwindcss`／`postcss`／`autoprefixer` を `devDependencies` から `dependencies` へ移動（本番ビルドで見つからず失敗していたため）。
- **Vercel本番デプロイ**：ユーザー確認により成功。

現状の `.github/workflows/`：`ci.yml`（push/PR時に `npm ci`→`npm test`→`npm run build` のみ実行、デプロイは行わない）と `backup.yml`。デプロイは Vercel 側のGit連携が担う。

---

## 3. 実行したテストの結果

```
npm test      → 全ブロック「失敗:0」。✗（失敗マーク）出力なし。全件 Pass。
npm run build → ✓ Compiled successfully（終了コード0）。
                既存の ESLint 警告（<img> の使用・useEffect 依存配列不足）のみ。
                新規エラー・新規警告なし。
```

---

## 4. 判明した注意事項・仕様書との相違点

1. **統合ファイル§0「記録欄5つが手つかず」は、現状のコードと一致しない**
   統合ファイル（9月11日付）は「食べ終えた時刻・就寝時刻の欄」「食事の印8つ」「要配慮個人情報の同意画面」「過去の日付で入れる経路」「C1（出なかった日の前3日）」の5点を“手つかず”としているが、リポジトリを確認したところ以下はいずれも実装済みだった。
   - 食事の印8つ：`lib/mealMarks.js`（テスト `components/tests/meal-marks.test.js` あり）
   - 食後〜就寝の間隔：`lib/analysisFamilies.js` の `reflux` 族に `mealToBedGap` が登録済み
   - 同意の門：`lib/consentGate.js`（撤回時に新規記録を止める設計、実装済み）
   - C1「さかのぼる」：`lib/lookBack.js`（前の夜・前の日・2日前を表示、ゲートなしで1日目から動く設計）
   - 過去の日付で入れる経路：`components/VocalTracker.jsx` の `<input type="date" ... max={todayISO()}>` に `min` 制限なし。selectedDate を過去日に変更して保存する経路が既にある
   → **次の担当者へ**：この5点が「いつ実装されたか」の記録がリポジトリ内に見当たらない（統合ファイルの日付より前から存在していた可能性がある）。Opus／坂本さんに実装済みである旨を伝え、統合ファイル§0の前提を更新してもらう必要がある。

2. **統合ファイル§5の「統計判定の食い違い」は解消済み**
   `lib/displayGates.js` に `NARRATIVE_MIN_EFFECT_SIZE = 0.50`・`EFFECT_SIZE_STEP = 0.12`・`NARRATIVE_FDR_Q = 0.10`（BH-FDR）が実装され、`lib/compareOrder.js` に固定順序法（1番目のみ補正なし、2番目以降は非開示）も実装済み。実行ルートv5とB01の食い違いは、しきい値0.50＋0.12刻みへの統一（2026-09-11付コメントによる）で解消されている。

3. **`docs/opus/` に「台帳-保留していること」の第5版が存在**
   統合ファイルは第4版までの内容を前提に書かれているが、`docs/opus/台帳-保留していること（9月11日・第5版）.md`（同日16:57作成、第4版は13:56）が既に存在する。第5版ではB14（レッスンの時刻・場所を後から直す）が追加され、A13（サーバの決まりを`has_can`へ移す）がA3より先と明記されている。**次の担当者は統合ファイルではなく第5版を優先して参照すること。**

4. **Vercelデプロイは自動連携方式に統一済み**
   GitHub Actions経由でのデプロイ（`vercel-token`等をsecretsで渡す方式）は行っていない。今後 `VERCEL_TOKEN` 等をGitHub Secretsに設定する作業は不要（Vercel側で完結する）。

---

## 5. 次に実行すべき具体的タスク（優先度順）

統合ファイル§3・第5版台帳§1（★いま〜10月19日）に基づく。

### 優先度0（9月15日まで・日付固定）
1. 上記4-1の食い違いをOpus／坂本さんに報告し、統合ファイル§0の前提を確定させる（記録欄が本当に手つかずか、既実装分を除いた残作業は何か）
2. 権限の総当たり確認（第5版では10役職×12画面＝120通り、A3）
3. 請求の計算を確定文書に合わせる（A1／A2）
4. 売り文句と画面の突き合わせ（A4）
5. 4つの状態の走査：空／読込中／失敗／オフライン（A5）
6. サーバの決まりを`has_can`へ全面移行（A13。§2の統計と異なり**A3より先**に必要）
7. おうちのポイントが現金で買えるか、の確認（v5 §1①・未回答）
8. Woolsongの名前を4か所押さえる（坂本さん・10分・無料）

### 優先度1（実装待ち・UI統合）
9. `ReviewPanel.jsx`／`NotePanel.jsx`／`MorePanel.jsx` を `VocalTracker.jsx` に統合
10. `TopBar`／`SideNav`／`WallBar` の作成・統合
11. iPadレスポンシブ対応（`@media` 768px/1024px）

### 優先度2（B13の4条件が揃うまで着手しない）
12. 教室・組織・運営（D＋E＋F＋G＋H・28画面）は、比較画像がそろい・坂本さんが目で見て・差分0（または残すと決めた差分のみ）・A3とA4完了、の4条件がすべて揃うまで着手しないこと（第5版§2-1）

---

**作成日**：2026年9月12日
**次の担当者へ**：着手前に必ず統合ファイルではなく `docs/opus/台帳-保留していること（9月11日・第5版）.md` を確認すること（本書§4-3参照）。
