# ★門下の 帯 ── ★なぜ ちがう 字が 出たか

★全69行 ／ 末尾は「★★②は 見える 範囲の 話 です。★こちらでは 決めません。」

★2026-09-18 ／ ★`tools/monka_read_banner_check.py` が 書きました。

## ★答え

★**古い 版では ありません。**★別の ところの、★別の 字 です。

★2つ の 断りが、★別々の 門で、★別々の 場所に 出ます。

| | 裁定 その87 の 帯 | ご覧に なった 断り |
|---|---|---|
| どこ | 画面の いちばん 上 | 門下を 開いた あと の 本文の 上 |
| 行 | `components/Renraku.jsx:255` | `components/Renraku.jsx:172` |
| 門 | `monka_read` を 持つ こと | `role` が owner か admin（★役割の 名） |
| いつから | 2026-09-18（裁定 その87） | 2026-09-10（見本③） |

## ★なぜ 新しい 帯が 出なかったか

★★★`monka_read` を 持つ 役職が、★台帳に **0件** です。

★数えた 役職 …… 10件

★これは 正しい 姿 です（★裁定 その77 ──「普段は 切って あります」）。
★★持って いない ので 出ません。★壊れて いるのでは ありません。

★配備も 届いて います（★`確かめられる 役職です` を 本番で 確かめました）。

## ★★★見つけた こと ── ★古い 断りの 門が、★役割の 名の まま です

★`components/Renraku.jsx:170` ── `!canWrite && (role === "owner" || role === "admin")`

★★裁定 その76・その77 で、★門下を 読むのは `monka_read` に なりました。
★★この 断りだけ、★2026-09-10 の 形（役割の 名）の まま です。
★★★だから、★`monka_read` を 持たない 学長・事務長にも 出ます。

## ★★★もう 1つ ── ★`renraku_all` が 門下の やりとりを 開けます

★台帳の 決まり（`org_messages` を 読む）は 2本 です。

- `org_messages_select`
  - `((auth.uid() = teacher_id) OR (EXISTS ( SELECT 1`
- `org_messages_select_monka_read`
  - `has_can(org_id, 'monka_read'::text)`

★★`org_messages_select` の 3つ目の 枝 ── `has_can(org_id, 'renraku_all')`。
★★`renraku_all` は「学校全部へ **お知らせを 出す**」できこと です。
★★★それで、★門下の やりとりも 読めて います。

★いま `renraku_all` を 持つ 役職 …… **7件**
★いま `monka_read` を 持つ 役職 …… **0件**
★台帳の お便り …… 全部 1件 ／ うち 門下の もの 1件

★★★これは 裁定 その76 の 筋 と ちがいます ──
★★「事故・苦情の ときだけ、`monka_read` を 入れる」「開いた 記録が 残る」。
★★`renraku_all` で 読んだ ぶんには、★その 記録が 残りません。

## ★お決めを お願いしたい こと

★① 古い 断り（`Renraku.jsx:172`）を どう するか
★　㋐ 門を `monka_read` に 揃える　㋑ 消して 新しい 帯に 1本化する　㋒ そのまま

★② `org_messages_select` の `renraku_all` の 枝を どう するか
★　㋐ 門下の もの（teacher_id が 入って いる 行）は `monka_read` だけ に する
★　㋑ そのまま（★お知らせを 出す 方は、★門下も 読める）

★★②は 見える 範囲の 話 です。★こちらでは 決めません。
