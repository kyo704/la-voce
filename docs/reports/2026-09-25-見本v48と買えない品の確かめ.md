# ★見本 v48 の 取り込みと、★3つの 確かめ
★2026-09-25。
★出どころ `woolsong-2026-09-21_6.zip`（★3,191,363バイト・★9月25日 08:55 受領）。
★展開先 `docs/opus/pack-2026-09-21_45/`。

## ★1 見本の 直しを 入れました
```
  00-動く見本（さわれる・全画面）.html   ちがう（新 719,633 ／ 旧 719,611） → 写しました
  00-動く見本-iPhoneで開く用.html       ちがう（新 724,671 ／ 旧 724,649） → 写しました
  00-動く見本-PC・iPad（個人）.html      同じ
  00-動く見本-PC・iPad（運営）.html      同じ
  tools/prices.json                   同じ（v7 の まま）
  tools/price_check.py                ちがう → 写しました
```

★★直った ところ（★`プラン` の 1行 だけ）──
```
  旧  単品の 年額の 合計 14,200円
  新  単品の 年額の 合計 16,600円
      3,800円 おとく
```
★★16,600 ＝ つたえる6,000 ＋ しらべる4,800 ＋ よそおい5,800。
　★3,800 ＝ 16,600 − 12,800。★どちらも 合って います。

★★`design-v48.json` も 入りました（★v46・v47 も 同じ 束の 中に あります）。

## ★2 「4品 出して 3品 買えない」に なって いないか

★★★なって いません。

★★値段を 出す 画面は 2つ です。★どちらも 品は **1つ** です ──
```
  app/billing/page.js:106       PLANS.map(p => p.priceLabel)  → 580円／月　4,800円／年
                                PAID_FEATURES               → しらべる の 3つの できこと
  components/VocalTracker.jsx   PLAN_FREE_ROWS（無料の 行）
      29074行                   paidRows()                  → しらべる の 3行
      29029行                   monthlyPriceLabel(PLANS)    → 580円／月
```

★★`PLANS` の 鍵は `monthly` と `annual` の 2つ です。
　★★これは **1品の 2つの 払い方** で、★2品では ありません。

★★5品の 年額の 表（`YEARLY_PRICE_155`）は、★画面から 呼ばれて いません ──
```
  lib/plans.js:177        置いて ある ところ
  lib/yearlyRefund.js:24  呼ぶ ところ（★戻す 額の 計算・★画面では ない）
  components／app        ★1つも ありません
```

★★「準備中」「もうすぐ」「近日」も、★どちらの 画面にも ありません。

★★足した 見張り `components/tests/only-buyable-shown.test.js`（★22件）──
　★① 5品の 表を 画面から 呼んで いない
　★② 買えない 品の 名（つたえる・よそおい・ぜんぶ）が 画面に 出て いない
　★③「準備中」等を 書いて いない
　★④ 5品の 表は `yearlyRefund` だけが 使う
　★目盛り合わせ 4件 …… ★わざと 壊して 4つ とも 赤く なりました
★★★`PLANS` に 品を 足した 日は、★この 見張りが 赤く なります。★それで よい ──
　★足した なら 買える はず で、★特商法の ページも 同じ 日に 直す 引き金 です。

## ★3 `price-four-places.test.js` は 道具として 働いて いるか

★★★働いて います。★字の 上で 確かめず、★わざと 壊して 見ました ──
```
  一覧の 327番目 …… components/tests/price-four-places.test.js（★487本の 中）
  わざと 1か所 ずらす → ★NG(1) price-four-places  RESULT: NG（1件）
  戻す              → 487本 ／ 落ちた 0本
```
★★Opus の 注意（★「定義した だけで 呼んで いない」）に 当たって いません。
　★`tools/run_guards.py:23` が `components/tests/*.test.js` を すべて 拾います。

## ★★お返しする こと ── `tools/price_check.py` の 当たり 405件

★★写した ままで 回すと `RESULT: TOOL_ERROR（405件）` に なります。
　★★中身を 分けました ──
```
  BUNDLE_TOTAL  71件  ★ぜんぶ 過去の 束（docs/opus/pack-*）と 報告と 台帳
                      ★生きて いる 道での 当たり …… **0件**
  MISSING        1件  確定-学校の値段.md を リポジトリの 根で 探して います
  TOOL_ERROR     1件  見本を リポジトリの 根で 探して います
```

★★わけ ── `tools/price_check.py:15` が `PACK = dirname(tools/)` と 決めて います。
　★束の 中（`tools/` の 隣に 見本が ある）なら 合います。
　★リポジトリでは 見本は `docs/design/pack-final/` に あります。

★★★過去の 束を 直しては いけません ── ★あれは 入れた 日の 記録 です。
　★`/legal/` と `ruling-*` と `/reports/` は 除いて ありますが、
　★`docs/opus/pack-*` は 除かれて いません。

★★こちらでは 直して いません（★`ON_ERROR: 直さずに Opus に 返す`）。

## ★まだ 見て いない こと

★★見た目は 確かめて いません。★見本を 実際に 開いて 見て いません。