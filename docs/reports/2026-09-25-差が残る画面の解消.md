# ★差が 残る 画面の 始末
★2026-09-25。

## ★その人 ／ 型の中身 ── ★どちらも 当て先の 欠け でした
```
  型の中身  OpsPresets.jsx      → ＋ lib/lessonPresets.js    4件 → 0件  OK
  その人   OpsPeople.jsx（誤り） → OpsRoster.jsx＋lib/orgRoster.js＋OpsMonkaHito.jsx
                                              13件 → 9件  DIFF
```

★★`型の中身` の 4件 は、★ぜんぶ `lib/lessonPresets.js` の `EDIT_NOTES` に ありました。
　★見本は `<b>` で 途中を 太く します。★実装は 配列と `bold` に 分けて います。
　★同じ 文 です。★当て先に lib を 入れて いなかった だけ でした。

★★`その人` の 当て先も ちがって いました。★`OpsPeople.jsx` は **役職を 渡す** 画面 です。
　★見本の `その人` は **名簿の 中の ひとり** です。★在籍の 様子・出席・見られないもの。

★★残る 9件 ──
```
  レッスンの 出席（この3か月）
  休講（この回を しなかった）
  在籍の様子
  ✓ いま これです ／ これに する ›
  変えた記録が残ります（誰が・いつ・何から何へ）。あとから戻せます。
  見られないもの
  ％を 出しません。実数だけ。健康に関するものは 1つも ありません。
```
★★ひと かたまり です ── ★**在籍の 様子を 選ぶ ところ**（3つの 様子と、変えた 記録）。
　★まだ 作って いません。

## ★年をたずねる ── ★0件に なりました
```
  当て先 VocalTracker.jsx（誤り） → components/SignupForm.jsx ＋ lib/ageGate.js
  6件 → 0件  OK（★足した 1件・わざと 7件）
```

★★足した もの ── 「生まれた日は うかがいません。」（9か国語）
　★★これは **集めない** と いう お約束 です。★帯しか たずねません。
　　★書いて いないと、★いずれ 生年月日を 足したく なった とき 誰も 止めません。

★★★書か**なかった** もの 2つ ── ★どちらも 嘘に なる から です。

★★①「保護者の方に ひとこと いただきます」
　★`lib/ageGate.js:287`「★★もう 1つの 道（㋑ 保護者の 同意を 取る 仕組み）は 作りません」
　★15〜17歳の 方は、★いま 同意なしで 登録できます（`maySignUp` は 15歳未満だけ 弾きます）。
　★★取って いない 同意を「いただきます」と は 書けません。

★★②「変えたときは、そのときの ことも 記録に 残します」
　★`ageBandToProfilePatch` は `age_band` と `age_band_answered_at` を **上書き** します。
　★前の 帯は どこにも 残りません。★いつ 答えたか だけ 残ります。
　★★戻せない ものを「記録に 残します」と は 書けません。

★★ほかの 5件は 形の ちがい です。★見本は 1枚の 画面、★実装は 登録の 紙の ひと枠。
　★★見本の 中に 注記が あります ──
　　「★実装が正（2026-09-24 の判断）: 登録するときに たずねます。15歳未満は 登録を
　　　お断りする 仕組みと つながって いるため、『教室に 入るとき』に ずらすと
　　　その守りが 外れます」
　★ぜんぶ `tools/excluded_by_design.json` に 引き金つきで 置きました。

## ★プラン ── ★止まって います（★値段）
```
  当て先 lib/planScreen.js（欠け） → ＋ VocalTracker.jsx ＋ lib/plans.js
  38件 → 26件  DIFF
```

★★値段が 合いません ──
```
  tools/prices.json（v7・2026-09-24）  ぜんぶ 月980／年12,800　学生 月490／年6,400
  見本                        同じ
  lib/plans.js                月580／年4,800  ★★★古い
```

★★★これは 値段の 打ち直し では 済みません ──
　★見本は **4つの 品** です（ぜんぶ／つたえる／しらべる／よそおい・学生は 半額）。
　★`lib/plans.js` は **2つ** です（月ぎめ／1年分）。★組み立てが ちがいます。

★★止まって いる 理由 ── ★Stripe の Price が 要ります。
　★`.env.local` に `STRIPE_PRICE_ID` の 行が **1つも ありません**。
　★★入れ替えると、★お支払いの 画面が その場で 壊れます。

★★いま お金を 払って いる 方 ── ★本番の 台帳を 数えました ──
```
  active 2件
    kyo0703opera@gmail.com          2026-10-05 まで
    kyosakamoto0703+g4t2@gmail.com  2027-09-04 まで
  none  47件
```
★★どちらも 坂本さんの ご自分の ところ です。★ほかの 方は いらっしゃいません。
　★★それでも 外に 向く ことなので、★こちらでは 触りません。

## ★自分のコマ ── ★止まって います（★かけもち）
```
  25件 DIFF
```
★★仕組みは あります ── `periodsOf` は コマが 無ければ `DEFAULT_PERIODS`（学校の 基本）を
　★返し、★`isOwnPeriods` が「学校の まま／自分で 決めています」を 分けて います。

★★足りないのは **かけもちの 言い方** です ──
```
  どこにも 属さない
  いまは どの 学校からも 見えません
  ほかの 学校とは 混ぜません
  事務の 側は、これを 見るだけ です。直せません。
```
★★1つも 品の 中に ありません。★かけもちは まだ 作って いません。

★★ほかに、★確かめて いない 動きが 2つ あります ──
　★「学校の 基本を 変えても、自分で 決めた コマは 動きません（1回だけ 知らせが 来ます）」
　★「事務の 側からは 見えますが、直せません」
　★★動きを 確かめる 前に 文を 書くと、★年をたずねる と 同じ 嘘に なります。

## ★数の 動き
```
  A群 …… 67 → 69（その人・型の中身 が 戻りました）
  差が 残る …… 5 → 3（年をたずねる・型の中身 が 0件に）
    その人    9件  ← 在籍の 様子を 選ぶ ところ（作って いない）
    プラン    26件 ← 値段・Stripe（坂本さんの 決め 待ち）
    自分のコマ 25件 ← かけもち（作って いない）
```