# ポイントとよそおい ── 4点の確認

全152行 / 末尾は「先に直すと、★また書き替えることになります。」

読んだ日：2026-09-07 ／ 読んだもの：いま main にあるコード
★これは「設計はこうなっている」ではなく、「コードがこう書いてある」の報告です。

---

## ① 記録だけで、ひと月に何点たまるか → ★毎日書けば 150点

`lib/character.js:21` `DAILY_POINTS = 5`
2026-08-27 以降は、★その日に何か書けば、中身を問わず一律5点です。
項目を増やしても増えません（かんたん記録の方が不利にならないように、そう直しました）。

| 月に記録した日数 | ひと月のポイント |
|---|---|
| 30日（毎日） | **150点** |
| 20日（3日に2日） | 100点 |
| 15日（2日に1日） | 75点 |

このほかに、初めて使った項目のごほうびが★10種 × 3点 ＝ 30点。★一度きりです。

**では、150点で何が受け取れるか。**

いまお店にあるのは101点で、そのうち★着られるものは22点です（帽子6・服10・小物6）。
値段は5点から500点、★着られるもの22点の中央値は95点、平均は133点でした。

| ひと月ぶん | 安い順に受け取れる、着られるものの数 |
|---|---|
| 150点（毎日） | **8点** |
| 100点（20日） | 7点 |
| 75点（15日） | 6点 |

真ん中あたりの値段（95点）で見ると、★毎日書く方は、ひと月に**1〜2点**です。
安いものから取れば8点ですが、5点の麦わら帽子や8点の割り箸が並びます。
ドレス（180点）・燕尾服（180点）・王冠（400点）は、★ひと月では届きません。

★★お答え：「¥580 ＝ 月1点」は、★無料の方の実力（毎日書いて1〜2点）と、ほぼ同じです。
　★特別さがありません。★このままでは、お金を払う理由になりません。

★ただし、ここには大きな但し書きがあります。★④をお読みください。

---

## ② ポイントは、現金で買えるか → ★買えません（★9月7日に、さらに塞ぎました）

道が1本もありません。

・残高は `computeBalance(entries, pointsSpent)`。足す側は記録だけです。
・Stripe の webhook に points / wardrobe / inventory の語が★0件。
・`checkout` は `mode: "subscription"` のみ。単発決済は★リポジトリ全体で0件。
・`lib/freeTier.js` の `NEVER_PAID` に「羊とおうち・装いの表示」が入っています。

★★9月7日、★お金とは別の弱いところを1つ塞ぎました。

「使ったぶん」（`profiles.character_points_spent`）を、★ブラウザから直に書けていました。
この数を小さく書けば、★記録しないままポイントが増えます。★現金は関わりません。

直したこと：

1. 受け取る処理を、サーバへ移しました（`app/api/character/buy/route.js`）。
   ★受け取るのは品物の名前だけです。★値段は、サーバが引きます。
   ★値段を送らせません。★送らせると、0円で買えます。
2. 列そのものを、★2枚で守ります（Opus の決め）。
   ・`supabase/2026-09-08-profiles-サーバ側だけの列をトリガーで守る.sql`（8列に）
   ・`supabase/2026-09-08-profiles-列ごとの権限.sql`（4か所に追加）
3. 見張りを足しました（`components/tests/profile-server-only-columns.test.js`）。
   ・8列すべてが、一覧・トリガー・権限SQLの3つにそろっているか
   ・★画面の側から、その8列を書こうとしていないか
   ・その探し方が、★本当に見つけられるか（わざと当たる文字列で確かめます）

★★この見張りが、★すでに1件見つけました。
　`article_notes` の `deleted_at` を `profiles` と読み違えたのは★私の探し方の誤りで、
　表を見ずに列名だけで探していました。★表を見るように直しました。

★★SQL は2本とも、まだ流されていません。★デスクトップに置いてあります。

---

## ③「よそおい多め」（¥980）は、数か、期間か → ★9月7日に、期間へ書き替えました

**コードには、影も形もありません。** MORE_MONTHLY・よそおい多め・よそおい放題・980 は
`.js` `.jsx` で★0件。`PLANS` は月580円と年4,800円の2つだけです。

**文書は「毎月3点」＝個数の形でした。★書き替えました。**

`docs/opus/lavoce-価格と課金の正（9月6日・確定）.md` の3か所（§1の表・§3・§8）：

    その月のあいだ、装いを、1日1点ずつ、自由に選べます

あわせて、実装のときの決めを、★同じ文書に書き込みました。

・★「あと1点」「残り◯点」を、★どこにも出さないこと
・★使わなかった分を、★翌月へ持ち越さないこと
・★1日に1点まで。★貯まりません
・★受け取ったものは、解約しても残ります（これまでどおり）

★Stripe の商品説明にも「◯点」と書かないこと、も書き添えました。

---

## ④ 219点のうち、いまポイントで受け取れるのは何点か → ★0点です

★★ここが、いちばん大事なところです。

**新しいよそおいは、219点あります。**（`docs/assets/sheep-items-index.json`）

★文書には「決定版11・217点」とありますが（`lib/sheepItems.js:8`）、
　★実際の json は `version: 2026-09-05k` で、★219点でした。★2点ちがいます。

内わけ：era 60／world 39／opera 38／armor 24／accessory 17／stage 15／
business 10／shoes 8／season 4／wafuku 4。

**この219点には、値段が1つも付いていません。**
`cost` も `points` も `price` も、★219点すべてに★ありません。

**そして、持ち物の確かめが、ありません。**
`components/WardrobePanel.jsx:39` は `owned` を受け取っていますが、★一度も読んでいません。
`wear()`（:63）は、★押せば、そのまま着ます。
達成で開く5点（`UNLOCKS`）も、★薄く（opacity 0.45）表示されるだけで、★着られます。

★★つまり、いまの実装では、★219点ぜんぶが、★誰でも、★ただで着られます。
　★ただし `WARDROBE_PUBLIC = false` で、
　　`NEXT_PUBLIC_WARDROBE_USER_IDS` に入っている方にしか出ていません。

**古いお店の22点（帽子・服・小物）とは、別のしくみです。**
あちらにはポイントの値段が付いていて、①の表のとおりです。
新しい219点と、古い22点をつなぐコードは、★ありません。

---

## ①のご判断のために

「¥580 ＝ 月1点」を決める前に、★219点をどう配るかが、先に決まっている必要があります。

　・ポイントで受け取る形にするのか（古い22点と同じ）
　・プランに付ける形にするのか（月1点・その月は自由、など）
　・その両方か

いまは★どちらでもなく、★全部ただで着られる状態です。
★この状態のまま公開すると、★あとから有料にするのは、
　「受け取ったものを取り上げない」に、まっすぐぶつかります。

★★ここは、私が決めるところではありません。★ご判断をお待ちします。

---

## まだ手を付けていないこと

特商法のページの「よそおい 月1点」は、★①のご判断が決まってから直します。
先に直すと、★また書き替えることになります。
