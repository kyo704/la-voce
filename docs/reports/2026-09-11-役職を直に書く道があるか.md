# ★役職（post）を 直に 書く 道が あるか

★この 紙は tools/direct_post_writes.py が 書き出します。★手で 書いて いません。

## post_id

| 場所 | どこ | 行 |
|---|---|---|
| `app/api/org/posts/route.js:131` | ★この 経路の 中 | `.update({ post_id: top.id }).eq("org_id", orgId).eq("user_id", user.id);` |
| `app/api/org/posts/route.js:172` | ★この 経路の 中 | `.update({ post_id: null }).eq("org_id", orgId).eq("user_id", targetUser);` |
| `app/api/org/posts/route.js:246` | ★この 経路の 中 | `.update({ post_id: postId }).eq("org_id", orgId).eq("user_id", targetUser);` |

★経路（`app/api/org/posts/route.js`）の **外**に ある もの　**0 か所**

## org_posts

| 場所 | どこ | 行 |
|---|---|---|
| `app/api/org/posts/route.js:112` | ★この 経路の 中 | `const { data: made, error } = await admin.from("org_posts").insert(rows).select("id, nam` |
| `app/api/org/posts/route.js:146` | ★この 経路の 中 | `.insert({ org_id: orgId, name, perms: {}, sort_order: 999 });` |
| `app/api/org/posts/route.js:192` | ★この 経路の 中 | `const { error } = await admin.from("org_posts").update({ name }).eq("id", postId);` |
| `app/api/org/posts/route.js:214` | ★この 経路の 中 | `const { error } = await admin.from("org_posts").update({ perms: next }).eq("id", postId)` |
| `app/api/org/posts/route.js:262` | ★この 経路の 中 | `const { error } = await admin.from("org_posts").delete().eq("id", postId);` |

★経路（`app/api/org/posts/route.js`）の **外**に ある もの　**0 か所**

## memberships（ほか）

| 場所 | どこ | 行 |
|---|---|---|
| `app/api/org/invitation/accept/route.js:11` | ★★経路の 外 | `//       supabase.from("memberships").insert({ org_id, user_id, role: "teacher" })` |
| `app/api/org/invitation/accept/route.js:126` | ★★経路の 外 | `.insert({ org_id: orgId, user_id: user.id, role });` |
| `components/VocalTracker.jsx:10956` | ★★経路の 外 | `.insert({ org_id: org.id, user_id: userId, role: "owner" });` |
| `components/VocalTracker.jsx:11737` | ★★経路の 外 | `.update({ grade_label: value })` |
| `components/VocalTracker.jsx:11980` | ★★経路の 外 | `.update({ role: newRole }).eq("id", membershipId).select("id");` |

★経路（`app/api/org/posts/route.js`）の **外**に ある もの　**5 か所**

## ★答え

★`memberships.post_id` も `org_posts` も、
★**`app/api/org/posts/route.js` の 外から 直に 書いて いる 道は ありません。**

★★だから 50通りの「役職と 所属」10マスは、
　★★**私が、★画面の 通らない 道を 試した もの**です。
　★★不具合では ありません。

## ★★ただし、★別の ことが 見つかりました

★`memberships` の **ほかの 列**を、★ブラウザから 直に 書いて いる 道が
★**5 か所** あります。

- `app/api/org/invitation/accept/route.js:11`　`//       supabase.from("memberships").insert({ org_id, user_id, role: "teacher" })`
- `app/api/org/invitation/accept/route.js:126`　`.insert({ org_id: orgId, user_id: user.id, role });`
- `components/VocalTracker.jsx:10956`　`.insert({ org_id: org.id, user_id: userId, role: "owner" });`
- `components/VocalTracker.jsx:11737`　`.update({ grade_label: value })`
- `components/VocalTracker.jsx:11980`　`.update({ role: newRole }).eq("id", membershipId).select("id");`

★★50通りで、★`memberships` への UPDATE は こう 返って きました ──
　★`42501　permission denied for table memberships`

★★あれは `post_id` を 変えようと した ときの 返事です。
　★★`role` や `grade_label` にも 同じ ことが 起きるか どうかは、
　★★**まだ 確かめて いません**。★列ごとの 許しは 列ごとに ちがいます。

★★もし 同じなら、★この 2つの 働きは いま 動いて いません ──
　★「人の 役職（role）を 変える」
　★「学年の 札（grade_label）を 直す」
★★確かめる 一手が 要ります。★まだ 何も 直して いません。

