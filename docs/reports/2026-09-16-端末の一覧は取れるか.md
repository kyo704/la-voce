# 端末（セッション）の 一覧は 取れるか
全102行 / 末尾は「　★★作る ときは、そこまで 含めて お決め ください。」

★出どころ　坂本さん（2026-09-16）／★台帳 ㉚ の 引き金
★REPORT_ONLY ── 作って いません。1行も 足して いません。

## 答え（いまの ところ）

★**ライブラリからは 取れません。** ★台帳から なら、**たぶん 取れます。**
★★けれど、★**たぶん** で 引き金を 引きません。★問いを お渡しします。

---

## ① ライブラリ ── ★取れません（★手元の 書物で 結べます）

★`@supabase/supabase-js@^2.45.0` の `auth.admin` に 生えて いる 手を、
★そのまま 数えました（★node_modules/@supabase/auth-js/dist/module/GoTrueAdminApi.d.ts）。

| | |
|---|---|
| 手の 数 | 8 |
| 「一覧」の 手 | `listUsers` |
| 名に session を 持つ 手 | **ありません** |
| `User` の 姿に `sessions` | **ありません** |
| `User` の 姿に `last_sign_in_at` | 在り |

★★`signOut(jwt, scope)` は 在ります。★けれど これは **切る** 手 です。
　★しかも、★切りたい その 端末の jwt を **もう 持って いる** ことが 要ります。
　★★**数えられません。** 数えられない ものを「（2台）」とは 書けません。

★★`listUsers()` は 人の 一覧 です。★端末の 一覧では ありません。

---

## ② 台帳 ── ★たぶん 取れます。★けれど 確かめて いません

★Supabase の 認証（GoTrue）は、★`auth` という 別の 棚に 表を 持って います。
★そこに `auth.sessions` が 在る はず です ── ★端末の 名乗りと、★最後に 使った 時刻。

★★**「はず」です。★私は 見て いません。**
　★★2026-09-15、★紙だけ 読んで `lessons` の 決まりを 逆に 報告しました。
　　★書物に 無い 決まりが、★台帳には 在りました。
　★★あれ以来、★紙で 結んだ ことは、★台帳に 聞いてから 言います。

★取れると したら、★道は これ 1つ です ──

```
public.my_devices()  ── SECURITY DEFINER
   auth.sessions から、auth.uid() の 行だけ を 返す
   revoke all from public, anon, authenticated
   grant execute to authenticated
```

★★この 家に 同じ 形が 2つ あります ── `get_student_entries`／`admin_entry_stats`。
　★だから 新しい 仕掛けでは ありません。

★★けれど、★これは **認証の 棚に 手を 入れる** ことです。
　★★この 家で いちばん 触って いない ところ です。
　★★私の 一存で 進める ものでは ありません。

---

## ③ お願い ── 問いを 1つ 走らせて いただけますか

★supabase/問い-端末の一覧が取れるか.sql

★**読むだけ** です。1行も 書きません。1つも 変えません。
★`BEGIN` / `ROLLBACK` を 使って いません（2026-09-15 の 決め）。

★4つ 聞きます ──

1. `auth.sessions` は 在るか。どんな 列を 持つか（★名乗り・時刻が 要ります）
2. いま 何行 あるか（★中身は 見ません。数だけ）
3. `anon` / `authenticated` から 見えて いないか（★見えては いけません）
4. すでに `auth.sessions` を 読む 関数が ないか

---

## ④ 結果に よって どう なるか

| ①の 答え | ㉚ は |
|---|---|
| 列が 在り、名乗りと 時刻を 持つ | ★引き金が **引けます**。作れる ように なります |
| 表が 無い／列が 足りない | ★3つめの 組（除外）の まま です |
| すでに 見えて しまって いる | ★★**別件**です。先に そちらを 閉じます |

★★③ が もし 出たら、★触覚より 先です。★すぐ お知らせ ください。

---

## ⑤ 決めて いただく こと

★取れると 分かった としても、★**作るかは 別の お決め** です。

★★1つ お伝えして おきます ── ★見本の「（2台）」は 数を 出します。
　★数を 出す なら、★**いま 何台 つながって いるか**を 数える ことに なります。
　★それは「見る」だけの 画面では ありません。
　★★見た 人は、★次に「★切りたい」と 思います。
　★★切る 手（`signOut`）は 在りますが、★jwt が 要ります ── ★持って いません。
　★★つまり「★見えるが 切れない」画面に なり ます。
　　★これも 押せない 札 に 近い 形 です。
　★★作る ときは、そこまで 含めて お決め ください。
