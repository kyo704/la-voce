# ★★★Stripe ── ★足りない ものの 中身（2026-09-23）

---

## ★★★① 足りない 環境変数 ── 4つ

★コードを 数えました（`grep -rn "process.env.STRIPE" app/ lib/`）。

| 変数名 | 用途 | 読む ところ | 無いと どう なるか |
|---|---|---|---|
| `STRIPE_PRICE_ID_MONTHLY` | ★月580円の 価格ID | `lib/plans.js` の `PLANS[0].envKey` → `priceIdFor()` ／ `lib/tiers.js` の 段の 見分け | ★お支払いの 道が **503** で 止まる（`checkout/route.js:79`）。★よそおいの 段も 決まらない |
| `STRIPE_PRICE_ID_ANNUAL` | ★年4,800円の 価格ID（★買い切り） | 同上（`PLANS[1].envKey`） | 同上 |
| `STRIPE_PRICE_ID_FULL` | ★よそおい「ぜんぶ」の 段（★月1,280円）の 価格ID | `lib/tiers.js` の `tierFromPriceId()` ／ `webhook/route.js:108,157` | ★買った あと、★**段が 決まりません**。★`purchases.tier` が null。★よそおいが 受け取れない |
| `STRIPE_WEBHOOK_SECRET` | ★Stripe からの 知らせが 本物か 確かめる 合言葉 | `webhook/route.js:28` `stripe.webhooks.constructEvent()` | ★知らせが **すべて 400** で 弾かれる。★お支払いが 済んでも `subscriptions` に 入らない |

★★★`STRIPE_SECRET_KEY` は 置かれました。★残りは この 4つ です。

★★★`STRIPE_WEBHOOK_SECRET` は Stripe の 画面で「受け口（Webhook endpoint）」を
　作った ときに 出ます。★受け口の 宛先は ──

```
https://<本番の 住所>/api/stripe/webhook
```

---

## ★★★② つたえるの PLANS ── ★どこに 何を 足すか

### いま ある もの

```js
// lib/plans.js
export const PLANS = [
  { key: "monthly", buyOnce: false, priceYen:  580, interval: "month",
    envKey: "STRIPE_PRICE_ID_MONTHLY", label: "月々のお支払い",  priceLabel: "580円／月" },
  { key: "annual",  buyOnce: true,  priceYen: 4800, interval: "year",
    envKey: "STRIPE_PRICE_ID_ANNUAL",  label: "1年分のお支払い", priceLabel: "4,800円／年" }
];
```

★★「つたえる」は **1つも ありません**。★だから、★Stripe に 値段を 作っても 買えません。

### 足す もの（★2つ）

```js
  { key: "tsutaeru",         buyOnce: true, priceYen: 6000, interval: "year",
    envKey: "STRIPE_PRICE_ID_TSUTAERU",          label: "つたえる（1年分）",
    priceLabel: "6,000円／年" },
  { key: "tsutaeru_gakusei", buyOnce: true, priceYen: 3000, interval: "year",
    envKey: "STRIPE_PRICE_ID_TSUTAERU_GAKUSEI",  label: "つたえる（1年分・学生）",
    priceLabel: "3,000円／年" }
```

★★★`buyOnce: true` に しました。★裁定180 は 年払いで、★返す 額を 月あたりで 決めて います
　（★500円／250円）。★`annual` と 同じ 形 です。★★ここは お確かめ ください。

### ★★★けれど、★これだけでは 足りません ── ★門が 年齢しか 見て いません

```js
// lib/minorBilling.js:67
export function offeredPlans(band) {
  if (band === AGE_BAND.ADULT) {
    return [PLANS.MONTHLY_INDIVIDUAL, PLANS.ANNUAL_INDIVIDUAL, PLANS.ORGANIZATION];
  }
  return [];   // ★18歳未満には 何も 売りません
}
```

★★`checkout/route.js:71` は これを 通ります ──

```js
if (!offeredPlans(band).includes(planKey)) return 403;
```

★★★つまり、★新しい 2つを `PLANS` に 足すだけ では、★**403 で 弾かれます**。

★★さらに「学生 3,000円」は、★裁定180 で こう 決まって います ──

```
①その人が どこかの契約校の名簿に「在籍中」として 載っている
②その人が 在籍を 確かめることに 同意している（sql/02 の 同意の仕組み）
★台帳を 見るだけで 自動で 決まる（★自己申告も 学生証の 確認も しない）
★学校には 伝わらない
```

★★★`offeredPlans` は **年齢の 帯**しか 受け取りません。★在籍を 見る 手が ありません。

★★お尋ね（★お裁きが 要ります）──

```
㋐ `offeredPlans(band, 在籍)` に して、★呼ぶ 側が 在籍を 調べて 渡す
㋑ ★台帳に 関数を 1つ 作り（例 `my_tsutaeru_price()`）、★画面と サーバは それを 呼ぶ

★私の 見立ては ㋑ です。
  ★裁定180 …「台帳を 見るだけで 自動で 決まる」。★決めを 台帳に 置く 形と 揃います。
  ★★学校に 伝わらない のも 守れます（★security definer の 中で 数える）。
  ★★★㋐ だと、★在籍を 調べる 処理が 画面と サーバの 2か所に できます。
```

---

## ★★★③ 揃った あと、★どう 確かめるか

```
★★★1 配り直す（★環境変数は 配る ときに 入ります）
★★★2 読むだけ の 確かめ
      STRIPE_SECRET_KEY=sk_test_… python3 tools/stripe_check.py
      → ① 5つの 環境変数の 有無　② 口が 試しか 本物か　③ 値段が prices.json と 合うか
★★★3 値段を 作る（★まだ 無ければ）
      STRIPE_SECRET_KEY=sk_test_… python3 tools/stripe_prices.py --ok
      → 同じ lookup_key が あれば 作りません。★値が ちがえば **止まります**
★★★4 受け口（Webhook）を つなぐ
      Stripe → Developers → Webhooks → 宛先 https://<住所>/api/stripe/webhook
      拾う できごと … checkout.session.completed ／ customer.subscription.*
      → 出た `whsec_…` を `STRIPE_WEBHOOK_SECRET` に 置いて、★また 配り直す
★★★5 ★試しの 口で、★1回 買って みる
      ★カードは Stripe の 試しの 番号（4242 4242 4242 4242）
      ★確かめる ところ 3つ ──
        ・`subscriptions` に 行が 増えるか（★webhook が 届いて いる しるし）
        ・`purchases.tier` が 埋まるか（★STRIPE_PRICE_ID_FULL が 効いて いる しるし）
        ・退会の 道が 止まるか（★`paymentActive` の 409）
★★★6 本物の 口は、★5 が ぜんぶ 通って から
```

★★★5 は **私からは できません**。★お支払いの 画面を 人が 開く 必要が あります。
　★★試しの 口（`sk_test_`）なら 安全 です。★本物の 口では しません。

---

## ★いま はっきり して いない こと

```
★`REQUIRE_SUBSCRIPTION` が いま どちら か …… ★手元からは 見えません
  ★"true" なら、★お支払いの 無い 方は `/dashboard` に 入れません
    （app/dashboard/layout.js）
  ★★配り直す 前に、★ここを 確かめて ください。
    ★★★4つの 価格IDが 無い まま "true" に すると、
      ★★お支払いが できないのに 入れない、という 形に なります。
```
