# ★★★試しの 台帳に、★私の 知らない ものが 入って います（2026-09-23）

## いま

```
本番 …… 75本の 移行 ／ 表 124 ／ 関数 145
試し …… 同じ 土台 ＋ ★下の 6つの 関数・2つの 列・2つの しばりの ちがい
base_twin …… ★差 36件（★きょうの 昼まで 0件 でした）
```

★★★向きは **一方通行** です ── ★試しに だけ ある。★本番に だけ ある ものは 0 です。

---

## 何が 増えて いるか

```
★関数 6
    koen_copy_frame(p_from uuid, p_to uuid)
    koen_seed_simple(p_koen uuid, p_rows text[], p_slots text[])
    koen_sheet_export(p_koen uuid)
    koen_understudy_needed(p_session uuid)
    mark_closing_org()
    my_koen_schedule(p_koen uuid)

★列 2
    koen_rows.time_from   time without time zone
    koen_rows.time_to     time without time zone

★引き金 1
    organizations.organizations_mark_closing  → mark_closing_org()

★しばり 2（★中身が ちがう）
    koen.koen_kind_check
      本番 … opera / chorus / drama / orchestra / gala / chamber / band / dance / other
      試し … ★＋ recording ほか
    koen_slots.koen_slots_group_kind_check
      本番 … cast / orchestra / staff
      試し … ★＋ accompanist
```

---

## ★どこから 来たのか

```
★この 家の どの ファイルにも ありません
   grep -rl "koen_copy_frame" . --include=*.sql --include=*.md --include=*.js --include=*.py
   → ★1件も 当たりません

★試しの 移行の 記録にも ありません
   記録 …… 58本（★土台 11 ＋ 9/14以降 47）。★私の ファイルと 1つ 残らず 同じ です

★持ち主は postgres（★管理の 口 から 入って います）
```

★★★つまり ── ★**私 以外の どなたかが、★試しの 台帳に 直に 当てて います**。
　★試しの 台帳は 坂本さんが ご承認なしに 試せる 場所 です。★それ 自体は 決めの とおり です。

---

## ★何が 困るか

```
① 二重の 証し …… ★私は base_twin の「差 0件」を、★証しの 土台に して います。
     ★★差が 出ると、★どれが「本番と 同じ 形で 確かめた もの」なのか 言えなく なります。

② 上書き …… ★私が 同じ 名前の 関数を 試しに 入れると、★そちらの 作りかけを 消します。
     ★★きょう、★`set_member_post` を 何度か 入れ替えました。★近い ところ です。

③ 順番 …… ★`recording` や `accompanist` は、★本番の しばりでは **弾かれます**。
     ★★試しで 通った 画面が、★本番で 通りません。
```

---

## ★お願い（★決めて いただきたい こと）

```
㋐ その SQL を ファイルで ください
   ★`docs/opus/…/pack/sql/` に 置けば、★私が 本番に 当てられます。
   ★★いまの ままでは、★本番に 持って いけません（★字が どこにも 無い ので）。

㋑ それとも、★試しの ぶんは 捨てて よい ですか
   ★★捨てて よい なら、★私が 試しを 本番の 形に 戻します（★差 0件 に）。

㋒ 試しの 台帳を 触る ときは、★ひとこと いただけると 助かります
   ★★同じ 名前の ものを 入れ替えて しまわない ため です。
```

★★★**私は 何も 消して いません。** ★そのまま 置いて あります。
