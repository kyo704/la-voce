# design_4.zip（pack-final）52画面 ― いま どこまで できているか

全162行 / 末尾は「　 日は 過ぎると 取り戻せないので、ここだけ 順番と 別に 急ぎます。」

## 0. 数について

見本の 束（`docs/design/pack-final/screens/`）に 入っているのは、
「動く見本」を のぞいて **52画面** です。46ではありません。
A11／B4／C3／D6／E4／F8／G7／H3／J6 の 合計です。

## 1. どうやって 数えたか（★推測では ありません）

3つの 証拠を 見ました。

- ① その 画面を 描く **部品**（`components/*.jsx`）が あり、
  それが `VocalTracker.jsx` から **実際に 呼ばれている** こと。
  呼ばれていない 部品は、無いのと 同じです。
- ② その 画面の 決めごとを 持つ **`lib/` の 一枚**が あること。
- ③ その 画面を 見張る **試験**（`components/tests/*.test.js`）が あること。

★①だけの ものは「動くが、見本と 突き合わせて いない」状態です。
★これを ○ と 書きます。◎ は ③まで ある ものです。

★★お断り。
  ここで 言えるのは「**その 仕組みが 在るか**」までです。
  「**見本と ミリ単位で 同じか**」は、ここでは 分かりません。
  それは 坂本さんの 言われた 二段目の 精査の 仕事です。
  私は 画面を 描けず、寸法も 測れません。

## 2. 一覧

記号：◎ できていて 見張りも ある ／ ○ できている（見本との 突き合わせは まだ）
　　　△ 一部だけ ／ × 手つかず

### A群 ― 個人アプリ（11画面）

| | 画面 | 状態 | 根拠 |
|---|---|---|---|
|A01|きょう（生徒）|◎|`HomeV2` `TodayBand` ／ `a01-kyou.test.js` が 見本の CSS と 直に くらべています|
|A02|きょう（先生・出欠の帯）|○|`TodayBand` の 教える日の 枝。案B（2枚と みつけたことも 出す）で 実装|
|A03|記録（2タップ）|◎|`RecordV2Head` `lib/recordV2.js` ／ `a03-kiroku.test.js`|
|A04|ふりかえる／ならべる|○|`LookBackV2` ／ `look-back.test.js`|
|A05|ふりかえる／さかのぼる|○|`RangeCalendar` `lib/rangeCalendar.js` ／ `range-calendar.test.js`|
|A06|ノート|○|`NotesV2` `lib/notes.js` `lib/practiceNote.js` ／ `notes.test.js`|
|A07|ひつじ／ながめる|○|`CharacterHome`（全画面）`lib/roomCamera.js` ／ `room-camera.test.js`|
|A08|ひつじ／したく引き出し|○|`HomeDrawer` `WardrobePanel` ／ `home-drawer.test.js`|
|A09|ひつじ／さがす|○|`DrawerSearch` `lib/drawerSearch.js` ／ `drawer-search.test.js`|
|A10|歯車・もっと|○|`lib/moreMenu.js` ／ `more-menu.test.js`|
|A11|運営モード別のシェル|○|`OpsShell` `lib/opsShell.js` ／ `ops-shell.test.js`|

### B群 ― くらべる・かぞえる（4画面）

| | 画面 | 状態 | 根拠 |
|---|---|---|---|
|B01|くらべる（まだ出ていない）|○|`CompareV2` `lib/compareView.js` ／ `compare-view.test.js`|
|B02|くらべる（1文が出た）|○|同じ 部品の、値が 揃ったときの 姿。いまは まだ 誰にも 出ません（門が 開かないため。これは 設計どおり）|
|B03|かぞえる|○|`CountV2` `lib/countView.js` ／ `count-view.test.js`|
|B04|くらべる（本番の前後・お休み）|○|`LookBackV2` 156〜164行 ＋ `lib/quietDays.js`。「ならべる を見る／さかのぼる を見る」まで 在ります|

### C群 ― 本番でそろえる（3画面）

| | 画面 | 状態 | 根拠 |
|---|---|---|---|
|C01|本番でそろえる|×|重ねて 描く ところが ありません。「本番からの日数」という 横軸が、どこにも ありません|
|C02|まだ3回に満たない|×|「3回から」「いま 見られるもの」「つかえるもの」の いずれも ありません|
|C03|本番の前後（お休み）|○|`lib/quietDays.js` ／ `quiet-days.test.js`|

★C01・C02 は、坂本さんが **11月** と 決められた ぶんです。手つかずで 正しい 状態です。

### D群 ― 連絡（6画面）

| | 画面 | 状態 | 根拠 |
|---|---|---|---|
|D01|運営／連絡|○|`Renraku` `lib/renraku.js` ／ `renraku.test.js`|
|D02|運営／おしらせを書く|○|`AnnouncementCompose` ／ `notice-audience` `notice-batches`|
|D03|運営／門下をひらく|○|`Renraku` 215行に「開いた記録」／ `thread-guards.test.js`|
|D04|個人／ノート連絡|○|`Renraku` の 個人側|
|D05|個人／門下の連絡|○|同上|
|D06|個人／休むことを伝える|○|`TellTeacher` `lib/tellTeacher.js`|

### E群 ― パソコン・iPad（4画面）

| | 画面 | 状態 | 根拠 |
|---|---|---|---|
|E01|パソコン／連絡|○|`lib/renraku.js` の `BODY_WIDTH = 640` ＋ 2ペイン（決まりB）|
|E02|パソコン／おしらせを書く|○|`AnnouncementCompose`|
|E03|パソコン／開いた記録|○|`VocalTracker.jsx` 9402〜9415行。読んだ側にも 読まれた側にも 出します|
|E04|iPad たて／連絡|○|`Renraku` 43行の はば見（`innerWidth`）|

### F群 ― 運営（owner／admin・8画面）

| | 画面 | 状態 | 根拠 |
|---|---|---|---|
|F01|運営ホーム|○|`OpsHome`|
|F02|日程（先生を横に・PCだけ）|○|`lib/opsSchedule.js` の `dayGrid` ＋ `mayDragBlocks`（1024px以上）|
|F03|名簿・ご請求の人数|○|`OpsRoster` `lib/orgRoster.js` の `rosterCount` ／ `org-roster.test.js`|
|F04|行事|○|`OpsEvents` `lib/events.js`|
|F05|設定・ご請求|○|`OpsSettings` ＋ `monthlyFee` `setupFee` `yearlyFee`|
|F06|iPad 日程・出欠つけ（事務）|○|`layoutOf` の narrow。`staff` は 日程だけ（`tabsFor`）|
|F07|iPad 名簿たて|○|`OpsRoster`|
|F08|iPad 日程・見るだけ|○|`teacher` は 下タブ 空。読み取りだけ|

### G群 ― 教室（先生・7画面）

| | 画面 | 状態 | 根拠 |
|---|---|---|---|
|G01|教室ホーム|○|`OpsHome`|
|G02|名簿|○|`OpsRoster`。ただし さがす欄の 文言が 見本と ちがいます（下の ③）|
|G03|出欠つけ|○|`OpsSchedule`|
|G04|帯・1週間|○|`weekHeat`|
|G05|重なり|○|`overlapsOf`|
|G06|ご請求|○|`lib/orgRoster.js` の 料金の 段|
|G07|しぼり込み|△|「担当の先生で しぼる」の 面が ありません。いまは さがす欄に 先生名を 打つ形です|

### H群 ― よこ持ち（3画面）

| | 画面 | 状態 | 根拠 |
|---|---|---|---|
|H01|1日・先生をよこに|○|`dayGrid` ＋ `layoutOf` の wide|
|H02|1週間・濃さの地図|○|`weekHeat`|
|H03|よこ持ち（ほぼPCと同じ）|○|`WIDE_AT = 812` ＋ `orientationchange`／`ops-schedule.test.js`|

### J群 ― 羊のおうち（6画面）

| | 画面 | 状態 | 根拠 |
|---|---|---|---|
|J01|おうち／ながめる|○|`CharacterHome` 全画面 ＋ `InteriorLayer`|
|J02|おくスロット|△|置く 仕組みは あります（`InteriorPanel` `lib/sheepInteriorV2.js`）が、見本の **スロット**（かべ1・かべ2・まど・たな・つくえ・いす・ゆか）では なく、自由に 置く 形です|
|J03|かべ・ゆか・まど|△|`lib/homeDrawer.js` に かべ・ゆか・まどわく・そとのけしき・とびら は 在ります。見本の「かべ／ゆか／外の けしき」の 3つ組みの 面には なっていません|
|J04|たな（歌ってきたもの）|×|ありません。「はじめて 記録した日」「記録した日 ◯日 ／ 本番 ◯回」を 出す ところが どこにも ありません|
|J05|台帳（もっているもの）|×|ありません。手に入れた 日と、そのときの 数を 残す 仕組みが ありません|
|J06|まだ見えていないもの|×|ありません。伏せた 札と、手に入る きっかけの 3つを 出す 面が ありません|

## 3. まとめ

- ◎（見張りまで ある）　　　　　**2**：A01・A03
- ○（できている・突き合わせ前）**41**
- △（一部だけ）　　　　　　　　 **3**：G07・J02・J03
- ×（手つかず）　　　　　　　　 **6**：C01・C02・J04・J05・J06 ／ ★J04〜J06 は 3画面

数え直すと 52です。

## 4. 手つかず・一部の 6つ ＋ 3つ について、私の 見た 順番

★これは 私の 見立てです。順番は 坂本さんが お決めください。

1. **J04 たな（歌ってきたもの）** ―
   もう ある 材料（`repertoire_log` と 本番の 記録）を 並べるだけです。
   新しい 表は 要りません。いちばん 早く 出せます。
2. **J05 台帳（もっているもの）** ―
   「いちど あなたのものに なったものは、あなたのものです」を 目に 見える形に
   する 面です。★手に入れた 日を 残す 列が 要ります（SQLを お出しします）。
   ★★いまは、いつ 手に入ったかを 残していません。
   ★★★ここは 早いほど よいです。★きょう 手に入れた ものの 日は、
   　　きょうしか 分かりません。あとから 埋められません。
3. **J06 まだ見えていないもの** ― J05 の 裏面です。J05 の あとに 続けて。
4. **J02・J03（おうちの 面を 見本の 形に）** ― 中身は 在ります。並べ方の 話です。
5. **G07 しぼり込み** ― 名簿に 面を 1つ 足すだけです。
6. **C01・C02** ― 坂本さんの お決めで 11月。

★★2 の SQL だけ、先に お出ししてよろしいでしょうか。
　 日は 過ぎると 取り戻せないので、ここだけ 順番と 別に 急ぎます。
