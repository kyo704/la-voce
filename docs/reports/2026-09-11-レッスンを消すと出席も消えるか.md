# ★レッスンを 消すと、★出席も 消えるか

★この 紙は tools/delete_recreate_risk.py が 書き出します。★手で 書いて いません。

## ★まず、★消える か どうか

★★出席は `lessons` の **attendance 列** です。★別の 表では ありません。
★★だから 行を 消せば、★出席は **必ず 一緒に 消えます**。
　★★台帳に 尋ねる までも ありません。★列は 行の 一部です。

★★問いは そこでは なく ──「★その 道が、★いま 使われて いるか」です。

## ① ★lessons を 消す 道

★**1 か所** あります。

```
11367      fetchLessonsForLink(linkId);
11368    }
11369    async function handleDeleteLesson(lessonId, linkId) {
11370      const supabase = createClient();
11371      await supabase.from("lessons").delete().eq("id", lessonId);
11372      fetchLessonsForLink(linkId);
11373    }
```

## ② ★消して、★すぐ 作り直して いないか

★**ありません。**

★★消した すぐ あとで `lessons` に 入れ直して いる ところは、
　★★1か所も ありません。
　★★`handleDeleteLesson` は、★消して 一覧を 読み直すだけ です。

## ③ ★消す 札は、★どの レッスンに 出るか

```
16245                          <div className="space-y-1.5 my-3">
16246                            {studentLessons.filter((l) => new Date(l.scheduled_at) >= new Date()).
16247                              <div key={l.id} className="rounded-lg p-2 flex items-center justify-
16248                                <span>{new Date(l.scheduled_at).toLocaleString("ja-JP", { month: "
16249                                <button type="button" onClick={() => handleDeleteLesson(l.id, link
16250                              </div>
```

★★札が 出るのは、★`new Date(l.scheduled_at) >= new Date()` ──
　★★つまり **これから 先の レッスン だけ** です。
　★★過ぎた レッスンには、★消す 札が 出ません。

## ④ ★出席を 付ける 札は、★どの レッスンに 出るか

★★`lib/todayBand.js` の `buildBand` が `lessonsOn(…, todayISO, …)` を 使います。
　★★**その日の レッスン**が 並びます。★時刻では 絞って いません。

## ★★重なる ところ（★危ないのは ここだけ）

★★消す 札　……　これから 先の レッスン
★★出席の 札　……　その日の レッスン（★時刻を 問わない）

★★だから、★**その日の、★まだ 時刻の 来て いない レッスン**だけが、
　★★両方の 札を 同時に 持ちます。

★★そこで こう なり得ます ──
　★① 朝に、★10時の レッスンの 出席を 先に 付ける
　★② 9時半に、★その レッスンを ✕ で 消す
　★③ 付けた 出席は、★行ごと 消える。★何も 言われない。

★★過ぎた レッスンでは 起きません。★消す 札が 出ない ためです。

## ★いま 言える こと

★★「消して 作り直す」やり方は、★アプリの 中に **ありません**。
　★★だから「日程を 直す ために 消して いる」ことは、★起きて いません。
　★★お尋ねの「黙って 出席を 消して いる」形は、★この 道からは 出ません。

★★ただし、★**上の 重なりは 残ります**。
　★★✕ には 念押しが ありません。★何を 失うかも 出ません。
　★★`handleDeleteLesson` は 3行で、★出席が 入って いるか 見て いません。

★★人が 手で「消して、作り直す」ことも できます。
　★★その ときは、★同じ ことが 起きます。

## ★すぐ 打てる 手（★まだ 打って いません）

★㋐ ✕ を 押した とき、★出席が 入って いれば 念押しを 出す
　　★★`l.attendance` を 見るだけ です。★台帳を 触りません。
　　★★この家の 決め ──「★人が 書いた ものを 黙って 消さない」に 沿います。

★㋑ 出席の 入った レッスンには、★✕ を 出さない
　　★★消したい ときは、★先に 出席を 外して いただく。

★㋒ 当座の しのぎ ── ★消して 作り直す やり方を 使わない、と 申し合わせる
　　★★お尋ねに あった ものです。★ただ、★その やり方は
　　★★いま アプリの 中に 無いので、★申し合わせる 相手は 人の 手作業です。

★★どれも まだ して いません。★お決めを お待ちします。
