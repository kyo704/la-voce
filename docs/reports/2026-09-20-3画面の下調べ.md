# ★下調べ ── `P_kasaFix` ／ `P_okeru` ／ `P_monkaInvite`

★2026-09-20 ／ ★Opus が 設計書を 書く 前の 材料 です。
★見本は `docs/opus/visual-2026-09-19/pack`（第7版）、★台帳は 直に 数えました。

---

## ★1 見本の 中身

### ① `P_kasaFix` ── ★重なりを どこへ 動かすか（★見本 3810字）

    題 …… 「どこへ 動かしますか」
    添え …… 日・時・場所　／　★「動かすのは **あなたの ぶん** だけです」

    左（flex:3）…… ★週の 表（コマ × 曜日）。★1マスずつ 4つの 顔 ──
        `いま ここ` …… 重なって いる ところ
        `busy` …… ★ご自分の 予定（「自分」／「学校」の 2つを 分けて 出す）
        `mine` …… すでに 置いた レッスン（★お名前つき）
        `slot` …… ★空き（押せます）／★×（押すと
                   「○○さんは この時間 来られません」と 出ます）
    右 …… ★場所を 選ぶ ／ ★「ここに 動かす」

    ★★見本が 守って いる こと
        ★動かせるのは ご自分の ぶん だけ
        ★相手が 来られない ところは 押せない（★押すと わけを 出す）
        ★自動で 動かさない（★人が 選ぶ）

### ② `P_okeru` ── ★入れられる 枠の 一覧（★見本 951字）

    題 …… その方の お名前　／　添え「入れられる 枠　N」
    断り …… 「この方が **来られて**、まだ **空いている** 枠です。押すと、そこに 入ります」
    一覧 …… 枠 1つに つき 1行。★押すと その場で 入ります
    空の とき …… 「入れられる 枠が ありません。
                  自分の 予定を 外すか、コマを 足すと 増えます」
    下の 断り …… 「あなたの 予定が 入って いる 枠は 出しません」
                  「その 生徒が 来られない 枠も 出しません」

### ③ `P_monkaInvite` ── ★門下に 招く（★見本 2984字）

    2つの 道 だけ です。

    ①合言葉を 見せる
        8文字（★大きく・字間 広め）／★QR（読めない 端末の ため 字も 出す）
        「あと N人　／　（日）まで」／「作り直す」
        断り …… 顔合わせの ときに 手元の 画面で 見せる ／
                 人数に 達すると ひとりでに 閉じる ／
                 打ち間違いは **ひとり 10回**まで（★その人ごとに 数える）

    ②名簿から 招く
        ★学校の 名簿に いる 方 だけ が 出ます（★名簿の 外は 探せません）
        1行 …… お名前・学年・学科 ／ 右は 3つの どれか
            「招く」（押せる）／「招待中」／「入って います」
        断り …… 押すと その方の 画面に「招かれて います」と 出る ／
                 ★入るか どうかは ご本人が 決める

    ★★いちばん 下の 断り（★これが 設計の 芯 です）
        「★学生が 門下を さがす 仕組みは ありません。
          （さがせると、誰が どの先生に ついているかが 分かって しまう）
          入る 道は この 2つだけ ── 合言葉 か、招かれる か。
          どちらも、最後に **ご本人が 承知して はじめて** 入ります」

---

## ★2 いまの 台帳（関わる 表と 列）

### `lessons`（15列）
    id / link_id / scheduled_at / duration_minutes / note / created_by / created_at
    org_id / teacher_id / student_id
    attendance / attendance_at / attendance_by
    student_notice / student_notice_at
    ★2026-09-20 に 足しました …… `place_id`（`org_places`）／`kind`

### `assignments`（7列）
    id / org_id / teacher_id / student_id / started_at / ended_at / is_representative

### `teacher_invitations`（9列）
    code / teacher_id / expires_at / used_at / used_by_student_id
    org_id / monka_teacher_id / grade_year / division_id
    ★いまの 中身 …… 34行 ／ `monka_teacher_id` あり 20行 ／ 使われた 6行

### `my_timetable`（11列）
    user_id / weekday / period_id / title / teacher / room / memo / unavailable …
    ★★先生からは **1行も 読めません**（★台帳 08-11）。
      ★読めるのは「空いて いるか どうか」だけ（`get_student_free_slots`）。

### ★今日 作った もの
    `org_periods`（学校の コマ）／`org_places`（場所）

### ★無い もの
    `overlap_notices`（重なりの 知らせ）★`P_kasaFix` が 使います
    `lesson_slots`（置ける 枠）★`P_okeru` が 使います
    `monka_invitations`（門下への 招き）★`P_monkaInvite` の ②が 使います

---

## ★3 `monka_teacher_id` で 代われるか ── ★**半分だけ 代われます**

    ★★①合言葉の 道 …… ★**もう 代わって います**。★足す ものは ありません。
        `teacher_invitations.monka_teacher_id` に 先生を 入れて 合言葉を 作れば、
        ★その 合言葉で 入った 方は その 先生の 門下に なります。
        ★`app/api/enrollment/accept` が `monka_teacher_id` を 見て
          `assignments` を 作って います（★裁定 その83 の 訂正）。
        ★いま 20行が 使って います。★動いて います。

    ★★②名簿から 招く 道 …… ★**代われません**。★3つ 足りません。

        ㋐ 宛て先（誰を 招いたか）
            `teacher_invitations` は「合言葉を 作った」記録 です。
            ★誰に 出したかを しまう 列が ありません
              （`used_by_student_id` は **使われた あと** に 入ります）。
            ★だから「招待中」を 出せません。★見本の 3つ目の 顔が 作れません。

        ㋑ 1人に 1つ
            いまの 合言葉は「何人でも 使える 1つの 字」です。
            ★名簿から 招くのは「その方 1人 への 1通」です。★別の もの です。
            ★同じ 表で やると、★`used_at` が 1回で 閉じ、★ほかの 方が 入れません。

        ㋒ 相手の 画面に 出す こと
            見本 ──「押すと、その方の 画面に **「招かれて います」**と 出ます」。
            ★いまの 蔵に、★その 知らせを 置く ところが ありません。
            ★★`org_messages`（連絡）で 代わりに できます。★ただし
              ★連絡は「読む もの」で、★「承知する もの」では ありません。
              ★★承知の 押しどころを、★連絡の 中に 作る ことに なります。

    ★★★まとめ
        ★①だけ なら、★新しい 表は **要りません**。
        ★②まで 作るなら、★`monka_invitations`（宛て先つき・1人に 1つ）が 要ります。
        ★★あるいは `teacher_invitations` に 2列 足す 道も あります ──
          `invited_student_id`（宛て先）と `kind`（★合言葉 ／ 名指し）。
          ★★★ただし 1つの 表に 2つの 意味を 持たせる ことに なります。
            ★この 蔵で 何度も 痛い目を 見た 形 です（★`lessons` の `link_id` ／ `org_id`）。
            ★★私は **別の 表** を 推します。

---

## ★4 台帳98 で「10月に 要らない」と した 根拠（★再確認）

    ★出どころ `docs/ledgers/08-保留している決め.md` の 08-17 の いちばん 下 ──

        「★あと日
          ★重なりの 知らせ（`overlap_notices`）／★空き枠（`lesson_slots`）
          ★★裁定 その98 で「10月に 要らない」と 決まりました」

    ★★つまり 根拠は **裁定 その98 の お決め そのもの** です。
      ★台帳には「なぜ 要らないか」の 理屈が 書いて ありません。
      ★★★そこは 正直に お伝えします ── ★理由の 記録が ありません。

    ★★いま 分かる 範囲で、★要らなかった 筋（★推し当てでは なく、実装から）──
        ★`P_kasa`（重なりを 見る）は **出来て います**。★印を つける ところまで です。
        ★`P_kasaFix` は「動かす」画面 です。★動かす 相手（知らせ・返事）が
          要ります。★それが `overlap_notices` です。
        ★★★いまは「印を つける だけ。★自動で 動かさない」で 止めて います
          （★`lib/opsSchedule.js` の `OVERLAP_NOT_YET`）。
        ★`P_okeru` も 同じ 形 です ── ★`P_kumu`（組む）は 出来て いて、
          ★そこで 枠を 選べます。★`lesson_slots` は「枠を **しまう**」表 です。

    ★★★10月19日の 締切が 消えました（★実行ルート 第6版）。
      ★だから「10月に 要らない」という 線 そのものが、★いま 動いて います。
      ★★作るか どうかを、★もう一度 お決めいただく 場面 かも しれません。

---

## ★この 紙で して いない こと

    ★設計を して いません。★材料を 並べた だけ です。
    ★★`monka_invitations` の 列も 決めて いません（★Opus の お仕事 です）。
