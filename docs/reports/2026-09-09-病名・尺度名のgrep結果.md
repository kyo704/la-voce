# 病名・尺度名の grep ── 走らせた結果
全195行 / 末尾は「```」

2026年9月9日 ／ Code

---

# ★ 先に、いちばん大事な発見を1つ

**同意の文に、病名が残っています。**

```
　lib/consent.js:62
　　key   health.meal_sleep
　　label 食事と就寝の記録
　　text  「食事の内容と時刻、就寝時刻を保存し、★逆流に関わる分析に使います。」
```

**査読 §4-1 が「同意の文から 病名を 外します」と 名指ししている、その文です。**

**★ただし、私の判断では 直しません。ご指示をください。**
同意文は、**どの版に同意したかを記録に残している**もので
（`CONSENT_POLICY_VERSION`）、**すでに同意された方がいます。**
文を変えるなら**版を上げるか、上げないか**の判断が要ります。
**これは私が決めてよいことではありません。**

なお、見張り（`lib/refluxCare.js` の `FORBIDDEN_WORDS`）は
「逆流性食道炎／咽喉頭逆流／LPR」を見ていますが、**「逆流」の2文字は
入っていません。**だから、この文は素通りしていました。

---

# ① 多言語文書 ── **ご指定の3語は、0件でした**

**ご指定の grep、そのまま走らせました。**

```
grep -n -E "質問票|RSI|VFI|SVHI|EASE|公式の翻訳版|検証済みの尺度|独自に翻訳" \
     docs/lavoce-作業指示-多言語対応*.md
```

**当たったのは「質問票」の3件だけです。**

```
　38行　受診用サマリー ／ 質問票 ／ 書き出し・削除の画面
　219行　受診用サマリー（23）／ 履歴（8）／ 質問票（7）／
　295行　学ぶの枠 9／履歴 8／★質問票 7
```

**★RSI・VFI・SVHI・EASE・公式の翻訳版・検証済みの尺度・独自に翻訳 は、
　1件も ありません。**3件とも**画面の数を数えた表**で、**尺度の話ではありません。**

## ①-1 「完成.md（450行）」は、リポジトリに **ありません**

```
　find . -name "*多言語対応の完成*"　→ ★0件
　★あるのは 1つだけ　docs/lavoce-作業指示-多言語対応（伊英中）.md（307行・8月28日）
```

## ①-2 どちらが正本か ── **判断材料が ありません**

**比べる相手が 存在しないので、比べられません。**

```
　★（伊英中）.md　　307行　8月28日 22:22
　★完成.md　　　　　★ありません
```

**§10・§12 も ありません**（この文書は 0〜9 の 9節です）。
**450行の「完成.md」を、いただけますか。**届けば、その中で調べます。

---

# ② §4-2 の対応表 ── **半分は もう在り、半分は 空でした**

## ②-1 もう在るもの

```
　lib/freeTier.js　FEATURE_LABELS ＋ featureLabel(key)
　　★record／calendar／summary／compare … 12個
　　★出口3つが 通っています
　　　app/billing/page.js（2か所）／components/VocalTracker.jsx:19529
```

**「名前も、ここ1か所です。画面で書き直さないこと」と、すでに書いてあります。**

## ②-2 ★空だったもの ── `analysis.reflux` は、この表に **入っていません**

```
　lib/entitlements.js の FEATURES　16個
　　analysis.relations／analysis.range／analysis.cycle／★analysis.reflux／…
　★★この16個に、★表示名の 表が ありません
```

**★ただし、いま この鍵が 画面に出る所は 1つも ありません。**

```
　can(viewer, "analysis.reflux") …… ★出し分けに 使うだけ（17143行）
　CSV の見出し　　　　　　　　　　★0件
　保有データの一覧　　　　　　　　★0件
　メール　　　　　　　　　　　　　★0件
　同意の履歴　　　　　　　　　　　★鍵は health.reflux_care（★analysis.reflux ではない）
```

**つまり「出口4つに通す」の、通す先が いまは ありません。**
表だけ先に作ることはできますが、**呼ぶ所が無い表**になります
（★「作った関数は、必ずどこかから呼ばれているか」に反します）。

**★ご判断をください。**

```
　案A　★いま作らない。★鍵が画面に出る所ができたときに、そこで作る
　案B　★いま作る。★出口が無いことを注記に書いて残す
```

**私は案Aをおすすめします。**いま漏れているのは**鍵の名前ではなく、
同意文の日本語**（いちばん上）だからです。

---

# ③ Stripe ── **入っていません**

```
　app/api/stripe/checkout/route.js が Stripe に送るもの
　　line_items　　　[{ price: priceId, quantity: 1 }]　★価格IDだけ
　　metadata　　　　supabase_user_id ／ plan ／ age_band　★3つだけ
　★product_data も、★商品名を コードで作る所も、★1つも ありません
　★analysis.reflux などの 機能名は、★1文字も 出てきません
```

**商品名は Stripe のダッシュボードにあり、コードにはありません。**
**ダッシュボード側の商品名は、私からは見えません。ご確認をお願いできますか。**
（★もし病名が入っていても、**表示名だけ変えれば済みます。price_ の鍵は変えません。**）

---

# ④⑤⑥ ── **もとの文書が届いていません**

```
　④ §1の1〜6（「声がかれる原因のひとつ―胃からの逆流」など）
　　　★6項目の 一覧が 手元に ありません
　⑤ 学ぶの見出し「逆流と声」→「食べたあとと声」
　　　★★「逆流と声」という見出しは ★ありません
　　　★あるのは 434行「逆流性食道炎と声」です
　　　★これを「食べたあとと声」に変える、で よろしいですか
　⑥ design/spec.html:157
　　　★★design/spec.html は ★リポジトリに ありません（find で 0件）
```

**⑤だけ、見出しが1文字違います。**「逆流と声」ではなく「**逆流性食道炎と声**」です。
**同じものを指していると思いますが、確かめさせてください。**

---

# ★ 収集データ拡張案・統合実行ルート

## 収集データ拡張案 ── 16件（★12件ではありません）

```
　112〜115　★私が今日足した対応表（★新しい名前と 並べたもの）
　125　　　 ★取り消し線を引いた「同年代の平均より」
　130〜133　★出典の表（★RSI｜Reflux Symptom Index｜…）
　135〜146　★本文と PubMed へのリンク
　222　　　 ★「質問票（EASE / VFI / SVHI / RSI）はスクリーニング目的の道具」
```

**★下の表と出典は、置き換えません。**査読 §6 が
**「出典は『学ぶ』の記事に 参考文献として 書きます」**としており、
**置き換えると 出典が 意味を失います。**対応表を足す形にしてあります。

## 統合実行ルート v4 ── **2か所ではなく 4か所**

```
　 26行　「標準化質問票 RSI・VFI・SVHI-10・EASE」
　171行　「標準化質問票 RSI / VFI / SVHI-10 / EASE（最大の差別化要素）」
　524行　「★『食事と就寝』（胃からの逆流と声）6日」
　526行　「★逆流性食道炎は病名なので、病歴として…」
```

**★526行は、直す必要がありません。**「逆流性食道炎は病名なので」と、
**まさに病名の扱いを説明している文**だからです。消すと、決めた理由が消えます。

---

# まとめ ── いま できること

```
　★すぐ できる　⑤ 学ぶの見出し（★1文字の 食い違いを 確かめてから）
　　　　　　　　 統合実行ルート 26・171・524行
　★お決めが要る 同意文（lib/consent.js:62）★版を上げるか
　　　　　　　　 ② 対応表を いま作るか（★私は 案A）
　★文書が要る　 ④ §1の1〜6 ／ 多言語「完成.md」450行
　★ご確認が要る ③ Stripe ダッシュボードの商品名
　★存在しない　 ⑥ design/spec.html
```
