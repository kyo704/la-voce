# ★ひつじの 部屋の 座標 ── ★作業計画（★着手前）

全82行 / 末尾は「★ガイドが 届き次第、★①から 始めます。」

★これは **計画** です。★測った 数では ありません。
★測る のは ②の 道具が します。

## 〇 ★★はじめに ── ★ガイドが 手元に ありません

★★`hitsuji-room-fix-guide.md` を、★蔵の 中にも、★机の 上にも、
　★`docs/opus/` にも 見つけられません でした。★さがした ところ ──

- `docs/opus/`（★配られた 仕様の 置き場）
- `docs/` ぜんぶ ／ `docs/inbox/`
- `~/Desktop` ／ `~/Downloads`（★9月16日 以降の `.md`）

★★この 会話にも 届いて いません。★受け取って いれば、
　★決まりの とおり `docs/opus/` に 写して いた はずです。
　★★話の 途中で 文脈が 詰め直された とき、★添付は 消えます。
　★★それを 防ぐ ための 決まりでした。★今回 それが 効いて いません。

★★**中身を 推し量って 進めません。**
　★★手順も、★直す 場所も、★受け入れの 線も、★ガイドが 決める もの です。
　★★私が 見立てた もので 代えると、★別の ものを 直します。

★★お願い ── ★もう一度 お送りください。★届いたら すぐ `docs/opus/` に 写します。

## 一 ★待つ あいだに 調べた こと（★コードを 読んだ だけ）

★★ご指摘の「★したくの 時だけ 別の 部屋を 描いて いた」に 当たる ものを、
　★1つ 見つけました。★**まだ 直して いません。**

### ★舞台の 大きさが、★2つの 手で 別々に 決まって います

`components/CharacterHome.jsx`

- 2276〜2278 … `stage = fullBleed ? stageFit(…) : stageSize(…)`
- 2435 … `stageStyle(roomBoxW, roomBoxH, STAGE_ASPECT, leftPct)` を 展げる

`lib/roomStage.js`

- `stageStyle` は、★中で **いつも `stageSize`** を 呼びます（153行）。
- `stageFit` は「★**収める**」── ★部屋ぜんぶが 見えます。
- `stageSize` は「★**覆う**」── ★はみ出した ぶんは 切れます。
- ★同じ 箱に 対して、★2つは **違う 幅**を 返します。

★★`fullBleed`（★ながめる）の とき ──

- 元素の 幅・高さ は `stage`（＝`stageFit`）で 上書きされます（2439行）。
- ★けれど 横の ずらし `dx` は、★`stageStyle` の 中で
　★**`stageSize` の 幅**から 出て います（`stageShiftX(boxW, s.w, …)`）。
- ★★つまり **幅は fit、ずらしは size**。★2つが 食い違って います。

★★これは この 蔵の 持病の 形 です ──
　★**同じ 決めが 2か所に あり、★片方だけ 変わる**。
　★★`roomStage.js` 自身の 注記が、★まさに それを 戒めて います（★109〜127行）。

★★`fullBleed` が false（★したく）の ときは、★どちらも `stageSize` です。
　★★だから **したく だけ 揃って いて、★ながめる だけ ずれる**、
　　★あるいは その 逆に 見えます。★ご指摘の 形と 向きが 合います。

★★★ただし ── ★**これが ガイドの 言う 原因かは 分かりません。**
　★★私が 読んで 見つけた もの です。★測って いません。
　★★ガイドが 別の 経路を 指して いれば、★そちらが 正 です。

## 二 ★作業の 段取り（★ガイドが 届いてから）

1. ★ガイドを 読み、★`docs/opus/` に 写す。★指す 場所と 受け入れの 線を 書き出す。
2. ★**道具を 先に 作る** … `tools/room_coord_probe.py`（または `.js`）。
　 ★ながめる と したく の 両方で、★同じ 家具の **実測の 位置**を 取る。
3. ★**わざと 壊して、★止まる ことを 確かめる**。
　 ★片方の 舞台の 幅を わざと 変えて、★道具が 食い違いを 出す ことを 見る。
　 ★★出なければ 道具が 壊れて います。★そこで 止まります。
4. ★直す。★決めを **1つ**に する（★2か所で 計算しない）。
5. ★両画面で もう一度 実測し、★数が 揃う ことを 見る。
6. ★見張りを 足す。★2か所で 計算し直したら 落ちる 形に する。
7. ★見た目は 未確認の まま 報告し、★坂本さんに 撮って いただく。

★★②〜③は、★きょう 一日 続けて いる 順 です ──
　★道具の 較正を して から、★その 結果を 使います。

★ガイドが 届き次第、★①から 始めます。
