# U1 ── 先生に 見せる 関数の 棚おろし
全108行 / 末尾は「  ★他人の 行が 0行 だった、という 事実だけ です。」

★出どころ 2026-09-14、★裁定 その50 §3 ／ U1（★Opus・坂本さん 承認）

## ★★結論 ── ★**その 関数は、★台帳に ありません**

★★`get_student_entries` を、★実際に 呼んで みました。

```
POST /rest/v1/rpc/get_student_entries
{"code":"PGRST202",
 "details":"Searched for the function public.get_student_entries ...
   but no matches were found in the schema cache."}
```

★★**ありません。**
　★試し用の 企画は、★本番の 写しから 作られて います（★坂本さん）。

## なぜ 無いのか

★★`components/VocalTracker.jsx:1803` に 書いて ありました ──

```
★先生が生徒の記録の中身を見る仕組みは、2026-09-01 に削除しました。
  ★常時アクセスそのものをやめる、という判断です（坂本さん・2026-09-01）。
    共有は、生徒さんが自分で書き出して手渡す形にします。
  ★教室の役割（オーナー・管理者・担当講師）から健康データへ届く道は、
    もともと1本もありません。これは変わりません（永久に作らない・Opus 判断）。
```

★★`lib/shareScope.js:7` にも ──
　「★`get_student_entries`（SECURITY DEFINER）ごと 消えて います。」

## ★では、★あの 紙は 何か

★★`supabase/migration_teacher_student_entries_rpc.sql` は、
　★**2026-09-01 より 前の 形**を 書いた 紙です。
★★流せば 関数が できます。★けれど、★いま 流して いません。

★★だから ──
- ★`:40` `:82` の `voice_quality` を 消しても、★**何も 変わりません**
- ★消さなくても、★先生に 見えて いません

## 実際に 確かめた こと（★試し用の 企画）

| 試した こと | 結果 |
|---|---|
| `rpc/get_student_entries` を 呼ぶ | **PGRST202（関数が 無い）** |
| ほかの 人の `entries` を 読む | **0行** |
| `rpc/get_connected_names` | 404（無い） |
| `rpc/get_student_records` | 404（無い） |
| `rpc/get_my_teacher_names` | 200（★これは あります。★名前だけ） |

## ★それでも 危ないのは、★**紙が 残って いる** こと

★★この 紙は、★流せば 動きます。
★★中には 健康の 列が **48件** 並んで います ──

| 組 | 列 |
|---|---|
| 声 | throat_condition, voice_quality, voice_checkins, voice_entries, voice_memo, wake_note, routine_note, resonance_score, pianissimo_high_note, pianissimo_onset_delay, cpps_value |
| 症状 | throat_symptoms, throat_symptoms_other, morning_edema |
| 眠り | sleep_hours, sleep_quality, bedtime |
| 活動 | activity_type, activity_duration, activity_detail, repertoire, performance_quality, recovery, load_detail, exercise_minutes, exercises, exercise_level, speaking_level, non_performance_speech_minutes, longest_speech_block_minutes |
| 水 | water_intake, water_by_slot |
| 食事 | meal_notes, meals, carbs_g, protein_g, fat_g, fiber_g, protein_level, calorie_level, dinner_time, dinner_tags |
| からだ | weight_kg, body_fat_pct |
| 心 | ease, mental_reason, mental_tags |
| ひとこと | notes |

★★★どれも `entries` の 列です。★**ぜんぶ 健康の 記録**です。
　★★`voice_quality` **1つの 話では ありません。**

★★営業資料 p.2／p.3 と 契約書は「先生は 見られない」と 書いて います。
　★★紙が 1本 残って いる だけで、★**流せば 48列 開きます**。

## ご提案

| 案 | 中身 |
|---|---|
| ㋐ | 紙ごと 消す。★git の 履歴には 残ります |
| ㋑ | 紙の 頭に「★流さないで ください」と 大きく 書く |
| ㋒ | 紙を `supabase/retired/` に 移し、★名前を `DO_NOT_RUN_…` に する |

★★私の 見立ては **㋒** です。
　★★消すと、★「なぜ 消えたか」が 分からなく なります。
　★★残すなら、★誤って 流せない 名前に します。

★★★そして **見張り**を 1本 置く ことを ご提案します ──
　★「`entries` の 列を 他人に 渡す 関数が、★台帳に 無い こと」。
　★★いま 無い ことは 確かめました。★次に 誰かが 足しても 気づけます。

## ★記録番号に ついて

★★名簿の 書き出しの ときと **同じ 形では ありません**。
　★あれは「動いて いる 道が 開いて いた」でした。
　★これは「動いて いない 紙が 残って いる」です。
★★不具合では なく、★**片づけ残し**と 見ます。
　★★それでも、★9月15日に 書類を 渡す 日には 片づけて おく べきです。

## この 調べが 見て いない こと

- 本番の 台帳には、★私は 尋ねられません。
  ★試し用の 企画（★本番の 写し）で 確かめました。
  ★★本番でも 同じ 問い（`rpc/get_student_entries`）を お試しください。
- `entries` の 決まり（RLS）そのものは 読んで いません。
  ★他人の 行が 0行 だった、という 事実だけ です。
