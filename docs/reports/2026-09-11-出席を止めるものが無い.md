# ★出席を 止める ものが、★1つも ありません

全192行 / 末尾は「★★お待ちして いるもの ── ★④、★⑤（10校の id）、★上の 関数の 中身。」

## お尋ね

> 教授（shukketsu を 持たない 方）が `lessons.attendance` に 書けない ように
> している しくみは、★RLS・列ごとの 許し・アプリ側の どれかに ありますか。

## 答え ── ★ありません

★★3つの 層すべてで 確かめました。★どれも 止めて いません。

| 層 | 状態 | 証拠 |
|---|---|---|
| ① 行の 決まり（RLS） | ★止めない | ②の 結果 ── `lessons` 9本、`has_can` **0本** |
| ② 列ごとの 許し | ★止めない | ③の 結果 ── `attendance` と `scheduled_at` が 同一 |
| ③ アプリ側 | ★止めない | ★下の 2本の 道 |

---

## ★アプリ側の 証拠

★★出席を 書く 道は 2本 あります。★どちらにも 権限の 確認が ありません。

### 道①　`components/VocalTracker.jsx:9722`

```js
async function handleAttendance(lesson, status) {
  if (!lesson || !lesson.id) return;                                 // ★行が あるか
  if (status !== null && !ATTENDANCE_KEYS.includes(status)) return;  // ★値が 正しいか
  ...
  await supabase.from("lessons").update(patch).eq("id", lesson.id)   // ★そのまま 書く
```

### 道②　`components/VocalTracker.jsx:11293`

```js
async function handleSetLessonHeld(lessonId, nextStatus, linkId) {
  if (nextStatus !== null && !ATTENDANCE_KEYS.includes(nextStatus)) return;
  await supabase.from("lessons").update({ attendance: nextStatus, ... })
```

★★手前の 早い return も 読みました。
★★確かめて いるのは「行が あるか」と「値が 正しい 札か」の 2つだけです。
★★役職も、★できことも、★1度も 見て いません。

### ★決定的な こと

★★`shukketsu` という 字が、★帳面ぜんたいで **2ファイルにしか ありません**。

```
lib/opsPerms.js        ← ★決めて いる ところ（TEMPLATE_POSTS と PERM_KEYS）
tools/perm-matrix.js   ← ★道具
```

★★`components/` にも `app/` にも **1か所も ありません**。
★★書き込まれて いて、★どこでも 読まれて いない 値です。
★★Opus の N-1 の 決まりに、★そのまま 当たります。

> ★規則：書き込んでいる値は、必ずどこかで読まれているか。

---

## ★期待表に 対して 意味すること

★★教授・准教授・講師の「出席 = false」は、★いま どこにも 実装されて いません。

★★それだけでは ありません。★同じ 理由で、
★★期待表の 50マスの うち、★台帳が 判定できる マスは ごく 一部です。

| 表 | 決まり | `has_can` | 期待表の 鍵を 見て いるか |
|---|---|---|---|
| enrollments（名簿） | 2 | **0** | ★`meibo` を 見て いない |
| lessons（日程・出席） | 9 | **0** | ★`sched_all`／`sched_mine`／`shukketsu` を 見て いない |
| org_posts（役職） | 1 | **0** | ★`post`／`master` を 見て いない |
| memberships（所属） | 5 | 1 | ★`post` のみ（★今日 足した ぶん） |
| org_events（行事） | 5 | 3 | ★`gyoji`（★今日 足した ぶん） |

★★`has_can` が 入って いる 4本は、★すべて 今日の §7-3 で 足した ものです。
★★それ 以前は、★できことを 見る 決まりが **1本も ありませんでした**。

---

## ★できことの 鍵 13本の 棚おろし

★★見張り `components/tests/perm-keys-consumed.test.js` が 数えます（★落としません）。

```
  鍵           画面／API で 読む
  bill         lib/opsShell.js（★描画の gate）
  meibo        lib/opsShell.js（★描画の gate）
  post         app/api/org/posts/route.js  ← ★ここだけ 実際に 止める
  bill_pay     ★どこにも      sched_all    ★どこにも
  sched_mine   ★どこにも      gyoji        ★どこにも
  renraku_all  ★どこにも      shukketsu    ★どこにも
  koma         ★どこにも      koma_mine    ★どこにも
  master       ★どこにも      monka_write  ★どこにも
```

★★「どこにも」は、★`TAB_RULES`（どの 札を 出すか）の ほかに 読まれて いない、
★★という 意味です。★`mayEditRoster` も `canEdit` という 描画の 印を
★★渡して いるだけです（★`VocalTracker.jsx:13199`）。

★★**札を 出さない ことは、★書けない ことでは ありません。**
★★5つの 表の うち 4つは、★ブラウザが Supabase に 直に 書いて います。
★★札は 通り道では ありません。

---

## ★出席と 日程は、★同じ 表です

★★`attendance` は 別の 表では なく、★`public.lessons` の **列** でした
★（`VocalTracker.jsx:9730`）。

| 期待表の 言い方 | 実際 |
|---|---|
| 名簿 | `public.enrollments` |
| 役職と 所属 | `public.memberships.post_id` ／ `public.org_posts` |
| レッスンの 日程 | `public.lessons` |
| **レッスンの 出席** | **`public.lessons` の `attendance` 列** ← ★同じ 表 |
| 行事 | `public.org_events` |

★★行の 決まりは、★列を 隠せません。
★★日程を 直すのも 出席を 付けるのも、★同じ 行への UPDATE です。
★★分けるには、★列ごとの GRANT か `security definer` の 関数が 要ります
★（★この家では `get_student_entries` が その型です）。

---

## `can_view_ops` について

★★帳面に ある 書きぶりから、★`can_view_ops(auth.uid(), org_id, student_id)`
★★という 形だと 分かります。★**引数に できことの 鍵が ありません。**

★★「この方は この生徒を 見てよいか」という 問いで、
★★「この方は 出席を 付けてよいか」という 問いでは ありません。

★★ただし これは **形からの 推測です**。★中身を 見ないと 断定しません。
★★次の SQL で 読めます（★読むだけです）。

```sql
select p.proname as "関数", pg_get_functiondef(p.oid) as "中身"
from pg_proc p join pg_namespace n on n.oid = p.pronamespace
where n.nspname = 'public'
  and p.proname in ('can_view_ops', 'has_can', 'is_org_owner_or_admin', 'role_rank');
```

★★同じ 名前で 引数ちがいが 複数 あるかも しれないので、★全部 出ます。

---

## ★④に ついて

★★④は **同じ ファイルの 中に あります** ──
★`supabase/2026-09-11-§3-50通り-台帳の側（読むだけ）.sql` の ④。
★★机の上の ファイルを、★そのまま 下へ 進めて ください。
★★⑤（★10校の id）も その下に あります。

---

## ★進め方の ご相談

★★ここまでで、★**第2層の 答えは ほぼ 出ました** ──
★★台帳は 期待表の 鍵を ほとんど 見て いません。

★★そのうえで、★**第3層（通し）を 回す 意味は 残って います。**
★★「実際に 投げたら 通る」ことを、★推測では なく 数字で 示す ためです。
★★⑤の id を いただければ 回します。

★★ただ、★ご判断を いただきたい ことが 1つ あります。

★★第3層を 回せば、★おそらく「教授が 出席を 書けて しまう」ことが 実証されます。
★★これは §7 の「職員が 自分を 学長に できる」と 同じ 重さの 穴です。
★★9月15日の 関門の 前に 出て くる ことに なります。

★★**回してから 直すか、★先に 直してから 回すか。**

★★私の 考えは **回してから** です。
★★理由は、★直した あとに 回しても「もともと 通って いたのか」が
★★記録に 残らない からです。
★★安全管理の 書類は「いま こうです」と 言う 紙なので、
★★**穴が あった 事実と、★塞いだ 事実の 両方**が 記録に 要ります。

---

## ★いまの 状態

★★コードには 何も 入れて いません。★読んで 数えた だけです。
★★見張りは 277本 すべて 通って います。

★★お待ちして いるもの ── ★④、★⑤（10校の id）、★上の 関数の 中身。
