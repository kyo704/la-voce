# ★★★Stripe ── ★再デプロイは 要ります。★けれど それだけでは 足りません（2026-09-23）

## ★お尋ね ── 再デプロイは 要るか

```
★★★要ります。
```

★Vercel の 環境変数は、★**配る とき**に 中へ 入ります。
★★いま 動いて いる 配り物は、★置いた ときの 環境変数を 持った まま です。
★★★あとから 足しても、★その 配り物には 入りません。

★★坂本さんに お願いする こと ──

```
Vercel → la-voce → Deployments → いちばん 上の 配り物 → Redeploy
（★または、★何か 1つ push すれば 同じ ことが 起きます）
```

---

## ★★★けれど、★合言葉 1つ では 動きません

★コードを 数えました（`grep process.env.STRIPE`）。★要る 環境変数は **5つ** です。

| 環境変数 | どこが 読むか | いま |
|---|---|---|
| `STRIPE_SECRET_KEY` | `lib/stripe.js`（★お支払い・受け取りの 両方） | ★置かれました |
| `STRIPE_PRICE_ID_MONTHLY` | `app/api/stripe/checkout`・`webhook` | ★★分かりません |
| `STRIPE_PRICE_ID_ANNUAL` | 同上 | ★★分かりません |
| `STRIPE_PRICE_ID_FULL` | `app/api/stripe/webhook` | ★★分かりません |
| `STRIPE_WEBHOOK_SECRET` | `app/api/stripe/webhook` | ★★分かりません |

★★★`STRIPE_PRICE_ID_*` が 無い と、★お支払いの 道は ここで 止まります ──

```js
// app/api/stripe/checkout/route.js:79
const priceId = priceIdFor(planKey, process.env);
if (!priceId) {
  …
  return NextResponse.json({ error: "この機能は、いまお使いいただけません。" }, { status: 503 });
}
```

★★止まり方は **正しい** です（★黙って 通しません）。★けれど 買えません。

---

## ★★★裁定180 の 新しい 値段 ── ★まだ 売る 道が ありません

★`lib/plans.js` の `PLANS` に ある のは **2つ** だけ です ──

```
monthly … 月580円   envKey: STRIPE_PRICE_ID_MONTHLY
annual  … 年4,800円 envKey: STRIPE_PRICE_ID_ANNUAL
```

★★★「つたえる」は **1つも ありません**。
　★だから、★Stripe に 年6,000円を 作っても、★**画面から 買えません**。

★★要る 手順は 5つ です ──

```
① 配り直す（★合言葉を 効かせる）
② Stripe に 値段を 作る …… tools/stripe_prices.py --ok
     ind_tsutaeru_y          6,000円
     ind_tsutaeru_gakusei_y  3,000円
③ `lib/plans.js` の PLANS に 2つ 足す（★envKey を 決める）
④ その 環境変数を Vercel に 置く
⑤ ★もう 一度 配り直す
```

★★★③ は ★**お裁きが 要ります**。★私が 決めません ──

```
★「学生 年3,000円」を、★誰に 出すか
  裁定180 …「契約して いる 学校の 名簿に 在籍中」＋「在籍の 確かめに 同意」
  ★★これは **台帳が 決める** こと です（★自己申告も 学生証も しない）。
  ★★★けれど `offeredPlans(band)` は いま **年齢の 帯** だけ で 決めて います。
    ★在籍を 見る 仕組みが ありません。
  ★お尋ね …… ★どちらの 形に しますか
      ㋐ `offeredPlans` に 在籍の 判じを 足す
      ㋑ 台帳に 関数を 1つ 作り、★画面は それを 呼ぶ（★決めを 台帳に 置く）
  ★★私の 見立ては ★㋑ です（★裁定180 …「台帳を 見るだけ で 自動で 決まる」）。
```

---

## ★私が した こと

```
tools/stripe_check.py  …… ★読むだけ の 確かめ（★1円も 動かしません）
  ① 環境変数が 揃って いるか（★有/無 だけ。★値は 出しません）
  ② 口は 試しか 本物か・つながるか
  ③ いま ある 値段と `tools/prices.json` の 数が 合うか

tools/stripe_prices.py …… ★作る ほう（★前に 作りました）
  ★試しが 既定。★本物には `--honban` が 要ります
  ★★同じ 鍵が あれば 作りません。★値が ちがえば **止まります**
```

★★★どちらも、★合言葉の ある ところで 走らせて ください。
　★手元には ありません（★`STRIPE_SECRET_KEY` は Vercel の 側 です）。

```
STRIPE_SECRET_KEY=sk_test_… python3 tools/stripe_check.py
STRIPE_SECRET_KEY=sk_test_… python3 tools/stripe_prices.py --ok
```

★★★お支払いの 道が 本当に 動くか は、★**私からは 確かめられません**。
　★確かめるには 実際に お支払いの 画面を 開く ことに なります。
　★★試しの 口（sk_test_）で あれば 安全 です。★本物の 口では しません。
