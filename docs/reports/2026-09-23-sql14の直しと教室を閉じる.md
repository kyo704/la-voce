# sql/14 の 直し ／ 教室を 閉じる（2026-09-23）

## 一 ── ★お尋ねへの 答え：本番に 残って いません

```
本番   ops_audit_log の 表 …… 0     _audit の 引き金 …… 0
試し   ops_audit_log の 表 …… 0     _audit の 引き金 …… 0
```

★★★**本番は 戻って います。**★「学校を 閉じる」は 動きます。
　★戻した あと、★本番で `★50通り-10-職員` を 1つ 消して 確かめました（★通りました）。
　★そのあと FX9 の 12校を ぜんぶ 消して います。★どれも 通りました。

★★1つ だけ 残って いました …… `purge_ops_audit_log()`（★引数 **なし**）。
　★私の 戻しは `purge_ops_audit_log(integer)` を 消して いました。★形が ちがいます。
　★★消しました（本番・試し とも）。

---

## 二 ── sql/14 の 直しを 試しで 通しました

★束の 中の `20260930_14_ops_audit_log.sql` が 正式な 直し です（★外部キー 無し・`org_name_at` 付き）。
★★`sql/15` は 当てて いません。★本番に 14 が 無い ので、★14 の ほうが 筋 です。

★道具 …… `tools/close_org_prove.js` …… **6 / 6**

| | 見た こと | 結果 |
|---|---|---|
| ① | 行事・役職・場所・ご請求・名簿・在籍 を 持つ 学校を 作る | ○ 6つとも |
| ② | 画面と **同じ 順** で 閉じる | ○ 最後まで 通りました |
| ③ | 学校が 消えて いる | ○ |
| ④ | ぶら下がって いた ものも 消えて いる | ○ 6つとも 0 |
| ⑤ | 記録は **残る**（★消した ことも） | ○ 18 → 24 |
| ⑥ | そのときの 学校の 名前が 読める（`org_name_at`） | ○ |

★★★道具は 順を **写しません**。★`lib/orgClosure.js` から **読み込み** ます。
　★写すと、★片方が 古く なります（★きょう 何度も 見た 形 です）。

---

## 三 ── もう 1つの 不具合も 直しました（★ご指摘の とおり）

★`CLOSE_ORG_DELETE_ORDER` に、★行事・役職・場所・ご請求 ほかが ありません でした。

```
前 …… lessons → assignments → enrollments → org_invitations → memberships → organizations
後 …… lessons → assignments → enrollments → org_invitations →
       org_message_drafts → org_messages → org_events → org_places → org_periods →
       org_divisions → roster_drafts → lesson_presets → teacher_invitations →
       org_billing → memberships → org_posts → organizations
```

★★消えて いなかった わけでは ありません。★最後の `organizations` で **連鎖** して いました。
★★★危ない のは そこ です ── ★連鎖で 落ちると、★誤りは `organizations` の 1行 だけ に 見えます。
　★どの 表で 止まったかが 分かりません。★きょうの 事故が まさに それ でした。

### ★画面の 字も 直しました

```
前 …… 消えるもの：教室そのもの／先生と生徒の紐付け／レッスンの予定
後 …… 消えるもの：教室そのもの／先生と生徒の紐付け／レッスンの予定／
       行事／役職／場所／ご請求の設定／学科・コース／コマ／連絡と書きかけ
```

★★★少なく 書く のは、★書かない のと 同じ くらい 悪い です。
　★「それも 消えるとは 思わなかった」と 言われます。

---

## 四 ── ★★★見つけた もの ── 記録が、★学校を 閉じると 消えます

★本番の 外部キーを 数えました。

```
monka_read_log.org_id     → organizations   ON DELETE CASCADE
org_billing_log.org_id    → organizations   ON DELETE CASCADE
score_log.org_id          → organizations   ON DELETE CASCADE
export_log.org_id         → organizations   ON DELETE CASCADE
```

★★★つまり、★**学校を 閉じると、★門下を 開いた 記録も 消えます**。

★束0（FX1）で 直したのは **人** の ほう です ──
　★先生が 退会しても 記録は 残る（`SET NULL`）。
★★★けれど **学校** の ほうは `CASCADE` の まま でした。

★これは 約束と ぶつかります ──
　★「見た 記録は 消せません」（★約束 P-0386）
　★「ご本人から 求められたら、30日以内に お見せします」

★★★**直して いません。★決めの こと です。**
　★学校が 無く なった あと、★誰が その 記録を 読むのか。
　★読む 人が 居ないなら、★残す 意味も 変わります。
　★★`CLOSE_ORG_DELETE_ORDER` には **わざと 入れて いません**。★註を 置きました。

---

## 五 ── ★Opus へ ── `ledger_inventory.sql` が 流れません

```
ERROR: 42601 syntax error at or near "from"
LINE 6:    from pg_proc p join pg_namespace n on n.oid=p.pronamespace
```

★わけ …… 5行目の 終わりに、★`--` の 註が **閉じ括弧の 前** に あります。

```sql
…'e',(pg_get_functiondef(p.oid) ~* '…')   -- ★表として参照しているときだけ（…）))
                                          ↑ ★ここから 行末まで ぜんぶ 註 に なり、★`))` が 消えます
```

★★★これも **同じ 形** です …… ★註が 処理を 飲みました。
　★きょう 6度目 です（`strip.js` ／ `price_check` ／ `ask_ledger` ／ `apply_migration` ／
　　`A7` の 字の 一致 ／ ここ）。

★直し …… ★註を **行の 終わり**（`))` の 後ろ）か、★**前の 行**へ。

### ★A7 の 直し そのものは 効いて います（★別に 確かめました）

★新しい 式を、★台帳で 直に 当てました ──

```
admin_entry_stats         → True
character_unlock_summary  → True
get_public_portfolio      → ★False
submit_inquiry            → ★False
```

★★★直って います。★`get_public_portfolio` は もう 拾いません。
