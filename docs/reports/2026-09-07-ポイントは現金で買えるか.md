# ポイントは、現金で買えるか（3点の確認）

全102行 / 末尾は「書き替えるかどうかです。コードが無いので、いまなら文書を直すだけで済みます。」

読んだ日：2026-09-07 ／ 読んだもの：いま main にあるコード
★これは「設計はこうなっている」ではなく、「コードがこう書いてある」の報告です。

---

## ① ポイントは、いま、現金で買えるか → ★買えません

道が1本もありません。次の4つを、別々に確かめました。

**(1) ポイントの残高は、記録からしか作られていません。**

`lib/character.js:386`

    export function computeBalance(entries, pointsSpent) {
      return computeTotalEarned(entries) - (pointsSpent || 0);
    }

足す側は `computeTotalEarned(entries)` の1つだけです。`entries` は記録です。
「買った分」を足す項は、式の中にありません。
残高はデータベースに置かれておらず、★毎回この式で計算し直されています。
だから、たとえ誰かが外から数を書き込もうとしても、置き場所がありません。

**(2) 決済のあと、持ち物にもポイントにも、何もしていません。**

`app/api/stripe/webhook/route.js` の中に、
points / character / wardrobe / inventory / item という語が1つもありません。
webhook がしているのは `subscriptions` の行を書き直すことだけです。

**(3) 単発の決済そのものが、ありません。**

`app/api/stripe/checkout/route.js:142` は `mode: "subscription"` です。
`mode: "payment"`・`one_time`・`payment_intent` は、リポジトリ全体で0件でした。
Stripe を呼ぶ道は checkout / portal / webhook の3本だけで、
売っているのは `lib/plans.js` の2つ、★月580円と年4,800円の定期だけです。

**(4) 品物を手に入れる入口は、1つしかありません。**

`components/VocalTracker.jsx:8617` の `handlePurchaseItem` だけです。
ここが引くのは `character_points_spent`（ポイント）で、お金には触れていません。
`components/CharacterHome.jsx` には subscription / paid / 有料 という語が★0件です。
有料かどうかを、そもそも知りません。

**(5) 逆に、「絶対に有料にしない」側に書いてあります。**

`lib/freeTier.js:243` の `NEVER_PAID` に `"sheep"`（羊とおうち・装いの表示）が入っています。

---

## ② 閉じるべき経路 → ★ありません

①で見たとおり、開いている道がないので、閉じる作業は発生しません。
「最初の作業」は、別のところから始められます。

★ただし、お金とは別の弱いところが1つあります（今回の問いの外ですが、報告します）。

`character_points_spent` は、ブラウザから `profiles` に直接 update しています
（`components/VocalTracker.jsx:8628`）。残高は
「記録から計算した合計 − この数」なので、この数を小さく書き換えられれば、
記録せずにポイントを増やせます。★現金は関わりません。ただの自己申告です。
前払式支払手段の話とは別筋なので、直すかどうかは、別に決めてください。

---

## ③「よそおい多め」（¥980）は、数か、期間か → ★どちらでもありません。実装されていません

**コードには、影も形もありません。**

MORE_MONTHLY・よそおい多め・よそおい放題・980 のいずれも、
`.js` `.jsx` の中で★0件でした。`lib/plans.js` の `PLANS` は2つだけです。

**設計文書のほうは「数」で書かれています。**

`docs/opus/lavoce-価格と課金の正（9月6日・確定）.md:34`

    | **よそおい多め** | ¥980 | ¥9,800 | 毎月 **3点** ＋ 季節のよそおいが自動 |

同 :103 に `Price JPY 980 / month / recurring STRIPE_PRICE_ID_MORE_MONTHLY` とあります。

つまり、
・**定期課金**（recurring）である点は、期間のかたちです。
・しかし**渡すものは「毎月3点」という個数**です。ここが数のかたちです。

同 :238 に「★公開には要りません」と書かれており、
実装は始まっていません。★だから、いま書き替えれば、直す作業は発生しません。

---

## まとめ

| 問い | 答え |
|---|---|
| ① 現金で買えるか | 買えない。道が1本もない |
| ② 閉じる経路 | 無し。作業は発生しない |
| ③ ¥980 は数か期間か | 未実装。文書上は「定期課金だが毎月3点」＝★個数で書かれている |

★③だけ、決めていただくことがあります。
文書の「毎月3点」を、期間のかたち（例：その月のあいだ、装いを自由に取り替えられる）に
書き替えるかどうかです。コードが無いので、いまなら文書を直すだけで済みます。
