# 移行の 記録を 残そうと して、★止まりました（2026-09-22）

★裁定161 §6 の 最優先の お指図 ──
「S2・裁定160・束2 の 3つを、★`apply_migration` 経由で 正式に 記録して ください」。

---

## 一 ── 何を したか

★道具を 作りました …… `tools/apply_migration.py`。

★★当てる 前に「何度 流しても 同じ 形か」を 見ます（★較正 13件）──

| 見る こと | |
|---|---|
| `create table` | `if not exists` が 要る |
| `create policy` | `drop policy if exists` と 対 |
| `create function` | `or replace` か `drop function if exists` と 対 |
| `add constraint` | `drop constraint if exists` と 対 |
| `create trigger` | `drop trigger if exists` と 対 |
| `create index` | `if not exists` が 要る |

★★註と 括りの 中は 見ません（★「説明を 処理と 読む」を 避ける）。

★3つの ファイルは **3つ とも 通りました** ──

```
supabase/migration_monka_read_log_s2.sql        RESULT: OK
supabase/migration_org_post_perm_log.sql        RESULT: OK
supabase/migration_taba2_164w2_164w1_fx2.sql    RESULT: OK
```

---

## 二 ── ★★止まった ところ

```
POST https://api.supabase.com/v1/projects/xxjtplvpcneksrofkjmf/database/migrations
  → 403
  {"message":"Your account does not have the necessary privileges to access this endpoint."}
```

★★★**合言葉（`SUPABASE_ACCESS_TOKEN`）に、★この 口を 叩く 資格が ありません。**

★確かめた こと ──

| 試した こと | 結果 |
|---|---|
| `GET /database/migrations`（一覧を 読む） | ★**通ります**（31本 読めました） |
| `POST /database/migrations`（本番） | ★403 |
| `POST /database/migrations`（試しの 台帳） | ★**403**（★同じ） |
| `POST /database/query`（SQL を 流す） | ★通ります（★これまで 使って きた 道） |

★★試しの 台帳でも 同じ 403 です。★★**口座の 資格の 話** で、★事業の 話では ありません。

★`supabase` の 道具（CLI）は 手元に **ありません**。★`supabase/config.toml` も ありません。

---

## 三 ── ★なぜ 自分で 回り道を しなかったか

★回り道は あります ── `POST /database/query` で SQL を 流し、★そのあと
`supabase_migrations.schema_migrations` に 1行 自分で 入れる。

★★★しません でした。★裁定161 §6 に こう 書いて あります ──

```
rule: 本番の台帳を変えるのはapply_migrationだけ。直接のSQLは使わない
```

★★記録の 表に 自分で 行を 入れるのは、★まさに「直接の SQL」です。
★★★記録を 正しく する ために 記録を 手で 書く、★では 筋が 通りません。

---

## 四 ── ★坂本さんに お願いする こと（★どれか 1つ）

### ㋐ 合言葉を 作り直す（★お勧め）

```
Supabase の 画面 → 右上の お名前 → Account Preferences → Access Tokens
  → Generate new token
  ★★「Read only」に **しない** で 作る（★いまの ものは たぶん 読むだけ です）
  → 出た 文字を、★手元の ~/.zshrc の SUPABASE_ACCESS_TOKEN と 入れ替える
```

★★★古い 合言葉は、★入れ替えた あとに Revoke して ください。

### ㋑ Supabase の 道具（MCP）から 当てる

★Claude の 別の 席に Supabase の MCP が つながって いれば、★そこの
`apply_migration` で 同じ ことが できます。★私の 席には つながって いません。

### ㋒ `supabase` の 道具（CLI）を 入れる

```
brew install supabase/tap/supabase
supabase link --project-ref xxjtplvpcneksrofkjmf
supabase db push
```

★★これは `supabase/migrations/` という **決まった 置き場** を 見ます。
★★いまの 倉庫に その 置き場は ありません（★294本の SQL は `supabase/` に 平らに 並んで います）。
★★★入れるなら、★置き場を 作り直す 話に なります。★大きい 話 です。

---

## 五 ── いまの 一覧（★Opus へ）

### `supabase/migrations` …… ★**ありません**

```
$ ls supabase/migrations
　（そういう 置き場は ありません）
```

★`supabase/` の 下に、★`.sql` が **294本**、★平らに 並んで います。
★★名前に 日付も 番号も 付いて いません。★順番が 決まって いません。

### 本番の `schema_migrations` …… **31本**（2026-09-14 〜 09-18）

```
20260914042439  add_repertoire_tessitura_delete_policy
20260914073909  rename_role_master_voice_quality
20260914081001  a13_replace_has_can_policies
20260914081040  fix_org_messages_select_self_branches
20260914083726  no017_tighten_org_invitations_policies
20260914084508  no018_step1_drop_dead_update_policy
20260914085555  no018_step2_extend_get_invitation_teacher
20260914094014  no019_5_admin_entry_stats
20260914095145  no018_drop_open_invitation_lookup_policy
20260914095225  no019_5_character_unlock_summary
20260914135514  no020_accept_teacher_invitation_claim_first
20260914222431  no020_drop_dead_twin_function
20260915004849  no020_2_drop_unused_accept_invitation
20260915095702  no024_gate_inside_the_function
20260916082846  create_payment_records_table
20260916091644  create_code_attempts_and_rate_limit
20260916093742  code_pepper_table_and_salted_function
20260916112435  create_leave_enrollment_function
20260916115934  leave_enrollment_v2_close_assignments
20260916124116  fix_get_invitation_teacher_search_path
20260916125424  seed_visual_demo_3sections
20260917112038  place_test_furniture_for_room_drift_measurement
20260917112552  place_interior_furniture_for_drift_measurement
20260917232354  grant_perms_to_migrated_teacher_role
20260918003739  create_gakucho_post_for_forcode_test_org
20260918003748  assign_gakucho_post_to_forcode
20260918020628  seed_6_enrollments_for_screenshot
20260918061529  reseed_6_enrollments_for_meibo_shot
20260918071800  cleanup_test_enrollments_final
20260918081249  seed_teacher_assignment_and_lesson_for_shukketsu_test
20260918132651  revoke_truncate_trigger_references_from_authenticated
```

★★★9月18日で 止まって います。★9月19日 以降の 変更は **1本も 記録に ありません**。

★★記録に 無い もの（★分かって いる ぶん）──

| | 何 | いつ 本番に 入ったか |
|---|---|---|
| 1 | S2（`open_monka_thread` と `monka_read_log` の 列） | 2026-09-21 |
| 2 | 裁定160（`org_post_perm_log` と 引き金） | 2026-09-21 |
| 3 | 束2（164 W2・W1／FX2／167 A1） | 2026-09-22 |
| 4 | S1（`org_messages_select` の 枝を 消した） | 2026-09-21 |
| ― | ★`★50通り-01`〜`-10` の 試しの 学校 10件 | ★不明（★記録に ありません） |

★★★4つ目（S1）も 記録に ありません。★裁定161 §0 は 3つ と して いますが、★4つ です。

---

## 六 ── ★合言葉が 直ったら すぐ できる 形に して あります

```
python3 tools/apply_migration.py s1_close_monka_read_path        supabase/migration_monka_read_close.sql --ok
python3 tools/apply_migration.py s2_monka_read_log_and_open_monka_thread supabase/migration_monka_read_log_s2.sql --ok
python3 tools/apply_migration.py ruling160_org_post_perm_log     supabase/migration_org_post_perm_log.sql --ok
python3 tools/apply_migration.py taba2_164w2_164w1_fx2_167a1     supabase/migration_taba2_164w2_164w1_fx2.sql --ok
```

★★4本 とも「何度 流しても 同じ」形 です。★本番に もう 入って いる ので、★姿は 変わりません。
★★★変わるのは **記録** だけ です。
