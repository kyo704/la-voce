# A13 ── 札を 持った 人で 確かめました（★本物の JWT）
全92行 / 末尾は「| `/api/org/posts` を 通した 役職の 付け替え | ★手元の dev を その 台帳へ 向ける 必要が あります |」

★2026-09-14 ／ ★台帳 la-voce-test（smntpurraumeerselvsc）★本番は 触って いません
★口座 kyo0703opera+localtest@gmail.com（★課長 ／ meibo あり ／ post なし）
★この 文は tools/a13_ondevice.py が 書き出しました。

## 結果

| | 試した こと | 状態 | 行 | 見込み | |
|---|---|---|---|---|---|
| TEST_1 | enrollments の grade_label を 直す | **200** | 1 | 通る | ★合う |
| TEST_2 | 人の post_id を 変える | **403** | ─ | 止まる | ★合う |
| TEST_3 | memberships が 何行 見えるか | 200 | **1** | 1行 | ★合う |
| TEST_4 | enrollments の org_id を 書き換える | **403** | ─ | 止まる | ★合う |

★4つとも、★お見込みの とおり でした。

---

## ★★ただし TEST_2 は、★お考えの しくみで 止まって いません

★止めた ものの 正体を、★文面で 見分けました。

| 直そうと した 列 | 状態 | 文面 | ★止めた もの |
|---|---|---|---|
| `display_title`（自分の 行） | 403 | permission denied for **table** | ★権限（GRANT） |
| `post_id`（自分の 行） | 403 | permission denied for **table** | ★権限（GRANT） |
| `role`（自分の 行） | 403 | **row-level security** policy | ★決まり（RLS） |

★★`role` は 決まりの ところまで 届いて います。★`post_id` は 届いて いません。
　★★つまり UPDATE は 与えられて いますが、★**列を しぼって** あります。
　★`post_id` と `display_title` には、★はじめから 権限が ありません。

### ★これが 意味する こと

★★`memberships_update_role_management`（→ `has_can('post')`）は、
　★**一度も 呼ばれて いません**。★手前の 権限で 止まって います。

★★だから ── ★札を 学部長に 替えて もう一度 TEST_2 を しても、
　★★**同じ ところで 同じ ように 止まります**。
　★お確かめに なりたい「master は 裏口に ならない」は、
　★★この 道では 確かめられません。★決まりまで 届かない から です。

### ★では、役職は どこで 付くのか

★`app/api/org/posts/route.js:246` です。★**service_role** で 書いて います。
　★権限も 決まりも 通り抜けます。★門は route の 中の `mayChangePerson` です。
★★`lib/displayTitle.js:8` に 同じ 考えが 書いて あります ──
　「★書ける道は 関数1本だけに します」。★`post_id` も 同じ 形でした。

★★これは 不具合では ありません。★**そういう 作り** です。
　★ただし、★学部長の 確かめは `/api/org/posts` を 叩かないと 意味が ありません。

---

## ★#6 の 副作用 ── ★お見込みの とおりでした

★課長（meibo あり・post なし）に、★memberships は **1行** しか 見えません。

```
{
 "user_id": "4b027d0a-11de-4acc-ae63-f240300c78aa",
 "role": "staff",
 "post_id": "22222222-2222-4222-8222-222222222222"
}
```

### ★どの 画面が 空に なるか

★`components/VocalTracker.jsx:12037` の `fetchOrgDetail` が、
★画面の 台帳で `memberships` を 読み、★`setOrgMembers` に 入れて います。

| 一覧 | 出どころ | 課長に どう 見えるか |
|---|---|---|
| ★生徒の 名簿 | `enrollments` | ★**出ます**（★読めました・1行 確認） |
| ★職員の 一覧 | `memberships` | ★**自分 1人だけ** |

★★つまり、★meibo で 開く「名簿」の うち、★生徒側は 動きます。
　★★職員側だけが、★自分 1人の 一覧に なります。

★★決まりは 変えて いません（★お指図の とおり）。★お裁きを お待ちします。

---

## ★確かめられて いない こと

| | なぜ |
|---|---|
| 学部長（master・post なし）で TEST_2 | ★札を 替える SQL を 私は 流せません。|
| | ★★そして 替えても、★権限の ところで 止まります（★上） |
| `/api/org/posts` を 通した 役職の 付け替え | ★手元の dev を その 台帳へ 向ける 必要が あります |
