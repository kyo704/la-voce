# ★§5 ② の 答え ── ★保存して いるのは **部屋の 割合** です

全87行 / 末尾は「★★㋖ の 実装は 止めました。★1行も 書いて いません（★§7 の 1）。」

## 答え

★★**OK の ほう です。★画面の 座標では ありません。**
★★移し替えは **要りません**。

## 根拠 ── 3つ

### 一 ★作る ところ（★コード）

★位置が 生まれるのは 2か所 だけ です。★どちらも **割合**です。

`components/CharacterHome.jsx:1581`（★いまの 101点）

```
let pctX = ((e.clientX - rect.left) / rect.width) * 100;
```

`components/CharacterHome.jsx`（★`InteriorDraggable`・★内装 249点）

```
left: … g.left + ((e.clientX - g.x) / g.w) * 100
top:  … g.top  + ((e.clientY - g.y) / g.h) * 100
```

★★どちらも **入れ物の 幅・高さで 割って** います。
★★px は 残りません。★端末の 大きさも、★寄りの 倍率も 入りません。

### 二 ★しまう ところ（★コード）

`components/VocalTracker.jsx:9794`

```
const entry = { ...base, left: Math.round(leftPct * 10) / 10 };
if (topPct !== undefined) entry[topField] = Math.round(topPct * 10) / 10;
```

★台帳 … `profiles.character_equipped` の JSON。
★★入って いるのは `{ left, top }` の **2つの 数だけ** です。
★★画面の 大きさも、★どの 画面で 置いたかも、★倍率も 残して いません。

### 三 ★実機で 測った 数（★2026-09-17・本番）

| 台帳の 数 | したくで 描かれた 位置 |
|---|---|
| 20.8 | 20.799 |
| 54.2 | 54.199 |
| 87.5 | 87.501 |

★★そして **床を ものさし**に すると、★2つの 画面で 差が ありません ──

| 何 | 床基準の 差 左 | 床基準の 差 下 |
|---|---|---|
| view_02 ／ window_03 | 0 | -0.002 |
| door_10 | 0 | -0.001 |
| furniture_01 ／ 04 ／ 02 | 0.003 ／ -0.001 ／ 0 | -0.001 ／ -0.001 ／ 0.002 |
| showa_08 ／ 14 ／ 01 | 0.001 ／ 0 ／ 0 | -0.006 ／ -0.007 ／ -0.001 |

★★0.01％ 未満 です。★**部屋の 中では 1つも 動いて いません。**

★★もし 画面の 座標を しまって いたら、
★★箱の 高さが 634 と 278.56 で 倍以上 ちがう ので、
★★この 表は 0 に なりません。★なって います。★だから 割合 です。

## ★だから

- ★移し替え（migration）は 要りません。
- ★「取り上げない」に 触れません。★触る データが ありません。
- ★§7 の 2 は、★②に ついては **これで 終わり** です。

## ★★いただけて いない もの

★★§5 の **①と③** を、★私は 見て いません。
★★`§0`〜`§7` の 文書が、★こちらに 届いて いません。
★★①と③ の 問いを そのまま お送りいただければ、★同じ ように 答えます。

★★（★きょう 同じ ことが 2度 ありました ──
　`hitsuji-room-fix-guide.md` も 届いて いません でした。
　★添付は、★話が 詰め直される ときに 消えます。
　★★`docs/opus/` に 置いて いただくのが 確かです。）

## ★止めた もの

★★㋖ の 実装は 止めました。★1行も 書いて いません（★§7 の 1）。
