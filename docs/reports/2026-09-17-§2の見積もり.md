# ★§2 の 見積もり（★まだ 実装して いません）

全146行 / 末尾は「★★ご承認を いただいて から 動きます。」

★出どころ `docs/opus/裁定その71-羊の部屋の座標（2026-09-17）.md` §2・§4。

## 〇 ★やる こと（★決まり 1本）

> ★置く画面は、★見る画面と 同じ 絵を 出す。
> ★ただし 原点（`ox`/`oy`）だけ、★置く画面で 動かせる。

★★`k` を 1つに する。★これで 舞台基準の ずれが **0** に なります。

## 一 ★触る ところ ── ★4か所

### A ★`k` を 1つに する（★③ の 直し）

| ファイル | 行 | いま | あと |
|---|---|---|---|
| `components/VocalTracker.jsx` | 17959 | `cameraOn={layoutV2 && homeState !== DRESS}` | `cameraOn={layoutV2}` |
| `lib/roomCamera.js` | 93 | `if (!opt.on \|\| opt.editMode \|\| z <= 1)` | `opt.editMode` を 外す |

★★★2行目を 忘れると、★**掴んだ 瞬間に 部屋が 1.0倍に 戻ります**。
　★★`editMode` の とき 恒等に して いる のは、★2026-09-08 の 直し です。
　★★わけは「★変形が 恒等なら、掴む 位置は ずれない」。
　★★いまは 掴む ときに `getBoundingClientRect()`（★変形の **あと**の 形）を
　　★測って いる ので、★寄って いても ずれません。★外せます。
　★★★けれど これは **実機で 確かめる 必要が あります**。★机上では 決めません。

### B ★原点（`ox`/`oy`）を 足す

| ファイル | 足す もの | だいたいの 行数 |
|---|---|---|
| `lib/roomCamera.js` | `cameraOf` に `originLeftPct` / `originTopPct` を 足す。★来て いれば 羊では なく そちらを 見る | 10 |
| `components/CharacterHome.jsx` | `ox`/`oy` の 持ち物、★空いた ところを 引いて 動かす 手、★`cameraOf` へ 渡す | 40 |

★★★`ox`/`oy` は **しまいません**（★台帳に 書きません）。
　★★これは「いま 見て いる ところ」で あって、「置いた ところ」では ありません。
　★★しまうと、★次に 開いた とき 前の 見え方が 戻り、★置き場と 混ざります。

### C ★舞台の 式を 1本に する（★① の 残り）

| ファイル | 行 | いま |
|---|---|---|
| `components/CharacterHome.jsx` | 2276-2278 | `fullBleed ? stageFit(…) : stageSize(…)` |
| `lib/roomStage.js` | 153 | `stageStyle` は **いつも** `stageSize` を 呼ぶ |

★★どちらか 1つに して、★`stageStyle` にも 同じ ものを 使わせます（★7行 ほど）。
★★いまの 端末では 2つが 同じ 値を 返して います（★実測 390×278.56）。
★★比の ちがう 端末（★iPad よこ）では 分かれます。

### D ★見張り（★新しく 2本）

- `k` が 画面で 分かれて いない こと（★`homeState` で 枝分かれして いない）
- 舞台の 式が 1本 である こと（★`stageFit` と `stageSize` の 両方を 使って いない）

★★どちらも 較正つき。★わざと 枝を 戻して、★落ちる ことを 確かめます。

## 二 ★受け入れの 線（★数で 決まります）

★`tools/room_place_probe.js` を そのまま 使います。

| 何 | いま | あと（★合格の 線）|
|---|---|---|
| 舞台基準の 差（★家具 8点）| 最大 **23.50** | **0.5 未満** |
| 床基準の 差（★家具 8点）| 0.01 未満 | ★そのまま 0.01 未満 |
| 大きさの 倍 | 1.600 | **1.000** |

★★★「たぶん 直った」で 終わりに しません。★この 表が 出て から 報告します。

## 三 ★手戻り（★§4 の 問い）

### ★捨てる もの

```
★㋖ の 実装 …… ★0行（★作って いません）
★㋗ の 実装 …… ★0行（★作って いません）
★置く画面の 独自の 描き方 …… ★★ありません
```

★★§4-2 の 問い ──「★舞台方式が 1つの 関数に なって いれば、
★置く画面が それを 呼ぶ だけ」── ★**なって います**。
★★置く画面は 同じ `RoomScene` を 通って います。★独自の 道は ありません。
★★分かれて いるのは **引数 2つ** だけ です。

### ★捨てない もの

```
○ 9月14日の 舞台方式      ★前提に なります
○ 9月16日の 重複定義の 解消 ★これが 無いと §2 が できません
○ きょうの 道具（room_place_probe）★受け入れの 線を これで 測ります
○ 見本 2本への 直し        ★別件（★見本の 側）
```

## 四 ★★代償と、★見て いただきたい こと

### 1 ★したくの 部屋が、★1.6倍に なります

★★いちばん 大きな 見た目の 変わり目 です。
★★引き出しの 上の 部屋が、★いまより 大きく 映ります。
★★**坂本さんに ご覧いただく 必要が あります。**

### 2 ★掴む ときに、★部屋が 1.0倍に 戻らなく なります

★★A の 2行目 です。★実機で 確かめます。

### 3 ★引いて 動かす 手と、★ものを 掴む 手が ぶつかります

★★どちらも「指で 引く」です。★分けが 要ります ──
　★もの の 上を 引いた … ★その ものを 動かす
　★空いた ところを 引いた … ★部屋を 動かす（`ox`/`oy`）

### 4 ★比の ちがう 端末で、★額ぶちの 出かたが 変わります

★★C の ぶん です。★iPad で 1度 見て いただく のが よいと 思います。

## 五 ★段取り

```
1 見張りを 先に 書く（★較正して、★いまは 落ちる ことを 確かめる）
2 A（k を 1つに）      … ★2行
3 実機で 測る          … ★舞台基準の 差が 0 に なるか
4 掴んで みる          … ★A の 2行目の 確かめ
5 B（ox/oy）           … ★50行
6 C（舞台の 式）       … ★7行
7 もう一度 測る        … ★受け入れの 表
8 見た目は 未確認の まま 報告し、★坂本さんに 撮って いただく
```

★★2 と 3 の あいだで **一度 止まります**。
★★`k` を そろえた だけ で 数が 0 に なる はず です。
★★ならなければ、★見立てが まちがって います。★そこで 引き返します。

## 六 ★大きさ

```
★直す 行     … だいたい 60〜70行（★3ファイル）
★新しい 見張り … 2本
★捨てる 行   … 0
```

★★小さい 仕事 です。★大きいのは **見た目の 変わり目**（★四の 1）です。

## 七 ★まだ 実装して いません

★★ご承認を いただいて から 動きます。
