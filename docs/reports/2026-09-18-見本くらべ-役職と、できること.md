# ★見本くらべ ── ★役職と、できること（`stPost`）

★全53行 ／ 末尾は「★★`VISUAL: UNVERIFIED` です。★実機の 写真で お確かめ ください。」

★裁定 その84 NEW_ORDER 2 ／ ★2026-09-18 ／ ★`tools/screen_diff.py stPost` が 書きました。

★実装 …… `components/OpsPosts.jsx`／`components/OpsPostMatrix.jsx`／`lib/opsPostMatrix.js`／`lib/opsPerms.js`

## ★数

| | 節 |
|---|---|
| 見本の 節 | 16 |
| ★① 実装にも ある | 11 |
| ★② 実装に ない | 5 |
| ★③ 作らないと 決めた | 0 |

★機械が 数えた 見本の 印 …… btn 1／fl 1／h3 1／inp 1／note 1／tw 1／warn 2

## ★② 実装に ない ── ★これから 直す もの（5）

| 見本 | 見本の字 | どう ちがうか |
|---|---|---|
| warn ①の 2行目（決まりは 1つ） | 決まりは 1つ ──「学校全部に かかる ことは、自分が 持っていないと 渡せません」 | ★実装では 触った ときの 断り（`CANNOT_GRANT`）に だけ あります。★上の 帯に 出て いません |
| warn ②（見るだけの 方への 断り） | あなたは 見るだけです。ひとの 役職を 変えられるのは <誰> ＋ whoCan('post') | ★実装に ありません。★`post` を 持たない 方に、★何も 断って いません |
| note ①（できことを 左の 列に。文字が 切れません） | できることを 左の 列に 置きました。文字が 途中で 切れません。 | ★実装に ありません |
| note ②（見出しの 行と 左の 列は 貼り付いて います） | 見出しの 行と、左の 列は 貼り付いています。横に すべっても、どの できことの、どの 役職かが 分かります。 | ★実装に ありません。★仕掛けは ありますが、★そう 書いて いません |
| note ③（役職名を 押すと 1つの 画面へ／丸は その場で 変わる） | 上の 役職名を 押すと、その役職 1つの 画面へ。丸を 押すと その場で 変わります。 | ★実装に ありません |

## ★③ 作らないと 決めた（0）

★ありません。

## ★① 実装にも ある（11）

| 見本 | どう ちがうか |
|---|---|
| h3「役職と、できること」 | ★同じ 字 です |
| warn ①（役職が そのまま できること） | ★2行目・3行目が ちがいます（★下の ② を ご覧ください） |
| warn ①の 3行目（その方 自身にだけ） | ★実装では いちばん 下の 注に あります。★見本は 上の 帯 です |
| 表（tw mx ／ 見出し行と 左の 列が 貼りつき） | ★`.tblwrap` に 移しました（★裁定 その81 §5-1）。★見本の 1か所だけ `tw mx` です |
| 役職の 列の 見出し（名前を 押すと 役職 1つへ ＋ 人数） | ★同じ です |
| その方 自身にだけ かかる 行（薄い 帯） | ★同じ です |
| 丸（●／○・選んで いる ときは --pick の 地） | ★同じ です。★押せない ときは 札に しません（★Option_B） |
| 最下段「この役職で 出るナビ」 | ★中身は `tabsForPerms` から 引きます。★見本の `navOf()` とは 3役職で ちがいます（★実装が 正・2026-09-18 ご承認） |
| fl「役職を 足す」＋ 入力 ＋ btn「足す」 | ★同じ です |
| note ④（薄い 帯の 4行） | ★字が ちがいます（見本は「薄い 帯の 4行は…」）。★数（4）を 書いて いません |
| note ⑤（足した 役職は できことが 1つも ない／人が いる間は 消せない） | ★同じ こと を 2行に 分けて 書いて います |

## ★見た目

★この 道具は **字** を 見ます。★描かれた 姿を 見て いません。
★★`VISUAL: UNVERIFIED` です。★実機の 写真で お確かめ ください。
