# ★「決まりが 無くて、黙って 0行 成功」── ★紙の 側の 一覧

全361行 / 末尾は「★2つを 重ねた ところが、★本当の 危ない 場所 です。」

生成: `tools/silent_zero_row_audit.py`（2026-09-16）

## ★何を 数えたか

- 読んだ ファイル: 289 本（app / components / lib、tests は 除く）
- 見つけた 書き込み: 140 か所
- ★そのうち **行数を 見て いない**もの: 110 か所
- 表の 数: 35

## ★これが なぜ 危ないか

`update` / `delete` は、★当たる 行が 0でも 誤りを 返しません。
`.select()` を 付けない 限り、★呼ぶ 側に 0行は 見えません。
2026-09-16、★教室を やめる が これで 黙って 失敗しました。

★`insert` / `upsert` は 別 です ── ★入らなければ 誤りが 返ります。
★危ないのは `update` と `delete` です。★下の 表で 分けて います。

## ★表ごと（★あぶない 順 ── update / delete が 先）

### `profiles` ── 31 か所（★update/delete 31）

- ★`update` app/api/account/delete/route.js:281
  - `.from("profiles")`
- ★`update` app/api/account/restore/route.js:30
  - `const { error } = await admin.from("profiles").update({ deleted_at: null }).eq("id", user.id);`
- ★`update` app/api/line-webhook/route.js:83
  - `.from("profiles")`
- ★`update` components/VocalTracker.jsx:6063
  - `supabase.from("profiles").update({ pwa_installed_at: new Date().toISOString() }).eq("id", userId).then(() => {`
- ★`update` components/VocalTracker.jsx:6081
  - `supabase.from("profiles").update({ pwa_install_prompted_at: new Date().toISOString() }).eq("id", userId).then(`
- ★`update` components/VocalTracker.jsx:6481
  - `.from("profiles").update(adoptedOcc).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:6535
  - `.from("profiles").update(adopted).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:9750
  - `await supabase.from("profiles").update({ garden_theme: themeKey }).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:10650
  - `const { error } = await supabase.from("profiles").update(patch).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:10678
  - `.from("profiles")`
- ★`update` components/VocalTracker.jsx:10709
  - `.from("profiles")`
- ★`update` components/VocalTracker.jsx:10730
  - `.from("profiles")`
- ★`update` components/VocalTracker.jsx:10742
  - `.from("profiles")`
- ★`update` components/VocalTracker.jsx:10800
  - `const { error } = await supabase.from("profiles").update(finalPatch).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:10812
  - `const { error } = await supabase.from("profiles").update(patch).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:10822
  - `const { error } = await supabase.from("profiles").update({ practice_reviews: updatedReviews }).eq("id", userId`
- ★`update` components/VocalTracker.jsx:10832
  - `const { error } = await supabase.from("profiles").update({ folded_groups: updated }).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:10849
  - `const { error } = await supabase.from("profiles").update({ record_mode: mode }).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:10983
  - `.from("profiles").update({ occupation_notice_shown_at: at }).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:11136
  - `const { error } = await supabase.from("profiles").update({ folded_groups: updated }).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:11147
  - `const { error } = await supabase.from("profiles").update(patch).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:11160
  - `const { error } = await supabase.from("profiles").update(patch).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:11179
  - `const { error } = await supabase.from("profiles").update(patch).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:11190
  - `const { error } = await supabase.from("profiles").update({ survey_day7_response: answer }).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:11201
  - `const { error } = await supabase.from("profiles").update({ survey_day7_shown_at: shownAt }).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:11210
  - `const { error } = await supabase.from("profiles").update({ day_record_boundary_hour: hour }).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:11217
  - `const { error } = await supabase.from("profiles").update({ display_name: name }).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:12594
  - `const { error } = await supabase.from("profiles").update({ line_link_code: code }).eq("id", userId);`
- ★`update` components/VocalTracker.jsx:12600
  - `const { error } = await supabase.from("profiles").update({ line_notification_enabled: enabled }).eq("id", user`
- ★`update` components/VocalTracker.jsx:12607
  - `const { error } = await supabase.from("profiles").update({ line_user_id: null, line_linked_at: null }).eq("id"`
- ★`update` components/VocalTracker.jsx:20950
  - `const { error } = await supabase.from("profiles").update({ consent_stats_use_at: value }).eq("id", userId);`

### `memberships` ── 6 か所（★update/delete 4）

- 　`insert` app/api/org/invitation/accept/route.js:139
  - `.from("memberships")`
- ★`update` app/api/org/posts/route.js:130
  - `const { error: e2 } = await admin.from("memberships")`
- ★`update` app/api/org/posts/route.js:171
  - `const { error } = await admin.from("memberships")`
- ★`update` app/api/org/posts/route.js:245
  - `const { error } = await admin.from("memberships")`
- 　`insert` components/VocalTracker.jsx:11273
  - `const { error: memError } = await supabase.from("memberships")`
- ★`update` components/VocalTracker.jsx:12203
  - `const { error } = await supabase.from("memberships")`

### `org_posts` ── 4 か所（★update/delete 3）

- 　`insert` app/api/org/posts/route.js:145
  - `const { error } = await admin.from("org_posts")`
- ★`update` app/api/org/posts/route.js:192
  - `const { error } = await admin.from("org_posts").update({ name }).eq("id", postId);`
- ★`update` app/api/org/posts/route.js:214
  - `const { error } = await admin.from("org_posts").update({ perms: next }).eq("id", postId);`
- ★`delete` app/api/org/posts/route.js:262
  - `const { error } = await admin.from("org_posts").delete().eq("id", postId);`

### `recovery_codes` ── 4 か所（★update/delete 3）

- 　`upsert` app/api/recovery/issue/route.js:95
  - `const { error } = await admin.from("recovery_codes").upsert({`
- ★`update` app/api/recovery/redeem/route.js:139
  - `.from("recovery_codes").update(patch).eq("user_id", user.id);`
- ★`update` app/api/recovery/redeem/route.js:155
  - `.from("recovery_codes")`
- ★`update` app/api/recovery/redeem/route.js:188
  - `await admin.from("recovery_codes")`

### `cycle_periods` ── 2 か所（★update/delete 2）

- ★`delete` components/VocalTracker.jsx:8587
  - `const { error } = await supabase.from("cycle_periods").delete().eq("id", target.id).eq("user_id", userId);`
- ★`update` components/VocalTracker.jsx:8604
  - `.from("cycle_periods").update({ end_date: endISO, updated_at: new Date().toISOString() })`

### `entries` ── 3 か所（★update/delete 2）

- 　`upsert` components/VocalTracker.jsx:2333
  - `(r) => supabase.from("entries").upsert(r, { onConflict: "user_id,date" }),`
- ★`delete` components/VocalTracker.jsx:13346
  - `const { error } = await supabase.from("entries").delete().eq("user_id", userId).eq("date", date);`
- ★`delete` components/VocalTracker.jsx:13356
  - `await supabase.from("entries").delete().eq("user_id", userId).eq("date", date);`

### `org_event_participants` ── 3 か所（★update/delete 2）

- ★`delete` components/VocalTracker.jsx:12147
  - `await supabase.from("org_event_participants").delete().eq("id", existing.id);`
- 　`insert` components/VocalTracker.jsx:12149
  - `await supabase.from("org_event_participants").insert({ user_id: userId, org_event_id: ev.id });`
- ★`update` components/VocalTracker.jsx:12158
  - `await supabase.from("org_event_participants")`

### `org_invitations` ── 3 か所（★update/delete 2）

- ★`update` app/api/org/invitation/accept/route.js:93
  - `await admin.from("org_invitations")`
- ★`update` app/api/org/invitation/accept/route.js:144
  - `.from("org_invitations")`
- 　`insert` components/VocalTracker.jsx:12372
  - `const { error } = await supabase.from("org_invitations").insert({ code, org_id: orgId, invited_by: userId, exp`

### `project_master` ── 4 か所（★update/delete 2）

- 　`upsert` components/VocalTracker.jsx:12769
  - `const { error } = await supabase.from("project_master").upsert({`
- 　`upsert` components/VocalTracker.jsx:12915
  - `await supabase.from("project_master").upsert({`
- ★`delete` components/VocalTracker.jsx:12919
  - `await supabase.from("project_master").delete().eq("user_id", userId).eq("project_name", from);`
- ★`delete` components/VocalTracker.jsx:13013
  - `const { error: projError } = await supabase.from("project_master")`

### `repertoire_tessitura` ── 6 か所（★update/delete 2）

- 　`upsert` components/VocalTracker.jsx:10111
  - `const { error } = await supabase.from("repertoire_tessitura")`
- 　`upsert` components/VocalTracker.jsx:12675
  - `.from("repertoire_tessitura")`
- 　`upsert` components/VocalTracker.jsx:12789
  - `const { error } = await supabase.from("repertoire_tessitura").upsert({`
- ★`delete` components/VocalTracker.jsx:12834
  - `const { error: deleteError } = await supabase.from("repertoire_tessitura").delete().eq("user_id", userId).eq("`
- 　`upsert` components/VocalTracker.jsx:12891
  - `const { error } = await supabase.from("repertoire_tessitura").upsert({`
- ★`delete` components/VocalTracker.jsx:12898
  - `const { error: delError } = await supabase.from("repertoire_tessitura")`

### `role_master` ── 4 か所（★update/delete 2）

- 　`upsert` components/VocalTracker.jsx:12746
  - `const { error } = await supabase.from("role_master").upsert({`
- 　`upsert` components/VocalTracker.jsx:12905
  - `await supabase.from("role_master").upsert({`
- ★`delete` components/VocalTracker.jsx:12911
  - `await supabase.from("role_master").delete().eq("user_id", userId).eq("role_name", from);`
- ★`delete` components/VocalTracker.jsx:13010
  - `const { error: roleError } = await supabase.from("role_master")`

### `subscriptions` ── 3 か所（★update/delete 2）

- 　`insert` app/api/stripe/checkout/route.js:119
  - `.from("subscriptions")`
- ★`update` app/api/stripe/webhook/route.js:134
  - `.from("subscriptions")`
- ★`update` app/api/stripe/webhook/route.js:229
  - `.from("subscriptions")`

### `article_notes` ── 2 か所（★update/delete 1）

- 　`insert` components/VocalTracker.jsx:11873
  - `const { error } = await supabase.from("article_notes").insert({`
- ★`update` components/VocalTracker.jsx:11882
  - `await supabase.from("article_notes").update({ deleted_at: new Date().toISOString() }).eq("id", noteId);`

### `events` ── 2 か所（★update/delete 1）

- ★`delete` app/api/cron/purge-events/route.js:29
  - `.from("events")`
- 　`insert` lib/events.js:164
  - `supabase.from("events").insert(row).then(() => {}, () => {});`

### `lessons` ── 3 か所（★update/delete 1）

- 　`insert` components/VocalTracker.jsx:11727
  - `const { error } = await supabase.from("lessons").insert({ link_id: linkId, scheduled_at: scheduledAt, note: ne`
- ★`delete` components/VocalTracker.jsx:11734
  - `await supabase.from("lessons").delete().eq("id", lessonId);`
- 　`insert` components/VocalTracker.jsx:12535
  - `const { error } = await supabase.from("lessons").insert({`

### `link_consents` ── 1 か所（★update/delete 1）

- ★`update` components/VocalTracker.jsx:11578
  - `const { error: consentError } = await supabase.from("link_consents")`

### `my_timetable` ── 2 か所（★update/delete 1）

- ★`delete` components/MyTimetable.jsx:272
  - `const r = await supabase.from("my_timetable").delete().eq("id", row.id);`
- 　`upsert` components/MyTimetable.jsx:276
  - `const r = await supabase.from("my_timetable").upsert(`

### `period_markers` ── 2 か所（★update/delete 1）

- 　`insert` components/VocalTracker.jsx:9868
  - `const { error } = await supabase.from("period_markers").insert(row);`
- ★`delete` components/VocalTracker.jsx:9872
  - `const { error } = await supabase.from("period_markers")`

### `account_deletions` ── 1 か所（★update/delete 0）

- 　`insert` lib/accountDeletion.js:379
  - `const { error } = await admin.from("account_deletions").insert({ deleted_at: deletedAt });`

### `age_answer_changes` ── 1 か所（★update/delete 0）

- 　`insert` components/VocalTracker.jsx:11164
  - `const { error: logError } = await supabase.from("age_answer_changes").insert({`

### `article_progress` ── 3 か所（★update/delete 0）

- 　`upsert` components/VocalTracker.jsx:11795
  - `await supabase.from("article_progress").upsert(row, { onConflict: "user_id,article_id" });`
- 　`upsert` components/VocalTracker.jsx:11808
  - `await supabase.from("article_progress").upsert({`
- 　`upsert` components/VocalTracker.jsx:11859
  - `await supabase.from("article_progress").upsert(`

### `assignments` ── 2 か所（★update/delete 0）

- 　`insert` app/api/enrollment/accept/route.js:151
  - `.from("assignments")`
- 　`insert` components/VocalTracker.jsx:12486
  - `const { error } = await supabase.from("assignments").insert({ org_id: orgId, teacher_id: teacherId, student_id`

### `chapter_state` ── 1 か所（★update/delete 0）

- 　`upsert` components/VocalTracker.jsx:11848
  - `await supabase.from("chapter_state").upsert(rows,`

### `character_inventory` ── 2 か所（★update/delete 0）

- 　`insert` app/api/character/buy/route.js:126
  - `.from("character_inventory")`
- 　`insert` app/api/character/gift/route.js:84
  - `.from("character_inventory")`

### `consent_records` ── 3 か所（★update/delete 0）

- 　`insert` components/VocalTracker.jsx:9787
  - `const { error: recError } = await supabase.from("consent_records").insert(`
- 　`insert` components/VocalTracker.jsx:9820
  - `const { error: recError } = await supabase.from("consent_records").insert(`
- 　`insert` components/VocalTracker.jsx:9848
  - `const { error } = await supabase.from("consent_records").insert(row);`

### `email_change_log` ── 2 か所（★update/delete 0）

- 　`insert` app/api/account/email/route.js:102
  - `const { error: logErr } = await admin.from("email_change_log").insert({`
- 　`insert` app/api/recovery/redeem/route.js:197
  - `const { error: logErr } = await admin.from("email_change_log").insert({`

### `enrollments` ── 1 か所（★update/delete 0）

- 　`upsert` app/api/enrollment/accept/route.js:114
  - `.from("enrollments")`

### `feedback` ── 1 か所（★update/delete 0）

- 　`insert` app/api/feedback/route.js:37
  - `const { error: dbError } = await admin.from("feedback").insert({`

### `my_periods` ── 1 か所（★update/delete 0）

- 　`insert` components/MyTimetable.jsx:373
  - `const r = await supabase.from("my_periods").insert(`

### `org_message_reads` ── 1 か所（★update/delete 0）

- 　`insert` components/VocalTracker.jsx:10011
  - `const { error } = await supabase.from("org_message_reads")`

### `purchases` ── 1 か所（★update/delete 0）

- 　`insert` app/api/stripe/webhook/route.js:111
  - `const { error } = await admin.from("purchases").insert({`

### `system_alerts` ── 1 か所（★update/delete 0）

- 　`insert` lib/systemAlert.js:71
  - `const { error } = await admin.from("system_alerts")`

### `teacher_invitations` ── 1 か所（★update/delete 0）

- 　`insert` components/VocalTracker.jsx:11306
  - `const { error } = await supabase.from("teacher_invitations")`

### `teacher_notes` ── 1 か所（★update/delete 0）

- 　`upsert` components/VocalTracker.jsx:11681
  - `const { error } = await supabase.from("teacher_notes").upsert({ link_id: linkId, body, updated_at: new Date().`

### `user_notices` ── 2 か所（★update/delete 0）

- 　`insert` app/api/cron/pricing-notice/route.js:140
  - `const { error: markErr } = await admin.from("user_notices")`
- 　`upsert` components/VocalTracker.jsx:10661
  - `.from("user_notices")`

## ★この 一覧で 足りない こと

★これは **紙** です。★台帳では ありません。
「`.select()` が 無い」と「決まりが 無い」は 別の 話 です ──
決まりが あって ちゃんと 書けて いる ところも、★ここに 並びます。

★本当に 知りたいのは、
**`authenticated` に 表の 権利が あって、対応する 決まりが 無い 表**です。
★それは 台帳に しか ありません。
`supabase/問い-決まりの無い書き込み.sql` を お走らせください。

★2つを 重ねた ところが、★本当の 危ない 場所 です。
