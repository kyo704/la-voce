# ★数える 人数が、★どの 学校でも 0人 です

全100行 / 末尾は「## 五 ★まだ 何も 直して いません」

## 一 ★見つけた こと

★★⑥の 絵を 撮る ため、★仮の 在籍を 入れる SQL を 書こうと して、★止まりました。

```
台帳 memberships の 決まり（memberships_role_check）
  role は  owner ／ admin ／ teacher ／ staff  の 4つ だけ
  ★'student' は 入れられません

lib/orgRoster.js
  NOT_COUNTED_ROLES = ["teacher", "staff", "owner", "admin"]
  isCounted() … この 4つは **数えません**

つまり
  rosterCount(memberships) は ★★どんな 場合でも 0 です
```

★★本番の memberships を 数えました ── ★owner 7／admin 7／teacher 7／staff 3。
　★★**4つ とも、★数えない ほう** です。★例外は 1行も ありません。

## 二 ★だから どうなって いるか

```
数える 人数     0人      ← ★いつも
1人あたり       —
今月の ご請求   0円      ← ★いつも
⑥の 内訳の 箱  出ません  ← ★0円の ときは 出さない 決め（きょう 私が 書きました）
```

★★★仮の 在籍を 入れても、★この 数は 動きません。
　★★`memberships` に 生徒を 足せません（★決まりが 許しません）。
　★★`enrollments` に 足しても、★この 画面は そちらを 見て いません。

## 三 ★どこを 見れば よかったか ── ★もう 直って いる 画面が あります

★★名簿の 画面は、★すでに 正しい ほうを 見て います。

```js
// components/VocalTracker.jsx:13940
//   ★★生徒は enrollments に 居ます。★そちらを 読みます。
const members = toRosterRows(
  orgEnrollments[opsOrgId] || [],
  orgAssignments[opsOrgId] || []);
```

★★同じ 直しが、★ほかの 4か所に 届いて いません。

| どこ | いま 渡して いる もの | 出て いる 数 |
|---|---|---|
| `OpsSettings`（ご請求） | `opsMembers`（memberships） | ★いつも 0 |
| `OpsHome`「名簿の人数」 | `opsMembers` | ★いつも 0 |
| `AnnouncementCompose` の `memberCount` | `mem`（memberships） | ★いつも 0 |
| `opsTargetOf`（出席の 分母） | `opsMembers` | ★いつも 0 |
| `OpsRoster`（名簿） | `toRosterRows(enrollments, …)` | ★正しい |

★★`lib/orgRoster.js:188` に、★同じ かたちの 直しの 記録が 残って います ──

```
★★2026-09-13、★ここで 役割を 見て いました。
  ★★台帳が 許す 役割は owner／admin／teacher／staff の 4つ だけ で、
    ★★その 4つ とも ここで 落ちて いました。
  ★★だから 名簿は **どの 学校でも 1行も 出ません**でした。
```

★★★`matchesChip` は 直り、★`rosterCount` の 呼ぶ 側 4つが 残りました。
　★★同じ 決めが 2か所に あって、★片方だけ 直った ── ★この 蔵の 持病 です。

## 四 ★お伺い

★★★仮の 在籍（テストデータ）の 投入は、★**お待ちください**。
　★★入れても 数は 動きません。★消してよい 教室であっても、★意味が ありません。

★★直し方は 小さい です ──
　★★運営の ところで 1つ だけ 作り、★4か所に 同じ ものを 渡します。

```js
const opsRoster = toRosterRows(
  orgEnrollments[opsOrgId] || [],
  orgAssignments[opsOrgId] || []);
```

★★★ただ、★これは ⑥ の 外です。★user に 見える 数が 4画面で 変わります。
　★★0人 → 実際の 人数。★ご請求の 額も 0円 から 動きます。
　★★だから、★お決めを いただいて から 直します。

★★お決めの 選び方

```
㋐ いま 直す（★お勧め）
   ★★⑥は「稟議の 数字」の ため の もの です。
   ★★数が いつも 0 の まま だと、★⑥を 作った 意味が ありません。
㋑ ⑦へ 進み、★これは 別の 番として 後で
   ★★画面の 形は そろいます。★数は 0の まま です。
```

## 五 ★まだ 何も 直して いません
