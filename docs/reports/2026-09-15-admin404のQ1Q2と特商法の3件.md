# /admin の Q1・Q2 の答え ／ 特商法の3件 ／ 近い行事

全197行 / 末尾は「未push 1本になります（近い行事＋error.js）。ご判断をお待ちします。」

- 日付 … 2026-09-15

---

# 第1部 ── ★Q1・Q2（3便つづけてお尋ねいただいた件）

★答えは前便でもお出ししていますが、中継で落ちているようです。
**本番のコミットそのもの**を読んで確かめました。手元の作業中の姿ではありません。

## Q1 ★引数は、渡しています

```
$ git show 4bb53bb:app/admin/page.js | sed -n '165,166p'

  const { data: entryStats, error: entryStatsError } =
    await admin.rpc("admin_entry_stats", { p_user_id: user.id });
```

★引数なしの呼び出しは、本番に**0件**です。

```
$ git show 4bb53bb:app/admin/page.js | grep -c 'rpc("admin_entry_stats")'
0
```

★**Opus の見立ては当たっていません。そして Opus の落ち度でもありません。**
SQL とコードは、同じ形で入っています（`e85e3fe` と No.024 の紙）。

## Q2 ★投げません。受けて、画面に出します

```js
const { data: entryStats, error: entryStatsError } =
  await admin.rpc("admin_entry_stats", { p_user_id: user.id });
...
const statsOk = !!(entryStats && entryStats.fill);
...
{!statsOk && (
  <p>入力率を 数えられませんでした{entryStatsError ? `（${entryStatsError.message}）` : ""}。
     古い 取り方には 戻しません。</p>
)}
```

★`PGRST202` が返っても、404 にはなりません。
この赤い1行が出て、★ほかの中身はそのまま出ます。

★ページの中で `try` に包まれていない `await` は8つありますが、
どれも `{ data, error }` で受けています。★supabase-js は投げません。

投げうるのは `createAdminClient()` だけです
（`SUPABASE_SERVICE_ROLE_KEY` が無ければ supabase-js が投げます）。
★ただしそれなら `/api/feedback` も `/api/character/unlock` も同時に壊れます。

## ★私にできることと、できないこと

できたこと ──

| 確かめ | 結果 |
|---|---|
| `4bb53bb` にページが在るか | ★在る（28,346バイト） |
| 出す道 | `/admin`。入れ子でない |
| `notFound()` | ★無い |
| `not-found.js` / `error.js` | ★無かった（★いま足しました。下記） |
| `middleware.js` | 行き先を変えない。`next()` を返すだけ |
| `vercel.json` | `/admin` の扱い無し |
| `.vercelignore` | `/admin` を外していない |
| 組み立て | `ƒ /admin` として出る |
| ★本番に直に | `/admin` → **HTTP 307**（ログインへ）。★404 ではない |

できないこと ── ★ログインした状態を作れません。
★Vercel の記録も読めません（CLI が入っていません）。

## ★だから、見えるようにしました

`app/admin/error.js` を足しました（★未push）。
`/admin` の下だけにかかる、投げたものを受け止める1枚です。

★これで、次にお開きになったときに答えが出ます。

| 出るもの | 意味 |
|---|---|
| 「管理画面を 出せませんでした」＋ 文 | ★どこかが投げています。★その文をお知らせください |
| ★それでも 404 | ★投げていません。★配りの道の話です（PWA／Service Worker／別の行き先） |

★推測をこれ以上重ねません。1回の再読み込みで切り分きます。

---

# 第2部 ── 特商法の3件

## Q1 ★「一年のよそおい」は、売れません

`lib/plans.js` の `PLANS` は**2件だけ**です。

```
monthly / annual
```

★`12,800` という金額は、`lib/plans.js` にありません。
アプリ全体でも、出てくるのは `lib/orgRoster.js`（★学校の束の下限額。
2026-09-13 に 9,800円へ改めた記録）だけで、★別のものです。

`app/api/stripe/checkout/route.js` は `PLAN_KEYS` 以外を受け取りません。

★つまり、**作られていません。** 11条の義務は、まだ生じていません。
★裁定のとおり、できるまで紙に触りません。

## Q2 ★最終確認画面は、Stripe の画面です

アプリの中に、押す前の確認画面はありません。
`/billing` から `api/stripe/checkout` へ行き、★Stripe の画面へ移ります。

★例外が1つあります ── 15〜17歳の方には、
**先に同意の画面**が出ます（`app/billing/page.js:303`）。
そこを押してから、月額だけへ進みます。同じ帯で Checkout も止めています。

★12条の6が言う「最終確認画面」を私たちが持っていないことになります。
★Stripe の画面に返品特約を載せられるかは、Stripe の設定の話です。私には分かりません。

## ★1件だけ、いますぐお伝えしたいこと

裁定1（未経過月×400円の返金）について。

★**紙と、いまのコードは、合っています。**
食い違っているのは、★B案の裁定のほうです。

`lib/plans.js` の `annual` は ──

```js
// ★★1回払いの 買い切りです（★2026-09-09 の 方向転換）。
//   ★★自動更新は ありません。★お申し込みの日から 1年です。
//   ★途中で やめる 仕組みは 作りません。★期間中は いつでも 使えます。
buyOnce: true,
```

★返金の道は、**1本もありません**。
紙を先に直すと、★守れない約束を書くことになります
（★書き出しの ㋒ で、同じ順序の話をしたばかりです）。

★順序としては ── 返金の道を作る → そのあとで紙を直す、です。
どちらを先にするかは、坂本さんのご判断です。

## ★残すべき4行、控えました

裁定のとおり、書き直しのときに失わないよう記録します。

```
記録・書き出しは、無料です
受け取られたよそおいは、解約のあとも、そのまま残ります
18歳未満の方…理由は伺いません
★体験期間はありません。直近7日は、はじめからずっと無料です
```

---

# 第3部 ── 近い行事（★入れました・未push）

読むだけです。裁定のとおり ──

- ★「出ます」の印をつける道を置いていません。`org_event_participants` に触れていません
- ★添える1行「行事の 出欠は 集めません。知らせるだけです。」（見本のまま）
- ★日づけは日づけのまま。「あと◯日」と書いていません
- ★曜日×コマの型紙に重ねていません。日づけの並びです
- ★日づけが変わった行事は「9月18日 から 変わりました」と言います
  （`previous_date` は前の日で、繰り返しではありません）
- ★1件も無ければ、節ごと出しません

見張り `classroom-shell.test.js` は 35本 → **54本**。
わざと4通り壊して、落ちることを確かめました。

---

# 第4部 ── ご報告のみ

★門の試しの一覧に入っている方は、★有料の面を1度も見られません。

`mayViewSummary` は、`NEXT_PUBLIC_GATE_TEST_USER_IDS` に載っている方に
`true` を返します。だから `paidGateApplies` が `false` になり、
★値段の札そのものが出ません。

★つまり坂本さんは、有料まわりの直しを**目で確かめられません**。
一覧に入っていない試しの口を1つ持たれるのがよいと思います。

★env の一覧には触れていません（裁定のとおり）。

---

## たしかめ

- `npm run lint` … エラーなし
- `npm run build` … ✓ Compiled successfully
- 見張り … 通り307／落ち10（落ちた10本は、もとから落ちているものです）

未push 1本になります（近い行事＋error.js）。ご判断をお待ちします。
