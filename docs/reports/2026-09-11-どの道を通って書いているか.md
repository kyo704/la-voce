# ★どの 道を 通って 書いて いるか

★この 紙は tools/write_paths.py が 書き出します。★手で 書いて いません。

★★見つけた 行を そのまま 並べます。★言い分は 足しません。

## 役職と 所属　（memberships ／ org_posts）

| 場所 | 道 | 行 |
|---|---|---|
| `app/api/org/invitation/accept/route.js:11` | **サーバ（RLS あり）** | `//       supabase.from("memberships").insert({ org_id, user_id, role: "teacher" })` |
| `app/api/org/invitation/accept/route.js:126` | **サーバ（RLS あり）** | `.insert({ org_id: orgId, user_id: user.id, role });` |
| `app/api/org/posts/route.js:112` | **裏口（service role）** | `const { data: made, error } = await admin.from("org_posts").insert(rows).select("id, name");` |
| `app/api/org/posts/route.js:131` | **サーバ（RLS あり）** | `.update({ post_id: top.id }).eq("org_id", orgId).eq("user_id", user.id);` |
| `app/api/org/posts/route.js:146` | **サーバ（RLS あり）** | `.insert({ org_id: orgId, name, perms: {}, sort_order: 999 });` |
| `app/api/org/posts/route.js:172` | **サーバ（RLS あり）** | `.update({ post_id: null }).eq("org_id", orgId).eq("user_id", targetUser);` |
| `app/api/org/posts/route.js:192` | **裏口（service role）** | `const { error } = await admin.from("org_posts").update({ name }).eq("id", postId);` |
| `app/api/org/posts/route.js:214` | **裏口（service role）** | `const { error } = await admin.from("org_posts").update({ perms: next }).eq("id", postId);` |
| `app/api/org/posts/route.js:246` | **サーバ（RLS あり）** | `.update({ post_id: postId }).eq("org_id", orgId).eq("user_id", targetUser);` |
| `app/api/org/posts/route.js:262` | **裏口（service role）** | `const { error } = await admin.from("org_posts").delete().eq("id", postId);` |
| `components/VocalTracker.jsx:10956` | **ブラウザ 直** | `.insert({ org_id: org.id, user_id: userId, role: "owner" });` |
| `components/VocalTracker.jsx:11737` | **ブラウザ 直** | `.update({ grade_label: value })` |
| `components/VocalTracker.jsx:11980` | **ブラウザ 直** | `.update({ role: newRole }).eq("id", membershipId).select("id");` |

★通って いる 道　**サーバ（RLS あり）／ブラウザ 直／裏口（service role）**

## レッスンの 日程　（lessons）

| 場所 | 道 | 行 |
|---|---|---|
| `components/VocalTracker.jsx:11364` | **ブラウザ 直** | `const { error } = await supabase.from("lessons").insert({ link_id: linkId, scheduled_at: scheduledAt…` |
| `components/VocalTracker.jsx:11371` | **ブラウザ 直** | `await supabase.from("lessons").delete().eq("id", lessonId);` |
| `components/VocalTracker.jsx:12048` | **ブラウザ 直** | `const { error } = await supabase.from("lessons").insert({` |

★通って いる 道　**ブラウザ 直**

## レッスンの 出席　（lessons の attendance 列）

| 場所 | 道 | 行 |
|---|---|---|
| `components/VocalTracker.jsx:9739` | **ブラウザ 直** | `.update(patch).eq("id", lesson.id).select("id");` |
| `components/VocalTracker.jsx:11300` | **ブラウザ 直** | `.update(nextStatus` |

★通って いる 道　**ブラウザ 直**

## ★まとめ

| 何を 書くとき | どの 道 |
|---|---|
| 役職と 所属 | **サーバ（RLS あり）／ブラウザ 直／裏口（service role）** |
| レッスンの 日程 | **ブラウザ 直** |
| レッスンの 出席 | **ブラウザ 直** |

## ★これが 50通りの 読み方に どう 効くか

★★`tools/perm_matrix.py` は、★**ブラウザ 直**の 道を 試して います。

### 役職と 所属

★★道が **混ざって** います。★1つでは ありません。

- ★ブラウザ 直 …… 3 か所
  - `components/VocalTracker.jsx:10956`　`.insert({ org_id: org.id, user_id: userId, role: "owner" });`
  - `components/VocalTracker.jsx:11737`　`.update({ grade_label: value })`
  - `components/VocalTracker.jsx:11980`　`.update({ role: newRole }).eq("id", membershipId).select("id");`
  ★★この ぶんは、★50通りの 結果が そのまま 当てはまります。
- ★サーバ／裏口 …… 10 か所
  ★★この ぶんは、★50通りの 結果が 当てはまりません。
  ★★裏口（service role）は、★許しも 決まりも 飛び越えます。
  ★★止めて いるのは 台帳では なく、★その 経路の コードだけ です。

### レッスンの 日程

★画面も **ブラウザ 直**です。★50通りが 試したのと 同じ 道です。
★★だから 結果は、★その 道に ついては 本物です。
- `components/VocalTracker.jsx:11364`　`const { error } = await supabase.from("lessons").insert({ link_id: lin`
- `components/VocalTracker.jsx:11371`　`await supabase.from("lessons").delete().eq("id", lessonId);`
- `components/VocalTracker.jsx:12048`　`const { error } = await supabase.from("lessons").insert({`

### レッスンの 出席

★画面も **ブラウザ 直**です。★50通りが 試したのと 同じ 道です。
★★だから 結果は、★その 道に ついては 本物です。
- `components/VocalTracker.jsx:9739`　`.update(patch).eq("id", lesson.id).select("id");`
- `components/VocalTracker.jsx:11300`　`.update(nextStatus`

## ★日程を **あとから 直す** 働きが あるか

★`scheduled_at` を `.update(` で 書き換えて いる 行は **ありません**。

★★つまり、★レッスンの 時刻を あとから 直す 働きは、★いま ありません。
　★★あるのは「作る」「消す」「出席を 付ける」の 3つ だけ です。
　★★期待表の「レッスンの 日程」は、★作る ことを 指して いると 読めます。

