# B01（くらべる）と、注記を 畳む こと

全120行 / 末尾は「★`<Note fold>` と 書いた くらべる の 3か所だけです。」

## 1　いま どこまで

**コードに 入れました。見張りは 通りました。見た目は 未確認です。実機で お確かめください。**

比較画像は、★まだ ありません。★理由は §5 に 書きます。

## 2　B01（くらべる）に 入れた もの

### 2-1　調べていることの 順番（★見本 SC['順番']）

見本の この 画面が、★実装に ありませんでした。★作りました。

- `lib/compareOrder.js`（★新しい 帳面）
- `components/CompareV2.jsx` の `OrderScreen`
- 見張り `components/tests/compare-order.test.js`（★28）

決まりは、★見本の 字を そのまま 使っています。

| 見本の 字 | どこ |
|---|---|
| 調べていることの 順番 | 題／くらべる の 行 |
| 疑っている 順に、5つまで。**1番目だけ**、補正なしで 見ます。 | 上の 帯 |
| 1番目　／　いま 見ています | 下の 札 |
| 2番目から先　／　1番目で 差が 見えなかったので、ここで 止まっています | 下の 札 |
| 順番を 変えたので、ここから 数え直します | 入れ替えた ときの 一言 |

- 5つまでです。★6つ 渡しても 5つに 切ります。
- いちばん 上の ↑ は 押せません（★見本も opacity .25 で 押せません）。
- **調べるのは 1番目だけ**です。★だから 検定は いつも 1回で、
  ★たくさん 撃った ぶんの 上のせが かかりません（★差の 大きさ 0.50）。
- **好きな ものを 直に 選ぶ 札は、置いていません。**
  ★見てから 選び直せると、★いちばん よく見える 組を 選べて しまうからです。
- 並びは **端末の 覚え書き**（localStorage）です。★列を 増やしていません。

### 2-2　1文の 3つの門

`lib/compareView.js` に `compareVerdict()` を 置き、★門を 1か所に しました。

- 10日以上　／　差の 大きさ　／　q
- 通らない ときは、★**止まった 理由だけ** 出します。
- 「あと◯日」は 出しません。★確率も、割合も、順位も 出しません。

## 3　★訂正　── 見本には、畳む しくみが ありました

9月11日、★私は「見本に 畳む しくみは ありません」と ご報告しました。
**これは 誤りです。**

- 私は `<details>` と `<summary>` を 数えて、★0 だったので そう 申しました。
- 見本は `<details>` を 使わず、**JavaScript で 畳んで** いました。
  `foldNotes(root)`（★見本の 4064行あたり）が、★描くたびに
  `.note` を すべて 閉じ、★上に 札を 1つ 足します。
- 札の 字は「**くわしい 決まりを 見る**」。★押すと 開き、「**閉じる**」に なります。

坂本さんは、★この 誤った ご説明の 上で ㋐（畳まず 隠す）を お決めに なりました。
**お決めの 前提が、誤っていました。** お詫び します。

### 3-1　いま どう しているか

- `components/UiV2.jsx` の `Note` に、★畳む しくみを 足しました。
- **既定は「畳まない」です。**　`<Note fold>` と 書いた ところだけ 畳みます。
- いま 畳んでいるのは **くらべる（B01）の 3か所だけ**です。
- 見張り `components/tests/note-fold.test.js`（★12）が、
  ★畳んでいる 画面が くらべる だけで あることを 見ています。

### 3-2　なぜ 既定を「畳まない」に したか

一度、★既定を「畳む」に しました。★そのあと 呼び出し元を 数えて、
**門（layoutV2）の 外でも `Note` が 使われている** ことに 気づき、戻しました。

| 門の 外で `Note` を 使っている ところ | どこに 出るか |
|---|---|
| `components/LookBackPanel.jsx` | 分析タブの `!layoutV2` の 側 |
| `components/OpsPosts.jsx` | 門で 囲われて いません |
| `components/RangeCalendar.jsx` | 門で 囲われて いません |

既定を「畳む」に した ままなら、★**38人の 画面が 変わって いました**。

### 3-3　お尋ね

ほかの 画面も、★見本どおり 畳みますか。

- ㋐　畳む。★1画面ずつ、★門の 中の 画面から 順に 移す
- ㋑　くらべる だけ 畳む。★ほかは いまの まま
- ㋒　畳まない。★くらべる も 戻す（★`fold` を 外すだけです）

## 4　そのほか、この 便で 入れた もの

- ノートの seg が「連絡」の ときは **＋ を 出しません**（★Opus の 裁定・案A）。
- 撮影の 名前を、★見本の `SC[]`／`SH[]` に 直しました。
  ★`A06-*` などの 名は、★見本に ありません。
- `SH-newnote` は、★**実装の 辿り方が まだ 分かりません**。
  ★これまで「連絡」＋「＋」で 撮っていました。★誤りです。
  ★見つかるまで 撮りません。★くらべる 絵には「★ありません」と 出します。
- 古い design.zip と、★そこから 展開した もの ── ★もとから ありませんでした。
  ★`docs/design/design_opusによる修正版.zip` だけ 消しました。
  ★正は `design_2.zip` から 作り直した `docs/design/pack-final` です。
  ★4本の 動く見本 HTML は、★前の 一式と **1バイトも 変わりません**でした。

## 5　★止まっている こと　── 比較画像が 作れません

比較画像を 作るには、★実装の 側を 撮る 必要が あります。
撮影は **本番（woolsong.app）に 入って** 行います。

- `git push` が、★この 場から できませんでした（★許可が 下りませんでした）。
- `.env.local` が 手元に 無いため、★手元で 動かして 撮る ことも できません。

**commit は 済んでいます。**　`b0066a8` と、★この あとの 1つ。
push を 通していただければ、★撮って 比較画像を お出しします。

見本の 側（左半分）は、★29枚 撮り終えています。
`docs/design/compare/all/mihon/`

## 6　38人の 画面

変わりません。★この 便の 見た目の 変更は、★どれも 門（layoutV2）の 中か、
★`<Note fold>` と 書いた くらべる の 3か所だけです。
