# sql/10・11 ── 試しの 台帳で 通しました（2026-09-23）

★道具 …… `tools/sql1011_prove.js`
★結果 …… **10 / 11**

---

## ★★★Opus へ お返しする もの（★直して いません）

### `rotate_calendar_token` が 動きません

```
ERROR: function gen_random_bytes(integer) does not exist
```

★わけ（★台帳を 読んで 確かめました）──

```
pgcrypto は 入って います。★ただし 置き場が `extensions` です。
  試しの 台帳 …… pgcrypto | extensions
  本番の 台帳 …… pgcrypto | extensions        ★どちらも 同じ

rotate_calendar_token の 設定 …… search_path=public
  ★★`extensions` が 入って いません。★だから 見つかりません。
```

★★★おもしろい ところ ── ★**列の 既定は 動きます**。

```
calendar_tokens.token の 既定 …… encode(gen_random_bytes(24), 'hex')
★試しの 台帳に 1行 入れて みました → ★通りました
```

★★既定は、★入れる ときの 場（session）の `search_path` で 解かれます。
★★関数は、★自分で 留めた `search_path` で 解かれます。★そこに `extensions` が ありません。

★★★直し方は 2つ あります。★**どちらも 当てて いません**（★Opus に お返しします）。

```
㋐ set search_path to 'public', 'extensions'
㋑ extensions.gen_random_bytes(24)  と 名ざしで 書く
```

★★`set search_path to ''`（空）に して 全部 名ざし、という 形も あります。
　★この 倉庫の ほかの 関数（`profiles_guard_server_only_columns`）は それ です。

---

## 通った 10件

### 一 レッスン割（sql/10）

| | 見た こと | 結果 |
|---|---|---|
| ① | 自分の 希望を 書ける（★較正・通る 側） | ○ 1行 |
| ② | よその 人の 希望は 書けない | ○ RLS 違反 |
| ③ | 締切の 過ぎた 回には 書けない | ○ RLS 違反 |
| ④ | `pref_map` が 返す 列は `slot_key` / `maru` / `sankaku` **だけ** | ○ |
| ④-2 | 人の id も 名前も 返って いない | ○ |
| ⑤ | カレンダーの 住所を 作り直せる | ★**落ちました**（上） |

★★★④が いちばん 大事な ところ です。★地図は **数 だけ** を 返します。
　★誰が ◎を 付けたかは、★返り値の どこにも ありません。

### 二 ポートフォリオ／ホームページ（sql/11）

| | 見た こと | 結果 |
|---|---|---|
| ⑥ | `set_web_type` が 答えを 返す | ○（★返り `need_payment`） |
| ⑦ | 紙の 型は いつでも 変えられる | ○ 1行 |
| ⑧ | 公開して いない slug への 問い合わせ → `false` | ○ |
| ⑨ | 公開して いる slug → `true`・1行 増える（★較正） | ○ 0 → 1 |
| ⑩ | 入って いない 人（anon）には 見えない | ○ permission denied |

★★★⑧が 大事な ところ です。★`false` だけ 返ります。★わけを 返しません。
　★「その 人が 居るか」を 探る 道に なりません。

---

## ★正直に 書いて おく こと

★⑥は「答えが 返る」ことしか 見て いません。

★返り `need_workflow`… ではなく `need_payment` でした。★試しの 行に
`trial_until` を 入れて いない から です。
★★★つまり「14日の あいだは ただ」の 枝を、★**まだ 通して いません**。

★★通すには、★`trial_until` を きょうより 後に した 行が 要ります。
★★★480円の 枝・持って いる 形へ 戻る 枝 も、★まだ です。

---

## 形の 確かめ（★当てた あとに 台帳を 読みました）

```
表 6つ（★6つとも RLS 入り）
  lesson_rounds / lesson_prefs / lesson_ng_dates / calendar_tokens /
  page_types_owned / page_inquiries

関数 5つ（★5つとも security definer）
  pref_map / rotate_calendar_token / seed_prefs_from_timetable /
  set_web_type / submit_inquiry

決まり 10本
  lesson_rounds_select / _update / _write
  lesson_prefs_own / _read_staff
  lesson_ng_own / _read_staff
  calendar_tokens_own
  page_types_owned_select_own
  page_inquiries_owner_select
```

★2本 とも **2度** 流しました。★2度目も 誤り 0 です。

---

## 本番

★★★**1本も 当てて いません。**
★★`rotate_calendar_token` の 直しが Opus から 返って から に します。
