# ★検査 その4 ── ★同じ形の 穴

★2026-09-20 ／ ★`tools/insp_same_shape.py` が 書きました。

★★★1度 起きた 誤りを、★形として 書き留め、★全体に 当てます。
★★どの 形にも 較正（当たり・外れ）が 付いて います。

| 形 | 見つかった 数 |
|---|---|
| 0行を 成功に する（`.update` / `.delete` に `.select()` が 無い） | 66 |
| 禁じ字を 覚え書きごと さがして いる 見張り | 0 |
| 測った 数を 覚えて いる 見張り | 3 |
| 門の 外れに 倒れる 比べ（`!== "Bearer undefined"` の 形） | 0 |
| `select("*")` を 画面の 門の 後ろに 置く | 32 |
| 読めなかった ときに 空を 入れる | 0 |

## ★0行を 成功に する（`.update` / `.delete` に `.select()` が 無い）

★わけ …… PostgREST は 0行でも 成功を 返します。★消えて いないのに「消しました」と 出ます。

★★同じ 形でも 誤りで ない もの …… 消えたか どうかを 見なくて よい 書き（★見た 印・並びの 保存 など）。★1つずつ 見て ください。

- `app/api/account/delete/route.js:281 .from("profiles")`
- `app/api/account/restore/route.js:30 const { error } = await admin.from("profiles").update({ deleted_at: nu`
- `app/api/cron/purge-events/route.js:29 .from("events")`
- `app/api/enrollment/accept/route.js:183 .from("roster_drafts")`
- `app/api/line-webhook/route.js:83 .from("profiles")`
- `app/api/org/invitation/accept/route.js:93 await admin.from("org_invitations")`
- `app/api/org/invitation/accept/route.js:144 .from("org_invitations")`
- `app/api/org/posts/route.js:161 const { error: e2 } = await admin.from("memberships")`
- `app/api/org/posts/route.js:232 const { error } = await admin.from("org_posts").update({ name }).eq("i`
- `app/api/org/posts/route.js:254 const { error } = await admin.from("org_posts").update({ perms: next }`
- `app/api/org/posts/route.js:316 const { error } = await admin.from("org_posts").delete().eq("id", post`
- `app/api/recovery/redeem/route.js:139 .from("recovery_codes").update(patch).eq("user_id", user.id);`
- `app/api/recovery/redeem/route.js:155 .from("recovery_codes")`
- `app/api/recovery/redeem/route.js:188 await admin.from("recovery_codes")`
- `app/api/stripe/webhook/route.js:134 .from("subscriptions")`
- `app/api/stripe/webhook/route.js:229 .from("subscriptions")`
- `components/MyTimetable.jsx:283 const r = await supabase.from("my_timetable").delete().eq("id", row.id`
- `components/VocalTracker.jsx:6192 supabase.from("profiles").update({ pwa_installed_at: new Date().toISOS`
- `components/VocalTracker.jsx:6210 supabase.from("profiles").update({ pwa_install_prompted_at: new Date()`
- `components/VocalTracker.jsx:6610 .from("profiles").update(adoptedOcc).eq("id", userId);`
- `components/VocalTracker.jsx:6664 .from("profiles").update(adopted).eq("id", userId);`
- `components/VocalTracker.jsx:6673 .from("profiles").update(帯).eq("id", userId);`
- `components/VocalTracker.jsx:8807 const { error } = await supabase.from("cycle_periods").delete().eq("id`
- `components/VocalTracker.jsx:8824 .from("cycle_periods").update({ end_date: endISO, updated_at: new Date`
- `components/VocalTracker.jsx:9970 await supabase.from("profiles").update({ garden_theme: themeKey }).eq(`
- `components/VocalTracker.jsx:10092 const { error } = await supabase.from("period_markers")`
- `components/VocalTracker.jsx:10953 const { error } = await supabase.from("profiles").update(patch).eq("id`
- `components/VocalTracker.jsx:10987 .from("profiles")`
- `components/VocalTracker.jsx:11018 .from("profiles")`
- `components/VocalTracker.jsx:11039 .from("profiles")`
- `components/VocalTracker.jsx:11051 .from("profiles")`
- `components/VocalTracker.jsx:11109 const { error } = await supabase.from("profiles").update(finalPatch).e`
- `components/VocalTracker.jsx:11121 const { error } = await supabase.from("profiles").update(patch).eq("id`
- `components/VocalTracker.jsx:11131 const { error } = await supabase.from("profiles").update({ practice_re`
- `components/VocalTracker.jsx:11141 const { error } = await supabase.from("profiles").update({ folded_grou`
- `components/VocalTracker.jsx:11158 const { error } = await supabase.from("profiles").update({ record_mode`
- `components/VocalTracker.jsx:11292 .from("profiles").update({ occupation_notice_shown_at: at }).eq("id", `
- `components/VocalTracker.jsx:12249 await supabase.from("overlap_notices").delete().eq("lesson_id", lesson`
- `components/VocalTracker.jsx:12280 await supabase.from("overlap_notices").delete().eq("lesson_id", lesson`
- `components/VocalTracker.jsx:12552 const { error } = await supabase.from("profiles").update({ folded_grou`
- `components/VocalTracker.jsx:12563 const { error } = await supabase.from("profiles").update(patch).eq("id`
- `components/VocalTracker.jsx:12576 const { error } = await supabase.from("profiles").update(patch).eq("id`
- `components/VocalTracker.jsx:12595 const { error } = await supabase.from("profiles").update(patch).eq("id`
- `components/VocalTracker.jsx:12606 const { error } = await supabase.from("profiles").update({ survey_day7`
- `components/VocalTracker.jsx:12617 const { error } = await supabase.from("profiles").update({ survey_day7`
- `components/VocalTracker.jsx:12626 const { error } = await supabase.from("profiles").update({ day_record_`
- `components/VocalTracker.jsx:12633 const { error } = await supabase.from("profiles").update({ display_nam`
- `components/VocalTracker.jsx:13046 const { error: consentError } = await supabase.from("link_consents")`
- `components/VocalTracker.jsx:13202 await supabase.from("lessons").delete().eq("id", lessonId);`
- `components/VocalTracker.jsx:13350 await supabase.from("article_notes").update({ deleted_at: new Date().t`
- `components/VocalTracker.jsx:13755 await supabase.from("org_event_participants").delete().eq("id", existi`
- `components/VocalTracker.jsx:13766 await supabase.from("org_event_participants")`
- `components/VocalTracker.jsx:13811 const { error } = await supabase.from("memberships")`
- `components/VocalTracker.jsx:14709 const { error: e3 } = await supabase.from("lesson_preset_targets")`
- `components/VocalTracker.jsx:15159 const { error } = await supabase.from("profiles").update({ line_link_c`
- `components/VocalTracker.jsx:15165 const { error } = await supabase.from("profiles").update({ line_notifi`
- `components/VocalTracker.jsx:15172 const { error } = await supabase.from("profiles").update({ line_user_i`
- `components/VocalTracker.jsx:15399 const { error: deleteError } = await supabase.from("repertoire_tessitu`
- `components/VocalTracker.jsx:15463 const { error: delError } = await supabase.from("repertoire_tessitura"`
- `components/VocalTracker.jsx:15476 await supabase.from("role_master").delete().eq("user_id", userId).eq("`
- …… ほか 6件

## ★禁じ字を 覚え書きごと さがして いる 見張り

★わけ …… 覚え書きに 書いた「〜を 出さない」で、★自分の 説明に 落ちます。★`readCode`（覚え書きを 外した 字）で 見る こと。

★ありません。

## ★測った 数を 覚えて いる 見張り

★わけ …… 数は 動きます。★正しい 直しで 落ちる 見張りは、★直すのを 止めます。

★★同じ 形でも 誤りで ない もの …… 決めた 数（★職業 11個・合言葉 12文字 など）。★あれは 測った 数では なく、★決めた 数 です。

- `components/tests/occupation.test.js:29 assertEqual(O.OCCUPATIONS.length, 11, "職業は11個");`
- `components/tests/recovery-code.test.js:48 eq(m.RECOVERY_ALPHABET.length, 32, "32文字から選ぶ（12文字で60ビット）");`
- `components/tests/recovery-code.test.js:57 eq(m.recoveryCodeFromBytes(bytes).length, 12, "渡した数から12文字を作る");`

## ★門の 外れに 倒れる 比べ（`!== "Bearer undefined"` の 形）

★わけ …… 合言葉が 無い とき、★`Bearer undefined` を 送る 相手に 通ります。★無い ときは 503 で 止める こと。

★ありません。

## ★`select("*")` を 画面の 門の 後ろに 置く

★わけ …… 決まり（RLS）は 行を 守ります。★列は 隠せません。★画面に 出さなくても、★通信には 乗ります。

★★同じ 形でも 誤りで ない もの …… ご本人の 行 だけ を 引く ところ（★自分の 記録）。★よその 方の 行を 引く ところ だけ が 危ういです。

- `app/admin/page.js:68 const { data: subs } = await admin.from("subscriptions").select("*");`
- `app/api/character/unlock/route.js:92 .select("*")`
- `app/api/org/invitation/accept/route.js:56 .select("*")`
- `app/api/org/invitation/lookup/route.js:47 .select("*")`
- `app/api/recovery/redeem/route.js:111 .from("recovery_codes").select("*").eq("user_id", user.id).maybeSingle`
- `app/billing/page.js:235 .select("*")`
- `components/MyTimetable.jsx:64 supabase.from("my_periods").select("*").eq("user_id", userId).order("o`
- `components/MyTimetable.jsx:65 supabase.from("my_timetable").select("*").eq("user_id", userId)`
- `components/VocalTracker.jsx:6264 () => supabase.from("entries").select("*").eq("user_id", userId),`
- `components/VocalTracker.jsx:6335 () => supabase.from("questionnaire_responses").select("*").eq("user_id`
- `components/VocalTracker.jsx:6352 () => supabase.from("repertoire_tessitura").select("*").eq("user_id", `
- `components/VocalTracker.jsx:6388 supabase, () => supabase.from("role_master").select("*").eq("user_id",`
- `components/VocalTracker.jsx:6421 supabase, () => supabase.from("project_master").select("*").eq("user_i`
- `components/VocalTracker.jsx:10891 let q = supabase.from(table).select("*").eq("user_id", userId);`
- `components/VocalTracker.jsx:10905 .from("teacher_student_links").select("*").eq("student_id", userId);`
- `components/VocalTracker.jsx:13070 .select("*").eq("teacher_id", userId).eq("status", "active");`
- `components/VocalTracker.jsx:13072 .select("*").eq("student_id", userId).eq("status", "active");`
- `components/VocalTracker.jsx:13217 const { data: chapters } = await supabase.from("chapter_state").select`
- `components/VocalTracker.jsx:13223 const { data: progress } = await supabase.from("article_progress").sel`
- `components/VocalTracker.jsx:13335 const { data } = await supabase.from("article_notes").select("*").eq("`
- `components/VocalTracker.jsx:13385 .from("organizations").select("*").eq("created_by", userId);`
- `components/VocalTracker.jsx:13520 .from("enrollments").select("*").eq("student_id", userId).eq("status",`
- `components/VocalTracker.jsx:13544 const { data: assignments } = await supabase.from("assignments").selec`
- `components/VocalTracker.jsx:13626 .select("*").eq("org_id", orgId).eq("teacher_id", userId).is("ended_at`
- `components/VocalTracker.jsx:13656 supabase.from("org_events").select("*").gte("event_date", addDays(toda`
- `components/VocalTracker.jsx:13657 supabase.from("org_event_participants").select("*").eq("user_id", user`
- `components/VocalTracker.jsx:13666 const { data } = await supabase.from("org_events").select("*").eq("org`
- `components/VocalTracker.jsx:14814 supabase.from("memberships").select("*").eq("org_id", orgId),`
- `components/VocalTracker.jsx:14815 supabase.from("enrollments").select("*").eq("org_id", orgId).eq("statu`
- `components/VocalTracker.jsx:14816 supabase.from("assignments").select("*").eq("org_id", orgId).is("ended`
- `components/VocalTracker.jsx:14860 .from("org_billing").select("*").eq("org_id", orgId)`
- `components/tests/count-in-db.test.js:74 const uStarAt = unlock.indexOf('.select("*")');`

## ★読めなかった ときに 空を 入れる

★わけ …… 「無い」と「読めて いない」が 同じ 顔に なります。★空の 一覧を 出し、★『ありません』と 嘘を つきます。

★★同じ 形でも 誤りで ない もの …… 添えものの 一覧（★時限・読んだ 印 など）で、★空でも 嘘に ならない ところ。

★ありません。

## ★この 検査が 見て いない こと

★★字の 形を 見て います。★通るか どうかは 見て いません。
★★★見つかった もの が すべて 誤り では ありません。★1つずつ 見て ください。
