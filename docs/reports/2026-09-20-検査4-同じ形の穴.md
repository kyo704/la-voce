# ★検査 その4 ── ★同じ形の 穴

★2026-09-20 ／ ★`tools/insp_same_shape.py` が 書きました。

★★★1度 起きた 誤りを、★形として 書き留め、★全体に 当てます。
★★どの 形にも 較正（当たり・外れ）が 付いて います。

| 形 | 見つかった 数 |
|---|---|
| 0行を 成功に する（`.update` / `.delete` に `.select()` が 無い） | 63 |
| 禁じ字を 覚え書きごと さがして いる 見張り | 0 |
| 測った 数を 覚えて いる 見張り | 3 |
| 門の 外れに 倒れる 比べ（`!== "Bearer undefined"` の 形） | 0 |
| `select("*")` を 画面の 門の 後ろに 置く | 36 |
| 読めなかった ときに 空を 入れる | 7 |

## ★0行を 成功に する（`.update` / `.delete` に `.select()` が 無い）

★わけ …… PostgREST は 0行でも 成功を 返します。★消えて いないのに「消しました」と 出ます。

★★同じ 形でも 誤りで ない もの …… 消えたか どうかを 見なくて よい 書き（★見た 印・並びの 保存 など）。★1つずつ 見て ください。

- `app/api/account/delete/route.js:281 .from("profiles")`
- `app/api/account/restore/route.js:30 const { error } = await admin.from("profiles").update({ deleted_at: nu`
- `app/api/cron/purge-events/route.js:29 .from("events")`
- `app/api/line-webhook/route.js:83 .from("profiles")`
- `app/api/org/invitation/accept/route.js:93 await admin.from("org_invitations")`
- `app/api/org/invitation/accept/route.js:144 .from("org_invitations")`
- `app/api/org/posts/route.js:161 const { error: e2 } = await admin.from("memberships")`
- `app/api/org/posts/route.js:232 const { error } = await admin.from("org_posts").update({ name }).eq("i`
- `app/api/org/posts/route.js:254 const { error } = await admin.from("org_posts").update({ perms: next }`
- `app/api/org/posts/route.js:316 const { error } = await admin.from("org_posts").delete().eq("id", post`
- `app/api/recovery/redeem/route.js:139 .from("recovery_codes").update(patch).eq("user_id", user.id);`
- `app/api/recovery/redeem/route.js:155 .from("recovery_codes")`
- `app/api/recovery/redeem/route.js:188 await admin.from("recovery_codes")`
- `app/api/stripe/webhook/route.js:134 .from("subscriptions")`
- `app/api/stripe/webhook/route.js:229 .from("subscriptions")`
- `components/MyTimetable.jsx:283 const r = await supabase.from("my_timetable").delete().eq("id", row.id`
- `components/VocalTracker.jsx:6135 supabase.from("profiles").update({ pwa_installed_at: new Date().toISOS`
- `components/VocalTracker.jsx:6153 supabase.from("profiles").update({ pwa_install_prompted_at: new Date()`
- `components/VocalTracker.jsx:6553 .from("profiles").update(adoptedOcc).eq("id", userId);`
- `components/VocalTracker.jsx:6607 .from("profiles").update(adopted).eq("id", userId);`
- `components/VocalTracker.jsx:6616 .from("profiles").update(帯).eq("id", userId);`
- `components/VocalTracker.jsx:8745 const { error } = await supabase.from("cycle_periods").delete().eq("id`
- `components/VocalTracker.jsx:8762 .from("cycle_periods").update({ end_date: endISO, updated_at: new Date`
- `components/VocalTracker.jsx:9908 await supabase.from("profiles").update({ garden_theme: themeKey }).eq(`
- `components/VocalTracker.jsx:10030 const { error } = await supabase.from("period_markers")`
- `components/VocalTracker.jsx:10884 const { error } = await supabase.from("profiles").update(patch).eq("id`
- `components/VocalTracker.jsx:10918 .from("profiles")`
- `components/VocalTracker.jsx:10949 .from("profiles")`
- `components/VocalTracker.jsx:10970 .from("profiles")`
- `components/VocalTracker.jsx:10982 .from("profiles")`
- `components/VocalTracker.jsx:11040 const { error } = await supabase.from("profiles").update(finalPatch).e`
- `components/VocalTracker.jsx:11052 const { error } = await supabase.from("profiles").update(patch).eq("id`
- `components/VocalTracker.jsx:11062 const { error } = await supabase.from("profiles").update({ practice_re`
- `components/VocalTracker.jsx:11072 const { error } = await supabase.from("profiles").update({ folded_grou`
- `components/VocalTracker.jsx:11089 const { error } = await supabase.from("profiles").update({ record_mode`
- `components/VocalTracker.jsx:11223 .from("profiles").update({ occupation_notice_shown_at: at }).eq("id", `
- `components/VocalTracker.jsx:12037 const { error } = await supabase.from("profiles").update({ folded_grou`
- `components/VocalTracker.jsx:12048 const { error } = await supabase.from("profiles").update(patch).eq("id`
- `components/VocalTracker.jsx:12061 const { error } = await supabase.from("profiles").update(patch).eq("id`
- `components/VocalTracker.jsx:12080 const { error } = await supabase.from("profiles").update(patch).eq("id`
- `components/VocalTracker.jsx:12091 const { error } = await supabase.from("profiles").update({ survey_day7`
- `components/VocalTracker.jsx:12102 const { error } = await supabase.from("profiles").update({ survey_day7`
- `components/VocalTracker.jsx:12111 const { error } = await supabase.from("profiles").update({ day_record_`
- `components/VocalTracker.jsx:12118 const { error } = await supabase.from("profiles").update({ display_nam`
- `components/VocalTracker.jsx:12525 const { error: consentError } = await supabase.from("link_consents")`
- `components/VocalTracker.jsx:12681 await supabase.from("lessons").delete().eq("id", lessonId);`
- `components/VocalTracker.jsx:12829 await supabase.from("article_notes").update({ deleted_at: new Date().t`
- `components/VocalTracker.jsx:13191 await supabase.from("org_event_participants").delete().eq("id", existi`
- `components/VocalTracker.jsx:13202 await supabase.from("org_event_participants")`
- `components/VocalTracker.jsx:13247 const { error } = await supabase.from("memberships")`
- `components/VocalTracker.jsx:13612 const { error: e3 } = await supabase.from("lesson_preset_targets")`
- `components/VocalTracker.jsx:14018 const { error } = await supabase.from("profiles").update({ line_link_c`
- `components/VocalTracker.jsx:14024 const { error } = await supabase.from("profiles").update({ line_notifi`
- `components/VocalTracker.jsx:14031 const { error } = await supabase.from("profiles").update({ line_user_i`
- `components/VocalTracker.jsx:14258 const { error: deleteError } = await supabase.from("repertoire_tessitu`
- `components/VocalTracker.jsx:14322 const { error: delError } = await supabase.from("repertoire_tessitura"`
- `components/VocalTracker.jsx:14335 await supabase.from("role_master").delete().eq("user_id", userId).eq("`
- `components/VocalTracker.jsx:14343 await supabase.from("project_master").delete().eq("user_id", userId).e`
- `components/VocalTracker.jsx:14434 const { error: roleError } = await supabase.from("role_master")`
- `components/VocalTracker.jsx:14437 const { error: projError } = await supabase.from("project_master")`
- …… ほか 3件

## ★禁じ字を 覚え書きごと さがして いる 見張り

★わけ …… 覚え書きに 書いた「〜を 出さない」で、★自分の 説明に 落ちます。★`readCode`（覚え書きを 外した 字）で 見る こと。

★ありません。

## ★測った 数を 覚えて いる 見張り

★わけ …… 数は 動きます。★正しい 直しで 落ちる 見張りは、★直すのを 止めます。

★★同じ 形でも 誤りで ない もの …… 決めた 数（★職業 11個・合言葉 12文字 など）。★あれは 測った 数では なく、★決めた 数 です。

- `components/tests/occupation.test.js:29 assertEqual(O.OCCUPATIONS.length, 11, "職業は11個");`
- `components/tests/recovery-code.test.js:48 eq(m.RECOVERY_ALPHABET.length, 32, "32文字から選ぶ（12文字で60ビット）");`
- `components/tests/recovery-code.test.js:57 eq(m.recoveryCodeFromBytes(bytes).length, 12, "渡した数から12文字を作る");`

## ★門の 外れに 倒れる 比べ（`!== "Bearer undefined"` の 形）

★わけ …… 合言葉が 無い とき、★`Bearer undefined` を 送る 相手に 通ります。★無い ときは 503 で 止める こと。

★ありません。

## ★`select("*")` を 画面の 門の 後ろに 置く

★わけ …… 決まり（RLS）は 行を 守ります。★列は 隠せません。★画面に 出さなくても、★通信には 乗ります。

★★同じ 形でも 誤りで ない もの …… ご本人の 行 だけ を 引く ところ（★自分の 記録）。★よその 方の 行を 引く ところ だけ が 危ういです。

- `app/admin/page.js:68 const { data: subs } = await admin.from("subscriptions").select("*");`
- `app/api/character/unlock/route.js:92 .select("*")`
- `app/api/org/invitation/accept/route.js:56 .select("*")`
- `app/api/org/invitation/lookup/route.js:47 .select("*")`
- `app/api/recovery/redeem/route.js:111 .from("recovery_codes").select("*").eq("user_id", user.id).maybeSingle`
- `app/billing/page.js:235 .select("*")`
- `components/MyTimetable.jsx:64 supabase.from("my_periods").select("*").eq("user_id", userId).order("o`
- `components/MyTimetable.jsx:65 supabase.from("my_timetable").select("*").eq("user_id", userId)`
- `components/VocalTracker.jsx:6207 () => supabase.from("entries").select("*").eq("user_id", userId),`
- `components/VocalTracker.jsx:6278 () => supabase.from("questionnaire_responses").select("*").eq("user_id`
- `components/VocalTracker.jsx:6295 () => supabase.from("repertoire_tessitura").select("*").eq("user_id", `
- `components/VocalTracker.jsx:6331 supabase, () => supabase.from("role_master").select("*").eq("user_id",`
- `components/VocalTracker.jsx:6364 supabase, () => supabase.from("project_master").select("*").eq("user_i`
- `components/VocalTracker.jsx:10822 let q = supabase.from(table).select("*").eq("user_id", userId);`
- `components/VocalTracker.jsx:10836 .from("teacher_student_links").select("*").eq("student_id", userId);`
- `components/VocalTracker.jsx:12549 .select("*").eq("teacher_id", userId).eq("status", "active");`
- `components/VocalTracker.jsx:12551 .select("*").eq("student_id", userId).eq("status", "active");`
- `components/VocalTracker.jsx:12636 const { data } = await supabase.from("lessons").select("*").eq("link_i`
- `components/VocalTracker.jsx:12646 supabase.from("lessons").select("*").eq("teacher_id", userId).order("s`
- `components/VocalTracker.jsx:12696 const { data: chapters } = await supabase.from("chapter_state").select`
- `components/VocalTracker.jsx:12702 const { data: progress } = await supabase.from("article_progress").sel`
- `components/VocalTracker.jsx:12814 const { data } = await supabase.from("article_notes").select("*").eq("`
- `components/VocalTracker.jsx:12864 .from("organizations").select("*").eq("created_by", userId);`
- `components/VocalTracker.jsx:12956 .from("enrollments").select("*").eq("student_id", userId).eq("status",`
- `components/VocalTracker.jsx:12980 const { data: assignments } = await supabase.from("assignments").selec`
- `components/VocalTracker.jsx:13062 .select("*").eq("org_id", orgId).eq("teacher_id", userId).is("ended_at`
- `components/VocalTracker.jsx:13092 supabase.from("org_events").select("*").gte("event_date", addDays(toda`
- `components/VocalTracker.jsx:13093 supabase.from("org_event_participants").select("*").eq("user_id", user`
- `components/VocalTracker.jsx:13102 const { data } = await supabase.from("org_events").select("*").eq("org`
- `components/VocalTracker.jsx:13711 supabase.from("memberships").select("*").eq("org_id", orgId),`
- `components/VocalTracker.jsx:13712 supabase.from("enrollments").select("*").eq("org_id", orgId).eq("statu`
- `components/VocalTracker.jsx:13713 supabase.from("assignments").select("*").eq("org_id", orgId).is("ended`
- `components/VocalTracker.jsx:13714 supabase.from("lessons").select("*").eq("org_id", orgId).order("schedu`
- `components/VocalTracker.jsx:13741 .from("org_billing").select("*").eq("org_id", orgId)`
- `components/VocalTracker.jsx:13972 supabase.from("lessons").select("*").eq("student_id", userId).order("s`
- `components/tests/count-in-db.test.js:74 const uStarAt = unlock.indexOf('.select("*")');`

## ★読めなかった ときに 空を 入れる

★わけ …… 「無い」と「読めて いない」が 同じ 顔に なります。★空の 一覧を 出し、★『ありません』と 嘘を つきます。

★★同じ 形でも 誤りで ない もの …… 添えものの 一覧（★時限・読んだ 印 など）で、★空でも 嘘に ならない ところ。

- `components/VocalTracker.jsx:11331 setPortfolioEntries(e.error ? [] : (e.data || []));`
- `components/VocalTracker.jsx:11332 setPortfolioRecordings(r.error ? [] : (r.data || []));`
- `components/VocalTracker.jsx:11720 setKumuSlots(s.error ? [] : (s.data || []));`
- `components/VocalTracker.jsx:11721 setKumuPeriods(p.error ? [] : (p.data || []));`
- `components/VocalTracker.jsx:11946 setAttendingPresets(r.error ? [] : (r.data || []));`
- `components/VocalTracker.jsx:13384 setMadaNudges(n.error ? [] : (n.data || []));`
- `components/VocalTracker.jsx:13541 setMonkaHitoPeriods(p.error ? [] : (p.data || []));`

## ★この 検査が 見て いない こと

★★字の 形を 見て います。★通るか どうかは 見て いません。
★★★見つかった もの が すべて 誤り では ありません。★1つずつ 見て ください。
