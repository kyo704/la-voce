# 2026年9月15日 ── きょうのまとめ

全177行 / 末尾は「実機でお確かめください。」

## 0. ★先にしていただきたいこと（2つ）

### ① SQL を流す

```
~/Desktop/migration_no024_gate_inside_the_function.sql
```

Supabase の SQL エディタに、そのまま貼って流してください。
★BEGIN / ROLLBACK は使っていません。2度流しても同じ結果になります。
★中に「確かめ」の問いも入っています。どれも読むだけで、書き換えません。

見ていただきたいところ ──

- `"引数"` が `uuid` の行が**1つだけ**であること
  （2つあれば、門のない古いほうが残っています）
- `"権限"` に `anon=X` や `authenticated=X` が**出ない**こと
- 「居ない人」の2つが、どちらも `null` であること

### ② push の可否

未push 2本です。

```
e85e3fe  門を経路の外へ出す（No.024）／招待の入口が無い件の調べ
6c8a3ed  招待の入口を、もっとに置く（No.025）
```

---

## 1. 画面の直し（もっとの先・9枚）

| 枚 | 何をしたか | 記録 |
|---|---|---|
| 設定 | ③④⑤⑩ ＋ プロフィールへ戻る道 | |
| 聞いてほしいこと | ── | |
| アカウント | 押せない札2つを片づけた | |
| プラン | ★年額の誤り 5,800 → 4,800 を直した | |
| 学ぶ | 見本の5つを入れた | No.021 |
| もっているもの | 台帳に「何で手に入ったか」を出した | No.022 |
| 書き出す | 見出しを日本語にし、中身と約束を出した | No.023 |
| 退会する | レパートリー明記・台帳の言い方・約束の2行 | |
| 同居問題 | 第1段（門の中だけ器を分け、ログアウトを引っ越し） | |

### ★とくにお伝えしたいこと

**プランの値段がまちがっていました。** `CountV2` が年額を 5,800円 と
出していました。正しくは 4,800円 です。`lib/plans.js` から引く形にし、
特商法の紙も同じ形にそろえました。手作業の保守に頼らない形です。

**書き出しの見出しが、列の名前のままでした。** 医師に渡る可能性のある
ファイルです。日本語にしましたが、★列の名前をそのまま訳していません ──
`throat_condition` に入っているのは のどではなく「からだの感じ」です
（裁定 その29／その49）。見張りは警告ではなく**失敗**にしています。

**退会の一覧に、レパートリーがありませんでした。** 実際には消えます。
消えるのに書いていない、というのがいちばん悪い形です。足しました。

---

## 2. 教室の殻

「次のレッスン」だけ入れました。近い行事・先生からの連絡は着手していません。

★**「両方拾う」は、これ以外に正解がありませんでした。**
個人指導のレッスンは `student_id` を持ちません。
`student_id` だけで引くと、個人指導の方に1件も出ず、★エラーも出ません。

行事の下調べも済んでいます（`org_events` は日づけ1つ・繰り返しの列なし）。
日づけのある時間割の眺めは `OpsSchedule.jsx` にあります。
★ただし先生・事務の画面で、生徒の殻ではありません。

---

## 3. 権限まわり

### No.024 ── 門を経路の外へ出す（★SQL を流していただきます）

門が `app/admin/page.js` の1か所にしかありませんでした。
経路が変われば門も外れます。`get_student_entries` と同じ形です。

関数の中にも1枚置きました。呼べる人は増やしていません（service_role のみ）。

★お尋ねへの答え ── `p_user_id` は要求の本文ではありません。
`POST()` は引数を持たず、本文を1度も読みません。

### 管理画面の古い道を消しました

`entries` から12列（自由記述とお薬の記録を含む）を取り直す逃げ道がありました。
逃げ道があると、消したはずの道が静かに生き返ります。

★取れなかったときに 0% と出しません。「数えられませんでした」と書きます。

### No.025 ── 招待の入口

★**入口が1つもありませんでした。** 押せないのではなく、たどり着けません。
しくみは壊れていません。道だけが無くなっていました。

```
生徒0人 → レッスン0件 → 行が出ない → タブへ行けない
       → 招待できない → 生徒0人のまま
```

もっとに1行足しました。見本のほうにも同じ行を足しています。

---

## 4. ★私のまちがい（きょう気づいたもの）

正直に並べます。どれも見張りか、坂本さん・Opus のご指摘で止まりました。

| 何 | どう止まったか |
|---|---|
| `lessons` の決まりを、紙だけで結んだ（結論が逆） | Sonnet が本番を直に引いて |
| 見本に `teach()` という判定を書いた（見本に無い関数） | 自分で気づき、取り消し |
| `useEffect` を宣言より前に置いた（実機なら落ちる） | ★`no-tdz` だけが止めた。lint も build も通っていた |
| 記録の器の中に新しい節を置いた | `record-leak` |
| ★自分のコメントが数えられた（3度） | 道具・見張り |
| ★目分量の窓（1400字・3000字）で切っていた（2度） | 較正と、行を1つ足したとき |
| 緑の文字（対比2.76） | `display-prefs` |
| 「達成」という禁止語 | `no-nagging-words` |

★とくに1つ目です。「紙を読んで結ぶ」ことをやめ、
必ず台帳に直接照会してから結ぶ、という工程を挟むことにしました。
道具の頭にも「これは下書きです。答えではありません」と出るようにしています。

---

## 5. ご判断をお待ちしているもの

| 何 | 中身 |
|---|---|
| `is_admin` | 48人中0人です。この画面は誰にも開けません。★No.024 が入れば、is_admin を立てるだけで動きます |
| 教室に招く（㋑） | 同じ場所にあるので、同じくたどり着けないはずです。別に報告します |
| 近い行事 | 裁定は届いています。ご指示をお待ちします |

---

## 6. 台帳に残したこと

| 番号 | 何 | 引き金 |
|---|---|---|
| ㉚ | 「端末を見る」を戻す | 端末一覧の API ができたとき |
| ㉛ | 古い「じぶんの記録」の枠を消す | 門を広げるとき／その枠を触るとき |
| ㉜ | 試験の台帳に、記録の無い適用が1件 | 試験と本番で答えが食い違ったとき |

★㉜ は追いかけません（裁定どおり）。ただし消しません。
実害はありませんが、記録の無い適用は、私たちが何度もつまずいている形です。

---

## 7. たしかめ

- `npm run lint` … エラーなし
- `npm run build` … ✓ Compiled successfully
- 見張り … 通り307／落ち10

★落ちた10本は、けさから落ちているものです。私の直しで増えていません。

```
contrast / line-up-stack / look-back / no-missing-token / note-fold /
practice-note / repertoire-edit-delete / repertoire-identity /
repertoire-pitch-entry / text-scale
```

きょう足した見張り … 学ぶ30本／台帳51本／退会44本／書き出し66本／
教室の殻35本／管理画面20本／id の出どころ21本／招待の入口26本。
どれも、わざと壊して落ちることを確かめてあります。

---

コードに入れました。見張りは通りました。見た目は未確認です。
実機でお確かめください。
