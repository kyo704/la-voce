# ★A2 の 着手前の 確かめ（★まだ 直して いません）

全104行 / 末尾は「★★0人 なら、★そのまま 2 へ 進みます。」

## ① ★呼ぶ 側は、★できこと を 持って いますか

★★`tabsFor()` を 呼ぶ ところは、★**1つ だけ** です。

```
components/VocalTracker.jsx:13674
  const gate = permsOfMember(opsMembership, opsPostsById) || role;
  … <OpsShell role={gate} …>
components/OpsShell.jsx:40
  const tabs = tabsFor(role);
```

★★★つまり、★**もう できことを 渡して います**。★ただし `|| role` が あります。

```
役職（post）を 持つ 方 …… ★できこと（Set）が 渡ります → `tabsForPerms` → ★食い違い 0
役職を 持たない 方 ……… ★★名前の ちから（"owner" など）に 落ちます → `BY_ROLE`
```

★★★**33通りの 食い違いは、★この 落ち道 から だけ 出ます。**
　★★仕掛けを 作る 仕事では ありません。★★落ち道を 閉じる 仕事 です。

★★`components/OpsPosts.jsx:111` は、★はじめから `tabsForPerms(s)` です。★直す 所は ありません。

## ② ★落とし戸 ── ★3つ ありました（★1つでは ありません）

```
lib/opsShell.js:122  const keys = BY_ROLE[roleOrPerms] || [];          … tabsFor
lib/opsShell.js:149  return roleOrPerms === "owner" || … === "admin"; … mayEditRoster
lib/opsShell.js:175  return roleOrPerms === "owner";                   … maySeeMoney
```

★★ご指示は「125行の `=== 'owner' || === 'admin'`」でした。
★★実際は **149行**と **175行**の 2か所 です。★122行の `BY_ROLE` と 合わせて 3つ。

★★どれも 同じ 形 です ── ★`isPerms()` なら できこと、★でなければ 名前の ちから。
★★★3つ とも 一緒に 外さないと、★片方だけ 残ります。★この 蔵の 持病の 形 です。

## ③ ★いまの 数

```
node tools/perm-matrix.js
　見本の できること 14 ／ 役職 10 ／ 画面 12 ／ 総当たり 120通り
　★食い違い 33通り（★うち 漏れ 3通り）
```

★漏れ 3通り（★実装が よけいに 出す）──
```
学部長 … 役職を 変える
学科長 … 役職を 変える ／ 行事
```

## ④ ★★門の 外の 38人 ── ★ここが 決め手 です

★★落ち道（`BY_ROLE`）を 外すと、★**役職を 持たない 方の 運営画面が 空に なります**。
　★★`OpsShell` は `tabs.length === 0` で `null` を 返します（★40〜44行）。
　★★入口も 消えます（`mayEnterOps` も 同じ 道を 通ります）。

★★★だから、★外す 前に **1つ だけ** 知る 必要が あります ──

```
★役職（post）を 持たない 方は、★何人 いますか
　　0人 …… ★そのまま 外せます
　　1人以上 … ★★先に 役職を 付けてから でないと 外せません
```

★★これは 台帳に しか ありません。★問いを 書きました ──
`supabase/問い-役職を持たない人.sql`（★読むだけ・49行）

```
【一】教室ごと … 人数／役職あり／★役職なし／役割の 内わけ
【二】役職を 持たない 方の 役割の 内わけ（全体）
【三】★役職の 行き先が 消えて いる 人（★`permsOfMember` は null を 返します）
【四】役職（org_posts）に、★できことが 入って いるか
```

★★★【三】も 大事 です。★`post_id` は あるのに、★その 役職が 消えて いると、
　★★`permsOfMember` が `null` を 返し、★**名前の ちからに 落ちます**。
　★★`post_id` が あるから 安心、では ありません。

## ⑤ ★進め方（★1画面 1コミット）

```
0 ★台帳に 尋ねる（★上の 問い）………………… ★★いま ここ
1 ★perm-matrix に「★落ち道を 使った 回数」を 数えさせる（★較正）
2 ★`mayEditRoster`（149行）の 落ち道を 外す ── ★1コミット
3 ★`maySeeMoney`（175行）の 落ち道を 外す ── ★1コミット
4 ★`tabsFor`（122行・`BY_ROLE`）を 外す ── ★1コミット
5 ★`BY_ROLE` そのものを 消す（★呼ぶ 人が 居なく なります）
6 ★perm-matrix が 0通りに なる ことを 見る
7 ★門の 外の 38人の 画面が 変わらない ことを 見る（★④の 答え 次第）
```

★★2→3→4 の 順に します。★小さい ものから。
★★どれか 1つで 38人の 画面が 変われば、★そこで 止まって ご報告します。

## ⑥ ★まだ 直して いません

★★④の 答え（★役職を 持たない 方が 何人 いるか）を いただいてから 動きます。
★★0人 なら、★そのまま 2 へ 進みます。
