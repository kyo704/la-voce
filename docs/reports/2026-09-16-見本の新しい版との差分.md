# 見本の 新しい 版との 差分
全384行 / 末尾は「★★入れ替える なら、★上の「★取り直し」の 画面を 撮り直します。」

★出どころ　坂本さん（2026-09-16）「微妙に修正した 正式版」
★**まだ 入れ替えて いません。** ご判断を お待ちします。

## ★先に、いちばん 大事な こと 3つ

### ① ★★2つ、戻って います

★新しい 版は、★2026-09-13 と 2026-09-15 の お決め **より 前**の 土台から
★作られて います。★そのまま 入れ替えると、★決めた ことが 黙って 消えます。

| 消える もの | いつ 決めた | 新しい 版では |
|---|---|---|
| 料金の 下限 **9,800円** | 2026-09-13 | **12,800円**に 戻って います |
| もっとの「教室 ▸ **生徒を 招待する**」 | 2026-09-15（裁定 ㋒／No.025） | **行ごと ありません** |

★★2つめは、★実装の 側に 残ります。★すると 次の 突き合わせで
　★「実装に 在って 見本に 無い」と 出ます。
　★★あれは「先生の 側の 入口が 1つも 無い」を 直した 行 です。
　★見本に 無いから と いって、★外さないで ください。

### ② ★トークンが、もう 当たって います

★新しい 版は **裁定その62 の STEP1 が 済んだ 姿**です。
★`--sp1/--sp2/--sp3`・画面ごとの 密度（`--d`）・`--enji-bg` の 廃止・
★`prefers-reduced-motion` の 網 ── すべて 入って います。

★★だから、★**きょう 測った トークンの 差は、ほぼ 意味を 失います**。
　★例）`.li` の 字　13px → **14.5px**。★実装は もとから 14.5px です。
　　★きょう「ちがい」と 数えた もの が、★新しい 版では **一致**に なります。

### ③ ★1つ、正しく 消えて います

★同意の 画面から「改善のための 統計」の 行が 消えました。
★★これは No.027 で 実装から 外した もの です。★見本が 実装に 追いついた 形 です。
★戻さないで ください。

---

---

## さわれる・全画面

| | いま | 新しい 版 |
|---|---|---|
| 行 | 4201 | 4214 |
| 大きさ | 431168 | 431425 |

### A　組み立て（画面の 増減）

★ありません。**画面は 1つも 増えず、1つも 減って いません。**

### C　見た目（`<style>` の 値）

★変わった 66 ／ 増えた 30 ／ 減った 2

| どこ | 何 | いま | 新しい 版 |
|---|---|---|---|
| `.bar` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.bd` | `padding` | `0 15px 18px` | `0 16px 24px` |
| `.box` | `margin-bottom` | `9px` | `12px` |
| `.box` | `padding` | `0 12px` | `0 14px` |
| `.btn` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.cal i.on` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.cal i.on` | `border-color` | `var(--enji-bg)` | `var(--enji)` |
| `.card` | `margin-bottom` | `9px` | `12px` |
| `.card` | `padding` | `12px 13px` | `16px` |
| `.dr2 span.on` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.dr2 span.on` | `border-color` | `var(--enji-bg)` | `var(--enji)` |
| `.drt .k` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.fbtn` | `font-size` | `11.5px` | `12.5px` |
| `.fbtn` | `margin-top` | `14px` | `16px` |
| `.g5 .c.sel:after` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.gr .c.sel` | `border` | `2px solid var(--enji-bg)` | `2px solid var(--enji)` |
| `.h3` | `font-size` | `10.5px` | `11px` |
| `.h3` | `letter-spacing` | `.12em` | `.14em` |
| `.h3` | `margin` | `14px 0 7px` | `var(--sp3) 0 var(--sp1)` |
| `.hd h2` | `font-size` | `17px` | `18px` |
| `.hfab` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.koma` | `border-left` | `3px solid var(--enji-bg)` | `3px solid var(--enji)` |
| `.li` | `font-size` | `13px` | `14.5px` |
| `.li` | `min-height` | `44px` | `48px` |
| `.li` | `padding` | `11px 0` | `12px 0` |
| `.li s` | `font-size` | `11.5px` | `12.5px` |
| `.mini` | `font-size` | `11.5px` | `12.5px` |
| `.minibar` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.mkrow .cells i.on` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.note` | `font-size` | `11px` | `12px` |
| `.note` | `margin-top` | `9px` | `var(--sp2)` |
| `.ord .no` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.pill` | `font-size` | `11.5px` | `12.5px` |
| `.pill` | `padding` | `7px 12px` | `9px 14px` |
| `.pill.on` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.pill.on` | `border-color` | `var(--enji-bg)` | `var(--enji)` |
| `.pill.sm` | `font-size` | `10.5px` | `11.5px` |
| `.pill.sm` | `padding` | `5px 9px` | `7px 11px` |
| `.seg div` | `font-size` | `11.5px` | `12.5px` |
| `.seg div` | `padding` | `8px 0` | `12px 0` |
| `.sh3` | `margin` | `14px 0 5px` | `var(--sp3) 0 var(--sp1)` |
| `.slots .s.on` | `border` | `2px solid var(--enji-bg)` | `2px solid var(--enji)` |
| `.spt` | `font-size` | `11px` | `12px` |
| `.spt` | `line-height` | `1.65` | `1.7` |
| `.sw.on` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.tb.on i` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.tri div.on` | `border` | `2px solid var(--enji-bg)` | `2px solid var(--enji)` |
| `.usu` | `font-size` | `11px` | `12px` |
| `.usu` | `margin-top` | `2px` | `3px` |
| `.warn` | `font-size` | `11px` | `12px` |
| `.warn` | `line-height` | `1.75` | `1.8` |
| `.warn` | `margin-bottom` | `10px` | `12px` |
| `.warn` | `padding` | `9px 11px` | `11px 13px` |
| `:root` | `--ink2` | `#6D5D50` | `#5A4C40` |
| `:root` | `--ink3` | `#7C6C5E` | `#6B5C4E` |
| `body` | `gap` | `13px` | `16px` |
| `body` | `line-height` | `1.7` | `1.75` |
| `body` | `padding` | `22px 14px 60px` | `24px 16px 64px` |
| `html[data-th="dark"] .btn` | `background` | `var(--enji-bg)` | `#8E1230` |
| `html[data-th="dark"] .btn` | `border-color` | `var(--enji-bg)` | `#8E1230` |

★ほか 6 件（★全部は 報告の 紙に 入りません）

**増えた 言い付け**
・`*,*::before,*::after` … `animation-duration: .01ms!important`
・`*,*::before,*::after` … `animation-iteration-count: 1!important`
・`*,*::before,*::after` … `scroll-behavior: auto!important`
・`*,*::before,*::after` … `transition-duration: .01ms!important`
・`.bd` … `--sp1: calc(8px*var(--d))`
・`.bd` … `--sp2: calc(24px*var(--d))`
・`.bd` … `--sp3: calc(48px*var(--d))`
・`.bd>.h3:first-child,.bd>.sh3:first` … `margin-top: var(--sp1)`
・`.bd[data-sc="sub"]` … `--d: 1.15`
・`.bd[data-sc="ふりかえる"]` … `--d: 1.15`
・`.bd[data-sc="ノート"]` … `--d: 1.3`
・`.bd[data-sc="記録"]` … `--d: 1`
・`.hd` … `padding: var(--sp2) 1px var(--sp1)`
・`.seg div` … `display: flex`
・`.usu` … `line-height: 1.7`
・`/* ── 押せる大きさ 44px を 下回らない ── */ .p` … `align-items: center`
・`/* ── 押せる大きさ 44px を 下回らない ── */ .p` … `display: inline-flex`
・`/* ── 押せる大きさ 44px を 下回らない ── */ .p` … `justify-content: center`
・`/* ── 押せる大きさ 44px を 下回らない ── */ .p` … `min-height: 44px`
・`/* ── 画面ごとの 密度（リズム）── */ .bd[data-` … `--d: 1.45`
・`/* ── 間を、実際に 当てる ── */ .card,.box` … `margin-bottom: var(--sp2)`
・`/* ══ 間（ま）の 段階 ── 3つだけ ══ sp1 同じ ま` … `--d: 1`
・`/* ══ 間（ま）の 段階 ── 3つだけ ══ sp1 同じ ま` … `--sp1: 8px`
・`/* ══ 間（ま）の 段階 ── 3つだけ ══ sp1 同じ ま` … `--sp2: 24px`
・`/* ══ 間（ま）の 段階 ── 3つだけ ══ sp1 同じ ま` … `--sp3: 48px`
・`body` … `font-feature-settings: "palt"`
・`body` … `font-variant-numeric: tabular-nums`
・`body` … `letter-spacing: .02em`
・`html[data-th="dark"] .koma` … `border-left-color: var(--enji)`
・`html[data-th="dark"] .sw.on` … `background: #8E1230`

**★★減った 言い付け**
・`/* ══════════ 暗い 画面（ダークモード） ══════` … `--enji-bg: #8E1230`
・`:root` … `--enji-bg: #840C24`

### B　文言（目に 見える 字）

★増えた 5 ／ 減った 40

**増えた 字**
・いちばん安いもの。下限 12
・下限 12
・修正の記録
・消せない。日付・原因・直した内容・確かめた方法。
・直した本人も 消せない

**★★減った 字**
・いちばん安いもの。下限 9
・いつも 出る 形で 描いて います。
・が あります。
・が ありません。
・この 見本には その 印が ないので、
・この 見本は 門の 外の 画面を 写した もので、あちらの 帯には
・この行が 無いために、
・この見本では 招待しません
・だから ここに 無いのは 誤りでは ありません。
・でした。
・と 書いて ありましたが、その 2つは
・の かたまりを 足しました（
・の 画面の 中に しか なく、
・もっとへ 移す
・を 持つ 方にだけ 出します。
・アプリでは、
・タブへ行けない
・レッスン0件
・レッスンが 1件でも ある ときだけ 出ます（
・下限 9
・先生の 側の 入口が 1つも 無く なって いました。
・入口は ホームに 2つ 残る
・円（
・前回も 答えは
・同じ形を 生徒の 側で 1度 解いて います
・同意していません
・場所を 移しただけで、鶏と 卵は 解けて いなかった
・変更では なく 補い）。
・押せない 札に しません。
・招待できない。
・持って いない 方には 出しません。
・描かれて いない 状態を 埋めた だけ です（
・改善のための 統計
・改定。前は 12
・無いことを 毎朝 知らせない）。
・生徒0人
・生徒を 招待する
・行が出ない
・裁定
・門の 中の 帯（

### ★★戻って いないか（決めた ことが 消えて いないか）

★★**戻って います。2 件。**

#### ★★料金の 下限 9,800円（2026-09-13 の お決め）

・いま　　　`下限 9,800円`
・新しい 版　`下限 12,800円`
・見本に「★2026-09-13 改定。前は 12,800円」と 書いて あります。

#### ★★もっとの「教室 ▸ 生徒を 招待する」（2026-09-15 の お決め）

・いま　　　`生徒を 招待する`
・新しい 版　`（行ごと ありません）`
・裁定 ㋒／No.025。★これが 無いと 先生の 側の 入口が 1つも ありません。

★★新しい 版は、★これらの お決め **より 前**の 土台から 作られて います。
　★**そのまま 入れ替えると、決めた ことが 黙って 消えます。**

### ★きょう 測った 画面に 触れて いるか

★★**触れて います。★きょうの 数は 取り直しです。**

| 画面 | きょうの 数 | 消えた行 | 足した行 |
|---|---|---|---|
| もっと | 1.89倍 | 18 | 0 |

### 触れた ところ（画面ごと）

| 画面 | 消えた行 | 足した行 |
|---|---|---|
| （画面の 外） | 44 | 74 |
| ★ もっと | 18 | 0 |
| 出欠つけ | 4 | 4 |
| hito | 3 | 3 |
| 自分のコマの時間 | 1 | 3 |
| 同意 | 2 | 1 |
| したく | 1 | 1 |
| 調べる | 1 | 1 |
| 行事を出す | 1 | 1 |
| 集まり具合 | 1 | 1 |

---

## PC・iPad（運営）

| | いま | 新しい 版 |
|---|---|---|
| 行 | 3110 | 3130 |
| 大きさ | 206885 | 207928 |

### A　組み立て（画面の 増減）

★ありません。**画面は 1つも 増えず、1つも 減って いません。**

### C　見た目（`<style>` の 値）

★変わった 33 ／ 増えた 18 ／ 減った 2

| どこ | 何 | いま | 新しい 版 |
|---|---|---|---|
| `.btn` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.calh .dh.tdy .dd` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.card` | `margin-bottom` | `12px` | `var(--sp2)` |
| `.ev` | `border-left` | `3px solid var(--enji-bg)` | `3px solid var(--enji)` |
| `.fl` | `font-size` | `11px` | `11.5px` |
| `.fl` | `margin` | `14px 0 6px` | `var(--sp2) 0 var(--sp1)` |
| `.h3` | `font-size` | `11px` | `11.5px` |
| `.h3` | `letter-spacing` | `.13em` | `.14em` |
| `.h3` | `margin` | `18px 0 8px` | `var(--sp3) 0 var(--sp1)` |
| `.koma` | `border-left` | `3px solid var(--enji-bg)` | `3px solid var(--enji)` |
| `.mini .mg b.t` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.note` | `margin-top` | `12px` | `var(--sp2)` |
| `.pill.on` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.pill.on` | `border-color` | `var(--enji-bg)` | `var(--enji)` |
| `.sh3` | `font-size` | `11px` | `11.5px` |
| `.sh3` | `margin` | `14px 0 5px` | `var(--sp3) 0 var(--sp1)` |
| `.side .gp` | `font-size` | `10.5px` | `11px` |
| `.spt` | `font-size` | `11px` | `11.5px` |
| `.spt` | `line-height` | `1.65` | `1.7` |
| `.sw.on` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.tag` | `font-size` | `11px` | `11.5px` |
| `.wkc.on` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `.wkc.on` | `border-color` | `var(--enji-bg)` | `var(--enji)` |
| `/* ── 上のバー ── */ .top` | `background` | `var(--enji-bg)` | `var(--enji)` |
| `:root` | `--ink2` | `#6D5D50` | `#5A4C40` |
| `:root` | `--ink3` | `#7C6C5E` | `#6B5C4E` |
| `body` | `line-height` | `1.7` | `1.75` |
| `html[data-th="dark"] .btn` | `background` | `var(--enji-bg)` | `#8E1230` |
| `html[data-th="dark"] .btn` | `border-color` | `var(--enji-bg)` | `#8E1230` |
| `html[data-th="dark"] .pill.on` | `background` | `var(--enji-bg)` | `#8E1230` |
| `html[data-th="dark"] .pill.on` | `border-color` | `var(--enji-bg)` | `#8E1230` |
| `html[data-th="dark"] .tri div.on` | `background` | `var(--enji-bg)` | `#8E1230` |
| `html[data-th="dark"] .tri div.on` | `border-color` | `var(--enji-bg)` | `#8E1230` |

**増えた 言い付け**
・`*,*::before,*::after` … `animation-duration: .01ms!important`
・`*,*::before,*::after` … `animation-iteration-count: 1!important`
・`*,*::before,*::after` … `scroll-behavior: auto!important`
・`*,*::before,*::after` … `transition-duration: .01ms!important`
・`.spt` … `font-variant-numeric: tabular-nums`
・`/* ── 表の 数字は 桁を そろえる ── */ table t` … `font-variant-numeric: tabular-nums`
・`/* ══ 間（ま）の 段階 ── 3つだけ ══ sp1 同じ ま` … `--sp1: 6px`
・`/* ══ 間（ま）の 段階 ── 3つだけ ══ sp1 同じ ま` … `--sp2: 16px`
・`/* ══ 間（ま）の 段階 ── 3つだけ ══ sp1 同じ ま` … `--sp3: 30px`
・`body` … `font-feature-settings: "palt"`
・`body` … `font-variant-numeric: tabular-nums`
・`body` … `letter-spacing: .02em`
・`html[data-th="dark"] .koma` … `border-left-color: var(--enji)`
・`html[data-th="dark"] .mini .mg b.t` … `background: #8E1230`
・`html[data-th="dark"] .mini .mg b.t` … `color: #fff`
・`html[data-th="dark"] .seg .on,html` … `background: #8E1230`
・`html[data-th="dark"] .seg .on,html` … `color: #fff`
・`html[data-th="dark"] .sw.on` … `background: #8E1230`

**★★減った 言い付け**
・`/* ══════════ 暗い 画面（ダークモード） ══════` … `--enji-bg: #8E1230`
・`:root` … `--enji-bg: #840C24`

### B　文言（目に 見える 字）

★増えた 5 ／ 減った 4

**増えた 字**
・いちばん安いもの。下限 12
・下限 12
・修正の記録
・消せない。日付・原因・直した内容・確かめた方法。
・直した本人も 消せない

**★★減った 字**
・いちばん安いもの。下限 9
・下限 9
・円（
・改定。前は 12

### ★★戻って いないか（決めた ことが 消えて いないか）

★★**戻って います。1 件。**

#### ★★料金の 下限 9,800円（2026-09-13 の お決め）

・いま　　　`下限 9,800円`
・新しい 版　`下限 12,800円`
・見本に「★2026-09-13 改定。前は 12,800円」と 書いて あります。

★★新しい 版は、★これらの お決め **より 前**の 土台から 作られて います。
　★**そのまま 入れ替えると、決めた ことが 黙って 消えます。**

### ★きょう 測った 画面に 触れて いるか

★**触れて いません。** きょうの 数は そのまま 使えます。

### 触れた ところ（画面ごと）

| 画面 | 消えた行 | 足した行 |
|---|---|---|
| （画面の 外） | 30 | 50 |

---

## まとめ

| 見本 | 画面 | きょうの 数 | どう なるか |
|---|---|---|---|
| さわれる・全画面 | もっと | 1.89倍 | ★取り直し |
| PC・iPad（運営） | 設定 | 2.35倍（見本729px / 実装1711px） | そのまま |
| PC・iPad（運営） | もっと | 1.89倍 | そのまま |
| PC・iPad（運営） | account | 1.00倍（見本274px / 実装275px） | そのまま |

★★入れ替えるかは、坂本さんの お決め です。私からは しません。
★★入れ替える なら、★上の「★取り直し」の 画面を 撮り直します。
