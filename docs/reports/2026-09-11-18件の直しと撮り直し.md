# 18件の ちがいの 直しと、撮り直し

全172行 / 末尾は「描かれた 画面の 見張り（`forbidden-render`）も、★27件の 書き出しで 通りました。」

コードに 入れました。見張りは 通りました。見た目は 未確認です。実機で お確かめください。

比較画像　`docs/design/compare/画面-ふりかえる-くらべる.png`
　　　　　`docs/design/compare/SC-順番.png`
撮影　iPhone 12（390×844・倍率3）／`fullPage: true`／見本 左・実装 右／29画面・撮れなかった もの 0件

## 1　ご指示の 7点

### ① 帯の 位置（逆に なっていた）── **直りました**

見本の `S_furi` は `hd → seg → 各タブ` の 順で 組み立て、`.warn` は
`narabe()` と `kuraberu()` の **中**に あります。
`sakanobo()` と `kazoeru()` には ありません。

実装は 帯を 画面ぜんぶの 頭に 1つ 置いて いました。
→ **並べる と くらべる の 中へ 移しました。**
→ くらべる で 何も 出ていない ときは 出しません（★見本は `stateBlock` で 先に 返ります）。

### ② 「並べる」vs「ならべる」── **直りました**

| どこ | 前 | いま |
|---|---|---|
| 切替の 札 | ならべる | **並べる** |
| お休みの 案内 | ならべる と さかのぼる は… | **並べる** と さかのぼる は… |
| お休みの 押しどころ | ならべる を見る／さかのぼる を見る | **並べるを 見る**／**さかのぼるを 見る** |

★内部の 鍵（`narabe`）は 変えて いません。★覚えた ものが 外れません。

**あわせて、字が 古い ままの ものを 2つ 見つけました。**

- 帯の 字 ── 9月9日の 古い 見本④の もので、
  「**疑いながら 見てください。**」が 付いて いました。
  ★いまの 見本には ありません。★外しました。
- 切替の 字 ── 「**つづいた日は、**初日だけで くらべる」
  ★いまの 見本は「初日だけで くらべる」です。★直しました。

### ③ たて軸の 20時間 ── **直りました**

`lib/timeGap.js` は **正しい**です。★種（seed）が ありえない 組を 入れて いました。

夕食 23:00・就寝 22:30 の 日が あり、★日を またいだ と みなされて **23.5時間**。
→ 就寝を、★夕食の **1時間 以上 あと**の 札から 選ぶ ように しました。
→ いまは **1〜5時間**（9通り）に 収まって います。

**あわせて、軸を 0 から 始める ように しました。**
見本の `dotplot` は `sy(v)=H-v/6*H` の 固定で、★0・2・4・6時間 の 4本です。
実装は 点の 最小値から 始めて いて、★2時間から でした。
→ 長さの 項目（食べ終えてから 寝るまで／睡眠／歌った・声を使った 時間）だけ
　0 から 始めます。★湿度・むくみは 始めません（★下に 潰れます）。

### ④ 点が すべて ○ ── **● に なりました**

坂本さんに SQL を 実行して いただきました（★31件・すべて live・他の方 0件）。
撮り直した 画像で、**点が ●（塗りつぶし）に なって います。**

★なぜ ○ だったか ── 画面の 誤りでは ありません。
`source` は データベースの 引き金が 決めます（★翌日 23:59 まで `live`）。
30日 さかのぼって 書けば、★古い 日は `later` に なります。★正しい 動きです。

### ⑤ まんなかの 破線 ── **両方 出ました**

`medLine` は もともと 両方の 群に 描いて いました。
片方が 出なかったのは、★判定に 入る 点が 無く、★まんなかが 決まらなかった ためです（★④が 因）。
→ ④が 直り、★**2時間00分**（よく出た日）と **2時間30分**（出なかった日）の 2本が 出ています。
→ あわせて 濃さを 上げました（★0.85 → 1）。★薄くて 見えませんでした。

### ⑥ 下の 帯が かぶさる ── **撮り方の くせでした**

実機の 不具合では ありません。
`position: fixed` の ものは、★`fullPage` で 撮ると
**最初の 1画面ぶんの 位置に 焼き付きます。**

中身の 下には **56＋16px** の 余白が あります（`components/VocalTracker.jsx:13478`）。
→ 撮る ときだけ 帯を ページの いちばん下へ 移します。
→ あわせて、★**中身が 帯の 下に 潜って いないかを 数えて** います。★0件でした。

★★1度 しくじりました。`static` に しただけでは、★組み立ての 順に 出て
**画面の 上**に 来ました。★いちばん下へ 移し直して います。

### ⑦ 順番が 入れ子だった ── **1枚の 画面に なりました**

見本の `push('順番')` は 画面ごと 入れ替えます。
→ 開いているかを `LookBackV2` が 持ち、★開いている あいだは
　**頭（ふりかえる）も、4つの 札も 描きません。**

★あわせて 2つ 直しました。
- 見出しが 11.5px の 小見出しでした。★見本は 画面の 題（17px）です。
- 1行が 64px でした。★見本の `.ord` は 46px です。
  ★行にも ↑ にも 44px を 入れて いて、★足し合わさって いました。

## 2　まだ 残って いる ちがい

**くらべる**

1. 軸の 目盛りが 0／2／4 の 3本（★見本は 0／2／4／6 の 4本）。
   ★見本は 0〜6時間の **固定**です。★実装は 点に 合わせて 伸びます。
   ★固定に すると、★6時間を 超える 方の 点が 外に 出ます。
   ★★どちらに するか、★お決めください。
2. 点の 数（★実装 5日／5日、★見本 14日／11日）。★種の 中身の ちがいです。
3. 帯・カードの 余白が 実装の ほうが 広く、★縦に 長く なります。
4. 「まだ、はっきりした差は…」の 2行目が 実装では 3行に 折れます。

**順番**

5. 1行の 高さが、★まだ わずかに 見本より 高い（★46 → 実装 44〜48）。

## 3　SH-newnote ／ SH-notemeta の 辿り方（★見本の 中を 数え直しました）

### 3-1　`SH['newnote']`（新しく 書く）── **見本に 入口が ありません**

- 定義　1235行（`SH['newnote']=function(){…}`）
- 呼び出し　**0件**。★`newnote` という 字は、★見本ぜんぶで **その 1行だけ**です。
- 中身　「稽古の メモ／レパートリーに 曲を 足す／受診用の 1枚」の 3行

★＋を 押したときに 何が 出るかは、★`newNote()`（★**2756行**）が 決めます。

```
function newNote(){
  if(S.note==='レパートリー') push('曲を足す');
  else if(S.note==='受診用')  push('日を選ぶ',{from:'受診用',mode:'range'});
  else                        push('稽古を書く');
}
```

★どれも `push(…)` ── **1枚の 画面**です。★シートを 開きません。
★つまり `SH['newnote']` は、★見本 自身の 中で 行き場の 無い 部品です。
→ **くらべる 相手が いないので、撮りません。**

### 3-2　`SH['newrep']`（曲を 足す）── **これも 呼ばれて いません**

- 定義　1237行
- 呼び出し　**0件**
- レパートリーの ＋ は `push('曲を足す')` → `SC['曲を足す']`（★**2778行**）

★★これまで `SH-newrep` として 撮り、★実装の ＋ と くらべて いました。
★**相手が ちがって いました。**
→ `SC-曲を足す` に 直しました。

### 3-3　`SH['notemeta']`（日付と 先生）── **入口が あります**

- 定義　1270行
- 呼び出し　**1079行**、`SC['ノート本文']` の 中

```
+'<div class="two" style="margin-top:10px">'
+'<div class="btn g" onclick="toast(\'消しました\');pop()">消す</div>'
+'<div class="btn g" onclick="openSheet(\'notemeta\')">日付と 先生</div></div>'
```

★辿り方　ノート → 稽古 → **すでに ある メモを 押す** → 本文の 画面 → 「日付と 先生」

★★実装には、★この シートが ありません。
★日付と 先生は、★**稽古を書く 画面の 中の 欄**です（`PRACTICE_FIELDS`）。
★同じ 中身が、★別の 形で 出ます。★抜けて いる わけでは ありません。
→ 実装側は「稽古を書く」の 画面を 相手に します。

### 3-4　ついでに 数えた もの

見本の シート（`SH[]`）は **22件**。★そのうち **4件**が、★どこからも 呼ばれて いません ──
`newnote` ／ `newrep` ／ `gyojid` ／ `newgyoji`。

（★`ねむり` `こえ` `honban` `tabe` `karada` `hito` `したく` は、
　`rowIn()` が 名前を 組み立てて 開きます。★呼ばれて います。）

## 4　この 便の 見張り

267本。★落ちた もの 0件。★build・lint 通過。
描かれた 画面の 見張り（`forbidden-render`）も、★27件の 書き出しで 通りました。
