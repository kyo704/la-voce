# ★★★見張りの 棚卸し（★Opus の ACTION_1・ACTION_2 ／ 2026-09-23）

```
★見張り 450本 ／ ★落ちた 4本 ／ ★止めて いる 1本
★道具 …… tools/run_guards.py（★終わりの 番号だけ で 判じる）
          tools/guard_vacuity.py（★「確かめの 数」を 数える）
```

---

## ★★★ACTION_1 ── ★「ずっと 緑」か、★「何も 見て いない」か

★`tools/guard_vacuity.py` を 作りました。★見るのは 2つ です ──

```
㋐ 終わりの 番号（★0 か どうか）  … run_guards.py
㋑ ★通った しるしの 数（✓ / ok / ○ / ✅ / PASS）
   ★★0 なら、★その 見張りは **1つも 確かめて いません**
```

★★★㋑が 肝 です。★`contrast` は ㋐で 捕まりましたが、
　★「静かに 何も せず 0 で 終わる」形は ㋐では 捕まりません。

### ★見つかった もの（★2本）

| 見張り | 何が 起きて いたか | どう しました |
|---|---|---|
| `a01-kyou` | ★2026-09-11 から **止めて** あり、★`process.exit(0)`。★166行の 確かめは 下に 眠った まま。★数えると「緑」 | ★`RESULT: PAUSED` を 出す ように し、★`run_guards.py` が **緑とも 赤とも 数えない** 形に |
| `perm-keys-consumed` | ★表を 出して `process.exit(0)`。★「★★落としません。★数えて 並べるだけ です」と 自分で 書いて ある。★1つも 確かめ なし | ★確かめを **1つ** 足しました ── 「画面 **か** SQL の どちらかには 出て くる」。★どちらにも 無い 鍵は 消し忘れ |

★★`perm-keys-consumed` の 当たり合わせ …… ★使われて いない 鍵を 混ぜる → ★赤。

★★★`a01-kyou` は 消しません。★静止画が 参照元から 外れた ため 止めた もの です。
　★動く見本で 作り直す のが 筋 です。★いまは「止めて いる」と 見える ように しました。

---

## ★★ACTION_2 ── ★Opus の 道具

★私の 手元に ある もの だけ 見ました。

```
tools/base_manifest_check.py  … ★あります。★きょう 使いました（11区分 ○）
tools/ledger_inventory.py     … ★あります。★きょう 使いました（audit で A4 を 見つけました）
tools/price_check.py          … ★きょう 入れました
tools/release_check.py        … ★★ありません（★Opus の 手元 です）
tools/mobile_parity.py        … ★★ありません（★同上）
```

★★★`release_check` と `mobile_parity` は **この 家に ありません**。
　★★だから、★私からは わざと 壊して 確かめられません。
　★★★Opus の 側で 同じ ことを して いただく 必要が あります ──
　　★① 見張って いる 対象を 1つ わざと 壊す
　　★② 赤く なるか
　　★③ 戻して 緑に なるか

★★`base_manifest_check` は きょう 壊して 確かめて います ──
　★土台の 色を ずらす → ★赤 ／ 戻す → ★緑（★本報告の 前の 記録に あります）。

---

## ★お指図の 5件

| | お指図 | いま |
|---|---|---|
| `wardrobe-v2-not-yet` | 消す | ★消しました |
| `type-steps` | 12.5px に | ★**六段**を 12.5 に（★下記） |
| `bill-breakdown` | 名の 追随 ＋ 9/10 の 食いちがい | ★追随しました。★食いちがいは **直って いました**。★別の 赤が 出ました（★下記） |
| `repertoire` 3件 | 調べる | ★下記 |
| ACTION_1 / 2 | ― | ★上記 |

### ★★type-steps ── ★「いま」は 動かしませんでした

★Opus の お指図は「実装を 12.5px に」でした。★★1つだけ 変えて いません。

```
UIV2_TYPE の 形
  `いま`  … ★門の **外**（★38人の 古い 画面）の 大きさ
  `六段`  … ★門の **中**（★新しい 画面）の 大きさ
```

★★`いま` を 動かすと、★お伺いせずに **38人の 画面**が 変わります。
★★★だから `六段` だけ 12 → 12.5 に しました（★ほかの 5つと 同じ 形に なりました）。
　★`components/tests/uiv2-type-gate.test.js` が「`いま` は 12 未満」を 見張って います。
　★★そちらも 通ります。

★あわせて、★裁定179 で 戻した レーンの 札（`LookBackV2.jsx`）が 10.5px でした。
★★★これは 私の 戻しが 連れて きた もの です。★12.5px に しました。
★`tools/display_registry.json` に 1行 足しました（★お指図の とおり）。

### ★★bill-breakdown ── ★9/10 の 食いちがいは 直って いました

```
見本の いま …… function gakkoYen(n){
  return Math.max(12800, Math.min(n*400, Math.max(n,100)*350, Math.max(n,300)*250)) }
```

★`Math.max(n,100)*350` ／ `Math.max(n,300)*250` ── ★条件は **入って います**。
★`lib/orgRoster.js:296` も `{ min: 100, rate: 350 }`。★合って います。
★★見張りに、★その 条件を 見る 確かめを 3つ 足しました（★下限 12,800 も）。

★★★ただし、★動く ように した とたん **別の 赤**が 出ました ──

```
★落ちました ── 6人で 内訳と 答えが ずれます

  5人  段=400:2000 350:35000 250:75000  月額=0
  6人  段=400:2400 350:35000 250:75000  月額=2980
 20人  段=400:8000 350:35000 250:75000  月額=2980
```

★★`billPlans(6)` は 3段を 返します。★いちばん 安い のは 2,400円 です。
　★けれど `monthlyFee(6)` は **2,980円**（★教室の 値段）です。
★★★内訳と 答えが 合いません。

★★これは **お金の 話** です。★私の 見立てでは、
　★`billPlans` は 学校の 段の 表 で、★6人は 教室の プラン です。
　★★「学校の 段」を 教室の 人に 見せて いる ことが そもそもの ずれ に 見えます。
★★★勝手に 直しません。★お決めを お待ちします。

### ★★repertoire 3件 ── ★調べました

| 見張り | 落ちて いる 1件 |
|---|---|
| `repertoire-identity` | ★`record を 生の 名前で 引いて いない` |
| `repertoire-edit-delete` | ★`ただし、全部 空には できない（行の 意味が 無く なる）` |
| `repertoire-pitch-entry` | ★`呼ぶ先（handleSaveRepertoire）は 3つの どれか 1つで 受け付ける` |

★どれも 1件ずつ です（★29／43／18 は 通って います）。
★★中身の 当たり外れ（★実装が 正か、★見本が 正か）は、★Opus の お決め 待ち です。
★★★私からは 直しません。
