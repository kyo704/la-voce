# ③【止まり】5件 ── 機能が 止まって いないかの 調べ

全115行 / 末尾は「　★★作る 前に、★この 報告を 思い出して ください。」

門は 立って いるが、★決まりが 0件の 表。★**漏れでは ありません。**
★逆に、★**誰も 読めません**。★ご本人も です。

★★ただし、★**裏口（service role）は 門を 飛び越えます。**
　★★裏口 だけが 触る 表なら、★決まりが 0件で **正しい** のです。
　★★だから、★1つずつ「誰が 触るか」を 調べました。

## まとめ

| 表 | 誰が 触るか | 決まり 0件は | 機能は |
|---|---|---|---|
| `account_deletions` | **裏口だけ** | ★正しい | ★動いて います |
| `recovery_codes` | **裏口だけ** | ★正しい | ★動いて います |
| `notice_batches` | **どこからも 触って いません** | ★正しい | ★まだ 使って いません |
| `import_staging` | **どこからも 触って いません** | ★正しい | ★空の まま（★仕様どおり） |
| `system_alerts` | ── | ★正しい | ★わざと 決まりを 作らない、と お決め済み |

★★**5件とも、★静かに 止まって いる ものは ありません。**

## 1つずつ

### `account_deletions`（退会の 数）

| どこ | 客 |
|---|---|
| `lib/accountDeletion.js:309` | `admin`（★裏口） |
| `app/admin/page.js:161` | `admin`（★裏口） |

★どちらも 裏口です。★画面（ログイン済みの 客）からは 1度も 触りません。
★★`app/admin/page.js` は、★先に ご自分の `profiles.is_admin` を 見てから
　裏口を 使います。★入口は 守られて います。

★★この 表は「退会が 何件 あったか」だけを 持ちます。★誰が、は 持ちません。
　★だから、★ご本人が 読む 道が そもそも 要りません。

### `recovery_codes`（控えの 合言葉）

| どこ | 客 |
|---|---|
| `app/api/recovery/issue/route.js:50` | `admin` |
| `app/api/recovery/issue/route.js:95` | `admin` |
| `app/api/recovery/redeem/route.js:111` | `admin` |
| `app/api/recovery/redeem/route.js:139` | `admin` |
| `app/api/recovery/redeem/route.js:155` | `admin` |
| `app/api/recovery/redeem/route.js:188` | `admin` |

★**6か所 すべて 裏口**です。★画面からは 1度も 触りません。

★★これは、★わざと そう して あります。
　★★控えの 合言葉は、★ご本人にも **読ませては いけない** ものです。
　　★★台帳に 入って いるのは、★合言葉そのものでは なく、★その 影です。
　　★★読める 道を 作ると、★影を 持ち出せます。
　★★だから、★決まりが 0件で 正しいのです。★むしろ、★作っては いけません。

### `notice_batches`（お知らせの 束）

アプリの どこからも 読み書きして いません。

| どこ | 何 |
|---|---|
| `lib/backupTables.js:153` | ★控えを 取る 表の 一覧に 名前が あるだけ |
| `lib/exportData.js:56` | ★「書き出しに 含めません」と 書いて あるだけ |
| `lib/accountDeletion.js:86` | ★「消しません」と 書いて あるだけ |

★★`notice_targets` も、★どこからも 触って いません。

★★運営の 側の 表です。★まだ 画面が ありません。
　★★だから、★止まって いるのでは なく、★**まだ 始まって いません**。

### `import_staging`（取り込みの 置き場）

アプリの どこからも 触って いません。
作った SQL 自身が、★こう 書いて います ──

```
supabase/2026-09-09-1年ぶんの取り込み.sql:17
　★★import_staging の 表は、★空のまま 残っています。
supabase/2026-09-09-1年ぶんの取り込み.sql:94-95
　revoke all on public.import_staging from anon;
　revoke all on public.import_staging from authenticated;
```

★★**わざと、★誰も 触れない ように して あります。**
　★裏口だけが 使う 置き場です。★決まりが 0件で 正しいのです。

### `system_alerts`

坂本さんの お決めどおり、★わざと 決まりを 作らない 表です。

## この 調べで 使った 手

「誰が 触るか」は、★**客の ちがい**で 決まります。

| 客 | どこ | 門（RLS）を |
|---|---|---|
| 画面（browser） | `lib/supabase/client.js` | ★通ります |
| 入口（route handler） | `lib/supabase/server.js` | ★通ります |
| **裏口** | `lib/supabase/admin.js` | ★**飛び越えます** |

★★だから、★`from("表")` を 探すだけでは 足りません。
　★その 行の 客が `admin` か どうかまで 見る 必要が あります。
　★★今回、★6か所 すべてを 1行ずつ 目で 追いました。

## お勧め

**5件とも、★いま 触る 必要は ありません。**

★★ただし、★1つだけ お勧めが あります。
　★`notice_batches` と `notice_targets` は、★これから 画面が できます。
　★★その とき、★**画面から 触る なら 決まりが 要ります**。
　★★作る 前に、★この 報告を 思い出して ください。
