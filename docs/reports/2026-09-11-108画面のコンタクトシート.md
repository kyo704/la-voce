# 108画面の コンタクトシート

全104行 / 末尾は「描かれた 画面の 見張り（`forbidden-render`）も、★34件の 書き出しで 通りました。」

コードに 入れました。見張りは 通りました。見た目は 未確認です。実機で お確かめください。

## 1　軸の お決め（★①）── 入れました

「見本どおり（0〜6時間の 固定）に してください。」

- **食べ終えてから 寝るまで** … **0〜6時間の 固定**に しました。
  ★見本の `dotplot` が `sy(v)=H-v/6*H` の 固定です。
- 端の 外の 点は、**描きません**。★端に 貼り付けると、そこに 点が あるように 見えます。
  ★代わりに、図の 右上に「**6時間より 外に ◯日**」と 数だけ 書きます。★黙って 消しません。

### ★1つだけ、お伝えする ことが あります

**ほかの 項目は 固定に して いません。**

- **昨夜の 睡眠** … 0〜6時間に すると、★7〜8時間が ふつうなので
  **ほとんどの 点が 外に 出ます。**
- **声を使った 時間** … 分です。★6分では ありません。
- **部屋の しめり** … ％です。

見本に 図が あるのは「食べ終えてから 寝るまで」の 1つだけです。
★そこだけを 固定に し、★ほかは 0 から 始めて 上だけ 点に 合わせて います。
★★ほかも 固定に する 場合は、★項目ごとの 上限を お決めください。

## 2　コンタクトシート

`docs/design/compare/all/sheet-01.png` 〜 `sheet-14.png`（★15枚目まで）
1ページ 8枚・見本 左／実装 右・iPhone 12（390×844・倍率3）・`fullPage: true`

1画面 1枚の 絵も 111枚 あります（`docs/design/compare/<名前>.png`）。

### 2-1　数

| | 数 |
|---|---|
| 見本（★SC 76 ／ SH 22 ／ 帯 10） | **108** |
| 実装が ある もの | **34** |
| 実装が まだ 無い もの | **74** |

★撮れなかった もの　**0件**（★見本・実装 とも）。

### 2-2　名前の 取り方

見本の 名前は、★**見本 自身から** 取って います。★私が 書き写して いません。

```
Object.keys(window.SC)  → 76
Object.keys(window.SH)  → 22
```

★書き写すと、★見本が 増えた とき 片方だけ 古く なります。

### 2-3　引数の 要る 7画面

はじめ、★7画面が 撮れませんでした。★見本の 中の 呼び出しから 写しました。

| 画面 | 渡す もの |
|---|---|
| `SC['前3日']` | 日の 番号（★12） |
| `SC['組織']` | 欄の 名前（★`'gakubu'`） |
| `SC['授業を入れる']` | `"曜-コマ"`（★`'1-0'`）＋ `KOMA_MY[ME]` |
| `SC['予定の中身']` | 同じ 形 |
| `SC['自分のコマの時間']` | 番号 ＋ `KOMA_MY[ME]` |
| `SC['受診プレビュー']` | `S.ju`（★選んだ 欄）を 先に 作る |
| `SH['したく']` | `S.k1` `S.k2`（★`openDr` と 同じ） |

## 3　実装が まだ 無い 74画面

ほとんどが **教室・組織・運営**の 画面です（★坂本さんが ㋑ で 後回しに されたぶん）。

- 時間割／コマ／授業／出欠／レッスン
- 役職／門下／組織／代表／合言葉／招く
- 請求書／領収書／支払い方法／プラン
- 行事／予定／重なり／日程

そのほか ──

- ひつじ　たな／まだ／全部／置ける枠
- ノート　ノート本文／たぶん本文／曲／曲を直す／宛先／未送信／さがす
- 受診用　受診プレビュー／日を選ぶ／聞いてほしいこと
- もっと　同意／設定の 中／見やすさ／記事
- シート　`repMenu` `yousu` `sagasu` `shiboru` `tsuchi` `motteru` `account` ほか

★★`newnote` `newrep` `gyojid` `newgyoji` の 4つは、
★**見本 自身の 中で どこからも 呼ばれて いません**。★入口が ありません。

## 4　この 便で 直した もの

- `forbidden-render` の 禁じ手を、★**画面ごとに 絞りました**。
  ★設定の 画面の「『食事の詳細記録（食品別のPFC）』を30日間 記録して
  いません。畳みますか？」という **別の 機能の お誘いの 文**に つまずいて いました。
  ★節が 出ている わけでは ありません。★記録の 画面だけを 見ます。
- 押しどころの 探し方を **3段**に しました。
  ★もっと の 一覧の 行は、★字が `<span>` で 押しどころは その 外側です。
  ★これで もっと の 中の 7画面が 撮れる ように なりました。

## 5　見張り

267本。★落ちた もの 0件。★build・lint 通過。
描かれた 画面の 見張り（`forbidden-render`）も、★34件の 書き出しで 通りました。
