# 裁定167 の CHECK_FIRST 3点 ── 確かめました（2026-09-22）

★Opus は リポジトリを 見られません。★3点 とも Code が 確かめる もの です。

---

## A1 ── 「持って いる 品」は どちらで 決めて いるか

★★★答え …… **`character_inventory` です。**

```
components/VocalTracker.jsx:6773
  supabase.from("character_inventory").select("item_key").eq("user_id", userId)
```

★`item_acquisitions`（台帳）は、★書き出しと 退会の 消し込みで しか 出て きません。
★画面の「持って いる／いない」は、★`character_inventory` 1本 で 決まって います。

★本番の 数 …… `character_inventory` **326行** ／ `item_acquisitions` **4行**。

★★★つまり 裁定167 の 読みは 当たって いました ── ★**いま 払わずに 使えました。**

### ★塞ぎました（2026-09-22・束2 と 一緒に・坂本さんの ご承認）

```
前 …… authenticated に 表ごとの SELECT / INSERT / UPDATE / DELETE
後 …… SELECT だけ
```

★★壊れません。★書く 道は **3つ とも `service_role`** です ──

| 道 | ファイル |
|---|---|
| 買う | `app/api/character/buy/route.js:125`（`createAdminClient()`） |
| 贈る | `app/api/character/gift/route.js` |
| 退会の 消し込み | `purgeAccount(admin, …)` |

★試しの 台帳で 確かめました …… ★足せない・書き換えられない・読むのは そのまま（13/13）。

★★★残って いる 決め …… 「持って いる 品の 正」を `item_acquisitions` 1本に するか。
　★いまは 塞いだ だけ です。★2つの 表が ある ことは 変わって いません。
　★★`item_acquisitions` は 4行 しか ありません。★326行 とは 合って いません。
　★★★片方を 正に する なら、★先に この 322行の 差を どう するかを 決めます。

---

## B1 ── 公開ページの 読み方

★★★答え …… **公開ページは、まだ ありません。**

★`app/` の 下に、★ポートフォリオの 道（route）は 1つも ありません。

```
app/  …… admin / announcer-theory / api / auth / billing / dashboard /
          feedback / guardian / hajimeru / legal / login / parents /
          performer-theory / recovery / reset-password / shindan /
          signup / start / vocal-theory / voice-actor-theory
```

★`app/api` にも `portfolio` の 字は **0か所** です。

★いま `portfolios` を 引いて いるのは、★`components/VocalTracker.jsx` だけ です
（★入って いる ご本人の 鍵で。★決まりは「本人だけ」）。

★★★だから B1 は **いまの 不具合では ありません**。
　★「公開ページを 作る 日の 決め」です。
　★★裁定167 の とおり、★`get_public_portfolio(slug)` の 関数 1本で 読む 形に します。
　★★その 日まで、★`service role` で 表を 直に 読む コードを 1行も 書きません。

---

## A2 ── webhook の 署名 と「使えるか」の 判定

### ① 署名 …… ★**確かめて います**

```
app/api/stripe/webhook/route.js
  const body = await request.text();                       ★JSON.parse しません
  const signature = request.headers.get("stripe-signature");
  event = stripe.webhooks.constructEvent(body, signature, process.env.STRIPE_WEBHOOK_SECRET);
  → 落ちたら 400 を 返します
```

★★Stripe の 支度が できて いない ときは、★その 前に **503** で 止まります。
★★`STRIPE_WEBHOOK_SECRET` が 無ければ `constructEvent` が 投げます → 400。
★★★閉じる 向きに 倒れて います。★良い 形 です。

### ② 「使えるか」の 判定 …… ★★**1つの 関数に なって いません**

★いま 判じて いる ところ ──

| どこ | 何を 見て いるか | 期限を 見て いるか |
|---|---|---|
| `app/dashboard/layout.js:29` | `status === "trialing" \|\| status === "active"` | ★★**見て いません** |
| `app/billing/page.js:67` | `status, current_period_end` | ★出すだけ（門では ありません） |

★★★裁定167 A2 が 言う 形に なって いません ──
　★求め …… 「`status` が active／trialing」**かつ**「`current_period_end` が 今より 後」。
　★いま …… `status` だけ。

★★★何が 起きるか ── ★webhook が 止まった まま 期限が 過ぎても、
　★行は `active` の まま 残り、★**使い続けられます**。
　★★これは まさに 裁定167 が「作らない」と 言った 形 です。

★★いま 実害が 無い わけ …… `REQUIRE_SUBSCRIPTION` が `true` では ありません。
　★この 門は いま **1人にも かかって いません**。
　★★けれど、★値段を 始める 日に そのまま 効き始めます。

### ★お勧め（★決めは 坂本さん・Opus）

```
lib/entitlement.js（新）
  export function usable(sub, purchase, nowISO)
    ・sub  …… status in ('trialing','active') かつ (current_period_end ?? period_end) > now
    ・purchase … status が 有効 かつ ends_at > now
  ★門は これ 1本を 呼びます。★画面の 各所で 別々に 判じません。
```

★★`lib/freeTier.js` の `mayViewSummary` / `paidGateApplies` は **別の 問い** です。
　★あちらは「勧誘の 板を 出すか」であって、「読んで よいか」では ありません。
　★★混ぜません（★台帳の 覚えの とおり）。

---

## ★あわせて ── 届いた zip が、古い 版でした

★きょう 届いた `woolsong-2026-09-21.zip` は、★**第10版** の 束 でした。
★手元には すでに **第11版**（裁定168・169・170 つき）が 入って います。

★展くと、★4つの ファイルが 古い ものに 戻りました ──

| ファイル | 戻った 中身 |
|---|---|
| `README.md` | 第11版 → 第10版 |
| `引き継ぎ_Opus_2026-09-21.md` | 第11版 → 第10版 |
| `tools/ruling_mock_registry.json` | ★裁定170 の 1行が 消えました |
| `実行ルート統合版_第10版_2026-09-21.md` | （★第11版とは 別の ファイル。★そのまま） |

★★3つを 戻しました（`_old-2026-09-22b/` から）。★裁定170 の 行も 戻って います。
★★★これが、★台帳の 覚えに ある「新しい 束は 黙って 上書きする」形 です。
　★これから も、★展く たびに 新旧を 見ます。
