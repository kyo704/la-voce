# ★分析 ①③④ ── ★作る 前の 下ごしらえ

★この 紙は `tools/analysis134_readiness.py` が 書きました。★手で 足して いません。
★数えた 日 …… 2026-09-26
★もと …… `docs/opus/未決機能の設計書_第3版_2026-09-14.md` §2-1〜§2-6

★★**1行も 実装して いません**。★下ごしらえ だけ です。

## ★〇 先に ひとこと ── ②は もう つながって います

★設計書は ①②③④ の 順を 決めて います。★②「自分で 問いを 立てる」は`components/Shirabeteiru.jsx` が **つながって います**（★`VocalTracker.jsx:28600`）。
★だから ①③④ の 話に なります。★③は ②の 中の 設定 です。

## ① まだ 分からない ことを 見せる

★置き場所 …… ふりかえる → くらべる の 最上部 ／ ★無料（設計書 §2-1・§2-2）

### ★いま ある もの

- `ProgressDots`（`components/VocalTracker.jsx:3570`）── ★「◯日分たまりました」の 1行。★進捗バーと「あと◯日」は 2026-09-11 に 消して あります（★裁定 §2-4）。
- `lib/analysisBoost.js`（82行）── ★R1〜R6。★`VocalTracker.jsx:9938` が 呼び、★26062行あたりで 描いて います。
- `lib/displayGates.js`・`lib/analysisCardVisibility.js` ── ★門と 出し分け。

### ★足りない もの

- ★設計書の 形は **組ごとの 行**（★「眠った時間 × 声の調子　10日のうち 7日」）です。★いまの `ProgressDots` は **カード 1枚の 中の 1行** で、★呼ぶ ところは 1か所 だけ です。
- ★`components/CompareV2.jsx`（550行）の 最上部に、★その 一覧を 出す ところが ありません（★「たまりました」は 497行の 1文 だけ ── ★出なかった日の 数 です）。
- ★組ごとの 日数を 数える 関数が どこにも ありません。★門は `lib/displayGates.js` が1つ だけ 持つ 決め なので、★そこに 足す ことに なります。

### ★main が 作る もの

- ①`lib/displayGates.js` に「この 組は 何日 たまったか」を 答える 関数を 1つ。★数える もとは `entries` だけ です。★列も 表も 足しません。
- ②`components/CompareV2.jsx` の 最上部に 一覧。★②の 組（`Shirabeteiru`）から組の 一覧を 受け取る 道。
- ③「◎7日 △4日」の ◎△ を 何で 分けるか を 先に 決める（★門の 10日 か、★別の 線か）。

### ★気を つける こと

- ★「あと◯日」を 書かない（★2026-09-11 に 消した もの です。★戻すと 同じ 裁定に 当たります）。
- ★％・進捗バー・信号色は 1つも 出さない（★`CompareV2.jsx` の 頭の 註）。

## ③ 遅れて 効く もの（時間差）

★置き場所 …… くらべる → 組の 設定の 中 ／ ★有料（設計書 §2-4）

### ★いま ある もの

- `lib/lagChoice.js`（177行）── ★`LAGS` 4種・`defaultLagOf`・`judgingLagOf`・`testsPerItem`・`needsRecount`。★「表示は 4つ、判定は 1つ」の 決めは 入って います。
- ★読み手は 5つ ── `lib/compareView.js`／`lib/compareOrder.js`／`lib/shirabeteiru.js`／`components/CompareV2.jsx`／`components/VocalTracker.jsx`。
- `lib/shirabeteiru.js`（121行）── ★無料1組（`FREE_PAIRS`）・`mayAddPair`。★画面（`components/Shirabeteiru.jsx`）は **つながって います**。

### ★足りない もの

- ★★**選んだ 時間差を しまう 先が ありません**。★`lib/shirabeteiru.js` の 註がそのまま 書いて います ──「時間差を しまう 列・表・関数は 1つも ありません。画面の 状態 だけ」。
- ★本番の 台帳でも 確かめました（★下の 表）── ★それらしい 表 0・`profiles` に 列 なし。
- ★だから 設計書 §2-2 の「★変えたら、そこから 数え直します」が **成り立ちません**。★いつ 変えたかを 覚えて いない ため です。
- ★有料／無料の 出し分けの 印が ありません。★`lib/entitlements.js` の `FEATURES` に時間差の 名が ありません（★`analysis.range` は 期間の 話 で 別 です）。

### ★main が 作る もの

- ①組と 時間差を しまう 表 を 1つ。★列は 少なく ── `user_id`／`life_key`／`voice_key`／`lag`／`decided_on`。★`decided_on` が「そこから 数え直す」の もと です。
- ②その 表の 決まり（RLS）── ★`USING` と `WITH CHECK` の **両方**。★`USING(true)` を 書かない。
- ③SQL は 紙に 書いて 坂本さんに 渡す（★この 蔵に 自動の 仕組みは ありません）。
- ④有料の 線を `lib/entitlements.js` に 1行。★`mayViewSummary`／`paidGateApplies` を使い回さない（★あれは「壁を 出すか」の 問いで、★「読んで よいか」では ありません）。

### ★気を つける こと

- ★字（4種の 名）は `lib/lagChoice.js` が 1か所で 持ちます。★表にも 画面にも 写さない。
- ★「2日後」と 書かない ── ★見本は「2日まえ」です（★裁定197・未来と 読めない 字）。

## ④ 本番の 前の 1週間

★置き場所 …… ふりかえる → 本番の ふりかえり ／ ★有料（設計書 §2-5）

### ★いま ある もの

- `lib/honbanFurikaeri.js`（123行）── ★`DAYS=7`・`isOwn`・`rowsOf`・`mayCompare`・`sleepWord`・`NOT_WRITTEN`。★約束 4行も ここに あります。
- `components/HonbanFurikaeri.jsx`（60行）── ★画面は できて います。
- ★台帳 `performances` は 本番に あります（★10列・`org_event_id` は 空でも よい）。
- ★`entries.sleep_hours` は 118行 入って います（★7日 並べる もとは あります）。

### ★足りない もの

- ★画面が **つながって いません**。★`VocalTracker.jsx` に `import HonbanFurikaeri` がありません。★`tools/not_wired_yet.json` の 引き金 ──「ふりかえる から 本番 1件を 押す 道を 作った とき」。
- ★★**本番の 行が 0 です**（★本番の 台帳・2026-09-26）。★書く ところは`VocalTracker.jsx:28677` の `insert` 1か所 だけ です。★つないでも 中身が 出ません。
- ★名ざしの 見張り `components/tests/honban-furikaeri.test.js` が **在りません**（★`lib/honbanFurikaeri.js` の 註が 指して います）。★いま 触れて いるのは `components/tests/c-group-night.test.js` だけ です。
- ★2回目からの「重ねる」相手を どう 選ぶかが 決まって いません（★`mayCompare` は 可否を 返す だけ です）。

### ★main が 作る もの

- ①ふりかえる → 本番 1件 → この 画面 の 道。
- ②本番を 登録する 入口を 先に 確かめる（★0行 の まま 出すと、★空の 画面に なります）。
- ③`org_event_id is null` の 絞りを **問い合わせの 側** に 置く（★`isOwn` は 画面で 弾くだけ です。★列ごと 取って きません）。
- ④名ざしの 見張りを 1本 書く（★約束 4行 ── ★判定しない／2回目から／前日と当日は出さない／学校の 行事から 引かない）。

### ★気を つける こと

- ★`org_events` から 日付を 引かない（★層を またぐ 結合 ── 設計書 §1-3）。
- ★書いて いない 日は 0 に しない。★「書いていません」と 出す（★`NOT_WRITTEN`）。

## ★台帳を 見た 結果（★2026-09-26・読み取り のみ）

| 見た もの | 答え |
|---|---|
| public に compare/pair/shirabe/lag/analys の 名の 表 | 0 表 |
| profiles の 77列に 組・時間差を しまう 列 | ありません（★`folded_groups` は 別物） |
| performances の 列 | 10列（id・user_id・performed_on・kind・label・morning_words・repertoire_name・org_event_id・created_at・updated_at） |
| performances の 行 | 0 行（★利用者 0人） |
| entries の 行 | 125 行 ／ 利用者 18人 ／ 2026-08-12〜2026-09-25 |
| entries.resonance_score が 入って いる 行 | 83 行 |
| entries.sleep_hours が 入って いる 行 | 118 行 |
| 眠り×響き が 10日 そろって いる 利用者 | 2人 |

★問い（★同じ ものを もう 一度 引ける よう、★そのまま 置きます）

```sql
select table_name from information_schema.tables where table_schema='public' and (…ilike…);
select column_name from information_schema.columns where table_name='profiles' and (…ilike…);
select column_name from information_schema.columns where table_name='performances';
select count(*) from public.performances;
select count(*), count(distinct user_id), min(date), max(date) from public.entries;
select count(*) from public.entries where resonance_score is not null;
select count(*) from public.entries where sleep_hours is not null;
select count(*) from (select user_id from public.entries where resonance_score is not null and sleep_hours is not null group by user_id having count(*) >= 10) x;
```

## ★紙と 見張りの 対応（★註に 名が あって、★実在するか）

| もとの 紙 | 名ざしの 見張り | 在るか |
|---|---|---|
| `lib/analysisBoost.js` | ★名ざし なし | — |
| `lib/lagChoice.js` | `components/tests/lag-choice.test.js` | ○ |
| `lib/honbanFurikaeri.js` | `components/tests/honban-furikaeri.test.js` | ★無い |
| `lib/shirabeteiru.js` | ★名ざし なし | — |

## ★画面が つながって いるか（★`VocalTracker.jsx` から たどれるか）

★★直に 読まれて いなくても、★読み手の 読み手を たどります（★`CompareV2` は `LookBackV2` の 中に あります）。

| 部品 | たどれるか | 読み手 |
|---|---|---|
| `components/Shirabeteiru.jsx` | ○ | `components/VocalTracker.jsx` |
| `components/HonbanFurikaeri.jsx` | ★いいえ | ★ありません |
| `components/CompareV2.jsx` | ○ | `components/LookBackV2.jsx` |
| `components/LookBackV2.jsx` | ○ | `components/VocalTracker.jsx` |

## ★この 紙で 言えない こと

- ★台帳の 数は 2026-09-26 の もの です。★次に 読む ときは もう 一度 引いて ください。
- ★設計書 §2-8 の 3つ（★どれが 2値か／`deltaST` を 選択項目に するか／列名の 直し）はまだ 決まって いません。★ここで 決めて いません。
- ★門（n≥10・|g|≥0.50・0.50+0.12×(k−1)）の 数は `lib/displayGates.js` が 持ちます。★この 紙に 写しません（★写すと 2か所に なります）。

