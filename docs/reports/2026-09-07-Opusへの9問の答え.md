# Opus からの9問への答え

全232行 / 末尾は「★この2つが決まれば、★62点の取り込みは、そのまま進められます。」

読んだ日：2026-09-07 ／ 読んだもの：`stripe-test` の HEAD
★これは「設計はこうなっている」ではなく、「コードがこう書いてある」の報告です。

★別紙が2つあります。
　・`docs/reports/2026-09-07-着せかえ219点の全一覧.md`（④）
　・`docs/reports/2026-09-07-おうちの道具101点の全一覧.md`（⑦）

---

## ① ポイントは、現金で買えるか → ★買えません

道が1本もありません。

・残高は `computeBalance(entries, pointsSpent)`。足す側は記録だけです。
　残高はデータベースに置かれず、★毎回この式で計算し直しています。
・Stripe の webhook に points / wardrobe / inventory の語が★0件。
・`checkout` は `mode: "subscription"` のみ。単発決済は★リポジトリ全体で0件。
・`lib/freeTier.js` の `NEVER_PAID` に「羊とおうち・装いの表示」が入っています。

★★9月7日、★お金とは別の弱いところを1つ塞ぎました。
　「使ったぶん」（`profiles.character_points_spent`）を、★ブラウザから直に書けていました。
　★この数を小さく書けば、★記録しないままポイントが増えます。★現金は関わりません。

　・受け取る処理を、サーバへ移しました（`app/api/character/buy/route.js`）。
　　★受け取るのは品物の名前だけです。★値段は、サーバが引きます。
　・列を2枚で守ります（トリガー8列＋列ごとの権限）。★坂本さんが適用ずみです。
　・見張り `components/tests/profile-server-only-columns.test.js`

---

## ② 記録だけで、ひと月に何点たまるか → ★毎日書けば 150点

`lib/character.js:21` `DAILY_POINTS = 5`。
2026-08-27 以降は、★その日に何か書けば、★中身を問わず一律5点です。
項目を増やしても増えません（かんたん記録の方が不利にならないように直しました）。

| 月に記録した日数 | ひと月のポイント |
|---|---|
| 30日（毎日） | **150点** |
| 20日 | 100点 |
| 15日 | 75点 |

★初めて使った項目のごほうび：10種 × 3点 ＝ **30点**。★一度きりです。

★古いお店の101点は、値段の中央値が80点、★着られるもの22点は中央値95点です。
　★だから、★毎日書く方は、★ひと月に1〜2点を受け取れる計算になります。

---

## ③「よそおい放題」は、数か、期間か → ★9月7日に、期間へ書き替えました

**コードには、影も形もありません。**
MORE_MONTHLY・よそおい多め・よそおい放題・980 は `.js` `.jsx` で★0件。
`PLANS` は月580円と年4,800円の2つだけです。

文書のほうを、★3か所書き替えました
（`docs/opus/lavoce-価格と課金の正（9月6日・確定）.md` の §1・§3・§8）。

　　その月のあいだ、装いを、1日1点ずつ、自由に選べます

★あわせて、実装のときの決めも同じ文書に書き込みました。
　★残り数を出さない／持ち越さない／1日1点／受け取ったものは残る。

---

## ④ 219点の全一覧 → ★別紙にしました

`docs/reports/2026-09-07-着せかえ219点の全一覧.md`

★鍵・名前・group・slot・目録の slot・theme・箱、を1点ずつ並べています。

★★数について、1つ訂正があります。
　`lib/sheepItems.js:8` の注釈は「決定版11・217点」と書いていますが、
　実際の `docs/assets/sheep-items-index.json` は `version: 2026-09-05k` で、
　★**219点**でした。

　★差の2点は、★目録384点のほうに載っていないものです。
　　`propUmbrella`（傘・business）と `propSword`（剣・opera）。
　　★つまり「384と一致するのは217点」で、★これが「217」の出どころです。

---

## ⑤「食べ終えた時刻」「就寝時刻」は、すでに記録できるか → ★どちらもあります

| 中身 | アプリの中の名前 | 列 |
|---|---|---|
| 夕食の時刻 | `dinnerTime` | `dinner_time` |
| 就寝時刻 | `bedtime` | `bedtime` |

　・`rowToEntry`（:1673-1674）で読み、`entryToRow` で書いています。
　・画面の文言 `labelBedtime`「就寝時刻」（`lib/translations.js:587`）。
　・「夕食時刻と就寝時刻を入力すると、間隔が表示されます」も、すでにあります。
　・同意の文にも入っています（`lib/consent.js:62`）。
　・自分の記録の書き出しにも入っています（`lib/ownRecordFields.js:57`）。

★★ただし「★食べ **終えた** 時刻」ではありません。★「夕食の時刻」です。
　★逆流を見るなら、★終えた時刻のほうが正しいはずです。
　★言葉を変えるか、★別の欄にするか、★ご判断ください。

---

## ⑥ 3つの門は、本当に実装されているか → ★3つとも入っています。★ただし1件、死んでいます

`lib/displayGates.js`

```
① 各群 n ≥ 10        NARRATIVE_MIN_N_PER_GROUP = 10
② |Hedges' g| ≥ 0.4   NARRATIVE_MIN_EFFECT_SIZE = 0.4
　（相関なら |ρ| ≥ 0.3  NARRATIVE_MIN_RHO = 0.3）
③ BH-FDR で q < 0.10  NARRATIVE_FDR_Q = 0.10 ／ requiresFdrPass: true
```

`evaluateGate` が、★この順に3つとも見ています（:163・:172・:179）。
★どれか1つでも欠けたら、★通しません。★安全側に倒れています。

★★1件、★見つけたことがあります。

　`role.narrative` は、★`{ n1, n0 }` しか渡されていません（VocalTracker :8400）。
　★効果量も、★FDR も、渡っていません。

　→ `effectPasses` が「判定不能」を返し、★そこで止まります。
　→ **つまり、この文は、★一度も出たことがありません。**

　★安全側には倒れています（★勝手に語りません）。
　★ですが、★作った文が、★どこからも出ない状態です。
　★直すか、★やめるか、★ご判断ください。

---

## ⑦ おうちの道具101点の全一覧 → ★別紙にしました

`docs/reports/2026-09-07-おうちの道具101点の全一覧.md`

★鍵・名前・分類・大きさ・値段（ポイント）を、1点ずつ並べています。

| 分類 | 点数 |
|---|---|
| 床 floor | 15 |
| 壁 wall | 17 |
| 窓 window | 11 |
| 景色 scenery | 7 |
| 背景 backdrop | 10 |
| 家具 furniture | 6 |
| 庭 garden | 8 |
| 壁かけ wallhang | 5 |
| 帽子 hat | 6 |
| 服 outfit | 10 |
| 小物 accessory | 6 |
| **計** | **101** |

　★値段は5点〜1,800点。★合計 13,269点。
　★中央値は80点です。★毎日書く方なら、★ひと月に1〜2点です。

★★このうち★着られるもの22点（帽子6・服10・小物6）は、
　`lib/legacyWearables.js` で、★新しい219点のほうへ引き当ててあります。
　★すでにお持ちの方は、★どう決まっても、そのまま持ち続けます。

---

## ⑧ 166点のうち62点だけを、先に入れられるか → ★入れられます

　・62点の鍵は、★62点すべてが `docs/opus/items.json` にありました（★欠けなし）。
　・絵も、★62点すべてが zip の中にありました
　　（`assets/wardrobe-v2/羊-着せかえアイテム166点（v4・帽子と靴を直し）.zip`）。
　　★`parts/<部位>/<鍵>@2x.png` という形です。
　・大きさは 1024×1024 で、★いま動いている絵と同じでした。

★★ただし、★絵を置くだけでは着られません。★仕組みを足す必要があります。

　いまの重ね順は、こうです。
　　`["body", "garment", "neck", "head", "shoes", "hat", "prop"]`

　★上（top）・下（bottom）・羽織り（outer）・目元（eyes）が、★ありません。
　★62点のうち★39点が、★この4つに入ります。

　★★`garment` に全部入れると、★1枚しか着られません。
　　★上と下を同時に着られないので、★「全身が組める」が成り立ちません。

　★触るのは、次の4つです。
　　① `lib/sheepWardrobe.js` の `LAYER_ORDER`
　　② `docs/assets/sheep-items-index.json` の `order` と、62点の追記
　　③ `lib/wardrobeOutfits.js` の `OUTFIT_SLOTS`
　　④ `docs/assets/sheep-items-index.json` の `groups`（★新しい組を1つ）

　★`components/SheepDressed.jsx` は `LAYER_ORDER` を見ているので、★直しません。

　★重ね順の案：`body → bottom → top → outer → garment → neck → head → eyes → shoes → hat → prop`
　★★この順でよいか、★ご確認ください。★絵の重なりに直に効きます。

---

## ⑨ 同じ名前のものは、まとめてあるか → ★まとめてありません

★いまは、★1つもまとめていません。★突き合わせると、こうなります。

| 名前 | 実装ずみ | 取り込む62点 | 備考 |
|---|---|---|---|
| ニット帽 | `hatKnit` | `hats_02` | ★完全に同名 |
| 麦わら帽子 | `hatStraw` | `hats_06` | ★完全に同名 |
| スニーカー | `shoesSneakers` | `shoes_01` | ★完全に同名 |
| ベレー | `hatBeret`（ベレー帽） | `hats_03`（ベレー） | ★ほぼ同じ |
| マフラー | `scarfCheck`（チェックのマフラー） | `neck_01`（マフラー） | ★`neck_02` チェックのマフラーは62点に入っていません |
| カルメン | `operaCarmen2` | ── | ★目録に `operaCarmen` もありますが、★実装ずみは `operaCarmen2` だけです |

★★カルメンの答え：★正は **`operaCarmen2`** です。★`operaCarmen` は目録だけで、実装されていません。

★★「新しい鍵を採ってください」という §3-2 のご指示に、★1つ問題があります。

　`hat_straw`（麦わら帽子・5点）と `hat_knit`（ニット帽・30点）は、
　★古いお店で**実際に売っていた**品です。★買った方がいらっしゃいます。
　★`lib/legacyWearables.js` が、★これを新しい鍵へ引き当てています。

　★新しい鍵に切り替えるだけだと、★お持ちのものが消えます。
　★★「受け取ったものは取り上げない」に、まっすぐ当たります。

　★私の案：★古い鍵は**持ち物として残したまま**、★箱2で配るのは新しい鍵にする。
　　★お店の一覧には、★新しい鍵だけを並べます。
　　★★これなら、★誰からも何も取り上げません。
　★この形でよいか、★ご確認ください。

---

## いま止まっているところ

　・⑧の重ね順（`bottom → top → outer` と `eyes` の位置）のご確認
　・⑨の、同名のものの扱い（★古い鍵を残す案でよいか）

★この2つが決まれば、★62点の取り込みは、そのまま進められます。
