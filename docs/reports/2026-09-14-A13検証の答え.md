# A13 検証の 答え（★7項目）
全119行 / 末尾は「- 課長・学部長の 2つは、★札付きの 人が 要ります。」

★2026-09-14　★Opus の 見直しへの 答え

★★この 紙を 作ったわけ ── ★同じ お尋ねが 3度 届きました。
　★私の 答えが 中継で 落ちて います（★draft_key は 7度 求められました）。
　★★だから 紙に します。★これを そのまま お渡しください。

## ① perm-matrix　★33 → **33**

```
食い違い 33 通り（うち ★漏れ 3 通り）／ 全 120 通り
★★できことで 決めたら ── 食い違い 0 通り（うち 漏れ 0 通り）
```

★★動かない わけ。★この 道具は **台帳を 見て いません**。
　★`tools/perm-matrix.js:64` が `lib/opsShell.js` を 読みます。
　★★測って いるのは **画面の 出し分け** です。

```js
// lib/opsShell.js:71
const BY_ROLE = Object.freeze({
  owner: ["home","schedule","roster","events","threads","settings"],
  admin: ["home","schedule","roster","events","threads"],
  staff: ["schedule"],
  teacher: []
});
```

★★画面は いまも **役** で 決めて います。★A13 は 台帳だけ を 移しました。
★★0 に する には `lib/opsShell.js` を 移します。★未着手・お決め待ちです。

## ② 7番は 移された か　★**私には 分かりません**

★★`pg_policies` は、★匿名の 鍵でも 利用者の 鍵でも 読めません。

★★台帳で これを 流して ください ──

```sql
select policyname, qual, with_check from pg_policies
 where schemaname='public'
   and policyname='memberships_update_role_management';
```

★★`has_can` が 入って いたら、★戻さずに お知らせください。

## ③ 流す 前の 4つ

| | 中身 | 済んだか | 出どころ |
|---|---|---|---|
| a | `has_can` の 持ち主 ／ FORCE RLS | **済** | 坂本さん「持ち主 postgres／4表とも false」 |
| b | 条件文の 実際の 字 | **済** | 坂本さん「表名の 付かない `org_id` 形式」 |
| c | `_a13_policy_backup` | **済** | 坂本さん「12件 保存」 |
| d | 役の 名前を 包む | ★**分かりません** | 下記 |

★★d に ついて。★私の 紙には 入って います。
　★★けれど、★実際に 流されたのは **別に 受け取った SQL** です。
　★★そちらに 入って いたかを、★私は 知りません。

## ④ 第3部③の 期待値が 変わりました

★★Opus の 見込み … 8・11・12 が 出る
★★いまの 正しい 姿 … ★**0本**

★★12本 ぜんぶが 移り終えて いる からです。
★★7番だけは `role` の 字を 持ちますが、★`role_rank` の ぶんです。★正しい です。

## ⑤ 実機（★試し用の 企画で、★本当に 動かしました）

★★学校を 1つ 作り、★自分を owner として 入れました。
　★★**役職（札）は 1つも 持って いません。**

| 試した こと | 結果 | 返り |
|---|---|---|
| 自分の membership 行を 読む | ★**読めます** | `[{"role":"owner",…}]` |
| `meibo` 無しで `enrollments` に 書く | ★**拒まれます** | `42501` / HTTP 403 |
| `meibo` 無しで 招待を 作る | ★**拒まれます** | `42501` / HTTP 403 |

★★1つ目が、★6番の 変更（`meibo` → `post`）の いちばん 大事な 検査です。
　★★**自分の 枝は 生きて います。**
★★2つ目が、★Opus の 検査②です。★通りました。

★★★まだ 試せて いない 2つ
- 課長（`meibo` あり・staff）が `enrollments` を 直せるか
- 学部長が 他人の 役職を 変えられないか

★★どちらも **別の 利用者と 札** が 要ります。
　★★私の 鍵では、★他人の membership を 作れません。
　　★`memberships` の INSERT は「自分を owner として 入れる」だけ です。
★★札を 付けた 利用者を ご用意 いただければ、★同じ やり方で 確かめます。

## ⑥ 6番の 危なさ（★予測）

★★`components/VocalTracker.jsx:13338` の `opsMembers` が `memberships` を 読みます。

★★`meibo` を 持ち `post` を 持たない 人は、★いま **自分の 行しか** 返りません。
　★★職員の 一覧・先生の 一覧が、★**自分 1人だけ** に なる おそれが あります。

★★これは 予測です。★実機で 確かめて いません（★札付きの 人が 要ります）。
★★起きたら、★**戻さずに** ご報告します。

## ⑦ 片づけの お願い

★★試しに 作った 学校が、★試し用の 企画に 残って います。

```
★A13たしかめ校　e24038f2-a450-493e-8027-9c3eb22e876a
```

★★自分では 消せません。★`memberships` の DELETE が
　`has_can(org_id,'post')` を 求めるからです。★私は 持って いません。
★★★これも、★A13 が 効いて いる 証拠では あります。

## この 紙が 見て いない こと

- 台帳の 決まりの 本文は 読めません。★②は お尋ねする ほか ありません。
- 実機の 検査は **試し用の 企画**です。★本番では ありません。
- 課長・学部長の 2つは、★札付きの 人が 要ります。
