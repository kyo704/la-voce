# ★書き出す（校務システムへ）── ★出す 列の 一覧（ご確認 ねがいます）

★2026-09-20 ／ ★出どころ 見本 `stExport` の `EXP_COLS`
★台帳の 列は `tools/ask_ledger.py` で 1つずつ 確かめました。

---

## ★いちばん 大事な こと

    ★★★健康に かかわる ものは、★**選択肢に ありません**（★見本の 字の とおり）。
      ★声の 記録・からだの こと・ノート・レパートリー …… ★1列も 出しません。
    ★★★順位・偏差値・平均との 差も 出しません（★裁定 その105 §Q1）。
    ★★保護者の メールアドレスも 出しません（★裁定 その107 ── 学校に 渡す ものでは ない）。

---

## ★① 名簿（見本 9列）

| 見本の 列 | 台帳 | 出せるか |
|---|---|---|
| 学籍番号 | ★ありません | ★出せません（★列が ありません） |
| 氏名 | `profiles.display_name` | ○ |
| 氏名カナ | ★ありません | ★出せません |
| 学部・研究科 | `org_divisions`（`memberships.division_id` の 親） | ○ |
| 学科・コース | `org_divisions`（`memberships.division_id`） | ○ |
| 学年 | `enrollments.grade_label` ／ `grade_year` | ○ |
| 担当の 先生 | `assignments`（`ended_at is null`） | ○ |
| 状態 | `enrollments.status`（在籍／休会／退会） | ○ |
| 入学年月 | `enrollments.enrolled_at` | ○ |

★★★出せない 2列（学籍番号・氏名カナ）に ついて
　★この 蔵に、★学籍番号を しまう ところが ありません。
　★★校務システムに 戻す ときは、★学籍番号が いちばん 大事な 鍵 です。
　★★★**お決めが 要ります** ── ★列を 足しますか。
　　★足すなら `enrollments.student_number`（学校ごと）が よいと 考えます。
　　★★`profiles` に 足すと、★学校を またいで 同じ 番号に なって しまいます。

---

## ★② レッスンの 日程（見本 9列）

| 見本の 列 | 台帳 | 出せるか |
|---|---|---|
| 日付 ／ 開始 ／ 終了 | `lessons.scheduled_at` ／ `duration_minutes` | ○ |
| 曜日 | ★日付から 出します | ○ |
| 先生 | `lessons.teacher_id` → `profiles.display_name` | ○ |
| 学籍番号 | ★ありません | ★出せません（上と 同じ） |
| 氏名 | `profiles.display_name` | ○ |
| 場所 | ★`lessons` に 場所の 列が ありません | ★出せません |
| 種別 | ★`lessons` に 種別の 列が ありません | ★出せません |

★★場所は、★今日 作った `org_places` に 名前が あります。
　★けれど「その レッスンが どこか」を しまう 列が `lessons` に ありません。
　★★★お決めが 要ります ── ★`lessons.place_id` を 足しますか。

---

## ★③ レッスンの 出席（見本 7列）

| 見本の 列 | 台帳 | 出せるか |
|---|---|---|
| 日付 | `lessons.scheduled_at` | ○ |
| 学籍番号 | ★ありません | ★出せません |
| 氏名 | `profiles.display_name` | ○ |
| 先生 | `lessons.teacher_id` | ○ |
| 出欠 | `lessons.attendance`（came／absent／canceled） | ○ |
| つけた人 | `lessons.attendance_by` | ○ |
| つけた時刻 | ★`lessons` に その 列が ありません | ★出せません |

---

## ★④ 行事（見本 7列）

| 見本の 列 | 台帳 | 出せるか |
|---|---|---|
| 日付 ／ 開始 ／ 終了 | `org_events.event_date` ／ `start_time` ／ `end_time` | ○ |
| 行事名 | `org_events.title` | ○ |
| 場所 | `org_events.place` | ○ |
| 対象 学年 | `org_events.target_grade_years` | ○ |
| 対象 学科 | `org_events.target_division_ids` → 名前 | ○ |

★★★行事は **7列 とも 出せます**。

---

## ★⑤ 役職と 所属（見本 5列）

| 見本の 列 | 台帳 | 出せるか |
|---|---|---|
| 氏名 | `profiles.display_name` | ○ |
| 役職 | `org_posts`（`memberships.post_id`） | ○ |
| 学部・研究科 ／ 学科・コース | `org_divisions` | ○ |
| 事務の 分野 | ★ありません | ★出せません |

---

## ★まとめ ── ★お決めを いただきたい こと 3つ

    ①学籍番号を しまう 列を 足しますか（★`enrollments.student_number`）
        ★足さない なら、★校務システムに 戻す ときに 人が 手で 突き合わせます。
        ★★氏名だけ では、★同じ お名前の 方で 取りちがえます。

    ②`lessons` に 場所（`place_id`）と 種別を 足しますか
        ★足さない なら、★日程の 書き出しは 7列（場所・種別 ぬき）に なります。

    ③氏名カナを 足しますか（★`profiles.kana`）
        ★校務システムは カナで 並べる ものが 多い です。
        ★★ご本人に 入れて いただく 欄が 要ります。

    ★★★足さずに 出す こともできます。★その ときは、
      ★出せない 列を「（この 蔵に ありません）」と 空で 出すのでは なく、
      ★★**列ごと 出しません**。★空の 列を 並べると、★取り込む 側が 迷います。

---

## ★この 紙で して いない こと

    ★まだ 1行も 作って いません。★お決めを いただいて から 作ります。
    ★「読み込む」（校務システム → うち）は、★この あと 別に お出しします。
