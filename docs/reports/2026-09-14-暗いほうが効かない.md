# 暗い ほうが 効かない わけ
全102行 / 末尾は「- 直書きの 1,079件は、★読み替えでは 変わりません。★別に 数え直しが 要ります。」

★出どころ 2026-09-14、★坂本さん「端末は いつも 暗い ほうなのに、明るい ままです」

## 答え

★★**この アプリには、★暗い ほうの しくみが 1つも ありません。**

| 探した もの | 見つかった 数 |
|---|---|
| `data-th` | **0** |
| `prefers-color-scheme` | **0** |
| `color-scheme:` | **0** |
| `darkMode`（Tailwind の 設定） | **0** |
| `dark:` の クラス | **0** |
| `matchMedia` で 明暗を 見る | **0** |
| `theme-color` | **0** |

★★285の ファイルを 見ました（★`app` `components` `lib` `public` と 設定）。
　★`matchMedia` は 9件 ありますが、★どれも **動きを 減らす 設定**
　（`prefers-reduced-motion`）です。★明暗とは 別の 話です。

## だから、★次の 3つが すべて 言えます

- ★① 端末の 設定を **読んで いません**。★暗く しても 伝わりません。
- ★② 切り替える 入口も **ありません**。「もっと」の 10行に 明暗は 無し。
- ★③ 暗い ほうの 色じたい、★**アプリには 入って いません**。

## ★No.013 の 直しに ついて

★★きょう 直した `--enji-bg`（★38か所 → 87か所）は、
　★**見本の HTML の 中だけ**の 話です。
★★アプリの 画面には、★まだ 一度も 出て いません。
　★★実機で 確かめる ことも、★いまは できません。

★★だから 控え ⑮（★残り 5色の 役分け）は、
　★**暗い ほうを 作ってから** で よい、と 考えます。
　★★順番が 逆でした。★色を そろえても、★出る 場所が ありません。

## 見本は どう して いるか（★写すなら）

★★3つを めぐる 1つの ぼたんです。

```js
var THS=[['auto','端末に合わせる','◐'],['light','明るい','☀'],['dark','暗い','☾']];
function setTh(i){
  var v=THS[i][0];
  if(v==='auto') document.documentElement.removeAttribute('data-th');
  else           document.documentElement.setAttribute('data-th',v);
}
```

★★「端末に合わせる」は、★札を **外す** ことで やって います。
　★そのとき この 決まりが 効きます ──

```css
@media (prefers-color-scheme:dark){
  html:not([data-th="light"]):not([data-th="dark"]){color-scheme:dark}
}
```

★★暗い ほうの 決まりは、★見本 1本に つき 66〜74行 あります。

## ★作るなら、★どれくらいの 仕事か

★★ここが 難しい ところです。

| | 数 |
|---|---|
| 画面が `C.○○` を 使って いる ところ | **3,285** |
| 直に `#XXXXXX` と 書いて ある ところ | **1,079** |

★★この アプリは、★色を **その場の style** で 当てて います。
　★CSS の 変数では ありません。
★★見本は CSS の 変数（`var(--enji)`）なので、
　★`data-th` を 立てるだけで 全部 変わります。
★★アプリは そう なって いません。
　★★`C` を 読み替える しくみを 先に 作らないと、
　　★3,285か所を 直す ことに なります。

## お決めを お願いします

- ㋐ **`C` を 読み替える しくみを 作る**
  ★`lib/tokens.js` の `C` を、★CSS の 変数を 指す ものに 変えます。
  ★★1か所で 済みます。★ただし 出る 色が 全部 変わる 変更です。
  ★★38人の 画面に かかります。★門で 囲って、★まず 坂本さんだけに。
- ㋑ **先に 入口だけ 作る**
  ★「もっと」に 明暗の 行を 足し、★`data-th` を 立てます。
  ★★立てても 何も 変わりません。★意味が ありません。
- ㋒ **いまは やらない**
  ★控えに 積み、★9月15日の あとに まわします。

★★私の 見立ては ㋒ です。
　★★㋐は 大きな 変更です。★9月15日の 前に 触る ものでは ありません。
　★★そして 控え ⑮（5色の 役分け）も、★これと 一緒に 動かす べきです。

## この 数えが 見て いない こと

- 字の 並びだけ を 見ます。★端末で 開いて いません。
- `C` を 読み替えた ときに、★どこが 崩れるかは 見て いません。
- 直書きの 1,079件は、★読み替えでは 変わりません。★別に 数え直しが 要ります。
