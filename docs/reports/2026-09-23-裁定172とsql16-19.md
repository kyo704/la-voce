# 裁定172 と sql/16〜19 ── 試しの 台帳で 通しました（2026-09-23）

## 一 ── sql/16（★裁定172）…… **9 / 9**

★道具 …… `tools/close_org_prove.js`

| | 見た こと | 結果 |
|---|---|---|
| ① | 行事・役職・場所・ご請求・名簿・在籍 を 持つ 学校を 作る | ○ |
| ② | 画面と 同じ 順で 閉じる | ○ 最後まで |
| ③ | 学校が 消えて いる | ○ |
| ④ | ぶら下がって いた ものも 消えて いる | ○ 6つとも 0 |
| ⑤ | `ops_audit_log` が 増えて いる | ○ 30 → 36 |
| ⑥ | **門下を 開いた 記録が 残って いる** | ○ 3行 |
| ⑦ | そのときの 学校の 名前が 読める（`org_name_at`） | ○ 3行 |
| ⑧ | 学校の id は 空（★連鎖で 消えて いない） | ○ 1行 |

★★★⑧が 大事な ところ です。★行が **残って いて**、★`org_id` だけ 空に なって います。
　★束0（FX1）で 人に した ことと、★同じ 形 です。

### 外部キーの 姿（★当てた あとに 読みました）

```
monka_read_log.org_id      → SET NULL   （★前は CASCADE）
score_log.org_id           → SET NULL   （★同じ）
org_billing_log.org_id     → SET NULL   （★同じ）
export_log.org_id          → SET NULL   （★同じ）
contract_owner_log.org_id  → SET NULL   （★同じ）
```

★`org_name_at` の 列 …… 6つ の 記録の 表 ぜんぶ に 入りました。

---

## 二 ── sql/17・18・19 …… ★流れました

| | 中身 | できた もの |
|---|---|---|
| 17 | 退会の NOT NULL の ちぐはぐを 直す | ― |
| 18 | 公演の 部屋・集合・段 | `koen_rooms` / `koen_room_members` / `koen_calls` |
| 19 | 型の 鍵 と 年度 | `portfolios` の 縛り（`web_type` `t01`〜`t15` ／ `paper_type` `T1`〜`T6` ／ `field` 4つ） |

★★4本 とも「何度 流しても 同じ」形 です（★道具で 確かめました）。

---

## 三 ── ★つまずいた ところ（★私の 試しの 行 でした）

★sql/19 が 1度 当てられません でした。

```
ERROR: 23514 check constraint "portfolios_paper_type_check" of relation "portfolios"
       is violated by some row
```

★わけ …… ★私が sql/11 の 確かめの ときに、★`paper_type = 'kami-01'` を 入れて いました。
★★sql/19 は 紙の 型を `T1`〜`T6` に 縛ります。★`kami-01` は 入って いません。

★★★**Opus の SQL の 誤りでは ありません。**★私の 試しの 行が 当たって いました。

★直した こと ──
　★試しの 行を 消しました。
　★`tools/sql1011_prove.js` の 字を、★台帳の 縛りに 合わせました
　　（`kata-a` → `t01` ／ `seigaku` → `voice` ／ `kami-01` → `T1`）。
　★走らせ直して **16 / 16**。

★★★本番の `portfolios` に、★`paper_type` や `web_type` が 入って いる 行は **0行** です。
　★★本番で 同じ ことは 起きません。

---

## 四 ── sql/14 が また 変わって いました（★読みました）

```
+ -- ★学校ごと消えるとき（organizations が既に無い DELETE）は残さない。
+ --   1つの学校を閉じるだけで数百〜数千行になり、読めない記録になるため
+ if tg_op = 'DELETE' and v_org is not null
+    and not exists (select 1 from public.organizations o where o.id = v_org) then
+   return old;
+ end if;
```

★★学校を 閉じた ときの 何百行を **書かない** ように なりました。

★★★けれど、★本番に 入って いるのは **1つ 前の 版** です（★きょう 当てた もの）。
　★この 直しは まだ 当てて いません。
　★★試しには sql/16〜19 と 一緒に 入って いません（★14 は 流して いません）。
　★どう するか、★お指図を お待ちします。
