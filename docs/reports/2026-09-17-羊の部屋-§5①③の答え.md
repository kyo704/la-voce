# ★§5 ① と ③ の 答え

全101行 / 末尾は「★見積もりに 進めます。★§8（★総ざらい）は その あと です。」

★出どころ `docs/opus/裁定その71-羊の部屋の座標（2026-09-17）.md` §5。

## ★③ を 先に（★裁定の お指図 ──「まず ③ を 見て ください」）

### 答え ★★「置く 最中だけ 外れる」── **これが 犯人です**

`components/VocalTracker.jsx:17959`

```
cameraOn={layoutV2 && homeState !== DRESS}
```

`lib/roomCamera.js`

```
export const ZOOM = 1.6;
…
if (!opt.on || opt.editMode || z <= 1) return { x: 0, y: 0, z: 1 };
```

| 画面 | `cameraOn` | ★k（倍率）|
|---|---|---|
| ながめる（見る）| true | **1.6** |
| したく（置く）| false | **1.0** |

★★引き出しを **開く**と `homeState === DRESS` に なり、★k が **1.6 → 1.0**。
★★**閉じる**と **1.0 → 1.6** に 戻ります。

### ★これは 読んだ だけ では ありません。★測って います

★実機（本番・iPhone 12・2026-09-17）── ★同じ 絵の 幅を 2つの 画面で 測りました。

★12個 とも、★ながめるが したくの **1.600倍** でした。
★★`k` が 2つ ある ことの、★実物の 証し です。

### ★★だから「3日間の 犯人」です

★置く ときは 等倍。★見る ときは 1.6倍。
★★置いた ところは 部屋の 中で 動いて いません（★床を ものさしに して 0.01％ 未満）。
★★けれど **見る ときだけ 寄る** ので、★寄りの 外に 置いた ものが 画面に 入りません。

- 壁の もの … 中心より **上** → ★上へ 逃げる
- 手前の もの … 中心より **下** → ★下へ 逃げる（★116％・★画面の 外）
- 床の 家具（54％前後）… ★中心の すぐ 近く → ★ほとんど 動かない

★★坂本さんの おっしゃった 2つの 症状と、★そのまま 合います。

## ★① 置く画面と 見る画面は、★同じ 描画を 通って いるか

### 答え ★★**関数の 名は 同じ。★引数が ちがう。**

★★裁定の 字 ──「★同じ 名前でも、★**引数が 違えば 別の 式です**」。
★★こちら です。

★通る 道は、★2つの 画面で **1本** です ──

```
VocalTracker
  └ CharacterHome（roomOnly）
      └ RoomScene
          ├ cameraStyle(cameraOf(…))   ← カメラ
          └ stageStyle(…)              ← 舞台
```

★★分かれるのは、★**渡す 引数 2つ**だけ です。

| 引数 | ながめる | したく | 効く ところ |
|---|---|---|---|
| `cameraOn` | true | false | ★k が 1.6 か 1.0 か（★③ の 正体）|
| `fullBleed` | true | false | 舞台の 大きさを `stageFit` で 出すか `stageSize` で 出すか |

★`VocalTracker.jsx:17959-17960` の 2行 です。

### ★`fullBleed` の ほうは、★いまの 端末では 効いて いません

★実測 ── ★舞台は 2つの 画面で **同じ** でした（390×278.56・比 1.4）。
★★箱の 比が たまたま どちらも 7:5 を またいで いる ため、
★`stageFit` と `stageSize` が 同じ 値を 返して います。

★★**けれど 式は 2本 です。** ★比の ちがう 端末（★iPad よこ など）では 分かれます。
★★§2 を 当てる とき、★ここも 1本に して ください。

## ★まとめ

```
① 同じ 道・ちがう 引数  → ★別の 式。★§2 を 当てる
② 部屋の 割合で 保存    → ★○（★移し替え 不要・答え済み）
③ 置く 最中だけ 外れる  → ★★これが 犯人
```

★★①と③は、★**同じ 1つの こと**を 別の 側から 見た もの です ──
★`cameraOn` が 画面ごとに ちがう。★それだけ です。

## ★次

★★§2（★同じ camera 関数のまま、★原点 `ox`/`oy` だけ 動かせる ように する）の
★見積もりに 進めます。★§8（★総ざらい）は その あと です。
