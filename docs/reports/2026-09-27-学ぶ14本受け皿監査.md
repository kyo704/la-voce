# ★学ぶ 14本 の 受け皿 ── ★書く 前に 見ました

★この 紙は `tools/learn14_intake_audit.py` が 書きました。★手で 足して いません。
★見た 日 …… 2026-09-27

★★**見るだけ です。★コードは 1行も 書いて いません。**

## ★一 題と 章立ての もと は どこか

| 紙 | 何本 | 本文の 鍵 | 本文が 入って いる 数 |
|---|---|---|---|
| `docs/learn-content/articles.json` | 66 | `body` | 66 |
| `docs/learn-content/shobai.json` | 14 | `bodyMd` | 14 |
| `lib/learnContent.js`（★アプリが 読む もの） | 88 | `bodyMd`／`lead` | ★空 0 |

★★★生きて いるのは `lib/learnContent.js` の `ARTICLES` **1つ だけ** です。
★`components/VocalTracker.jsx` が 読みます。★`docs/learn-content/*.json` は **もと** で、
★`scripts/build-learn-content.js` が id で 突き合わせて 移します。

★★★鍵が 2つ ある こと に 気を つけて ください ── `articles.json` は `body`、`shobai.json` は `bodyMd`。
★片方 だけ で 数えると、★書いて ある 14本が「空」に 見えます（★2026-09-25 の 一件）。

★章立て …… `docs/learn-content/manifest.json` に 7 章（この仕事の声／何が声を削るか／アプリの数字の読み方／整える／守る／続くときは／用語）。
★★`ARTICLES` の `chapter` は 1〜9 まで あります（★商いの 8・9 を 含みます）。
★お仕事の 別 …… classical-musical／announcer／voice-actor／pops-rock（`manifest.json` の `tabs`）。

## ★二 見本『もっと深く』の 15題 ── ★いま 何本 ある か

| 節 | 見本の 題 | 実装に ある か |
|---|---|---|
| 技術 | ベルカント発声法の 全体像 | ★無い |
| 技術 | 声区と パッサッジョ ── 正解表は 存在しない | ○ `声区と パッサッジョ ── 通過点で 何が起きているか` |
| 技術 | appoggio ── 支えの 作り方と 検算 | ★無い |
| 技術 | 母音修正（aggiustamento） | ★無い |
| 技術 | singer | ★無い |
| 技術 | SOVT ── ウォームアップ | ○ `SOVTE ── ストロー発声で 声を軽く戻す` |
| からだ | 歌手のための 体幹 | ★無い |
| からだ | 姿勢ワーク | ★無い |
| からだ | 呼吸筋トレーニング | ★無い |
| からだ | 栄養・加湿・睡眠 | ★無い |
| 健康 | vocal dose | ★無い |
| 健康 | 喉頭外筋の 緊張と MCT | ★無い |
| 健康 | 逆流（LPR / GERD）の 長期管理 | ○ `逆流性食道炎・咽喉頭逆流症（LPR）と 声` |
| ことば | アジリタ・装飾・カデンツァ | ★無い |
| ことば | 用語集 | ○ `用語集` |

★★15題 中 **4本** が 実装に あります。★残り **11本** が ありません。
★★★裁定211 の「14本」は、★この 残り と 数が 近い です（★11）。★同じ ものか どうかはこちらでは 決められません。

## ★三 いちばん 大きい 穴 ── ★出す 画面が ありません

- ★`lib/moreBundles.js` に 行は あります …… `{ to: "もっと深く", name: "もっと 深く", sub: "声楽・ミュージカルの ことば", readyAt: null }`
- ★★`readyAt` が `null` の 行は **出しません**（★`lib/moreBundles.js` の 頭の 決め／`components/MoreBundle.jsx`）。★だから いまは 入口が ありません。
- ★★★記事を 14本 書いても、★それだけでは 誰も 読めません。★要る のは 3つ ──
  ①『もっと深く』の 画面 ②`readyAt` に 日付 ③記事を `ARTICLES` に 足す（★章の 割り当て）。
- ★`ARTICLES` は「1件 足すだけ」で 増える 形 です（★`lib/learnContent.js` の 頭の 約束）。
  ★★**記事の 受け入れ そのものは できて います**。★足りないのは 出口 です。

## ★四 『まだ 書いて いません』の 1行 ── ★仕組みは ありません

★★★註（コメント）を 落として から 数えます。★落とさないと、★「近日公開と 書かない」と いう **禁じ手の 註** が、★そのまま『書いて ある』に なります。

| 探した 字 | ★人に 出る ところ（註を 落とした あと） | ★註 だけ の 紙 |
|---|---|---|
| 「まだ 書いて いません」 | ★ありません | — |
| 「まだ書いていません」 | ★ありません | — |
| 「準備中」 | `app/login/page.js` | `components/Tsutaeru.jsx`／`components/VocalTracker.jsx`／`lib/tsutaeru.js` |
| 「近日公開」 | ★ありません | `components/KoenMine.jsx`／`components/LessonRoundArea.jsx`／`components/VocalTracker.jsx`／`lib/entitlements.js`／`lib/moreMenu.js` |
| 「未執筆」 | ★ありません | — |

★★`app/login/page.js` の「開演準備中…」は **待って いる あいだ の 札** です（★`btnLoading`）。★『近日公開』の たぐいでは ありません。

★★★いまの 決めは **逆** です ── ★行き先の 無い 行は **出しません**。
★★裁定176 §3 が こう 書いて います ── 「近日公開」と見せる（★隠すとは、存在を見せないこと。期待を作らない）」
★★`components/Tsutaeru.jsx` にも「★『準備中』も 書きません（★裁定176 §3）」と あります。

★★★だから 58本を 1行で 出す には、★**先に 裁定が 要ります**。★こちらでは 決めません。

★もし 出す と 決まった とき に 要る もの（★いまは 1つも ありません）──

1. ★**未執筆の 題の 一覧**。★いま 題は `ARTICLES` に しか なく、★書いて いない 記事の 題は **どこにも ありません**。
2. ★その 一覧と `ARTICLES` の 差を 出す 関数（★1か所 だけ）。
3. ★1行の 字（★9言語。★`lib/t.js` の `tx()` を 通す こと）。
4. ★見張り 1本（★裁定176 §3 と ぶつからない こと を 確かめる もの）。

## ★五 この 紙で 言えない こと

- ★**「58本」の 出どころが この 蔵に ありません**。★学ぶ の 文脈で その 数を 見つけられません（★見本『もっと深く』は 15題、★`ARTICLES` は 88本、★`articles.json` は 66本）。★数の もとを お知らせください。
- ★裁定211 の 本文を 読んで いません（★この 蔵に ありません）。★「14本」が 上の 残り 11本 と 同じ かは 確かめられません。
- ★題の 突き合わせは 字の 一致 です。★言い回しが 変われば 外れます。

