# ★受診プレビュー ── ★24件の 中身

★2026-09-25。★見本 `SC['受診プレビュー']`（★さわれる・全画面）と
　★`components/VocalTracker.jsx` の `activeTab === "clinicSummary"`（★26830行〜）。

## ★★いちばん 大事な こと ── ★お約束が 紙に 乗って いません

★★この 画面には、★印刷の ときに 隠す 決めが 1行 あります ──
```
  26836行  @media print { header, nav, .no-print { display: none !important; } }
  26837行  <div className="rounded-2xl p-4 border no-print">
```
★★`no-print` の 中に、★題（受診用サマリー）と、★「独自の指標は含めず」の 一文と、
　★選ぶ ところ（`CLINIC_NOTICE`・`CLINIC_FOOT`）が 入って います。
　★★つまり **選ぶ 人には 見えて、渡す 相手には 見えません**。

★★紙（`id="clinic-summary-content"`・26934行〜）に 残る 文は、★1つ だけ です ──
```
  26936行  本書はご本人の自己申告に基づく記録です。医学的な診断や検査結果ではありません。
```
★★見本の 紙には、★これが 載って います（★`この紙について` と `.note`）──
```
  本人が 毎日 つけた 記録から 作りました。診断では ありません。
  点数・尺度・他人との くらべは 使っていません。実数だけです。
  1マス＝1日。点数では ありません（3段の 記録を そのまま 並べたものです）
  病名では ありません。本人が 選んだ ことばです。
  数えただけです。原因かどうかは 分かりません。検定は かけていません。
  ・質問票（尺度）の 点数は 使っていません
  ・載せる項目は 本人が 選びました。選ばなかったものは 載っていません
  ・元の記録は、本人の 端末から CSV で お出しできます
  ・アプリの中から、医療機関へ 送る道は ありません
  この紙は 要配慮個人情報です。渡す相手を、ご自分で 決めてください。
```
★★紙の 中に 字が あるか、★言葉ごとに 見ました ──
```
  診断 …… ある      点数 …… ★ない    尺度 …… ★ない
  病名 …… ★ない     検定 …… ★ない    要配慮 …… ★ない
  くらべ …… ★ない    氏名は …… ★ない
```
★★★この 紙は、★人の 手を 離れて 医療機関に 渡ります。
　★★「点数を 使って いない」「他人と くらべて いない」「診断では ない」は、
　　★この 品の いちばん 根の 決め事 です。★その 決め事が、★渡る 紙の 上に
　　★書いて ありません。★画面の 上に だけ あります。

## ★言い方の ちがい（★中身は ある）
```
  見本 1枚に して 出す ／ 記録の まとめ    ← 実装 受診用サマリー
  見本 PDFで 保存 ＋ 印刷する         ← 実装 印刷 / PDFで保存（1つ）
  見本 載せるものを 選び直す           ← 実装 上の 選ぶ ところが そのまま 出て います
  見本 本人の ことば（選んだ 3日ぶん）     ← 実装 自由記入欄
```

## ★足りて いる ところ

★★選ぶ ところは 見本の とおり です。★`lib/clinicSheet.js` が 持って います。
　★既定は 空（`CLINIC_DEFAULT = []`）。★`既往・服薬` も `自由記入欄` も `基本情報` も、
　★押さない かぎり 紙に 出ません（`isOn(clinicPick, …)`）。
　★★ここは 直す ところが ありません。★はじめに 疑いましたが、★ちがい ました。

## ★まだ 見て いない こと

★★見た目は 確かめて いません。★印刷の 見え方も 実機で 見て いません。