# もっと 画面 ── 1件ずつの 内訳
全178行 / 末尾は「★★字が あるか だけ です。★実機で お確かめ ください。」

★2026-09-15 ／ ★見本 `SC['もっと']`
★この 文は tools/motto_breakdown.py が 書き出しました。

## ★はじめに ── 2つ、つまずきました

| | つまずき | 直した こと |
|---|---|---|
| ① | 見本が `function` では なく `SC['もっと']` だった | ★道具を `SC[]` の 形にも 対応させました |
| ② | `lib/moreMenu.js` を 読む 一覧に 入れて いなかった | ★入れました。★8件 → **5件** |

★★②は、★A05 の `LookBackPanel.jsx` と **まったく 同じ 形** です。
　★★「★無い ファイルを 入れる」は、★止められる ように しました。
　★★「★**在る** ファイルを 落とす」は、★まだ 止められません。

---

## ㋐　「毎日、聞いてほしいこと」の 行の 右

**見本の値**

> `<s>5つまで ›</s>` ── ★**数と 矢印の 両方**

**いまの値**

> `lib/moreMenu.js:45` `right: "5つまで"`
> `components/VocalTracker.jsx` `<Li right={r.right || "›"}>`
> ★★`right` が あると、★**矢印が 置きかわります**。★足されません。
> ★画面に 出るのは「5つまで」だけ。★`›` が 消えます。

**verdict** … ★ちがう

★★ほかの 行は すべて `›` が 出ます。★この 行 **だけ** 出ません。
　★★「押すと 何か 開く」印が、★この 行から だけ 落ちて います。
★★見本は `<s>` 1つに 2つを 入れて います。★`right` と `›` は 別 物 では ありません。

---

## ㋑　「通っている ところ」の 行

**見本の値**

> `<div class="li" onclick="push('通っているところ')">`
> `<span>通っている ところ</span><s>` + ORGS.length + `つ ›</s>`

**いまの値**

> ★**ありません。** `lib/moreMenu.js` に 行が ありません。
> ★「通っている」は、★倉庫の どこにも 出て きません（★0件）。

**verdict** … ★★足りない

★★「教室」の 組に ある のは `運営モード` **だけ** です（:64）。
　★★これは **運営に 入る** 入口 です。
　★★見本の「通っている ところ」は、★**どの 教室に 在籍して いるか**の 一覧 です。
　★別の もの です。★役職の 無い 生徒には、★いま 教室の 行が 1つも 出ません。

---

## ㋒　下の 注記（1行目・2行目）

**見本の値**

> 法律の 行き先を、この 1か所に 集めています。
> 退会を 隠しません。引き止めを 2回以上 出しません。

**いまの値**

> ★**ありません。** ★注記そのものが ありません。

**verdict** … ★★足りない

★★この 2行は、★**約束** です。★飾りでは ありません。
　★「法律の 行き先を 1か所に」＝ ★探させない、という 約束。
　★「引き止めを 2回以上 出しません」＝ ★退会の 邪魔を しない、という 約束。
★★書いて いない 約束は、★守って いても 伝わりません。

---

## ㋓　下の 注記（3行目）

**見本の値**

> この画面の 題は「もっと」です（規約が この語で 書かれています）。

**いまの値**

> ★**画面には ありません。**
> ★同じ 意味の 文は、★`VocalTracker.jsx:21803` と `lib/moreMenu.js:13` の
> ★★**コメント**に あります。★出ません。

**verdict** … ★★足りない

★★これを 1度「★アプリに あります」と 数えました。★誤り です。
　★★コメントを 外して いません でした。★この 倉庫の 持病 です。
　★`components/tests/_source.js` が、★同じ 罠で 2度 作られて います。
★★道具を 直しました。★③の 検算は、★コメントを 外して 見ます。

---

## ㋔　下の 注記（4行目）

**見本の値**

> 運営モードは、**役割の ある方にだけ** 出ます。

**いまの値**

> ★**ありません。**
> ★出し分けは して います（`hasOrgRole` ／ `mayEnterOps`）。★字が ありません。

**verdict** … ★★足りない

★★して いる ことを、★言って いません。
　★★役職の 無い 方は「★なぜ 自分には 出ないのか」が 分かりません。

---

## ③ 拾い残し 4件の 仕分け

★★`。` で 切った 断片 なので、★見本の 1文 とは 限りません。

| # | 断片 | アプリに | 仕分け |
|---|---|---|---|
| 1 | この画面の 題は「もっと」です。 | ★★アプリに ありません | ★㋓ と 同じ もの です。★1件です。 |
| 2 | 毎日、聞いてほしいこと5つまで。 | ★★アプリに ありません | ★★**取り違え** です。★見本では 行の 字（`毎日、聞いてほしいこと`）と |
| 3 | 規約が この語で 書かれています。 | ★★アプリに ありません | ★㋓ の 括弧の 中 です。★1件です。 |
| 4 | 運営モードは、役割の ある方にだけ 出ます。 | ★★アプリに ありません | ★㋔ と 同じ もの です。★1件です。 |

**1. この画面の 題は「もっと」です。**

★㋓ と 同じ もの です。★1件です。

**2. 毎日、聞いてほしいこと5つまで。**

★★**取り違え** です。★見本では 行の 字（`毎日、聞いてほしいこと`）と
　★右の 字（`5つまで ›`）が **別の 札**です。★`。`で 切った ときに くっつきました。
　★★どちらも アプリに あります。★差分では ありません（★㋐ の 矢印を 除く）。

**3. 規約が この語で 書かれています。**

★㋓ の 括弧の 中 です。★1件です。
　★★はじめ「あります」と 出ました。★コメントを 数えて いました。

**4. 運営モードは、役割の ある方にだけ 出ます。**

★㋔ と 同じ もの です。★1件です。

★★まとめ ── ★4件の うち **3件**は、★上の ㋓㋔ と 同じ ものです。
　★★残る 1件（2番）は、★`。`で 切った ことに よる **取り違え** です。
　★★つまり、★③から 新しい 差分は **出ません** でした。

---

## ★見本に 無いのに、★アプリに ある もの

★★これは「差分」の もう 片側 です。★数え落とさない ため に 書きます。

| 行 | 見本に | |
|---|---|---|
| `もっているもの` | ★**あります**（7件） | ★見本では `⚙ → もっているもの`。★置き場所の ちがい |
| `ここから区切りをつける` | ★★**0件** | ★アプリだけ の 行 |
| 組の 見出し `じぶんの記録` `教室` | ★★見本の もっと には ありません | ★アプリだけ |

★★`ここから区切りをつける` と 組の 見出しは、★**お裁きが 要ります**。
　★消すのか、★見本に 足すのか、★このまま 置くのか。

## ★この 数えが 見て いない こと

| | |
|---|---|
| 色・大きさ・並び・余白 | ★見て いません |
| 行の **順番** | ★見て いません（★見本と アプリで ちがう 可能性） |
| 押した 先の 画面 | ★見て いません |

★★字が あるか だけ です。★実機で お確かめ ください。
