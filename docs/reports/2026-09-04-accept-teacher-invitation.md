# accept_teacher_invitation / create_org_event — 実装と検査（2026-09-04）
全82行 / 末尾は「★招待は11件すべて期限切れです（2026-09-03 03:52:03 UTC）。試すには新規発行が要ります。」

## 結論

★画面側の差し替えは完了し、commit `adcccdf` で push 済みです。
★検査120本すべて通過、lint 0件、ビルド通過。
★`teacher_student_links` と `org_events` への直接の insert は、コードに0件です。

## ★関数名について（重要）

ご指定は `accept_invitation` でしたが、★**本番に入っているのは
`accept_teacher_invitation`** です。実装はそちらで行いました。
★`accept_invitation` で呼ぶと `function does not exist` になります。

## 差し替えた場所

| 行 | 何を |
|---|---|
| `components/VocalTracker.jsx:9568` | `.rpc("accept_teacher_invitation", { p_code: pendingInvitation.code })` |
| `components/VocalTracker.jsx:10037` | `.rpc("create_org_event", { p_org_id, p_event_date, p_kind, p_title })` |

## 消した処理（★関数の中で行うため）

| 旧の行 | 何を | なぜ消したか |
|---|---|---|
| 9556 | `teacher_student_links` の insert | ★権限を外したため、もう通りません |
| 9589 | `link_consents` の insert | ★片方だけ成功する状態を作らないため |
| 9592 | `teacher_invitations` の `used_at` 更新 | ★0行に当たっていました（#004）。無音の失敗でした |
| 10019 | `org_events` の insert | ★権限を外したため |

## エラーの対応づけ

| 関数が上げるもの | 画面に出る文 |
|---|---|
| `MINOR_NOT_ALLOWED` | いまはまだ、先生とつながることができません。保護者の方の確認の仕組みを準備しています。 |
| `ALREADY_LINKED` | この先生とは、すでにつながっています。 |
| `INVITATION_NOT_USABLE` | このコードは使えません。先生に確認してください。 |
| それ以外 | 連携に失敗しました。もう一度お試しください。 |

★`INVITATION_NOT_USABLE` は「無い・使用済み・期限切れ」を分けません。
★分けると、コードの当たりはずれを調べる道具になります。
★`isMinorLinkBlocked` も残しました。トリガーが直に例外を上げる経路に備えてです。

`create_org_event` は、権限が無いとき★例外ではなく `null` を返します。
★例外にすると、教室の uuid の当たりはずれを調べる道具になります。
画面には「この教室に予定を作る権限がありません。」と出します。

## 検査

| | 結果 |
|---|---|
| 検査ファイル | 120本 |
| 失敗 | ★0 |
| lint | ★エラー0件 |
| ビルド | ★通過 |
| 直接の insert（2表） | ★0件 |

★検査を1本、守りの移った先に合わせました。
`teacher-link-reconnect.test.js` が「23505 を画面で見分けているか」を見ていましたが、
★いまは関数が `ALREADY_LINKED` を返します。★見張る中身は同じです
――「重複を、読める言葉にしているか」。重複時の文が残っていることも見ます。

## ★これで確かめられたことの範囲

★検査が通ったことは「壊れていない」の一部でしかありません。
★この検査群は本番に接続しません。関数が実際に動くかは、実機でしか分かりません。

★配信後に、必ず実機で確かめてください。

| 試すこと | 期待 |
|---|---|
| 新しい招待コードを1件発行し、生徒として入れる | つながりができ、★招待が「使用済み」になること |
| 期限切れのコードを入れる | 「このコードは使えません。先生に確認してください。」 |
| 同じ先生にもう一度つなぐ | 「この先生とは、すでにつながっています。」 |
| 教室の責任者として予定を作る | 作れること |
| 責任者でない人が予定を作る | 「この教室に予定を作る権限がありません。」 |

★1行目の後半を必ず見てください。★`used_at` が立つかどうかが、
#004 が本当に直ったかの唯一の確認です。

★招待は11件すべて期限切れです（2026-09-03 03:52:03 UTC）。試すには新規発行が要ります。
