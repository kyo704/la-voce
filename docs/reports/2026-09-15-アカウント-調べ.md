# 「アカウント」── 調べ（★実装して いません）
全135行 / 末尾は「| もっと ＞ アカウント の 欄の 中身 | ★字の 数だけ。★動かして いません |」

★2026-09-15 ／ ★見本 `SH['account']` ／ ★もっとの 先 3枚目
★この 文は tools/account_audit.py が 書き出しました。

★★見本は **引き出し**（`SH[]`）です。★1枚の 画面では ありません。
　★★道具は `function` と `SC[]` しか 読めません でした。
　★★2026-09-15、★`SH[]` にも 対応させました。★見本の 引き出しは 22枚 あります。

## 数え

```
見本の 字 5語 ／ ★足りない 1語
```

| 見本 | いま | verdict |
|---|---|---|
| メールアドレス | ★あります | ○ |
| パスワードを 変える | ★あります | ○ |
| 端末を 見る**（2台）** | ★「端末を 見る」（★**数が ありません**） | ★ちがう |
| ログアウト | ★あります | ○ |
| アカウント（題） | ★あります | ○ |

★★字の 差は **1つ だけ** です。★数（2台）が 出ません。

---

## ★★けれど ── ★4行の うち **2行が 押せません**

★`components/VocalTracker.jsx:22934` の 中を 読みました。

| 行 | 押せるか | 行き先 |
|---|---|---|
| メールアドレス | ★★**押せません** | ★ただの 字を 返して います |
| パスワードを 変える | ★押せます | `openAccountAction("password")` |
| 端末を 見る | ★★**押せません** | ★同上 |
| ログアウト | ★押せます | `handleSignOut` |

★★わけは、★その場に 書いて あります ──

> ★★「パスワードを 変える」だけ、いま 押せます。
> ★★「メールアドレス」「端末を 見る」は、まだ ここから 開けません。
> 　★★「メールアドレス」は、もっと ＞ アカウント の 欄に すでに あります。
> 　★★「端末を 見る」は、台帳（API）が まだ 無いため、押せません。

★★これは **押せない 札**（★§8⑤）です。★4行 中 2行 ── **半分** です。
　★★A2 の 33件に、★Opus が 同じ 名前を 付けて います。

---

## ★★もう 1つ ── メールアドレスの 欄に、★**たどり着けません**

★「メールアドレスは もっと ＞ アカウント の 欄に すでに あります」と 書いて あります。
★★その 欄が どこに あるかを 追いました。

```
inMore("アカウント") の 節 ── 3つ
  :22480  メールアドレス
  :22531  パスワード
  :22588  復旧の番号（控え）
```

★★この 節が 出るのは、★`moreSection === "アカウント"` の ときだけ です。
★★では、★誰が そう しますか ──

```
setMoreSection("アカウント") を 呼ぶ ところ ── ★**1か所 だけ**
  :10412   openAccountAction() の 中
```

★★そして `openAccountAction` を 呼ぶ ところは ──

```
openAccountAction("password")   :22944   ★**これ 1つ だけ**
```

### ★★つまり

| 出したい もの | 行き方 |
|---|---|
| パスワードを 変える | ★もっと → アカウント → パスワードを 変える |
| ★**メールアドレス** | ★★もっと → アカウント → **パスワードを 変える** → 同じ 1枚の 中に ある |
| ★**復旧の番号** | ★★同上 |

★★メールアドレスを 変えたい 方は、★**「パスワードを 変える」を 押さないと**
　★その 欄に たどり着けません。
★★引き出しの「メールアドレス」の 行を 押しても、★何も 起きません。

★★★「無い」のでは ありません。★**届かない** のです。
　★★`withdrawConsent`（★けさ 直した もの）と、★同じ 形 です。

---

## ★古い もの・死んだ ところ（★数えました）

| | 状態 |
|---|---|
| `openAccountAction("email")` | ★★**呼ばれて いません**（★枝が 死んで います） |
| `openAccountAction("device")` | ★★**呼ばれて いません**（★同上） |
| `devicePanel`（`useState`） | ★★`setDevicePanel(true)` は ありますが、★**読む ところが 0件** |
| 端末を 見る の API | ★`app/api/` に ありません |

★★`devicePanel` は、★**立てても 何も 描かれません**。
　★★N-1 の 決まり ──「★書いて いる 値は、必ず どこかで 読まれて いるか」。
　★★読まれて いません。

★★これは「古い 機能」では なく、★**作りかけ** です。
　★台帳（API）が 無い ことは、★その場に 書いて あります。

---

## ★お裁きを お願いしたい こと

| | こと | 私の 見立て |
|---|---|---|
| ㋐ | メールアドレスの 行を 押せる ように する | ★★**する**。★`openAccountAction("email")` は もう あります |
| ㋑ | 端末を 見る の 行を どう するか | ★★**出さない**。★台帳が 無い もの を 出しません |
| ㋒ | `devicePanel` と `openAccountAction("device")` | ★★㋑と 同時に 片づける |
| ㋓ | 「（2台）」の 数 | ★㋑を 決めて から |

★★㋐は、★きょう 直した「同意を とりけす」と **同じ 形** です。
　★仕掛けは 在る。★入口が 無い。★1行 で つながります。

★★㋑は、★§8⑤ の 話 です。★押せない 札を 置かない。
　★★ただし「消す」では なく「出さない」です ──
　★台帳が できた 日に、★また 出します。

## ★この 調べが 見て いない こと

| | |
|---|---|
| 実機での 見え方 | ★見て いません |
| 引き出しの 高さ・並び | ★見て いません |
| もっと ＞ アカウント の 欄の 中身 | ★字の 数だけ。★動かして いません |
