# ★写真の Exif を 落とす ── ★技術の 検討（★裁定199）

★2026-09-25。★まだ 作って いません。★作る 前の 検討 です。

## ★決まって いる こと（裁定199）
```
  ① Exif は 1つ 残らず 落とす（機種・撮影日時・シリアル番号も 本人特定の 手がかり）
  ② 端末と サーバの 両方で 除去
  ③ 元画像は 保持しない
  仕様 …… 長辺 1,200px ／ 20枚まで（見本・裁定145 が 正）
  肖像権の 確かめの 答えは 保存しない
```

## ★いま 台帳に ある もの（★sql/83・本番・127本目）
```
  portfolio_photos  id, user_id, path, w, h, tone, shape, sort_order,
                    exif_cleared_at, created_at
  決まり portfolio_photos_own（本人・ALL）
         portfolio_photos_read（★exif_cleared_at が null の 写真は 他人に 出ない）
  関数   assert_photo_limit（20枚）／mark_exif_cleared／photos_without_exif_clear
  path の 形 …… user_id/uuid.webp（★Storage の 中）
```

★★★台帳の 側は もう 守って います ── ★印の 無い 写真は 他人に 出ません。
　★残って いるのは「★印を 立てて よいか」を 誰が どう 決めるか です。

## ★★道具の 手持ち（★package.json・2026-09-25）
```
  画像を 扱う 道具 …… ★1つも ありません
    sharp ／ jimp ／ exifr ／ piexifjs …… ★入って いません
  Storage を 使って いる ところ …… ★1つも ありません（★はじめて に なります）
```

## ★★★案 ── ★3段に します

### ★1段 端末（★落とす）

★★`<canvas>` に 描き直して 出す ── `drawImage` → `toBlob`。

★★★これは「消す」処理では ありません。★**作り直す** 処理 です。
　★canvas が 持って いるのは 点の 色 だけ です。★Exif の 入る 場所が ありません。
　★★だから 機種も 撮影日時も シリアル番号も、★写す もとが ありません。
　★★★向き（Orientation）だけは 気を つけます ──
　　★Exif を 落とすと、★横向きの 写真が 横に 倒れた まま に なります。
　　★`createImageBitmap(blob, { imageOrientation: 'from-image' })` で
　　★先に 立て直して から 描きます。★立て直した あとは もう 要りません。

★★長辺 1,200px も 同じ 一手で 済みます ── ★描く ときの 大きさ です。
　★★小さく **しか** しません（★1,200px より 小さい 写真を 引き伸ばしません）。

★★★元画像は 送りません ── ★送るのは canvas の 出した もの だけ です。
　★これで ③（元画像は 保持しない）が 守られます。★消すのでは なく、上げません。

### ★2段 サーバ（★確かめる）

★★★ここで もう一度 落とす のでは なく、★**入って いないかを 確かめて 断る** 形に します。

★★わけ ──
```
  ① 画像を 扱う 道具が ありません（sharp を 入れる 話に なります）
  ② 落とし直すと、★落とせた つもり に なれます
     ★★確かめて 断る 形なら、★通った ものは「★無い」と 言い切れます
  ③ 端末が 壊れて いても（★古い 携帯・細工した 呼び出し）、★ここで 止まります
```

★★見る ところ（★印の ある／なし だけ。★中身は 読みません）──
```
  JPEG  0xFFE1（APP1）に "Exif\0\0" ／ 0xFFE2（APP2・ICC）
        0xFFED（APP13・Photoshop IRB）／0xFFEE（APP14）／0xFFFE（COM）
  PNG   "eXIf" ／ "tEXt" ／ "iTXt" ／ "zTXt" の かたまり
  WebP  "EXIF" ／ "XMP " の かたまり
  ぜんぶ 場所は 決まって います。★中身を 読む 必要が ありません。
```

★★通ったら ── ★Storage に 置き、★`mark_exif_cleared(id)` を 呼びます。
　★★★順番が 大事 です ── ★置く → 印。★印 → 置く に すると、
　　★置くのに 失敗した とき、★印だけ 残ります。★それは 他人に 出ます。

★★断ったら ── ★その場で 消します。★Storage にも 台帳にも 残しません。
　★★字は「もう一度 お試しください」では 足りません ──
　　★なぜ 断ったかを 書きます（★「写真に 撮った ときの 情報が 入って います」）。

### ★3段 台帳（★出さない）── ★もう 入って います

★★`portfolio_photos_read` が `exif_cleared_at is not null` を 見ます。
　★★★印の 立って いない 写真は、★どんな 手違いが あっても 他人に 出ません。
　★★これが いちばん 下の 守り です。★1段・2段が 抜けても ここで 止まります。

## ★★確かめ方（★作る ときに 一緒に）
```
  ① Exif つきの 写真を 用意し、★端末を 通した あと 印が 無いことを 数える
  ② 台帳の 印を わざと 立てずに、★他人の 目で 0件 に なることを 見る
  ③ サーバに Exif つきを 直に 送り、★断られる ことを 見る（★端末を 飛ばした とき）
  ④ 向きの ある 写真（Orientation 6）が 倒れない ことを 見る
  ⑤ 1,200px より 小さい 写真が 引き伸ばされない ことを 見る
  ⑥ 21枚目が `assert_photo_limit` で 止まる ことを 見る
```

## ★★決めて いただきたい こと

★★① サーバは「確かめて 断る」で よいか（★`sharp` を 入れない 形）。
★★② 断った ときの 字 ── ★上の 案で よいか。
★★③ Storage の 入れもの（bucket）の 名と、★公開の 仕方（★署名つきの 道か 公開か）。
　★★★`portfolio_photos_read` は 台帳の 行を 守りますが、
　　★★Storage の ファイルは **別の 守り** です。
　　★入れものを 公開に すると、★道を 知って いれば 誰でも 取れます。
　　★印の 無い 写真も 取れて しまいます。★ここは 署名つきの 道が 要ると 思われます。

★★★③は 作る 前に 決めないと、★守りに 穴が 開きます。★お尋ねします。