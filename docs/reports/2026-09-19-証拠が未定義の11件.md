# ★証拠が まだ 決まって いない 約束 11件 ── ★㋐㋑の 切り分け

★2026-09-19 ／ ★出どころ `docs/ledgers/07-利用者に示している約束.md`
★数えた 道具 `tools/insp_claims.py`（★宣言は `tools/claims.json`）

★★★切り分けの 決まり（★Opus の お指図・2026-09-19）
　㋐ …… ★実装で 証明できる → ★Opus が 証拠を 定義する
　㋑ …… ★書類（38個の 空欄）が 埋まらないと 決まらない → ★坂本さん 待ち
　★★㋑を 先に 定義すると、★書いて いない 約束を 作る ことに なります。

---

## ㋐ 実装で 証明できる（6件）

| 約束 | いまの 状態（台帳07 より） | 何を 見れば よいか（★案） |
|---|---|---|
| 3. 撤回しても、書き出しと削除はできる | 書き出しと 削除は 動いて いる。撤回そのものが 無いので 組み合わせは 試せて いない | `lib/exportData.js` と `lib/accountDeletion.js` が、撤回の 状態を 見ない こと（★条件に `withdrawn` を 入れない） |
| 9. 担当と、個別のつながりは、別のもの | 守れて いる | つながりの 解除が `assignments` を 触らない こと（★`teacher_student_links` の 解除の 道に `assignments` が 出て こない） |
| 10. ★まだ終わっていないこと（サーバ側の撤回の門） | ★無い。画面側だけ（`lib/consentGate.js`） | `entries` と `cycle_periods` の INSERT/UPDATE に、撤回を 見る 門が 在るか（★いまは 0件） |
| 12. 教室を離れたら、そこから先は参照されない | 守れて いる（本番で 確認） | `can_view_ops` の 枝に「在籍が 有効」が 在る こと |
| 13. ★薄い層（`lessons` の UPDATE に WITH CHECK が 無い） | 急ぎでは ない。★欠陥では ある | `lessons` の UPDATE の 決まりに `with check` が 在るか（★いまは 2本 無し） |
| 16. 教室での呼び方は、教室が決めます | ★これから | 呼び方（`display_title`）が できことを 変えない こと（★呼び方を 見て 門に して いる ところが 0件） |

---

## ㋑ 書類・お決めが 先（5件）

| 約束 | なぜ 実装では 決められないか |
|---|---|
| 6. 15歳未満のこと | ★仕組みが ありません。年齢を 聞く 画面も、保護者の 同意の 道も ありません。★何歳から・どう 確かめるかが、書類の お決め です |
| 7. 料金 | いまは 無償。★新しい 利用規約が 未公開 です。★値段と 課金の 開始が 決まって いません（★2027年1〜3月に 延びました） |
| 8. お知らせ画面 | ★期日が 2026-09-10 の まま です。★出すか どうかの お決めが 要ります |
| 14. 症状の日に出るものは、お金の後ろに置かない | ★課金が 始まって いません。★どの カードが 有料に なるかが 決まって から でないと、線を 引けません |
| 15. 受診の案内は、いつも同じ文で出す | ★まだ 作って いません。★文そのものが 決まって いません |

---

## ★台帳07 の 状態が 古く なって いる もの 2件（★今日 台帳を 見ました）

★★★報告の あとに 直った もの です。★台帳07 の 字を 足しました（★消して いません）。

| 約束 | 台帳07 の 字 | ★台帳（2026-09-19 に 見ました） |
|---|---|---|
| 10. サーバ側の 撤回の 門 | 「★ありません。いまは 画面側だけ」 | `entries` は **見て います**（`entries_insert_own_not_withdrawn` ／ `entries_update_own_not_withdrawn`）。★ただし `cycle_periods` は 見て いません（決まり 1本・`*`） |
| 13. ★薄い層（`lessons` の UPDATE に WITH CHECK が 無い） | 「2本に WITH CHECK が ありません」 | **3本 とも 在ります**（`Teacher can update or delete lessons` ／ `Teachers can update or delete org lessons` ／ `lessons_student_notice`） |

★★★つまり 13 は 閉じられます。★10 は **半分** です ── `cycle_periods` が 残って います。

---

## ★この 紙で して いない こと

★★㋐の「何を 見れば よいか」は **案** です。★証拠の 定義は Opus が します。
★★ここで 定義すると、★実装から 作った 表に なります。★実装が 誤って いれば、
　★誤った まま 一致します。
