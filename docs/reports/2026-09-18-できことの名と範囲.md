# ★できことの 名と、★それが 開く 範囲

★全56行 ／ 末尾は「★③ 関数（SECURITY DEFINER）の 中。★`can_view_ops_perm` は 呼ぶ 側が 名を 渡します。」

★裁定 その88 ROOT_CAUSE ／ ★2026-09-18 ／ ★`tools/perm_scope_audit.py` が 書きました。

★きっかけ ── ★`renraku_all`（「学校全部へ **出す**」）で、
★門下の やりとりが **読めて** いました。★名前と 範囲が ちがいました。

## ★数

★できこと 14 ／ ★`has_can` を 使う 決まり 23

## ★1件ずつ

| 表 | 決まり | 何を | できこと | その 言い方 |
|---|---|---|---|---|
| assignments | `assignments_all_owner_admin` | ぜんぶ | `meibo` | 学校全部の 名簿を 見る・直す |
| assignments | `assignments_select` | 読む | `meibo` | 学校全部の 名簿を 見る・直す |
| enrollments | `enrollments_all_owner_admin` | ぜんぶ | `meibo` | 学校全部の 名簿を 見る・直す |
| memberships | `memberships_delete_admin` | 消す | `post` | ひとの 役職を 変える |
| memberships | `memberships_select` | 読む | `post` | ひとの 役職を 変える |
| memberships | `memberships_update_needs_can_post` | 直す | `post` | ひとの 役職を 変える |
| memberships | `memberships_update_role_management` | 直す | `post` | ひとの 役職を 変える |
| monka_read_log | `monka_read_log_insert_self_monka_read` | 足す | `monka_read` | 事故のときに 門下を 読む（普段は 切ってあります） |
| monka_read_log | `monka_read_log_select_master_or_self` | 読む | `master` | 学校の 形（学部・学科・学年・分野）を 直す |
| org_billing | `org_billing_insert_bill` | 足す | `bill` | ご請求を 見る |
| org_billing | `org_billing_select_bill` | 読む | `bill` | ご請求を 見る |
| org_billing | `org_billing_update_bill` | 直す | `bill` | ご請求を 見る |
| org_events | `org_events_delete_needs_can_gyoji` | 消す | `gyoji` | 行事を 出す・直す |
| org_events | `org_events_insert_needs_can_gyoji` | 足す | `gyoji` | 行事を 出す・直す |
| org_events | `org_events_update_needs_can_gyoji` | 直す | `gyoji` | 行事を 出す・直す |
| org_events | `org_events_write_admin` | ぜんぶ | `gyoji` | 行事を 出す・直す |
| org_invitations | `org_invitations_insert` | 足す | `meibo` | 学校全部の 名簿を 見る・直す |
| org_invitations | `org_invitations_select` | 読む | `meibo` | 学校全部の 名簿を 見る・直す |
| org_invitations | `org_invitations_update` | 直す | `meibo` | 学校全部の 名簿を 見る・直す |
| org_messages | `org_messages_insert` | 足す | `renraku_all` | 学校全部へ お知らせを 出す |
| org_messages | `org_messages_select` | 読む | `monka_read`／`renraku_all` | 事故のときに 門下を 読む（普段は 切ってあります）／学校全部へ お知らせを 出す |
| post_change_log | `post_change_log_insert` | 足す | `post` | ひとの 役職を 変える |
| post_change_log | `post_change_log_select` | 読む | `post` | ひとの 役職を 変える |

## ★★★見て いただきたい もの（1）

★「出す」「書く」「払う」を 名に 持つ できことが、★**読む** 決まりで 使われて いる ところ です。

| 表 | 決まり | できこと | その 言い方 |
|---|---|---|---|
| org_messages | `org_messages_select` | `renraku_all` | 学校全部へ お知らせを 出す |

## ★この 道具が 見て いない こと

★① 名に 動きの 語が 無い できこと（`meibo`「学校全部の 名簿を 見る・直す」など）。
★★「見る・直す」の 両方を 名に 持つ ものは、★読む 決まりで 使って も 正しい です。
★② 決まりの 中の **枝**。★1つの 決まりに 2つの できことが ある とき、
★★どちらの 枝が どこに かかるかまでは 読んで いません。★式を ご覧ください。
★③ 関数（SECURITY DEFINER）の 中。★`can_view_ops_perm` は 呼ぶ 側が 名を 渡します。
