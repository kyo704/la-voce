# 「並べる」の 撮影が 失敗した 件

全98行 / 末尾は「　★★次に 字を 変える ときは、★`grep` で 道具の 側も 探してから 変えます。」

## 0　お答え（結論）

**いまは 撮れて います。★失敗して いません。**

坂本さんが ご覧に なった `撮れなかったもの.txt` は、**1時間 前の 記録**でした。
★★私の 道具の 誤りです。★**0件の ときに 書き直して いません**でした。

## 1　証拠

| | 時刻 |
|---|---|
| `撮れなかったもの.txt` | **09:40** |
| `画面-ふりかえる-並べる@390.png` | **10:37** |
| いちばん 新しい コマ | **10:40** |

★コマの ほうが **1時間 新しい**です。
★★つまり、★09:40 に 失敗した あと、★10:37 に 撮り直して **成功して います**。
　★けれど 記録の ほうが、★09:40 の ままでした。

中身も 確かめました。

    画面-ふりかえる-並べる@390.json　★40件の 要素

　「声の 出来」「のどの 調子」「起きたときの むくみ」「声を 使った 時間」
　「昨夜の 睡眠」「食べ終えてから 寝るまで」「湿度」── ★中身が 入って います。

コンタクトシートの **15ページ目**にも、★実装の 側が 写って います。

## 2　なぜ 失敗したか（★09:40 の 回）

**私が 札の 名前を 変え、★撮影の 手順を 直し忘れました。**

| | |
|---|---|
| `95d7181` | 切替の 札を「**ならべる**」→「**並べる**」に しました（★見本どおり） |
| `c39c404` | 撮影の 手順を `steps: ["並べる"]` に 直しました |

★★この 2つの あいだの 回が、★09:40 です。
　★道具は まだ「ならべる」を 探して いました。
　★その 名前の 押しどころは、★もう ありません。
　★★30秒 待って、★`locator.evaluate: Timeout 30000ms exceeded` に なりました。

### ★なぜ「並べる」だけか

**字を 変えたのが、★その 1つだけ だからです。**

| 札 | 前 | いま | 撮影 |
|---|---|---|---|
| 並べる | ならべる | **並べる** | ★失敗 |
| さかのぼる | さかのぼる | さかのぼる | ○ |
| くらべる | くらべる | くらべる | ○ |
| かぞえる | かぞえる | かぞえる | ○ |

★390px だけが 失敗した わけでは ありません。
★★いまは 390px でしか 撮って いません（★坂本さんの お決め・9月11日）。
　★幅の せいでは ありません。★**字の せい**です。

## 3　もっと 重い 誤り ── ★記録が 古い ままに なる

原因は 2つ あります。★2つめの ほうが 重いです。

    if (missed.length) {          ← ★ここ
      fs.writeFileSync(… "撮れなかったもの.txt" …);
    }

★1件も 落ちなかった 回は、★**書き直して いません**でした。
★★だから 古い 失敗が 残り、★直った あとも 失敗した ように 読めます。

**「黙って 消さない」ための 仕組みが、★黙って 古い ままに なって いました。**

### 直しました

- **いつでも 書き直します**（★0件でも）。
- **撮った 時刻を 1行目に 書きます。** ★コマの 時刻と くらべられる ように。
- 見本の 側（`tools/mihon-all.js`）にも 同じ 直しを 入れました。

いまの 中身 ──

    ★撮れなかった もの（0件）
    ★この 記録は、★撮るたびに 書き直します（★0件でも）。
    ★撮ったのは 2026-09-11 11:07 です。★コマの 時刻と 合っているか 見てください。
    ★黙って 飛ばしていません。★理由を そのまま 残します。

    　★1件も ありません。★34 コマ すべて 撮れました。

## 4　この 件から 学んだ こと

**画面の 字を 変えたら、★その 字で 押している 道具も 一緒に 直す。**

いま 字で 押している ところ ── `tools/compare.js` ／ `tools/seed.js` ／
`tools/shot.js`。★どれも 画面の 字を そのまま 使って います。

★★同じ 形の 失敗が、★この 帳面で 3度目です（★`ならべる`／`ふつう`／`ない`）。
　★★次に 字を 変える ときは、★`grep` で 道具の 側も 探してから 変えます。
