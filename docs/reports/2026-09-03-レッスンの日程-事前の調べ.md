# 軽いレッスンの日程 ── 実装前の調べ（2026-09-03）
全241行 / 末尾は「★★先に作り替えないこと。★2つ同時に変えると、どちらが効いたか分かりません。」

★実装していません。★スロット⑥の先頭です。
★作らないもの：部屋・時間割の仕組み・繰り返しの仕組み・自動の割り当て（Stage 3）。

---

## ① いま、レッスンは誰が、どこから作れるか

### 作る処理は2つあります。★片方は、呼ぶ人がいません。

    handleCreateLesson(linkId)          VocalTracker.jsx:9971
      入れる列  link_id / scheduled_at / note / created_by
      呼ぶ場所  13771（レッスンタブ・教える側の、生徒ごとのカード）
      ★つながり（teacher_student_links）ごとの、1対1のレッスンです。

    handleCreateOrgLesson(orgId, teacherId, studentId, dateStr, timeStr, note)
                                        VocalTracker.jsx:10556
      入れる列  org_id / teacher_id / student_id / scheduled_at / note / created_by
      呼ぶ場所  ★ありません。定義の1か所だけです。
      ★★教室のレッスンを作る画面が、いまありません。

★★これは N-1 の違反です。★作った関数が、どこからも呼ばれていません。
★この機能は「作れる関数はあるが、押す場所が無い」状態です。

### scheduled_at は、本人が決められます

    9974   const scheduledAt = new Date(`${newLessonDate}T${newLessonTime}:00`).toISOString();
    10559  同じ形

★「いま」ではありません。★日付と時刻を、画面から選びます（`13763` の
`<input type="date">` と、時刻の選択）。
★★ただし、★選べるのは1件ずつです。範囲はありません。

---

## ② レッスンのタブは、立場ごとに何を出しているか

★タブは1つで、中で立場を切り替えます（`resolveLessonRole`・660-679）。
★両方に当てはまる人にだけ、切り替えを出します。

    習う側（learn）        13551-
      LessonCalendar（月のカレンダー）… myAllLessons
      ★1対1と教室の両方を、1つに合わせて見せます（fetchMyAllLessons・10567）
      ★作れません。読むだけです。

    教える側（teach）      13545-
      生徒ごとのカード。★その中に
        ・終わったレッスンの一覧（実施したか・8件まで）
        ・日付と時刻を入れて「作る」
      ★カレンダーは、教える側にも出ます（13589 付近の一覧）

    オーナー・教室の責任者
      ★レッスンの画面は、★ありません。
      ★教室のカード（もっと ＞ 教室）に、
        「在籍している生徒（◯人）と担当」があるだけです（14009 付近）。
      ★★教室ぜんぶのレッスンを、日で見る画面はありません。

★★つまり、いま在るのは
    ・生徒：自分のぶんの月のカレンダー
    ・先生：生徒ごとの縦の一覧
★★無いのは
    ・先生の「今日」「今週」
    ・教室ぜんぶの「今日」

---

## ③ lessons の RLS ── 教室ぜんぶを読めるのは can_view_ops です

    supabase/migration_fix_lessons_org_null_policies.sql:64-72
      "Ops-visible lessons (org-based)"  for select
        using (org_id is not null
               and (auth.uid() = student_id
                    or can_view_ops(auth.uid(), org_id, student_id)))

    ★INSERT / UPDATE / DELETE も、同じ can_view_ops です（75-96）。

### ★確かめました（2026-09-03・本番）── 推測ではなくなりました

    can_view_ops
      在籍が有効であること
      ★かつ（オーナー・責任者 ★または いま担当している講師）
      SECURITY DEFINER ／ search_path = public（★固定ずみ）

★★案1 の線は、ここでも守られています。
　★講師が読めるのは、★自分が担当している生徒のレッスンだけです。
★ですから、レッスンの画面は★いまの権限のまま作れます。★直しは要りません。

### ★ポリシーの並びも、確かめました

★教室向けのポリシーは、すべて `(org_id IS NOT NULL) AND` から始まります。
★2026-09-02 の事故の形（左が真になって関数が呼ばれない）は、★ありません。

### ★思いがけない収穫 ── 規約 第5条が、もう実装されています

★在籍が有効であることが条件なので、★教室を離れた生徒は、
★その時点で先生・責任者から★見えなくなります。

    利用規約 第5条（09-v1・未公開）
      「教室等から離れた後、当該教室等の指導者・運営者は、
        利用者の予定および出欠を★新たに参照することはできません。」

★★書こうとしている約束が、★すでに仕組みとして在りました。
　★珍しいことです。★ふつうは逆（書いてあるが無い）でした。

### ★薄い層が1つ（★急ぎではありません）

★UPDATE のポリシー2本に、★WITH CHECK がありません。

    ★2026-09-04 の決まり
      「★WITH CHECK の無い UPDATE のポリシーは、欠陥です。例外はありません。」

★いまは、身元を固定するトリガーと、★列ごとの権限で守られています。
★★ですから急ぎではありません（運営者の判断）。
★★ですが「欠陥ではない」ということではありません。★薄い層が1枚です。

★★#34 で lessons を触るときに、★同時に厚くしてください。
　★そのとき、★lessons の列ごとの UPDATE 権限が
　　いま何になっているかも、★あわせて確かめること
　　（`grant update (held)` だけのはずですが、★確かめていません）。

★★org_id が null の行（1対1のレッスン）は、
　★別のポリシー（"Teacher and student can view lessons"）が見ます。
　★2026-09-02 に、ここで事故が起きています。
　　★`(org_id IS NULL) OR …` と書いてあり、★左が真になるので
　　★`can_view_ops` が★一度も呼ばれていませんでした。
　★同じ形を、また書かないこと。

---

## ④ Google カレンダーへ出るもの ── ★レッスンだけ、3つだけ

    lib/calendarExport.js
      CALENDAR_ALLOWED = ["start", "end", "title"]   ★これで全部
      ★CALENDAR_FORBIDDEN に note が入っています

    VocalTracker.jsx:2576 buildGoogleCalendarUrl(lesson, t)
    VocalTracker.jsx:2591 downloadLessonICS(lesson, t)
      ★どちらも★題名を引数に取りません。
      ★呼ぶ側が、名前やメモを入れる余地がありません。

    付いている場所  2685 と 13589（★どちらもレッスンです）
    ★教室の予定（org_events）には、付いていません。

★★過去に漏れていたもの（★直す前）
    ・題名に先生のお名前（利用者の Google の予定表に残りました）
    ・説明欄に lesson.note（体調のことも書けます）
★★「入れないでください」ではなく、★入れられない形にしてあります。

★★日程の画面を増やしても、★カレンダーへ出るものは増えません。
　★増やすときは、★はっきり決めたときだけです。

---

## ⑤ できるかどうか（★作らずに、見立てだけ）

### 5-1 先生の「今日」「今週」

★できます。★新しい列も、ポリシーの変更も要りません。
    ★いま読んでいる `myUpcomingLessons` / `studentLessons` を、
      日で束ねるだけです（`LessonCalendar` が既に日で束ねています・2629-2636）。
★★遠い先を畳むこと。★静かな画面にします。
    ★既定は「今日」と「今週」。それより先は、押したら開く形に。

### 5-2 教室ぜんぶの「今日」（オーナー・責任者）

★できます。★ただし★can_view_ops の中身を確かめてからです（③）。
★読む道は既にあります（`fetchOrgDetail` が lessons を org_id で読んでいます・10184）。
★★講師には出しません。★講師が読めるのは自分の担当だけ、という
　案1 の線を、ここでも守ります。

### 5-3 同じ先生の重なりを知らせる（★止めない）

★できます。★画面側だけで足ります。
★同じ `teacher_id` で、時間が重なる行があれば、★知らせるだけ。
★★止めないこと。★合唱や合わせで、わざと重ねることがあります。
★★「重なっています」と言い切らないこと。
　★読めていない行があるかもしれません。
　★「重なっているかもしれません。ご確認ください。」の形に。

### 5-4 ★範囲の複製（週ごと・1／3／6／12か月）

★できます。★ただし★列が1つ要ります。

    lessons.batch_id uuid   ★新しい列

★作り方
    1  もとの1件を選ぶ
    2  範囲を選ぶ（1／3／6／12か月）
    3  ★作る前に、★何件になるかを見せる
       「11月3日まで、★13件を作ります」
       ★見せてから、押してもらいます。
    4  同じ batch_id を付けて、★ふつうの行として1件ずつ入れる

★★「まとまり」という概念は作りません。
　★1件ずつが、独立した、ふつうのレッスンです。
　★1件だけ動かしても、ほかは動きません。★それでよい、という設計です。

★取り消し
    ★同じ batch_id を、★1回の操作で消せます。
    ★★held_at が入っている行は、★飛ばします。
      ★終わったレッスンを、まとめて消さないためです。
    ★何件消して、何件残すかを、★消す前に見せること。

★★#34 とぶつかるところ（★ここが大事です）
    ・#34 は `lessons` に `held_at` と `actual_minutes` を足します。
    ・★範囲の複製の取り消しは、`held_at` を見ます。
    ★★つまり、★#34 が先に入っていないと、取り消しの条件が書けません。
    ★順番：★#34（スロット④）→ 範囲の複製（スロット⑥）。
    ★逆にすると、★終わったレッスンを消せてしまいます。

    ・★列ごとの UPDATE 権限（`grant update (held)` だけ）にも触れます。
      ★#34 で `held_at` / `actual_minutes` を足すときに、
      ★`batch_id` も一緒に考えてください。
      ★batch_id は★作るときに入れるだけで、★更新しません。
        ★UPDATE の権限は、★要りません。

### 5-5 ★batch_id は、7つの台帳の手続きに乗せること

★新しい列なので、`docs/ledgers/README.md` の手順を回します。

    01 削除処理      ★lessons は、いま物理削除です（9836 の handleDeleteLesson）。
                     ★batch_id を持つ行も、同じように消えます。
                     ★退会のときは、外部キーの cascade に任せています。
    02 バックアップ  ★lessons が入っているかを確かめること
    03 auth 参照     ★関係ありません（batch_id は uuid ですが auth.users を指しません）
    04 書き出し      ★lessons は、本人の書き出しに入っていません。
                     ★org_events と同じ扱い（★教室のものは、その人のものではない）
    05 外に出る経路  ★カレンダーへは出しません（④のとおり、出るのは3つだけ）
    06 起きたこと    ★作るときは関係ありません
    07 約束          ★「まとめて作ったものを、まとめて取り消せる」は約束になります。
                     ★★終わったレッスンは消えない、も約束です。書くこと。

---

## ⑥ 消し方について（★運営者の判断・2026-09-03）

★軽い版では、★物理削除のままにします。
★印をつける形への作り替えは、★#34 の held_at の作業と一緒に考えます。
★★先に作り替えないこと。★2つ同時に変えると、どちらが効いたか分かりません。
