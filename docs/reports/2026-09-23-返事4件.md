# ★★★お尋ね 4件への 返事（2026-09-23 夜）

---

## ★★★A ── 退会の 画面は 引き継ぎを 促して いません でした

```
①促して いるか …… ★★いいえ
②黙って「契約者の いない 学校」が できるか …… ★いいえ。★止まります。
   ★けれど、★なぜ 止まったのか **伝わって いません** でした。
```

### 調べた こと

★サーバ（`app/api/account/delete/route.js`）は、★**正しく** 止めて います ──

```js
.from("organizations").select("id, name").eq("contract_owner_user_id", user.id)
…
return NextResponse.json({
  contractOwner: true,
  orgs: 出,                     // ★学校ごとに、引き継げる 方の 数まで 数えて います
  notice: LEAVE_BLOCKED,        // 「先に、契約者を 引き継いでください。」
  how: LEAVE_BLOCKED_HOW,       // 「もっと → 学校 → 契約者を 変える」
  noneNotice: CONTRACT_NONE, noneHow: CONTRACT_NONE_HOW
}, { status: 409 });
```

★★ところが 画面（`components/VocalTracker.jsx` の `handleDeleteAccount`）は、
　★409 の うち **4つ**しか 受けて いません でした ──

```
data.blocked ／ data.cancelFailed ／ data.paymentActive ／ data.payerNotice
★★`data.contractOwner` は ありません
```

★★★そのため、★契約者の 方は `!res.ok` の 枝に 落ちます。
　★その 返事には `error` が 無い ので、★画面に 出るのは ──

```
「削除できませんでした。」
```

★★なぜ 止まったのか、★どう すれば よいのか、★引き継げる 方が いるのか。
　★**どれも 伝わりません** でした。

★★★本番の 7学校 とも 契約者が 入って います。★7人が この 道に 入ります。

### 直しました

★受け皿と 出しどころを 足しました。★言葉は `lib/orgContract.js` の もの を そのまま 使います。

```
引き継げる 方が いる  → 「先に、契約者を 引き継いでください。」
                        「もっと → 学校 → 契約者を 変える」
                        ［契約者を 変える］  ★押せる 札
引き継げる 方が いない → 「この学校に、引き継げる方が いません。」
                        「先に どなたかに、学校全部の 札を 渡してください。」
                        ［学校を 見る］      ★押せる 札
```

★★★字だけ 置かずに **押せる 札**に しました（★台帳 …「道を 出すなら 押せる ように」）。

---

## ★★★B ── share_scope は、★引いて いますが **使って いません**

```
★読んで いるか …… ★1か所 だけ 引いて います
★使って いるか …… ★★いいえ。★引いた あと **捨てられて** います
★書いて いるか …… ★いいえ。★1か所も ありません
```

### 道すじ

```
lib/dbColumns.js:70
  COLS_TEACHER_STUDENT_LINKS = "… status, share_scope, invited_at, …"
      ↓
components/VocalTracker.jsx:11094（★書き出しの ところ）
  .from("teacher_student_links").select(COLS_TEACHER_STUDENT_LINKS).eq("student_id", userId)
      ↓
lib/exportData.js:119  sanitizeShareHistory()
  SHARE_HISTORY_SAFE_COLUMNS = ["status","accepted_at","revoked_at","revoked_by","created_at"]
      ↓
★★★share_scope は ここで **落ちます**。★書き出しには 入りません。
```

★★`lib/exportData.js:112` にも「★share_scope は 2026-09-01 に 外しました」と 書いて あります。
★★`components/tests/teacher-link-reconnect.test.js` も「書き出しに 載せない」を 見張って います。

★★★つまり ── ★網を 通って ブラウザまで 運ばれ、★その場で 捨てられて いました。
　★★行の 決まり（RLS）は 列を 隠せません。★**引かない** ことでしか 止められません。
　★★★本番の 11行 は、★どれも 中身の 入った `share_scope` を 持って います。
　　★11人ぶんの 共有の 好みが、★使いもしないのに 運ばれて いました。

### した こと

★`COLS_TEACHER_STUDENT_LINKS` から `share_scope` を 外しました。★見張り 30/30 通ります。

★★★**列ごと 消して よい** と 思います（★裁定138 の 形）。
　★画面は 読んで いません。★書いても いません。★書き出しにも 入りません。
　★★消す SQL は Opus の ぶん です。★私は 書いて いません。

---

## ★★★C ── 色の 2系統は「良し悪し」では ありません。★指標の 系統 です

★`lib/lineUp.js` の `LANES` を そのまま お読みください ──

```js
{ key:"voice",  label:"声の 出来",          tone:"enji",   scale:"three" }
{ key:"nodo",   label:"のどの 調子",        tone:"enji",   scale:"three" }
{ key:"muku",   label:"起きたときの むくみ", tone:"enji",   scale:"three" }
{ key:"sing",   label:"声を 使った 時間",    tone:"midori", scale:"n", unit:"分" }
{ key:"sleep",  label:"昨夜の 睡眠",        tone:"midori", scale:"n", unit:"時間" }
{ key:"yuu",    label:"食べ終えてから 寝るまで", tone:"midori", scale:"n", unit:"時間" }
{ key:"shitsu", label:"湿度",              tone:"midori", scale:"n", unit:"%" }
```

★★`tone` は `scale` に ついて います ──

```
enji   … ★◎○△ の もの（scale:"three"）
midori … ★量の もの（scale:"n"・分 ／ 時間 ／ ％）
```

★★★良し悪しでは ありません ──
　★「声の 出来」も「のどの 調子」も、★良い 日も 悪い 日も **同じ えんじ** です。
　★「湿度」と「睡眠」が 同じ みどり なのも、★どちらも 量 だから です。
　★★濃淡（.95 / .66 / .42）は **どの レーンか** を 分ける もので、
　　★値の 大小では ありません。

★★同じ ファイルの 註が、★はっきり そう 書いて います ──

```
★★色は 2系統だけです（★裁定 §1-3）。
  ★◎○△ の もの … えんじの 濃淡
  ★量の もの　　 … みどりの 濃淡
★★系列ごとに 別の 色相を 使いません（★tokens.md）。
  ★見本は いちど 5色に していて、★信号色に 見えると 直されました。
```

★★★2系統は、★信号色を **避ける ため に** 作られた 仕組み です。
　★★ですから ── ★**復元して よい** と お答えします。★もう 戻して あります。
　★`tools/display_guard.py` が、★消えたら 止める ように なって います。

---

## ★★★D ── 土台の 目録を 出しました

```
tools/ledger_snapshots/2026-09-23c_base_prod.json
```

```
tables        129   e065c686a06c
columns      1156   3bbee241ecba
constraints   515   1397f9f90c76
indexes       255   4cd2fcd23deb
policies      217   cfe54cba3cc6
functions     162   ef8f34a48171
grants       1223   9bb14c6e269d
column_grants 2547  fb7dd7079178
triggers       54   8384ec9dc114
extensions      6   5c72d7e072db
rls           129   9a2d2e2e9d4c
```

★★★試しの 環境の リセットは **して いません**。

★わけ ── ★いま `base_twin` は **0件** です（★pack_16 で 字が 届いた ため）。
★★リセットすると、★きょう 積み上げた 種と 証しが 消えます。
★★★もし それでも リセットが 要る なら、★ひとこと ください。★1回で 作り直せます
　（`tools/schema_dump.py` → `tools/fx7_replay.py` → `tools/test_seed.py`）。
