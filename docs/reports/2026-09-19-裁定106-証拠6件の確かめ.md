# ★裁定 その106 ── ★売り文句の 証拠 6件

★2026-09-19 ／ ★`tools/insp_promises6.py` が 書きました。

★基準は Opus が 定義した もの です。★こちらで 作って いません。

| 約束 | 言えるか | 数 |
|---|---|---|
| 3 撤回しても 書き出しと 削除は できる | ○ 言えます | 同意を 見る 行 …… 0件 |
| 9 担当と 個別の つながりは 別 | ○ 言えます | `ended_at` で 絞って いない 枝 …… 0件 |
| 10 サーバ側の 撤回の 門 | ○ 言えます | 撤回を 見て いない 書きの 決まり …… 0件 |
| 12 教室を 離れたら 参照されない | ○ 言えます | `leave_enrollment` が `assignments` を 閉じる …… はい |
| 13 薄い層（`lessons` の WITH CHECK） | ○ 言えます | WITH CHECK の 無い UPDATE …… 0本 ／ 全 3本 |
| 16 教室での 呼び方（名前で 分けない） | ★言えません | 名前で 分けて いる 行 …… 3件 |

## ★16 教室での 呼び方（名前で 分けない）

- `lib/orgClosure.js:349 if (mine && mine.role === "owner") {`
- `lib/renraku.js:185 if (isAnnouncement) return role === "owner" || role === "adm`
- `lib/renraku.js:199 return role === "owner" || role === "admin";`
