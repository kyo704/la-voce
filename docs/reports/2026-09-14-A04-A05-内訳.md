# A04 ならべる ／ A05 さかのぼる ── 1件ずつの 内訳
全159行 / 末尾は「　★手で 見本を 読み直して 拾いました（★A05-㋒）。」

★2026-09-14 ／ ★見本 `docs/design/pack-final/00-動く見本（さわれる・全画面）.html`
★この 文は tools/a04_a05_breakdown.py が 書き出しました。

## ★★はじめに ── 「12件」は 多すぎ ました

★前に お伝えした 12件は、★道具の 誤り でした。★訂正します。

| 誤り | 直した こと |
|---|---|
| `components/LineUpV2.jsx` を 読む 一覧に 入れて いた | ★**その ファイルは ありません**。私の 書き間違い |
| A05 の 一覧に `components/LookBackPanel.jsx` が 無かった | ★さかのぼるの 本体です。★入れました |

★★無い ファイルは 黙って 飛ばされ、★在る 字も「無い」と 数えられて いました。
★★直した あとの 数 ── ★**A04 1件 ／ A05 3か所（字で 6語）**。

★★この 訂正は、★直しが 減った という 話では ありません。
　★**数え方が 誤って いた** という 話です。

---

## A04-㋐　期間の 札（pills）

**見本の値**

> 14日 ／ 4週 ／ 3か月 ／ **本番でそろえる** ── ★4つ。
> 4つ目は お休みの あいだ うすく（opacity .42）、押すと「いまは お休みです」。選ぶと `soroeru()` の 画面。

**いまの値**

> `lib/lineUp.js:28` の PERIODS は ★**3つ**（14日／4週／3か月）。
> 「本番でそろえる」の 札は ありません。`soroeru()` に あたる 画面も 0件。

**verdict** … ★足りない ── ★ただし **わざと** です

`lib/quietDays.js:105` に こう 書いて あります ──
「★★2026-11 以降、★「本番でそろえる」も ここに 入ります」。
★あとの 号に 送った もの です。★抜けでは ありません。

★数えました（★components と lib・tests を のぞく）

| 字 | 件数 |
|---|---|
| `本番でそろえる` | 3 |
| `soroeru` | ★0（★ありません） |

---

## A05-㋐　上の 1行（.note）

**見本の値**

> 出なかった日を 選ぶと、その前の 3日に 書いたことが そのまま 出ます。

**いまの値**

> `components/LookBackPanel.jsx:68`
> 出なかった日を選ぶと、その前の3日に書いたことが そのまま出ます。

**verdict** … ★**空白だけ** ちがう

字は 1つも ちがいません。★分かち書きの 空白が 3つ 抜けて います。
★見本は「選ぶと」「3日に」「書いたことが」「出ます」の 前に 空白。
★★見張りが「無い」と 数えたのは、★この 空白の ため です。

---

## A05-㋑　一覧の 行の 小見出し

**見本の値**

> 3行に 2行 … △出づらい と書いた日
> 3行に 1行 … 本番で 出なかった日
> ★（見本は `i%3===0` で 出し分けて います）

**いまの値**

> `components/LookBackPanel.jsx:79`
> ★どの行も 同じ 「前の3日を 見る」。★2種類の 出し分けが ありません。

**verdict** … ★ちがう

★見本は「なぜ この日が 並んで いるか」を 行ごとに 言って います。
★★本番で 出なかった日 と、★自分で △出づらい と 書いた日 ──
　★2つは 別の 出どころ です。★いまは どちらか 分かりません。
★★`LookBackV2.jsx:277` の 注記は、★2つを 分けると 書いて います。
　★分けて 作って ありますが、★画面の 字には 出て いません。

★数えました（★components と lib・tests を のぞく）

| 字 | 件数 |
|---|---|
| `△出づらい と書いた日` | ★0（★ありません） |
| `本番で 出なかった日` | ★0（★ありません） |

---

## A05-㋒　下の 3行（.note）

**見本の値**

> 2つとも 使います ── **△出づらいと 書いた日** と **本番で 出なかった日**。
> 本番だけに すると、0件に なります。
> 文章を 添えません。確率も 割合も 出しません。

**いまの値**

> `lib/lookBack.js:118` の LOOK_BACK_NOTE ──
> 書いたものを そのまま 出しています。
> 文章を 添えません。確率も 割合も 出しません。「これが 原因です」と 言いません。
> 見て、ご自分で 気づくための 画面です。

**verdict** … ★2行が 足りない ／ 1行は 多い

★3行目「文章を 添えません。確率も 割合も 出しません。」は ★あります。
　★ただし、★うしろに 「「これが 原因です」と 言いません。」が 足して あります。
★★足りないのは 1行目と 2行目 ──
　★「2つとも 使います ──」と「本番だけに すると、0件に なります。」。
★★これは ★㋑と 同じ 話 です。★2つの 出どころを 使って いる、という 説明。

★数えました（★components と lib・tests を のぞく）

| 字 | 件数 |
|---|---|
| `2つとも 使います` | ★0（★ありません） |
| `本番だけに すると、0件に なります` | ★0（★ありません） |

---

## まとめ

| | 場所 | verdict | 直すか |
|---|---|---|---|
| A04-㋐ | 期間の 札 | 足りない（★わざと） | ★直しません（2026-11） |
| A05-㋐ | 上の 1行 | 空白だけ | ★直せます（1行） |
| A05-㋑ | 行の 小見出し | ちがう | ★お決め いただきたい |
| A05-㋒ | 下の 3行 | 2行 足りない／1行 多い | ★お決め いただきたい |

### ★私の 見立て

★**A05-㋐ は、★いますぐ 直せます。** 空白だけ です。★迷う ところが ありません。

★**A05-㋑・㋒ は、★1つの 話 です。**
　★「この 一覧は、★2つの 出どころを 合わせて います」と 言うか、言わないか。
　★★見本は 言って います（★行ごとの 小見出し ＋ 下の 2行）。
　★★いまの 画面は 言って いません。★作りは 2つを 分けて います
　　（`LookBackV2.jsx:277`）が、★字に 出して いません。

★**A04-㋐ は 直しません。** `lib/quietDays.js:105` が 2026-11 と 書いて います。
　★★勝手に 前倒ししません。

### ★この 数えが 見て いない こと

★字が あるか だけ です。★色・大きさ・並び・余白は 見て いません。
★★見本の 字は 機械で 抜きます。★`+` で つないだ ところは 落ちます。
　★★実際、★「本番だけに すると、0件に なります。」は 落ちて いました。
　★手で 見本を 読み直して 拾いました（★A05-㋒）。
