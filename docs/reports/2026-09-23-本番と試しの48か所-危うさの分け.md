# 本番と 試しの 48か所 ── ★危うさで 分けました（2026-09-23）

★数え方 …… `tools/ledger_inventory.py twin`（★控え `2026-09-23b_prod.json` ／ `_test.json`）
★中身の 見くらべ …… `tools/twin_body_diff.py`（★きょう 作りました）

---

## ★★★いちばん 大事な こと

★「中身が違う」と 出た 4件を、★1つずつ 並べて 見ました。
★★★**4件の うち 2件は 空白や 註の ちがい だけ**、★**2件は 本当に ちがう 処理** でした。

★★md5 の くらべ だけ では、★この 2つを 分けられません。
　★★「違う」の 山を 見て 慌てる のでは なく、★**中身を 開く** 必要が あります。

---

## 一 ── ★★危うさ 大（★2件）

### ① `accept_teacher_invitation` …… ★試しは **古い・競り負けの ある 形**

```
本番 …… 先に 取りに 行く（★行錠は この update 自身が 取る）
  update teacher_invitations set used_at = now(), used_by_student_id = auth.uid()
   where code = … and used_at is null and expires_at > now()
  returning teacher_id into v_teacher;
  ★★負けた 側は 0行 に なり、★弾かれます

試し …… ★先に 読む。★最後に 無条件で update
  select i.teacher_id into v_teacher from teacher_invitations i where … ;
  …
  update teacher_invitations set used_at = now(), … where code = p_code;
  ★★★2人が 同時に 入れて しまいます
```

★★本番には 移行が 入って います …… `no020_accept_teacher_invitation_claim_first`（2026-09-14）。
★★★試しには **入って いません**。

★★危うさ …… ★招きの 競りを 試しで 試すと、★**直る 前の 姿**を 試す ことに なります。
　★「直った」と 言えません。

### ② `get_invitation_teacher` …… ★試しは **合言葉の 塩・胡椒が 無い 形**

```
本番 …… search_path に 'extensions'。★`v_hash` `v_tries` `v_pepper` を 持つ
        （★`code_pepper` と 試行の 数え。2026-09-16 の migration_code_attempts ほか）
試し …… ★それが ありません。★`teacher_invitations%rowtype` を 直に 読むだけ
```

★★危うさ …… ★合言葉の 総当たり よけ を 試しで 試せません。
　★試しで「何度でも 試せる」ことが、★本番の 姿と ちがいます。

---

## 二 ── 危うさ 小（★4件）── ★片方に だけ ある

| | どこに ある | |
|---|---|---|
| `can_view_ops(viewer, org, student)` | ★試しに だけ | ★古い 形の 残り と 見られます |
| `create_org_event(4引数)` | ★試しに だけ | ★引数の 少ない 古い 形 |
| `create_org_event(9引数)` | ★本番に だけ | ★いまの 形 |
| `mark_attendance(lesson, status)` | ★本番に だけ | ★試しに ありません |

★★危うさ …… ★その 道を 試しで 通せません（★`mark_attendance`）。
　★または、★試しで 通した 道が 本番に ありません（★`create_org_event` の 4引数）。

---

## 三 ── ★空白・註 の ちがい だけ（★2件）★危うさ なし

| | ちがい |
|---|---|
| `has_can` | ★本番は `has_can_user(auth.uid(), …)` を 呼ぶ 形。★試しは 同じ 式を 直に 書いた 形 |
| `are_connected` | ★註（`--`）の 行 が 2つ、★試しに ありません |

★★★`has_can` は **振る舞いが 同じ** です。★確かめました ──
　★`has_can_user` を 2つの 台帳で 並べたら、★**1文字も 同じ** でした。

★★これは 大事な ところ です。★`has_can` は 何十本もの 決まりが 呼びます。
　★★もし ちがって いたら、★きょう 試しで 通した 証しの ほとんどが 揺れます。
　★★★揺れて いません。

---

## 四 ── `anon` が 呼べる（★37件）★危うさ **小**（★向きが 良い）

```
本番 …… False   試し …… True
has_can / has_can_user / leave_enrollment / retire_teacher / move_lesson /
send_message_draft / get_org_member_names / …（★37件）
```

★★★向きは「★試しの ほうが **開いて いる**」です。

★★これが なぜ 危うさ **小** か ──
　★「閉じて いる」ことを 試しで 試すと、★**試しで 落ちます**。★大きな 声で 分かります。
　★★黙って 通る（★偽の 安心）方向では ありません。

★★★ただし 逆が あります …… ★試しで 通った **動き** が、★本番で 通らない。
　★`anon` で 呼ぶ 道を 試しで 作ると、★本番で 動きません。
　★★きょうの `submit_inquiry` の 確かめは これに 当たります（★本番でも anon に 渡して あります。★確かめ済み）。

---

## 五 ── ★★`authenticated` が 呼べる（★3件）★危うさ **中**

```
admin_entry_stats(p_user_id uuid)        本番=False  試し=★True
character_unlock_summary(p_user_id uuid) 本番=False  試し=★True
purge_code_attempts()                    本番=False  試し=★True
```

★★★はじめの 2つは、★**体調の 記録（`entries`）を 読む 関数** です。

★本番では、★入って いる 人からも 呼べません（★サーバの 鍵 だけ）。★裁定167 D1 の とおり です。
★★★試しでは、★**入って いる 人なら 誰でも 呼べます**。

★★危うさ …… ★試しの 台帳に、★本番に 無い 穴が 開いて います。
　★試しの 台帳に 本物の 記録は ありません。★だから いま 害は ありません。
　★★★けれど「試しは 本番の 写し」と 思って 作業すると、★取り違えます。

---

## 六 ── まとめ

| 危うさ | 数 | 何 |
|---|---|---|
| ★大 | **2** | `accept_teacher_invitation`（競り）／`get_invitation_teacher`（合言葉の 守り） |
| 中 | **3** | `authenticated` が 体調の 記録の 関数を 呼べる（試しだけ） |
| 小 | **4** | 片方に だけ ある 関数 |
| 小（向きが 良い） | **37** | `anon` が 呼べる（試しの ほうが 開いて いる） |
| なし | **2** | 空白・註 の ちがい |

★★★FX7 で いちばん 先に 直すべきは、★**危うさ 大の 2件** です。
　★どちらも「本番には 直しが 入って いて、★試しには 入って いない」形 です。
　★★つまり FX7（★本番の 移行から 作り直す）で、★そのまま 直ります。
