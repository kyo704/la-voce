# 書き出す ── 見本との 違いの 調べ（★実装していません）
全157行 / 末尾は「実装していません。ご判断をお待ちします。」

- 日付 … 2026-09-15
- もっとの先 7枚目
- 道具 … `python3 tools/screen_vs_mihon.py 書き出す`
- 読んだ … `components/VocalTracker.jsx`／`lib/exportData.js`／`lib/statNumbers.js`
- 見本 … `SC['書き出す']`（★1つだけ。重複なし）

## 1. 数え直し

足りない … **7 / 9**。今回は作り物のデータがありません。7件とも本当の違いです。

## 2. ★先に申し上げること（★実装の前にご判断が要ります）

見本の下から2つ目の行は、こう書いています。

```
病気の 名前・尺度の 名前は、見出しにも 入れません。
見出しは、画面と 同じ ことばです（のどの調子／声の出来 …）。
```

**いまのアプリは、この2行目を守っていません。**

CSV の見出しは、データベースの列の名前がそのまま出ます。

```
date,throat_condition,voice_quality,resonance_score,sleep_hours,
cpps_value,pianissimo_onset_delay,longest_speech_block_minutes, …（67列）
```

`lib/exportData.js` の `entriesToCsv()` は、行に入っている鍵を
そのまま見出しにします。日本語に直す表を通していません。

1行目（病気の名前・尺度の名前）も、`cpps_value` が引っかかります。
CPPS は声の分析の尺度の名前です。

★この2行を画面に足すと、**守っていない約束を書くこと**になります。
順番として、先に見出しを直すか、この2行を足さないか、どちらかです。

日本語の名前の表は `lib/recordedDay.js` にありますが、5件だけです。
67列に対しては足りません。

## 3. 本当の違い（★7件）

### ㋐ 「形」── CSV / JSON の 選び

見本 … 「形」の下に［CSV］［JSON］の2つの札。CSVが選ばれた状態。
アプリ … 選びがありません。押すと**両方**が落ちてきます。

落ちてくるファイル（最大3つ）──

| ファイル | 中身 |
|---|---|
| `la-voce-YYYY-MM-DD.json` | 全部（19の表＋プロフィール＋共有の履歴） |
| `la-voce-entries-YYYY-MM-DD.csv` | 記録だけ（67列） |
| `la-voce-係数-YYYY-MM-DD.csv` | 相関の係数（出るときだけ） |

★アプリのほうが多く渡しています。減らす話ではありません。
「選ばせない」ことをどう言うか、というご判断です。

### ㋑ 「入れるもの」の 一覧が ありません

見本は5行に ✓ を付けて見せます。

| 見本の行 | アプリの表 | 入っているか |
|---|---|---|
| 記録（全ての 項目） | entries | ✓ |
| ノート | notes | ✓ |
| レパートリー | repertoire_tessitura | ✓ |
| 行事の 出欠 | org_event_participants | ✓ |
| 手に入れたものの 台帳 | item_acquisitions | ✓ |

**5つとも入っています。** 入っていないのではなく、見せていないだけです。

アプリは代わりに、文章で伝えています ──
「記録したすべての内容を、JSON と CSV の2つのファイルとして保存します。
　既往症・アレルギー・常用薬も含みます。」

実際に入れている表は **19** あります（`EXPORTED_TABLES`）。
見本の5つは、その中の代表的なものです。

### ㋒ 「病気の 名前・尺度の 名前は…」の2行がありません

§2 のとおりです。足すなら、先に見出しを直す必要があります。

### ㋓ 「無料です。退会したあとでも、書き出した ファイルは あなたのものです。」

ありません。

★この1行は、書き出しが有料の入口ではないことを言っています。
プランの画面の「無料のうちで できること」とつながる約束です。

### ㋔ 見出しの語

見本 … 「形」「入れるもの」
アプリ … どちらもありません。文章だけです。

## 4. アプリが 見本より 多いところ（★消さないでください）

### ㋕ 書き出す前に、本人か確かめます

`startExport()` が、5分以内に確かめていなければ
再ログインを求めます（`REAUTH_ACTIONS.EXPORT`）。

見本にはありません。端末を置いたまま離れた場合に、
他人がすべての記録を持ち出せないようにするものです。

### ㋖ 「記録の控えを開く（印刷・PDF）」

見本にありません。受診用サマリーとは別で、本人が
「ちゃんと入っているか」を確かめるためのものです。同じ無料の一部です。

### ㋗ 月経周期の1行

`mentionsCycleInDataLists(profile)` が真のときだけ
「月経周期の記録も含まれます。」が増えます。

`cycle_periods` は先生には一切共有しませんが、本人の持ち出しには
必ず含めます（「共有しない」と「本人も取り出せない」は別の話）。

### ㋘ 既往症・アレルギー・常用薬を名指しで伝えています

見本の文にはありません。何が入るかを先に言う形です。

### ㋙ 相手を特定できる値を落としています

共有の履歴は `sanitizeShareHistory()` を通します。
残してよい列だけを通す形（fail closed）で、相手の teacher_id は出ません。
代わりに「連携1」「連携2」と番号だけ振ります。

## 5. ご判断をいただきたいこと

| 印 | 何を | 私の見立て |
|---|---|---|
| ㋒ | CSV の見出しを日本語にするか | ★ここが先です。直さずに文だけ足すと、守っていない約束になります。67列ぶんの日本語名が要ります |
| ㋓ | 「無料です。退会したあとでも…」を足すか | 足すほうがよいと思います。守れている約束です |
| ㋑ | 「入れるもの」の一覧を出すか | 出せます。5つにするか、19の表ぜんぶにするか、ご判断ください |
| ㋐ | 「形」の選びを置くか | 置かずに「両方 落ちてきます」と書くほうが、渡す量が減りません |
| ㋔ | 見出しの語（形／入れるもの） | ㋐㋑ を入れるかどうかで決まります |
| ㋕㋖㋗㋘㋙ | 見本より多いところ | 現状維持。消しません |

## 6. ㋒ を選ばれた場合の見立て（★参考）

67列の日本語名を1か所に置く形になります。

- 置き場所 … `lib/exportData.js`（書き出しの決めは、もうここにあります）
- 足りない名前がある列は、**列の名前のまま出す**のが安全です。
  空にすると、どの列か分からなくなります。
- 見張りは「67列すべてに名前があること」ではなく、
  「名前のない列を見つけたら知らせること」の形にします。
  列は今後も増えるからです。

★病気の名前・尺度の名前が見出しに入っていないことも、
同じ見張りで見られます（禁止語の一覧と突き合わせます）。

実装していません。ご判断をお待ちします。
