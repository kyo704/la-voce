# ★消せない 記録の 確かめ

★2026-09-19 ／ ★`tools/audit_tables_check.py` が 書きました。
★全46行 ／ 末尾は「★★この 4つの 表に `update` / `delete` を 渡して いない ことを 見ます。」

★★★記録が 書き換えられると、★見張る 意味が なく なります。

## ★使う 方（`authenticated`）に 渡して いる もの

| 表 | 渡して いる |
|---|---|
| `monka_read_log` | INSERT SELECT |
| `org_billing_log` | INSERT SELECT |
| `org_message_reads` | INSERT SELECT |
| `post_change_log` | INSERT SELECT |

★★★4つ とも `INSERT` と `SELECT` だけ です。
★★直す・消す・まるごと 消す は、★1つも 渡して いません。

## ★決まり（RLS）

| 表 | 決まり | 足す | 読む | 書き換え |
|---|---|---|---|---|
| `monka_read_log` | 入 | 1 | 1 | 0（★正しい） |
| `org_billing_log` | 入 | 1 | 1 | 0（★正しい） |
| `org_message_reads` | 入 | 1 | 1 | 0（★正しい） |
| `post_change_log` | 入 | 1 | 1 | 0（★正しい） |

## ★引き金（trigger）

- `monka_read_log` …… 0
- `org_billing_log` …… 0
- `org_message_reads` …… 0
- `post_change_log` …… 0

★★引き金が 0なら、★裏から 書き換える 道も ありません。

## ★お伝えして おく こと ── `service_role`

★`postgres` と `service_role` は、★4つ とも 何でも できます。
★★★これは 直せません。★台帳を 作る 鍵 そのもの だからです。
★★この 鍵を 使うのは、★私たちが 書いた サーバの 道 だけ です。
★★（`app/api/…` ／ `lib/supabase/admin.js`）。★画面には 渡して いません。

★★★見張り `components/tests/audit-tables.test.js` が、
★★この 4つの 表に `update` / `delete` を 渡して いない ことを 見ます。
