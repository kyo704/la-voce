# 裁定173 と、★取引の 制約（2026-09-23）

## 一 ── ★お尋ねへの 答え ── **一致して いません**

★裁定173 は こう 書いて います。

```
サーバは、★利用者の 処理を 始める ときに 1行 入れる:
  select set_config('app.actor_id', <ログインしている人の id>, true);   -- true＝この取引の中だけ
```

★★★この とおりに すると、★**残りません**。

## 二 ── 試しの 台帳で 並べました（★2026-09-23）

```
★① 別々の 呼びで set_config → update（★PostgREST と 同じ 形）
     select set_config('app.actor_id', '…', true);     ← ★1つ目の 取引
     update public.memberships set post_id = …;        ← ★2つ目の 取引
   記録 …… changed_by = ★null

★② 同じ 呼びの 中で set_config → update（★1つの 取引）
     select set_config(…); update public.memberships set post_id = …;
   記録 …… changed_by = ★'eafa63c2-…'
```

★わけ …… ★`supabase-js`（PostgREST）は、★1つの 呼びを **1つの 取引** で 走らせます。
　★`set_config(…, true)` は その 取引の 中だけ です。
　★★別の 呼びで `update` を する ころには、★印は もう 消えて います。

★★★裁定173 の `actor_id()` の **仕組みは 正しい** です。
　★変えるのは「**どこで 印を 置くか**」だけ です。

## 三 ── 直した 形（★㋑ 3点セット・ご承認 済み）

### ㋑-1 …… 関数が 両方 する

```sql
create or replace function public.set_member_post(
  p_org_id uuid, p_user_id uuid, p_post_id uuid, p_actor uuid)
…
  if p_actor is null then raise exception 'ACTOR_REQUIRED'; end if;
  perform set_config('app.actor_id', p_actor::text, true);   -- ★同じ 取引の 中
  update public.memberships set post_id = p_post_id
   where org_id = p_org_id and user_id = p_user_id;
```

★★`revoke all … from public, anon, authenticated`。★サーバ だけ が 呼べます。
　★★`p_actor` を 画面から 渡せると、★誰にでも なりすませます。

★★★裁定173 は「入れ忘れた 処理は `system` として 残る」と 書いて います。
　★ここでは **断り**ます。★同じ 裁定が「役職の 変更は 誰が やったか 分からないと 意味が ない」と
　　書いて いる から です。★`system` に しません。

### ㋑-2 …… 呼ぶ 側（3か所）

```
app/api/org/posts/route.js
  166行 …… はじめの 1人を 学長に
  210行 …… 役職を 外す
  299行 …… 役職を 付ける
★3か所 とも `admin.rpc("set_member_post", { … p_actor: user.id })`
```

★★★返りが 変わりました ── ★配列では なく **真偽** です。
　★`done.length === 0` の ままだと、★いつも「0行」と 読みます。★`done !== true` に 直しました。

### ㋑-3 …… 見張り

```
components/tests/post-actor.test.js …… 5 / 5
  ①`app/api/` に `.update({ post_id` が **0件**
  ②`set_member_post` を 3か所で 呼んで いる
  ③「誰が」を 必ず 渡して いる（`p_actor: user.id`）
  ④較正 …… わざと 1件 作ったら 見つかる ／ 註の 中は 見つけない
```

★★これが 無いと ㋑は いまより **弱く** なります。
　★いま …… 書き忘れたら 記録が 増えない（★数えれば 分かる）
　★㋑だけ …… 記録は 増えるが `changed_by` が 空（★気づきにくい）

## 四 ── 直した 見張り（★3本）

| | 前 | いま |
|---|---|---|
| `ops-posts` | `.update({ post_id: null })` を 見て いた | `set_member_post(… p_post_id: null)` ／★直の 書き方が 0件 |
| `ops-people` | `.select("user_id")` の 行数 | `done !== true` で 断る こと |
| `template-bootstrap` | `.eq("user_id", user.id)` | `p_user_id: user.id` |

★★どれも「見る ところ」は 変えて いません。★書き方が 変わった だけ です。

## 五 ── Opus へ

★裁定173 の §1 の「サーバは 1行 入れる」を、★次の 版で
「サーバは **関数を 呼ぶ**。★関数が 同じ 取引の 中で 印を 置く」に
直して いただけますか。

★★仕組み（`actor_id()`・優先の 順・取引の 中だけ）は そのまま です。
★★★`evaluation_scores_guard` が `app.score_edit` を 見る 形も、★同じ 問いを 抱えて いるはず です。
　★そちらも「関数が 置く」形に なって いるか、★次に 見ます。
