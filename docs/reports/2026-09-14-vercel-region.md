# Vercel の 地域 ── ヘッダから 読みました（2026-09-14）
全95行 / 末尾は「　★ご覧ください。`supabase.io` なら ★未設定 ── ★Supabase 自身が 送り手です。」

★この 文は tools/vercel_region_check.py が 書き出しました。
★数と 記号は すべて、★実際に 投げた HEAD の 返事から 取って います。

## ★答え

```
STATIC_REGION:   hnd1（日本・東京）
FUNCTION_REGION: iad1（アメリカ合衆国・バージニア州（US East））
```

### ★★関数は 日本の 外で 動いて います。

★保管は 東京（ap-northeast-1）です。★しかし 読み出しと 処理は アメリカ合衆国・バージニア州（US East） です。
★Opus の お見立てのとおりでした。

## 生の 返事

### 関数を 通る 道

| 道 | x-vercel-id | 区切りの 数 | 玄関 | ★関数の 地域 |
|---|---|---|---|---|
| `/` | `hnd1::iad1::fc9wm-1789378351562-c82297770f80` | 3 | hnd1 | ★iad1 |
| `/api/version` | `hnd1::iad1::j6gr8-1789378352163-0b112784ff71` | 3 | hnd1 | ★iad1 |
| `/api/feedback` | `hnd1::iad1::7bt8m-1789378352791-6ac6c9772930` | 3 | hnd1 | ★iad1 |

### 関数を 通らない 道（★較正）

| 道 | x-vercel-id | 区切りの 数 | ★関数の 地域 |
|---|---|---|---|
| `/manifest.json` | `hnd1::dkjkh-1789378353342-0f1bbf68af7e` | 2 | ★無し（＝関数を 通って いない） |
| `/sw.js` | `hnd1::z6k8k-1789378353706-0a16c44867ae` | 2 | ★無し（＝関数を 通って いない） |
| `/favicon.ico` | `hnd1::4qldw-1789378353986-461d9bab8cd0` | 2 | ★無し（＝関数を 通って いない） |

## ★較正 ── 読み方が 正しいことの 確かめ

★「iad1 と 書いて あるから 米国」だけでは、★読み違えが 起きます。
★答えの 分かって いる ものに 当てて、★道具の ほうを 先に 疑いました。

| 当てた もの | 期待 | 実際 | |
|---|---|---|---|
| 静的ファイル 3本 | ★区切りは 2つ（関数を 通らない） | 2、2、2 | ★合う |
| 関数 3本 | ★区切りは 3つ | 3、3、3 | ★合う |

★静的は 2つ、関数は 3つ。★きれいに 分かれました。
★★真ん中の 記号が『関数の 動いた 地域』である、という 読み方が 裏づけられました。

## ★では、何が 日本の 外へ 出るのか

★★ここを 確かめずに『記録が 米国で 処理されて いる』とは 言えません。
　★記録の ほとんどは、★画面（ブラウザ）から ★直に Supabase（東京）へ 行きます。
　　`components/VocalTracker.jsx:6085` ── `supabase.from("entries").select("*")`
　　★これは Vercel の 関数を ★1つも 通りません。★東京と 利用者の 間だけです。

★Vercel の 関数の 中で entries を 読む 所は、★次の とおりです。

| # | 場所 | 取って いる 列 | ★値が 要るか |
|---|---|---|---|
| 1 | `app/admin/page.js:102` | ★12列（weight_kg・medication_tags・mental_reason・voice_memo ほか） | ★要りません。★『埋まって いるか』しか 見て いません（:180-188） |
| 2 | `app/api/character/gift/route.js:46` | `date` のみ | ─ |
| 3 | `app/api/character/unlock/route.js:55` | ★`select("*")`（全列） | ★要りません。★`v == null` の 判定だけです（lib/character.js:288） |
| 4 | `app/api/character/buy/route.js:54` | `date` のみ | ─ |
| 5 | `app/api/advice/route.js:89` | ★`select("*")`（全列） | ★要ります。★文に して 送ります。★㋐で 消します |
| 6 | `app/api/cron/line-reminder/route.js:107` | `date` のみ | ─ |

### ★★いちばん 重い のは、★advice では ありません

★`app/admin/page.js:102` は、★admin の 鍵で ★全員の entries を 読みます。
　★`.eq()` が ありません。★38人 ぜんぶです。
　★取って いるのは ★服薬タグ・気持ちの 日記・声の メモ ── ★自由記述を 含みます。
　★★それを ★アメリカ合衆国・バージニア州（US East） の 関数の 中へ 運んで、
　★★`typeof === "number"` と `.length > 0` と `.trim()` しか して いません。

★`app/api/character/unlock/route.js:55` も 同じ 形です。★本人の 全列を 運んで、
　★`v == null` を 見るだけです。★値は ★1つも 使いません。

★★どちらも、★数える 仕事です。★数えるなら、★台帳の 中で 数えられます。
　★`count(*) filter (where weight_kg is not null)` の 形に すれば、
　★★値は ★1つも 東京を 出ません。★出るのは 数だけです。

## ★まだ 分からない こと

| もの | なぜ |
|---|---|
| ログの 保管地域 | ★ヘッダには 出ません。★管理画面だけ |
| 組み立ての 成果物の 置き場所 | ★同上 |
| 請求書の 発行元（契約の 相手） | ★同上。★宛先では なく ★発行元 |
| Supabase Auth の SMTP | ★再設定メールの 差出人を 見る 必要が あります |

★Auth の SMTP は、★私からは 試せません。
　★メールを 送るのは 外向きの 行いで、★受け取り箱も 私には 見えません。
　★坂本さんが 再設定メールを 1通 出して、★差出人（From / Return-Path）を
　★ご覧ください。`supabase.io` なら ★未設定 ── ★Supabase 自身が 送り手です。
