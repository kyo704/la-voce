# ★坂本さんは すでに 名簿に 入って います ── ★足す 必要が ありません

★2026-09-25。

## ★★★足す 前の 確かめ（★1）── ★環境変数は **組み立ての ときに 焼き込まれます**

★★`NEXT_PUBLIC_` で 始まる ものは、★Next.js が 組み立ての ときに **字で 埋め込みます**。
　★実行の ときに 読み直しません。

★★★組み上がった ものを 開いて 数えました ──
```
  .next/static/chunks/app/dashboard/page-*.js の 中に
    `4b027d0a-11de-4acc-ae63-f240300c78aa` …… ★字の まま 入って います
    `process.env.NEXT_PUBLIC_LAYOUT_V2_USER_IDS` …… ★0件（★残って いません）
```

★★★だから ── ★値を 変える たびに **組み直しと 配りが 要ります**。
　★「見て、戻す」なら **2回** です。★Opus の お見立ての とおり です。

## ★★★足す 必要が ありません（★2）

★★本番の 名簿の 2つを 引いた 結果 ──
```
  99b695d8-ae90-43a5-9767-a8d073a4003d
    kyo0703opera@gmail.com  ★坂本さん ご自身（is_admin true）
  f7520dc1-9154-4524-a350-ba0bcddbf0b2
    kyo0703opera+forcode@gmail.com  ★Code の ための 試しの ところ
```

★★★坂本さんは **すでに 入って います**。★足す ものが ありません。
　★★`mayUseLayoutV2` に 本番の 値を 渡して 押しました ── ★`99b695d8` は `true`。

★★★お決め の 字は「坂本さんの user_id のみ を 追加」でした。
　★★その とおりに する なら、★**`f7520dc1` を 外す** 作業に なります。
　★★★それは 組み直しが 要ります。★そして 外すと、★`+forcode` の ところで
　　★確かめが できなく なります。★どうされますか。

★★こちらでは 触って いません ── ★環境変数は 外に 向く 設定 です。

## ★お尋ね 1 ── ★`!layoutV2` の 37か所は 何か

★★★「未実装」では ありません。★**古い 姿を 出す ため** の 分かれ道 です。
```
  && 古い姿を 出す …… 29か所
  || どちらでも    …… 3か所
  覚え書き・その他  …… 5か所
```

★★つまり 38名の 方には 今まで どおりの 画面が 出ます。
　★★★2026-09-09 の お指図「★一般の 38人の 画面は、★一切 変えないこと」を
　　★守る ための 仕掛け です。★直す ものでは ありません。

## ★★★お尋ね 2 ── ★`layoutV2 ?` の 25か所 ── ★3つは **中身が 変わります**

★★見た目 だけ …… 22か所（`className` ／ `style`）。★ここは 問題 ありません。

★★★中身が 変わるのは 3か所 です ──

### ★① 7943行 ── ★★数が 変わります
```
  () => (layoutV2 ? countRecordedDays(entries) : Object.keys(entries).length)
```
★★`Object.keys(entries).length` …… ★**行が ある 日**の 数
★★`countRecordedDays(entries)` …… ★**夜の 3つが そろった 日**の 数
　★`lib/recordedDay.js` ──「★3つ そろって、はじめて そうです。★1つでも 欠けたら 数えません」

★★★だから 新しい 姿では **数が 減って 見えます**。
　★★坂本さんの 確かめ どころ「★数字が 変わって いないか」に、★まさに 当たります。
　★★★減るのが 正しい（★「記録した日」の 意味が 厳しく なった）か、
　　★それとも 同じで ある べきか ── ★これは お決めの ところ です。

### ★② 13261行 ── ★記録の 出し方が 変わります
```
  const recordModeInUse = layoutV2 ? "full" : profile.record_mode;
```
★★新しい 姿は `full` に 決め打ちます。★その方が 選んだ `record_mode` を 見ません。
　★★「かんたん記録」に して いた 方には、★項目が 増えて 見えます。

### ★③ 18876行 ── ★下の 帯が 変わります
```
  const displayTabs = (layoutV2 ? TABS_V2 : TABS).filter(...)
  TABS_V2 …… home today analysis notes garden（★5つ）
```
★★帯の 数と 並びが 変わります。★行き先が 変わります。

## ★確かめて いない こと

★★★実際の 画面を 開いて 見て いません。★こちらから 本番を 開く 道が ありません。
　★★確かめたのは、★組み上がった ものの 中の 字と、★関数の 返り と、★分かれ道の 数 です。