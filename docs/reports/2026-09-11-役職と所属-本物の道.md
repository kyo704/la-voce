# §3　★役職と 所属 ── ★本物の 道（API）で 確かめた

★この 紙は tools/api_path_posts.py が 書き出します。★手で 書いて いません。

★叩いた 先　`https://woolsong.app/api/org/posts`

★通行証の 検め　状態 `400`　{"error":"足りない指定があります。"}

| 役職 | 期待 | 実際 | 返事 |
|---|---|---|---|
| 学長 | ★通る | ★通る | 門 `400` ／ rename `200`　{"ok":true} |
| 副学長 | ★通る | ★通る | 門 `400` ／ rename `200`　{"ok":true} |
| 事務長 | ★通る | ★通る | 門 `400` ／ rename `200`　{"ok":true} |
| 学部長 | ★通る | ・通らない | 門 `403` ／ rename `403`　{"error":"あなたの役職には「ひとの 役職を 変える」が ありません。"} |
| 学科長 | ・通らない | ・通らない | 門 `403` ／ rename `403`　{"error":"あなたの役職には「ひとの 役職を 変える」が ありません。"} |
| 教授 | ・通らない | ・通らない | 門 `403` ／ rename `403`　{"error":"あなたの役職には「ひとの 役職を 変える」が ありません。"} |
| 准教授 | ・通らない | ・通らない | 門 `403` ／ rename `403`　{"error":"あなたの役職には「ひとの 役職を 変える」が ありません。"} |
| 講師 | ・通らない | ・通らない | 門 `403` ／ rename `403`　{"error":"あなたの役職には「ひとの 役職を 変える」が ありません。"} |
| 課長 | ★通る | ★通る | 門 `400` ／ rename `200`　{"ok":true} |
| 職員 | ・通らない | ・通らない | 門 `403` ／ rename `403`　{"error":"あなたの役職には「ひとの 役職を 変える」が ありません。"} |

## ★期待表と ちがう マス　1 件

- **★学部長**　期待 通る / 実際 通らない　── 門 `403` ／ rename `403`　{"error":"あなたの役職には「ひとの 役職を 変える」が ありません。"}

## ★★学部長に ついて（★1件の ちがい）

★期待表の 決まり　`役職と 所属 ＝ post OR master`
★実装の 門（app/api/org/posts/route.js）

　　function mayTouchPosts(member, perms) {
　　  if (!member) return false;
　　  if (perms) return perms.has("post");      ★← ★ここ
　　  return member.role === "owner";
　　}

★★`post` だけを 見て います。★`master` を 見て いません。

★★学部長の できこと（★lib/opsPerms.js）──
　　bill・meibo・sched_all・gyoji・renraku_all・shukketsu・koma・**master**
　★★`post` が ありません。★`master` は あります。
　★★だから 期待表では 通り、★実装では 止まります。

★★どちらが 正しいかは、★私が 決める ことでは ありません。
　★㋐ 期待表に 合わせる …… ★門を `post` または `master` に する
　★㋑ 実装に 合わせる …… ★期待表の 決まりを `post` だけに する

　★★㋐は 学部長に「ひとの 役職を 変える」を 与えます。★力が 増えます。
　★★㋑は 期待表を 書き換えます。★Opus の お決めです。
　★★**私は どちらも して いません。**

★★この 1件は、★**私の 試し方の 話では ありません。**
　★★画面が 通る 道を、★そのまま 叩いて 出た ちがい です。

## ★この 紙が 見て いる こと・いない こと

★★見て いる　★`rename`（★役職の 名前を 変える）が 通るか。
　★★`mayTouchPosts` を 通る かどうか が 出ます。
　★★名前を 元の ままで 送るので、★中身は 変わりません。

★★見て いない　★`assign`／`perm`。★あれらは §7-4 の 別の 門も 通ります。
　★★1つの 門を 試す ため、★いちばん 単純な ものを 選びました。

★★「門」の 数字の 読み方
　★400 …… ★`mayTouchPosts` を **通った**（★働きの 名前で 落ちた）
　★403 …… ★`mayTouchPosts` で **止まった**
　★401 …… ★通行証が 届いて いない（★この 紙では 出て いません）
