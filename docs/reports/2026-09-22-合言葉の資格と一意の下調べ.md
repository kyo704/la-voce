# 合言葉「claude_code」の 資格 ／ 一意の 決まりの 下調べ（2026-09-22）

## 一 ── 合言葉は、★私が 使って いる ものです

| 見た こと | |
|---|---|
| 出どころ | `~/.bash_profile` の `SUPABASE_ACCESS_TOKEN` |
| その ファイルが 最後に 変わった 時 | **2026-09-18 09:59:21**（★4日前） |
| いま | 2026-09-22 22:37 |
| 頭 | `sbp_…`（★44文字。★中身は 出しません） |

★★★つまり、★私は 合言葉を **作って いません**。★4日前から ある ものを 読んで います。
　★「3分前」は 最後に 使った 時 です。★私の 問い合わせ です。

★★私の 道具が 叩く 口は、★2つ だけ です ──

```
POST /v1/projects/{ref}/database/query        tools/ask_ledger.py
GET  /v1/projects/{ref}/database/migrations   tools/apply_migration.py
POST /v1/projects/{ref}/database/migrations   （★403 で 止まりました）
```

★合言葉を 作る 口（`/v1/oauth/…` など）は、★道具の どこにも ありません。

## 二 ── では、★なぜ 403 なのか

★★★**資格の 幅の 話では ありません。★その 口だけ の 話 です。**

★読む 口は ぜんぶ 通ります ──

```
GET /v1/organizations                       200
GET /v1/projects                            200
GET /v1/projects/{ref}/database/migrations  200   ★読むのは 通ります
GET /v1/projects/{ref}/api-keys             200   ★鍵まで 読めます
GET /v1/projects/{ref}/config/auth          200
GET /v1/projects/{ref}/secrets              200
GET /v1/projects/{ref}/functions            200
GET /v1/projects/{ref}/branches             200
```

★書く 口も、★1つは 通ります ──

```
POST /v1/projects/{ref}/database/query   中身が 正しい …… 200
POST /v1/projects/{ref}/database/query   中身が 壊れた …… 400（★中身の 話）
```

★★★問題の 口 だけ、★**中身が 壊れて いても 403** です ──

```
POST /v1/projects/{ref}/database/migrations  中身が 正しい …… 403
POST /v1/projects/{ref}/database/migrations  中身が 壊れた …… 403   ★同じ
```

★★中身を 見る 前に 断られて います。★つまり **入口で 止まって います**。

★★★読み取り だけ の 合言葉では ありません（★`query` で 書けて います）。
　★★この 口 だけが、★別の 資格を 求めて います。

### ★考えられる こと（★確かめて いません）

- ★この 口が、★Supabase の 道具（CLI）の 口座からの 呼び出し だけを 受ける。
- ★組織の 役（Owner）とは 別の、★口ごとの 許しが 要る。

★★★どちらも 私からは 確かめられません。★Supabase の 画面か、★お問い合わせ です。

### ★回り道（★していません）

★`POST /database/query` で SQL を 流し、★`supabase_migrations.schema_migrations` に
自分で 1行 入れる ことは **できます**。

★★していません。★裁定161 §6 が こう 決めて います ──
「本番の台帳を変えるのは apply_migration だけ。直接の SQL は使わない」。

★★★記録を 正しく する ために 記録を 手で 書く、★では 筋が 通りません。

## 三 ── 05_uniques の 下調べ（★本番を 数えました）

★Opus の 書きぶり …… 「本番の重複0件。そのまま当てられる」。

| 一意に する 組 | 重なり |
|---|---|
| `assignments (org_id, teacher_id, student_id) where ended_at is null` | **0件** |
| `org_billing (org_id)` | **0件** |
| `enrollments (org_id, student_id)` | 0件 |
| `memberships (org_id, user_id)` | 0件 |
| `evaluation_judge_done (event_id, judge_id)` | 0件（★もう 主キーが あります） |
| **`teacher_student_links (teacher_id, student_id)`** | ★★**2件** |

★★★`teacher_student_links` だけ、★重なりが あります。

```
行 …… 11    ちがう (teacher_id, student_id) の 組 …… 9
status …… active 9 ／ revoked 2
revoked_at が 空の ものの 重なり …… ★0件
```

★★★つまり ── ★**切った あとに 結び直した 組** が 2つ あります。

★★`(teacher_id, student_id)` に そのまま 一意を 足すと **落ちます**。
★★`where revoked_at is null` を 付ければ 通ります。

★★★05 に `teacher_student_links` が 入って いるかは、★分かりません。
　★ファイルが まだ 届いて いません（★下）。

## 四 ── ★届いて いない もの

★きょうの お指図に ある もの のうち、★手元に **無い** もの ──

| | 何 |
|---|---|
| 1 | `sql/01_billing_foundation` 〜 `05_uniques`（★5本） |
| 2 | `sql/README.md` |
| 3 | `確定-学校の値段.md` **第4版**（★手元は 第3版。★101行目に「口座振替」が まだ あります） |

★受け取った zip 6本 の 中身を ぜんぶ 見ました。★どれにも 入って いません。

★★★無い ものは 試せません。★作りません。★届いたら すぐ 試します。

## 五 ── 04_profile_consent_guards の CHECK_FIRST（★先に お答えします）

★お尋ね …… 「画面が `line_user_id`・`line_link_code`・`created_at` を 直に 書いて いないか」。

| 列 | 画面 | サーバ |
|---|---|---|
| `line_user_id` | ★**書いて います** …… `VocalTracker.jsx:15899・15906` | `api/line-webhook/route.js:84`（★`service_role`） |
| `line_link_code` | ★**書いて います** …… `VocalTracker.jsx:15893` | 同上 |
| `created_at` | ★**0か所** | ★0か所 |

★★★だから、★`created_at` は いま 足せます。★`line_*` は 先に サーバへ 移す 話 です。

★★きょう、★`created_at` を 含む 5列は **もう 試しの 台帳で 足して あります**
（★`docs/decisions/2026-09-22-束2b-167A3-その1.md`・8/8）。
★★`line_*` の 2列は そこから 外して あります。★04 と 同じ 判じ です。
