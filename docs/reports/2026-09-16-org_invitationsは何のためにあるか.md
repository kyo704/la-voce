# `org_invitations` は 何のために あるか

★出どころ　Opus（2026-09-16）／★調べただけ です。1行も 直して いません。

## 答え ── ★使われて います。★**講師を 招く** ための 表 です

★`advice` と 同じ 形（★道が 在って 誰も 通らない）では ありません。

## Q1　書き込んで いる 経路（INSERT の 呼び手）

★**在ります。**

| どこ | |
|---|---|
| `components/VocalTracker.jsx:12294` | `handleGenerateOrgInvite(orgId)` |
| 同 `:12295` | `.from("org_invitations").insert({ code, org_id, invited_by, expires_at })` |
| 同 `:17480` | それを 呼ぶ 押しどころ |

★★押しどころの 字は ── **「講師を 招待する」**。
★★その 上の 見出しも ── **「講師を招待する」**（`:17476`）。

## Q2　読んで いる 画面

★**在ります。**

| どこ | 何を するか |
|---|---|
| `:17477-17478` | 発行した コードを 画面に 出す（運営の 画面） |
| `:12304` `handleLookupOrgInviteCode` | `/api/org/invitation/lookup` を 呼ぶ |
| `:16943` `:17245` | それを 呼ぶ 押しどころ（★コードを 入れる 口） |
| `:12335` `handleAcceptOrgInvitation` | `/api/org/invitation/accept` を 呼ぶ |

## Q3　生きて いる 行は 何件か

★★**私には 数えられません。** ★台帳を 見る 道が ありません。
★問いを 書きました ── `supabase/問い-org_invitationsは生きているか.sql`。

## ★★私の 前の 報告を、取り消します

★私は こう 書きました ──
　「`org_invitations` は `role` を 持ち（既定 `"teacher"`）、`memberships` に 入れます。
　　→ 講師の 道に 見えます」

★★**`role` の 列は ありません。** ★台帳が そう 答えました。

★★行き先（講師）は 合って いました。★けれど **わけが 誤り**でした。
　★★私は `app/api/org/invitation/accept/route.js:123` の
　　`const role = invitation.role || "teacher";` を 読んで、
　　★「`role` という 列が ある」と 思いました。
　★★実際は ── ★**その 列が 無い ので `undefined` に なり、
　　いつも `"teacher"` に なって いる**だけ です。
　★★答えが 合って いたのは、★偶然 では ありませんが、★読み方は 誤って いました。

## ★ついでに 見つかった こと（★直して いません）

★`invitation.role || "teacher"` の `||` は、★**永久に 右側**です。

★★このままだと、★次に 読む 人も 私と 同じ 誤解を します。
　★★「役を 選べる ように 見えて、★実は 選べない」形 です。
★★直すなら 2つ ──
　★㋐ `const role = "teacher";` と 書き、★わけを 添える
　★㋑ `org_invitations` に `role` の 列を 足す（★選べる ように する）
★★どちらも お決め が 要ります。★私からは 変えません。

## ★消すか どうか

★★**消さないで ください。** ★使われて います。
　★講師を 招く 道は、★これ 1本 です。
★★No.017 で 決まりを 締めた とき 0件 だった のは、
　★その ときに まだ 誰も 招いて いなかった から と 思われます。
　★★いまの 件数は、★上の 問いで 数えて ください。
