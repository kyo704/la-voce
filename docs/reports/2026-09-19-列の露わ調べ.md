# ★列の 露わ 調べ ── ★裁定 その93 の 横断

★2026-09-19 ／ ★`tools/column_exposure_audit.py` が 書きました。
★全62行 ／ 末尾は「★★この 表は、★お尋ねする ところの 一覧 です。★答えでは ありません。」

★★★決まり（RLS）は **行** を 守ります。★**列** は 守りません。
★★行を 1つ 読める 方には、★その 行の 列が 全部 渡ります。

## ★調べた 範囲

★決まりの ある 表 …… **49**
★そのうち、★学生・生徒ご本人・結びついた 先生・自分の コマ の
★どれかで 開いて いる 表 …… **5**

## ★当たり ── ★気に なる 列を 持つ 表（2）

| 表 | 気に なる 列 | 画面が 頼んで いるか | 在籍 | ご本人 | 結びつき | 自分の コマ |
|---|---|---|---|---|---|---|
| `lessons` | `note`／`created_by` | 頼む／★頼んで いない | ー | ○ | ○ | ○ |
| `org_events` | `created_by` | ★頼んで いない | ○ | ○ | ー | ー |

## ★すでに ある 読み道（`security definer`）

| 名 | 返す もの |
|---|---|
| `accept_teacher_invitation` | uuid |
| `admin_entry_stats` | jsonb |
| `are_connected` | boolean |
| `assert_student_is_adult` | trigger |
| `assignments_old_identity` | TABLE(org_id uuid, teacher_id uuid, student_id uuid) |
| `can_grant_post` | boolean |
| `can_view_ops` | boolean |
| `can_view_ops_perm` | boolean |
| `can_view_organization` | boolean |
| `character_unlock_summary` | jsonb |
| `consent_withdrawn` | boolean |
| `create_org_event` | uuid |
| `get_connected_names` | TABLE(id uuid, display_name text, vocal_profession text) |
| `get_invitation_teacher` | jsonb |
| `get_lesson_preset_for_student` | TABLE(name text, total_count integer) |
| `get_monka_free_counts` | TABLE(student_id uuid, free_count integer) |
| `get_monka_read_notices` | TABLE(viewed_at timestamp with time zone, teacher_name text) |
| `get_my_teacher_names` | jsonb |
| `get_org_member_names` | TABLE(user_id uuid, display_name text, display_title text, role |
| `get_student_free_slots` | TABLE(weekday smallint, period_ord smallint, is_free boolean) |
| `handle_new_user` | trigger |
| `has_can` | boolean |
| `has_can_user` | boolean |
| `is_org_member` | boolean |
| `is_org_owner_or_admin` | boolean |
| `leave_enrollment` | integer |
| `purge_code_attempts` | integer |
| `set_member_display_title` | jsonb |

## ★この 調べに できない こと

★★★列の 名 だけ を 見て います。★中身は 見て いません。
★★名が おとなしくても、★中に 覚え書きが 入る 列は あり得ます。
★★★「その 役目に 要るか」は、★人が 決める こと です。★機械は 当てられません。
★★「画面が 頼んで いる」列は、★見せる つもりの もの かも しれません。
★★★頼んで いない のに 渡って いる 列 こそ、★露わ です。
★★この 表は、★お尋ねする ところの 一覧 です。★答えでは ありません。
