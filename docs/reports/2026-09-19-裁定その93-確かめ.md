# ★裁定 その93 ── ★列は 読み道 でしか 渡りません

★2026-09-19 ／ ★`tools/ruling93_verify.py` が 書きました。
★全40行 ／ 末尾は「★★★やめた 方（`left`）には、★0行 返ります。」

## ★決まり ── ★在籍の 枝を 取り消しました

| 表 | 決まり | いつ | 在籍 | 事務 | 門下 |
|---|---|---|---|---|---|
| lesson_preset_targets | `lesson_preset_targets_select` | r | ー | ○ | ○ |
| lesson_preset_targets | `lesson_preset_targets_write` | * | ー | ○ | ー |
| lesson_presets | `lesson_presets_select` | r | ー | ○ | ○ |
| lesson_presets | `lesson_presets_write` | * | ー | ○ | ー |

★★★裁定 その92 で 足した 枝は、★1本も 残って いません。

## ★読み道

| | |
|---|---|
| 名 | `get_lesson_preset_for_student` |
| 決まりを 越えるか | はい（`security definer`） |
| 綴りの 道 | `search_path=""` |
| 返す もの | `TABLE(name text, total_count integer)` |
| 呼べる 方 | authenticated |

★`lesson_presets` の 列 …… `id`／`org_id`／`name`／`total_count`／`note`／`created_by`／`created_at`／`updated_at`／`need_count`

★★★台帳の 外に 出るのは、★そのうち **2つ** だけ です。
★★出ない もの ── `note`（事務の 覚え書き）／`need_count`（足りると される 回数）
★★／`created_by`（誰が 作ったか）／`id`／`org_id`／`created_at`／`updated_at`。

★★★`need_count` を 渡さない わけ ── ★足りる 回数が 分かると、
★★「足りない 見込み」の 出どころが 読めます。★率に 近づきます（★裁定 その90）。

## ★門は どこに あるか

★★決まりを 越えて 読む ので、★門を **関数の 中** に 立てて います。
★★`enrollments`（`status = 'active'`）に 居る 方 だけ が 中身を 受け取ります。
★★★やめた 方（`left`）には、★0行 返ります。
