# もっているもの ── 見本との 違いの 調べ（★実装していません）
全169行 / 末尾は「実装していません。ご判断をお待ちします。」

- 日付 … 2026-09-15
- もっとの先 6枚目
- 道具 … `python3 tools/screen_vs_mihon.py もっているもの`
- 読んだ … `components/OwnedLedger.jsx`／`lib/itemLedger.js`／`components/VocalTracker.jsx`

## 0. 名前のこと（★先に）

見本79枚に「もっているもの」という名の**画面はありません**。

見本は、こうなっています。

| 見本 | 何か |
|---|---|
| `SH['motteru']` | 「もっているもの」という題の**シート**（4行の入口） |
| `SC['台帳']` | 題は「台帳」。戻る先が「‹ もっているもの」 |
| `SC['全部']` | 題は「全部」。同上 |
| `SC['まだ']` | 題は「まだ 見えていないもの」。同上 |

アプリは、この4つを**1枚にまとめて**います。
題は「もっているもの」、中に［台帳］［ぜんぶ］の2つの札。

`tools/audit-source.js:61` が、前から `SC-台帳 → OwnedLedger.jsx` と
書いていました。道具にも同じ対応を登録しました。

## 1. 数え直し

足りない … **14 / 15**。ただし、その内訳は次のとおりです。

| 種類 | 件数 | 中身 |
|---|---|---|
| 見本の**作り物のデータ** | 12 | はおり／マフラー／めがね／傘／ニット帽 と、その日付5つ、手に入れた理由5つ |
| ★本当の違い | 2 | 下の §2 ㋐㋑ |

作り物のデータ12件は、見本が絵を見せるために置いた例です。
実装するものではありません。数から外して読んでください。

## 2. 本当の違い（★2件）

### ㋐ 台帳の行に、「何で 手に入ったか」がありません

見本の1行 ──
```
はおり                      2026年9月2日
記録が 50日に なった日
```
名前の下に、手に入れた**理由**が小さく入ります。

アプリの1行 ──
```
はおり            9月8日　記録 60日
```
右に「日付＋そのときの数」。**理由の行がありません**。

見本のシートも、入口の札にこう書いています ──
「台帳（**いつ・何で** 手に入ったか）」。
「何で」のほうが、アプリには出ていません。

### ㋑ 「連続日数を 出しません。」の1行がありません

見本の下の注記（2行） ──
```
いつ・何で 手に入ったかを 残します。
記録を 消しても 減りません。連続日数を 出しません。
```

アプリ ──
```
いつ 手に入ったかと、そのときの 数が 残ります。
（別の枠）記録を 消しても、手に入れたものは なくなりません。
　　　　　数え直して 減ることは ありません。
　　　　　いちど あなたのものに なったものは、あなたのものです。
```

言い方は、アプリのほうが長く、やわらかいです。
ただ「**連続日数を 出しません**」という約束だけが、どこにもありません。

これは禁止事項（進捗バーの一種）に関わる約束です。
`lib/character.js:173` には「★すべて累計ベース。『連続◯日』を条件にしないこと」と
コメントで書いてありますが、**画面には出ていません**。

## 3. わざと違えているところ（★消さないでください）

### ㋒ 「まだ 見えていないもの」を、まるごと消しました

見本 `SC['まだ']` は「8つ あります」と数を出します。
アプリには、この画面がありません。

出どころ … `docs/opus/回答-とだなの数字はどこか（9月10日）.md` §3-2・§3-3
＋ 坂本さんのお決め（2026-09-11）
「『置ける点数』、『まだ』の 枠、これらは、今後も、実装しないでください。
　進捗バーの一種として、禁止事項に該当します」

`lib/itemLedger.js` の末尾に、消した3つ（UNSEEN_TILES／unseenKeys()／
UNSEEN_HINTS）と理由が残してあります。**直さないでください。**

### ㋓ 「全部」の見せ方を、逆にしました

見本 `SC['全部']` … 全36個の枠を並べ、持っていないものは「まだ」と出す。
　　　　　　　　　頭に「28／36」。注記「持っていないものも 場所だけ 見せます」。

アプリ「ぜんぶ」 … **持っているものだけ**を並べる。数を出さない。
　　　　　　　　　注記「持っているものだけを 並べます。まだ 手に入れていない
　　　　　　　　　ものは、枠も 数も 出しません」。

同じ2026-09-11のお決めです。理由は「枠があれば数えられる」。
これも**直さないでください**。

### ㋔ 見本にない行を、1つ足しています

「これより 前に 手に入れた N点は、いつ 手に入れたかは、残っていません。」

2026-09-11より前に手に入れたものには、日付がありません。
きょうの日で埋めていません（migration §5）。
「日が無い」と「きょうもらった」は別のことだからです。

### ㋕ 「たな（歌ってきたもの）」は、別の場所にあります

見本のシートは4行目に「たな（歌ってきたもの）3曲 ›」を置いています。
アプリでは、羊のおうちの引き出しの中にあります（`lib/homeDrawer.js:91`）。
もっているものの中ではありません。場所の違いだけで、機能はあります。

## 4. 調べていて見つかった別件（★N-1の破れ）

`lib/character.js:177` の `UNLOCK_CONDITIONS` は、**誰も読んでいません**。

```
export const UNLOCK_CONDITIONS = {
  firstPerformance: { labelKey: "unlockFirstPerformance" },
  ...
};
```

- 読んでいるのは `components/tests/unlock-reachable.test.js` だけです。
  つまり、見張りのためだけに存在しています。
- 中の `labelKey` 5つは、`lib/translations.js` に**1つもありません**。
  出そうとしても、鍵の文字がそのまま出ます。

規則「作った関数は、必ずどこかから呼ばれているか」に触れています。

★ここが ㋐ と つながります。
理由の行を出すなら、この5つの言葉が要ります。
品との対応は `lib/sheepWardrobe.js:515` の `UNLOCKS` にあります。

```
firstPerformance  → propBouquet      初めての本番 → 花束
performances10    → tailcoat         本番10回   → 燕尾服
firstPianissimo   → propBaton        初めてのピアニッシモ → 指揮棒
tenFieldKinds     → propMetronome    10種類の記録 → メトロノーム
practiceGoalDone  → hatCamellia      稽古の目標達成 → 椿の髪かざり
```

ただし `item_acquisitions` が持つ `acquired_by` は
`shop／gift／unlock` の3つだけで、**どの条件で開いたかは残っていません**。
理由の行を出すには、品の鍵から `UNLOCKS` を逆に引くことになります。
買ったもの・贈りものには、理由がそもそもありません。

## 5. ご判断をいただきたいこと

| 印 | 何を | 私の見立て |
|---|---|---|
| ㋐ | 台帳の行に、手に入れた理由を足すか | 足せます。ただし §4 の言葉5つを先に書く必要があります。買ったものは「買いました」、贈りものは「贈りもの」でよいか、ご判断ください |
| ㋑ | 「連続日数を 出しません。」の1行を足すか | 足すほうがよいと思います。禁止事項の約束で、いまは画面に出ていません |
| ㋒㋓㋔㋕ | わざと違えているところ | 現状維持。直しません |
| ㋖ | §4 の `UNLOCK_CONDITIONS`（誰も読んでいない） | ㋐ を足すなら使います。㋐ を足さないなら、消すか台帳に残すかのご判断を |

実装していません。ご判断をお待ちします。
