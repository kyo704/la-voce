# ★羊の 部屋 ── ★Q1〜Q5 の 答え（★コードを 読んだ もの）

全114行 / 末尾は「★★それまで 直しに 入りません。」

★★これは **コードを 読んだ** もの です。★測った 数は 別の 紙に あります ──
`docs/reports/2026-09-17-羊の部屋-実機で測った数.md`（★`tools/room_place_probe.js` が 書きました）。

## Q1 ★`stageFit` ／ `stageSize` は 何を 計算して いますか

`lib/roomStage.js`。★どちらも **入れ物（箱）の 実寸**を 受け取り、
★**舞台の 実寸**（`{w, h}`）を 返します。★比は いつも `STAGE_ASPECT = 7/5`。

| | 入力 | 出力 | 考え方 |
|---|---|---|---|
| `stageSize(boxW, boxH, 1.4)` | 箱の 幅・高さ | 舞台の 幅・高さ | ★箱を **覆う**（はみ出しは 切る）|
| `stageFit(boxW, boxH, 1.4)` | 同じ | 同じ | ★箱に **収める**（余白が 出る）|

★★縮尺も 原点も 返しません。★返すのは **大きさ だけ** です。
★★置き場は `stageStyle`（153行）が 決めます ──
`left:50% / top:50%` ＋ `translate(-50%,-50%)` ＋ `stageShiftX` `stageShiftY`。

★★★ここに 食い違いが あります。
`stageStyle` は、★中で **いつも `stageSize`** を 呼びます（154行）。
★けれど 呼ぶ 側（`CharacterHome.jsx:2276`）は、
★`fullBleed` の とき `stageFit` を 使って います。
★★`CharacterHome.jsx:2439` が 幅と 高さを `stageFit` の 値で 上書きしますが、
★横の ずらし `dx` は `stageStyle` の 中で **`stageSize` の 幅**から 出た まま です。

★★ただし ── ★**実機で 測ると、舞台は 2つの 画面で 同じ 大きさ でした**
（390×278.56・比 1.4）。★この 端末では 2つの 手が 同じ 値を 返して います。
★★比の ちがう 端末（★横長・iPad）では 分かれる 見込みが あります。★未測 です。

## Q2 ★配置の ときと 全画面の ときで、★入力は 変わりますか

★変わります。★**箱の 高さ**が 変わります。

- ながめる … 箱 390 × **634**
- したく　 … 箱 390 × **278.56**

★★引き出しが 出ると、★箱が 低く なります。
★★そして **計算し直して います** ── `roomBoxRef` を `ResizeObserver` で 見て、
★`roomBoxW` / `roomBoxH` を 入れ直し、★舞台を 出し直して います。
★★結果、★舞台は どちらも 390×278.56 に なりました。★ここは 揃って います。

## Q3 ★置いた ものの 座標は、★何で 持って いますか

★★**㋑ 入れ物の ％** です。

- 掴んだ とき（`CharacterHome.jsx:1576`）──
  `container.getBoundingClientRect()` を 取り、
  `pctX = (clientX - rect.left) / rect.width * 100`。
- `container` は、★その もの の **親**（★舞台の 中の `inset:0` の 1枚）。
- 保存（`VocalTracker.jsx:9794 handleUpdatePosition`）──
  `{ left: 小数1桁, top: 小数1桁 }` を `characterEquipped.<種類>Positions[鍵]` に 入れます。

★台帳 … `profiles.character_equipped`（★JSON 1つ）。
★★`left` / `top` の **％ だけ** です。★px も、★舞台の 大きさも、
★どの 画面で 置いたかも、★**残して いません**。

★★★％ そのものは 悪く ありません ── ★**何に 対する ％か**が 一定なら。
★★いまは 舞台の 中の `inset:0` に 対する ％ です。★舞台は 比が 決まって います。
★★だから 理屈の 上では ずれません。★2026-09-14 の 舞台方式は、★そこを 直した もの です。

## Q4 ★保存する ときと 描く ときで、★同じ 縮尺を 使って いますか

★★**使って いません。★測りました。**

★★実機（iPhone 12・390×844・本番）──
★同じ ものが、★**ながめるでは したくの 1.600倍**の 大きさで 描かれて います。
★9つ 測って、★9つ とも 1.600倍 でした。

★★舞台は 同じ 大きさ（390×278.56）です。★なのに 中身が 1.6倍。
★★ちがうのは **カメラ（寄り）**です ──
`CharacterHome.jsx:2318` … `zoom: Math.max(1, ZOOM / (stage.w / roomBoxW))`。
★`stage.w / roomBoxW` は どちらも 390/390 ＝ 1。★つまり `zoom = ZOOM = 1.6`。
★★したくでは 1.0 に なって います。★なぜ 1.0 に なるかは **まだ 追えて いません**。

★★★カメラは 舞台の **中**で かかります。
★★掴む ときに 測る `rect` も、★カメラの かかった あとの 形 です。
★★だから **理屈の 上では ％は 合います**。★倍率は 割り算で 消えます。
★★けれど、★これは **測って 確かめた ことでは ありません**。

## Q5 ★2026-09-14 の 舞台方式は 何を 解決しましたか

★記録 … `lib/roomStage.js` の 頭（★1〜33行）。★坂本さんの お言葉が 引かれて います ──

> 「したくの 画面は、★ながめるの 画面と 別々に 計算された
> 　似た 見た目では なく、★**同じ 場面を そのまま 拡大して 切り抜いた もの**に して ほしい」

★★直した もの ── ★**箱の 比が 2つ あった** こと。
　★ながめる … 画面いっぱい（端末しだい）
　★したく　 … `aspectRatio: 4/3` の 固定
★★同じ ％でも、★比が ちがえば 床の 線からの 見え方が ずれて いました。
★★羊の 大きさも 同じ ％で 渡して いて、★29.4％ と 63.5％ に なって いました。

★★**いま その 解決は 生きて います。**★実機で 測りました ──
★舞台は 2つの 画面で 同じ 390×278.56、★比 1.4。

★★つまり「再発」では ありません。★舞台の ところは 直った まま です。
★★残って いるのは、★**その 上に 乗る カメラ**の 側 です。

## ★いま 言える こと・言えない こと

★言える ──
- 舞台は 揃って いる（★測った）
- 中身が 1.600倍 ちがう（★測った）
- 座標は 入れ物の ％で、★台帳には ％ だけ（★読んだ）
- `stageStyle` が `stageFit` を 見て いない（★読んだ・★この 端末では 影響なし）

★言えない ──
- ★**置いた 家具**が ずれるか どうか。★試しの 口に 家具が 1つも 無く、★測れて いません。

★★家具を 1つ 置いて いただければ、★同じ 道具で すぐ 測れます。
★★それまで 直しに 入りません。
