# ログインの現状 ── Opus からの4つの確認（2026-09-04）
全155行 / 末尾は「★★これは★帰国後の作業です。★いまは何もしません。」

★出どころ `docs/opus/lavoce-判断-メールを失うこと（9月4日）.md`

★★2つは★コードで確かめました。★2つは★私には見えません。
　★見えないほうは、★照会をお願いします。

---

## ① パスワードだけで入れるか ── ★★入れます

    app/login/page.js:168
      supabase.auth.signInWithPassword({ email, password })

★★メールに触らずに入れます。★これが、いまの主な道です。

★ただし、★登録のときには★メールの確認を通ります。

    components/SignupForm.jsx:137,158
      supabase.auth.signUp({ …, options: { emailRedirectTo: …/auth/callback } })

★★つまり
    ★はじめの1回だけ、★メールが要ります。
    ★その後は、★パスワードだけで入れます。★メールは要りません。

★★ここが、いまの「守り」です。
　★メールを失っても、★パスワードを覚えていれば、入れます。
　★★6桁だけにすると、★この守りが消えます。★Opus のご指摘のとおりです。

---

## ② パスワードの再設定に、メール以外の道があるか ── ★★ありません

    app/login/page.js:154   resetPasswordForEmail(email, { redirectTo })
    app/reset-password/page.js:62  updateUser({ password })

★★メールだけです。★ほかの道は、1つもありません。

★★つまり、★いまでも
    ★メールを失い、★かつ★パスワードを忘れた方は、★入れません。
    ★★その方を救う道は、★いまも★ありません。

★★これは大事な点です。
　★6桁にすると「新しく」入れなくなる、のではありません。
　★★入れなくなる★条件が、★1つ減るのです。
    いま   メールを失い、★かつ★パスワードを忘れたとき
    6桁後  メールを失ったとき
　★★だから、★合鍵（recovery code）が要ります。

---

## ③ セッションの持ち時間 ── ★★私には見えません

★コードでは、★決めていません。

    lib/supabase/client.js / server.js  ★何も指定していません（★既定のまま）
    lib/supabase/admin.js:10            persistSession: false（★サーバ用・別物）
    middleware.js                       ★毎回 getUser() を呼び、cookie を更新

★★持ち時間は、★Supabase の設定で決まります。
　★Authentication ＞ Sessions ／ JWT expiry。
　★★ダッシュボードでご確認ください。

★確かめていただきたいこと

    ・JWT の持ち時間（★既定は3600秒のはずです）
    ・Refresh token の持ち時間（★既定は無期限に近いはずです）
    ・「Inactivity timeout」が設定されているか
    ★★長いセッションにするなら、★ここを触ることになります。

---

## ④ ★パスワードで入っている方が、どれだけいるか ── ★★照会が要ります

★★これがいちばん効きます。★ほぼ0なら、移行は簡単です。

★`auth.users` の `last_sign_in_at` は、★入り方を持っていません。
★入り方は、★`auth.audit_log_entries` に残ります。

```sql
-- ★読むだけです。★1行も書き換えません。
-- ★★人数は、この照会では出しません（2026-09-02 の凍結）。
--   ★見えるのは、行の数です。★私が数を書き残しません。
select
  payload->>'action'  as "action",
  date_trunc('day', created_at)::date as "日",
  count(*) as "回数"
from auth.audit_log_entries
where created_at > now() - interval '90 days'
group by 1, 2
order by 2 desc, 3 desc;
```

★見たいのは、★`login` の行の中身です。★もう1つ。

```sql
-- ★どの入り方が使われているか。
select
  payload->>'action'                    as "action",
  payload->'traits'->>'provider'        as "provider",
  count(*)                              as "回数"
from auth.audit_log_entries
where created_at > now() - interval '90 days'
  and payload->>'action' in ('login', 'token_refreshed', 'user_signedup')
group by 1, 2
order by 3 desc;
```

★★`auth.audit_log_entries` は、★保存の期間が決まっています。
　★古いものは消えます。★90日で足りない場合は、期間を広げてください。

★★もし `login` の行が★ほとんど無ければ
    ★みなさん、★cookie が続いているうちに入り直しているだけ、ということです。
    ★★つまり、★パスワードを実際に打っている方は、ほとんどいません。
    ★★そのときは、★移行は★ずっと簡単になります。

---

## ★合鍵（recovery code）について ── 読みました

★★理解した形

    ・登録のときに★1回だけ見せる（例 WOOL-3F7A-92KD-1XQP）
    ・★サーバは★ハッシュだけを持つ。★元の文字列を持たない
    ・★1回きり。★使ったら無効
    ・★ログインしているあいだは、★作り直せる

★★「困ったときの窓口」は★作らない、も理解しました。
　★遠くから本人だと確かめる道が、★ありません。
　★★9か月、判断できる人がいない状態では、★窓口はかえって危ういです。

★★入り直しを求める4つの操作も、理解しました。
    書き出し／退会／メールアドレスの変更／★合鍵の作り直し
★「この端末を信用する」は★作りません。★共有の端末があるためです。

★★新しく始める方に、★パスワードを選ばせません。★6桁だけです。
　★いまお使いの方のパスワードは、★消しません。
　★★切り替えを選んだ方だけ、★一方通行になります。

---

## ★羊の件（★第6便の撤回）── 取り込みました

    docs/opus/lavoce-訂正-羊は本物が正（9月4日）.md
    ★★第6便（季節の服16点）は★撤回です。★使わないこと。
    ★本物の羊には★腕がありません。★毛のひとつづきの形です。
      ★巻きつけるもの（襟巻き・ケープ・ショール・前掛け・帽子）だけが合います。
      ★★仕立てた服（コート・ベスト・シャツ）は、★形として合いません。
    ★16点のうち★4点だけを作り直します。
    ★★これから作るときは、★1点ずつ。
      ★★本物の PNG（sheep-front-1024-transparent.png）と★突き合わせること。
      ★★私が描いた SVG と、★突き合わせないこと。

★★これは★帰国後の作業です。★いまは何もしません。
