# 教室の予定に備考をつける ── 実装前の調べ（2026-09-03）
全94行 / 末尾は「★断言（見えません／絶対に）も入っていません。」

★実装していません。★スロット⑥です。

---

## ★カレンダーへ出る心配 ── ★いまは、道がありません

★2つの理由で、備考は Google カレンダーに出ません。

### 理由1　教室の予定に、カレンダーへ出す道が★1つもありません

    AddToCalendarButtons が付いているのは★レッスンだけです。
      VocalTracker.jsx:2685   （レッスンの一覧）
      VocalTracker.jsx:13589  （教室のレッスン）
    ★components/OrgEventList.jsx に、カレンダーの語は1つもありません。
    ★出している列は event_date / kind / title / previous_date / id だけです。

### 理由2　レッスンの側は、★入れられない形にしてあります

    lib/calendarExport.js
      CALENDAR_ALLOWED   = ["start", "end", "title"]   ★これで全部
      CALENDAR_FORBIDDEN = note / teacher_note / teacherName /
                           studentName / location / throat / symptom / health

★`buildGoogleCalendarUrl` も `downloadLessonICS` も、★題名を引数に取りません
（2576 / 2591）。★呼ぶ側が名前やメモを入れる余地がありません。
★「入れないでください」と書くのではなく、★入れられない形にしてあります。

### ★いま出ているもの（レッスン）

    日付・時刻・一般的な題名（★誰の名前も入りません）
    ★.ics も同じです。★落ちる先が違うだけで、出ていくものは同じです。

---

## ★これから危ないところ（★ここが本当の指摘です）

★誰かが「教室の予定にもカレンダーに追加を付けよう」と思ったとき、
★`buildGoogleCalendarUrl` は使えません。★org_events は `scheduled_at` を
持たず、`event_date` だからです。★新しい組み立てを書くことになります。

★★その新しい組み立ては、★いまの検査（calendar-export.test.js）の
外にあります。★備考がそこから出ても、★誰も気づきません。

★ですから、備考を足すときに、★同時に次を1本入れてください。

    ★教室の予定をカレンダーへ出す道ができたら落ちる検査
      ・OrgEventList.jsx と、予定を描く場所に
        buildGoogleCalendarUrl / downloadLessonICS / VCALENDAR が
        現れたら、★落とす
      ・現れてよいのは、★CALENDAR_ALLOWED を通る形にしたときだけ

★★備考が書き出しに乗るのは、★はっきり決めたときだけです。
　★黙って乗ることが、無いようにします。

---

## 足す列（★7つの台帳の手順どおりに）

    org_events.note text

    01 削除処理      ★関係ありません（org_events は組織に属し、
                     　organizations の削除に cascade します）
    02 バックアップ  ★org_events が入っているかを確かめること
    03 auth 参照     ★関係ありません（note は文字です）
    04 書き出し      ★本人の書き出しに org_events は入っていません。
                     　★入れるかどうかは、★別の判断です
    05 外に出る経路  ★上のとおり。★いまは出ません
    06 起きたこと    ★作るときは関係ありません
    07 約束          ★「教室の人が読める」と書くこと

    権限   grant update (note) を、いまの列ごとの grant に足す
           ★org_events は列ごとの UPDATE になっています
           ★足さないと、42501 で★文ごと落ちます（2026-09-03 の件と同じ形）
    書ける人  ★オーナーと教室の責任者だけ（org_events_write_admin のまま）
           ★作った人だけ、という新しい段は作りません
    トリガー  trg_org_event_identity_immutable は org_id と created_by だけを
           見ています。★note を足しても影響しません（★実物で要確認）
    関数   create_org_event に p_note を足す（★省略できる形で）

---

## 入力欄の言葉（案・★ご確認ください）

    当日の段取りを書く欄です。部屋、オンラインの案内、持ち物など。
    ここに書いたことは、教室の方が読めます。
    体調のことや、ほかの方のお名前は、別の場所へ。

★「書かないでください」と叱る形にしていません。
★「読めます」という★事実だけを置いて、判断は書く人に渡します。
★急かす言葉（まだ／忘れ／…）は入っていません。
★断言（見えません／絶対に）も入っていません。
