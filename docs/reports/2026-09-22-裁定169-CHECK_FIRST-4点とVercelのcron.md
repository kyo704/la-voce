# 裁定169 の CHECK_FIRST 4点 ／ Vercel の cron（2026-09-22）

---

## ★★★まず、★前の 報告を 取り消します ── cron の 本数

★きのう（この 日の 昼）、★私は こう 書きました ──

```
★vercel.json に cron が 5本。★Hobby の 本数の 上限を 超えます。
★★つまり すでに Pro か、★いくつかの cron が 黙って 動いて いないか。
```

★★★**これは 誤り でした。**

★Vercel の 文書（`/docs/cron-jobs/usage-and-pricing`・2026-07-15 更新）──

| | 1つの プロジェクトの cron の 数 | いちばん 短い 間 | 時刻の 正しさ |
|---|---|---|---|
| **Hobby** | **100本** | 1日 1回 | 1時間の 幅（±59分） |
| Pro | 100本 | 1分 に 1回 | 分 まで |

★★「cron は 100本まで、★どの プランでも」に 変わって いました
（`Cron jobs now support 100 per project on every plan`）。

★★★つまり **5本は 上限の 内 です**。★どれも 止まって いません。
　★坂本さんが 画面で 見て くださった とおり、★5本とも 1日 1回 で 登録されて います。

★★私が 見て いたのは **古い 上限（2本）** です。★確かめずに 書きました。

### ★残る 注意 …… 時刻が ±59分 ずれます

| cron | 書いて ある 時刻 | 実際に 動く 幅（JST） |
|---|---|---|
| `pricing-notice` | 01:00 UTC | 10:00 〜 10:59 |
| `keep-alive` | 12:00 UTC | 21:00 〜 21:59 |
| `purge-deleted` | 03:30 UTC | 12:30 〜 13:29 |
| `purge-events` | 06:00 UTC | 15:00 〜 15:59 |
| `line-reminder` | 22:00 UTC | 07:00 〜 07:59（★翌日） |

★★`pricing-notice` は「今日」を UTC で 作って います（★裁定168 ★4 の 報告）。
　★10:00〜10:59 JST の あいだ、★UTC は 01:00〜01:59 で **同じ 日** です。★いまは ずれません。
　★★時刻を 変えた 日に ずれます。★「たまたま 合って いる」の まま です。

★★★`line-reminder` が 7時台 に 届きます。★ローマに 居られる ときは **前日の 深夜** です。

---

## 1 ── Vercel の いまの プラン

★★坂本さんの お調べで **Hobby** です（★私からは 測れません）。

★★★個人向けの 販売の 前に Pro が 要ります。★理由は **本数では なく 規約** です ──
　★Hobby は 商用が 禁じられて います。★課金を 始めた 日から、★確かな 違反に なります。
　★★予告なく 止められます。★その とき 38人が 使えなく なります。

★★これは 裁定132・裁定170 の とおり です。★出発（10/19）の 前に。

---

## 2 ── Pro に した とき、★試しの プロジェクトを Free に 残せるか

★★★**残せません。★組織を 分ける 必要が あります。**

★Supabase の 払いは **組織ごと** です。★プロジェクトごとでは ありません。

```
組織 ajwrscitducvqhwsxrjg（plan = free）
  ├ xxjtplvpcneksrofkjmf  ★本番（38人）
  └ smntpurraumeerselvsc  ★試し（la-voce-test）
```

★★この 組織を Pro に すると、★2つ とも Pro の 側に 入ります。

### ★分ける 手順（★坂本さんの お手元で）

```
① 新しい 組織を 作る（Free の まま）
     Supabase の 画面 → 左上の 組織の 名 → New organization
② 試しの プロジェクトを そちらへ 移す
     試しの プロジェクト → Settings → General → Transfer project
③ もとの 組織（本番 だけ に なる）を Pro に する
```

★★★`smntpurraumeerselvsc` という 名（ref）は **変わりません**。
　★`.env.local` も 道具も、★1文字も 直す 必要が ありません。

★★★分ける ほうを お勧めします。★理由 ──
　1. 試しの 台帳には 本番より 広い 権限が 残って います
　　（`org_messages` に TRUNCATE・DELETE、`character_inventory` に TRUNCATE。★2026-09-22 に 見ました）。
　　★同じ 組織に あると、★鍵 1つで 両方に 届きます。
　2. 試しを 止める とき、★本番を 触らずに 済みます。
　3. いま 移すのが いちばん 安い です。★試しの 中身は 捨てても 困りません。

---

## 3 ── 学校の 管理者に 多要素の 認証を 求められるか

### ★いまの 姿

| | |
|---|---|
| 入り方 | `signInWithPassword`（★メールと 合言葉） |
| 多要素の 仕組み | ★**コードに 0か所**（`mfa` / `aal` / `totp` を 探しました） |
| `recovery_codes` | ★本番に **9行** |
| `code_attempts` | ★本番に 8行 |

### ★★`recovery_codes` は 多要素では ありません

★`lib/recoveryCode.js` の 頭に、★作った わけが 書いて あります ──

```
★メールを 失った 瞬間に 締め出されます。★だから 代わりを 1つ 置きます。
★★コードを 持っている 人が、★本人です。★これが、この 仕組みの 全部です。
```

★★★これは **入口を 増やす** もの です。★守りを 増やす ものでは ありません。
　★12文字（60ビット）で、★メールも 合言葉も 要らずに 入れます。
　★★多要素に 使うと、★逆に 弱く なります。

### ★★できるか …… できます。★ただし 作る ことに なります

★Supabase の 認証には TOTP（スマホの 認証アプリ）の 仕組みが あります
（`supabase.auth.mfa.enroll` / `challenge` / `verify`）。

★★★台帳の 側で「多要素で 入った 人 だけ」に 絞る ことも できます ──

```sql
-- ★aal2 ＝ 2つ目の 鍵まで 通った 印
(auth.jwt() ->> 'aal') = 'aal2'
```

★★これを `has_can(org_id, 'master')` の ような 強い できことに 重ねる 形 です。

### ★★★決めて いただく こと（★私は 決めません）

- ★誰に 求めるか（★`master` を 持つ 人 だけ か、★学校の 人 ぜんぶ か）
- ★求める 前に、★**入れなく なる 人を 作らない** 手当て
  ★★スマホを 失った 方が 入れなく なります。★`recovery_codes` は 使えません
  　（★あれは メールの 代わり であって、★2つ目の 鍵では ありません）。
- ★いつ から（★学校向けの 販売は 2027年4月 です）

★★★**先に すべき こと が 1つ あります** ── ★坂本さん ご自身の Supabase の 口座 です。
　★`mfa_enabled = false`。★38人の 記録に、★合言葉 1つで 届きます。
　★★学校の 管理者に 求める 前に、★こちらが 先 です。

---

## 4 ── 年度の 切り替えを、★名簿の 読み込みで 直す 前提か

★★★**いまは、その 道が ありません。**

★`grade_year` が 流れる 道 ──

```
名簿の 読み込み → roster_drafts.grade_year
                    ↓（招く とき・VocalTracker.jsx:12624）
                  teacher_invitations.grade_year
                    ↓（受けた とき）
                  enrollments.grade_year
```

★★★**1度だけ** です。★招く ときに 入り、★そのあと 触る 道が ありません。

★★名簿を もう 一度 読み込んでも ──
　★すでに 在籍して いる 方は `roster_drafts` に **入りません**
　　（★12650行の 註 …… 「②まだ 居ない 方 … `roster_drafts`（下書き）に 入ります」）。
　★★つまり、★**進級しません**。

★本番の 数 …… `enrollments`（active）20行・そのうち `grade_year` が 入って いる **0行**。

### ★★決めて いただく こと

★裁定168 ★4 の 報告に 書いた 4つ です。★ここでは 1つ 足します ──

```
⑤ 進級を「名簿の 読み込み」で するなら、
   ★すでに 在籍して いる 方も 下書きに 出す 形が 要ります。
   ★★いまの 決めは 逆（居る 方は 出さない）です。★わざと そう して あります。
     ★★二重に 招く ことを 防ぐ ため です。
   ★★★変えるなら、★「招く」と「学年を 直す」を 別の 操作に します。
```

★★★引き金 …… `enrollments.grade_year` に **最初の 1行** が 入る 日。
　★いま 0行 です。★その 前に 決めれば、★直す 行は 0行 です。

---

## ★裁定169 の ADD_TO_ORDER について（★下調べ だけ）

| | 何 | いまの 本番 |
|---|---|---|
| `subscription_items` | 1人が 複数の 月払いを 持てる 形 | ★表が **ありません**（`subscriptions` 49行・1人1行） |
| `stripe_events` | `event_id` を 主キーに して 二重受けを 止める | ★表が **ありません**。★いま webhook は 二重受けを 止めて いません |
| `assignments` の 一意の 索引 | `(org_id, teacher_id, student_id) where ended_at is null` | ★ありません（★28行） |
| `org_billing` の 一意 | `(org_id)` | ★次に 見ます |
| 管理の 操作の 記録 | 誰が・いつ・何を | ★`post_change_log` と `org_post_perm_log` は あります |

★★`stripe_events` が 無い ことは、★★**いま すでに 効いて います**。
　★Stripe は 届かなかった と 思うと 送り直します。★同じ 知らせを 2度 受けます。
　★★いまは `upsert` で 上書きして いる ので 害が 小さい です。
　★★★けれど「解約 → 再契約」の 順が 入れ替わると、★解約が 後から 効きます。
