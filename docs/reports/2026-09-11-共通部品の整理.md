# 共通の 部品の 整理（★㋑の 第1段）

全121行 / 末尾は「269本、落ちた もの 0件。★build・lint 通過。」

コードに 入れました。見張りは 通りました。見た目は 未確認です。実機で お確かめください。

**A01 は まだ 直して いません**（★部品の 整理が 終わってから、との お指図）。

## 1　洗い出し ── 見本の 部品は 143種、よく 使うのは 24種

見本の `<style>` から 数えました。★私が 書き写して いません。

| 部品 | 見本で 使われた 回数 | 実装に あったか |
|---|---|---|
| `.usu` 小さな そえ字 | 177 | **✗ 無い** |
| `.li` 一覧の 行 | 161 | ○ Li |
| `.btn` ボタン | 148 | △ 主だけ。`.g`（枠だけ）`.sm`（小）が **無い** |
| `.pill` 札 | 114 | ○ Pill |
| `.note` 注記 | 110 | ○ Note |
| `.hd` 画面の 頭 | 96 | ○ ScreenHead |
| `.box` 白地の 入れもの | 90 | **✗ 無い** |
| `.h3` 小見出し | 86 | ○ H3 |
| `.fl` 入力欄の 見出し | 85 | **✗ 無い** |
| `.card` カード | 81 | ○ Card |
| `.warn` 断りの 箱 | 51 | △ **色が ちがう** |
| `.wl` 白い 断りの 帯 | 27 | **✗ 無い** |
| `.two` 横に 2つ | 26 | **✗ 無い** |
| `.inp` 1行の 入力欄 | 23 | **✗ 無い** |
| `.sht` 1枚の 題 | 21 | **✗ 無い** |
| `.mini` 小さな 見出し | 20 | ○ TYPE.mini |
| `.ta` 書く枠 | 16 | **✗ 無い** |
| `.empty` 何も 無い ときの 枠 | 15 | **✗ 無い** |
| `.tag` 小さな 札 | 12 | **✗ 無い** |
| `.sw` 切替 | 9 | **✗ 無い**（★写しが 2か所） |
| `.gear` 頭の 丸 | 9 | ○ HeadRound |
| `.back` 戻る | 8 | **✗ 無い**（★写しが 6か所） |
| `.bar` 棒 | 8 | ○ BarRow |
| `.seg` 切替の 帯 | 1（★関数） | ○ Seg |

**13の 部品が、★1つも ありませんでした。**

## 2　写しの 数（★直す 前）

| 形 | 写しの 数 |
|---|---|
| `cardStyle` を じかに 書いて いる | **28** |
| `TYPE.usual` を じかに 書いて いる | **25** |
| 枠だけの ボタン | **20** |
| 入力欄 | **12** |
| 戻る（‹） | **6** |
| 書く枠（textarea） | **3** |
| 切替（44×26） | **2** |

★★これが、★一斉の 確かめで「余分な もの 20件 超え」が 12画面 あった 理由です。

## 3　足した 部品（★13）

`Box`（.box）／ `Usu`（.usu）／ `FieldLabel`（.fl）／ `Wl`（.wl）／ `Two`（.two）／
`Input`（.inp）／ `TextArea`（.ta）／ `SheetTitle`（.sht）／ `EmptyBox`（.empty）／
`Switch`（.sw）／ `Back`（.back）／ `Btn`（.btn / .btn.g / .btn.sm）／ `Tag`（.tag）

値は **見本の CSS の まま**です。★2つだけ、★わざと ちがえて います。

1. **`ink3` を 小さい字に 使いません**（★2026-09-10・坂本さんの お決め）。
   ★見本が `var(--ink3)` と 書いて いる ところ（.usu / .fl / .h3）も `ink2` に します。
2. **入力欄・書く枠の 字は 16px**（★見本は 13.5 / 13px）。
   ★iOS は 16px 未満の 入力欄で、★触ると 画面を 拡大します。
   ★`app/globals.css` が すでに そう して います。★そろえました。

★★書く枠の `resize` も 変えました。★見本は `none` です。
　★長い 文を 書く 方が います。★縦だけ 伸ばせる ように しました。★取り上げに しません。

## 4　見つかった 不具合 ── `Warn` の 色

見本と ちがって いました。

| | 実装（★前） | 見本 |
|---|---|---|
| 地 | `C.paper` **#F6F1E7** | **#F6F1E4** |
| 枠 | `C.line` **#E4DCC9** | **#E8DFC8** |

★★近い 色ですが、★同じでは ありません。
　★★地が **下地と 同じ 色**だったので、★**箱に 見えて いませんでした**。
　★見本は、★下地より わずかに 濃くして 箱に 見せて います。

★`Warn`（.warn）と `Wl`（.wl・#F6EFDF）は **別の 箱**です。★1つに まとめません。
★見本が 2つを 使い分けて います。

## 5　差し替えた ところ（★第2便）

| 画面 | 差し替えた もの |
|---|---|
| くらべる | 戻る（.back）／ 切替（.sw） |
| ノート | 戻る／ 入力欄 ×2／ 書く枠／ 見出し（.fl） |
| ふりかえる | 枠だけの ボタン ×2／ 横に 2つ（.two） |

★くらべる の 戻るは、★**色も ちがって いました**（★ink2 ／ 見本は えんじ）。

## 6　見張り

`components/tests/ui-parts.test.js`（★**82**）を 作りました。

- **見本の CSS を その場で 読んで くらべます。** ★数を 書き写して いません。
  ★見本が 変われば、★ここが 落ちます。
- 写しの 形（★44×26 の 切替・「‹ もどる」・`const ghostBtn`）が
  ★戻って いないかを 見ます。
- あわせて、★**部品を 使って いる ことも 見ます**。★片道の 見張りに しません。
- わざと ちがえた 2つも、★見本の 値と 並べて 書いて あります。

★★1つ 落とし穴が ありました。★見本の CSS を 引くとき、
　★「`.mkbox{`」のような 長い 名前に 当たらない ように、★前の 1文字を 見て います。
　★はじめ「`};,`」だけを 許して いて、★覚え書きの 終わり（★`/`）を 落として いました。
　★`.fl` と `.sw` を 取り逃して いました。★直しました。

## 7　この あと

1. 残りの 写し（★`cardStyle` 28／`TYPE.usual` 25）を、★画面ごとに 差し替えます。
2. そのあと、★A01（きょう）の 4つの お決めに 着手します。
3. 以降、★1画面ずつ 仕上げます。

269本、落ちた もの 0件。★build・lint 通過。
