# ★名簿の 画面は、★誰かを 出せるのか

★この 紙は tools/roster_empty.py が 書き出します。★手で 書いて いません。

## ① ★名簿に 渡される もの

```js
// components/VocalTracker.jsx
const members = orgMembers[opsOrgId] || [];
```

★★`orgMembers` は `memberships` を そのまま 入れた ものです ──
```js
supabase.from("memberships").select("*").eq("org_id", orgId)
```

★★`enrollments` は **別の 入れもの**に 入り、★名簿には 渡されません。

## ② ★名簿が 行を 出さない 決め

```js
// lib/orgRoster.js
export const NOT_COUNTED_ROLES = Object.freeze(["teacher", "staff", "owner", "admin"]);

export function matchesChip(member, chipKey) {
  if (!member) return false;
  if (NOT_COUNTED_ROLES.includes(member.role)) return false;
  if (!chipKey || chipKey === "all") return true;
  return (member.status || "enrolled") === chipKey;
}

/** ★「すべて」を いちばん 先に した、★先生の 一覧（★G07 の 1枚）。 */
export const TEACHER_FILTER_ALL = "all";

/**
 * ★担当の先生ごとの 人数。
 *
 *   ★★1人の 生徒が 2人の 先生に つくことが あります（★見本「三浦・小林」）。
 *     ★その 生徒は、★どちらの 先生の 数にも 入ります。
 *     ★★足すと 名簿の 人数を 超えます。★それで 正しいです。
 *       ★「先生ごとに 何人 見ているか」を 数えているからです。
 *   ★★先生・事務は、★数えられる 側には 立ちません。
 *
 * @param {object[]} members
 * @param {(id:string)=>string} nameOf  ★先生の 名前
 * @returns [{ id, label, count }]  ★すべてが 先頭
 */
export function teacherFilterOptions(members, nameOf) {
  const count = new Map();
  (members || []).forEach((m) => {
    if (!m || NOT_COUNTED_ROLES.includes(m.role)) return;
    (m.teacher_ids || []).forEach((id) => {
      if (!id) return;
      count.set(id, (count.get(id) || 0) + 1);
    });
  }
```

★★1行目で 落ちます。★`owner` `admin` `teacher` `staff` の 4つ とも。

```js
// components/OpsRoster.jsx
const list = useMemo(() => {
    const s = q.trim();
    return (members || []).filter((m) => {
      if (!matchesChip(m, chip)) return false;
```
★★数えるだけでは ありません。★**行そのものを 出しません。**

## ③ ★台帳が 許す `role` の 値

```
memberships_role_check
CHECK ((role = ANY (ARRAY['owner', 'admin', 'teacher', 'staff'])))
★空でよいか … NO（★NOT NULL）
```
★★2026-09-13、★坂本さんが 台帳に 尋ねて くださった 答えです。

## ★★④ 食い違い

★★名簿に 出るのは「4つの どれでも ない 方」です。
★★台帳が 許すのは「4つの どれか」だけ です。★空も 許しません。

★★つまり ── ★**名簿は、★どの 学校でも、★誰も 出せません。**

## ⑤ ★見張りは 何を 期待して いるか

★`components/tests/org-roster.test.js` が 使って いる `role` の 値 ──

- `admin`　★台帳も 許します
- `owner`　★台帳も 許します
- `staff`　★台帳も 許します
- `student`　★★台帳が 許しません
- `teacher`　★台帳も 許します

★★見張りは `student` を 前提に 書かれて います。
　★★台帳に その 値は ありません。
　★★見張りは 作り物の 行を 使うので、★通って しまいます。
　★★台帳を 見て いないからです。

## ⑥ ★`status` の ほうも

★★`matchesChip` は `member.status`（★enrolled／paused／invited）を 見ます。
★★`memberships` に `status` という 列は **ありません**
　（★2026-09-11 に 10列を 数えました ──
　★id／org_id／user_id／role／created_at／display_title／
　★display_title_updated_by／display_title_updated_at／grade_label／post_id）。
★★だから 札（ようす）の しぼり込みも、★はじめから 働きません。

## ★私が まちがえた こと

★★はじめ「種まきが 足りない」と 見立てました。
　★★`role='teacher'` で 相手役を 入れれば 出る、と 思い込んで いました。
★★次に「`role` を 空に すれば よい」と 申しました。
　★★`NOT NULL` でした。★坂本さんが 止めて くださいました。
★★**どちらも、★名簿が 誰を 並べるのかを 読まずに 言いました。**

## ★これから どう するか（★私は 決めません）

★★台帳を ゆるめる ことは、★ここでは **しません**。
　★★`NOT NULL` も 4つの 値も、★わざと そう して ある かも しれません。
　★★分からない まま 広げるのが、★いちばん 危ない ことです。

★先に 決めて いただきたい こと ──

　★㋐ 生徒は `memberships` に 居るのか、★`enrollments` に 居るのか
　★㋑ 居るのが `enrollments` なら、★名簿は そちらを 読むべきでは ないか
　★㋒ `memberships` に 居るなら、★`role` に 5つ目（★student）が 要る
　★　　★その ときは `status` の 列も 要ります

★★どちらに しても、★これは ⑥の 段取りの 話では なく、
　★★**名簿の 画面が 誰も 出せない**という 話です。

## ★⑥は どこまで 見られるか

★(a) 学部長が 人の 役職を 変える　　→ ★**見られません**（★名簿が 空）
★(b) 学長が 変える　　　　　　　　→ ★**見られません**（★同じ 画面）
★(c) 持って いない 鍵を 渡そうと する → ★**見られます**
　★★設定の タブ（OpsPosts）は、★名簿を 通りません。
　★★課長に 付け替えれば、★いま すぐ ご覧いただけます。

★★(a)(b) を 見るには、★先に 上の ㋐㋑㋒ の お決めが 要ります。
