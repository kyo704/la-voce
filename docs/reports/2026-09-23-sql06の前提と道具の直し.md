# sql/06 の 前提 ／ `ledger_inventory.sql` の 直し（2026-09-23）

## 一 ── sql/06 の 前提 …… ★2つ 外し、★1つは **外せません**

### 外した もの（★画面から の 直の insert）

| | どこ | どう したか |
|---|---|---|
| `org_billing_log` | `VocalTracker.jsx` ご請求の 宛名 | ★消しました。★引き金（`org_billing_log_change`）に 任せます |
| `org_billing_log` | `VocalTracker.jsx` 支払い方法 | ★同じ |
| `export_log` | `VocalTracker.jsx` 書き出し | ★`record_export(…)` を 呼ぶ 形に しました |

★★書き出しの「★先に 記録し、★残せなければ 出さない」は そのまま です。
　★1つでも 残せなければ、★書き出しへ 進みません。

### ★★★外せなかった もの ── `post_change_log`（★sql/06 ① に 問いが あります）

```
app/api/org/posts/route.js:79   admin.from("post_change_log").insert({ … changed_by: by })
```

★★これを 消して 引き金に 任せると、★**誰が 変えたかが 消えます**。

★わけ ──

```
sql/06 ① の 引き金 …… insert … changed_by = auth.uid()
                        -- サーバ（service role）の変更は null

役職を 変える 道 …… app/api/org/posts/route.js
  33行・161行・203行・291行 …… ★ぜんぶ `admin`（service role）です
    → ★引き金の 中の `auth.uid()` は **null** です
```

★★★つまり、★`post_change_log.changed_by` が **いつも 空** に なります。
　★裁定160 の「誰が・いつ・何を」の「誰が」が 消えます。

★本番の `post_change_log` は **0行** です。★過ぎた 分は 失われません。

### ★Opus へ ── 案（★決めて いません）

| | 案 | |
|---|---|---|
| ㋐ | `post_change_log` は 引き金を 作らず、★サーバの insert の まま | ★いちばん 小さい |
| ㋑ | サーバが `set_config('app.actor', uid, true)` を 置き、★引き金が それを 読む | ★書く 人は 1つに なる |
| ㋒ | 引き金は `auth.uid()` が null の とき **書かない**。★サーバが 書く | ★2つの 道が 残る |

★★私は ㋑が 筋だと 思います。★けれど 決めません。

---

## 二 ── `ledger_inventory.sql` を 直しました（★お許しを いただいた ので）

★★註を **閉じ括弧の 前** から、★**前の 行** へ 移しました。★1行 動かした だけ です。

```sql
-- 前
…'e',(… ~* '…')   -- ★表として参照しているときだけ（…）))
                   ↑ ★ここから 行末まで 註。★`))` が 消える

-- 後
-- ★表として参照しているときだけ（…）
-- ★★註を この 行に 置きます。★閉じ括弧の 前に 置くと、★`--` が `))` を 飲みます
'e',(… ~* '…')))
```

### ★流れました。★A4・A7 は **0件** に なりました

```
A3 …… 1件（export_log.user_id が CASCADE）
A5 …… 8件（移行の 名前。★中身は きょう 消しました）
A8 …… 20件（表ごとの 書き込み権）
A4 …… ★0件   ← ★許可リストに 2つ 足した ぶん が 効いて います
A7 …… ★0件   ← ★語の 区切りで 合わせる 直しが 効いて います
```

---

## 三 ── ★★FX7 の 土台 ── 本番と 試しが **48か所 ちがいます**

★`ledger_inventory.py twin` で 数えました。

| ちがい | 数 |
|---|---|
| `anon` が 実行できるか | **37** |
| `authenticated` が 実行できるか | 3 |
| 関数の 中身 | 4 |
| 本番に だけ ある | 2 |
| 試しに だけ ある | 2 |

★★★いちばん 多いのは「★試しでは `anon` が 呼べる」です。

```
has_can / has_can_user / leave_enrollment / retire_teacher /
mark_attendance / move_lesson / send_message_draft / …
  → ★本番 False ／ 試し **True**
```

★★★**試しの ほうが 開いて います。**

★これが 何を 意味するか ──
　★「閉じて いる」ことを 試しで 証しても、★本番で 同じとは 限りません。
　★★逆も あります …… ★試しで 通った 動きが、★本番で 通らない。

★★★FX7（試しを 本番から 作り直す）が 要る 理由が、★数で 出ました。
　★★きょうの 証しの 多くは「試しで 通した」もの です。★土台が 揺れて います。
