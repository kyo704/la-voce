# coreFillNote ── 取り除いたものの原文と、出ていた条件
全121行 / 末尾は「★これは事実のご報告です。★戻すかどうかの判断は、★Opus と坂本さんにお任せします。」

宛先 Opus ／ 2026年9月7日
★2026-09-07 に、③（助言をやめる）と一緒に取り除いたものです。
★推測ではありません。★git から、そのまま取り出しました（HEAD~3）。

---

## ① 出ていた文（★これが全文です）

```js
return `${CORE_FILL_LABEL[key]}は、まだ${n}日ぶんです`;
```

★実際に出ていた形は、この5通りだけです。

```
睡眠時間は、まだ4日ぶんです
練習以外で話した時間は、まだ4日ぶんです
気温と湿度は、まだ4日ぶんです
その日の活動は、まだ4日ぶんです
むくみは、まだ4日ぶんです
```

★★名前の一覧は、こう決まっていました。

```js
const CORE_FILL_LABEL = {
  sleepHours: "睡眠時間",
  offStageVoiceMinutes: "練習以外で話した時間",
  absoluteHumidity: "気温と湿度",
  dayAfterPerformance: "その日の活動",
  morningEdema: "むくみ"
};
```

---

## ② 出ていた条件（★全文）

```js
const coreFillNote = useMemo(() => {
  const total = Object.keys(entries).length;
  if (total < 3) return null;               // 記録がごく少ないうちは何も言わない
  const behind = Object.entries(coreFillCounts)
    .filter(([, n]) => n < total)            // 記録した日より少ないものだけ
    .sort((a, b) => a[1] - b[1]);            // いちばん少ないものを1つだけ
  if (behind.length === 0) return null;
  const [key, n] = behind[0];
  if (n >= NARRATIVE_MIN_N_PER_GROUP * 2) return null;  // 十分たまったら黙る
  return `${CORE_FILL_LABEL[key]}は、まだ${n}日ぶんです`;
}, [coreFillCounts, entries]);
```

★言葉にすると、こうです。

```
・記録が3日に満たないうちは、出さない
・「記録した日数」より少ない項目だけを見る
・そのうち、いちばん少ないものを1つだけ出す
・その項目が20日ぶん（NARRATIVE_MIN_N_PER_GROUP × 2）たまったら、もう出さない
```

---

## ③ 出ていた場所

**ホームの「今日やるといいこと」の枠です。** ★同じ枠を、助言と分け合っていました。

```js
const todayOneLine = todaySuggestion || coreFillNote;
```

★★助言（todaySuggestion）が優先で、★助言が無い日にだけ coreFillNote が出ました。
　★元の覚え書きには、こう書かれていました。

> ★新しい枠は作りません（§2-4「既存の枠の使い道を変えるだけ」）。
> 　予報からの提案が無い日にだけ、中核の埋まり具合を1行出します。
> 　★両方を同時に出さないこと。1つだけ、が §3.3 の決めごとです。

★出る条件は、さらにもう1つありました。

```js
{todayOneLine && !isRecordedToday && ( … )}
```

★★**今日まだ記録していない日にだけ**、出ていました。
　★記録したあとは、消えていました。

---

## ④ 作った側の覚え書き（★当時、そのまま書かれていたもの）

> ★事実だけを書きます（憲章 §8-1・中核5項目 §2-4）。
> 　「記録しましょう」「あと◯日です」と書かないこと。催促は、
> 　調子が悪い日の記録率を下げます。

★つまり、★作った時点で「催促にしない」ことは意識されていました。
　★「◯◯しましょう」とは書かず、★「まだ◯日ぶんです」という
　★事実の形にしてありました。

---

## ⑤ 実装の側から、1つだけ申し添えます

★★「まだ」という言葉は、★このリポジトリの別の見張りに引っかかります。

`components/tests/no-nagging-words.test.js` が、★急かす言葉を止めています。
★2026-09-07、★レパートリーの画面に
「まだ曲名が書かれていません」と書いたところ、★この試験に止められました。

```
★急かす言葉: VocalTracker.jsx「まだ曲名が…」← まだ
```

★★coreFillNote の文（「まだ◯日ぶんです」）も、★同じ言葉を使っています。
　★当時どうして通っていたのかは、★確かめていません。
　★★もし戻される場合は、★「まだ」を使わない言い方が要るかもしれません。

★これは事実のご報告です。★戻すかどうかの判断は、★Opus と坂本さんにお任せします。
