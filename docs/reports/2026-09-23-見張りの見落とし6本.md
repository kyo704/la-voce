# ★★★訂正 ── ★「ぜんぶ 通りました」は 誤りでした（2026-09-23）

## 何を まちがえたか

★私は きょう 何度も「見張りは ぜんぶ 緑 です」と 申し上げました。
★★**誤り でした。**★数えかたが 足りて いません でした。

```
私の 数えかた …… 画面に 出る 字を 探す
    `✗ N` ／ `N件 落ちました` ／ `RESULT: NG` ／ `NG `

★★ところが、★見張りごとに 言い方が ちがいます ──
    `❌ 失敗あり  成功:29 失敗:1`
    `❌ 2 件`
    `★止まりました ── …`
    ★そもそも 落ちて 何も 出ない（★Node が 止まる）

★★★だから **6本の 赤**を、★ずっと 見落として いました。
```

★直しました …… `tools/run_guards.py`。★字で 探すのを やめ、
★**終わりの 番号（exit code）だけ**を 見ます。

```
★見張り 451本 ／ ★落ちた 6本
```

---

## 直した もの（★1本）

### `contrast` ── ★止まって いました。★26の 確かめが 死んで いました

```
TypeError [ERR_UNSUPPORTED_RESOLVE_REQUEST]:
  Failed to resolve module specifier "@/lib/visualTokens"
```

★`lib/tokens.js` は 2026-09-18 から `@/lib/visualTokens` を 読みます。
★★`data:` の 形で 読み込むと、★`@/` を 結べません。

★★★これは **色の 見やすさ**（★比 4.5）の 見張り です。
　★止まって いる あいだ、★読めない 色が 入っても 誰も 気づきません。
★ほかの 見張りと 同じ 形（★`@/lib/x` を `file://` に 直す）に しました。
★→ ★成功 26 ／ 失敗 0。

---

## 残り 5本（★どれも きょうの 作業より 前から 赤 です）

| 見張り | 中身 | 見立て |
|---|---|---|
| `type-steps` | `★小さい 字が あります: 10.5` | ㋑ ★見本が 12.5px に 動いた ぶん。★台帳12 §二㋑ と 同じ 一群 です |
| `bill-breakdown` | `見本に billPlans が ありません` | ㋑ ★新しい 運営の 見本は `billPlan`（単数）です。★名が 変わりました |
| `repertoire-identity` | `record を 生の 名前で 引いて いない` | ㋐ ★中身を 見て いません |
| `repertoire-edit-delete` | `全部 空には できない` | ㋐ 同上 |
| `repertoire-pitch-entry` | `handleSaveRepertoire は 3つの どれか 1つで 受け付ける` | ㋐ 同上 |
| `wardrobe-v2-not-yet` | `あつ森様式310点 は zip の まま` | ★★決めが 変わりました ── ★坂本さんは 2026-09-21 に「ほどく」に お決めに なりました。★見張りの ほうが 古い です |

★★★`wardrobe-v2-not-yet` は、★決めが 変わった ぶん です。
　★★直すには「ほどいた あとの 姿」を 見る 形に 変えます。★見張りの 向きを 変える ので、
　　★勝手に しません。★お伺いします。

★★`repertoire-*` の 3本は 中身を まだ 見て いません。★次に 見ます。

---

## ★これから

```
★報告の たびに `python3 tools/run_guards.py` を 走らせます。
★★「ぜんぶ 緑」と 申し上げる ときは、★この 道具の RESULT だけ を 根拠に します。
```
