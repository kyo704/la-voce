# いま 確かめられない こと
全101行 / 末尾は「  ★試し用の 別の 企画（Supabase project）でも 足ります。」

★出どころ 2026-09-14、★No.013 ITEM_4（★SEVERITY: HIGH）

## 何が できて、★何が できないか

| | できるか | 理由 |
|---|---|---|
| 見本の HTML を 開いて 撮る | ○ | Google Chrome を 借りて います |
| 色を 測る・くらべる | ○ | 上と 同じ |
| **アプリの 画面を 出す** | **×** | `.env.local` が ありません |
| **作りの 不具合を 確かめる** | **×** | 上と 同じ |

★★だから、★この 便の「確かめました」は **見本の 色だけ** です。
　★アプリの 画面は 1枚も 見て いません。

## 足りない もの ①　`.env.local`

★★`npm run dev` が 500 を 返します。★`middleware.js` が 落ちます ──

```
Error: Your project's URL and Key are required to create a Supabase client!
    at Object.middleware (middleware.js:18)
```

★★最低 この 2つが 要ります。

| 名前 | 手もとに |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | **なし** |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | **なし** |

★★どちらも Supabase の 管理画面 → Settings → API に あります。
　★`anon` の 鍵です。★`service_role` では ありません。

★★★お願いの 形。★私は 本物の 鍵を 預かりません。
　★坂本さんが `.env.local` を 作って ください（★`.gitignore` 済み）。
　★作って いただければ、★私の 側で 画面を 出して 撮れます。

## 足りない もの ②　画面を 撮る 道具

| | |
|---|---|
| playwright（道具本体） | あり |
| playwright の Chromium | あり |
| Google Chrome | あり |

★★`npx playwright install chromium` は 通りません ──

```
Error: Playwright does not support chromium on mac12
```

★★この 機械は macOS 12 です。★新しい playwright は 見て いません。
　★いまは `channel: "chrome"` で、★入って いる Chrome を 借りて います。
　★★これで 足ります。★止まって いる 理由は ①だけ です。

## そのほかの 環境変数（★あれば できる ことが 増えます）

| 名前 | 手もとに | 使う ところ |
|---|---|---|
| `AI_ADVICE_ENABLED` | なし | `app/api/advice/route.js` |
| `ANTHROPIC_API_KEY` | なし | `app/api/advice/route.js` |
| `CRON_SECRET` | なし | `app/api/cron/keep-alive/route.js` |
| `FEEDBACK_FROM_EMAIL` | なし | `app/api/account/email/route.js` |
| `LINE_CHANNEL_ACCESS_TOKEN` | なし | `app/api/cron/line-reminder/route.js` |
| `LINE_CHANNEL_SECRET` | なし | `app/api/line-webhook/route.js` |
| `NEXT_PUBLIC_BUILD_AT` | なし | `app/layout.js` |
| `NEXT_PUBLIC_BUILD_SHA` | なし | `app/layout.js` |
| `NEXT_PUBLIC_GATE_TEST_USER_IDS` | なし | `app/api/version/route.js` |
| `NEXT_PUBLIC_LAYOUT_V2_USER_IDS` | なし | `app/api/version/route.js` |
| `NEXT_PUBLIC_LINE_ADD_FRIEND_URL` | なし | `components/VocalTracker.jsx` |
| `NEXT_PUBLIC_OPS_FIX_ALLOWLIST` | なし | `app/api/version/route.js` |
| `NEXT_PUBLIC_SECOND_COLOR_USER_IDS` | なし | `app/api/version/route.js` |
| `NEXT_PUBLIC_SITE_URL` | なし | `app/api/cron/pricing-notice/route.js` |
| `NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA` | なし | `app/api/version/route.js` |
| `NEXT_PUBLIC_WARDROBE_ALL_ITEMS_USER_IDS` | なし | `app/api/version/route.js` |
| `NEXT_PUBLIC_WARDROBE_USER_IDS` | なし | `app/api/version/route.js` |
| `NEXT_PUBLIC_X` | なし | `components/tests/ops-fix-gate.test.js` |
| `NODE_ENV` | なし | `components/tests/email-restrictions.test.js` |
| `REQUIRE_SUBSCRIPTION` | なし | `app/billing/page.js` |
| `RESEND_API_KEY` | なし | `app/api/account/email/route.js` |
| `STRIPE_PRICE_ID_ANNUAL` | なし | `app/api/stripe/checkout/route.js` |
| `STRIPE_PRICE_ID_FULL` | なし | `app/api/stripe/webhook/route.js` |
| `STRIPE_PRICE_ID_MONTHLY` | なし | `app/api/stripe/checkout/route.js` |
| `STRIPE_SECRET_KEY` | なし | `app/api/stripe/checkout/route.js` |
| `STRIPE_WEBHOOK_SECRET` | なし | `app/api/stripe/webhook/route.js` |
| `SUPABASE_SERVICE_ROLE_KEY` | なし | `lib/supabase/admin.js` |
| `VERCEL_BRANCH_URL` | なし | `lib/baseUrl.js` |
| `VERCEL_ENV` | なし | `app/api/version/route.js` |
| `VERCEL_GIT_COMMIT_MESSAGE` | なし | `app/api/version/route.js` |
| `VERCEL_GIT_COMMIT_SHA` | なし | `app/api/version/route.js` |
| `VERCEL_URL` | なし | `lib/baseUrl.js` |
| `X` | なし | `components/tests/ops-fix-gate.test.js` |

## この 数えが 見て いない こと

- 鍵の 中身は 見て いません。★名前が あるかだけ を 見ます。
- 本番の 鍵を 手もとに 置く ことは、★お勧めして いません。
  ★試し用の 別の 企画（Supabase project）でも 足ります。
