# 専門的な 記録項目は、分析で 使われているか

全125行 / 末尾は「- 列も 記録も、★1つも 消して いません。」

## 0　お答え（結論）

**使われていません。記録されているだけです。**

ただし、**名前の よく似た 別の ものが 1つ あり、そちらは 使われています。**
混同しやすいので、分けて 書きます。

| | 内部の 名前 | どこに 入るか | 分析で 使うか |
|---|---|---|---|
| ① パッサッジョの**通りにくさ** | `passaggioDifficulty` | `entries.type_fields` | **使いません** |
| ① 高音の**出しやすさ** | `highNoteEase` | `entries.type_fields` | **使いません** |
| ② パッサッジョの**通過感** | `passaggioFeel` | `activities[].detail` | **使います**（★門の外だけ） |

## 1　①「お仕事に合わせた記録」の 5項目 ── 1つも 使われていません

`lib/typeFields.js` が 持つ 5つです。

| 内部の 名前 | 画面の 名前 | 型 |
|---|---|---|
| `passaggioDifficulty` | パッサッジョの通りにくさ | 1〜5 |
| `highNoteEase` | 高音の出しやすさ | 1〜5 |
| `scriptVolume` | 台詞・原稿の分量 | 少ない／ふつう／多い |
| `longestTalkMinutes` | 連続してしゃべった最長の時間 | 分 |
| `projectedVoiceMinutes` | 遠くへ飛ばした時間 | 分 |

### 1-1　調べ方

この 5つの 名前を、★帳面 ぜんぶで 探しました（★見張りの ぶんを 除く）。

    grep -rn "passaggioDifficulty|highNoteEase|scriptVolume|
              longestTalkMinutes|projectedVoiceMinutes"

**当たったのは `lib/typeFields.js` の 5行だけ**です ── **自分の 定義です**。
読んでいる ところが、★1つも ありません。

### 1-2　入れ物（`type_fields`）を 触る ところ

名前では なく、★入れ物 ごと 探した 結果です。

| どこ | 何を して いるか | 分析か |
|---|---|---|
| `VocalTracker.jsx:1823` | 読み出し（`row.type_fields` → `entry.typeFields`） | ちがう |
| `VocalTracker.jsx:2321` | 書き込み（`entryToRow`） | ちがう |
| `VocalTracker.jsx:10744` | その方に 出す 項目を 決める | ちがう |
| `VocalTracker.jsx:14629〜14675` | 記録の 入力欄 | ちがう |
| `lib/exportData.js` | 書き出し（ご本人の 持ち出し） | ちがう |
| `lib/ownRecordFields.js:134` | 「自分の記録」の 一覧に 名前を 出す | ちがう |
| `lib/shareScope.js:84` | **先生に 渡さない** 列の 一覧 | ちがう |
| `lib/entryColumns.js:44` | 列の 名簿 | ちがう |
| `lib/recordV2.js:274` | 節の 置き場所の 表 | ちがう |

### 1-3　分析の 側

次の 帳面を 探しました。**0件**です。

    lib/analysisFamilies.js　★検定の 族
    lib/lagChoice.js　　　　 ★くらべる の 項目
    lib/lineUp.js　　　　　　★ならべる の レーン
    lib/compareView.js　　　 ★くらべる の 中身
    lib/displayGates.js　　　★出してよいかの 門

**ふりかえる・くらべる・かぞえる の どれにも 入っていません。**

### 1-4　仕様も そう 書いて います

`lib/typeFields.js` の 冒頭（★出どころ
`docs/lavoce-作業指示-職業を声の型で切り直す.md` §9）──

> ★ここで足す項目は、当面は記録だけです（§9）。
>   検定に入れると族が増え、検出力が落ちます。explore に置きます。
>   分析の族（lib/analysisFamilies.js）には、1つも入れないでください。

**仕様と 実装が、一致して います。**

## 2　②「パッサッジョの 通過感」── こちらは 使われて います

名前が 似ていますが、**別の ものです。**

- 内部の 名前　`passaggioFeel`
- 置き場所　　 `activities[].detail`（★本番・リハーサル の 中）
- 出る 相手　　声楽の 方だけ
- 画面　　　　 記録 → 本番・レッスン → 本番／リハーサル の 中

### 2-1　何に 使われて いるか

`VocalTracker.jsx:8403` の `passaggioStability` が 計算します。

- 28日ぶんの **中央値**と **MAD**（★頑健統計）
- 直近 3日が すべて z ≦ −1.5 なら、★お知らせを 出す
- 14日ぶん 無いと、★出ません

描くのは `VocalTracker.jsx:18243`、★「パッサッジョの安定度」の カードです。

### 2-2　★ただし、門の 中では 出ません

このカードは `VocalTracker.jsx:18033` の

    {activeTab === "analysis" && !layoutV2 && (

の 中に あります（★18033〜19727 が その 塊です）。

- **門の外（38人）** … 出ます
- **門の中（名簿の 方）** … 出ません。★ふりかえる（LookBackV2）に 置き換わって います

### 2-3　値が 入って いるか

`VocalTracker.jsx:768` の 覚え書き（★2026-08-28 に 数えた もの）──

> ★対照（passaggioFeel）も0件でした。

**当時、1件も 入って いませんでした。**
いま 何件 あるかは、★私には 数えられません（★台帳を 見られません）。

## 3　まとめ

- 「パッサッジョの通りにくさ」「高音の出しやすさ」を 含む **専門の 5項目は、
  書かれて いるだけ**で、★分析には 1つも 使われて いません。
- 名前の 似た 「パッサッジョの通過感」は 使われて いますが、
  ★**門の外の 古い 分析画面 だけ**です。★門の中では 出ません。
- 画面から 隠しても、**分析で 失われる ものは ありません。**
- 列も 記録も、★1つも 消して いません。
