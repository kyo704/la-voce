# 合言葉（Supabase の Access Token）の 権限を 見直して いただきたい（2026-09-23）

★坂本さんへ。★お手元でしか できない こと です。★お手数を おかけします。

---

## 一 ── 何が 困って いるか

★本番の 台帳を 変えた のに、★**移行の 記録に 残せて いません**。

★いま、★記録の 外に ある 本番の 変更が **13本** あります。

| | 何 | いつ |
|---|---|---|
| 1 | S1（門下を 読む 道を 閉じる） | 9/21 |
| 2 | S2（`open_monka_thread` と 列） | 9/21 |
| 3 | 裁定160（`org_post_perm_log` と 引き金） | 9/21 |
| 4 | 束2（164 W2・W1／FX2／167 A1） | 9/22 |
| 5 | 束0（FX1／168 ★1〜3・外部キー 21本） | 9/22 |
| 6 | 束1（FX3・FX4・FX5・FX6／163 §2） | 9/22 |
| 7 | 束2b①（同意の 日時など 5列） | 9/22 |
| 8〜13 | Opus の 台帳 SQL 01・02・03・05・07・08 | 9/23 |

★本番の `supabase_migrations.schema_migrations` は **31本の まま** です（★9/18 で 止まって います）。

---

## 二 ── なぜ 残せないか（★確かめました）

```
POST https://api.supabase.com/v1/projects/{ref}/database/migrations
  → 403
  {"message":"Your account does not have the necessary privileges to access this endpoint."}
```

★★★**その 口 だけ** が 断られて います。★合言葉が 狭い わけでは ありません。

| 試した こと | 結果 |
|---|---|
| `GET /organizations` `/projects` `/api-keys` `/config/auth` `/secrets` `/functions` `/branches` | ★ぜんぶ 200 |
| `GET /database/migrations`（読む） | ★200 |
| `POST /database/query`（SQL を 流す・★書けます） | ★200 |
| `POST /database/query`（中身を 壊す） | ★400（★中身の 話） |
| `POST /database/migrations`（中身は 正しい） | ★**403** |
| `POST /database/migrations`（★中身を 壊す） | ★**403**（★同じ） |

★★中身を 見る 前に 断られて います。★入口で 止まって います。
★★試しの 台帳でも 同じ 403 です。★事業の 話では なく、★口座の 話 です。

---

## 三 ── お願いしたい こと（★どれか 1つ）

### ㋐ 合言葉を 作り直す（★いちばん 早い）

```
Supabase → 右上の お名前 → Account Preferences → Access Tokens
  → Generate new token
  ★★「Read only」に **しない** で 作って ください
  ★★名前は 何でも かまいません（いまのは「claude_code」です）
```

★できた 文字を、★`~/.bash_profile` の `SUPABASE_ACCESS_TOKEN=` と 入れ替えます。
★★入れ替えた あとに、★古い ほうを Revoke して ください。

★★★もし いまの ものが すでに「Read only では ない」なら、★㋐では 直りません。
　★そのときは ㋑か ㋒ です。

### ㋑ Supabase の 道具（MCP）を つなぐ

★Claude の 席に Supabase の MCP が つながって いれば、★そこの `apply_migration` で 同じ ことが できます。
★★いまの 私の 席には つながって いません。

### ㋒ Supabase に お尋ねする

★「Management API の `POST /v1/projects/{ref}/database/migrations` が 403 に なる。
　★Owner の Personal Access Token で、★`/database/query` は 通る」

★★これは Supabase 側の 決めの 話 かも しれません。★私からは そこまで 分かりません。

---

## 四 ── 直ったら、★すぐ できる 形に して あります

```
python3 tools/apply_migration.py --list          ★いまの 一覧
python3 tools/apply_migration.py <名> <file> --ok ★当てて、記録する
```

★★`supabase/migrations/` に、★13本 ぜんぶ 置いて あります。
★★★どれも「何度 流しても 同じ」形 です（★道具が 当てる 前に 確かめます）。
　★本番に もう 入って いる ので、★流しても **姿は 変わりません**。★変わるのは 記録 だけ です。

---

## 五 ── なぜ 回り道を しないか

★`POST /database/query` で SQL を 流し、★`supabase_migrations.schema_migrations` に
自分で 1行 入れる ことは **できます**。★していません。

★裁定161 §6 …… 「本番の台帳を変えるのは apply_migration だけ。直接の SQL は使わない」。

★★★記録を 正しく する ために 記録を 手で 書く、★では 筋が 通りません。
★★それに、★手で 書いた 1行は「本当に その SQL が 流れた」証しに なりません。

---

## 六 ── 急ぐ わけ

★FX7（試しの 台帳を、★本番の 移行から 作り直す）が、★この 13本を **落とします**。
★★落ちた 試しの 台帳で 試すと、★本番と ちがう ものを 試す ことに なります。

★★★10月19日の 総点検の 前に、★ここが 揃って いる 必要が あります。

---

# ★★★追記（2026-09-23 06:41）── 合言葉が **効かなく なりました**

```
GET https://api.supabase.com/v1/organizations
  → 401 {"message":"Unauthorized"}
```

★`~/.bash_profile` の 中身は **9月18日から 変わって いません**（★ファイルの 日付で 見ました）。
★★つまり、★Supabase の 側で **止められた（Revoke された）** という ことです。

★★★おそらく、★上の お願いを 読んで くださって、
　★古い ほうを 先に 止めて くださったのだ と 思います。★ありがとうございます。

★★いま、★私は 台帳に **1つも 問い合わせできません**（★読むのも できません）。

★新しい 合言葉が できたら、★`~/.bash_profile` の この 行を 入れ替えて ください。

```
export SUPABASE_ACCESS_TOKEN=sbp_…（新しい もの）
```

★★入れ替えて いただければ、★すぐ 13本を 記録に 入れます。

---

# 新しい 合言葉に、★どこまでの 権限が 要るか

★お尋ね …… 「Migrations・Database の ほかに、★Edge Functions や
Database Webhooks の 書き込みが 要るか」。

## ★★答え …… **要りません**（★これからの 作業では）

★倉庫を 数えました（2026-09-23）。

| | 使って いるか | 数 |
|---|---|---|
| Edge Functions | ★**使って いません** | `supabase/functions/` が ありません。`functions.invoke` も 0か所 |
| Database Webhooks | ★**使って いません** | SQL に `supabase_functions` / `http_request` / `net.http` が 0か所 |
| Storage（写真・録音） | ★**使って いません** | `supabase.storage` の 呼び出しが 0か所。録画は YouTube の URL です |
| `pg_cron` | ★**使って いません** | 1か所に 註が ある だけ（「在れば それで」）。時計は Vercel の cron です |

★これから 当てる 3本が 使う もの ──

```
04  create or replace function / create trigger / grant
06  alter table / create or replace function / create trigger / grant / revoke
09  drop table
```

★★★ぜんぶ **ふつうの SQL** です。★`Database` と `Migrations` で 足ります。

## ★★ただし、★2つ だけ 先に お伝えして おきます

### ① 多要素の 認証を「学校の 管理者に 求める」なら（★裁定169 CHECK_FIRST 3）

★仕組みを 入れる には、★**入れ物の 設定**（`config/auth`）を 変える ことに なります。
　★`PATCH /v1/projects/{ref}/config/auth`
★★これは `Database` でも `Migrations` でも ありません。

★★★いま 決まって いません。★決まってから で 構いません。
　★決まった ときに、★合言葉を もう 一度 作り直す ことに なります。

### ② FX7（試しの 台帳を 作り直す）の 読み方が 2つ あります

| 読み方 | 要る もの |
|---|---|
| ㋐ いまの 試しの 入れ物に、★本番の 移行を 流し直す | ★`Database` ＋ `Migrations` で 足ります |
| ㋑ 入れ物ごと 作り直す（★新しい プロジェクトを 作る） | ★★`Projects` の 作る 権限が 要ります |

★★私は ㋐の つもりで います（★入れ物は 同じ・中身を 揃える）。
★★★㋑なら、★`.env.local` の URL も 鍵も 変わります。★別の 話に なります。
　★どちらか、★Opus に お確かめ ください。

## ★お勧めする 権限（★新しい 合言葉）

```
Database   …… Read / Write   ★いま すでに 通って います
Migrations …… Read / Write   ★★ここが 403 でした。★これが 要ります
そのほか   …… Read だけ で 足ります（★Edge Functions・Webhooks・Storage・Secrets）
```

★★もし 権限を 細かく 選べない 形なら、★ふつうの（Read only では ない）合言葉 1つ で 構いません。
