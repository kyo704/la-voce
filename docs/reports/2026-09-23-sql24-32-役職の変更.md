# ★★★役職の 変更 ── sql/24・sql/32（2026-09-23）

## EVIDENCE

### 一 ── ★役職の 変更は、★もう 止まって いません

```
本番の 移行 …… 53本
  20260923013615  set_member_post_actor   ★当てました
  20260923014222  opus_24_actor_id        ★当てました

本番で 確かめました（★行は 1つも 変えて いません）──
  set_member_post(p_org_id uuid, p_user_id uuid, p_post_id uuid, p_actor uuid)
    → boolean ／ security definer ／ search_path=public
  権限 …… anon=false  authenticated=false  service_role=true
  ①p_actor を 渡さない      → ★断られました（ACTOR_REQUIRED）
  ②当たらない 組（0行）      → false（★画面の `done !== true` が 正しく 働きます）
  役職の ついた 名簿 …… 前 10 ／ 後 10（★変わって いません）
  ops_audit_log ……… 前 0 ／ 後 0（★増えて いません）
```

★★Opus の「本番に set_member_post も actor_id も 無い」は、★**当てる 前**の 姿 です。
　★いまは どちらも あります。

### 二 ── sql/24 は ①〜④ を 当てました。★⑤ は 止めて います

```
supabase/migrations/20260923180000_opus_24_actor_id.sql   ★当てました（①〜④・字は そのまま）
supabase/pending/opus_24_5_記録の表を閉じる.sql            ★当てて いません
```

★★⑤ を 止めた わけ ── ★Opus の 書いた 前提が 揃って いません。

```
Opus の 字 …… 「⑤ …（裁定161 FX8。★06 の record_export を当ててから）」

本番の いま
  record_export …… ★ありません（0）
  components/VocalTracker.jsx:12761 は `rpc("record_export", …)` を 呼びます
    → ★書き出しの 記録は **いま すでに 落ちて います**
  org_billing_log …… ★画面から 直に 書いて います
    components/VocalTracker.jsx:12222  supabase.from("org_billing_log").insert(…)
    → ⑤ を 当てると、★ご請求の 宛先の 引き継ぎが 止まります（★本番に 1行）
```

### 三 ── 試しの 台帳

```
orutyqtfygvzcuhjgsch（la-voce-test2）
base_twin …… 14区分 ★差 0 件（★本番と 同じ 形に 戻して あります）
```

---

## DIFFS_I_SEE

### ① ★★sql/32 は、★サーバから 呼ぶと **ほとんど 断られます**（★動かして 確かめました）

★試しの 台帳に sql/32 を 入れ、★事務長（`post` あり・meibo・sched_all・gyoji・
　renraku_all・monka_read）が、★相手の 役職を 変えて みました ──

```
『事務長』を 渡す → ok=false  CANNOT_GRANT_WIDER_THAN_SELF
『学長 』を 渡す → ok=false  CANNOT_GRANT_WIDER_THAN_SELF
『教授 』を 渡す → ok=true          ← ★学校ぜんぶに かかる ことが 0 の 役職 だけ
『課長 』を 渡す → ok=false  CANNOT_GRANT_WIDER_THAN_SELF
      ★★課長は meibo・renraku_all だけ。★事務長の 持ち物の **部分** です
```

★わけ ── ★⑤ の `can_grant_post` は、★中で `has_can(org, perm)` を 呼びます。
　★`has_can` は `auth.uid()` を 見ます。★service_role では **null** です。

```
can_grant_post（service_role）…… false
has_can（service_role）………… false
has_can_user（事務長）………… ★true
```

★★★sql/32 は `v_actor` を 出して いるのに、★⑤ だけ それを 使って いません。
★★「自分が 持って いない ものは 渡せない」という **考え**は 正しい です。
　★★見て いる 人が ちがいます。

★★★直して いません。★Opus に お返しします（★ON_ERROR の 決め）。
　★`can_grant_post` に 人を 渡す 形が 要ります。★考えは 変えません。

### ② ★★sql/32 の 返す 形が、★画面の 読み方と 合いません

```
sql/32 …… returns table(ok boolean, reason text)
PostgREST が 返す もの …… [{"ok":true,"reason":"NO_CHANGE"}]

画面（app/api/org/posts/route.js:214, 305）
  if (done !== true) → ★失敗として 扱います
  ★配列は true では ありません。★うまく いっても「失敗」に なります
```

★Opus の お指図 …… 「違えば 名前を 直すか、Opus に 差し戻して ください」。
★★**直して いません**。★①と 同じ 本の ことなので、★2つ 一緒に お返しします。

★直す なら 2通り あります（★お選び ください）──

```
㋐ 画面を 直す …… data?.[0]?.ok を 見る。★reason を 画面に 出せます（★Opus の 形の まま）
㋑ 関数を 直す …… returns boolean に 戻す。★reason は 消えます
```

### ③ ★★sql/24 ② は、★本番の 表の 形と 合いません（★引き金を 付けた 瞬間に 落ちます）

★試しの 台帳で、★sql/06 の 引き金を 手で 付けて 動かしました ──

```
①サーバが set_member_post で 変える
   → changed_by=3b952206…  changed_by_kind=person   ★残りました

②印を 置かずに 直に 変える（★Opus の 字 …「入れ忘れ → system・null（処理は通る）」）
   → ★★ERROR 23502
      null value in column "changed_by" of relation "post_change_log"
      violates not-null constraint
```

```
本番の post_change_log.changed_by …… is_nullable = NO
```

★★つまり「処理は 通る」ではなく、★**役職の 変更 そのものが 止まります**。
★★★いまは 引き金が 本番に 無い ので、★害は 出て いません。
　★sql/06 ①（引き金を 付ける）を 当てる **前** に、★直しが 要ります。

★★直して いません。★Opus に お返しします。

### ④ ★★`record_export` が 本番に ありません。★画面は もう 呼んで います

```
components/VocalTracker.jsx:12761  supabase.rpc("record_export", …)
  ★origin/main に 入って います
本番 …… record_export ★ありません
```

★★運営の 書き出しは、★いま 本番で 落ちて います（★set_member_post と 同じ 形）。
★★★sql/06 の 適用が 要ります。★ご承認を お待ちします。

### ⑤ ★sql/32 は、★私が 当てた ものと 中身が ちがいます（★sql/32 の ほうが 厚い）

```
いま 本番に ある もの（20260923170000）…… 守り 2つ
  p_actor が null → 断る ／ p_org_id・p_user_id が null → 断る
  ★権限の 確かめは 画面（route.js）が して います

sql/32 …… 守り 5つ（post の 権限・在籍・owner・広さ・変わらないとき）
```

★★sql/32 の ほうが よい 形 です。★①②を 直して いただけたら、★当てます。
★★いま 当てると、★上の とおり 役職の 変更が **止まります**。

---

## NOT_DONE

```
★sql/32 …… ★当てて いません（★上の ①②・Opus に お返し）
★sql/24 ⑤ … ★当てて いません（supabase/pending/opus_24_5_記録の表を閉じる.sql）
★sql/06 …… ★当てて いません。★record_export・post_change_log の 引き金が 要ります
★sql/30（機能の 切り替え）…… ★まだ 読んで いません
★sql/31（読み取りの 直し）…… ★裁定165 の あと
★学校の 連絡担当者への メール …… ★まだ 確かめて いません
★148作品・1,224場面 …… ★まだ です
```
