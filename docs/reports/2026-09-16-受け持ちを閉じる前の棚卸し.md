# ★受け持ちを 閉じる ── ★先に 数えた もの

全128行 / 末尾は「- ★出欠は `lessons` の 列に 入って います。★行を 消さない ので、★学校の 記録は 残ります。」

生成: `tools/assignments_dependency_survey.py`（2026-09-16）

★★較正: 通りました（★assignments を 見る 決まりが 4 本、★うち `ended_at is null` が 3 本 出ました）。

## 一 ★台帳の 決まり ── ★受け持ちで 決めて いる もの

| 決まり | 表 | `ended_at is null` | 閉じると |
|---|---|---|---|
| `assignments_all_owner_admin` | `assignments` | いいえ | 変わりません（★持ち主・管理者だけの 決まり） |
| `org_message_reads_select` | `org_message_reads` | ★はい | ★見えなく なります |
| `org_messages_insert` | `org_messages` | ★はい | ★見えなく なります |
| `org_messages_select` | `org_messages` | ★はい | ★見えなく なります |

★★`org_messages`（先生からの 連絡）と `org_message_reads` が、★受け持ちで 決まって います。
★★だから いま、★やめても 連絡が 届き 続けて います。

## 二 ★画面の 側

| ところ | 何を して いるか | 絞り | 閉じると |
|---|---|---|---|
| `components/VocalTracker.jsx:9938` | fetchRenrakuStudios（門下の一覧） | ended_at is null | ★消える（★見本どおり） |
| `components/VocalTracker.jsx:11944` | fetchMyEnrollments（自分の担当） | ended_at is null ＋ 在籍で絞る | ★消える（★在籍で先に落ちる） |
| `components/VocalTracker.jsx:12025` | fetchMyOrgAssignments（先生の受け持ち） | ended_at is null | ★消える（★先生から見えなくなる） |
| `components/VocalTracker.jsx:12295` | fetchOrgDetail（学校の名簿の担当） | ended_at is null | ★消える |
| `app/api/enrollment/accept/route.js:138` | 入るとき・担当の重複しらべ | ★ended_at を 見て いない | ★★壊れます ── 下に 書きます |
| `app/api/enrollment/accept/route.js:151` | 入るとき・担当づくり | 上の しらべ 次第 | ★★作られません |

## 三 ★閉じた あと、★壊れる か

### 請求 ── ★壊れません

enrollments も assignments も 読んで いません（lib/planScreen.js・lib/paymentRetention.js に 参照なし）。★お代は お一人ごと です。

### 出欠 ── ★壊れません

lessons の 列に 入って います。★assignments では ありません（2026-09-08-レッスンの出欠.sql）。★閉じても 消えません。

### これからの レッスン ── ★要ご確認 ── 台帳の 決まりを 引いて から

lessons は teacher_student_links と org で 決まります。★assignments の 決まりは 掛かって いません。★★予定の 行は 残ります。★出るか どうかは 在籍（org_events とは 別）。

### ★入り直し ── ★★閉じる と 同時に 壊れます

accept route:138 が (org_id, teacher_id, student_id) だけで しらべ、★`ended_at is null` を 見て いません。★閉じた 行が 見つかって しまい、★新しい 担当が 作られません。

## 四 ★先に お直しが 要る ところ

`app/api/enrollment/accept/route.js:138` の しらべに `.is("ended_at", null)` を 足します。
★★足さないと、★一度 やめた方は、★入り直しても 受け持ちが 付きません。
★★受け持ちが 無ければ、★先生からの 連絡も 門下も 出ません。
★★いまは まだ 閉じて いない ので、★この 不具合は 起きて いません。
★★閉じる のと **同じ ひと組** で 直します。

★★閉じた 行を どう するか、★2つ あります ──

- ㋐ 新しい 行を 足す（★過去の 受け持ちが 記録として 残ります）
- ㋑ 閉じた 行の `ended_at` を null に 戻す（★行は 1本の まま）

★★㋐ に して います。★「行を 消さない・過去は 記録として 残す」と 同じ 向き です。
★★ただし `(org_id, teacher_id, student_id)` に 束ねが あると 足せません。
★★束ねの 有無は 台帳に しか ありません ── `問い-決まりの無い書き込み.sql` に 足しました。
★★束ねが あった ときは ㋑ に 落ちます（★23505 を 受けて 戻す）。

## 五 ★この 紙で 足りない こと

★★これは **紙** です。★台帳では ありません。
★★決まりは SQL の ファイル から 読みました。
★★本番に 別の 決まりが 増えて いれば、★ここには 出ません。
★★`問い-決まりの無い書き込み.sql` の 結果と 重ねて ください。

## 六 ★台帳の 答え（2026-09-16・操作者が 引いた もの）

★この 節だけ、★私では なく 台帳が 元 です。

- 【三】`enrollments_org_id_student_id_key = unique(org_id, student_id)` ── ★あり。
  ★入り直しは `upsert` で 通ります（★Q2 の 答え）。
- 【六】`assignments` ── ★主キーと 外つなぎ 3つ だけ。★一意の 束ねは **無し**。
  ★だから ㋐（★新しい 行を 足す）で 進めます。★過去の 受け持ちが 記録として 残ります。
- 【七】★受け持ちで 決めて いる 決まりは **4本**。★紙と 台帳が 一致。
  `org_messages_select` / `org_messages_insert` / `org_message_reads_select` が
  `ended_at is null` を 見ます。`assignments_all_owner_admin` は 見ません。
- 【四】【五】★まだ いただいて いません。
  ★★【五】は 「やめた のに 受け持ちが 開いた まま の 数」です。
    ★いま どなたかが その 目に 遭って いるか が、★これで 分かります。

## 七 ★★もう 1つ 出てきた もの ── ★レッスンの 予定（★裁定待ち）

★`lessons` の 読む 決まり `Ops-visible lessons (org-based)` は、こう 書かれて います ──

```
org_id is not null
and ( auth.uid() = student_id or can_view_ops(auth.uid(), org_id, student_id) )
```

★生徒の 枝は `auth.uid() = student_id` **だけ** です。★在籍を 見て いません。

- ★行事（`org_events_select_member`）は `enrollments`（status='active'）で 見ます。★消えます。
- ★連絡（`org_messages_select`）は `assignments`（ended_at is null）で 見ます。★閉じれば 消えます。
- ★予定（`lessons`）は **何も 見て いません**。★やめても 残ります。

★見本の 字 ──「この教室の 予定・行事・門下の 連絡が、あなたの 画面から 消えます」。
★★3つの うち、★予定 だけが そう なって いません。

★画面の 側（`components/VocalTracker.jsx` の レッスン①）にも、★同じ 覚え書きを 置きました。

### ★どこまで 閉じるか ── ★2つ あります

- **㋐ 行事と 同じ形にする** ── ★在籍が active の ときだけ 見える。
  ★★これからの 分も、★過去の 分も、★まとめて 消えます。
  ★★見本の 字には 素直です。★ただし ご自分が 受けた レッスンの 記録も 見えなく なります。
- **㋑ これからの 分だけ 閉じる** ── ★在籍が active か、★`scheduled_at` が 過ぎて いるか。
  ★★「予定が 消えます」に 合い、★これまで 受けた 分は 手元に 残ります。
  ★★見本の 別の 字 ──「これまでの 出欠と、連絡に 書いたことは、教室の 記録として 残ります」
    ── とも、★向きが 揃います。

★★私は ㋑ が 合って いると 思います。★けれど これは 坂本さんの 判断 です。
★★どちらでも、★**決まり（RLS）の ほうを** 直します。★画面で 隠すのは 見た目だけ です。

### ★閉じる とき、★壊れないか

- ★先生・事務の 側は `can_view_ops` の 枝で 見ます。★生徒の 枝を 締めても 変わりません。
- ★個人指導（`teacher_student_links`）の レッスンは `org_id is null` です。
  ★この 決まりの 外 です。★教室を やめても、★先生との つながりが 続く 限り 残ります。
- ★出欠は `lessons` の 列に 入って います。★行を 消さない ので、★学校の 記録は 残ります。
