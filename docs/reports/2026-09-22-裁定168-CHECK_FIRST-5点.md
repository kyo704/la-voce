# 裁定168 の CHECK_FIRST 5点 ── 確かめました（2026-09-22）

---

## 1 ── 退会は どこで 動き、★失敗したら 画面に 何が 出るか

### どこで 動くか

| 道 | 鍵 | いつ |
|---|---|---|
| `app/api/account/delete/route.js` | `service_role`（`createAdminClient`） | ★ご本人が「今すぐ 消す」を 押した とき |
| `app/api/cron/purge-deleted/route.js` | 同じ | ★猶予（30日）が 明けた とき。★API を 通りません |

★どちらも `lib/accountDeletion.js` の `purgeAccount(admin, userId)` を 呼びます。
★★画面の 鍵では 消して いません。★`service_role` です。

### 画面に 何が 出るか

★★★**止まった 形が 5つ あります。**★どれも「失敗」とは 書きません。

| 状態 | いつ | 画面 |
|---|---|---|
| `blocked` | 教室に ほかの 方が 居る | ★教室の 一覧を 出します。★1行も 消えて いません |
| `payerNotice` | 契約者が 居なく なる | ★先に 1度だけ 知らせ、★了解を 押せば 進みます |
| `paymentActive` | お支払いが 続いて いる | ★順番を 示します。★隠しません |
| `cancelFailed` | お支払いを 止められなかった | ★退会して いません。★1行も 消えて いません |
| `error` | 表の どれかで 止まった | 「削除できませんでした。」 |

★`error` の とき、★どの 表で 止まったかは **画面に 出しません**。
　★`console.error("★退会が止まった表:", …)` に 出します。
　★★註に わけが 書いて あります ──「利用者に 表の 名前を 見せる 意味は ありません」。

★通信そのものが 落ちた とき …… 「通信に失敗しました。時間をおいてもう一度お試しください。」

### ★気づいた こと

★★`purgeAccount` が **途中で 止まる** ことが あります（`failures` が 返ります）。
　★そのとき、★**消えた 表と 消えて いない 表が 混ざります**。
　★★画面は「削除できませんでした」と だけ 言います。
　★★★もう 一度 押せば 続きから 進みますが、★それは 画面から 読み取れません。

★★★これは 決めの こと です。★私は 直して いません。
　★案 …… 「一部だけ 消えました。もう一度 押すと 続きから 進みます」と 書く。

---

## 2 ── 「今日」を 作って いる ところ

### ★★形が **3つ** あります

| 形 | どこ | 何に なるか |
|---|---|---|
| ㋐ その 端末の こよみ | `components/VocalTracker.jsx:1150 todayISO()` | ★**`entries.date` は これ です** |
| ㋑ UTC ＋ 9時間 | `app/api/cron/line-reminder/route.js:24` ／ `lib/systemAlert.js:29` ／ `lib/entrySource.js:103` | ★日本時間の こよみ |
| ㋒ UTC の まま | `app/api/cron/pricing-notice/route.js:45` ／ `app/api/onboarding/count/route.js:38` | ★世界標準時の こよみ |

★数（★註と 字を 落として 数えました）──
`new Date()` 124 ／ `Date.now()` 35 ／ `toISOString().slice(0,10)` 18 ／ `now()` 37。
★`Asia/Tokyo` という 字は **0か所** です。★時差は ぜんぶ「＋9時間」の 足し算 です。

### ★★★`entries.date` は 誰が 決めて いるか

★**その 端末** です。

```
components/VocalTracker.jsx:5697
  const [selectedDate, setSelectedDate] = useState(todayISOUTC());   ★最初は UTC（画面の ちらつき よけ）

components/VocalTracker.jsx:6163-6167
  useEffect(() => {
    const localToday = todayISO();      ★その 端末の こよみ
    if (localToday !== utcToday) setSelectedDate(localToday);
  }, []);
```

★★つまり、★**開いて いる 端末が 何時だと 思って いるか** で 決まります。

### ★★★欧州へ 行かれる ときに 何が 起きるか

| | 日本（UTC+9） | ローマ（UTC+2 夏／+1 冬） |
|---|---|---|
| 日が 変わる とき | 24時（JST） | 24時（ローマ） |
| その とき 日本は | ― | ★**朝の 7時 か 8時** |

★★記録の 日付は、★端末の こよみに ついて 行きます。
★★★行きの 飛行機で 日が **戻り**、★帰りで 日が **飛びます**。
　★9月23日に ローマで 書いた 記録は、★日本の 9月23日 とは 7〜8時間 ずれた 1日 です。

★★一方、★知らせ（LINE）は **日本時間** で 動きます（㋑）。
　★★ローマに 居る 方には、★朝の 7時（ローマの 前日 23時）に 届きます。

★★★`pricing-notice` だけ UTC です（㋒）。
　★いまの cron は `0 1 * * *`（＝ UTC 1時 ＝ JST 10時）なので、★日付は 一致します。
　★★けれど 時刻を 変えた 日に ずれます。★**たまたま 合って いる** だけ です。

### ★お勧め（★決めは 坂本さん・Opus）

```
lib/today.js（新）
  todayInZone(zone, now)   ★「＋9時間」を 3か所に 書かない
  ★サーバは ぜんぶ これを 通す。★クライアントの 日付は いまの まま（端末の こよみ）
```

★★★`TZ=Asia/Tokyo` と `TZ=Europe/Rome` の 両方で 見張りを 走らせる（★裁定168 ★4）のは、
　★この 3つの 形が 揃って から です。★いま 走らせると、★㋒だけが 落ちます。

---

## 3 ── 失敗が 痕跡を 残さない ところ

| どこ | 失敗したら | 痕跡 |
|---|---|---|
| `api/feedback` の メール（Resend） | ★黙って 続けます（わざと） | `console.error` だけ |
| `api/cron/line-reminder` の LINE 送信 | ★1人ずつ 続けます | `console.error` だけ |
| `api/cron/keep-alive` | ★**メールで 知らせます** | `sendAlertOncePerDay`（★1日1通） |
| `api/stripe/webhook` | 400 を 返す（Stripe が 再送） | Stripe の 側に 残ります |
| `api/cron/purge-deleted` | `failures` を 返す | `console.error` だけ |
| `api/cron/purge-events` | ― | ★見て いません |

★★★`sendAlertOncePerDay` を 使って いるのは **`keep-alive` 1本 だけ** です。
　★ほかは ぜんぶ `console.error` です。★Vercel の ログを 見に 行かないと 分かりません。
　★★裁定168 ★5 が 言う「送信・webhook・関数の 失敗が 痕跡を 残さない」は、★そのとおり です。

★★`system_alerts` の 表は あります（`lib/systemAlert.js`）。★使い道が 1つ しか ありません。

---

## 4 ── `teacher_student_links` は まだ 使われて いるか

★★★**使われて います。**

| 本番の 数 | |
|---|---|
| `teacher_student_links` | **11行** |
| `assignments`（新しい ほう） | 28行 |
| `lessons` | 11行 |

★画面の 呼び出し（★`components/VocalTracker.jsx`）──

```
7796   lessons を 引く とき の 内部結合（link:teacher_student_links!inner）
11046  自分の つながりを 引く
13656  つながりを 切る
13700  先生として ／ 13702  学生として の 一覧
13797  先生の 受け持ち ぜんぶの レッスン（★旧 1:1 と 新しい ほうの 両方）
```

★★13792行目の 註に こう 書いて あります ──
「先生が 担当する 全生徒の レッスンを、★旧1:1連携（`teacher_student_links` 経由）と …」

★★★つまり **2つの 道が 並んで 動いて います**。★片方は 古い ほう です。
　★`teacher_student_links` への insert は、★すでに authenticated から 外して あります（13544行の 註）。
　★★新しく 増えません。★けれど 11行は 生きて いて、★読む 道も 生きて います。

★★決めの こと …… ★いつ 畳むか。★畳む 日に、★11行を どう するか。

---

## 5 ── 裁定167 の CHECK_FIRST 3点

★別の 紙に 書きました …… `docs/reports/2026-09-22-裁定167-CHECK_FIRST-3点.md`

- ★A1 …… `character_inventory` で 決めて いました。★**本番で 塞ぎ済み** です。
- ★B1 …… 公開ページは まだ ありません。★「作る 日の 決め」です。
- ★A2 …… 署名は 確かめて います。★けれど「使えるか」は `status` だけ で、★期限を 見て いません。

---

## ★あわせて ── 列の 絞りが 台無しに なって いる 表は **0件** でした

★裁定164 W2 の `same_shape`（★「列ごとに 絞った つもり」の 点検）を、★**全表** で 走らせました。

| 表 | できこと | 渡して いる 列 | 状態 |
|---|---|---|---|
| `application_messages` | INSERT | 4 / 6 | ★列だけ（効いて います） |
| `applications` | INSERT / UPDATE | 9 / 12・6 / 12 | ★同じ |
| `assignments` | UPDATE | 1 / 7 | ★同じ |
| `lessons` | SELECT / UPDATE | 16 / 17・2 / 17 | ★同じ |
| `matching_cuts` | INSERT | 4 / 6 | ★同じ |
| `matching_reports` | INSERT / SELECT | 5 / 10・6 / 10 | ★同じ |
| `memberships` | UPDATE | 2 / 13 | ★同じ |
| `org_events` | SELECT / UPDATE | 14 / 15・6 / 15 | ★同じ |
| `org_messages` | UPDATE | **1 / 11** | ★同じ（★9月22日に 直した ところ） |
| `postings` | INSERT / SELECT / UPDATE | 12・13・10 / 14 | ★同じ |
| `profiles` | UPDATE | 68 / 76 | ★同じ |
| `teacher_student_links` | UPDATE | 3 / 9 | ★同じ |

★★★**18通り とも、★表ごとの GRANT は ありません。**★列の 絞りは ぜんぶ 効いて います。
★★裁定164 W2 の 確かめ（`org_messages` の authenticated:UPDATE が 無い こと）も、★これで 取れました。

---

## ★道具を 1つ 直しました

★`tools/ask_ledger.py` の 見張りが、★**註の 中の `GRANT`** に 当たって 止まりました。

★★これは「説明を 処理と 読む」形の **6度目** です（★台帳 08-10）。
　★①周期の 語彙 ②「データ不足」 ③実績の 数 ④`gate_mismatch.py` ⑤`lib/strip.js` ⑥**ここ**。

★直し …… 括りを 外した あと、★**行ごと** に `--` を 落とし、★そのあと `/* */` を 落とします。
　★★順が 決め手 です。★改行を 先に 潰すと `--` が 後ろ 全部を 飲みます。
★較正 8件（★註の 中は 見つけない ／ 本物は 見つける の 両側）。
