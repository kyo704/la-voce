# 「生徒を招待する」の入口が、門の中から消えています（★直していません）
全142行 / 末尾は「直していません。ご判断をお待ちします。」

- 日付 … 2026-09-15
- 出どころ … [ACTION] Opus → Code「★SEPARATE DEFECT — report, do not fix yet」
- 対象 … +forcode（`teacher_beta_access = true`・門の中）

## 1. 結論

★**入口が1つもありません。** 押せないのではなく、たどり着けません。

そして★No.020 とは別の欠陥です。招待の仕組みではなく、**画面の帯**の話です。

## 2. Q1 ── 何がその札を閉じているか

`teacher_beta_access` だけではありません。**3つ**そろって初めて出ます。

招待の札は2つあり、どちらも同じ場所にいます。

| 印 | 何 | 行 | 条件 |
|---|---|---|---|
| ㋐ | 個人の門下に招く（`teacher_invitations`） | `:16918` | ① `activeTab === "lesson"` ② `lessonRole === "teach"` ③ `canSeeBetaFeatures(profile)` |
| ㋑ | 教室に招く（`org_invitations`） | `:17188` | ①②③ ＋ ★**owner/admin の教室を持っていること** |

`canSeeBetaFeatures` は `is_admin || teacher_beta_access` です。
+forcode は③を満たします。★詰まっているのは①です。

## 3. Q2 ── 動いたのか、落ちたのか

★**落ちました。画面が変わったときです。**

`components/VocalTracker.jsx:688`

```js
const TABS_V2_ORDER = ["home", "today", "analysis", "notes", "garden"];
```

★門の中の帯に、**`lesson` がありません**。

外していること自体は、書いてあります（`:671`）──

> ★★レッスンを 外しています。★画面は 消えていません。
> 　★入口は ホームに 2つ 残ります（★教室に 入っている方は 行けます）。

★その「2つ」を、実際にたどりました。

| 入口 | 行 | 出る条件 |
|---|---|---|
| きょうの帯の「すべて見る」 | `:14150` | ★`lessonToday` の行が出るときだけ |
| ホームの同じ札 | `:14202` | 同上 |
| 「通っているところ」から | `:14243` | `myEnrollments.length > 0`（★生徒として在籍している人） |

そして `lib/todayBand.js` は、はっきりこう書いています。

> ★★「該当がなければ その行は出ない」を、★守ること。
> 　★「今日のレッスンはありません」と、★書かないこと。

★つまり**レッスンが1件もなければ、その行は出ません**。行が出なければ札も出ません。

## 4. ★鶏と卵が、もう一度できています

+forcode の状態を順に置くと、こうなります。

```
生徒が0人  → レッスンが0件
           → きょうの帯に lessonToday の行が出ない
           → 「すべて見る」が出ない
           → レッスンのタブへ行けない（帯にも無い）
           → 招待の札に、たどり着けない
           → 生徒を招待できない
           → 生徒が0人のまま
```

★これは `lib/featureFlags.js:37-47` が、**生徒の側で**すでに1度書いている形です。

> 1. 新規ユーザーは「先生とつながる」に永久に到達できない。
>    （…）場所を移しただけで、鶏と卵は解けていなかった。

同じことが、今度は**先生の側**で起きています。
前回は「もっと」へ移して直しました。今回は、移した先がありません。
`lib/moreMenu.js` の11行に、招待の行は**1つもありません**。

## 5. 門の外（38名）はどうか

★こちらは詰まっていません。`TABS`（門の外）には `lesson` があり、
`hasLessonTab = canTeachLessons || canLearnLessons` で出ます。

`canTeachLessons` は `canSeeBetaFeatures(profile) || hasStudentLinks` なので、
`teacher_beta_access` があれば帯にタブが出て、既定の立場は「教える」です
（`resolveLessonRole`：★「既定は『教える』」）。

★つまり、**門の中に入ったことで詰まりました**。
門の外のままなら、+forcode は招待できていたはずです。

## 6. ★これが実機の確認を止めていた理由

No.020② の確認で「招待のボタンが見当たらない」となったのは、
招待の仕組みが壊れていたからではありません。

★**帯からレッスンのタブを外したとき、招待への唯一の道が一緒に消えた**からです。

仕組みのほうは動いています（`handleGenerateTeacherInvite` も
`teacher_invitations` への insert も残っています）。

## 7. ★直し方の候補（★ご判断をお願いします）

直していません。3つ考えられます。

| 印 | 何 | 私の見立て |
|---|---|---|
| ㋒ | もっとに「生徒を招待する」の行を足す | ★勧めます。前回、生徒側で同じ詰まりを「もっとへ移す」で解いています。同じ形です |
| ㋓ | `TABS_V2_ORDER` に `lesson` を戻す | ★勧めません。見本の帯は5つで固定です（2026-09-09・§9） |
| ㋔ | きょうの帯に、レッスンが0件でも札を出す | ★勧めません。`lib/todayBand.js` の「無いことを毎朝知らせない」に触れます |

★㋒ なら、見本の帯も、帯の決まりも、どちらも触りません。
足すのは `lib/moreMenu.js` の1行と、その行き先です。

★ただし、もっとの一覧は見本 `SC['もっと']` の9行を写したものです。
招待の行は見本にありません。★見本にない行を足す判断になります。

## 8. ★別件 ── 試験の台帳への適用が、記録にありません

Opus の指摘（la-voce-test にも新しい形が入っている）について調べました。

`docs/` の中で `la-voce-test` に触れているのは2か所だけで、
どちらも**読むため・試すため**の記述です。

- `docs/reports/2026-09-14-A13-実機の確かめ.md:4` … A13 を試した記録
- `docs/records/やること-9月15日のあと.md:655,658,691` … 控えから戻す試しの手順

★No.018 の紙を試験の台帳へ流した、という記録は**ありません**。
私が流したものでもありません（★私は台帳を引けません）。

★誰が・いつ流したかは、私には分かりません。

## 9. たしかめ（★別件・No.024 のぶん）

- `npm run lint` … エラーなし
- `npm run build` … ✓ Compiled successfully
- 見張り … 通り306／落ち10（落ちた10本は、もとから落ちているものです）

直していません。ご判断をお待ちします。
