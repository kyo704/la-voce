# ★★★sql/06 ── 何が 入って いて、★いま 当てると どう なるか（2026-09-23）

★お尋ね …… 「record_export 以外に 何が 含まれ、★それぞれが いまの 本番に どう 影響するか」。

---

## 一 ── 5つの かたまり

| | 中身 | 本番の いま | 当てると |
|---|---|---|---|
| 頭 | `post_change_log.changed_by` / `target_user_id` の NOT NULL を 外す | ★**もう 外れて います**（sql/34 ②） | ★何も 起きません |
| ① | `log_post_change()` ＋ 引き金 `memberships_log_post_change` | ★関数は あります（sql/24 の 形）／★引き金は **ありません** | ★★下の 【A】【B】 |
| ② | `log_org_billing_change()` ＋ 引き金 `org_billing_log_change` | ★どちらも **ありません** | ★★下の 【C】 |
| ③ | `record_export()` | ★**もう あります**（sql/34 ④・字は 同じ） | ★何も 起きません |
| ④ | 3つの 記録の 表への、★画面からの insert を 閉じる | ★まだ 開いて います | ★★下の 【D】 |

---

## 【A】★★① は、★いま 本番に ある 関数を **古い 形に 戻します**

```
本番の いま（sql/24 ②）…… declare v_actor uuid := public.actor_id();
sql/06 ①   ………………… now(), auth.uid()   -- サーバ（service role）の変更は null
```

★★`actor_id()` は「画面なら `auth.uid()`、★サーバなら 印（`app.actor_id`）」を 見ます。
★★`auth.uid()` だけに 戻すと、★**サーバ経由の 役職の 変更が また 名無しに なります**。

★★★裁定173 の 直しが 消えます。★★①は そのまま 当てられません。

---

## 【B】★★① を 当てると、★役職の 記録が **二重に** 残ります

```
app/api/org/posts/route.js:79   記録する()
  await admin.from("post_change_log").insert({ … })     ← ★画面の 側が 1行 書いて います
sql/06 ① の 引き金               ← ★台帳の 側が もう 1行 書きます
```

★★どちらも 動くので、★1回の 変更で **2行** 残ります。
★★★引き金を 付ける なら、★同時に `route.js:79` の insert を 消す 必要が あります。

```
post_change_log の 行 …… 0（★いまは どちらも 走って いません。★引き金が 無い ため）
```

---

## 【C】★★② は 新しい 記録です。★こちらも 二重に なります

```
components/VocalTracker.jsx:12222
  await supabase.from("org_billing_log")
    .insert({ org_id, actor_id: userId, what: logWordForAtesaki(名) })   ← ★画面の 側

sql/06 ② の 引き金 org_billing_log_change                                 ← ★台帳の 側
```

★★`org_billing` の 7つの 列（宛名・宛先のメール・宛先の人・支払い方法・部署・
　ご担当・インボイス）が 変わった ときに 1行 残します。★中身は よい 形 です。

```
org_billing の 行 …… 1
org_billing_log の 行 …… 1
```

★★★②だけ なら 害は ありません（★記録が 増えるだけ）。★けれど 二重に なります。

---

## 【D】★★④ を いま 当てると、★ご請求の 宛先の 引き継ぎが **止まります**

```
revoke insert … on post_change_log, org_billing_log, export_log from anon, authenticated;
```

| 表 | 画面から 書いて いるか | 閉じると |
|---|---|---|
| `post_change_log` | ★いいえ（route.js は `admin` ＝ service_role） | ★何も 起きません |
| `export_log` | ★いいえ（★`record_export` に 移り 済み） | ★何も 起きません |
| `org_billing_log` | ★**はい**（VocalTracker.jsx:12222・`authenticated`） | ★★**止まります** |

★★★`org_billing_log` だけが 残って います。★② の 引き金に 移してから 閉じます。

---

## 二 ── ★私の お勧め（★順番）

```
㋐ いま 当てられる もの …… ★★ありません
   ★頭 と ③ は もう 済み。★①②④ は どれも 画面の 直しが 先 です

㋑ 1つ目 …… ★画面の 直し（★私が します・ご承認を お待ちします）
   1 route.js:79 の `記録する()` を 消す（★引き金に 任せる）
   2 VocalTracker.jsx:12222 の insert を 消す（★引き金に 任せる）
   ★★見張りも 一緒に 直します（post-actor / ops-posts / 請求の 見張り）

㋒ 2つ目 …… ★sql/06 ①（★ただし `auth.uid()` ではなく `actor_id()` の まま）
   ★★これは Opus に お返しする ところ です。★①の 字を そのまま 当てると 【A】が 起きます

㋓ 3つ目 …… ★sql/06 ②（引き金）

㋔ 4つ目 …… ★sql/06 ④（閉じる）
   ★★Opus も 同じ 順を 書いて います ──
     「順番: 画面を record_export と引き金に移す → そのあとで 06 ④」
```

---

## 三 ── ★ご承認を いただきたい こと

```
① 画面の 直し 2か所（route.js:79 ／ VocalTracker.jsx:12222）を、★私が します
   ★★この 2つは 記録を **消しません**。★書く 人が 画面から 台帳に 移るだけ です
   ★★届く 人 …… 役職を 持つ 7人（★ぜんぶ 坂本さんの お口座）
   ★★行 …… post_change_log 0 ／ org_billing_log 1（★どちらも 消しません）

② sql/06 ① の 字を、★`auth.uid()` から `actor_id()` に 変えて よいか
   ★★変えないと、★裁定173 の 直しが 消えます
   ★★★私の 判断では 変えません。★Opus に お返しします
```

★★★どちらも まだ して いません。
