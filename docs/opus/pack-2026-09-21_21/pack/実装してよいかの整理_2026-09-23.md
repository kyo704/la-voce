# 「Code が実装して 問題が起きないか」への答え ── 2026-09-23 Opus

## 1. ★正直な答え：言えません

```yaml
今日1日で、私が書いた SQL から ★9件の誤りが出ました。
  ★5件は「動かして初めて」分かったもの（読んでも 分からない）
  ★3件は 私が入れた守りが ★退会・学校を閉じる・保存期間の掃除を 止めていたもの
  ★1件は 掛け持ちの人で ★どちらが出るか決まらない形（本番に既に3人）
同じ密度なら、★まだ残っていると考えるべきです。
★「絶対に問題が起きない」とは 言えません。
```

## 2. ★代わりにしたこと（道具を1つ作った）

```yaml
tools/sql_schema_check.py ── ★SQL の中の 表・列・値が 本番にあるかを 突き合わせる
  なぜ: 今日の9件のうち ★5件が この型（無い列・無い表・違う値）でした
  見るもの:
    A 表があるか（from／join／insert／update／alter）
    B insert の列／C update の set の列
    D ★CHECK に列挙された値と 比較の値（status = 'enrolled' ← 本当は 'active'）
    E ★別名で書いた列（k.name_at／en.grade）
  ★今日の5件を 当時の形で流して、★6件すべてを捕まえることを確かめました
  ★自己試験つき（good/bad/alias の3つ・PASS）
使い方（★Code が走らせる。Opus は DB に繋げません）:
  psql "$DB" -At -F'|' -f tools/sql_schema_dump.sql > tools/schema_snapshot.txt
  python3 tools/sql_schema_check.py sql tools/schema_snapshot.txt
★お願い: ★当てる前に 必ず1度 走らせてください。62本ぶん まとめて出ます
```

## 2b. ★もう1つ作った（守りの書き方の見張り）

```yaml
tools/sql_guard_lint.py ── ★今日の実害 4種を 機械で見つける
  G1 止める引き金に ★逃げ道の印があるか（退会・親の削除・掃除で詰まらないか）
     ★delete／update でも動く引き金だけを見る（足すときだけの引き金は 詰まらない）
  G2 ★ビューに security_invoker と revoke があるか（RLS の素通りを防ぐ）
  G3 ★org_id を limit 1 で選んでいないか（掛け持ちの人で 決まらない）
  G4 ★security definer に set search_path があるか
★62本に当てたところ 3件出ました:
  ✓ 18（人数の確かめ）── ★誤検知。足すときだけ動く引き金なので 詰まらない → 道具を直した
  ★48（公開の作品を消せない）── ★本物。運営でも消せず、将来の掃除も詰まる形だった → 逃げ道を付けた
  ★56（limit 1）── ★本物。63 の直しを 56 の本体に取り込んだ（★これから当てる人は 56 だけで足ります）
いまは ★3つの見張りとも OK（guard_lint・fail_closed_lint・schema_check の自己試験）
```

## 3. ★この道具でも 捕まえられないもの（正直に）

```yaml
① ★繋ぐ先の表そのものを 間違えた場合
   （my_timetable を org_periods に繋いだ。★両方 実在するので 文法上は正しい）
② ★時刻の型の取り違え（9時間ずれ。★0件を静かに返す）
③ ★守りが 退会・親の削除・掃除を止める形
④ ★掛け持ちの人・複数の学校のような「1件だけ選ぶ」形
⑤ ★権限の抜け（ビューが RLS を素通りする など）
→ ①〜⑤は ★動かさないと分かりません。
  ★だから「種を作って 数える」を 当てる前に 必ず。
  ★Code の「当てる直前の姿に戻して試す」が いちばん確かです
```

## 4. いま「当ててよい」と言えるもの／言えないもの

```yaml
○ 当ててよい（★種を作って数えた）:
  56＋63（重なり）／57・61（集計）／59・61・62（実技試験）／60（ICS）
  37（連絡先・学校を閉じられることを確認）／44・50・55（退会が通ることを確認）
  54（順番と冪等を 模型で確認）／48・53（模型で確認）
△ 当ててよいが ★1度 実在の試しの利用者で:
  46（合言葉）／45（公演の後半）／41（自分の予定・代役）／35（立ち上げ）
  ★どれも auth.uid() が要るため、SQL の画面からは試せませんでした
✕ まだ言えない（★私が動かしていない）:
  01〜09・11〜13・18〜23・25〜30・36・38〜40・47・49・51・52・58
  ★このうち 47・49・52・58 は ★今日書いたもの（ポリシーと表だけ・関数なし）
  ★古い束（01〜30）は 前のセッションで Code が確かめています（私は見ていません）
```

## 5. ★これからの決まり（自分に課す）

```yaml
1 SQL を書いたら ★sql_schema_check を通す（Code に頼む）
2 関数を書いたら ★種を作って 数える。「作れた」で終わらせない
3 ★守り（引き金・ポリシー）を入れたら、必ず5つを確かめる:
  ①退会 ②親を消す ③連鎖の SET NULL ④保存期間の掃除 ⑤身元の固定×SET NULL
4 ★「1件だけ選ぶ」を書いたら、★掛け持ちの人で 正しいかを 必ず問う
5 ★ビューを作ったら security_invoker と revoke を 必ず入れる
```

---

## 6. ★道具を測ったら、道具の穴と 本物の穴が 出た（2026-09-23 夜）

```yaml
やったこと: ★今日の9件を そのままの形で並べ（tools/fixtures）、★いまの道具が何件捕まえるかを測った
  → tools/guard_coverage.py（★「道具がある」と「効いている」は 別）

★測って分かった 道具の穴:
  sql_guard_lint の逃げ道の判定に 'is not null' を入れていた
  → ★ふつうの条件文にも出るので、「逃げ道がある」と誤って判定していた
  → ★9件のうち1件（消せなくする引き金）を 見逃していた。直した

★直したら 62本から 6件出た。うち ★1件は本物（しかも 本番に既に当たっている）:
  ★確定した点のある学生が 退会できない
    evaluation_scores.student_id / evaluation_reviews.student_id は ★CASCADE
    → 退会で 点が消される → sql/48 の引き金が その削除を止める
    ★本番に 引き金は すでにある。いま確定した点は0件なので 詰まっている人はいない
    ★点が入った瞬間に出る（＝販売開始のあと）
  → sql/64 で直した:
    ★退会のときは通す（本人のものだから）／人が「なかったことにする」のは これまでどおり止める
    ★試しの環境で 実際に人を消して確かめた（①人が消す=止まる ②退会=通る ③点も一緒に消える）

★残る5件は 誤検知でした:
  ・消す操作では動かない引き金（UPDATE だけ・INSERT だけ）
  ・逃げ道を 印ではなく 関数の中の条件で書いたもの（sql/55）
  ・後の版で直しているもの（33→44／48→53・64）
  → ★「-- guard-ok: 理由」を 1行書く仕組みを足した（★理由を必ず書かせる）

いまの数: ★9件中7件を 機械が捕まえる。★残り2件は「繋ぐ先の表の取り違え」と「時差の向き」
  → ★この2つは 動かさないと分かりません。だから ★種を作って数える工程を 必ず挟む
```

## 7. ★道具をこれ以上 増やすか

```yaml
★増やしません。理由:
  いま3つ（守りの書き方・閉じ方・表と列）＋測る道具1つ。★これで 9件中7件
  残る2件は ★静かに0件を返す形で、★文からは 判定できません
  → ★道具を足すより、★「種を作って数える」を 必ずやることのほうが 効きます
★代わりに決めたこと:
  1 ★SQL を書いたら 3つの見張りを通す（Code が当てる前に）
  2 ★関数を書いたら 種を作って数える（「作れた」で終わらせない）
  3 ★逃げ道を置かないときは「-- guard-ok: 理由」を 必ず1行
  4 ★道具を直したら guard_coverage で 測り直す（★道具の穴が いちばん怖い）
```
