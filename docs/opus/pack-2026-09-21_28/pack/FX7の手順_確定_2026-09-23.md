# FX7 ── 試しの台帳を作り直す（確定の手順）

2026-09-23 ／ Opus ／ 裁定161 FX7。以前の「FX7の進め方」を、Code の実測（48か所の差）を受けて確定版にしたもの

## 0. 選択：★㋑（入れ物ごと作り直す）で確定

```yaml
決め手（Code の実測 2026-09-23）:
  試しと本番の差 48か所。★そのうち2つは「本番にある安全の直し」が試しに入っていない
    ・招待の取り合いを防ぐ直し
    ・合言葉の総当たりよけ
  → 試しで「通った」証明が、本番の安全を示していなかったことが、これで確かめられた
なぜ ㋐（流し直す）では足りないか:
  移行を流し直しても「足りないものを足す」だけで、★試しに余分にあるものは消えない
  （未ログインで呼べる関数 40以上・本番に無い関数・余分な権限）
  ㋑なら「移行だけで作れること」も同時に確かめられる（移行の抜けがあれば、その場で止まる）
```

## 1. 前提（これが済むまで始めない）

```yaml
P1: 記録の外で本番に当てた変更が、すべて移行として記録されていること
    ★確かめ方: Opus が ledger_inventory drift を走らせ、「目録は変わったのに移行が増えていない」が0件
P2: 試しの利用者を作る種（test_seed）が1本になっていること（Code・半日）
    含めるもの: 学校2つ（1つは教室くらいの大きさ）／学長・事務長・課長・先生2・学生3・保護者1／
      札の組み合わせ（master・post・meibo・sched_all・sched_mine・gyoji・renraku_all・monka_write・monka_read は切った状態も）／
      名簿・担当・レッスン・連絡・行事・公演を最小限
    ★利用者は Supabase の管理 API で作る（パスワードは環境変数。SQL に書かない）
P3: いまの試しで動いている作業が無いこと（束の証明の途中で消さない）
```

## 2. 手順（Code）

```yaml
1 種を書く（P2）。いまの試しで1回流して、動くことを確かめる
2 新しい入れ物を作る: Supabase の新しいプロジェクト（名前 la-voce-test2・東京・Free）
  ★無料枠は 組織あたり2つ。先に 古い試しを止めるか、組織を分ける（坂本さんの判断が要る）
3 移行を頭から当てる（apply_migration）。★エラーが出たら、それが「移行の抜け」→ 直さずに Opus に返す
4 種を流す
5 これまでの証明（S1・S2・束0〜3・sql/01〜25 の VERIFY）を一度すべて通す
6 Opus が ledger_inventory twin を走らせ、本番との差が0件になるまで直す
7 0件になったら、接続先の差し替えを1か所にまとめ、古い試しを消す
```

## 3. 確かめ（Opus が走らせる）

```yaml
python3 tools/ledger_inventory.py twin <本番の控え> <新しい試しの控え>   → 0件
python3 tools/ledger_inventory.py audit <新しい試しの控え>              → A1〜A8 が0件（許可リストを除く）
python3 tools/ledger_inventory.py drift <前の本番> <いまの本番>          → 記録漏れ0件
```

## 4. 期日と順番

```yaml
9/24〜9/25: P1（移行の記録）と P2（種）
9/26:       坂本さんの判断（新しいプロジェクトを作るか・古い試しを消すか）
9/29〜10/1: 手順2〜7
★これ以降の証明は、すべて新しい試しで行う（公演・値段の実装の証明もここで）
```
