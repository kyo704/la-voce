# 自分の道具を壊して確かめた（2026-09-23・Opus）

Code から「26件の検査が ずっと動いていなかった」と報告があったので、★自分の道具も同じかを確かめた。
方法: わざと壊して ★赤くなるかを見る（緑のままなら その道具は何も見ていない）

## 1. 結果

| 道具 | 壊し方 | 結果 |
|---|---|---|
| mobile_parity | スマホ2本の片方の文字を変える | ★当たる（食い違い2件） |
| ruling_mock_check | 裁定178 が求める文字を見本から消す | ★当たる（NG 1件） |
| allsc | 画面の中で 存在しない関数を呼ぶ | ★当たる（開けない 6→7） |
| works/check.py | 実在しない役を場面に入れる | ★当たる（NG 1件） |
| ★price_check | 個人の値段（しらべる 580→777）を台帳だけ変える | ★★当たらなかった |

## 2. ★見つけた抜け（price_check）

```yaml
何が起きていたか:
  price_check は ★学校の値段（1人あたり・段・下限）しか見ていなかった
  ★個人の値段（ぜんぶ・学生・しらべる・よそおい・つたえる・型）は 一度も突き合わせていなかった
なぜ気づかなかったか:
  今日 つたえるを 3,600→6,000円 に変えたとき、この道具が「食い違い0件」と言った。
  ★それは「個人の値段を見ていない」からで、★合っていたからではない
直した: check_individual を足した（正の値段が ★「円」付きで 見本に出ているかを見る）
  ★数字だけの一致では駄目だった。「777」は 色の指定（#777）にも紛れる → ★「777円」の形で見る
  selftest にも「個人の値段を変えたら当たるか」を入れた（★いまは PASS）
直したあと: しらべるを 777円にすると ★MISMATCH（1件）と出る
```

## 2b. 続き（⑥⑦・同じ日の夜）

| 道具 | 壊し方 | 結果 |
|---|---|---|
| promise_diff | 「出演者の体調は、どの画面からも見られません」を逆の意味に変える | ★当たる（増えた1・消えた1） |
| ★fail_closed_lint | search_path の無い security definer を anon に渡す SQL を置く | ★★当たらなかった |

```yaml
fail_closed_lint の抜け:
  F4（search_path）・F5（anon に渡していない）を ★記録を書く関数にしか かけていなかった
  → ★記録を書かない関数は、search_path が無くても anon に渡しても 素通り
  直した: F4・F5 を ★すべての security definer の関数に かける
  ★意図して未ログインに渡すもの（公開ページ・お問い合わせ・保護者の同意・値段の計算）は
    ★理由つきの一覧に入れて 許す（そうしないと 毎回赤くなり、見なくなる）

★直したら 自分の SQL で 誤検知が出た（もう1つの発見）:
  F2 が「いちばん外の begin」から見ていたため、★記録を外に出してある正しい関数まで 赤にしていた
  → ★いちばん近い（内側の）begin から見る形に直した
  ★自己試験の側が この誤りに合わせて書かれていた → 試験も正した
  教訓: ★試験が通っているのは、試験が正しいという意味ではない
```

## 2c. 残り2つ（⑧⑨）

| 道具 | 壊し方 | 結果 |
|---|---|---|
| policy_diff | 体調の記録のポリシーに `has_can(org_id,'meibo')` を足した前後を渡す | ★当たる（札+['meibo']と出る） |
| promise_coverage | 約束2件・試験1件（片方に番号なし）を渡す | ★当たる（覆われていない1件） |

```yaml
★policy_diff は 今日いちばん効く道具だった:
  「先生は生徒の記録を見られない」を壊す変更は、★必ずポリシーの変更として現れる
  → ★本番のポリシーを 毎日1回控えて、前日と比べるだけで、この製品の背骨が守れる
  いまは 人が呼ぶ形。★毎日の見張りに入れるべき（Code へ）
```

## 3. 学んだこと

```yaml
★「緑」は「見ている」という意味ではない。★見ていないだけかもしれない
→ 道具を作ったら、★必ず壊して赤くなることを確かめる（CLAUDE.md の selftest の決まりを 全部の道具に）
★今日の私の発言の訂正: 「値段の食い違い0件」は ★学校の値段について だけ正しかった
★試験が通っていることは、★試験が正しいことを意味しない（fail_closed_lint の F2 がそうだった）
```

## 4. まだ壊して確かめていない道具

```yaml
release_check（上の束。個別に確かめたので 一応足りる）
ledger_inventory（本番と試しの突き合わせ。★DB が要るので Code と一緒に）
perm-matrix（★実装を読む道具。Code の持ち物）
base_manifest_check（今日 直したばかり。auth の引き金の抜けを 実際に見つけた＝当たる）
ledger_inventory（本番と試しの突き合わせ。★DBが要るので Code と一緒に）
perm-matrix（★実装を読む道具。Code の持ち物）
base_manifest_check（★今日 直したばかり。auth の引き金の抜けは 実際に見つかった＝当たる）
→ 次にやるときは この順で
```

---

## 5. 今日の結論（9つ試した）

```yaml
当たった: mobile_parity／ruling_mock_check／allsc／works check／promise_diff／policy_diff／promise_coverage（7）
★当たらなかった: price_check（個人の値段を見ていない）／fail_closed_lint（記録を書かない関数を見ていない）（2）
  → どちらも直した。selftest にも入れた
★誤検知も1件見つけて直した: fail_closed_lint の F2（記録が外にある正しい関数を赤にしていた）
  ★自己試験が その誤りに合わせて書かれていた

★決まりにすること:
  1 道具を作ったら ★必ず壊して赤くなることを確かめる（selftest を全部の道具に）
  2 ★「緑」は「見ている」ではない。見ていないだけかもしれない
  3 ★「試験が通る」は「試験が正しい」ではない
  4 ★policy_diff を 毎日の見張りに入れる（背骨を守る いちばん安い方法）
```
