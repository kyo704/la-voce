# 事前確認 ── 「消える処理」と「引き金」の組み合わせ（読み取りだけ）

2026-09-23 ／ Opus。★今日の事故（ops_audit_log の外部キーで「学校を閉じる」が止まった）と同じ型が、ほかに無いかを本番で確かめたもの

## 0. いまの本番の状態（確認）

```yaml
ops_audit_log: ★無い（Code が戻した）。引き金8本も無い
→ 本番には sql/14（修正版）だけを当てればよい。sql/15 は、古い 14 を当てた環境（試し）のためのもの
既に本番にあるもの（Code が当てた分）: koen ほか公演の表・org_contracts・lesson_rounds・org_post_perm_log・monka_read_log・get_public_portfolio
```

## 1. 学校を消すときに動く引き金（本番・全部）

| 表 | 引き金 | いつ動くか | 消えるときに動くか | 危ないか |
|---|---|---|---|---|
| org_posts | trg_log_org_post_perm | 作る・変える・消す | ★動く | ✓安全。書き込み先（org_post_perm_log）に**外部キーが1本も無い**（Code の作り。正しい） |
| memberships | guard_grade_label | 作る・変える | 動かない | 安全 |
| enrollments | guard_enrollment_grade_label | 作る・変える | 動かない | 安全 |
| org_events | 2本（変える） | 変える | 動かない | 安全 |
| lessons・assignments・evaluation_scores・koen_sessions | 変えるとき | 変える | 動かない | 安全 |

結論：**いまの本番に、同じ型の危険は残っていない。**

## 2. これから当てる SQL の見直し（同じ型がないか）

```yaml
14（管理の操作の記録）: ★外部キーを外した（修正済み）。さらに ★学校ごと消えるときは記録しない を足した
  理由: 216人の学校を閉じると、連鎖で数百〜数千行の記録ができ、読めなくなる。閉じたこと自体は閉じる処理の側で1行
06（記録を台帳が書く）: 引き金は「変えたとき」だけ。学校を消すときには動かない → 安全
12（公演）: 稽古の変更の引き金も「変えたとき」だけ → 安全
13（子ども）: ★直した。保護者が退会すると 子どもの枠ごと消えていた（公演の香盤表から子どもが消える）
  → 保護者の欄を空にして 枠と呼び名は残す。緊急の連絡先だけ 消す（引き金を1本足した）
10・11・01・02・05・07・08: 消えるときに動く引き金なし → 安全
```

## 3. これから守る手順（事前確認に足す）

```yaml
新しい外部キーを作るとき:
  1 その親の表を消す処理があるか（学校を閉じる・退会）
  2 同じ親への既存の外部キーがどうなっているか（CASCADE か SET NULL か）を並べて見る
  3 ★記録の表には外部キーを付けない（記録は親より長生きする）
新しい引き金を付けるとき:
  1 その表が連鎖で消える側か（消える側なら、DELETE でも動く引き金は慎重に）
  2 動いた先の書き込みが、消えかけの親を参照していないか
  3 1つの親を消したときに、何行になるか（数千行になるなら、まとめて1行にする）
確かめの項目に必ず入れる:
  ★「行事・役職・場所・請求・公演を持つ学校を作って、閉じきれること」
  ★「名前がどこにでもある人が、退会しきれること」
```
