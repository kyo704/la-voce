# RULING 159 — 門下を開いた記録（monka_read_log）が書かれていなかった

```yaml
ruling: 159
date: 2026-09-21
version: design-v19
severity: ★最優先（利用者への約束が事実と違っていた）
from: Code「monka_read_log への書き込み処理が存在しない（insert のポリシーはあるが呼び出すコードが無い）」
related: 仕様シート②（開いた記録：生徒・先生の画面に出さない。求められたら30日以内に開示。消せない）／③（開いた記録は消せない）
```

## 1. すぐに

```yaml
STEP_0: 書き込みができるまで、monka_read で他人の門下の中身を返す経路を止める（台帳の側で）
  reason: 「見ると記録に残る」が偽の間、読める状態を残さない。本番は0件なので影響なし
  画面: 「いまは 調整中のため 門下を 開けません」（理由を技術的に書かない）
```

## 2. 作り

```yaml
Q1_何を開いたとするか: 門下のやりとりの中身が返ったとき、門下ごとに1行。門下の一覧（名前だけ）は数えない
WHERE: 中身を返す処理（RPC）の中で、台帳が先に記録を書き、書けたら中身を返す。書けなければ返さない（fail closed）。画面からの呼び出しに任せない
Q2_理由: 空を許さない。選ぶ形 ①事故・苦情の調べ ②ご本人からの求め ③法令に基づく求め ④その他（短文必須・100字まで）
Q3_同じ日に何度も: 開くたびに1行。まとめない。画面で日ごとにまとめて見せるのは可
列: 誰（user_id）・そのときの役職・いつ・org_id・どの門下・理由の種類・理由の短文
ポリシー: insert は RPC だけ。update・delete を作らない。select は monka_read を持つ人と運営だけ
文言: 実装の「ご本人にも伝わります」を直す
  正: 「見ると 記録に 残ります（誰が・いつ・どの 門下を）。消せません。ご本人から 求められたら、30日以内に お見せします」
```

## 3. 過去の分

```yaml
STEP_2: Supabase のログが残っている範囲で、monka_read を持つ人が他人の門下の中身を取得した跡を確かめる
RECORD: 結果にかかわらず「修正の記録」（消せない）に1行 ── いつから約束と実装が違っていたか・いつ直したか・過去の読み取りの有無・確かめた方法
IF_FOUND: Code は DECISION_NEEDED で報告。本人への伝え方は Opus が決める
```

## 4. VERIFY

```yaml
- 中身を開くと1行増える／一覧だけでは増えない
- 理由が空だと開けない（④で短文が空でも）
- 記録の書き込みを失敗させると中身が返らない
- update・delete が台帳で拒否される
- 生徒・先生の画面に開いた記録が出ない
```

## 5. 繰り返さないために

```yaml
- 「記録に残ります」「消せません」「伝わります」など、画面が約束している文を一覧にし、それぞれを実装のどこが守っているかを台帳に1行ずつ（約束の台帳）。守る処理が無い約束を0件にする
- insert のポリシーだけがあって呼び出しが無い表を探す検査を1本
```

## 6. STEP_1 の実装判断（2026-09-21 追記）

```yaml
Q1_理由の種類: DB の CHECK 制約（4値）。参照表は作らない
  reason: 値は4つで固定。変えるなら移行で意図して変える。参照表は結合と RLS が1つ増えるだけ
Q2_そのときの役職: 役職名を文字列でその時点のまま写す（post_name_at）。post_id も参照用に持つ（null 可・外部キーの連鎖削除なし）
  あわせて: 見た人の表示名もその時点で写す（name_at）。user_id は連鎖削除しない
  reason: 記録は当時の事実。役職や名前が後で変わっても、アカウントが消えても、記録は変わらず・消えない
Q3_範囲: まとめない。3つに分ける。ただし「全部の画面を RPC に」ではない
  - RPC を通すのは「monka_read で他人の門下を読む」道だけ。先生が自分の門下・学生が自分の門下を読む直接の select は今のまま
  - 分け方:
      S1: org_messages の select のポリシーから monka_read の枝を外す（＝STEP_0 の台帳側の止め。これで直接の select では他人の門下が0行）
      S2: 表（monka_read_log）＋ RPC（security definer・search_path 固定・anon から実行権を外す・has_can(org_id,'monka_read') を確かめる・理由が無ければ拒否・記録を書いてから返す）＋試験
      S3: 運営の画面の「門下を読む」だけを RPC に。文言の訂正。見本の入口（お知らせ・連絡の札）
  - 順: S1 → S2 → S3。S1 だけで約束の偽りは止まる

約束の台帳（§5）:
  下書き: 484件（しない429・する55）。印（note/warn/usu/wl）だけを対象にする抽出の決まり（Sonnet）
  順番: ★「する」55件から（記録に残る・伝わる・消せない・残る ── 今回壊れていた型）。次に「しない」のうち、台帳や権限でしか守れないもの（見えない・渡らない・作らない列）
  守る処理の欄: コードを読んだだけで埋めない。試験（Playwright か SQL）で確かめた結果を書く。確かめられないものは「未確認」と書き、0件になるまで残す
  仕様シート①〜⑤の行も同じ台帳に入れる
```

## 7. 進み（2026-09-21 追記）

```yaml
S1: 本番適用済み。証明 5/5（実在の試しの利用者で。なりすましは使わない ── ask_ledger.py 決まり3）
S2: 試しの台帳で 13/13。本番は未適用（SQL 全文と試験の中身を示して、坂本さんの承認のあと）
順番: ★STEP_2（過去の跡）を S3 より先に。Supabase のログは残る期間が短く、待つほど確かめられる範囲が消える
```

## 8. 表の重なりと「本人に伝わるか」の決定（2026-09-21・Code の催促への答え）

```yaml
事実（Opus が本番で確かめた）:
  org_message_reads: 0行。どの関数も使っていない。insert は本人の名なら誰でも何にでも（所属の確かめなし）。select は本人・先生・その門下の学生
  monka_read_log: 0行。open_monka_thread だけが書く（S2）
  get_monka_read_notices（本番にある）: monka_read_log から、呼んだ学生が居た門下の分だけ「いつ（viewed_at）・どの先生の門下か」を返す。
    誰が開いたか・理由は返さない。コメントに「裁定 その76 追補」（やめた後にも確かめられるように。ended_at で絞らない）
  見本 P_kaita の 9/10 の決定: 「生徒の画面に常に出すと、見られているという気持ちを毎日与える。記録の正直さを下げる」→ 運営の中だけ

DECISION:
  Q1: ★monka_read_log を正式な1本にする。org_message_reads は廃止（リポジトリに呼び出しが無いことを Code が確かめてから drop）
  Q2: 併存させない
  見え方（2段。9/10 の理由と その76 追補の両方を満たす形）:
    運営（monka_read を持つ人・master）: 全部（誰が・そのときの役職・いつ・どの門下・理由）
    門下の学生: 「いつ・どの門下が開かれたか」だけ。★自分で見に行ったときだけ（設定 → 門下を 開いた 記録）。
      知らせ・印（バッジ）・帯・きょうの画面には出さない ── 9/10 の「常に出すと見られている気持ち」を避ける
      誰が・理由は出さない（求めがあれば30日以内に全部を開示）
    先生（門下の本人）: この版では出さない（その76 追補は学生の分だけ）
  文言（見本 design-v21 で3本とも直した）:
    「その記録は 運営の 中だけで 見ます。門下の 学生は、自分で 見に行けば『いつ 開かれたか』だけ 分かります
     （誰が・理由は 出しません。知らせも 出しません）。ご本人から 求められたら、30日以内に 全部 お見せします。」
  ★実装の「ご本人にも伝わります」: 「伝わる」を「自分で見に行けば分かる」に直す（押しつけの知らせではないことを言葉で）

確かめてほしいこと（Sonnet）: 台帳の その76 追補の本文。もし「知らせ（通知・帯）で伝える」と書いてあれば、9/10 の決定と正面から食い違うので、
  坂本さんに1問で聞く。上の DECISION は「自分で見に行けば分かる」で置いた
S3 への影響: S3 仕様 §3 に「学生の画面：設定 → 門下を 開いた 記録（get_monka_read_notices・いつ・どの門下だけ）」を足した
```

## 9. 「その76 追補」の結果（2026-09-21・Sonnet の確認）

```yaml
結果: 「通知・帯で知らせる」の記述は0件。9月10日の決定と食い違いは無い → §8 の決定はそのまま
問題: その76 追補の本文が台帳（docs/ledgers/）に無い。言及は関数の註1か所と報告書1か所だけ ── 失われた裁定の可能性
決定: 本文を再構成して、この §8・§9 を正とする
  再構成した内容（本番の get_monka_read_notices の註と §8 から）:
    - 門下の学生は、自分の門下が開かれた「いつ・どの先生の門下か」を確かめられる（誰が・理由は出さない）
    - 門下をやめたあとも確かめられる（ended_at で絞らない）
    - 学校（org_id）と先生（teacher_id）の両方で合わせる（別の学校の同じ先生に当たらないように）
    - 知らせ・帯・バッジでは出さない。自分で見に行ったときだけ（§8）
Sonnet: 台帳に「その76 追補：本文喪失。裁定159 §8・§9 で再構成（2026-09-21）」を1行。
  ほかにも本文の無い裁定が無いか、台帳の番号の抜けを数える（その1〜その最後まで、本文のある番号の一覧）
```
