# 未決機能の実装 ── 間違いを減らす手順

2026年9月16日／Opus
★一般論ではありません。★2026年9月14日〜16日に実際に起きた失敗だけを対象にしています。

---

## 0 この文書の作り方

★今日〜3日で起きた見落としを、★原因ごとに数えました。

| 原因 | 件数 | 例 |
|---|---|---|
| ★A 実物を見ずに判断した | ★13 | 「匿名に開いている」「引数なしで呼んでいる」「LPRは章5」 |
| ★B 文言だけで確認した | ★4 | 文字5段→3段／足せるもの8→6／学ぶ4→2／プランが空 |
| ★C 片方向しか数えていない | ★1 | 「足したもの」は数えたが「抜けたもの」を数える道具が無かった |
| ★D 中継で指示が落ちた | ★2 | draft_key 7回／教室シェルの指示が未達 |
| ★E 配線の漏れ | ★1 | 「応募を選ぶ」に行けない（★私が作った） |
| ★F 二度手間な画面 | ★1 | 「項目を減らす」を1画面増やした（★私が作った） |
| ★G 先行する指示ファイルを見なかった | ★1 | articles.json |
| ★H 同じ名前の画面を上書きした | ★1 | `SC['さがす']`（★私が既存を潰した） |
| ★I 重複定義を数えていなかった | ★6 | `SC['学ぶ']` `SC['さがす']` `setBusy` `bCol` `bRow` `bAllOn` |
| ★J 遷移先を確かめていなかった | ★1 | 「くわしい決まりを見る」→ 無関係な `/legal/terms` |

★★AとBで17件。★**ここを潰せば大半が消えます。**

★★ただし ★I と J は、★この手順の初版で ★**防げませんでした。**
★§4に「配線」と書きましたが、★★押した先がどこかを見ていませんでした。
★★手順のほうが誤っていました。§4と§4-2で直します。

---

## 1 ★実装の前に ── 画面台帳を機械で作る

### 1-1 見本から抽出する

```bash
node -e "
const fs=require('fs');
const s=fs.readFileSync('見本.html','utf8');
const def=[...s.matchAll(/SC\[.([^'\]]+).\]\s*=/g)].map(m=>m[1]);
console.log('画面数', def.length);
def.forEach(k=>console.log('  ', k));
"
```

★★**手で一覧を書かないこと。** ★手で書くと、必ず数が合いません。

### 1-2 台帳に3列を持つ

```
| 画面名 | 見本 | 実装 | ★区分 |
```

★★区分は ★**4つ**です。★3つではありません。

```
① 一致            ★何もしない
② 見本にあって実装に無い   ★作る
③ 実装にあって見本に無い   ★理由を書く
★④ 見本にあるが、意図して実装しない  ★★除外リストに入れる
```

★★★④が無いと、★同じ議論が毎回起きます。★今日「端末を見る」で起きました。

★★④の初期リスト

```
端末を 見る      ← Supabase にセッション一覧の API が無い
ダークモード      ← 未実装（台帳㉜）
```

---

## 2 ★確認は、必ず両方向

★今日いちばん重い道具の欠陥です。

```
✕ 見本の字が実装にあるか      ← ★これだけを見ていた
○ ★見本にあって実装に無いもの  ← ★数える
○ ★実装にあって見本に無いもの  ← ★数える
```

★★**両方を出力する道具にしてください。** ★片方だけの報告は受け取りません。

---

## 3 ★画像で並べる ── 文言一致では出ない4件

```
文字の大きさ  見本5段 → 実装3段
足せるもの    見本8つ → 実装6つ
学ぶ          見本4件 → 実装2件
プラン        見本に中身 → ★実装は空
```

★★★**どれも文言の検索では出ません。**

### ★手順

```
1 ★同じ幅（360px）で、見本と実装のスクリーンショットを撮る
2 ★左右に並べる
3 ★数を数える（札の数・行の数・段の数）
4 ★空でないか見る
```

★★運営は ★役職で画面が変わります。★`P_home` は3通り。★**1画面で最大3枚**です。

---

## 4 ★配線を機械で確かめる

★私が今日使ったものです。★★「応募を選ぶ」の漏れは、これで見つかりました。

```bash
node -e "
const fs=require('fs');const s=fs.readFileSync('対象.html','utf8');
const def=new Set([...s.matchAll(/SC\[.([^'\]]+).\]\s*=/g)].map(m=>m[1]));
const used=new Set();let m;
const re=/push\(\\\\'([^\\\\']+)\\\\'/g; while((m=re.exec(s)))used.add(m[1]);
const re2=/push\('([^'\\\\]+)'\)/g; while((m=re2.exec(s)))used.add(m[1]);
console.log('★呼ばれているが定義なし（壊れたリンク）');
[...used].filter(k=>!def.has(k)).forEach(k=>console.log('  ',k));
console.log('★定義はあるが呼ばれていない（行き止まり）');
[...def].filter(k=>!used.has(k)).forEach(k=>console.log('  ',k));
"
```

### ★4-2 ★重複定義を数える（★初版に無かった）

★2026年9月16日、★見本に ★6件の二重定義が ありました。
★**どれも目では見つかりませんでした。**

```bash
node -e "
const fs=require('fs');const s=fs.readFileSync('対象.html','utf8');
const d={};[...s.matchAll(/^SC\['([^']+)'\]=/gm)].forEach(m=>d[m[1]]=(d[m[1]]||0)+1);
const f={};[...s.matchAll(/^function ([A-Za-z_][A-Za-z0-9_]*)\(/gm)].forEach(m=>f[m[1]]=(f[m[1]]||0)+1);
console.log('★SC重複:', Object.keys(d).filter(k=>d[k]>1).join(', ')||'なし');
console.log('★関数重複:', Object.keys(f).filter(k=>f[k]>1).join(', ')||'なし');
"
```

★★★**後の定義が前を潰します。★静かに。**

★★実際に起きたこと

```
SC['学ぶ']   ★古い4項目版と新しい7章版。★古いほうが死んでいた
SC['さがす'] ★★私が既存の画面に同じ名前を付け、★潰した
setBusy 他4件 ★★中身が違う（TBUSY vs getT）。★どちらが正か不明
```

### ★4-3 ★新しい画面を足す前に、名前を確かめる（★初版に無かった）

```bash
grep -c "SC\['その名前'\]=" 対象.html
```

★★★**0 でなければ、★別の名前にしてください。**

★私は ★`さがす` で ★既存の画面を潰しました。

### ★4-4 ★遷移先を確かめる（★初版に無かった）

★§4 は ★「どの画面へ push するか」しか見ていませんでした。
★★**押した先が正しいかは、見ていませんでした。**

```bash
# ★画面の外へ出る道を全部出す
grep -oE "(location\.href|href=|router\.push|window\.open)[^\"']*[\"'][^\"']+" 実装 | sort -u
```

★★確かめること

```
□ ★その URL は 実在するか
□ ★★その URL は、この行が言っていることと 合っているか
□ □ 「くわしい 決まりを 見る」→ ★/legal/terms は ★誤り
```

★★★2026年9月16日、★これが本番に出ていました。

### ★4-5 ★4つの網が要ります

```
① ★文言    見本の字が実装にあるか（★これだけを見ていた）
② ★構造    数を数える（★札の数・段の数・行の数）
③ ★配線    壊れたリンク・行き止まり・★重複定義・★遷移先
④ ★画像    並べて見る
```

★★★**1つでは足りません。**

```
ISSUE_1 戻るボタンが無い     → ★③で出る
ISSUE_2 言葉がアイコンに      → ★④でしか出ない
ISSUE_4 無関係なページに飛ぶ  → ★③で出る
学ぶの重複                   → ★★③でしか出ない
文字5段→3段                  → ★②か④
```

---

★★★**道具そのものを較正すること。**

★私は今日、★正規表現を間違えて ★**全画面が「到達✕」と出ました。** ★道具を直してから答えています。

```
較正のやり方
　★わざと1つ配線を外して、★検出されることを確かめる
　★検出されなければ、★道具が壊れています
```

---

## 5 ★実装の単位 ── 1画面1コミット

```
✕ ポートフォリオ4画面をまとめて1コミット
○ ★1画面ずつ。★1コミットずつ
```

★理由：★壊れたとき、★どれが原因か分かります。

★★今日、★`/admin` が404になった原因を★特定できませんでした。★複数のコミットが同時に入っていたためです。

---

## 6 ★実装の前に、1行で答えさせる

★画面を作る前に、★次の3つを書かせてください。

```
1 ★この画面の入口は、どの画面の、どの行か
2 ★この画面から戻ると、どこに戻るか
3 ★この画面を持たない人には、何が出るか（★無効化ではなく、無い）
```

★★★1が書けないなら、★**配線が設計されていません。**

★★今日、★私が「応募を選ぶ」でこれを書き忘れました。

---

## 7 ★先に、既存の指示ファイルを探す

★今日、★`articles.json` を見ずに「LPRは章5」と裁定しました。★誤りでした。

```
実装の前に ★grep する
　*.json ／ *.config.* ／ docs/ の中
　★その機能の名前で
★見つかったら、★それが正
★私の裁定と食い違ったら、★ファイルに従い、★私に報告
```

---

## 8 ★中継で落ちる問題

★draft_key は ★7回送って届きませんでした。

```
✕ 長い指示文の中に表を埋める
○ ★ファイルに書いて、ファイルを渡す
○ ★短い1つの表だけを、単独で送る
★受け取ったら「届いた」と1行返す
```

★★★**届いたかどうかを確かめずに、次を送らないでください。**

---

## 9 ★出来上がったら確かめること

### 9-1 機械で

```
□ ★構文が通る
□ ★壊れたリンク 0
□ ★行き止まり 0
□ ★★重複定義 0（SC・関数とも）
□ ★★外へ出る道の 行き先が、その行の字と 合っている
□ ★見本にあって実装に無い → ★④の除外リスト以外は 0
□ ★色の直書き 0（トークンから取る）
□ ★独自の margin 0（--sp1/--sp2/--sp3 から取る）
□ ★14px未満の字 0（個人側）
□ ★「★」が画面の字に 0
```

### 9-2 目で

```
□ ★見本と並べた画像（★役職ぶん全部）
□ ★空の画面が無い
□ ★持っていない人に、押せない札が出ない
□ ★戻るの字が正しい（‹ 何に戻るか）
```

### 9-3 ★報告の形

```
STATUS: SUCCESS / FAIL
SCREEN: <画面名>
ENTRY: <どこから入るか>
BACK: <どこへ戻るか>
ABSENT_FOR: <持っていない人に何が出るか>
MOCK_DIFF: <見本との差。★無ければ「なし」>
GUARDS: 構文○ / 壊れたリンク0 / 行き止まり0 / 直書き0
ERR: <NULL または内容>
```

---

## 10 ★見本が誤っていることもあります

★今日、★見本に ★5,800円 と書いてありました。★確定は 4,800円です。

```
見本と確定文書が食い違ったら
　★確定文書が正
　★★見本を直す。★実装を見本に合わせない
　★私に報告する
```

★★★**見本が正、は「見本を勝手に変えない」という意味です。** ★誤りを残す意味ではありません。

---

## 11 ★私（Opus）への報告で、必ず書いてほしいこと

★今日、私は ★**13件撤回しました。** ★全部「実物を見ずに答えた」ものです。

```
★私が「たぶん」「はずです」と書いたら
　→ ★実物を引いて、★違っていたら教えてください

★私が章・列・ファイル名を指定したら
　→ ★実在するか確かめてください
　→ ★無ければ「ありません」と返してください
```

★★★**私の裁定を、確かめずに実装しないでください。**

### ★11-2 ★私が作った画面も、同じように疑ってください

★2026年9月16日、★私は ★`SC['さがす']` を足して、★**既存の画面を潰しました。**

```
★私が「新しい画面を足しました」と言ったら
　→ ★同じ名前が 既に無いか grep してください
　→ ★あれば「既にあります」と返してください
```

★★私は ★14回 撤回しています。★★**私の出力は、確かめる対象です。**

---

## 12 ★実装の順番（この機能群）

```
1 ★経歴・公開の範囲・公開ページ・紙   ← ★表が要る。RLS を同じ紙に
2 ★録画を足す                         ← 1の続き
3 ★しらべていること                   ← ★N1・N2 の報告が先
4 ★本番のふりかえり
5 ★もっと 深く・深い記事               ← ★売らない。tier の出し分けを作らない
6 ★評価の型・評価の項目（運営）
7 ★行事の対象・採点・点を入れる（運営）
8 ★さがす 一式（8画面）               ← ★いちばん重い。最後
```

★★★各段で ★§9 の確認を通してから、次へ。

---

## 13 ★新しい表を作るときの決まり（再掲）

```
★RLS の決まりを、★表を作るのと同じ紙に書く
★roles は authenticated。★{public} は auth.uid() の縛りつきのみ
★USING と WITH CHECK の両方
★org 系の表には org_id
★USING(true) を書かない
```

★★今日、★`org_invitations` と `teacher_invitations` で ★同じ形の穴が2件出ました。★★後から足すと、こうなります。

---

*この手順は、2026年9月14〜16日の実際の失敗から作りました。
★第2版（9月16日 夕）── ★初版で防げなかったもの3件を足しました：
　★重複定義（§4-2）／★名前の衝突（§4-3）／★遷移先（§4-4）。
★★初版は「配線」と書きながら、★押した先を見ていませんでした。手順のほうが誤っていました。

★守れなかった項目があれば、その項目を直してください。★手順のほうが誤っている可能性が、
★実際に ありました。*
