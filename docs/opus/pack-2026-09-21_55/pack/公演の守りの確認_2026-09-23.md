# 公演の守りの確認（2026-09-23・Opus が本番を読み取りだけで）

## 1. 招待の穴 ── ★無い（良い）

```yaml
9月14日に見つかった型（匿名で読める・書き換えられる）を 公演でも確かめた
koen_join_codes: ★{public} のポリシーが無い。運営（koen_can_manage）だけ
  入るのは 関数（join_koen_by_code）を通る。★合言葉の行そのものは読めない
→ org_invitations・teacher_invitations と同じ穴は ★無い
```

## 2. 期限切れの公演を読めるか ── ★読める（裁定144 のとおり）

```yaml
koen_can_see は 期限（valid_until）を見ていない → ★期限後も読める・書き出せる
書き込みだけ 引き金で止まる（sql/33・44）。★決めたとおりの形
```

## 3. 子どもの緊急の連絡先 ── ★正しい形（裁定147）

```yaml
koen_kid_contacts: RLS が有効で ★ポリシーが0本＝誰も直接は読めない
読み書きは 関数（read_kid_contact・set_kid_contact）だけ。読んだ記録が残る仕組みもある
```

## 4. 公演を誰が作れるか ── ★台帳ですでに決まっていた

```yaml
koen_insert の条件: 自分が持ち主 かつ（個人の公演 または ★行事の札（gyoji）を持つ）
→ 学校の中では ★行事の札を持つ人だけが作れる。学生は勝手に作れない
★私が「決まっていない」と言ったのは誤り。台帳を見れば書いてあった
```

## 5. ★見つけた2件（sql/46 で直す）

```yaml
① 合言葉の総当たりに 回数制限が無い:
   join_koen_by_code は 理由を返さない作り（正しい）だが ★何回でも試せる
   教室の招待には code_attempts（失敗を数える表）がある。★公演では使っていなかった
   直し: 同じ人の直近15分の失敗が10回を超えたら断る。★断る答えは「合っていない」のまま（理由を分けない）
② 人数の段に達しても 合言葉が閉じない:
   見本は「人数に達したら この合言葉は自動で閉じます」と約束している
   いまは 段を超えると 引き金が例外を投げるだけ＝★入ろうとした人にエラーが出る
   直し: 段に達していたら ★合言葉を閉じ、入ろうとした人には「合っていない」と同じ答えを返す
   ★運営には koen_code_status で「閉じたこと・人数・段・直近15分の失敗の回数」を出す
```

## 6. ★書きながら見つけた落とし穴（同じ間違いを繰り返さないために）

```yaml
pgcrypto（digest）は ★extensions スキーマにある。
関数は search_path を 'public' に固定しているので、★digest( と書くと 見つからない
→ ★extensions.digest( と書く。そのまま当てていたら 合言葉が使えなくなっていた
★新しい試し環境で 実際に動くことを確かめた
```

## 7. 坂本さんの判断が要る（1件だけ残った）

```yaml
★20時より後の呼び出しの警告を、誰に出すか（裁定147）
  いまの見本: 運営にだけ出る
  案A（勧め）: 運営にだけ出す ── 決めるのは運営であり、保護者に出すと 断りづらくさせる
  案B: 保護者にも出す ── 知らせる義務を重く見るなら
```
