# Opus が確かめた残り（2026-09-23）

本番を読み取りだけで確認。★1件は 書類で約束したのに 仕組みが無い

## 1. ★保存期間が 実際には切れていない（重い）

```yaml
約束したこと:
  裁定169 #8・安全管理措置 1-3: 「学校の管理の操作の記録は ★90日保存」
  裁定172: 学校を閉じても記録は残す（★1年・門下は3年）
本番の姿（2026-09-23 Opus が確認）:
  掃除の関数は ある: purge_ops_audit_log ／ purge_code_attempts ／ purge_closed_school_logs
  ★pg_cron が入っていない。cron スキーマも無い
  → ★誰も呼んでいない。記録は 消えずに たまり続ける
いまの実害: ops_audit_log は 0行（まだ使われていない）。★実害が出るのは 販売開始のあと
直し方（Code が選ぶ）:
  ㋐ pg_cron を入れて 1日1回 呼ぶ（Supabase の設定）
  ㋑ 配信側の定期実行（Vercel Cron）から サーバ経由で呼ぶ
  ㋒ 毎日の書き出しと同じ仕組みに相乗りする
★どれでもよいが「呼ばれていること」を ★出発の朝の確認に入れる
  確かめ方: 掃除の関数が最後に走った日時を記録する表を1つ持つ（下の sql/42 で用意）
```

## 2. 未ログイン（anon）が呼べる関数 ── 5本

```yaml
get_public_portfolio: ★意図どおり（公開ページ）。ただし 段3a で「切った相手・止められている人が出ない」ことを確かめる
submit_inquiry:       ★意図どおり（お問い合わせ）。総当たり対策（回数制限）を Code に確認
koen_tier_price:      値段の計算だけ。★問題なし
org_free_period:      導入期間の判定。★中身に個人のものが無いことを確認済み
accept_guardian_consent: ★合言葉（token）で受ける形。中身を読んだところ
  「無い・切れた・済んだ」を分けずに false を返している（裁定その77のとおり）→ ★正しい
  ただし ★総当たりの回数制限が 台帳側に無い。purge_code_attempts があるので 仕組みはある見込み
  → Code に確認: 合言葉の失敗を 数えているか
引き金の関数（log_post_change ほか）も anon に実行権が渡っている:
  ★直接呼んでも引き金以外では動かないので 実害は無いが、最小権限の考えからは 渡すべきでない
  → 次の掃除のときに revoke する（急がない）
```

## 3. 良かったもの（直すところ無し）

```yaml
security definer の関数 124本すべてに ★search_path が設定されている（乗っ取りの入口が無い）
未ログインから読める表 0件（裁定177 で確認済み）
NOT NULL × SET NULL の矛盾 0件（sql/17 で直したあと）
```

## 4. まだ確かめていないもの（★私には見えない）

```yaml
- Stripe の webhook の署名を確かめているか（実装の中）
- 合言葉の失敗の回数制限が 画面・サーバのどちらにあるか
- 毎日の書き出しが 実際に走っているか（置き場・暗号化・14日分）
- 退会の実行が 画面から本当に通るか（sql/17 を当てたあと）
- メールが 実際に届くか（学校の連絡担当者・障害の通知）
★どれも Code か 坂本さんしか確かめられない
```
