# P1〜P5 を 実際に動かして確かめた（2026-09-23・Opus）

★前回の報告の訂正: 「動かした」と言いましたが、実際は ★「関数が作れた」だけでした。
今回は ★種を作って 答えが正しいかを 数えました。

## 1. ★見つけた誤り（2件・どちらも 読んだだけでは分からない）

```yaml
① ★繋ぐ先の表を間違えていた（sql/56）
   my_timetable.period_id は ★my_periods（本人の時限表）を指すのに、
   私は ★org_periods（学校の時限表）に繋いでいた
   → ★外部キー違反で入らず、実際に入れてみて分かった
   ★「時限表は学校のもの」という思い込み。読んだだけでは気づけない
② ★時刻が9時間ずれていた（sql/56）
   generate_series(日付, 日付, interval) は ★timestamptz を返す。
   timestamptz に at time zone を付けると ★逆向きに変換される（UTC→JST）
   → 授業が ★1件も当たらなかった（0件のまま静かに通る＝いちばん危ない形）
   直し: ★d.d::date にしてから 足す
```

## 2. 確かめた結果（sql/56）

| 確かめること | 結果 |
|---|---|
| 同じ時間にレッスン → 重なりが出る | ★1件（正） |
| 重ならない時間 → 出ない | ★0件（正） |
| ★個人の公演の稽古 → 出ない | ★出ない（正・裁定183） |
| 授業・学生の同意なし → 出ない | ★0件（正） |
| 授業・学生の同意あり → 出る | ★1件（正） |
| 別の曜日 → 出ない | ★0件（正） |

```yaml
★確かめられなかったもの: busy_at そのもの（auth.uid() が空だと 0行で終わるため）
  → 中身の式だけ 同じまま取り出した確かめ用の関数で 判定を見た（その関数は ★消しました）
  ★札（sched_all）の確認は Code が 実在の試しの利用者で
```

## 3. ほかの確かめ

```yaml
sql/57: 5人未満 → ★全項目が空・suppressed=true（正）／5人以上は まだ確かめていない（種が要る）
sql/58: 4項目が返る。★ICS は 台帳に記録が無いので 常に「未」
sql/59: ★まだ1行も動かしていない（正直に）
```

## 4. ★まだ足りないもの（正直に）

```yaml
P1: ★SQL を1本も書いていない（ICS の購読の記録を持つかが 決まっていないため）
P3: ★毎月呼ぶ仕組みを 繋いでいない（run_retention への相乗り）／3項目は 元データが無い
P5: ★割り当てる関数が無い（枠の表だけ）。重なりの確認・採点への引き渡しも まだ
→ ★「文が通る」までは できた。「正しく動く」は ★56 だけ
```

## 4b. 続き（60・61 を作って 動かした・同じ日の夜）

```yaml
坂本さんの決定（2026-09-23）:
  ①★ICS の購読は 記録する（日付だけ・中身も回数も持たない）
  ②★くばりものは 数えない ③★学生の登録率は やめる
作ったもの:
  60 ics_subscriptions（本人だけが読める）＋ note_ics_seen（サーバだけ）＋ 数える関数
  61 P3 の仕上げ（使わない2列を外す・ICS を入れる・★月の1日に 掃除と同じ道で集計）
     ＋ P5 の jury_layout（枠を並べる）と jury_conflicts（重なりを教える）
★動かして確かめた:
  60: 同じ日に2回 → 行は1つ（★回数を持たない）／翌日 → last_seen だけ変わる／first_seen は残る
  61（P5）: 札が無い → ★NOT_SAITEN で止まる／部屋1 → ★15分ずつずれる（12分＋3分）／
     部屋2 → ★2人ずつ同じ時刻／★学年の順に並ぶ
★動かして見つけた誤り（3つ目・4つ目）:
  ③ enrollments.status の値は ★'active'（★'enrolled' ではない）→ 1人も並ばないところだった
  ④ 名前が空の人がいる → ★空の紙が出た。name で補い、それも無ければ「（名前なし）」
```

## 4c. 採点への引き渡し（sql/62・P5 の4点目）

```yaml
作ったもの: scoring_queue（★枠の順に並ぶ採点の画面）／scoring_next（次に入れる人）
★動かして確かめた（6項目とも 期待どおり）:
  ①その回の審査員でない → ★0行
  ②審査員に入れた → 枠の数だけ出る
  ③まだ点を入れていない → 済み 0
  ④1人ぶん入れた → 済み 1（★項目を全部入れ終えたら「済み」）
  ⑤自分の門下の学生 → ★印は出るが 枠は残る（外さない）
  ⑥並び → ★枠の順（ord）のまま。順位でも成績でもない
★試しながら 製品の守りが働いた:
  年齢が未回答の人を 門下にしようとして ★MINOR_TEACHER_LINK_BLOCKED で止まった（裁定どおり）
  ★見ているのは profiles.is_under_18（age_band ではない）
★意図して しないこと: 枠を並べ直しても ★点は消えない（別の表・結んでいない）
```

## 4d. ★掛け持ちの人で 間違う形だった（sql/63・自分で見直して発見）

```yaml
見つけたもの:
  busy_at は「日程の札を持つ学校」を ★limit 1 で 1つだけ選んでいた
  → ★掛け持ちの人（非常勤の先生・2校に通う学生）で ★片方しか見えない
    しかも ★どちらが選ばれるか 決まっていない（実行のたびに変わりうる）
  ★本番には すでに 3人います（複数の学校に所属）
直し（sql/63）:
  ★学校は 呼ぶ側が渡す（p_org）。こちらで選ばない
    先生は「いま どの学校の日程を組んでいるか」を 分かって呼んでいる
    ★こちらが勝手に選ぶと、別の学校の予定を 混ぜて見せることになる
  ★学生の同意も 学校ごとに持てる形に（主キーを user_id＋org_id に）
    → 「A大学には出す・B大学には出さない」が 選べる
★動かして確かめた（5項目とも正）:
  A校の景色＝1／B校の景色＝1（★混ざらない）／A校の同意＝あり／★B校の同意＝なし／
  ★同じ人が 学校ごとに 別々の同意を持てる
★これは「実装してよい」と言う前に 自分で見直して見つけたものです。
  ★確かめていない部分（札の確認）を「たぶん大丈夫」で通さない、が 効きました
```

## 5. ★Code へ1つ

```yaml
試しの環境の lessons が ★0行になっています（03:28 の1行が見当たりません）
  私の後片づけは 自分が作った行だけを指していたつもりですが、★消していないとは言い切れません
  ★必要なら 種を入れ直してください。以後、★私は 自分が作った id を控えてから消します
```
