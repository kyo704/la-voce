# RULING 190 — 代表が 見られるもの（★㋒を 採る）

```yaml
ruling: 190
date: 2026-09-24
from: Code の分析と 推奨（2026-09-24）
extends: 裁定138・139／台帳 08-11 の 境界線
```

## 0. ★㋒を 採ります。★Code の見立ては 正しい

```yaml
★㋐（名簿の鍵を 渡す）を 採らない理由 ── ★Code の言うとおり
  has_can('meibo') は ★学校ぜんぶの 名簿の鍵です
  名簿の 直接の 書き換え・招待・★よその門下の 生徒まで 付いてきます
  ★代表は 学生です。「★役職ではない・門下の中の 印」という 線が 崩れます
★㋑（何も 渡さない）を 採らない理由 ── ★これも Code の言うとおり
  代表の 存在意義は ★先生の手間を 減らすこと
  残るのが「重要を付ける」「投稿を消す」だけなら、★役目が ありません
★㋒（代表だけの 集計の関数を 作る）── ★これが 正しい
```

## 1. ★Code の追記（門下に限る）を ★裁定の本体に します

```yaml
★これが いちばん 大事な点です:
  get_timetable_submitted は ★学校ぜんぶを 返します
  monka_representative は ★「その門下」の 印です
  → ★そのまま 開くと、★よその門下の 生徒の 出し具合まで 見えます
★決定:
  ① ★同じ門下の 学生だけ（org_id ＋ teacher_id が 同じ）
  ② ★返すのは student_id と ★出したかどうか（真偽）だけ
     ★中身は 返さない（何時のコマか・どの授業か は ★見せない）
  ③ ★名前も 返さない ── ★画面が すでに 持っている 名簿と 突き合わせる
     （★関数から 名前を 返すと、★名前の 出どころが 2つに なります）
```

## 2. ★代表に できること（この3つだけ）

```yaml
★① 同じ門下の 誰が「まだ 出していないか」を 見る（★真偽だけ）
★② 門下の 投稿に 重要を 付ける／自分の 投稿を 消す（いままでどおり）
★③ 先生から 頼まれた 連絡を 門下に 流す（いままでどおり）
★できないこと（★はっきり 書く）:
  ✕ 名簿を 直す ✕ 招く ✕ 外す ✕ ★よその門下を 見る
  ✕ ★時間割の 中身を 見る（★「出した・出していない」だけ）
  ✕ ★体調の 記録（★言うまでもなく。代表でも 先生でも 見られません）
  ✕ ★催促を 送る（★代表にも 送る道は ありません。★見るだけ）
```

## 3. ★なぜ「真偽だけ」で 足りるか

```yaml
代表の 仕事は「★まだの人に 声をかける」ことです（★教室で・LINEで）
★そのために 要るのは「誰が まだか」だけ
★中身（何曜の 何コマか）は ★声をかけるのに 要りません
→ ★要らないものを 渡さない（裁定138 の 考えと 同じ）
```

## 4. どこに効くか（裁定188 の形）

```yaml
EFFECTS:
  prices:  ★なし
  ledger:  ★新しい関数 1本（下の sql/75）。★既存の get_timetable_submitted は ★変えない
           ★理由: 変えると 先生・事務の 画面に 影響します。★分けたほうが 安全
  mock:    ★スマホの「門下」に 代表のときだけ「まだの方」を 出す
           ★人数と 名前の 一覧だけ。★「出していません」と 書く（★責めない ことば）
  docs:    ★台帳 08-11 の 境界線の 文書に 1行（代表は 真偽だけ）
```

## 5. VERIFY

```yaml
- 代表が 呼ぶ → ★同じ門下の 学生だけ 返る
- ★よその門下の 学生は ★1件も 返らない
- ★代表でない 学生が 呼ぶ → 0行
- 先生が 呼ぶ → いままでどおり（★既存の関数は 変えない）
- ★返るのは student_id と 真偽だけ（★名前も 中身も 返らない）
- ★代表から 催促を 送る道が ない
```
