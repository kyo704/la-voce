# 見た目の 設計書 ── 実装に 当てるための 一式

2026年9月18日／Opus
裁定 その78・その81（暫定）
対象　★4本の 見本すべて。★内容・機能は 変えません。★見た目だけです

---

## 0 この 紙の 使い方

```
★第1章  ★色 ── ★これが 土台。★先に 入れてください
★第2章  ★文字の 大きさ
★第3章  ★しるし（アイコン）
★第4章  ★運営の ナビ
★第5章  ★表
★第6章  ★やって はいけないこと
★第7章  ★確かめ方
```

★★1章と2章は ★**全画面に 効きます。** ★ここを 先に 入れると、
★以後の 画面は 何も しなくても 揃います。

---

## 1 色 ── トークンに 集める

### 1-1 明るい 画面

```css
:root{
  /* ★文字 ── 4段。★★数値は 実測（2026-09-18・Code）*/
  --ink:#261913;      /* 本文・見出し                */
  --ink2:#5A4C40;     /* 従の 文字                   */
  --ink3:#6B5C4E;     /* 注記                        */
  --ink4:#706456;     /* ★いちばん 薄い（★4.71）★訂正 */

  /* ★地 */
  --bg:#EFE7D6;       /* 画面の 外        */
  --paper:#FBF6EA;    /* 画面の 地        */
  --card:#fff;        /* 箱              */
  --card2:#FDFAF3;    /* 箱（うすい）      */

  /* ★帯（節の 地色）── ★3つだけ。これ以上 増やさない */
  --band:#F6F1E4;
  --band2:#F3ECDD;
  --band3:#FBF3E2;

  /* ★線 ── 3段 */
  --line:#E4DAC4;
  --line2:#F0E9DA;
  --line3:#E8DFC8;

  /* ★強調 */
  --enji:#840C24;
  --enji-d:#8E1230;

  /* ★★状態 ── ★この 4色 いがいに 色を 使わない
     ★★これらは ★文字に 使います（「✓ 出席」「保存しました」など）
     ★→ ★4.5:1 が 要ります。★3:1 では 足りません          */
  --ok:#3C6A56;       /* 済んだ・合っている（★5.07）★訂正 */
  --warn:#825A14;     /* 気をつける        （★5.02）★訂正 */
  --err:#AD3846;      /* うまく いかない    （★5.01）★訂正 */
  --pick:#F6E4E8;     /* 選んでいる（★地。文字では ない）  */
}
```

### 1-2 暗い 画面

```css
html[data-th="dark"]{
  --ink:#EFE6D8; --ink2:#BCB1A3;
  --ink3:#A19383;     /* ★注記（★5.05）★訂正 */
  --ink4:#A18C7A;     /* ★いちばん 薄い（★4.71）★訂正 */
  --bg:#141210; --paper:#1E1812; --card:#241D17; --card2:#2A2219;
  --band:#2C241D; --band2:#2A231C; --band3:#3A1E22;
  --line:#3A342E; --line2:#2C2722; --line3:#3A322A;
  --enji:#E790A2; --enji-d:#C9607A;
  --ok:#69A082;       /* ★5.01 ★訂正 */
  --warn:#D9AC55; --err:#EA8894; --pick:#3A1E22;
}
```

### 1-3 決まり

```
★色を 直に 書かない。★必ず トークンを 通す
★★状態の 4色 いがいに 色を 使わない
　（調べ：★装飾で 色が 現れると、★状態を 示す 力を 失う）
★灰色は ★--ink2 / --ink3 / --ink4 の 3段だけ
```

### ★1-5 ★コントラストの 決まり（★2026-09-18 実測）

```
★CONTRAST_FLOOR    ★4.5   ── ★WCAG 2.2 AA（本文）
★CONTRAST_AIM      ★5.0   ── ★★狙う 値
★CONTRAST_AIM_FAINT ★4.7  ── ★--ink4 だけ
```

★★★**なぜ --ink4 だけ 4.7 か**

```
★5.0 に すると、★--ink3（5.05）と 並びます
★→ ★★文字 4段の ★最下段が 見た目に 消えます
```

★★★**数値を 揃えることより、★階層を 保つほうが 大事です。**

★★見張りで ★`--ink4 < --ink3` を 測って 確かめること。

### ★1-6 ★状態の 4色は ★文字です

```
「✓ 出席」        ★--ok
「保存しました」   ★--ok
注意の 文        ★--err
```

★★★**3:1（部品）では 足りません。★4.5:1（本文）が 要ります。**

★★2026-09-18、★Code が ★実測して 7色を 直しました。
★★私は ★4.48 や 4.18 を「わずかに 届かない」として ★通しませんでした。

★★★**WCAG は 床であって 天井では ありません。**
★★床の すぐ上に 座らせず、★5.0 を 狙ってください。


### 1-4 ★例外 ── 絵の 色

```
★羊の 部屋の 色（木・布・壁・肌）は ★トークンに しない
```

★理由：★★絵の 色であって、★画面の 階層では ありません。
★`--ink3` に 寄せると、★木と 文字が 同じ 色に なります。

---

## 2 文字の 大きさ ── 6段

```
12px    ★極小 ── 札の 中の 数、表の 見出し
12.5px  ★小   ── 注記、補足
13px    ★小中 ── 小見出し、帯の 字
13.5px  ★本文
14.5px  ★本文大
15.5px  ★見出し小
16px 〜 ★見出し（そのまま）
```

### 2-1 決まり

```
★★この 6段 いがいを 使わない
★12px より 小さい 字を 使わない
```

★調べ：★NN/g（★65〜89歳を 含む 計123名・19年3期）

```
★65歳以上は ★43% 遅い
★加齢の 影響は ★20代から。25〜60歳で ★年 0.8% 低下
★「本文 最低 12pt（16px）。★かつ ★拡大できるように」
　https://www.nngroup.com/articles/usability-for-senior-citizens/
```

★Woolsong の 主要層は ★50〜60代。★直接 当てはまります。

### 2-2 ★文字を 大きく する つまみ

```
★既に あります（4段：ふつう／大きめ／もっと／一番）
★★行の 高さが それに 連動します
```

```css
.tblwrap td,.tblwrap th{ height: calc(40px * var(--z,1)) }
```

★★★**つまみを 2つに しません。** ★行の 高さの 別 つまみは ★作りません。

---

## 3 しるし（アイコン）

### 3-1 出どころ

```
★Lucide に ならった 形
　★ISC ライセンス。★商用可。★画面での 帰属表示は 不要
　★24×24 の ます目、★2px の 線、★角と 先は 丸
　https://github.com/lucide-icons/lucide
```

★★配る ときは、★ライセンスの 写しを `LICENSES/` に 置いてください。
★画面に「Icons by Lucide」と 出す 必要は ★ありません。

### 3-2 決まり

```
★★曲線（C / A）を 使わない。★直線・円・楕円だけ
```

★理由：★★曲線だと、★絵として 正しいか 確かめられません。

★実際、★最初 曲線で 描いて ★4つが 壊れていました。

### 3-3 運営の しるし 10種

```js
var ICO = {
 home:'<path d="M3.4 10.8 12 3.6l8.6 7.2"/>'
     +'<path d="M5.4 9.6V20h4.2v-5.6h4.8V20h4.2V9.6"/>',
 users:'<circle cx="9" cy="7.4" r="3.2"/>'
      +'<path d="M3.4 20v-2.1 M3.4 17.9 L5.4 15.4 M5.4 15.4 L12.6 15.4 '
      +'M12.6 15.4 L14.6 17.9 M14.6 17.9 V20"/>'
      +'<circle cx="18" cy="9" r="2.2"/>'
      +'<path d="M16.6 13.6 L19.6 13.6 M19.6 13.6 L21 15.8 M21 15.8 V20"/>',
 cap:'<path d="M12 4.2 L2.6 8.6 L12 13 L21.4 8.6 Z"/>'
    +'<path d="M6.6 10.4 V16.4 M6.6 16.4 L12 18.8 M12 18.8 L17.4 16.4 M17.4 16.4 V10.4"/>'
    +'<path d="M21.4 8.8 V14.4"/>',
 grid:'<rect x="3" y="4.8" width="18" height="15.6" rx="2"/>'
     +'<path d="M3 9.8h18"/><path d="M8.4 4.8V2.6M15.6 4.8V2.6"/>'
     +'<path d="M7.4 13.2h1.2M11.4 13.2h1.2M15.4 13.2h1.2"/>'
     +'<path d="M7.4 16.8h1.2M11.4 16.8h1.2M15.4 16.8h1.2"/>',
 star:'<path d="M12 3.4 L14.5 9 L20.6 9.6 L16 13.7 L17.3 19.7 L12 16.6 '
     +'L6.7 19.7 L8 13.7 L3.4 9.6 L9.5 9 Z"/>',
 mail:'<rect x="2.8" y="5.2" width="18.4" height="13.6" rx="2"/>'
     +'<path d="M3.6 7 L12 13 L20.4 7"/>',
 gear:'<circle cx="12" cy="12" r="3.2"/><circle cx="12" cy="12" r="7.6"/>'
     +'<path d="M12 2.6v2.3M12 19.1v2.3M21.4 12h-2.3M4.9 12H2.6"/>'
     +'<path d="M18.6 5.4 17 7M7 17l-1.6 1.6M18.6 18.6 17 17M7 7 5.4 5.4"/>',
 search:'<circle cx="10.8" cy="10.8" r="6.4"/><path d="M15.6 15.6 L21 21"/>',
 fold:'<path d="M14.6 6 L8.6 12 L14.6 18"/>',
 unfold:'<path d="M9.4 6 L15.4 12 L9.4 18"/>'
};
```

### 3-4 ★個人の 帯 5種

```js
var ICO_P = {
 'きょう':'<circle cx="12" cy="12" r="4.2"/>'
   +'<path d="M12 2.4V5 M12 19V21.6 M21.6 12H19 M5 12H2.4"/>'
   +'<path d="M18.8 5.2 17 7 M7 17 5.2 18.8 M18.8 18.8 17 17 M7 7 5.2 5.2"/>',
 '記録':'<path d="M4 20.4 L4 16.6 L16.4 4.2 L20.2 8 L7.8 20.4 Z"/>'
   +'<path d="M14.4 6.2 L18.2 10"/><path d="M4 16.6 L7.8 20.4"/>',
 'ふりかえる':'<path d="M3.4 20.2 V3.6"/><path d="M3.4 20.2 H20.6"/>'
   +'<path d="M6.6 16.4 L10.4 11.4 L14 14 L19 6.8"/>',
 'ノート':'<path d="M5 3.6 H19 V20.4 H5 Z"/><path d="M8.2 3.6 V20.4"/>'
   +'<path d="M11.4 8 H16.2 M11.4 11.4 H16.2 M11.4 14.8 H14.2"/>',
 'ひつじ':'<ellipse cx="12" cy="13" rx="6.2" ry="5.6"/>'
   +'<ellipse cx="5" cy="10" rx="2.8" ry="1.8"/>'
   +'<ellipse cx="19" cy="10" rx="2.8" ry="1.8"/>'
   +'<ellipse cx="12" cy="8.4" rx="4" ry="2.2"/>'
   +'<path d="M10 13.4 h.01 M14 13.4 h.01"/>'
};
```

★★「ひつじ」は ★4案 描いて、★★21px でも 読めるものを 選びました。
★最初は 横向き・脚4本で、★実際の 大きさで 潰れていました。

### 3-5 出し方

```js
function svg(k,sz){
 return '<svg viewBox="0 0 24 24" width="'+(sz||22)+'" height="'+(sz||22)+'" '
  +'fill="none" stroke="currentColor" stroke-width="1.9" '
  +'stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">'
  +(ICO[k]||'')+'</svg>'}
```

★`fill="none"` ★`stroke="currentColor"`。★色は 親から 受け取ります。

### 3-6 ★★選んだ ときの 見せ方 ── ★重要

```css
/* ★個人の 帯 */
.tb i{width:21px;height:21px;display:flex;align-items:center;
 justify-content:center;opacity:.72}
.tb.on i{background:transparent;color:var(--enji);opacity:1}

/* ★運営の ナビ（レール） */
.side.rail .nv{background:transparent;border-radius:0}
.side.rail .nv.on{background:transparent;box-shadow:inset 3px 0 0 var(--enji)}
.side.rail .nv.on i{color:var(--enji);opacity:1}
```

★★★**しるしを 塗りつぶさないこと。**

★★前の 実装は `.tb.on i{background:var(--enji)}` でした。
★★元の 枠が ★2px の 棒だったので 正しかったのですが、
★★★そこへ 絵を 入れると、★絵の 後ろが 塗りつぶされ ★四角に 見えます。

---

## 4 運営の ナビ

### 4-1 幅

```
展開  ★232px
レール ★68px
```

★★190px から 拡げました。★ラベルが 窮屈だったためです。

### 4-2 まとまり ── 5つ

```js
var NAVG = [
 ['',        [['ホーム','home']]],
 ['ひと',    [['名簿','users'],['門下','cap']]],
 ['とき',    [['日程','grid'],['行事','star']]],
 ['やりとり',[['連絡','mail']]],
 ['しらべ',  [['設定','gear']]]
];
```

★調べ：★Carbon「2次ナビが ★5項目 超なら 左パネル。★3階層は 作らない」

### 4-3 たたむ

```js
var WIDE = ['役職の一覧','名簿','日程を組む','置ける枠','採点'];
function autoRail(){
 var t = S.stack.length ? topOf().s : S.tab;
 return WIDE.indexOf(t) >= 0 && S.dev !== 'pc' }
function isRail(){ return S.rail || autoRail() }
```

```
★手で   ★Ctrl / ⌘ + B
★自動   ★表の 画面 ＋ iPad の とき
★★PC では 自動に しません（★幅が 足りているため）
```

### 4-4 ★たたんでも ラベルを 消さない

```css
.side.rail .nv span.lb{position:absolute;width:1px;height:1px;
 overflow:hidden;clip:rect(0 0 0 0)}
```

★★読み上げには 残します。★指を 乗せると 名前が 出ます。

★調べ：★NN/g「★デスクトップで、ナビを アイコンだけに 隠すな」
★★「★ナビゲーションでは、単語は 千の絵に 値する」

### 4-5 さがす（⌘K）

```js
var PALL = [
 ['画面',['ホーム','名簿','門下','日程','行事','連絡','設定']],
 ['よく する こと',['出欠つけ','日程を組む','行事を出す','お知らせを書く','招く','採点']],
 ['決まり',['役職の一覧','評価の型','コマを決める','場所を決める']]
];
```

```
★⌘K ／ Ctrl+K で 開く。★Esc で 閉じる
★★持っていない ものは 出さない（★役職で 出し分け）
★★★ナビの 代わりでは ない。★足すだけ
```

★調べ：★「★パレットは ナビを 補強する もので、★置き換えでは ない」
★★「★settings を ナビから 消して ⌘K に 隠すと、
　　★初日の ユーザーは 設定画面を 見つけられない」

---

## 5 表

### 5-1 貼り付け

```css
.tblwrap{overflow:auto;position:relative;max-height:560px;
 background:var(--card);border:1px solid var(--line);border-radius:12px}

/* ★見出しの 行 */
.tblwrap thead th,.tblwrap tr:first-child th{
 position:sticky;top:0;z-index:3;background:var(--paper);
 box-shadow:inset 0 -1px 0 var(--line)}

/* ★左の 列（錨）*/
.tblwrap .stick,.tblwrap .anc{position:sticky;left:0;z-index:2;
 background:var(--card);box-shadow:1px 0 0 var(--line2)}
.tblwrap thead .stick,.tblwrap thead .anc,
.tblwrap tr:first-child .stick{z-index:4;background:var(--paper)}

/* ★行の 高さ ── ★文字の 大きさに 連動 */
.tblwrap td,.tblwrap th{height:calc(40px * var(--z,1))}
```

★★`.tblwrap` に 当てるので、★**全部の 表に 一度に 効きます。**

### 5-2 なぜ 要るか

```
★NN/g「画面より 大きい 表では、★ヘッダー行・ヘッダー列を 固定せよ」
　https://www.nngroup.com/articles/data-tables/

★「ユーザーが 最初に 見る 列が ★錨（正しい 行を 見ている 確証）。
　★名簿なら 氏名。★横に すべって 錨が 流れると 位置を 見失い、
　★行ごとに 戻って 確かめる 手間が 生じる」

★「スクロールで 列ラベルが 消えると『ラベル なしで 数値を 読む』ことに なり、
　★似た 数値の 列で 取り違えが 起きる」
```

### 5-3 ★密度切替は 作らない

★Carbon は 32 / 40 / 48px の 3段を 持っていますが、★★入れません。

```
★つまみが 2つに なる（★文字4段 ＋ 行の 高さ）
★Woolsong は 設定を 増やさない 製品
★★50〜60代の 方には、★1つの つまみで 両方 動くほうが 分かる
```

### 5-4 ★表と カードの 切り替え

★裁定その75・その80。

```
★広いとき ★表  ── 横に 並べて 比べる
★狭いとき ★カード ── 1人・1件を 見る

★境界は ★実測から 組み立てる
　TABLE_AT = ★表の幅 + ★殻の 左右の 余白
　★★数値を 書き写さない
```

★★例（役職の 表）：★905 + 28 = ★933px

★★★見張りが ★殻の padding を 読み、★余白を 変えた日に 気づくこと。

---

## 6 ★やって はいけないこと

```
✕ ★色を 直に 書く
✕ ★状態の 4色 いがいに 色を 使う
✕ ★12px より 小さい 字
✕ ★6段 いがいの 文字サイズ
✕ ★★しるしを 塗りつぶす（background で 塗る）
✕ ★曲線（C / A）で しるしを 描く
✕ ★ナビを ハンバーガーに 隠す
✕ ★たたんだ とき ラベルを 消す（★読み上げからも）
✕ ★行の 高さの 別 つまみ
✕ ★境界の 数値を 直に 書く
✕ ★羊の 部屋の 色を トークンに 寄せる
```

---

## 7 ★確かめ方

### 7-1 機械で

```bash
node -e "
const fs=require('fs'), vm=require('vm');
const s=fs.readFileSync(F,'utf8');

// ★構文 ── ★new Function では 行番号が 出ません。★vm.Script を 使う
[...s.matchAll(/<script[^>]*>([\\s\\S]*?)<\\/script>/g)].forEach(x=>{
  try{ new vm.Script(x[1],{filename:'s.js'}) }
  catch(e){ const m=String(e.stack).match(/s\\.js:(\\d+)/);
    console.log('構文ERR', e.message, m?'行'+m[1]:'') }});

// ★トークン ── 未定義・自己参照
const used=new Set([...s.matchAll(/var\\(--([a-z0-9-]+)/g)].map(m=>m[1]));
const def =new Set([...s.matchAll(/--([a-z0-9-]+)\\s*:/g)].map(m=>m[1]));
console.log('未定義', [...used].filter(k=>!def.has(k)));
console.log('自己参照', s.match(/--([a-z0-9-]+)\\s*:\\s*var\\(--\\1\\)/g)||[]);

// ★文字の 段階
const sz=[...new Set([...s.matchAll(/font-size:(\\d+(?:\\.\\d+)?)px/g)].map(m=>+m[1]))]
  .filter(v=>v<16).sort((a,b)=>a-b);
console.log('段階', sz.join('/'), sz.some(v=>v<12)?'★12px未満あり':'OK');
"
```

### 7-2 ★道具を 較正する

```
★道具を 書いたら、★わざと 1件 該当するものを 作る
★★検出されなければ、★道具が 壊れています
```

★今日 実際に あったこと

```
★私の SVG 描画に ★楕円の 対応が なかった
　→ ★楕円を 使った 案が 全部 消えて 見えた
　→ ★★「この案は だめだ」と 誤って 判断しかけた

★構文検査に new Function を 使った
　→ ★行番号が 出ず、「構文OK」を ★3回 誤報告した
　→ ★vm.Script に 変えて、★括弧の 二重を 特定した
```

### 7-3 ★目で

```
□ ★しるしを ★実際に 使う 大きさで 見る（21px・26px）
□ ★選んだ ときに 塗りつぶされていないか
□ ★暗い 画面で 崩れていないか
□ ★文字の つまみを 4段 すべて 動かす
□ ★iPad たて（834）で 表が 横すべりしないか
```

---

## 8 ★入れる 順番

```
1 ★色の トークン（§1）        ── ★全画面に 効く
2 ★文字の 6段（§2）           ── ★全画面に 効く
3 ★表の 貼り付け（§5-1）       ── ★.tblwrap に 当てる。全表に 効く
4 ★しるし（§3）               ── ★帯・ナビ
5 ★ナビの たたみ（§4）         ── ★運営のみ
6 ★さがす（§4-5）             ── ★運営のみ
7 ★表と カードの 切り替え（§5-4）★役職・名簿
```

★★1〜3が ★**費用対効果が いちばん 高い。** ★一度 入れると、
★以後の 画面は 何も しなくても 揃います。

---

## 9 ★私が 今日 失敗したこと（★同じ轍を 踏まないために）

```
★1 文字サイズの 一括置換
   ★7.5〜12px を 全部 12.5px に した
   → ★★見出し・注記・帯が 同じ 大きさに なり、★階層が 消えた
   ★「底上げ」の つもりが ★「平坦化」だった

★2 色の 一括置換
   ★トークンの 定義まで 置換した
   → ★--ink:var(--ink) に なり、★画面が 黒や 白に なった
   ★★定義（--名前:#XXXXXX）と 使用（var(--名前)）を 区別する

★3 しるしを 入れた とき
   ★既存の .tb.on i{background:...} を 見なかった
   → ★絵の 後ろが 塗りつぶされ、★四角に 見えた

★4 羊の しるし
   ★大きい 表示だけで 選んだ
   → ★実際の 21px では 潰れて、★羊に 見えなかった
```

★★★**共通するのは、★変更が 当たる 範囲を 確かめずに 走らせたこと**です。

★★対策

```
★一括置換の 前に、★★何件 当たるかを 数える
★★置換の 対象外に すべきものを 先に 決める
★しるしは ★実際に 使う 大きさで 確かめる
```

---

*裁定 その78（レイアウト）・その81（色・文字・しるし）。
★2026年9月18日。★調べに 基づく 部分は 出典を 付けました。*
