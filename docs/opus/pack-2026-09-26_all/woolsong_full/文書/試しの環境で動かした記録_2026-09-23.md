# 試しの環境で動かした記録（2026-09-23・Opus）

★決まりのとおり: ①ファイルにしてから当てる ②当てたら その場で報告する（今日の反省から）

## 1. 当てたもの（新しい試し環境 orutyqtfygvzcuhjgsch）

```yaml
sql/45（公演の後半）: koen_runsheet_sheet／koen_fees_sheet／koen_dues_sheet／
  koen_money_summary／my_koen_paper  → ★5本とも 作れた（文の誤りなし）
sql/46（合言葉の守り）: join_koen_by_code（作り直し）／koen_code_status → ★作れた
  ★extensions.digest( の書き方で 通った（public のままなら「関数が見つかりません」で落ちる）
★このあと リセットする前提です。sql/43 と同じ扱いで、★本体は 45・46 のファイルを使ってください
```

## 2. ★動かして分かったこと（限界も含めて）

```yaml
確かめられた:
  文の誤り・列名の誤りが無いこと（作れた＝構文と列は通る）
  公演が無い id を渡すと 0行（例外にならない）
★確かめられなかった（正直に）:
  合言葉の中身（★段に達したら閉じる／★15分に10回で断る）は、
  ★SQL の画面から呼ぶと auth.uid() が空なので NOT_AUTHENTICATED で止まる
  → 実際に試したら「断られた（NOT_AUTHENTICATED）／合言葉は開いたまま」でした
  ★なりすまし（JWT の細工）は使いません（裁定の決まり）
→ ★ここは Code が「実在の試しの利用者」で確かめてください:
  ①段に達している公演に 合言葉で入ろうとする → 断られ、★合言葉が閉じる
  ②合っていない合言葉を11回 → 11回目も 同じ答え（NO_MATCH）／code_attempts に10行
  ③運営が koen_code_status → 閉じたこと・人数・段・直近15分の失敗が見える
  ④出演者が koen_code_status → 0行
```

## 3. 後片づけ

```yaml
確かめ用に作った公演・合言葉・出演者15人 → ★消した（koen 0行・code_attempts 0行）
保存期間の模型（t_demo）→ ★消した
★残っているのは 45・46 の関数だけ（リセットで消えます）
```
