# ★★★11 土台の 書き出し（★裁定175 ㋐）── ★積み上がる ほう

2026-09-23。★実行ルートには 書きません（台帳 …… 決まった ことは ここ）。

---

## 一 ── 決まった こと

```
★本番の 台帳を 変えるのは apply_migration だけ。
★★SQL Editor での 直接の 変更は **禁止**。
```

★裁定175 の 恒久の 決まり です。★94表の 抜けは、★この 決まりが 無かった 時期の 積み重ね です。

---

## 二 ── 土台は 何か

★`supabase/migrations/20260101000001〜11_base_*.sql`（11本）。
★★2026-09-23 の 本番の **形** を、★目録（pg_catalog）から 機械で 書き出した もの です。

| 本 | 中身 |
|---|---|
| 01 | 拡張（★`pg_trgm` は `public`）・スキーマの 権限・★これから 作る ものの 既定の 権限 |
| 02 | 表 104 ／ 列 959 ／ 連番 1 |
| 03 | しばり 425（主キー104・一意27・検査109・外部キー185） |
| 04 | 索引 216 |
| 05–07 | 関数 99 |
| 08 | 引き金 25（★うち `auth.users` の 1本） |
| 09 | 行の 決まり 104表 ／ ポリシー 182 |
| 10 | 権限 ── 表 305・列 152・関数 301 |
| 11 | 註 140 |

★★中身（行）は 入って いません。★`insert` は 1つも ありません。

---

## 三 ── ★忘れやすい ところ（★2回 つまずいた ところ）

### ㋐ ★`auth.users` の 引き金

```
on_auth_user_created  →  public.handle_new_user()
```

★★これが 無いと、★人を 作っても `profiles` の 行が できません。
★★★`public` だけを 見る 目録では **見えません**。★Opus の `base_manifest.sql` の
　`triggers` も `public` だけ を 数えます（★本番 24・新 24 で ○ に なりました）。

★★見つかった のは、★種まきが `MINOR_TEACHER_LINK_BLOCKED` で 止まった から です。
　★★**動かして みた から** 分かりました。★数えるだけ では 分かりません でした。

★`tools/base_twin.py` に「外の引き金」を 足しました。★わざと 落として 見つかる ことを 確かめました。

### ㋑ ★これから 作る ものの 既定の 権限

```
本番 …… postgres / public / 表 → postgres, authenticated, service_role   ★anon は 無い
新品 …… postgres / public / 表 → postgres, anon, authenticated, service_role
```

★★ここを 写さないと、★**あとの 移行が 作る 表**に `anon` が 付きます。
★★★土台の 01 で 外して います。

### ㋒ ★`AND` の 平ら化

★PostgreSQL は `AND` の 入れ子を 読み直す ときに 平らに します。
★`between` から 来た 入れ子は 残るので、★同じ 意味でも 字が 変わり、★md5 が 合いません。

★★`tools/base_constraint_fit.py` …… ★ちがう ものを 見つけて 書き換え、★また くらべ、
　★合わなければ 元に 戻します。★手で 1本だけ 直しません。

---

## 四 ── 作り直す 手順

```bash
python3 tools/schema_dump.py                      # ★本番の 形を 読む（読むだけ）
python3 tools/fx7_replay.py --ref=<入れ物> --ok    # ★頭から 当てる
python3 tools/test_seed.py --ref <入れ物> --ok     # ★種を まく
python3 tools/base_manifest_check.py <本番> <新>   # ★11区分（Opus）
python3 tools/base_twin.py <入れ物>                # ★14区分（Code）
python3 tools/ledger_inventory.py twin <本番> <新> # ★関数（Opus）
```

---

## 五 ── ★9/14 以降の 47本を どう するか

★★土台は「**きょうの** 本番の 姿」です。★47本が した ことは、★もう 土台の 中に あります。
★★だから、★頭から 流すと 2本目で `42710 already exists` に なります。

★★★決め ── ★47本は **記録するだけ** に します（★土台と 同じ やり方）。

```
新しい 入れ物の 記録 …… 58本（★土台 11 ＋ 9/14 以降 47）
★そのうち 実際に 流れた のは 土台の 11本 だけ
```

★★これで「移行だけで 作れる」は 成り立ちます。★作れる のは **土台** です。
★★★47本は 歴史 として 残ります。★消しません。

★★これは 裁定175 条件3 の 字とは ちがいます。★条件3 は「土台＋47本が 頭から 当たる」
　と 書いて います。★それが 成り立つ のは、★土台が **9/14 より 前の 姿** の ときだけ です。
　★その 姿は どこにも 残って いません。★だから 取れません。
