# La Voce ／ 作業中の状態（会話にしか無いことの控え）

**この文書の目的**：仕様書に書かれていない事柄だけを置く場所です。
会話が要約されると失われるもの — まだルート文書へ反映していない決定、
坂本さんの返事待ちの問い、調査の途中経過 — を、ここに落とします。

**★仕様の内容をここに写さないこと。** 仕様は docs/ の各文書が持っています。
写すと、同じことが2か所にあって片方だけ古くなります。

最終更新: 2026-08-27

---

## 1. 坂本さんの確認待ち（実機で見ていただくもの）

| 何を | どこで | 状態 |
|---|---|---|
| 「先生とつながる」が新規アカウントで見えるか | もっと | 確認待ち |
| 周期の帯の色・ホームの1行・「生理が始まった／終わった」 | ホーム／ノート | 確認待ち |
| /dashboard が開くか（タイムアウト対応後） | 全体 | 確認待ち |
| 招待画面に先生の名前が出るか | もっと＞先生とつながる | 確認待ち |
| カレンダーの升目が「色」から「点」に変わった見え方 | ノート | 確認待ち |
| ★羊のおうち A-1・A-2（§9 のゲート）柵と羊の重なり・接地の影 | おうち | ★ゲート。再確認待ち |
| ★羊が「寝る」ところ（zzz が出て、丸くなっている状態） | おうち＞お部屋 | ★未観測。座り姿と取り違えていた |
| 羊が「座る」ところ（本を持っている状態） | おうち＞お部屋 | 済。椅子の位置に正しく追随していた |
| A-4 ドラッグ中の落ちる先の目印 | おうち（模様替え） | 確認待ち |
| メーターの弧から段階ごとの色を外した見え方 | 分析 | 確認待ち |

## 2. 実行をお願いしている SQL

```
supabase/migration_cycle_periods.sql            実行済み（★再実行を推奨）
supabase/migration_data_region.sql              ★実行の確認が取れていない
supabase/migration_invitation_teacher_name.sql  実行済み
supabase/check_activity_minutes_coverage.sql    実行済み（③=4日）
supabase/migration_record_mode.sql              未実行（無くても動く）
```

★`migration_cycle_periods.sql` の再実行をお願いしています。
周期の二重書きを直す前に「今日の記録」から初日を付けた分が、
まだ `entries.cycle_start` にだけ残っている可能性があるためです。
何度実行しても重複しません（on conflict do nothing）。

## 3. まだルート文書に書いていない決定

- **ACWR の過去分は案A**（全期間に同じ規則）。実データの影響は4日のみ。
  ポイントと違い ACWR は保存された残高ではないため、切り替え日を作らない。
  → ルート文書 G2-10.5 に記載済み。テストでも固定済み。
- **分析画面の色は案A**（lib/tokens.js を正とし、仕様書の16進値は採らない）。
  仕様書の値は画素からの採色でずれている。→ tokens.js のコメントに記載済み。
- **値で色を変えない（案B）** を、確認を待たずに実施した。
  仕様 §7-3・§7-5・§1-4 が明示しており、見本も数字を一色で描いているため、
  「仕様が既に答えている」と判断した（escalation の基準に照らして）。
- **R4（この分析を強くする）** は、項目の条件が実在しないため日数だけを書く。
  嘘の「または◯◯を5日」を作らない。

## 4. 返事待ちの問い

いまのところ、返事待ちの問いはありません。

## 5. 調査の途中・積み残し

- **G3-15 の同意は作り直しが必要**。いまは profiles の3列で、
  textHash が無い／撤回の記録が無い／目的別に分かれていない／履歴が持てない。
  分ける単位は最低4つ（記録・統計・周期・食事と就寝）。
  ★撤回とアカウント削除は別の操作。撤回では行を消さない。
- **描画仕様 §3 の部品**：A・B・C・D・F・H・I 完了。E は色の違反だけ直した。
  ★E の完全版（1日ぶんの点を全部描くドットプロット）は未実装。
    いまは効果量＋信頼区間の形で、§3-E の目的（差を確かなものに見せない）は
    満たしているが、日ごとの点は描いていない。描くには
    effectiveHabitRanking に群ごとの生の値を持たせる必要がある
    （いまは n1・n0・g・ciLow・ciHigh だけ）。
  ★G（期間の帯＋点）は未実装。対象が「周期と声」で、周期の §7 分析自体を
    坂本さんの判断で見送っているため、いま作る先が無い。
    周期の分析に着手するときに、G と一緒に作ること。
- **表示名まわり**（低優先）：新規登録の名前が display_name に入っていない。
  もっと＞プロフィールの表示名の入力欄が2か所にある。
  レッスン画面からも変えられるようにする（未着手）。
- **LINE**：`NEXT_PUBLIC_LINE_ADD_FRIEND_URL` の設定待ち。

## 5.4 未受領の文書

- **lavoce-守りのテスト3本.md** … 受け取れていません。会話の貼り付けが
  空で届き、ディスクにもありません（済みMD にも無し）。再送待ち。
  ★内容は分かりませんが、狙いは聞いています。3本目（無料の書き出しを
  課金で塞がないこと）だけは、いま何の検査もありません。
  G3.2 に入る前に必ず入れること。

## 5.45 ★取り込まなかった「新しい版」の内容（毎回もどるので記録）

外から来る `lavoce-02-統合実行ルート-v4.md` の新しい版は、毎回
次を巻き戻してきます。取り込まないこと。

- G3.5 の 21・22 が「招待フローの正式化」「記録が途切れています」に戻る
  → 改善タスクv2 §3-7 で「レッスン記録の共有」に差し替え済み。
    招待フローの正式化と途切れ通知は、オーナー1名・生徒1名の規模に
    合わないため凍結（ルート文書 §10）。
- G3.5 の見出しが「指導者経由で広げる」に戻る
  → こちらの決定は「指導者が生徒に返す」。広げる話ではない。
- 29 のカレンダー連携から、名前の区別の注記が消える
  → G3.7-24.2「自分の予定カレンダー連携」と別機能。同じ名前で呼ばない。
- 済 の印が全部消える

★取り込むべきものもあります。判断して前へ進めること。
今回取り込んだもの: 15〜20人に配る（10人では薄い）／14.5 守りのテスト3本／
13 に「滞在時間を目標にしない」／9 の名称／G3.2 の箱。

## 5.6 ★族の分割（分析の検出力と族の設計.md）— G2-11 に戻るときに

進捗ドットの文言だけは先に直しました（§2-2）。残りは分析の作業に戻るときに。

- **中核5項目の族** … BH-FDR は、この5つの中だけに掛ける。
  いま全部を1つの族として扱っていれば、検出力が落ちて何も出なくなります。
- **探索の族** … 文章も数字も、一切出さない。図だけ。
- ★ゲートそのものは変えません（件数・効果量・FDR の3つは §6-1 のまま）。
  変えるのは「何を検定するか」と「どう説明するか」だけです。
- §3 効果量の不確かさを隠さない（点推定だけで「中程度」と書かない）

## 5.7 ★保留: LoadTracker がまるごと描かれていない（坂本さんの判断で先送り）

**急ぎではありません。忘れないための記録です。**

`LoadTracker`（components/VocalTracker.jsx）は、関数の定義が1つあるだけで、
呼び出しが0件です。★どの画面からも描かれていません。
そのため `LOAD_FIELDS_BY_PROFESSION` は、全職業ぶんが死んでいます。

  声楽家     4項目 … ★削除済み（音域の低・高／ダイナミクス／通過数）
  アナウンサー 3項目 … onAirMinutes / isLive / consecutiveSegments
  声優       3項目 … sessionMinutes / characterCount / hasExtremeVocalization
  ポップス   3項目 … venueVolume / monitorVolume / consecutivePerformanceDay

合計13項目のうち、4つを消し、9つが残っています。
（「12項目」と伺いましたが、声楽家は音域が低・高の2項目に分かれているため、
　数え方によって12にも13にもなります。コード上の登録は13です。）

**判断すること**
  ・残り9項目も消すか
  ・LoadTracker ごと消すか（`load_detail` 列は空のまま残ります）
  ・それとも、どこかから描くようにするか（＝仕様に戻す）

★消す前に、今回と同じ手順で実データを数えること。
今回、対照が0だったのに結論を出しかけました。数え方が違っていただけでした。
supabase/check_detail_keys.sql の②③（キーの列挙）が、その確認に使えます。

## 5.8 見た目の不具合（低優先・★寝姿の確認が済むまで直さない）

- **座っているときの本が、腕で持たれて見えない**
  開いた本が胸の前に浮いており、腕がそれを掴む形になっていません。
  本を持つポーズ用の腕の動きが無い（または不完全）と思われます。
  いま `.arm-l` / `.arm-r` は、座っているときも idleBob のままです。
  `showBook` のときだけ、腕を本の位置へ寄せる必要があります。

  ★寝姿が正しいと確認できるまで、直さないこと。
  同時に触ると、どちらの修正がどちらの症状を直したのか分からなくなります。
  今回それで何往復もしました（座り姿を寝姿だと思って追いかけていた）。

## 5.5 受領済みだが、まだ着手しない文書

- **権利と課金の線引き.md**（旧 entitlements）… ★G3 が全部終わるまで
  1行も実装しない。終わったら、一般公開の「前」に行う（G3.2）。
  ★既に作ったもののうち、書き出し・削除・同意・記録・羊は
  「絶対にゲートしない」側に入っている。有料にしないこと。
  ★法定の書き出しと「受診用の1枚」を同じ実装にまとめない
  （前者は無料の権利、後者は便宜）。いまは別実装で、条件を満たしている。
- **教師間コミュニケーション仕様.md** … G5。§3 の4つの線
  （生徒を1人も入れない／添付を作らない／生徒の健康データを流さない／
  1対1のDMを作らない）を最初に読むこと。

## 6. 坂本さんから受け取っている恒久の規則

会話で決まり、文書に無いもの。

- 全ユーザー横断のランキング・平均・集約テーブルを作らない（EU §4-2）。
  管理画面の k匿名化だけの話ではなく、製品全体の規則。
- ブレース構造のあるコードは、完全一致の文字列でだけ編集する。
  正規表現で境界を取らない（同じ失敗を3回している）。
- 報告は英語。ただし製品に出るもの（アプリの文言・コメント・
  コミットメッセージ・docs/）は日本語のまま。
- 報告は節の単位でまとめる。節の途中で報告しない。
- 仕様・ガードレール・凍結リストで答えが決まることは、自分で判断して進める。
