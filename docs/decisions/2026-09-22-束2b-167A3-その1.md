# 束2b ① ── 裁定167 A3 の うち、★画面が 書いて いない 5列

2026-09-22 ／ Code ／ 型: `docs/design/pack-final/tools/権限変更の型.md`
★週の 表 …… WEEK2 の ④（167 A3・A4）の 前半

★★★いまの 状態 …… **試しの 台帳に 当てました**。★本番には 当てて いません。

## DECISION_NEEDED

## 何を変えるか

★`profiles_guard_server_only_columns`（引き金）の 守る 列に、★5列 足します。

```
consent_health_data_at          同意の 日時
reflux_care_consent_at          同意の 日時
guardian_consent_declared_at    保護者の 同意を 申告した 日
is_under_18                     年齢の 区分
created_at                      登録の 日
```

★★★裁定167 A3 は **9列** を 挙げて います。★ここでは 5列 だけ です。

★わけ …… 残る 4列は、★いま 画面が 書いて います。
　★足した 瞬間に、★その 画面が 使えなく なります。
　★★先に 関数を 作る ことに なります。★別の 移行に します。

| 列 | 画面が 書く ところ | いま |
|---|---|---|
| `line_user_id` | `VocalTracker.jsx:15899・15906` ／ `api/line-webhook`（★サーバ） | ★残します |
| `line_link_code` | `VocalTracker.jsx:15893` | ★残します |
| `consent_health_data_withdrawn_at` | `VocalTracker.jsx:10088・10121` | ★残します |
| `consent_stats_use_at` | `VocalTracker.jsx:25628` | ★残します |

★★★数え方 …… 註と 字を 落として から、★`.update({… 列: …})` を 数えました。
　★上の 5列は **0か所** です。

## SQL全文

`supabase/migrations/20260922120000_taba2b_167a3_part1.sql`

```sql
begin;
create or replace function public.profiles_guard_server_only_columns()
returns trigger language plpgsql set search_path to ''
as $function$
declare guarded text;
begin
  if current_user not in ('anon', 'authenticated') then return new; end if;
  guarded := null;
  if new.is_admin is distinct from old.is_admin then guarded := 'is_admin';
  elsif new.is_tester is distinct from old.is_tester then guarded := 'is_tester';
  elsif new.cohort is distinct from old.cohort then guarded := 'cohort';
  elsif new.teacher_beta_access is distinct from old.teacher_beta_access then guarded := 'teacher_beta_access';
  elsif new.deleted_at is distinct from old.deleted_at then guarded := 'deleted_at';
  elsif new.reauth_at is distinct from old.reauth_at then guarded := 'reauth_at';
  elsif new.is_internal is distinct from old.is_internal then guarded := 'is_internal';
  elsif new.character_points_spent is distinct from old.character_points_spent then guarded := 'character_points_spent';
  -- ★2026-09-22 に 足した ぶん
  elsif new.consent_health_data_at is distinct from old.consent_health_data_at then guarded := 'consent_health_data_at';
  elsif new.reflux_care_consent_at is distinct from old.reflux_care_consent_at then guarded := 'reflux_care_consent_at';
  elsif new.guardian_consent_declared_at is distinct from old.guardian_consent_declared_at then guarded := 'guardian_consent_declared_at';
  elsif new.is_under_18 is distinct from old.is_under_18 then guarded := 'is_under_18';
  elsif new.created_at is distinct from old.created_at then guarded := 'created_at';
  end if;
  if guarded is not null then
    raise exception 'SERVER_ONLY_COLUMN: %', guarded
      using hint = 'この列は、サーバの側からだけ変えられます。';
  end if;
  return new;
end;
$function$;
commit;
```

## 戻すSQL

`supabase/migration_taba2b_167a3_part1_undo.sql`（★足した 5行を 外した 形）

```sql
begin;
create or replace function public.profiles_guard_server_only_columns()
returns trigger language plpgsql set search_path to ''
as $function$
declare guarded text;
begin
  if current_user not in ('anon', 'authenticated') then return new; end if;
  guarded := null;
  if new.is_admin is distinct from old.is_admin then guarded := 'is_admin';
  -- …（もとの 8列 だけ）
  end if;
  if guarded is not null then
    raise exception 'SERVER_ONLY_COLUMN: %', guarded
      using hint = 'この列は、サーバの側からだけ変えられます。';
  end if;
  return new;
end;
$function$;
commit;
```

## 変える前のポリシー

★決まり（RLS）は 触って いません。★引き金の 中身だけ です。

★変える 前の 守る 列（★本番の `pg_get_functiondef` を 読みました）──

```
is_admin / is_tester / cohort / teacher_beta_access /
deleted_at / reauth_at / is_internal / character_points_spent     ★8列
```

★列の 権限 …… `profiles` の UPDATE は 76列 中 **68列**。
　★★足す 5列は、★列の 権限の 側では まだ 渡した まま です。
　★★★引き金の 側で 断ります。★2つ 揃える かは、★次の 移行で。

## 変えた後のポリシー

★2026-09-22、★試しの 台帳に 当てた 直後に 読み直しました。★守る 列が **13列** に なりました。

## USINGとWITH CHECK

★該当なし。★決まり（RLS）を 触って いません。★`USING(true)` も 作って いません。

## 試験

★2026-09-22、★実在の 試しの 利用者（`kyo0703opera+local2@gmail.com`）で 走らせました。
★道具 …… `tools/taba2b_prove.js`。

★★★較正は 両方 取りました ──
　★守りを 外した とき …… **3 / 8**（★5列 とも 書けました）
　★守りを 足した とき …… **8 / 8**

| | 列 | 期待 | 外した とき | 足した とき |
|---|---|---|---|---|
| ① | `consent_health_data_at` | 期待 0行 | ✗ 1行 書けた | ○ `SERVER_ONLY_COLUMN` |
| ② | `reflux_care_consent_at` | 期待 0行 | ✗ 1行 | ○ 同じ |
| ③ | `guardian_consent_declared_at` | 期待 0行 | ✗ 1行 | ○ 同じ |
| ④ | `is_under_18` | 期待 0行 | ✗ 1行 | ○ 同じ |
| ⑤ | `created_at` | 期待 0行 | ✗ 1行 | ○ 同じ |
| ⑥ | `display_name` | 期待 1行（★較正・通る 側） | ○ | ○ 1行 |
| ⑦ | `garden_theme` | 期待 1行（★較正・通る 側） | ○ | ○ 1行 |
| ⑧ | 戻せた | 期待 1行 | ○ | ○ |

★★★見落として いた こと（★書いて おきます）──
　★引き金は「**変わった とき**」だけ 断ります（`is distinct from`）。
　★★同じ 値を 書くと 通ります。★それは 正しい 姿 です。
　★★1度目、★同じ 値を 書いて いて **3/8** に なりました。
　　★道具を 直しました …… ★いまの 値を 読み、★**ちがう 値**を 作って から 試します。

## なりすまし

★使っていない。★実在の 試しの 利用者で 入り直して 確かめました。

## fail closed

★該当なし。★記録を 書く 処理では ありません。
★★向きは 同じ です …… ★断る ほうへ 倒して います。

## 本番の影響

| 見た こと | 数 |
|---|---|
| `profiles` の 行 | 38 |
| 上の 5列を 画面から 書く ところ | **0か所** |
| 上の 5列を サーバから 書く ところ | **0か所**（★`api/` を 数えました） |

★★★**できなく なる 人は 0人** です。★いま 誰も 書いて いません。

★★★守る ことで 変わる こと ──
　★これから 誰かが この 列を 画面から 書こうと したら、★断られます。
　★★同意の 日時を あとから 書き換えられない、★という ことです。
　★★★これは 法20条2項の 証しの 話 です（★裁定167 A3）。

## 承認

★（空の まま お出しします）
