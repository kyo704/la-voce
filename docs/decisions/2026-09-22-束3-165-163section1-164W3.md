# 束3 ── 裁定165（審査員の表）＋ 裁定163 §1 ＋ 裁定164 W3

2026-09-22 ／ Code ／ 型: `docs/design/pack-final/tools/権限変更の型.md`

★★★いまの 状態 …… **試しの 台帳に 当てました**（2026-09-22・坂本さんの ご承認）。
　★本番には 当てて いません。★本番は この 紙の ご承認の あと です。
　★試験 …… 当てる 前 **4 / 9** ／ 当てた 後 **9 / 9**。

## DECISION_NEEDED

## 何を変えるか

- ★裁定165 §1 …… 表 `evaluation_judges` を 作ります（その回の 審査員として 組まれて いる 人）。
- ★裁定163 §1 ／ 165 §2 …… 点・講評・終えた印 の 3つの `with check` に、同じ 条件を 足します。
  いまは `judge_id = auth.uid()` だけ で、★入って いる 人なら 誰でも
  よその 学校の 行事に 点と 自由文の 講評を 入れられます。
- ★裁定164 W3 …… 終えた印 の insert に 同じ 条件を 足します。
  ★★一意の 決まりは **もう あります**（下の「変える前」を ご覧ください）。
- ★裁定165 §3 …… `confirmed_at` を 利用者から 外します。
  入れるのは `confirm_event_scores`（SECURITY DEFINER）だけ に します。

## SQL全文

`supabase/migration_taba3_165_163_164w3.sql`

```sql
begin;

create table if not exists public.evaluation_judges (
  org_id    uuid not null references public.organizations(id) on delete cascade,
  event_id  uuid not null references public.org_events(id)    on delete cascade,
  judge_id  uuid not null references auth.users(id)           on delete cascade,
  added_by  uuid references auth.users(id) on delete set null,
  added_at  timestamptz not null default now(),
  primary key (event_id, judge_id)
);
alter table public.evaluation_judges enable row level security;
revoke all on table public.evaluation_judges from public, anon, authenticated;
grant select, insert, delete on table public.evaluation_judges to authenticated;

drop policy if exists evaluation_judges_select on public.evaluation_judges;
create policy evaluation_judges_select on public.evaluation_judges
  for select to authenticated
  using (has_can(org_id, 'saiten') or judge_id = auth.uid());

drop policy if exists evaluation_judges_insert on public.evaluation_judges;
create policy evaluation_judges_insert on public.evaluation_judges
  for insert to authenticated
  with check (
    has_can(org_id, 'saiten')
    and added_by = auth.uid()
    and exists (select 1 from public.memberships m
                 where m.org_id = evaluation_judges.org_id
                   and m.user_id = evaluation_judges.judge_id)
    and exists (select 1 from public.org_events e
                 where e.id = evaluation_judges.event_id
                   and e.org_id = evaluation_judges.org_id)
  );

drop policy if exists evaluation_judges_delete on public.evaluation_judges;
create policy evaluation_judges_delete on public.evaluation_judges
  for delete to authenticated
  using (has_can(org_id, 'saiten'));

drop policy if exists evaluation_scores_write on public.evaluation_scores;
create policy evaluation_scores_write on public.evaluation_scores
  for all
  using (judge_id = auth.uid())
  with check (
    judge_id = auth.uid()
    and exists (select 1 from public.evaluation_judges j
                 where j.event_id = evaluation_scores.event_id
                   and j.org_id  = evaluation_scores.org_id
                   and j.judge_id = auth.uid())
    and exists (select 1 from public.evaluation_items i
                 where i.id = evaluation_scores.item_id
                   and i.event_id = evaluation_scores.event_id)
    and exists (select 1 from public.enrollments s
                 where s.org_id = evaluation_scores.org_id
                   and s.student_id = evaluation_scores.student_id)
  );

drop policy if exists evaluation_reviews_write on public.evaluation_reviews;
create policy evaluation_reviews_write on public.evaluation_reviews
  for all
  using (judge_id = auth.uid())
  with check (
    judge_id = auth.uid()
    and exists (select 1 from public.evaluation_judges j
                 where j.event_id = evaluation_reviews.event_id
                   and j.org_id  = evaluation_reviews.org_id
                   and j.judge_id = auth.uid())
    and exists (select 1 from public.enrollments s
                 where s.org_id = evaluation_reviews.org_id
                   and s.student_id = evaluation_reviews.student_id)
  );

drop policy if exists evaluation_judge_done_own on public.evaluation_judge_done;
create policy evaluation_judge_done_own on public.evaluation_judge_done
  for all
  using (judge_id = auth.uid())
  with check (
    judge_id = auth.uid()
    and exists (select 1 from public.evaluation_judges j
                 where j.event_id = evaluation_judge_done.event_id
                   and j.org_id  = evaluation_judge_done.org_id
                   and j.judge_id = auth.uid())
  );

revoke all on table public.evaluation_scores  from public, anon, authenticated;
revoke all on table public.evaluation_reviews from public, anon, authenticated;
grant select on table public.evaluation_scores  to authenticated;
grant select on table public.evaluation_reviews to authenticated;
grant insert (id, org_id, event_id, student_id, item_id, judge_id, points, entered_at, updated_at),
      update (points, entered_at, updated_at)
  on table public.evaluation_scores to authenticated;
grant insert (id, org_id, event_id, student_id, judge_id, body, created_at, updated_at),
      update (body, updated_at)
  on table public.evaluation_reviews to authenticated;

commit;
```

## 戻すSQL

`supabase/migration_taba3_165_163_164w3_undo.sql`

```sql
begin;
revoke all on table public.evaluation_scores  from public, anon, authenticated;
revoke all on table public.evaluation_reviews from public, anon, authenticated;
grant select, insert, update on table public.evaluation_scores  to authenticated;
grant select, insert, update on table public.evaluation_reviews to authenticated;

drop policy if exists evaluation_scores_write on public.evaluation_scores;
create policy evaluation_scores_write on public.evaluation_scores
  for all using (judge_id = auth.uid()) with check (judge_id = auth.uid());

drop policy if exists evaluation_reviews_write on public.evaluation_reviews;
create policy evaluation_reviews_write on public.evaluation_reviews
  for all using (judge_id = auth.uid()) with check (judge_id = auth.uid());

drop policy if exists evaluation_judge_done_own on public.evaluation_judge_done;
create policy evaluation_judge_done_own on public.evaluation_judge_done
  for all using (judge_id = auth.uid()) with check (judge_id = auth.uid());

drop table if exists public.evaluation_judges;
commit;
```

## 変える前のポリシー

★2026-09-22 に 本番を 読みました。

```
evaluation_scores_write      ALL  roles=PUBLIC
  using  ((judge_id = auth.uid()))
  check  ((judge_id = auth.uid()))
evaluation_reviews_write     ALL  roles=PUBLIC   ★同じ
evaluation_judge_done_own    ALL  roles=PUBLIC   ★同じ
evaluation_judges            ★表が ありません
```

★引き金 ── `evaluation_scores_guard_trg` …… `before_update = true` ／ `covers_insert = false`。
　★中身は「確定の あとに 点を 変えたら 断る」だけ です。★insert には かかりません。

★表ごとの GRANT（authenticated）──
```
evaluation_scores   SELECT / INSERT / UPDATE
evaluation_reviews  SELECT / INSERT / UPDATE
evaluation_judge_done  SELECT / INSERT / DELETE
```
★`confirmed_at` は 両方の 表で INSERT・SELECT・UPDATE が あります（★裁定165 §3）。

★一意の 決まり ── `evaluation_judge_done_pkey PRIMARY KEY (event_id, judge_id)`。
　★★裁定165 §2 は「いま 一意の 制約が 無い」と 書いて いますが、★有ります。
　★★`pg_constraint` で 見ました。★だから この SQL では 足しません。

## 変えた後のポリシー

★2026-09-22、★試しの 台帳に 当てた 直後に 読み直しました。

```
evaluation_judges_insert     check (has_can(org_id,'saiten') AND (added_by = auth.uid()) AND …)
evaluation_judges_select     using (has_can(org_id,'saiten') OR (judge_id = auth.uid()))
evaluation_judges_delete     using (has_can(org_id,'saiten'))
evaluation_scores_write      check ((judge_id = auth.uid()) AND (EXISTS ( SELECT 1 FROM evaluation_judges …
evaluation_reviews_write     check ((judge_id = auth.uid()) AND (EXISTS ( SELECT 1 FROM evaluation_judges …
evaluation_judge_done_own    check ((judge_id = auth.uid()) AND (EXISTS ( SELECT 1 FROM evaluation_judges …
```

★列の 権限（authenticated・`confirmed_at`）──

```
前 …… evaluation_scores   INSERT / SELECT / UPDATE
       evaluation_reviews  INSERT / SELECT / UPDATE
後 …… evaluation_scores   SELECT だけ
       evaluation_reviews  SELECT だけ
```

★表ごとの GRANT ── `evaluation_judges` …… SELECT / INSERT / DELETE（★UPDATE は ありません）。

## USINGとWITH CHECK

★書き込みの 決まりは すべて 両方 あります。★`USING(true)` は ありません。

| 決まり | USING | WITH CHECK |
|---|---|---|
| `evaluation_judges_select` | `has_can(org_id,'saiten') or judge_id = auth.uid()` | ― （select） |
| `evaluation_judges_insert` | ― （insert） | `has_can … and added_by = auth.uid() and memberships and org_events` |
| `evaluation_judges_delete` | `has_can(org_id,'saiten')` | ― （delete） |
| `evaluation_scores_write` | `judge_id = auth.uid()` | `＋ 組まれて いる ＋ 項目 ＋ 在籍` |
| `evaluation_reviews_write` | `judge_id = auth.uid()` | `＋ 組まれて いる ＋ 在籍` |
| `evaluation_judge_done_own` | `judge_id = auth.uid()` | `＋ 組まれて いる` |

## 試験

★2026-09-22、★実在の 試しの 利用者（`kyo0703opera+local2@gmail.com`）で 走らせました。
★道具 …… `tools/taba3_prove.js`（9件）。

★★★較正は 両方 取りました ──
　★当てる 前 …… **4 / 9**
　★当てた 後 …… **9 / 9**

★★★当てる 前に 開いて いた 穴（★これが 裁定163 §1 の 中身 です）──
　★① 組まれて いない 人が 点を 入れられました（**1行 入りました**）
　★⑦ よその 行事の 組み合わせも 入りました（**1行 入りました**）

★★★当てる 前の ④⑥ は「通った」ように 見えますが、★わけが ちがいます。
　★④ …… 表が まだ 無い から（`Could not find the table`）
　★⑥ …… 一つ 前の 行と 主キーが ぶつかった から
　★★どちらも「権限で 断られた」では ありません。★偽の PASS です。★書いて おきます。

★★★同じ 1人で 両側を 取る 順に して います。
　★試しの 台帳に 入れる 口は 1つ（local2）しか ありません。
　★だから「組まれて いない 人」の 側は、★**組む 前の 自分** で 代えて います。

| | 誰 | 何を した | 期待 |
|---|---|---|---|
| ① | 組まれて いない 人 | 点を 入れる | 期待 0行 | ✗ 1行 入った | ○ RLS 違反 |
| ② | `saiten` も 無く 組まれても いない 人 | 審査員の 一覧を select | 期待 0行 | ✗ 表が 無い | ○ 0行 |
| ③ | `saiten` を 持つ 人 | その 学校の 人を 審査員に 組む | 期待 1行（★較正・通る 側） | ✗ 表が 無い | ○ 1行 |
| ④ | 同じ 人 | その 学校に 居ない 人を 組む | 期待 0行 | △ 表が 無い | ○ RLS 違反 |
| ⑤ | 組まれた 人 | 点を 入れる | 期待 1行（★較正・通る 側） | ✗ 主キー | ○ 1行 |
| ⑥ | 同じ 人 | `confirmed_at` を 入れて insert | 期待 0行 | △ 主キー | ○ permission denied |
| ⑦ | 同じ 人 | 組まれて いない 行事の 組み合わせ | 期待 0行 | ✗ 1行 入った | ○ RLS 違反 |
| ⑧ | 組まれた 人 | 終えた印を 入れる | 期待 1行（★較正・通る 側） | ○ | ○ 1行 |
| ⑨ | 同じ 人 | 同じ 終えた印を もう 一度 | 期待 0行（主キー） | ○ | ○ 主キーで 断られた |

★★使った 試しの 行（★どれも すでに ありました）──
　★行事 `bbbbbbbb-…0001`（`★近い行事の たしかめ`）／ 項目 3件 ／ 在籍 2件（active）
　★`saiten` だけ ありません でした。★道具が 走る あいだ だけ 役職に 付け、★終わりに 外します。

## なりすまし

★使っていない。★実在の 試しの 利用者（`.env.e2e`）で 入り直して 確かめます。
★`BEGIN` / `set role` / `set_config` / `ROLLBACK` は 1つも 使いません。

## fail closed

★該当なし。★この 束は 記録を 書く 処理では ありません。
★ただし `confirmed_at` を 外す ところ は、★確定の 手順（`confirm_event_scores`）を
　飛ばせなく する もの です。★道を 1本に します。

## 本番の影響

★2026-09-22 に 本番を 数えました。

| 見た こと | 数 |
|---|---|
| `evaluation_scores` | 0 |
| `evaluation_reviews` | 0 |
| `evaluation_items` | 0 |
| `evaluation_judge_done` | 0 |
| `saiten` を 持つ 人 | 0 |
| いきている `assignments` | 21 |
| `enrollments`（active） | 20 |
| `memberships` | 25 |
| `org_events` | 54 |

★★★採点は 本番で **一度も 使われて いません**。
★★かたく 閉じても、★いま 困る 人は 0人 です。

★★★ただし **できなく なる ことが 1つ** あります ──
　★いまは 21人の 先生が 採点の 画面に 入れます（`assignments` で）。
　★当てた あとは、★`evaluation_judges` に 組まれた 人 だけ に なります。
　★★その 表は 最初 0行 です。★「＋ 審査員を 足す」の 画面が まだ ありません。
　★★つまり、★画面を 作る までは 誰も 点を 入れられません。
　★★いま 点は 0件・項目も 0件 なので、★失う ものは ありません（★裁定165 §4 と 同じ 見方）。

## 承認

★（空の まま お出しします）
