# 束2 ── 裁定164 W2・W1 と 裁定161 FX2

2026-09-22 ／ Code ／ 型: `docs/design/pack-final/tools/権限変更の型.md`

★★★いまの 状態 …… **試しの 台帳にも まだ 当てて いません。**
　★型の 1〜3（test で 作る・実在の 利用者で 証す・較正）に、★試しの 台帳への 書き込みが 要ります。
　★`tools/ask_ledger.py` の 見張りは、`grant` / `revoke` / `drop` に `--ok`（坂本さんの お許し）を 求めます。
　★★ですので **ひと言 いただいて から** 当てます。★下の 試験の 欄は まだ 空です。

## DECISION_NEEDED

## 何を変えるか

- ★W2（裁定164）…… `org_messages` の update を `withdrawn_at` の 1列だけに します。
  いまは 全部の 列が 書けます。★出した あとに 中身を 書き換えられます。
- ★W1（裁定164）…… `applications` の 応募者が `status` を `'chosen'` に できなく します。
  `'chosen'` を 付けるのは `choose_applicant`（SECURITY DEFINER）だけ です。
- ★FX2（裁定161）… `monka_read_log` に 直に insert できなく します。
  書くのは `open_monka_thread`（SECURITY DEFINER・所有者 postgres）だけ です。

## SQL全文

`supabase/migration_taba2_164w2_164w1_fx2.sql`

```sql
begin;

revoke all on table public.org_messages from public, anon, authenticated;
grant select, insert on table public.org_messages to authenticated;
grant update (withdrawn_at) on table public.org_messages to authenticated;

drop policy if exists org_messages_withdraw on public.org_messages;
create policy org_messages_withdraw on public.org_messages
  for update
  using  (auth.uid() = author_id)
  with check (auth.uid() = author_id and withdrawn_at is not null);

drop policy if exists applications_update_own on public.applications;
create policy applications_update_own on public.applications
  for update
  using  (auth.uid() = applicant_user_id)
  with check (auth.uid() = applicant_user_id
              and status = any (array['sent'::text, 'withdrawn'::text]));

drop policy if exists monka_read_log_insert_self_monka_read on public.monka_read_log;
revoke insert on table public.monka_read_log from public, anon, authenticated;

commit;
```

## 戻すSQL

`supabase/migration_taba2_164w2_164w1_fx2_undo.sql`

```sql
begin;
revoke all on table public.org_messages from public, anon, authenticated;
grant select, insert, update on table public.org_messages to authenticated;
drop policy if exists org_messages_withdraw on public.org_messages;
create policy org_messages_withdraw on public.org_messages
  for update using (auth.uid() = author_id) with check (auth.uid() = author_id);

drop policy if exists applications_update_own on public.applications;
create policy applications_update_own on public.applications
  for update using (auth.uid() = applicant_user_id) with check (auth.uid() = applicant_user_id);

grant insert on table public.monka_read_log to authenticated;
create policy monka_read_log_insert_self_monka_read on public.monka_read_log
  for insert
  with check (viewer_user_id = auth.uid() and has_can(org_id, 'monka_read'::text));
commit;
```

## 変える前のポリシー

★2026-09-22 に 本番を 読みました（`pg_policy`）。

```
org_messages_withdraw        update  using ((auth.uid() = author_id))
                                     check ((auth.uid() = author_id))
applications_update_own      update  using ((auth.uid() = applicant_user_id))
                                     check ((auth.uid() = applicant_user_id))
monka_read_log_insert_self_monka_read  insert
                                     check ((viewer_user_id = auth.uid()) AND has_can(org_id, 'monka_read'::text))
```

★列の 権限（`information_schema.column_privileges`・本番）──

```
org_messages   authenticated UPDATE … author_id / body / created_at / id / org_id /
                                      target_division_ids / target_grade_years / target_user_ids /
                                      teacher_id / title / withdrawn_at      ★11列 すべて
applications   authenticated UPDATE … available_days / show_career / show_photo /
                                      show_recordings / show_repertoire / status
monka_read_log authenticated INSERT / SELECT（表ごと）
```

★★`applications` は 表ごとの UPDATE が 外して あり、★列の GRANT が きいて います。
★★`org_messages` は 表ごとの UPDATE が 残って いる ので、★列の GRANT は 意味を 持って いません。
　★だから REVOKE を 先に 書きます（★2026-09-15 の 一件と 同じ 形）。

## 変えた後のポリシー

★NOT_YET。★when …… 試しの 台帳に 当てた 直後に `pg_policy` を 読み直して、ここに 貼ります。

## USINGとWITH CHECK

★3つ とも 両方 あります。★`USING(true)` は ありません。

| 決まり | USING | WITH CHECK |
|---|---|---|
| `org_messages_withdraw` | `auth.uid() = author_id` | `auth.uid() = author_id and withdrawn_at is not null` |
| `applications_update_own` | `auth.uid() = applicant_user_id` | `… and status in ('sent','withdrawn')` |
| `monka_read_log` insert | ★決まり ごと 消します（★insert の 道を なくす ため） | ― |

## 試験

★NOT_YET。★when …… 試しの 台帳に 当てる お許しを いただいた とき。
★★道具は 書いて あります …… `tools/taba2_prove.js`（8件・較正 4件 を 含む）。

| | 誰 | 何を した | 期待 |
|---|---|---|---|
| ① | 書いた 人（local2） | `withdrawn_at` を 入れる | 期待 1行（★較正・通る 側） |
| ② | 書いた 人（local2） | `body` を 書き換える | 期待 0行（断られる） |
| ③ | 書いて いない 人 | よその 連絡を 取り下げる | 期待 0行 |
| ④ | 書いた 人 | `withdrawn_at` を null に 戻す | 期待 0行 |
| ⑤ | 応募した 人 | `'withdrawn'` に する | 期待 1行（★較正・通る 側） |
| ⑥ | 応募した 人 | `'chosen'` に する | 期待 0行（断られる） |
| ⑦ | `monka_read` を 持つ 人 | log に 直に insert | 期待 0行 |
| ⑧ | 同じ 人 | `open_monka_thread` を 呼ぶ | 期待 1行（★較正・通る 側） |

★★試しの 行が 2つ 足りません（`supabase/seed_test_taba2.sql`・戻しも あり）──
　・よその 人が 書いた 連絡が 0件（★③が 確かめられない）
　・local2 の 応募が 0件（★⑤⑥が 確かめられない）

## なりすまし

★使っていない。★実在の 試しの 利用者（`.env.e2e`）で 入り直して 確かめます。
★`BEGIN` / `set role` / `set_config` / `ROLLBACK` は 1つも 使って いません。

## fail closed

★FX2 が まさに それ です。★`open_monka_thread` は 記録を 書いて から 中身を 返します
（裁定159 S2・2026-09-21 に 13件 で 確かめ 済み）。
★この 束は、その **外の 道**（直に insert）を 塞ぐ もの です。
★W2・W1 は 記録を 書く 処理では ありません。★該当なし。

## 本番の影響

★2026-09-22 に 本番を 数えました。

| 表 | 行 | いま できて いる こと | 変えた あと |
|---|---|---|---|
| `org_messages` | 4 | 書いた 人が 全部の 列を 書き換えられる | `withdrawn_at` だけ |
| `org_messages`（取り下げ済み） | 0 | ― | ― |
| `applications` | 0 | 応募者が 自分で `'chosen'` に できる | できない |
| `applications`（`'chosen'`） | 0 | ― | ― |
| `monka_read_log` | 0 | `monka_read` を 持つ 人が 直に 書ける | 道 ごと 無くなる |
| `monka_read` を 持つ 人 | 0 | ― | ― |

★★**読めなく なる 人は 0人** です。★select は 1つも 触って いません。

★★画面が 失う もの …… **ありません**。
　・`org_messages` を `update` する 呼び出しは、★リポジトリに **1つも ありません**
　　（`components/VocalTracker.jsx` の `withdrawn_at` の 書き込みは `org_events` です。14364行）。
　・`applications` を `update` する 呼び出しも ありません（insert だけ・11799行）。
　・`monka_read_log` に 直に insert する 呼び出しも ありません。

## 承認

★（空の まま お出しします）
