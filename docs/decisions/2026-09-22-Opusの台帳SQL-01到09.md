# Opus の 台帳 SQL（01〜09）── 試しの 台帳で 流しました

2026-09-22 ／ Code ／ 型: `docs/design/pack-final/tools/権限変更の型.md`
★元 …… `docs/design/pack-final/sql/`（★写し `supabase/from_opus/`）

★★★いまの 状態 …… **6本を 試しの 台帳に 当てました**。★本番には 1本も 当てて いません。
★★3本は **前提が 揃って いません**。★当てて いません（★下の 三）。

## DECISION_NEEDED

## 何を変えるか

| | ファイル | 中身 | 試しに 当てたか |
|---|---|---|---|
| 01 | `billing_foundation` | `subscription_items`・`stripe_events`・`purchases` の 列・`my_entitlements()` | ★当てました |
| 02 | `student_price_consents` | 表＋関数3本（★書くのは 関数 だけ） | ★当てました |
| 03 | `org_contracts_free_period` | `org_contracts`・`org_free_period()` | ★当てました |
| 04 | `profile_consent_guards` | LINE・登録日を サーバ専用に | ★★**当てて いません** |
| 05 | `uniques` | 有効な 担当・学校の 請求の 重なりを 止める | ★当てました |
| 06 | `logs_written_by_ledger` | 記録を 引き金で。直の insert を やめる | ★★**当てて いません** |
| 07 | `ops_alerts` | `ops_alerts`・`raise_alert()` | ★当てました |
| 08 | `portfolio_performance` | 録画は YouTube だけ・公開は 関数だけ | ★当てました |
| 09 | `drop_org_message_reads` | 古い 表を 消す | ★★**当てて いません** |

## SQL全文

★9本 とも `docs/design/pack-final/sql/` に あります。★1文字も 直して いません。

```sql
-- ★例（05・全文）
create unique index if not exists assignments_active_unique
  on public.assignments (org_id, teacher_id, student_id) where ended_at is null;
create unique index if not exists org_billing_org_unique
  on public.org_billing (org_id);
```

★ほかの 8本は 長い ので、★ファイルを そのまま ご覧ください。
★★`supabase/from_opus/` に 同じ ものを 置きました（★束の 版が 変わっても 残ります）。

## 戻すSQL

★★★**Opus の SQL に 戻しが 付いて いません。**★下は Code が 書いた 下書き です。
★★★この まま 使いません。★本番に 当てる ことが 決まった 日に、
　★その ときの 姿を 読み直して から 仕上げます。

```sql
-- ★下書き（★2026-09-22・試しの 台帳に できた ものから 逆に 書きました）
begin;

-- 05 …… 索引を 外す（★行は 消えません）
drop index if exists public.assignments_active_unique;
drop index if exists public.org_billing_org_unique;
drop index if exists public.purchases_payment_intent_key;

-- 07 …… 知らせの 表と 道
drop function if exists public.raise_alert(text, text, jsonb);
drop table if exists public.ops_alerts;

-- 03 …… 学校の 契約と 導入期間
drop function if exists public.org_free_period(date);
drop table if exists public.org_contracts;

-- 02 …… 学生の 値段の 同意
drop function if exists public.give_student_price_consent(text);
drop function if exists public.withdraw_student_price_consent();
drop function if exists public.student_price_eligible(uuid);
drop function if exists public._is_enrolled_somewhere(uuid);
drop table if exists public.student_price_consents;

-- 01 …… 払いの 土台
drop function if exists public.my_entitlements();
drop table if exists public.stripe_events;
drop table if exists public.subscription_items;
alter table public.purchases drop constraint if exists purchases_refund_le_amount;
alter table public.purchases drop constraint if exists purchases_status_check;
alter table public.purchases add constraint purchases_status_check
  check (status in ('active','expired'));
-- ★★足した 列は 残します（`lookup_key` / `ended_early_at` / `refunded_yen`）。
--   ★消すと、★入って いた 値が 消えます。

-- 08 …… 録画と 公開ページ
drop function if exists public.get_public_portfolio(text);
alter table public.portfolio_recordings drop constraint if exists portfolio_recordings_youtube_only;
-- ★★消した ポリシー（performances_own ほか 4つ）は、
--   ★当てる 日に `pg_policy` から 写して から 書きます。★いまは 書けません。

commit;
```

★★★`drop table` が 並んで います。★本番では **行が 消えます**。
　★当てる 日に 行数を 数え、★0行で ある ことを 確かめて から でなければ 使いません。

## 変える前のポリシー

★Opus が 本番を 読み取りだけ で 確かめ 済み です（`sql/README.md`）──

```
参照して いる 列 78個 …… 足りない 列 0
呼んで いる 関数 4つ ……… 4つとも ある（引数の 形も 同じ）
作る 名前（索引6・引き金4・制約3）…… ★制約 1つが ぶつかった → 直し済み
消す ポリシー 4つ ……… 4つとも ある
一意に する 列の 重なり …… 0件
select の 部分を where false で 本番に 流す …… 文法・列・型の 誤り 0
```

## 変えた後のポリシー

★2026-09-22、★試しの 台帳に 当てた 直後に 読み直しました。

```
表   subscription_items / stripe_events / student_price_consents /
     org_contracts / ops_alerts          ★5つとも できて、RLS が 入って います
関数 my_entitlements（definer）/ get_public_portfolio（definer）/ raise_alert（definer）/
     give_student_price_consent（definer）/ withdraw_student_price_consent（definer）/
     student_price_eligible（definer）/ _is_enrolled_somewhere（definer）/
     org_free_period（★definer では ありません。★計算 だけ）
索引 purchases_payment_intent_key / assignments_active_unique / org_billing_org_unique
列   purchases …… lookup_key / ended_early_at / refunded_yen
```

## USINGとWITH CHECK

★各ファイルの 決まりは Opus が 書いた もの です。★`USING(true)` は 1つも ありません
（★当てた あとに `pg_policy` を 読んで 数えました）。

## 試験

★2026-09-22、★試しの 台帳に **2度** 流しました。

| | 見た こと | 結果 |
|---|---|---|
| ① | 6本 とも 1度目に 通る | ○（★誤り 0） |
| ② | 6本 とも **2度目** も 通る（★何度でも 同じ） | ○ |
| ③ | 表 5つが でき、★RLS が 入って いる | ○ |
| ④ | 関数 8つが できて いる | ○ |
| ⑤ | 索引 3つが できて いる | ○ |
| ⑥ | `purchases` に 列 3つが 足されて いる | ○ |

★★★**まだ して いない 試験**（★NOT_YET。★when …… 前提の 3本の 見通しが 立った とき）──

| | 誰 | 何を した | 期待 |
|---|---|---|---|
| ⑦ | 学生（在籍 あり） | `give_student_price_consent` を 呼ぶ | 期待 1行（★較正・通る 側） |
| ⑧ | どこにも 在籍して いない 人 | 同じ | 期待 0行 |
| ⑨ | 払って いる 人 | `my_entitlements()` を 呼ぶ | 期待 1行以上 |
| ⑩ | 払って いない 人 | 同じ | 期待 0行 |
| ⑪ | 関わりの ない 人 | `get_public_portfolio(slug)` を 呼ぶ（★切られた 相手） | 期待 0行 |
| ⑫ | ふつうの 見る 人 | 同じ（★公開して いる 人） | 期待 1行以上（★較正） |
| ⑬ | 審査の 外の 人 | 有効な 担当を 2つ 作る | 期待 0行（★一意で 断られる） |
| ⑭ | 運営 | 導入期間を 36通り 数える | 期待 36行（★式の 確かめ） |

★★`teacher_student_links` の 重なり 2件（★本番）は、★05 の 対象では ありません。
　★対象は `assignments` と `org_billing` です。★どちらも 重なり 0件 です。

## なりすまし

★使っていない。★形は 台帳を 読んで 確かめました。

## fail closed

★07（`raise_alert`）が それ です。★失敗を 残す 道 です。
★★★`ops_alerts` に 何を 入れるかは、★サーバの 側の 工事が 要ります（★まだ）。

## 本番の影響

★★★**本番には 1本も 当てて いません。**★影響 0 です。

★当てる 日に 数える もの …… `purchases`・`subscriptions`・`assignments`・`org_billing` の 行数と、
　★一意に する 列の 重なり（★2026-09-22 の 時点では 0件。★`teacher_student_links` を 除く）。

## 承認

★（空の まま お出しします）
