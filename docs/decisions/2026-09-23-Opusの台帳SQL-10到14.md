# Opus の 台帳 SQL 10・11・12・13・14 ── 試しの 台帳で 通しました

2026-09-23 ／ Code ／ 型: `docs/design/pack-final/tools/権限変更の型.md`

★★★いまの 状態 …… **試しの 台帳で 5本 とも 通りました**。★本番には 1本も 当てて いません。

## DECISION_NEEDED

## 何を変えるか

| | ファイル | 中身 |
|---|---|---|
| 10 | `lesson_allocation` | レッスン割（回・希望◎△×・行けない日・カレンダーの住所） |
| 11 | `portfolio_homepage` | ホームページ（型の 所有・問い合わせ） |
| 12 | `productions_core` | 公演の 核（表・枠・回・変更の 記録） |
| 13 | `production_children` | 公演の 子ども（★緊急の 連絡先） |
| 14 | `ops_audit_log` | 管理の 操作の 記録（引き金で 9表） |

## SQL全文

★5本 とも `docs/design/pack-final/sql/` に あります。★1文字も 直して いません。

```sql
-- ★10 の 直った ところ（★2026-09-23・Opus が ㋑を 採りました）
token text not null unique default encode(extensions.gen_random_bytes(24), 'hex'),
v_token := encode(extensions.gen_random_bytes(24), 'hex');
```

## 戻すSQL

★Opus の SQL に 戻しが 付いて いません。★下は Code の 下書き です。

```sql
begin;
drop function if exists public.read_kid_contact(uuid, text);
drop function if exists public.set_kid_contact(uuid, text);
drop table if exists public.koen_kid_contact_reads;
drop table if exists public.koen_kid_contacts;
drop table if exists public.koen_kids;
drop function if exists public.purge_ops_audit_log(integer);
drop function if exists public.audit_row();
drop table if exists public.ops_audit_log;
drop function if exists public.log_koen_session_change();
drop table if exists public.koen_session_changes;
drop table if exists public.koen_sessions;
drop table if exists public.koen_cells;
drop table if exists public.koen_slots;
drop table if exists public.koen_rows;
drop table if exists public.koen_members;
drop function if exists public.koen_can_manage(uuid);
drop function if exists public.koen_can_see(uuid);
drop table if exists public.koen;
drop function if exists public.submit_inquiry(text, text, text, text);
drop function if exists public.set_web_type(text);
drop table if exists public.page_inquiries;
drop table if exists public.page_types_owned;
drop function if exists public.rotate_calendar_token();
drop function if exists public.seed_prefs_from_timetable(uuid);
drop function if exists public.pref_map(uuid);
drop table if exists public.calendar_tokens;
drop table if exists public.lesson_ng_dates;
drop table if exists public.lesson_prefs;
drop table if exists public.lesson_rounds;
commit;
```

★★★`drop table` が 並びます。★本番では 行が 消えます。
　★当てる 日に 行数を 数え、★0行で ある ことを 確かめて から でなければ 使いません。
★★9表に 足す 引き金（`*_audit`）も、★戻す ときに 外します。

## 変える前のポリシー

★Opus が 本番を 読み取りだけ で 確かめ 済み です。
★Code の 側でも、★作る 表の 名前が 本番と ぶつからない ことを 数えました（0件）。

## 変えた後のポリシー

★2026-09-23、★試しの 台帳に 当てた 直後に 読み直しました。

```
表 …… lesson_rounds / lesson_prefs / lesson_ng_dates / calendar_tokens /
       page_types_owned / page_inquiries /
       koen / koen_members / koen_rows / koen_slots / koen_cells /
       koen_sessions / koen_session_changes / koen_kids /
       koen_kid_contacts / koen_kid_contact_reads / ops_audit_log
       ★ぜんぶ RLS 入り

関数 …… pref_map / rotate_calendar_token / seed_prefs_from_timetable /
         set_web_type / submit_inquiry / koen_can_see / koen_can_manage /
         log_koen_session_change / set_kid_contact / read_kid_contact /
         audit_row / purge_ops_audit_log

引き金 …… koen_sessions_log_change ／
           enrollments_audit / lesson_rounds_audit / memberships_audit /
           org_billing_audit / org_events_audit / org_invitations_audit /
           org_places_audit / org_posts_audit        ★9表

権限 …… ops_audit_log は SELECT だけ
         koen_kid_contacts ／ koen_kid_contact_reads は **渡して いません**
```

## USINGとWITH CHECK

★`USING(true)` は 1つも ありません。
★★`koen_kid_contacts` は **select の 決まりを 作って いません**。★誰も 直に 読めません。

## 試験

★2026-09-23、★実在の 試しの 利用者（`kyo0703opera+local2@gmail.com`）で 走らせました。

### `tools/sql1011_prove.js` …… **16 / 16**

| | 見た こと | 期待 | 結果 |
|---|---|---|---|
| ① | 自分の 希望を 書ける | 期待 1行（★較正・通る 側） | ○ |
| ② | よその 人の 希望は 書けない | 期待 0行 | ○ RLS 違反 |
| ③ | 締切の 過ぎた 回には 書けない | 期待 0行 | ○ RLS 違反 |
| ④ | `pref_map` が 返す 列は 数 だけ | 期待 `slot_key/maru/sankaku` | ○ |
| ④-2 | 人の id も 名前も 返らない | 期待 0行 | ○ |
| ⑤ | カレンダーの 住所を 作り直せる | 期待 1行（★較正） | ○ |
| ⑥-1 | 持って いる 形へ | `ok_owned` | ○ |
| ⑥-2 | 14日の あいだ | `ok_trial` | ○ |
| ⑥-2-2 | 期限が 過ぎたら | `need_payment` | ○ |
| ⑥-3 | 分野ごとに 1つ | `ok_field_free` | ○ |
| ⑥-3-2 | 分野ただは 1回 きり | `need_payment` | ○ |
| ⑥-4 | それ以外 | `need_payment` | ○ |
| ⑦ | 紙の 型は いつでも | 期待 1行 | ○ |
| ⑧ | 公開して いない slug | 期待 0行・`false` | ○ |
| ⑨ | 公開して いる slug | 期待 1行（★較正） | ○ 0 → 1 |
| ⑩ | anon には 見えない | 期待 0行 | ○ |

### `tools/sql121314_prove.js` …… **8 / 8**

| | 見た こと | 期待 | 結果 |
|---|---|---|---|
| ① | 緊急の 連絡先を 直に 読めない | 期待 0行 | ○ permission denied |
| ② | 保護者は 入れられる | 期待 1行（★較正・通る 側） | ○ |
| ③ | 理由が 無いと 断る | 期待 0行 | ○ `REASON_REQUIRED` |
| ④ | 理由を 付けると 返る・記録が 1行 | 期待 1行（★較正） | ○ 0 → 1 |
| ⑤ | 断られた ときは 記録も 増えない | 期待 0行 | ○ |
| ⑥ | 記録に 残るのは 役割 だけ（名前が 無い） | 期待 0行（名前） | ○ |
| ⑦ | `ops_audit_log` に 直に 入れられない | 期待 0行 | ○ permission denied |
| ⑧ | 管理の 操作で 引き金が 1行 足す | 期待 1行以上（★較正） | ○ 3 → 5 |

★★★偽の PASS を 1つ 出して、★直しました（★書いて おきます）。
　★⑦を `op` `table_name` という **無い 列** で 試して いました。
　★★「列が 無い」で 断られたのを、★「権限で 断られた」と 読んで いました。
　★★台帳で 列を 数えて から 書き直し、★`permission denied` を 見て います。

## なりすまし

★使っていない。★実在の 試しの 利用者で 入り直して 確かめました。

## fail closed

★13 の `read_kid_contact` が それ です。★**記録を 先に 書いてから** 連絡先を 返します。
★★③で 断られた とき、★記録が 増えて いない ことを 別に 見て います（⑤）。

## 本番の影響

★★★**本番には 1本も 当てて いません。**★影響 0 です。

★当てる 日に 数える もの …… 作る 17表の 名前が 本番に 無い こと（★2026-09-23 に 0件）と、
　★引き金を 足す 9表（`enrollments` ほか）の 行数。

★★★引き金を 足す 9表は、★**いま 使われて いる 表** です。
　★`enrollments`（20行）・`memberships`（25行）・`org_events`（54行）ほか。
　★★書く たびに `ops_audit_log` に 1行 増えます。★遅く なる ことは ありません（★1行の insert）。
　★★けれど **行は 増え続けます**。★`purge_ops_audit_log(日数)` が 付いて います。
　★★★何日 残すかは **決まって いません**。★呼ぶ ところも まだ ありません。

## 承認

★（空の まま お出しします）
