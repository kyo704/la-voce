# 束0 ── 裁定161 FX1 ＋ 裁定168 ★1〜3（退会で 記録が 消える／退会が 止まる）

2026-09-22 ／ Code ／ 型: `docs/design/pack-final/tools/権限変更の型.md`

★★★いまの 状態 …… **試しの 台帳に 当てました**。★本番には 当てて いません。

## DECISION_NEEDED

## 何を変えるか

- ★裁定168 ★1 …… 退会が **止まる** 11本の 外部キーを `NO ACTION` → `ON DELETE SET NULL`。
  ★規約9条「いつでも 退会できます」が、いま 守れて いません。
- ★裁定168 ★2 …… 審査員が 退会すると 学生の 点と 講評が **消える**（`CASCADE`）→ `SET NULL` ＋ `judge_name_at`。
- ★裁定168 ★3 …… 先生が 退会すると 門下の 連絡が **全部 消える**（`CASCADE`）→ `SET NULL` ＋ `author_name_at`・`teacher_name_at`。
- ★裁定161 FX1 …… 記録の 表（`monka_read_log`・`post_change_log`・`score_log`）が 退会で 消える → `SET NULL`。

★`organizations.contract_owner_user_id` は **触りません**（`NO ACTION` の まま）。
　★契約者が 黙って 消えると お金の 行き先が 無く なります。★画面で 先に お伝えします。

## SQL全文

`supabase/migration_taba0_fx1_168_1to3.sql`（★長い ので、★要点を 写します）

```sql
begin;

-- 一 そのときの 名前を 写す 列
alter table public.evaluation_scores  add column if not exists judge_name_at   text;
alter table public.evaluation_reviews add column if not exists judge_name_at   text;
alter table public.org_messages       add column if not exists author_name_at  text;
alter table public.org_messages       add column if not exists teacher_name_at text;
alter table public.monka_read_log     add column if not exists target_name_at  text;

-- 二 退会が 止まる ところ（11本）── いずれも
alter table public.<表> drop constraint if exists <名>;
alter table public.<表> add constraint <名>
  foreign key (<列>) references auth.users(id) on delete set null;
--   lessons(student_id, teacher_id, created_by, attendance_by)
--   organizations(created_by)
--   org_invitations(used_by, invited_by)
--   roster_drafts(linked_user_id, imported_by)
--   lesson_presets(created_by)
--   teacher_invitations(used_by_student_id)

-- 三 消えては いけない のに 消える ところ
--   evaluation_scores(judge_id) / evaluation_reviews(judge_id)
--   org_messages(author_id, teacher_id)          ★CASCADE → SET NULL

-- 四 記録の 表
--   monka_read_log(viewer_user_id, target_monka_id)
--   post_change_log(target_user_id) / score_log(editor_user_id)   ★CASCADE → SET NULL

commit;
```

## 戻すSQL

`supabase/migration_taba0_fx1_168_1to3_undo.sql`

```sql
begin;
-- ★当てる 前の 姿へ。★足した 列は 残します（★消すと 写した 名前が 消えます）。
alter table public.lessons drop constraint if exists lessons_student_id_fkey;
alter table public.lessons add constraint lessons_student_id_fkey
  foreign key (student_id) references auth.users(id);
-- …（11本 すべて NO ACTION へ）
alter table public.evaluation_scores drop constraint if exists evaluation_scores_judge_id_fkey;
alter table public.evaluation_scores add constraint evaluation_scores_judge_id_fkey
  foreign key (judge_id) references auth.users(id) on delete cascade;
-- …（★2・★3・FX1 は CASCADE へ）
commit;
```

## 変える前のポリシー

★決まり（RLS）は **1つも 触って いません**。★外部キーと 列だけ です。

★変える 前の 姿（★2026-09-22 に 本番の `pg_constraint` を 読みました）──

```
lessons              student_id / teacher_id / created_by / attendance_by   NO ACTION
organizations        created_by                                            NO ACTION
                     contract_owner_user_id                                NO ACTION（★そのまま）
org_invitations      used_by / invited_by                                  NO ACTION
roster_drafts        linked_user_id / imported_by                          NO ACTION
lesson_presets       created_by                                            NO ACTION
teacher_invitations  used_by_student_id                                    NO ACTION
evaluation_scores    judge_id                                              CASCADE
evaluation_reviews   judge_id                                              CASCADE
org_messages         author_id / teacher_id                                CASCADE
monka_read_log       viewer_user_id / target_monka_id                      CASCADE
post_change_log      target_user_id                                        CASCADE
score_log            editor_user_id                                        CASCADE
export_log           user_id                                               CASCADE（★触りません・下の 註）
```

## 変えた後のポリシー

★2026-09-22、★試しの 台帳に 当てた 直後に 読み直しました。

```
lessons              4列 とも                       SET NULL
organizations        created_by                     SET NULL
                     contract_owner_user_id         NO ACTION   ★わざと そのまま
org_invitations      2列 とも                       SET NULL
roster_drafts        2列 とも                       SET NULL
lesson_presets       created_by                     SET NULL
teacher_invitations  used_by_student_id             SET NULL
evaluation_scores    judge_id                       SET NULL
evaluation_reviews   judge_id                       SET NULL
                     student_id                     CASCADE     ★わざと そのまま（ご本人の もの）
org_messages         author_id / teacher_id         SET NULL
monka_read_log       viewer_user_id / target_monka_id  SET NULL
post_change_log      target_user_id                 SET NULL
score_log            editor_user_id                 SET NULL
export_log           user_id                        CASCADE     ★触って いません
```

## USINGとWITH CHECK

★該当なし。★決まり（RLS）を 1つも 触って いません。★`USING(true)` も 作って いません。

## 試験

★NOT_YET（★本番の 分）。★when …… 本番に 当てる お許しを いただいた とき。

★★試しの 台帳では、★**形** を 読み直して 確かめました（25本）──
　★狙った 21本が `SET NULL` に なって いる こと。
　★`organizations.contract_owner_user_id` が `NO ACTION` の まま である こと。
　★`evaluation_*.student_id` が `CASCADE` の まま である こと。

★★★まだ して いない 試験（★裁定168 の VERIFY）──

| | 誰 | 何を した | 期待 |
|---|---|---|---|
| ① | レッスン・招待・名簿の下書きに 名前が ある 先生 | 退会する | 期待 1行以上（★通る。★いまは **止まります**） |
| ② | 同じ とき | レッスンの 行 | 期待 1行以上（★残る。★`teacher_id` は null） |
| ③ | 審査員 | 退会する | 期待 1行以上（★点と 講評が 残る・`judge_name_at` が 読める） |
| ④ | 先生 | 退会する | 期待 1行以上（★連絡が 残る・`author_name_at` が 読める） |
| ⑤ | 契約者 | 退会する | 期待 0行（★止まる。★画面で 先に お伝えします） |

★★これには「試しの 利用者を 1人 消す」ことが 要ります。★取り消せません。
★★★だから **まだ して いません**。★お指図を お待ちします。

## なりすまし

★使っていない。★形は `pg_constraint` を 読んで 確かめました。

## fail closed

★該当なし。★記録を 書く 処理では ありません。
★★ただし 向きは 同じ です ── ★人が 消えても **記録は 残す**、に 倒して います。

## 本番の影響

| 見た こと | 数 |
|---|---|
| `account_deletions`（これまでの 退会） | 7件 |
| `lessons` | 11行 |
| `org_messages` | 4行 |
| `evaluation_scores` / `reviews` | 0行 / 0行 |
| `monka_read_log` / `post_change_log` / `score_log` | 0 / ― / ― |

★★★**いま 読めなく なる 人は 0人** です。★`select` を 1つも 触って いません。

★★★変わる こと ──
　★これまで「退会が 止まって いた 方」が、★止まらなく なります。
　★★裁定168 は こう 見て います ── 7件が 通ったのは、★これらの 表に
　　★名前の 無い 方 だった 見込み。★つまり **止まった 方は まだ 居ない** はず です。
　★★★けれど、★止まった ことが どこにも 記録されて いません。★確かめられません。

★★画面の 側も 直しました（★同じ 日に）──
　★`org_messages` を 書く とき、★`author_name_at`・`teacher_name_at` を 写します。
　★`evaluation_scores` / `reviews` を 書く とき、★`judge_name_at` を 写します。
　★★読む ところの 列にも 足しました（`MESSAGE_COLUMNS` ほか）。
　★★`lib/renraku.js` に `nameOrKept(live, kept, fallback)` を 作りました。
　　★★順は「いま 居る 方が 先、★居なければ 写し」。★お名前を 直された 方の ため です。

## 承認

★（空の まま お出しします）
