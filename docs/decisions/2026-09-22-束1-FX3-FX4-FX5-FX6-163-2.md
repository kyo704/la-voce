# 束1 ── 裁定161 FX3・FX4・FX5・FX6 ＋ 裁定163 §2

2026-09-22 ／ Code ／ 型: `docs/design/pack-final/tools/権限変更の型.md`
★週の 表 …… WEEK1 の ③（束1）

★★★いまの 状態 …… **試しの 台帳に 当てました**。★本番には 当てて いません。

## DECISION_NEEDED

## 何を変えるか

- ★FX3 …… `org_post_perm_log` の TRUNCATE ほかを 外します（★SELECT だけ に）。
- ★FX4 …… `org_message_drafts` の TRUNCATE ほかを 外します（★1つずつ 消すのは 残します）。
- ★FX5 …… `are_connected` / `is_org_member` / `can_view_organization` を `anon` から 外し、
  ★使われて いない `is_org_owner_or_admin` を 消します。
- ★FX6 …… `can_view_organization` の「招待が あれば 誰にでも 見える」枝を 消します。
- ★163 §2 … `_a13_policy_backup` を 消します（★12行を ファイルに 写して から）。

## SQL全文

`supabase/migrations/20260922100000_taba1_fx3_fx4_fx5_fx6_163_2.sql`

```sql
begin;

revoke all on table public.org_post_perm_log from public, anon, authenticated;
grant select on table public.org_post_perm_log to authenticated;

revoke all on table public.org_message_drafts from public, anon, authenticated;
grant select, insert, update, delete on table public.org_message_drafts to authenticated;

revoke execute on function public.are_connected(uuid, uuid) from public, anon;
revoke execute on function public.is_org_member(uuid, uuid) from public, anon;
revoke execute on function public.can_view_organization(uuid, uuid) from public, anon;
drop function if exists public.is_org_owner_or_admin(uuid, uuid);

create or replace function public.can_view_organization(viewer_id uuid, p_org_id uuid)
returns boolean language sql stable security definer set search_path to 'public'
as $function$
  select
    exists (select 1 from memberships where org_id = p_org_id and user_id = viewer_id)
    or exists (select 1 from enrollments
                where org_id = p_org_id and student_id = viewer_id and status = 'active')
$function$;
revoke execute on function public.can_view_organization(uuid, uuid) from public, anon;
grant execute on function public.can_view_organization(uuid, uuid) to authenticated;

drop table if exists public._a13_policy_backup;

commit;
```

## 戻すSQL

`supabase/migration_taba1_undo.sql`（★`_a13_policy_backup` は 戻りません。★控えは ファイルに）

```sql
begin;
revoke all on table public.org_post_perm_log from public, anon, authenticated;
grant select, truncate, trigger, references on table public.org_post_perm_log to authenticated;
revoke all on table public.org_message_drafts from public, anon, authenticated;
grant select, insert, update, delete, truncate, trigger, references
  on table public.org_message_drafts to authenticated;
grant execute on function public.are_connected(uuid, uuid) to anon;
grant execute on function public.is_org_member(uuid, uuid) to anon;
create or replace function public.can_view_organization(viewer_id uuid, p_org_id uuid)
returns boolean language sql stable security definer set search_path to 'public'
as $function$
  select exists (select 1 from memberships where org_id = p_org_id and user_id = viewer_id)
    or exists (select 1 from enrollments where org_id = p_org_id and student_id = viewer_id and status = 'active')
    or exists (select 1 from org_invitations where org_id = p_org_id and used_at is null and expires_at > now())
$function$;
grant execute on function public.can_view_organization(uuid, uuid) to anon, authenticated;
commit;
```

## 変える前のポリシー

★決まり（RLS）は **1つも 触って いません**。★権限と 関数だけ です。

★2026-09-22 に 本番を 読みました ──

```
org_post_perm_log   authenticated  SELECT / TRUNCATE / TRIGGER / REFERENCES
org_message_drafts  authenticated  SELECT / INSERT / UPDATE / DELETE / TRUNCATE / TRIGGER / REFERENCES
_a13_policy_backup  authenticated  SELECT / INSERT / UPDATE / DELETE   ★RLS は false・12行
are_connected           anon・authenticated  execute   （security definer）
is_org_member           anon・authenticated  execute   （security definer）
is_org_owner_or_admin   anon・authenticated  execute   （security definer）
can_view_organization   anon・authenticated  execute   （security definer）
```

★`can_view_organization` の 3つ目の 枝（★これが FX6）──

```sql
or exists (select 1 from org_invitations
            where org_id = p_org_id and used_at is null and expires_at > now())
```

## 変えた後のポリシー

★2026-09-22、★試しの 台帳に 当てた 直後に 読み直しました。

```
org_post_perm_log   authenticated  SELECT だけ
org_message_drafts  authenticated  SELECT / INSERT / UPDATE / DELETE
_a13_policy_backup  ★表ごと ありません
are_connected           authenticated だけ
is_org_member           authenticated だけ
can_view_organization   authenticated だけ（★3つ目の 枝を 消しました）
is_org_owner_or_admin   ★関数ごと ありません
```

## USINGとWITH CHECK

★該当なし。★決まり（RLS）を 1つも 触って いません。★`USING(true)` も 作って いません。

★★`org_post_perm_log` の 絞りは、★もとから ある 決まり（`post`・`master`）が します。
★★`org_message_drafts` の 絞りも、★もとから ある 決まり（ご本人）が します。

## 試験

★NOT_YET（★本番の 分）。★when …… 本番に 当てる お許しを いただいた とき。

★★試しの 台帳では、★**形** を 読み直して 確かめました（8行）。
　★`anon` の execute が 3つとも 消えた こと。
　★TRUNCATE・TRIGGER・REFERENCES が 両方の 表から 消えた こと。
　★`is_org_owner_or_admin` と `_a13_policy_backup` が 無く なった こと。

★★★裁定161 の VERIFY（★まだ して いません）──

| | 誰 | 何を した | 期待 |
|---|---|---|---|
| ① | `authenticated` | `truncate org_post_perm_log` | 期待 0行（権限の 誤り） |
| ② | `anon` の 鍵 | `/rpc/is_org_member` | 期待 0行（権限の 誤り） |
| ③ | 関わりの ない 試しの 利用者 | 招待を 1つ 出した 学校を select | 期待 0行 |
| ④ | 合言葉を 持つ 人 | その 学校に 参加する | 期待 1行以上（★較正・通る 側） |
| ⑤ | 学校の 人 | 自分の 学校を select | 期待 1行以上（★較正・通る 側） |

## なりすまし

★使っていない。★形は `information_schema` と `pg_proc` を 読んで 確かめました。

## fail closed

★該当なし。★記録を 書く 処理では ありません。
★★ただし FX3・FX4 は「消せません」「書きかけは 消えません」の 約束を、
　★画面では なく **権限** の 側で 守る もの です。

## 本番の影響

| 見た こと | 数 |
|---|---|
| `org_post_perm_log` | ★引き金が 書く 表（★2026-09-21 から） |
| `org_message_drafts` | ★書きかけ |
| `_a13_policy_backup` | **12行**（★ファイルに 写しました） |
| 招待が 生きて いる 学校 | **0件**（★いま この 枝で 見えて いる 学校は ありません） |

★★★FX6 で 読めなく なる 人 …… ★**いま は 0人** です。
　★使われて いない・切れて いない 招待は **0件** でした（2026-09-22 に 数えました）。
　★★つまり、★この 枝で いま 見えて いる 学校は ありません。

★★★それでも 直す わけ ──
　★いままで …… 招待を 1つでも 出して いる 学校は、★入って いる 人 なら 誰でも
　　その 行（`created_by`・`contract_owner_user_id` を 含む）を 読めました。
　★これから …… その 学校の 人と 学生 だけ に なります。
　★★合言葉での 参加は 通ります。★2026-09-02 から サーバの 側の 処理 だから です。

★★`anon` から 外す 3つの 関数は、★画面の コードから **1か所も** 呼ばれて いません。
　★決まり（`organizations_select`）が `can_view_organization` を 使って いますが、
　★★決まりは 入って いる 人（`authenticated`）として 走ります。★残して あります。

## 承認

★（空の まま お出しします）
