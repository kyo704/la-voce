# reference/

原本のまま置いてある資料です。★書き換えないこと。

| ファイル | 中身 | 備考 |
|---|---|---|
| `Woolsong_設計書.pdf` | 設計書（2026-08-28・33ページ・8部構成） | ★下記のとおり、いまは機械で読めません |

## ★Woolsong_設計書.pdf を、いまの環境では読めません

バイト単位で同一のまま保管していますが、**中身のテキストを取り出せません。**

```
画像PDFではありません（画像0・フォント914・ToUnicode 904・文字描画 28,319）
→ 文字は入っていますが、フォントごとの対応表が要ります
→ pdftotext（poppler）も PyMuPDF も pypdf も、この環境にありません
→ 自前の抽出を試しましたが、拾えた文字は0件でした
```

**したがって、この文書の内容を根拠にした報告はできません。**
引用が必要なときは、次のいずれかをお願いします。

```
① Markdown か テキストで書き出したものを別途いただく
② brew install poppler を実行していただく（pdftotext が使えるようになります）
③ 該当箇所を、会話に貼っていただく
```

★「読めないまま、読めたことにして答える」ことは避けます。
　今日、参照だけあって実体が無い文書が3件見つかっており、
　そのうち1件は私が「存在しない」と誤って報告しました。
