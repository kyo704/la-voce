# Woolsong 画面の値（tokens）

版　tokens-2026-09-10　／　Opus
★**見本の HTML が「正」です。**この表は、その HTML から取り出した値です。
★食い違ったら、**HTML を正**としてください。

---

# 0. まず ── 使い方

```
　★見本は 1画面 1ファイルの HTML です（screens/）。
　★★画像から 読み取らないでください。★HTML に 正しい値が 入っています。

　screens/XXNN-なまえ.html　　★画面（そのまま 開けます）
　screens/XXNN-なまえ.txt　　 ★画面に出る 文字だけ（★改行も 見本どおり）
　screens/XXNN-なまえ.notes.md ★★実装への注記（★画面には 出しません）
```

★**★で始まる行は、★画面に出る文字では ありません。**実装への注記です。
★**`.notes.md` に 分けてあります。★画面の中には もう ありません。**

---

# 1. 色

## 1-1 地と面

| 名前 | 値 | 使うところ |
|---|---|---|
| `--bg` | `#EFE7D6` | いちばん外（画面の外・並べる地） |
| `--paper` | `#FBF6EA` | 画面の地 |
| `--card` | `#FFFFFF` | カードの面 |
| `--line` | `#E4DAC4` | カードの枠・区切り |
| `--line2` | `#F0E9DA` | 行と行の あいだの 細い線 |

★**`--line` は 見本によって `#E9E0CC` と `#E4DAC4` の2つが 混ざっていました。**
★**`#E4DAC4` に そろえます**（★濃いほう。★見分けやすい）。

## 1-2 文字

| 名前 | 値 | 紙の地との比 | 使うところ |
|---|---|---|---|
| `--ink` | `#261913` | 15.8 | 本文・見出し |
| `--ink2` | `#6D5D50` | 5.8 | 補助・**注記の文字** |
| `--ink3` | **`#7C6C5E`** | **4.7** | **罫線・アイコン・区切りだけ** |

★**`--ink3` は `#A79684`（比 2.65）でした。★基準（4.5）を 満たしていません。**
★**`#7C6C5E` に 直します。★そして 文字には 使いません。**
★**注記（`.note`）の文字は `--ink2` に します。**

## 1-3 主色

| 名前 | 値 | 紙の地との比 | 使うところ |
|---|---|---|---|
| `--enji` | `#840C24` | 9.5 | ブランド・選ばれた状態・ボタン・グラフの主色 |
| `--yama` | **`#8C6115`** | **5.1** | 気をつけて見るもの（重なり・有料の印） |
| `--midori` | `#447862` | 4.7 | 別の系列（伴奏・合わせ）・安心の帯 |

★**`--yama` は `#BF8722`（比 2.90）でした。★`#8C6115` に 直します。**

## 1-4 ★グラフの色

```
　★1色の 濃淡だけです。★系列ごとに 色を 変えません。

　こえの ちょうし　★--enji の 濃淡（不透明度 .47 〜 .92）
　歌った 時間　　　★--midori の 濃淡（同上）
　気になったこと　 ★#EFE7D6 → #E3CDD2 → #D7AFB7（3段）
　濃さの地図　　　 ★rgba(132,12,36, .22 + .16×段)　★0のマスは #F3ECDD
　重ねる図　　　　 ★1回ずつ＝--enji 不透明度 .26／まんなか＝--enji 実線 2.2px
```

★**やらないこと**

```
　✕ 信号色（赤・黄・青）
　✕ よい／わるいで 色を 分ける
　✕ 系列ごとに 別の色相
```

## 1-5 影

```
　--sh: 0 1px 0 rgba(0,0,0,.03), 0 6px 18px rgba(90,72,48,.07)   ★スマホ
　PC　  0 1px 0 rgba(0,0,0,.03), 0 8px 24px rgba(90,72,48,.08)
```

---

# 2. 書体

```
　★アプリは ★全部 ゴシックです。★明朝は 使いません。
　　（★明朝を 使うのは 紙もの＝PDF だけです）

　font-family: "Hiragino Sans","Noto Sans JP",system-ui,sans-serif
　★数字も 同じ書体。★等幅にしません
```

---

# 3. 大きさ（スマホ 360px 幅）

| ところ | px | 太さ |
|---|---|---|
| 画面の題（`h2`） | 17 | 400（字間 .06em） |
| 小見出し（`.h3`） | 10.5 | 400（字間 .12em・色 `--ink3`） |
| 本文 | 13.5 | 400 |
| 一覧の行（`.li`） | 13 | 400 |
| 注記（`.note`） | 11 | 400（★色は `--ink2`） |
| タブの文字 | 10 | 選ばれたら 700 |
| 丸いボタン（`.pill`） | 11.5 | 選ばれたら 700 |
| 切替（`.seg`） | 11.5 | 選ばれたら 700 |
| 大きな数字（`.big`） | 26 | 700（字間 −.01em） |
| 大きな数字の単位 | 12 | 400 |
| 主ボタン（`.btn`） | 16 | 700（字間 .06em） |

## 3-1 PC（1400px 幅）

| ところ | px |
|---|---|
| 画面の題（`h2`） | 22 |
| 説明（`.sub`） | 12.5 |
| 表の文字 | 13 |
| 上のバー | 14.5 |
| 左の並び（`.nv`） | 14.5 |

---

# 4. 角の丸み（px）

```
　スマホの外枠　30
　カード　　　  14　（PC・iPad は 12）
　主ボタン　　  13
　入力欄　　　  12　／　入力欄＋1行 の 組は 12 12 0 0 ／ 0 0 12 12
　切替（外）　  11　／　切替（中）　9
　丸いボタン　  99
　小さな札　　  5〜6
　棒グラフ　　  4
　シート（下から出るもの）　18〜22（上の2つだけ）
```

---

# 5. 余白（px）

```
　画面の 左右　　　　15（スマホ）／ 20〜24（PC）
　カードの 内側　　　12 13（スマホ）／ 13 15（PC）
　カードどうし　　　  9
　小見出しの 上下　　14 / 7
　一覧の行の 上下　　9
　押せるところ　　　★どの段でも 44 以上
　入力欄と その下の1行　★離さない（0）
```

---

# 6. 線

```
　カードの枠　　1px `--line`
　行の区切り　　1px `--line2`
　グラフの まんなかの線　2.2px
　グラフの 1回ずつの線　1px（不透明度 .26）
　目盛りの線　　1px `#F0E9DA`
　本番の日の 縦線　1px `--enji` 破線 3 3（不透明度 .55）
```

---

# 7. 決まった 形

```
　スマホ　　　360 × 812
　スマホ 横　　812 × 375
　iPad たて　  830 × 1120
　iPad よこ　  1180 × 830
　パソコン　　 1400 × 880

　下のタブ　　56px（★個人5つ／運営6つ）
　上のバー　　40px（スマホ）／52px（PC）
　健康の線　　26px（スマホ）／30px（PC）★どの画面にも 必ず 出す
　★流れ（連絡・ノート）の 本文の幅　★640px まで
```

---

# 8. ★未定（Code へ：勝手に 決めないでください）

```
　☐ 羊の毛の 色（★装いの 色24色とは 別）
　☐ 押したときの 見え方（★色を 少し 濃くするのか、影を 消すのか）
　☐ 読み込み中の 見え方（★ぐるぐるを 使うのか、灰色の 形を 置くのか）
　☐ アイコン（★見本は 文字だけで 作っています。★絵の アイコンは まだ ありません）
　☐ 文字を 大きくしたときの 4段（★別紙「文字の大きさと、読みやすさ」）

　★上の5つは、★候補を1つ 出して、確認を 取ってください。
```
