# G4 受け入れ確認（14項目）

> ## 2026-09-02 時点のまとめ
>
> **確かめ終わったもの（実地）**
>
> | | 項目 | 確かめ方 |
> |---|---|---|
> | ① | 他人のレッスンが見えない | なりすまし（1→0） |
> | ② | 先生から生徒の記録が読めない | なりすまし（+g4t2・+g4t3 とも0） |
> | ③ | 役割の書き換え | なりすまし4場面＋画面5場面 |
> | ④ | org_events を書けるのは owner/admin だけ | なりすまし（42501） |
> | ⑤ | 出席の印が他人に見えない／最初の1人 | 実機・なりすまし |
> | ⑧ | 在籍が作られる | +g4s1 が2教室に在籍 |
> | ⑨ | 予定の作成・移動・取り下げ・出席 | 実機4段階 |
> | ⑩ | 日付の変更が生徒に届く | 実機（タブを移って戻る） |
> | ⑭ | 白い画面が出ない | 実機の歩き |
> | ⑮ | 契約者がいなくなる知らせ | 実機（+g4t2） |
>
> 退会まわり（生徒・先生・オーナー）も、実地で確かめ終わりました。
>
> **★まだ確かめていないもの（下の各節に印があります）**
>
> | | 項目 | なぜ残っているか |
> |---|---|---|
> | ⑥ | メンバーの名前 | `migration_org_member_names.sql` が★未実行 |
> | ⑦ | 教室への招待 | 直した経路を、まだ一度も通していません |
> | ⑪ | 作りかけの教室 | わざと壊した教室を、まだ作っていません |
> | ⑫ | 運営者を0にしない | 役割変更の歯止め（退会の歯止めとは別物） |
>
> **★⑬は済みました（2026-09-03）。**
>
> `check_missing_profiles.sql` を本番で実行しました。結果は次のとおりです。
>
> - 本番のトリガー（`on_auth_user_created` / `handle_new_user`）は
>   `supabase/schema.sql` と★一致していました。書き換えられていません。
> - ★「本物の幽霊」は**0**でした。
>   つまり「profiles の行が無いのに、ログインしたことがある人」は居ません。
>   ★使えているのに何ひとつ保存されていない方は、居ないということです。
> - `profiles` の行が無い口座は、運営者ご自身の打ち間違い（3つ）だけでした。
>   いずれも一度もログインされていません。★実害はありません。
> - 確認メールを開いていない口座も、その3つだけでした。
>   ほかの利用者の方で未確認の方は、いらっしゃいません。
>
> ★数え上げは、合計ではなく**1件ずつの一覧**で確かめました
>   （「合計が合うことは、内訳が合っている証拠になりません」2026-09-02）。
>
> ★「確認メールを必須にする」は、★すでに有効でした（2026-09-03 に確認）。
>   ★私は「これから有効にする作業が残っています」と書きましたが、誤りでした。
>   設定を見ずに、そう書いていました。
>
>   ★そして、有効だったことは上の結果と辻褄が合います。
>   打ち間違いの3つが `email_confirmed_at` が空のまま残っているのは、
>   ★確認を必須にしている仕組みが、正しく働いている姿です。
>   届かないアドレスの口座は、確認されないまま残ります。
>   ★「未確認の口座がある」ことは、設定の不備ではありません。
>
>   ★⑬に、残っている作業はありません。
>
> ★`check_owner_role_protection.sql` の③（共同オーナー同士）と
> 　④（★オーナーが自分で降りる）も残っています。
> 　④が0行なら、オーナーには降りる手段がありません。
>
> ★表示ゲートのしきい値は **n ≥ 10**（各群）です。n<5 という規則は
> 　この repo にありません（`lib/displayGates.js:22`）。
> 　実地で確かめたのは「少ない記録で、断定的な文が出ないこと」で、
> 　それは★正しい確認です。数字だけ、ここに正しておきます。

**★この一覧の出どころについて。**
統合実行ルート v4 の §9（止まる場所）には、**G4 のゲートはありません**。
G1・G2の3/4・G2の12・G3 の4つだけです。「14項目」という一覧も、
docs/ のどこにも存在しません。
★この文書は、2026-09-02 の作業から**組み立てたもの**です。
　番号は、この文書のためにこちらで振りました。仕様書からの引用ではありません。
　（憲章 §13：文書と実装が食い違ったら、実装のほうが事実）

**使う口座**

| 呼び名 | 役割 |
|---|---|
| +t5 | 教室のオーナー |
| +t6 | 別の先生（招待される側） |
| +s1 | 生徒（+t5 の教室に在籍） |
| 講師 | role = 'teacher' の職員。①③で要ります |

**★service_role では確かめられません。**
　SQL は必ず、その人のセッションでなりすまして実行します。
　`request.jwt.claims` を**先**に、`set local role authenticated` を**あと**に。
　逆にすると `auth.uid()` が null になり、「止まった」ように見えて誤診します。

---

## 0. 先に実行する移行（★これが済むまで、下は始められません）

| ファイル | 状態 |
|---|---|
| `supabase/migration_org_member_names.sql` | ★未実行（⑥が落ちます） |
| `supabase/migration_backfill_orphan_org_memberships.sql` | ★未実行（数えてから直す形） |

`migration_protect_owner_role.sql` と `migration_fix_memberships_update_policy.sql`
は実行ずみです（②と③の一部を確認しました）。

---

## ① lessons ── 他人のレッスンが見えないこと

**満たすこと**：+s1 から見えるレッスンのうち、自分のものでない行が **0**。

**確かめ方**（+s1 のセッション）
```sql
select count(*) as "見える行数",
       count(*) filter (
         where not exists (
           select 1 from public.teacher_student_links l
            where l.id = lessons.link_id and l.student_id = auth.uid())
         and (student_id is null or student_id <> auth.uid())
       ) as "★自分のものでない行"
  from public.lessons;
```

**失敗の見え方**：「自分のものでない行」が 1 以上。
直す前は 1 でした（`(org_id IS NULL) OR …` が素通りさせていた）。

---

## ② entries ── 先生から生徒の記録が読めないこと

**満たすこと**：+t5 から見える他人の記録が **0**。

**確かめ方**（+t5 のセッション）
```sql
select count(*) as "見える生徒の記録行数"
  from public.entries where user_id <> auth.uid();
```

**失敗の見え方**：1 以上。直す前は 1 でした。
★`select("*")` は全列を返すので、「先生に決して渡さない11列」も渡ります
（cycle_start / medication_tags / location / temperature / humidity /
weather / environment_tags / ambient_noise_db / noisy_environment /
flight_hours / jetlag_hours）。

---

## ③ memberships ── 役割の書き換え（★8場面）

**満たすこと**：`supabase/check_owner_role_protection.sql` を実行し、

| | 場面 | 期待 |
|---|---|---|
| ① | 責任者が自分を owner に | **0行** |
| ② | 責任者がオーナーを降格 | **0行** |
| ③ | 共同オーナーAがBを降格 | **0行** |
| ④ | オーナーが自分で降りる | **1行** |
| ⑤ | ★最初の1人（教室を作る） | **1行** |
| ⑥ | オーナーが講師を責任者にする | **1行** |
| ⑦ | 責任者を講師に戻す | **1行** |
| ⑧ | 講師が自分を責任者に | **0行** |

★確認ずみは②と⑥だけです。残り6つは未確認です。

**失敗の見え方**
- ①②③⑧ が1行 → 権限の昇格が実在します。
- ★④⑤⑥⑦ が0行 → **締めすぎ**です。とくに**⑤が0行なら、誰も新しい教室を
  作れません**。攻撃を試すだけでは、これは絶対に見つかりません。

---

## ④ org_events ── 書けるのはオーナーと責任者だけ

**満たすこと**：+s1（在籍しているだけの生徒）からの INSERT が **42501** で止まる。

**確かめ方**（+s1 のセッション）
```sql
begin;
insert into public.org_events (org_id, event_date, kind, title)
values ('<教室の uuid>', current_date, 'その他', '権限の確認');
rollback;
```

**失敗の見え方**：INSERT が成功する。
★`org_events_write_admin` は `FOR ALL` で `with_check` が null です。
　省略時は USING が使われる、とされていますが、実地で確かめます。

---

## ⑤ org_event_participants ── 誰が出るかは、他の人に見えないこと

**満たすこと**：+t5（オーナー）から見える印の行が、**自分のぶんだけ**。

**確かめ方**（+t5 のセッション）
```sql
select count(*) as "見える印の行数",
       count(*) filter (where user_id <> auth.uid()) as "★他人の印（0であること）"
  from public.org_event_participants;
```

**失敗の見え方**：「他人の印」が 1 以上。
★誰が出席するかは、同じ教室の人にも見せない決めごとです。

---

## ⑥ メンバーの名前 ── 教室の中の人だけが引けること

**満たすこと**（3つとも）
1. 教室に居る人が呼ぶ → 人数ぶん返る
2. ★教室に居ない人が呼ぶ → **0行**
3. ★`profiles` そのものは、あいかわらず自分の行しか読めない

**確かめ方**（`migration_org_member_names.sql` の末尾に、そのまま入っています）
```sql
begin;
select set_config('request.jwt.claims','{"sub":"<関係のない人>","role":"authenticated"}', true);
set local role authenticated;
select count(*) as "★0 であること" from public.get_org_member_names('<教室の uuid>');
rollback;
```

**失敗の見え方**
- 2 が1行以上 → 教室の uuid さえ知れば、誰でも名簿が引けます。
- 3 が2行以上 → profiles が開いています。★同じ行に allergies・
  regular_medications・周期・is_under_18 があります。

**画面でも見ます**：教室を開き、メンバー欄に名前が出ること。
★404 が出たら、移行（0）が未実行です。

---

## ⑦ 教室への招待 ── 受けられること（★これまで一度も成功していません）

**満たすこと**：+t5 が発行したコードを +t6 が入れて、教室に入れる。

**手順**
1. +t5：教室を開く → 「招待コードを発行する」
2. +t6：もっと → 教室のコードを入れる → 教室名が出る → 「参加する」
3. +t6：教室が一覧に出る

**失敗の見え方**
- 「コードが見つかりませんでした」→ 確認の経路（lookup）が通っていません。
- 「参加できませんでした」→ サーバ側の理由がそのまま出ます。
- ★「参加に失敗しました。もう一度お試しください。」が出たら、**古い版**です。
  この文言は消してあります。何度やっても通らないものに再試行をすすめる文でした。

---

## ⑧ 在籍 ── 生徒が教室に入ること

**満たすこと**：+s1 が +t5 の招待を受けたあと、`enrollments` に行があり、
+t5 の名簿に +s1 が出る。

**確かめ方**（service_role でよい：事実を見るだけ）
```sql
select count(*) as "在籍の行数" from public.enrollments where status = 'active';
```

**失敗の見え方**：0行のまま。
★2026-09-01 まで、この表は**全体で0行**でした。生徒の一覧・名簿・担当・
レッスンが、すべて空のままでした。エラーは1つも出ていませんでした。

---

## ⑨ 教室の予定 ── 作る・動かす・取り下げる・出る

**満たすこと**（4つとも）
1. +t5 が予定を作る → +s1 のカレンダーに中立の印が出る
2. +t5 が日付を変える → +s1 に「◯月◯日から◯月◯日に変わりました」
3. +t5 が取り下げる → +s1 に「この予定は取り下げられました」＋「表示から消す」
4. +s1 が「出る」を押す → 「出ます」に変わる

**失敗の見え方**
- 升目が種類ごとに**色分け**される → §7-5 違反（値で塗り分けない）。
- 取り下げたのに、+s1 の画面から**黙って消える** → 行を消しています。
- 「あなたの出席登録は取り消されました」→ 主語が人になっています。

---

## ⑩ ★予定の変更が、生徒の画面に届くこと（今日いちばん見落としやすい所）

**満たすこと**：+t5 が日付を変えたあと、+s1 が
**アプリ内でタブを移って戻る**だけで、新しい日付が出る。

**手順**
1. +s1：ノート → カレンダーを開いたままにする
2. +t5：予定の日付を変える
3. +s1：ホーム → ノート → カレンダー と移る
4. ★新しい日付が出ていること

**失敗の見え方**：古い日付のまま。
★見ている最中には変わりません（購読は1つもありません）。それは**仕様どおり**です。
　確かめるのは「タブを移って戻ったら新しくなる」ことです。
★2026-09-02 の不具合は、これが**一度も起きない**ことでした。
　戻ったときの取り直しに `fetchTeacherLinks` しか入っておらず、
　しかもタブは状態なので再マウントもしませんでした。

---

## ⑪ 作りかけの教室 ── 見えるが、押せないこと

**満たすこと**：オーナーの行が無い教室が、金色の枠で出る。
★その中に、押せるものが1つも無い。

**失敗の見え方**
- 一覧に**出てこない** → 持ち主から見えず、直しようがありません。
- ボタンが**ある** → 押すと必ず 403 になります。案Bに反します。

---

## ⑫ 運営者を0にしないこと

**満たすこと**（2つとも）
1. オーナーが1人だけの教室で、その人を「講師」にしようとする → 止まる
2. ★**責任者が1人だけでオーナーが居ない**教室で、その人を「講師」にする → 止まる

**失敗の見え方**：2 が通る。
★2 は「最後のオーナー」しか数えていなかったときに、素通りしていた場面です。
　オーナーが居ないので、その判定に**一度も引っかかりません**。

---

## ⑬ profiles の行が無い人 ── 画面が崩れないこと

**満たすこと**：名前の出ない人が居ても、内部のID（`53ef27ed…` のような文字列）が
画面に出ない。「名前未設定」または「名前を読み込めませんでした」と出る。

**失敗の見え方**：uuid の断片が出る／空白になる。

★これは**表示の問題ではありません**。profiles の行が無い人は、
　ログインできて、アプリも使えますが、**保存がひとつも効きません**
　（書き込みは全部 `.update(...).eq("id", userId)`、INSERT のポリシーが無く、
　0行の更新を PostgREST は**エラーにしません**）。
　→ `supabase/check_missing_profiles.sql` で人数を数えてください。

---

## ⑮ オーナーが抜けて、教室は続く ── 契約者の知らせが出ること　★確認ずみ

> **2026-09-02、実地で確認しました。**
> +g4t2（音楽学校A のオーナー・+g4t3 が責任者として残る）が退会しようとして、
> ★止まらずに知らせが出ました。「運営者に連絡する」と
> 「了解しました。退会を続けます」の両方が押せる状態でした。
> ★実際の削除はしていません（教室が必要なため、知らせの画面で止めています）。

**満たすこと**：オーナーが退会しようとしたとき、その教室に★ほかの運営者
（責任者）が残っているなら、**止めずに**次の1行と連絡先が出る。

```
あなたが退会すると、この教室の契約者がいなくなります。
運営を続けるには、運営者にご連絡ください。
   [運営者に連絡する]（mailto・件名に教室ID）
   [了解しました。退会を続けます]
```

**確かめ方**
1. +g4t2 の教室に、+g4t3 が責任者として居ることを確かめる
2. +g4t2 で もっと → アカウントの削除 → 削除する を押す
3. ★止まらず、上の知らせが出ること
4. ★「了解しました」を押すと、もう一度押せば進める状態になること
   （★ここで実際に消さないでください。教室が必要です）

**失敗の見え方**
- 二択（教室を閉じる／教室を残す）が出る → ★止めすぎです。
  責任者が残っているので、教室は続きます。
- 何も出ずに消える → 契約者が黙って居なくなります。
- ★責任者（+g4t3）が抜けるときにも出る → 出しすぎです。
  責任者は契約者ではありません。
- ★生徒（+g4s1）が抜けるときに出る → 出しすぎです。

★「教室を動かせるか」と「誰が払うか」は、別の問いです。
　止めるのは前者が0になるときだけ。後者は知らせるだけです。

---

## ⑭ 共通ゲート（★どの段階でも、最後にこの4つ）

> ★この節は、G4 に限りません。**どの段階でも、最後に通す**ものです。
> 　（この文書の名前が G4 なのは、書かれた時期によるものです。
> 　　いずれ段階に依らない文書へ切り出すか、名前を変えます）

### E｜★書いた値は読まれているか、作った関数は呼ばれているか（2026-09-03 追加）

**★規則：書き込んでいる値は、必ずどこかで読まれているか。**
**★規則：作った関数は、必ずどこかから呼ばれているか。**

★**作ったその場で確かめること。**あとからでは見つかりません。
　新しく書き込む列・新しく作る関数について、
　**読む側／呼ぶ側を必ず示してください**（grep で示せます）。

```
grep -rn "その列名" components lib app --include=*.js --include=*.jsx
grep -rn "その関数名" components lib app --include=*.js --include=*.jsx
```

**★1件しか出なければ、それは書いている側／定義だけです。読む人も呼ぶ人もいません。**

この規則は、5つの実例から作られました。

| 何 | どうなっていたか |
|---|---|
| `consent_policy_version` | 書かれるが、★どこでも比べられていない（再同意が効かない） |
| `handleCreateOrgLesson` | 定義だけ。★呼ぶ場所が無い（教室のレッスンを作れない） |
| `handleLeaveOrg` | 定義だけ。★呼ぶ場所が無い（教室から抜けられない） |
| `suggestActivityKind` | 検査まであるのに、★画面につながっていない |
| `consent_records` | 表・移行・行を作る関数・4つの台帳登録。★呼ぶ人が1人もいない |

★「あとで繋ぐ」は、繋がりません。5件とも、そのつもりで残されたものです。



### F｜★本番に手で当てたものが、リポジトリに写っているか（2026-09-03 追加）

> **本番に手で当てない。どうしても当てたら、その日のうちにリポジトリへ写す。**

★**1日で3件出たので、規則にしました。**
`lessons` の `(org_id IS NULL) OR …`／`entries` の先生向けポリシー／
`profiles_connected_display_name` と `are_connected`。
★**最後の2つは、リポジトリに1文字もありません。**

★どれも「あとで写すつもり」でした。★**「あとで写す」は、写さないことと同じです。**

確かめ方：`supabase/AUDIT_full_schema_vs_repo.sql` を流し、
①ポリシー ②関数 ③トリガー ④権限 ⑤拡張 ⑥表と列 ⑦外部キー を
`supabase/schema.sql` と `supabase/migration_*.sql` に突き合わせます。

★出す表は2つです。
- 本番にあって、リポジトリに無いもの（★今日3件出たのが、これです）
- リポジトリにあって、本番に当たっていないもの（★「入れたつもり」）

★**後者も同じくらい危ないです。** 直したつもりのものが効いていません。

### A｜画面を歩く（★白い画面が出ないこと）

すべてのタブと、権限で囲まれた画面を開きます。
+t5（オーナー）・+t6（別の先生）・+s1（生徒）・講師 の**4人ぶん**歩きます。

**失敗の見え方**：押せるボタンの先が**何も描かれない**。
★条件が偽のとき、React は黙って何も描きません。
　憲章：「★画面ごと権限で囲まないこと。説明・案内・戻る道のどれかを必ず出す。」

### B｜禁じた言い回しが出ていないこと

- 「まだ」（★全面禁止）
- 「確認してください」（急かす言葉）
- 「もう一度お試しください」（★通らない経路に出していないこと）
- 「あなたの出席登録は取り消されました」（主語が人）
- 断定的な相関文（「◯◯が原因です」）
- 他の利用者との比較・平均・順位（★恒久的に作らない）

### C｜戻る道があること

行き止まりが無いこと。権限で止まる画面には、**必ず押せる選択肢**を置きます。
★ボタン1つ＋文章での案内、という形にしないこと（案内も押せるようにする）。

### D｜実機で歩く

iPhone の実機で、A と同じ道を歩きます。
- 入力欄に触れて、画面が拡大しないこと（16px 以上）
- 下端が安全領域に隠れないこと
- 機内モードにして、**白い画面にならない**こと

---

## 最後に（開発機で）

```bash
npm run build
npm run lint
for f in components/tests/*.test.js; do node "$f" >/dev/null || echo "失敗: $f"; done
```

★検査は**リポジトリの中しか見ていません**。
　①②のような穴は、ソースを読んでいるかぎり永久に見つかりません。
　見つけたのは、実際のセッションで**行を数えた**からです。
　この一覧の SQL は、その代わりにはなりません。**両方やってください。**
