# S2 を本番に当てました（裁定159・2026-09-21）

紙 `supabase/migration_monka_read_log_s2.sql`（リポジトリに入っています）

## 当てる前（本番）

| 見たこと | 数 |
|---|---|
| `monka_read_log` の行 | 0 |
| 列の数 | 6 |
| `open_monka_thread` | 0（ありません） |
| 決まりの数 | 2 |

## 当てたあと（本番）

| 見たこと | 結果 |
|---|---|
| `monka_read_log` の行 | 0（触れていません） |
| 列の数 | 11（5つ足しました） |
| `open_monka_thread` | 1 |
| security definer | True |
| execute の渡し先 | authenticated のみ（anon・PUBLIC には渡していません） |
| 決まりの数 | 2（変わっていません） |
| update・delete の決まり | 0 |
| `reason_kind` の縛り | 4値（jiko / honnin / horei / sonohoka） |
| `reason_note` の縛り | そのほかのときだけ必須・空白だけは不可・100字まで |
| S1（`org_messages` の monka_read の枝） | 外れたまま |

## まだ開きません

S1 で閉じた道はそのままです。`open_monka_thread` はありますが、
運営の画面はまだ `org_messages` を直に select しています（S3 が残っています）。

つまり、**いま門下のやりとりを開ける人はいません。**
記録の仕組みだけが先に入った状態です。順番はこれで正しいです。

## 試しの台帳で確かめたこと（13/13）

道具 `tools/s2_prove.js`。本当に入って、本当に呼びました。

| 見たこと | 結果 |
|---|---|
| 中身が返る（3行）／記録が1行増える／役職名が残る | PASS |
| 一覧だけでは増えない | PASS |
| 理由が空だと開けない／そのとき1行も増えない | PASS |
| そのほかで短文が空でも開けない | PASS |
| 記録が書けないときは中身が返らない（fail closed） | PASS |
| update・delete が拒まれる（42501）／記録は残る | PASS |
| 先生の画面に開いた記録が出ない | PASS |

## 気づいたこと（★次に決めることがあります）

もとからある `reason` 列に、`char_length(btrim(reason)) >= 4` の縛りがあります。
道は `reason` にも `reason_kind` と同じ値を入れています。

いまは通ります（いちばん短い `jiko` が4字ちょうど）。
けれど、**同じ値が2つの列にあります。** 名前を短くした日に、
`reason` の縛りだけが先に止まります。

`reason` を畳むか、`reason_kind` だけを正とするかは、決めのことです。
ここでは決めません。
