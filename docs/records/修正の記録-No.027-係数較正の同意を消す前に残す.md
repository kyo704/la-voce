# 修正の記録 No.027 ── 係数較正の同意を、消す前に残す

全174行 / 末尾は「まだ消していません。ご判断をお待ちします。」

- 日付 … 2026-09-15
- 裁定 … Opus「★DO NOT DELETE YET／★1 preserve the current wording, verbatim」
- 出どころ … 裁定 その27／その45（★曲の負荷係数 `d_override` を退けた）

★★この紙は、**消す前**に書いています。
　★消したあとでは、★何に同意していただいたのかが、どこにも残りません。

---

## 1. ★同意の文（★一字一句、そのまま）

### ㋐ 正の文 ── オンボーディング step 0（`components/VocalTracker.jsx:4736`）

```
（任意） 匿名化した統計として、発声負荷の係数など、分析に使う定数の較正に役立てることに同意します。個人を特定できる形で第三者に提供されることはありません。
```

### ㋑ プロフィールの中（`components/VocalTracker.jsx:20716`）

```
（任意）匿名化した統計として、発声負荷の係数など、分析に使う定数の較正に役立てることに同意する
```

### ㋒ 台帳の側（`lib/consent.js:104`・`key: "research.anonymized"`）

```
匿名化した統計として、発声負荷の係数など、分析に使う定数の較正に役立てることに同意します。個人を特定できる形で第三者に提供されることはありません。★オフでも、機能はすべて同じように使えます。
```

★㋒ の末尾の1文（「★オフでも、機能はすべて同じように使えます。」）は、
★㋐㋑ にありません。★`★` も付いたままです。

★`lib/consent.js` のコメントは、こう書いています ──

> ★画面に出ている文言と、一字一句そろえること（2026-09-01）。
> 　2か所に別の文言があると、どちらに同意したのか答えられなくなります。
> 　正は components/VocalTracker.jsx のオンボーディング step 0 です。

★**その約束が、すでに破れていました。** 3か所に3通りあります。

---

## 2. ★Q1 ── 動いています。字だけではありません

★Opus のお尋ね「is it computed, stored, displayed?」── ★**3つとも はい** です。

| 段 | どこ |
|---|---|
| 入口（人が選ぶ） | `:3893` `dOverrideChoice` ／ `:4034-4050` 押す札 ／ `:4073` 保存 |
| 列 | `d_override`（`rowToEntry:6205`／`entryToRow:12527` ほか） |
| 読む | `:7896` `resolveRepertoireD` … `record.dOverride` を読みます |
| 式 | `:3645` `computeSongFactor` ／ `songFactorFromD`（γ=1.7・κ=0.6） |
| 積む | `computeDailyLoad`（`:1121`）→ `acwrSeries`（`:7909`） |
| ★出す | `:19517` の折れ線グラフ ／ `:8705`「その日の声の使用量」 |

★つまり ── ★**利用者が押し、値が入り、式が回り、グラフに出ています。**

★「文だけが残っていた」のではありません。★数が出ています。

### ★ただし、順番があります

`resolveRepertoireD` は、上から順に見ます。

```
1  tessituraNote があれば それ（confidence: "entered"）
2  無ければ topNote から 推定（confidence: "estimated"）
3  ★どちらも 無ければ dOverride（confidence: "coarse"）
```

★`d_override` が効くのは、★音名をどちらも入れていない曲だけです。
★何件がそうなのかは、★台帳を見ないと分かりません。

---

## 3. ★Q2 ── 私には数えられません

`consent_records` を引く手がありません。★SQL は坂本さんが流されます。

```sql
-- ★研究への協力に同意された方の数と、いつからいつまで（★読むだけ）
select
  count(*)                      as "件数",
  count(distinct user_id)       as "人数",
  min(granted_at)::date         as "はじめ",
  max(granted_at)::date         as "おわり"
from public.consent_records
where consent_key = 'research.anonymized';

-- ★月ごと（★人を特定できない粒度にしています）
select
  to_char(granted_at, 'YYYY-MM') as "月",
  count(*)                       as "件数"
from public.consent_records
where consent_key = 'research.anonymized'
group by 1
order by 1;

-- ★いま「入」の方が何人か（★取り消しも見ます）
select
  count(*) filter (where revoked_at is null) as "いま 入",
  count(*) filter (where revoked_at is not null) as "取り消し済み"
from public.consent_records
where consent_key = 'research.anonymized';
```

★列の名前（`consent_key` / `granted_at` / `revoked_at`）が違うかもしれません。
★違えば、そのままお知らせください。★紙を直します。

---

## 4. ★お知らせの文（★案・まだ送っていません）

★裁定のとおり、★これは「同意の取り消しのお願い」ではありません。
★**こちらが やめる**というお知らせです。★何もしていただく必要はありません。

```
ご同意いただいていた「発声負荷の係数などの較正」について、
この取り組みを行わないことにしました。

この目的で、あなたの記録が使われたことはありません。
これからも使いません。

お手数をおかけすることはありません。何もしていただかなくて大丈夫です。
```

★3文です。★お願いを1つも書いていません。

★★ただし、2行目に条件があります ──
　★「使われたことはありません」と書けるのは、★**本当にそうだった場合だけ**です。
　★Q2 の答えと、★較正を実際に行ったかどうかを、★先に確かめてください。
　★★行っていたなら、★2行目は書けません。★別の文になります。

★送る前に、★Sonnet にお見せします（★裁定のとおり）。

---

## 5. ★消す順（★まだ消していません）

```
1  ★この紙（★済み）
2  Q2 の数を確かめる
3  お知らせの文を決める
4  消す ── 列 d_override ／ 式 :3645 ／ 同意の文 3か所
5  送る
```

★4 を 3 より先にしないこと。
★消したあとでは、★何をやめたのかを説明できません。

---

## 6. ★消すときに触るもの（★下調べ）

| 何 | どこ |
|---|---|
| 入口 | `components/VocalTracker.jsx:3893, 4029-4082, 4123, 5685` |
| 列 | `d_override`（`:6205, 12527, 12638, 12740`） |
| 読む | `:7896` の3段目 |
| 式 | `:3645-3661`（`REPERTOIRE_GAMMA` ほか） |
| 画面の字 | `:4575`「曲目ごとの負荷計算では…」 |
| 同意の字 | `:4736` ／ `:20716` ／ `lib/consent.js:104` |

★★`computeSongFactor` は `d_override` **専用ではありません**。
　★音名から出した `d` も、同じ式を通ります。
　★★式ごと消すと、★音名を入れた方の負荷も変わります。
　★★どこまで消すのか、★ご判断が要ります。

---

まだ消していません。ご判断をお待ちします。
