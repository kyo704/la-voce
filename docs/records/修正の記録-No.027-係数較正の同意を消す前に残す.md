# 修正の記録 No.027 ── 係数較正の同意を、消す前に残す

全566行 / 末尾は「ご判断をお待ちします。」

- 日付 … 2026-09-15
- 裁定 … Opus「★DO NOT DELETE YET／★1 preserve the current wording, verbatim」
- 出どころ … 裁定 その27／その45（★曲の負荷係数 `d_override` を退けた）

★★この紙は、**消す前**に書いています。
　★消したあとでは、★何に同意していただいたのかが、どこにも残りません。

---

## 1. ★同意の文（★一字一句、そのまま）

### ㋐ 正の文 ── オンボーディング step 0（`components/VocalTracker.jsx:4736`）

```
（任意） 匿名化した統計として、発声負荷の係数など、分析に使う定数の較正に役立てることに同意します。個人を特定できる形で第三者に提供されることはありません。
```

### ㋑ プロフィールの中（`components/VocalTracker.jsx:20716`）

```
（任意）匿名化した統計として、発声負荷の係数など、分析に使う定数の較正に役立てることに同意する
```

### ㋒ 台帳の側（`lib/consent.js:104`・`key: "research.anonymized"`）

```
匿名化した統計として、発声負荷の係数など、分析に使う定数の較正に役立てることに同意します。個人を特定できる形で第三者に提供されることはありません。★オフでも、機能はすべて同じように使えます。
```

★㋒ の末尾の1文（「★オフでも、機能はすべて同じように使えます。」）は、
★㋐㋑ にありません。★`★` も付いたままです。

★`lib/consent.js` のコメントは、こう書いています ──

> ★画面に出ている文言と、一字一句そろえること（2026-09-01）。
> 　2か所に別の文言があると、どちらに同意したのか答えられなくなります。
> 　正は components/VocalTracker.jsx のオンボーディング step 0 です。

★**その約束が、すでに破れていました。** 3か所に3通りあります。

---

## 2. ★Q1 ── 動いています。字だけではありません

★Opus のお尋ね「is it computed, stored, displayed?」── ★**3つとも はい** です。

| 段 | どこ |
|---|---|
| 入口（人が選ぶ） | `:3893` `dOverrideChoice` ／ `:4034-4050` 押す札 ／ `:4073` 保存 |
| 列 | `d_override`（`rowToEntry:6205`／`entryToRow:12527` ほか） |
| 読む | `:7896` `resolveRepertoireD` … `record.dOverride` を読みます |
| 式 | `:3645` `computeSongFactor` ／ `songFactorFromD`（γ=1.7・κ=0.6） |
| 積む | `computeDailyLoad`（`:1121`）→ `acwrSeries`（`:7909`） |
| ★出す | `:19517` の折れ線グラフ ／ `:8705`「その日の声の使用量」 |

★つまり ── ★**利用者が押し、値が入り、式が回り、グラフに出ています。**

★「文だけが残っていた」のではありません。★数が出ています。

### ★ただし、順番があります

`resolveRepertoireD` は、上から順に見ます。

```
1  tessituraNote があれば それ（confidence: "entered"）
2  無ければ topNote から 推定（confidence: "estimated"）
3  ★どちらも 無ければ dOverride（confidence: "coarse"）
```

★`d_override` が効くのは、★音名をどちらも入れていない曲だけです。
★何件がそうなのかは、★台帳を見ないと分かりません。

---

## 3. ★Q2 ── 私には数えられません

`consent_records` を引く手がありません。★SQL は坂本さんが流されます。

```sql
-- ★研究への協力に同意された方の数と、いつからいつまで（★読むだけ）
select
  count(*)                      as "件数",
  count(distinct user_id)       as "人数",
  min(granted_at)::date         as "はじめ",
  max(granted_at)::date         as "おわり"
from public.consent_records
where consent_key = 'research.anonymized';

-- ★月ごと（★人を特定できない粒度にしています）
select
  to_char(granted_at, 'YYYY-MM') as "月",
  count(*)                       as "件数"
from public.consent_records
where consent_key = 'research.anonymized'
group by 1
order by 1;

-- ★いま「入」の方が何人か（★取り消しも見ます）
select
  count(*) filter (where revoked_at is null) as "いま 入",
  count(*) filter (where revoked_at is not null) as "取り消し済み"
from public.consent_records
where consent_key = 'research.anonymized';
```

★列の名前（`consent_key` / `granted_at` / `revoked_at`）が違うかもしれません。
★違えば、そのままお知らせください。★紙を直します。

---

## 4. ★お知らせの文（★案・まだ送っていません）

★裁定のとおり、★これは「同意の取り消しのお願い」ではありません。
★**こちらが やめる**というお知らせです。★何もしていただく必要はありません。

```
ご同意いただいていた「発声負荷の係数などの較正」について、
この取り組みを行わないことにしました。

この目的で、あなたの記録が使われたことはありません。
これからも使いません。

お手数をおかけすることはありません。何もしていただかなくて大丈夫です。
```

★3文です。★お願いを1つも書いていません。

★★ただし、2行目に条件があります ──
　★「使われたことはありません」と書けるのは、★**本当にそうだった場合だけ**です。
　★Q2 の答えと、★較正を実際に行ったかどうかを、★先に確かめてください。
　★★行っていたなら、★2行目は書けません。★別の文になります。

★送る前に、★Sonnet にお見せします（★裁定のとおり）。

---

## 5. ★消す順（★まだ消していません）

```
1  ★この紙（★済み）
2  Q2 の数を確かめる
3  お知らせの文を決める
4  消す ── 列 d_override ／ 式 :3645 ／ 同意の文 3か所
5  送る
```

★4 を 3 より先にしないこと。
★消したあとでは、★何をやめたのかを説明できません。

---

## 6. ★消すときに触るもの（★下調べ）

| 何 | どこ |
|---|---|
| 入口 | `components/VocalTracker.jsx:3893, 4029-4082, 4123, 5685` |
| 列 | `d_override`（`:6205, 12527, 12638, 12740`） |
| 読む | `:7896` の3段目 |
| 式 | `:3645-3661`（`REPERTOIRE_GAMMA` ほか） |
| 画面の字 | `:4575`「曲目ごとの負荷計算では…」 |
| 同意の字 | `:4736` ／ `:20716` ／ `lib/consent.js:104` |

★★`computeSongFactor` は `d_override` **専用ではありません**。
　★音名から出した `d` も、同じ式を通ります。
　★★式ごと消すと、★音名を入れた方の負荷も変わります。
　★★どこまで消すのか、★ご判断が要ります。

---

まだ消していません。ご判断をお待ちします。

---

# ★追記（2026-09-15）── Q2 の答え。★同意は `consent_records` に入っていません

## 7. ★Sonnet が確かめてくださったこと

```
列名   consent_key ではなく ★purpose_key
       revoked_at  ではなく ★withdrawn_at
中身   purpose_key = "health.record" が 22件。★"research.anonymized" は 0件
```

★私の書いた SQL は、★列名も鍵も間違っていました。

## 8. ★保存先を追いました

★答えは ── ★**`profiles.consent_stats_use_at`** です。
★`consent_records` ではありません。

### ㋐ オンボーディング（`components/VocalTracker.jsx:4680`）

```js
consent_stats_use_at: statsConsent ? new Date().toISOString() : null,
```

### ㋑ プロフィール（`:20712`）

```js
const value = checked ? new Date().toISOString() : null;
const { error } = await supabase.from("profiles")
  .update({ consent_stats_use_at: value }).eq("id", userId);
```

★時刻が1つ入るだけです。★入＝時刻、切＝`null`。
★**歴史が残りません。** いつ入れて、いつ切ったかは分かりません。

## 9. ★もう1つ見つかりました ── `research.anonymized` は死んだ定義です

`lib/consent.js:104` に `key: "research.anonymized"` があります。
★**どこからも読まれていません。**

`consent_records` に書いているのは、この3か所だけです。

| どこ | 鍵 |
|---|---|
| `:9757`（撤回） | `"health.record"` |
| `:9790`（同意） | `"health.record"` |
| `components/RefluxCareConsent.jsx:29` | `"health.reflux_care"` |

★`purposeByKey()` を呼んでいるのは2か所で、どちらも別の鍵です。

★つまり `research.anonymized` は、★**書かれることも読まれることもありません**。
★規則「作った関数は、必ずどこかから呼ばれているか」に触れています。

### ★これが、文言が3通りに割れていた理由です

`lib/consent.js` は、こう書いていました ──

> ★画面に出ている文言と、一字一句そろえること（2026-09-01）。

★けれど、この定義は**誰にも使われていません**。
★だから、ずれても誰も気づきませんでした。★見張りもありません。

★字を1か所に置いても、★そこが読まれていなければ、1か所にした意味がありません。

## 10. ★Q2 のやり直し（★正しい相手に）

```sql
-- ① いま「入」の方は何人か
select
  count(*) filter (where consent_stats_use_at is not null) as "いま 入",
  count(*) filter (where consent_stats_use_at is null)     as "切",
  count(*)                                                 as "ぜんぶ"
from public.profiles;

-- ② いつご同意いただいたか（★月ごと。日ではありません）
select
  to_char(consent_stats_use_at, 'YYYY-MM') as "月",
  count(*)                                  as "人数"
from public.profiles
where consent_stats_use_at is not null
group by 1
order by 1;

-- ③ はじめとおわり
select
  min(consent_stats_use_at)::date as "はじめ",
  max(consent_stats_use_at)::date as "おわり"
from public.profiles
where consent_stats_use_at is not null;
```

★`consent_records` は引きません。★入っていないからです。

## 11. ★お知らせの文が変わります

★「いつご同意いただいたか」は、★`consent_stats_use_at` の時刻で分かります。
★けれど「一度入れて、あとで切った方」は**分かりません**。★歴史がないからです。

★だから、お知らせを送る相手は「★いま入の方」だけになります。
★過去に入れて切られた方には、★届けようがありません。

★★これは、お知らせの文に書くべきことです ──
　★「全員にお送りできたか」と聞かれたら、★「いいえ」が答えです。

## 12. ★消すときに増えたもの

| 何 | どこ |
|---|---|
| ★列 `profiles.consent_stats_use_at` | ★これが本体でした |
| 読む | `:6285`（取得する列の並び）／`:6403`／`:10714`／`:10898` |
| 書き出し | `lib/exportData.js:78`（★本人の持ち出しに入っています） |
| ★死んだ定義 | `lib/consent.js:104`（★誰も読んでいません） |

★`lib/exportData.js` に入っているので、★消すと**書き出しの中身も変わります**。
★消した列を書き出しから外す、という判断が要ります。

---

まだ消していません。ご判断をお待ちします。

---

# ★追記2（2026-09-15）── Q2 の答えが出ました

## 13. ★数（★Sonnet が本番で確かめてくださいました）

```
いま 入   29人
切        20人
ぜんぶ    49人

はじめ  2026-08-26
おわり  2026-09-15
  8月 …  3人
  9月 … 26人
```

★**29人**が、お知らせの相手です。

## 14. ★★全員にはお届けできません

★「切」の20人には、★2つの方が混ざっています。

```
・一度も入れていない方
・★一度入れて、あとで切った方
```

★**この2つを、見分けられません。**
★`consent_stats_use_at` は時刻を1つ持つだけで、★歴史がないからです。

★だから ──

> 「全員にお送りできましたか」
> ★**いいえ。** お送りできるのは、いま入の29人だけです。

★★これは、お知らせの文にも書きます。★黙って「29人に送りました」と
　★言うと、★あたかも全員に届いたように読めます。

★★同じことが、これからも起きます。
　★「いまの状態」しか持たない列に、★同意を入れているからです。
　★`consent_records` は歴史を持ちます。★こちらは持ちません。
　★★次に同意を作るときは、★歴史のほうへ入れてください。

---

# ★追記3（2026-09-15）── お知らせの文／書き出しの判断／死んだ定義

## 15. ★お知らせの文（★案。まだ送っていません）

★裁定のとおり、★3文です。★お願いを1つも書いていません。

```
ご同意いただいていた「発声負荷の係数などの較正」について、
この取り組みを行わないことにしました。

この目的で、あなたの記録が使われたことはありません。これからも使いません。

何もしていただく必要はありません。同意の設定は、こちらで外します。
```

### ★2文目について（★確かめが要ります）

★「使われたことはありません」と書けるのは、★本当にそうだった場合だけです。

★私が確かめられたこと ──

| 何 | 結果 |
|---|---|
| `consent_stats_use_at` を読むコード | ★4か所。どれも**画面の出し分けと下書きの除外**だけ |
| 統計・較正に使うコード | ★**1つも見つかりません** |
| 外へ送るコード | ★No.019 で助言の道を消して以来、★1本もありません（`lib/outboundRoutes.js`） |

★つまり、★**この同意を根拠にした処理は、コードに存在しません**。
★2文目は書けます。

★★ただし、★私が見たのはコードだけです。
　★手作業で集計されたことがあるかは、★坂本さんしかご存じありません。
　★★あったなら、★2文目は書き換えが要ります。

### ★3文目について

★「同意の設定は、こちらで外します」と書きました。

★列ごと消すので、★設定そのものが無くなります。
★「ご自分で外してください」とは書きません。★お手間をかけないためです。

### ★★お知らせに添える、もう1行（★§14）

★29人にしかお送りできません。★そのことを、報告の側に残します。

★お知らせの本文には書きません ──
★受け取った方には関係のない、★こちらの事情だからです。
★★けれど「全員に送った」とは、★どこにも書かないこと。

## 16. ★書き出しから外すか（★判断材料）

`lib/exportData.js:78` の `EXPORTED_PROFILE_COLUMNS` に入っています。

### ★読む側4か所の中身

| どこ | 何をしているか | 列を消したら |
|---|---|---|
| `:6285` | 取得する列の並び（`PROFILE_BASE_COLUMNS`） | ★消さないと**取得でエラー**になります。必ず外します |
| `:6403` | 取得した値を state に写す | ★消します。残すと undefined が入ります |
| `:10714` | 下書きから**除く**（フォームが編集しないため） | ★消せます。除く対象が減るだけです |
| `:10898` | 変更検知から**除く**（同上） | 同上 |

★`:10714` と `:10898` は、★「触らない」ための記述です。
★列が無くなれば、★そもそも触りようがありません。★安全に消せます。

### ★私の見立て ── ★外します

★理由は2つです。

```
1  列が無くなるので、★書き出そうとすると エラーになります
   ★lib/exportData.js の EXPORTED_PROFILE_COLUMNS は、
     ★並べた列だけを取ります（★entries と違い、自動では増えません）
   ★消し忘れると、★書き出しが丸ごと落ちます

2  ★「同意しなかった」という記録も、消えます
   ★29人の「入」だけでなく、★20人の「切」も消えます
   ★★けれど、★これは元から**歴史ではありません**。
     ★いまの状態を1つ持っていただけです。
   ★★消したものを書き出しに残すことはできません。
```

★★ただし、★1つ申し上げます。

★書き出しは「ご本人が、ご自分のものを取り出す」道です。
★★「同意した」という事実は、★ご本人のものです。

★消す前に取り出したい方がおられるかもしれません。
★★けれど、★消すことは既に決まっています（★裁定 その27／その45）。
　★★お知らせを先に送り、★間をおいてから消す、という順もあります。

★どちらにするか、★ご判断をお願いします。

## 17. ★`research.anonymized` ── 死んだ定義（★N-1違反）

`lib/consent.js:104` に定義があり、★**どこからも読まれていません**。

```
書く側  consent_records に入るのは "health.record" と "health.reflux_care" だけ
読む側  purposeByKey() を呼ぶのは2か所。どちらも別の鍵
```

★規則「★作った関数は、必ずどこかから呼ばれているか」に触れています。

★★そして、★これが文言の割れを見逃した原因です。

`lib/consent.js` のコメントは、こう書いていました ──

> ★画面に出ている文言と、一字一句そろえること（2026-09-01）。
> 　2か所に別の文言があると、どちらに同意したのか答えられなくなります。

★★その定義が、★誰にも使われていませんでした。
　★だから、3通りに割れても、★誰も気づきませんでした。★見張りもありません。

★★**字を1か所に置いても、そこが読まれていなければ、1か所にした意味がありません。**

★消します。★消した記録は、この紙が持ちます。

---

まだ消していません（★お知らせの文のご確認待ちです）。

---

# ★追記4（2026-09-15）── ★裁定の前提に、1つ 誤りがあります

★Opus の裁定「DO NOT SEND」を受けて、
★指示のとおり「書き出しが 壊れないか」を先に確かめました。

★★**書き出しは壊れません。** ★けれど、★別のことが分かりました。

## 18. ★書き出しは、同意の**文面を持っていません**

裁定には、こう書かれていました ──

> THE STRONGEST ARGUMENT FOR SENDING DOES NOT HOLD
> 「知る手段がなくなる」— it does not
>   the export already includes: 同意・お知らせ →「同意の 記録（いつ・どの 文面に）✓」
>   ★what they agreed to, and when, stays in their own hands

★**そうなっていません。** 2つの理由があります。

### ㋐ `consent_records` は、文面を持ちません。★ハッシュだけです

`supabase/migration_consent_records.sql:16-31` ──

```sql
purpose_key    text not null,
policy_version text not null,
text_hash      text not null,   -- ★★文面そのものでは ありません
locale         text not null default 'ja',
method         text not null default 'checkbox',
granted_at     timestamptz not null default now(),
withdrawn_at   timestamptz,
```

`lib/consent.js` の `buildConsentRow` も、そのとおりです ──

```js
text_hash: textHash(purpose.text),
```

★★**文面は入りません。** ★入るのは、文面から作った ハッシュです。

★書き出しても、★出てくるのは
`purpose_key` と `policy_version` と `text_hash` です。
★「何に同意したか」は、★読めません。

★★文面を持っているのは `lib/consent.js` ── ★**コードの側**です。
　★★つまり、★消すと 文面は どこにも 残りません。

### ㋑ そもそも、この同意の行が `consent_records` に ★ありません

★Sonnet が確かめてくださいました ──

```
purpose_key = "research.anonymized" … ★0件
```

★29人の同意は、★`profiles.consent_stats_use_at` にあります。
★★**時刻が1つだけ**です。★文面も、版も、方法も、ありません。

★書き出しに入るのは、★その時刻だけです（`EXPORTED_PROFILE_COLUMNS`）。

```
consent_stats_use_at: "2026-09-02T04:11:23.000Z"
```

★これを見て、★何にご同意くださったのかは分かりません。

## 19. ★だから、こう申し上げます

★裁定の**結論**（送らない）は、★残りの3つの理由だけでも立つと思います。

```
1  法21条3項は「目的の変更」のとき。★これは取りやめで、広がりません
2  何もしなくてよいお知らせは、★次の本当のお知らせを読まれなくします
3  「使われていたのか」という心配を、★こちらから作ることになります
```

★★けれど、★**4つ目の理由（書き出しで分かる）は、事実と違います。**

★「知る手段がなくなる」は、★なくなります。

★★No.027 の §1 に文面を写してあります。★いまはそれだけが残る形です。
　★★裁定の「step 1 must not be skipped」は、★まさにそのとおりでした。
　　★★あれが唯一の写しです。★書き出しではありません。

## 20. ★ご判断をお願いしたいこと

| 印 | 何 | 私の見立て |
|---|---|---|
| ㋐ | 送らない（★裁定のまま） | ★3つの理由で立ちます。私は反対しません |
| ㋑ | ★No.027 を、消す前に別の形でも残す | ★勧めます。この紙が唯一の写しです |
| ㋒ | ★`consent_records` が文面を持たないこと自体 | ★別の話ですが、★`health.record` の22件も同じです。★いつか同じ問いが来ます |

★★㋒ について一言 ──
　★いま `consent_records` を書き出しても、★読めるのはハッシュだけです。
　★「いつ・どの文面に」と札には書いてありますが、★**文面は出ていません**。
　★★これは `lib/exportData.js:363` の札の字と、★中身が食い違っています。
　★★きょう 見つけた「字はあるが、中身がない」形の、★もう1つです。

★消していません（★d_override・`consent_stats_use_at`・画面の設定）。
★ご判断をお待ちします。
