# 新しい 版を 展きました（2026-09-22・woolsong-2026-09-21.zip）

## 置いた 先

`docs/design/pack-final/`（★前の 版は `_old-2026-09-21m/` に 退避しました・51件）

| | 数 |
|---|---|
| 新しく 来た もの | 12 |
| 中身が 変わった もの | 5 |
| 同じ もの | 86 |

★新しく 来た もの ── `ruling-164` / `ruling-165` / `ruling-166` / `ruling-167`、
`FX9_試しデータ一覧の形`、`実行ルート統合版_第10版`、`引き継ぎ_Opus_2026-09-21`、
`legal/fable_T1-T7/` の 8本、`legal/法務の点検`。

★中身が 変わった もの ── 見本 3本、`S3_運営画面_門下を読む_仕様`、`tools/経路一覧.md`。

★★★展く ときの 落とし穴（★書いて おきます）──
　この zip は 名前を UTF-8 で 収めて いますが、★UTF-8 の 印（旗）が 立って いません。
　★Python の `zipfile` は 旗が 無いと cp437 と 見なします。★名前が 化けます。
　★★1度 化けた 名で 書いて しまい、★29件 消して 入れ直しました。
　★★次から …… `n.encode("cp437").decode("utf-8")` を 通してから 置きます。

## 確かめた こと（★本番を 読みました）

### 裁定165 …… ★私の お勧めと 同じ ものに 決まりました

★`evaluation_judges` を 作る（行事ごと）。★私が ㋐として お出しした 案 です。

★★★1つ、★裁定165 §2 と 本番が ちがいます ──

```
裁定165 §2  … 「alter table evaluation_judge_done add primary key (event_id, judge_id);
                 -- いま一意の制約が無い」
本番 ……… evaluation_judge_done_pkey  PRIMARY KEY (event_id, judge_id)   ★有ります
```

★`pg_constraint` で 見ました。★足すと 落ちます。★束3 の SQL では 足して いません。

### 裁定167 A1 …… ★本物 です。★Code に 尋ねられた ことを 確かめました

★お尋ね …… 「持って いる 品」を `character_inventory` で 決めて いるか、
`item_acquisitions` で 決めて いるか。

★★答え …… **`character_inventory` です**。

```
components/VocalTracker.jsx:6773
  supabase.from("character_inventory").select("item_key").eq("user_id", userId)
```

★本番の 数 …… `character_inventory` 326行 ／ `item_acquisitions` 4行。
★権限 …… authenticated に 表ごとの SELECT・INSERT・UPDATE・DELETE。

★★★つまり、★いま 入って いる 38人の 誰でも、★払わずに 有料の 品を 自分に 付けられます。
★★`item_key` を 書き換えれば、★無料の 品を 有料の 品に 変える ことも できます。

★★★閉じても 壊れません。★書く 道は **3つ とも `service_role`** です ──

| 道 | ファイル | 鍵 |
|---|---|---|
| 買う | `app/api/character/buy/route.js:125` | `createAdminClient()` |
| 贈る | `app/api/character/gift/route.js` | 同じ |
| 退会の 消し込み | `purgeAccount(admin, …)` | 同じ |

★画面からは `select("item_key")` の 1か所 だけ です。★読むのは そのまま に します。

★★裁定167 の お勧めの とおり、★束2 に 入れました。

### 裁定165 §3 …… `confirmed_at` も 本物 です

```
evaluation_scores   authenticated  INSERT / SELECT / UPDATE  (confirmed_at)
evaluation_reviews  authenticated  INSERT / SELECT / UPDATE  (confirmed_at)
```

★`confirm_event_scores` は SECURITY DEFINER で あります。★道は 1本に できます。

## 出した もの

| 束 | 中身 | 型の 紙 | 見張り |
|---|---|---|---|
| 束2 | 164 W2・W1 ／ FX2 ／ **167 A1** | `docs/decisions/2026-09-22-束2-164W2-164W1-FX2.md` | PASS |
| 束3 | 165 ／ 163 §1 ／ 164 W3 | `docs/decisions/2026-09-22-束3-165-163section1-164W3.md` | PASS |

★どちらも **試しの 台帳にも まだ 当てて いません**。
★`ask_ledger.py` の 見張りが `grant` / `revoke` / `create` に お許しを 求めます。
