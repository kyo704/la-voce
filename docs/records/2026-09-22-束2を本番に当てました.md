# 束2 を 本番に 当てました（2026-09-22）

★坂本さんの ご承認 …… 「束2_本番適用: 承認します」。

## 当てた もの

`supabase/migration_taba2_164w2_164w1_fx2.sql`

| | 裁定 | 何を 閉じたか |
|---|---|---|
| W2 | 164 | `org_messages` の update を `withdrawn_at` の 1列だけに |
| W1 | 164 | 応募者が 自分で `status` を `'chosen'` に できない |
| FX2 | 161 | `monka_read_log` に 直に insert できない |
| A1 | 167 | `character_inventory` に 自分で 品を 足せない |

## 当てる 前 → 当てた 後（★本番の `information_schema` を 読みました）

```
             前                             後
applications        DELETE                  DELETE
character_inventory DELETE INSERT SELECT UPDATE   SELECT
monka_read_log      INSERT SELECT           SELECT
org_messages        INSERT SELECT UPDATE    INSERT SELECT ＋ UPDATE(withdrawn_at)
```

★決まり（`pg_policy`）──

```
org_messages_withdraw    check ((auth.uid() = author_id) AND (withdrawn_at IS NOT NULL))
applications_update_own  check ((auth.uid() = applicant_user_id) AND (status = ANY (ARRAY['sent','withdrawn'])))
monka_read_log           ★insert の 決まりは ありません（消しました）
character_inventory      ★決まりは 触って いません。★権限の 側で 閉じました
```

## 行は 減って いません

| 表 | 前 | 後 |
|---|---|---|
| `character_inventory` | 326 | **326** |
| `org_messages` | 4 | **4** |
| `applications` | 0 | 0 |
| `monka_read_log` | 0 | 0 |
| `item_acquisitions` | 4 | 4 |

## 画面が 失う もの …… ありません

- `org_messages` を update する 呼び出しは、★リポジトリに 1つも ありません。
- `applications` を update する 呼び出しも ありません（insert だけ）。
- `monka_read_log` に 直に insert する 呼び出しも ありません。
- `character_inventory` に 書く 道は **3つ とも `service_role`** です ──
  買う（`app/api/character/buy/route.js`）／贈る（`gift`）／退会の 消し込み（`purgeAccount(admin, …)`）。
  ★画面からは `select("item_key")` の 1か所 だけ です。

## 試験

★試しの 台帳で 確かめました …… 当てる 前 **6 / 13** ／ 当てた 後 **13 / 13**。
★道具 …… `tools/taba2_prove.js`。★実在の 試しの 利用者で 入り直して 確かめました。
★なりすまし（`set role` / `set_config` / `ROLLBACK`）は 使って いません。

★★本番では 試験を 走らせて いません。★走らせると 本番に 行が 入ります。
★★本番で 確かめたのは、★決まりと 権限と 行数 です（★上の 3つの 表）。

## 戻す とき

`supabase/migration_taba2_164w2_164w1_fx2_undo.sql`
