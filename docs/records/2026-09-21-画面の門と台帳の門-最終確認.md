# ★画面の 門と 台帳の 門 ── ★最終確認（★2026-09-21）

★実行ルート 第9版 §1 に「★画面の門と台帳の門 33件／38人に影響／要確認」と
★あります。★数え直しました。

★道具 …… `tools/gate_mismatch.py`（★本番の 台帳を 読みます。★書きません）

## ★数え直した 結果

| 形 | 前（33件 と された とき） | ★いま |
|---|---|---|
| ①台帳の 読み道が 役割の 名で 判じる | — | **0件** |
| ②台帳の 決まり（policy）が 役割の 名で 判じる | — | **0件** |
| ③画面・`lib` が 役割の 名で 出し分ける | — | 13件（★下に 仕分けます） |
| ④台帳が 1度も 見て いない できこと | — | 1件（`koma_mine`） |

## ★① が 0件に なった わけ ── ★道具が 誤って いました

★きょう 走らせた とき、★道具は `create_org_event` を 挙げました。
★★本番の 中身を 直に 読みました ──

```
if not public.has_can(p_org_id, 'gyoji') then
--   ★★もと … `public.is_org_owner_or_admin(auth.uid(), p_org_id)`
```

★★★**直って います**。★挙がったのは、★直した ことを 書き残した
★**説明の 行** でした。★自分の 説明で 落ちる ── ★これで **4度目** の 形 です
（★裁定 その135）。

★★あわせて、★もっと 悪い 面が ありました。★道具は 先に 改行を 潰して
★いました。★そう すると `--` が そこから **後ろ 全部** を 飲みます。
★★★本当の 門も 見えなく なります。★見落とす 側 の 誤り です。

★直しました ── ★① 説明を 落とす ★② それから 1行に する。
★較正 …… ★当たる 例（`is_org_owner_or_admin` 自身の 定義）で 当たり、
★当たらない 例（説明の 中）で 当たらない こと を 確かめました。

★★`supabase/migration_org_events_place_target.sql` は **古い 版** です。
★★台帳の ほうが 進んで います。★紙を 台帳と 思わない こと。

## ★③ 13件の 仕分け

### ★㋐ 直しました（★1件）

| 所 | 何が ずれて いたか |
|---|---|
| `lib/orgClosure.js:357` | ★契約者を `memberships.role === "owner"` で 見て いました |

★★契約者は `organizations.contract_owner_user_id` です（★裁定 その116）。
★★決めは `lib/orgContract.js` の `isContractOwner` が 1つ 持って います。

★★★ずれて いると、★2つ とも 誤ります ──
- ★契約を 譲った あとの 元オーナーに、★要らない 知らせが 出ます。
- ★役割が owner で ない **本当の 契約者** は、★知らせを 受けません。
  ★★学校が 黙って 支払う 人を 失います。★こちらが 重い です。

★見張り `components/tests/org-closure.test.js` に 2つ 足しました ──
★「役割は admin だが 契約者」「役割は owner だが 譲った あと」。
★★片方だけ 直して 済ませない ため です。

### ★㋒ わざと（★4件・★このまま）

| 所 | わけ |
|---|---|
| `VocalTracker.jsx:15614` `ORG_OPERATOR_ROLES` | ★画面側の 歯止め。★権限の 壁では ない と 註に あります |
| `lib/orgClosure.js:277` `OPERATOR_ROLES` | ★「退会で 学校を 放り出さない 網」。★広めに 取る のが 目的 です |
| `lib/orgRoster.js:36` `NOT_COUNTED_ROLES` | ★名簿は いま `enrollments` を 読みます。★当たりません。★古い 呼び手の ため に 残して あります |
| `lib/opsShell.js:28` `ROLES` | ★呼ぶ 側が **1つも ありません**（★数えました）。★死んで います |

### ★㋓ 38人には 届いて いません（★8件）

`VocalTracker.jsx` の 21760／21809／21914／21923／28672 ── ★8件。
★★すべて `canSeeBetaFeatures(profile)` の 中 です。

```
canSeeBetaFeatures = profile.is_admin || profile.teacher_beta_access
```

★本番で 数えました …… ★人 **50**／★見える 人 **9**（★admin 1・beta 9）。
★★「★38人に 影響」は、★この 8件に ついては **当たりません**。
★★古い 教室の 画面 です。★裁定142 の あと、★仕分けの 対象 に なります。

## ★④ `koma_mine` ── ★台帳が 1度も 見て いません

★できこと 14の うち、★`koma_mine`（自分の コマを 決める）だけ が、
★台帳の 読み道にも 決まりにも 1度も 現れません。
★★`lib/opsPerms.js:65` に あり、★「教授」の 既定に 入って います
（`supabase/2026-09-11-§3-50通り-下ごしらえ.sql:66`）。

★★★つまり ── ★画面は この 門で 出し分けますが、★台帳は 見て いません。
★★画面を 通さずに 直に 書ける なら、★門は 無い のと 同じ です。

★★★**DECISION_NEEDED** ── ★台帳の 側に 門を 足すのは、
★38人が 使って いる 表の 決まりを 変える こと です。★自分で 決めません。

## ★残す 数

★直した もの 1 ／ ★わざと 4 ／ ★38人に 届いて いない 8 ／
★**人の 判断が 要る もの 1**（`koma_mine`）。
