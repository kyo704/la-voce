# 裁定162（IDOR）の調べ ── 本番と試しで結果が違います（2026-09-21）

## ORDER 1 ── 過去のログ（先に調べました）

記録の保ち期間は **1日** です（2026-09-21 に数えました）。

| 見たこと | 数 |
|---|---|
| `admin_entry_stats` の呼び出し | **1** |
| `character_unlock_summary` の呼び出し | **0** |

その1件

```
2026-09-21 09:30:25 UTC（＝18:30 JST）
POST /rest/v1/rpc/admin_entry_stats
status 200 ／ search なし（引数は本文のため、記録から UUID は読めません）
```

★`edge_logs` は本文を残しません。**引数の UUID は記録から読めません。**
「呼んだ本人と違う UUID か」は、ログからは判別できません。

## Q ── その呼び出しに心当たりはあるか

**私（Code）の試験ではありません。** 今日、私は `admin_entry_stats` を
1度も呼んでいません。私の道具はどれも anon の鍵か Management API です。

秘密鍵（service_role）で呼ばれています。`app/admin/page.js` が同じ形です
（`profiles.is_admin` を確かめてから管理の客で呼びます）。
★坂本さんが管理の画面を開かれた時刻と合うかどうかは、私には分かりません。

## ★本番と試しで、結果が違います（★ここが要点です）

### 渡し（grant）

| | `admin_entry_stats` | `character_unlock_summary` |
|---|---|---|
| **本番** | postgres ／ service_role のみ | postgres ／ service_role のみ |
| **試し** | PUBLIC ／ anon ／ authenticated ／ postgres ／ service_role | 同じ |

### 実際に押して確かめました

**本番**（読むだけ）

| 誰 | 何をした | 結果 |
|---|---|---|
| 入らずに（anon） | 他人の UUID で `admin_entry_stats` | **断られた** 42501 permission denied |
| 入って（E2E_TEACHER） | 他人の UUID で `admin_entry_stats` | **断られた** 42501 |
| 入って | 他人の UUID で `character_unlock_summary` | **断られた** 42501 |

**試し**

| 誰 | 何をした | 結果 |
|---|---|---|
| 入らずに（anon） | 他人の UUID で `admin_entry_stats` | ★**通った** |
| 入って | 他人の UUID で `admin_entry_stats` | ★**通った** |
| 入って | 他人の UUID で `character_unlock_summary` | ★**通った**（中身が返りました） |

試しで返った中身

```
{"fieldKinds":20,"performances":0,"hasPianissimo":false}
```

★よその方の記録の数です。★**試しの台帳では、IDOR が成り立ちます。**

## 分かったこと

| | |
|---|---|
| 本番 | ★いま ふつうの利用者からは **呼べません**。渡しが service_role だけです |
| 試し | ★入らなくても 呼べます。よその UUID で 中身が返ります |
| 過去 | ★判別できません。記録に引数が残らず、保ち期間も1日です |

★「本番に穴がある」とは書きません。★「試しに穴があり、本番では渡しが閉じている」です。

★ただし、引数で人を指せる形そのものは残っています。
渡しを1つ足した日に、そのまま穴になります。
裁定162 の「引数を無くして `auth.uid()` を中で使う」は、その日を来させない直し方です。

## まだしていないこと

- 2つの関数の直し（型で出してから）
- `get_timetable_submitted` に `STABLE`（急ぎではない）
- 試しの渡しを本番に合わせるか、「試しは緩くてよい」と決めるか（★坂本さんのご判断）
