# 束0 と 束1 を 本番に 当てました（2026-09-22）

★坂本さんの ご承認 …… 「束0・束1の本番適用: 承認します」。

## 当てる 前に 確かめた こと

★★★FX6（`can_view_organization` の 枝を 消す）だけ、★人が 読めなく なる 話 です。
　★当てる 前に、★組織を 読む 道を **ぜんぶ** 見ました ──

| どこ | 何を 読むか | 影響 |
|---|---|---|
| `VocalTracker.jsx:12461` | 契約者（自分の 学校） | ★無し（居る 学校） |
| `VocalTracker.jsx:13308・13316` | 作りかけの 教室（`created_by` が 自分） | ★無し |
| `VocalTracker.jsx:14033` | 自分が 作った 教室 | ★無し |
| `VocalTracker.jsx:14186` | 在籍して いる 学校の 名 | ★無し（在籍の 枝が 残る） |
| `app/api/org/invitation/lookup/route.js` | 合言葉から 学校の 名 | ★無し（`service_role`） |
| `get_invitation_teacher`（RPC） | 合言葉から 先生 | ★無し（`SECURITY DEFINER`） |

★★本番の「使われて いない・切れて いない 招待」…… **0件**。
★★★つまり、★この 枝で いま 見えて いる 学校は ありません。★読めなく なる 人は 0人 です。

## 束0 ── 当てた あと（★本番の `pg_constraint` を 読みました）

```
lessons              student_id / teacher_id / created_by / attendance_by   SET NULL
organizations        created_by                                            SET NULL
                     contract_owner_user_id                                NO ACTION  ★わざと
org_invitations      used_by / invited_by                                  SET NULL
roster_drafts        linked_user_id / imported_by                          SET NULL
lesson_presets       created_by                                            SET NULL
teacher_invitations  used_by_student_id                                    SET NULL
evaluation_scores    judge_id                                              SET NULL
                     student_id                                            CASCADE    ★わざと
evaluation_reviews   judge_id                                              SET NULL
org_messages         author_id / teacher_id                                SET NULL
monka_read_log       viewer_user_id / target_monka_id                      SET NULL
post_change_log      target_user_id                                        SET NULL
score_log            editor_user_id                                        SET NULL
export_log           user_id                                               CASCADE    ★触って いません
```

★足した 列 …… `judge_name_at`（2表）／`author_name_at`・`teacher_name_at`／`target_name_at`。

## 束1 ── 当てた あと

```
org_post_perm_log   authenticated  SELECT だけ
org_message_drafts  authenticated  SELECT / INSERT / UPDATE / DELETE
are_connected           authenticated だけ（★anon を 外しました）
is_org_member           authenticated だけ
can_view_organization   authenticated だけ・★招待の 枝を 消しました
is_org_owner_or_admin   ★関数ごと ありません
_a13_policy_backup      ★表ごと ありません（★12行は ファイルに 写しました）
```

## 行は 減って いません

| 表 | 前 | 後 |
|---|---|---|
| `org_messages` | 4 | **4** |
| `lessons` | 11 | **11** |
| `organizations` | 19 | **19** |
| `teacher_student_links` | 11 | **11** |

## ★まだ して いない こと

★★退会の 試験（裁定168 の VERIFY）。★試しの 利用者を 1人 消します。★取り消せません。
★★束1 の VERIFY 5件（`anon` の 鍵で 叩く・招待を 出した 学校を 読む）。

★★★移行の 記録。★`schema_migrations` は 31本の まま です。
　★これで **6本** が 記録の 外に あります ── S1・S2・裁定160・束2・**束0**・**束1**。
