# 訂正 ── lessons の決まりを、紙だけで結びました
全79行 / 末尾は「3つとも読めます。STEP 1 のデータは揃っています。」
PLACEHOLDER

- 日付 … 2026-09-15
- 誤った報告 … `docs/reports/2026-09-15-台帳の決まりとadmin_entry_statsの鍵.md` §5
- 指摘 … 坂本さん（Sonnet が本番の `pg_policy` へ直接照会）

## 1. 何を誤ったか

§5 に、こう書きました。

> `lessons` の決まりを全部並べると、★**SELECT の決まりが1つ**しかありません。
> （…）★つまり紙のうえでは、★教室に属さないレッスン（`org_id` が null）は、
> ★**生徒も先生も読めません**。

**逆でした。**

本番の `pg_policy` には、紙に無い SELECT の決まりがもう1つあります。

```
"Teacher and student can view lessons"
  teacher_student_links 経由 ／ org_id の条件なし
```

生徒ははじめから自分の行を読めていました。
SQL エディタで直に打たれたもので、`supabase/*.sql` のどこにもありません。

## 2. なぜ起きたか

道具の不具合ではありません。**紙は台帳ではない**、というだけです。

同じ形を、同じ日に2度踏んでいます。

- `admin_entry_stats` … 紙が流れているか、紙からは分かりませんでした
- `lessons` … 紙に無い決まりが、台帳にありました

`tools/rls_for_tables.py` は、自分の検算で**別の**盲点を1つ見つけていました
（`execute format('create policy …')` で作る紙。表の名前が変数で読めない）。
そして私は、それを「台帳への問いが必ず要る理由」として書き、
★それでも問いの答えを待たずに結びを書きました。

★但し書きは付いていました。付いていても、誤った結びは旅をしました。

## 3. これから

坂本さんのお決め（2026-09-15）──

> 紙ベースの調査は、今後「台帳に直接照会して裏を取る」工程を必ず挟んでください
> （Sonnet が担当可能）。

`tools/rls_for_tables.py` を直しました。

- 頭に「★これは**下書き**です。答えではありません」を出します。
  ★末尾ではなく頭です。末尾の但し書きは読み飛ばされます。
- その場に、今回の実例（`"Teacher and student can view lessons"`）を書いています。
- 「下の問いで裏を取るまで、ここから結びを書かないでください」と言います。

★但し書きを付けて出す、では足りません。裏が取れるまで、結びを書かないことです。

## 4. 報告そのものは直していません

`docs/reports/2026-09-15-台帳の決まりとadmin_entry_statsの鍵.md` は、そのままです。

報告は、ある時点の記録であって、生きた文書ではありません。
書いたときには、そう見えていました。あとから直すと、
「いつ・何を根拠にそう思ったか」が追えなくなります。

訂正は、この紙が持ちます。

## 5. STEP 1 の、正しい答え

| 表 | 生徒が自分の行を読めるか |
|---|---|
| `org_events` | 読めます |
| `org_messages` | 読めます |
| `lessons` | ★**読めます**（`org_id` が null でも。`teacher_student_links` 経由の決まりがあります） |

3つとも読めます。STEP 1 のデータは揃っています。
