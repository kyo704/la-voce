# S3 §5 ── 実機で 通し、★不具合を 2つ 見つけました（2026-09-22）

★道具 …… `tools/s3_monka_read_shot.py`（Python・Playwright）
★結果 …… **22 / 22**（★直した あと）

---

## ★★★見つけた 不具合 ①── 入口が ありません でした

★`monka_read` を 持つ 事務の 方の 画面に、★**よその 門下の 行が 出ません** でした。

★出どころ …… `components/VocalTracker.jsx` の `fetchRenrakuStudios`

```js
rows.forEach((r) => {
  if (r.student_id === userId || r.teacher_id === userId) mine.set(r.teacher_id, r.org_id);
});
```

★★自分が **学生 か 先生** の 門下 だけ を 並べて いました。
★★`monka_read` を 持つ 方は、★どちらでも ありません。★1行も 出ません。

★★★つまり ──
　★S2（`open_monka_thread`・fail closed の 道）も
　★S3（理由を たずねる 画面）も 作った のに、★**押す ところが ありません** でした。

★仕様シート §2 は こう 書いて います ──
「入口: 連絡の 一覧の『門下』の 行。★`monka_read` を 持ち、
　★かつ その 門下の 先生で ない ときだけ、この 流れ」。
★★行は **出る** 前提 です。

### 直し

★`monka_read` を 持つ 方には、★その 学校の 門下を ぜんぶ 並べます。
★★★尋ねる 先は **台帳** です（`has_can`）。★画面で 判じません。
　★画面の 門と 台帳の 門が ずれない ように（★台帳 08-1）。
★★聞けなかった ときは 広げません。★閉じる 側に 倒します。
★★出すのは 名前と 最終更新 だけ です。★中身は 引きません。

★決めは `lib/renraku.js` の `mayListAllStudios(perms)` が 1つ 持ちます。

---

## ★★★見つけた 不具合 ②── ご自分の 門下が 開けません でした

★運営の 画面で **ご自分の** 門下を 押すと、★**何も 起きません** でした。

★出どころ …… `components/VocalTracker.jsx` の `onOpenStudio`

```js
onOpenStudio={(tid, kind, note) => {
  if (!tid) { setOpenStudio(null); return; }
  void (async () => {
    const ok = await openMonkaThread(opsOrgId, tid, kind, note);   // ★ぜんぶ 道を 通す
    if (!ok) return;                                               // ★断られたら 何も しない
    …
```

★★ご自分の 門下は 理由を 聞かれません（★`needsReadReason` が false）。
★★だから `kind` が 空の まま 道を 呼び、★道が `REASON_REQUIRED` で 断ります。
★★`if (!ok) return` で、★**黙って 何も 起きません** でした。

★仕様シート §5 ⑥ …… 「先生 ご本人が 自分の 門下を 押す → 理由を 聞かれない（今のまま直接読める）」。

### 直し

```js
if (!needsReadReason({ perms: gate, teacherId: tid, meId: userId })) {
  setOpenStudio(tid);
  void fetchRenraku(opsOrgId, tid);   // ★いままで どおり そのまま 読みます
  return;
}
```

★決めは `lib/renraku.js` の `needsReadReason` が 1つ 持ちます。★画面で 判じません。

---

## ★★どちらも、★見張り（442本）では 見つかりません でした

★字も 処理も、★見張りの 目には 正しく 見えて いました。
★★**押して みて** はじめて 分かりました。

★★★これが「見本の HTML では 測れない」の 中身 です。

---

## 通した 22件

| | 見た こと | |
|---|---|---|
| ① | よその 門下を 押す → ★中身が 出ない・理由を 聞かれる | ○ |
| ② | 理由を 選ばずに「開く」→ ★「理由を 選んでください」・記録 0 のまま | ○ |
| ③ | 「その他」で 空欄の まま「開く」→ ★記録 0 のまま | ○ |
| ④ | 「事故・苦情の 調べ」で 開く → ★記録が **1行 増え**、★理由は `jiko` | ○ |
| ⑤ | 別の 行へ 行って 戻る → ★また 理由を 聞かれる。★開くと **もう 1行** | ○ |
| ⑥ | ご自分の 門下 → ★理由を 聞かれない・★記録は 増えない | ○ |
| ⑦ | 「ご本人にも 伝わります」「先生と 学生からも」が **0件** | ○ |

★画像 …… `docs/design/compare/renraku/monka-riyuu-390.png` ／ `monka-hiraita-390.png`

---

## ★道具を 書く ときに つまずいた ところ

1. ★`get_by_text("開く")` は、★「門下を 開いた 記録」にも 当たります。
   ★★札（button）を **名前 ちょうど** で 押す 形に しました。
2. ★「‹ もどる」が 2つ あります。★上は 運営から 出る もの、★下は 一覧へ 戻る もの。
   ★★上を 押して いて、★戻れて いません でした。★**下** を 押します。
3. ★★★1度、★**嘘の PASS** を 出しました。
   ★「理由を 聞かれない」を、★行を 押せて いない まま 通して いました。
   ★★「押せた か」を 先に 見る 形に 直しました。★13/18 に 落ちて、★本当の 姿が 出ました。
