# sql/14（直した もの）を 本番に 入れました（2026-09-23）

★坂本さんの ご承認 …… 「SQL_14_PRODUCTION_APPLY: 承認します」。

## 本番の 姿（★当てた あとに 読みました）

```
表 …… ops_audit_log            あります
列 …… org_name_at              あります
★外部キー ops_audit_log_org_id_fkey …… **0**   ← ★事故の 元。★もう ありません
引き金 …… 8つ（enrollments / lesson_rounds / memberships / org_billing /
              org_events / org_invitations / org_places / org_posts）
権限 …… authenticated は SELECT だけ
```

★移行の 記録 …… `opus_14_ops_audit_log_fixed`（★51本目）。

## ★確かめの 範囲を 正直に 書きます

★★★本番で 確かめたのは **形** です ── ★外部キーが 無い こと、★引き金が 8つ ある こと。
★★★**本番で 学校を 閉じて みて いません。**
　★わけ …… ★試しの 学校を 本番に 作らない、と 決めて います（CLAUDE.md）。
　★FX9 で 消した ばかり の ものを、★また 作る ことに なります。

★★動きの 確かめは **試しの 台帳** で 取りました …… `tools/close_org_prove.js` **6 / 6**。
　★行事・役職・場所・ご請求・名簿・在籍 を 持つ 学校を 作り、★最後まで 閉じられました。
　★★試しと 本番は、★この 表に ついては 同じ 形 です（★上の 読み直しで 確かめました）。

## 90日で 消す 道を 作りました

| | |
|---|---|
| 道 | `app/api/cron/purge-audit`（★毎日 16:00 UTC ＝ 翌 1:00 JST） |
| 中身 | 台帳の `purge_ops_audit_log()` を 呼ぶ だけ |
| 合言葉 | `CRON_SECRET`。★無ければ 503（★他の 定期処理と 同じ） |
| 決め | `lib/opsAudit.js` …… `OPS_AUDIT_RETENTION_DAYS = 90` |
| 見張り | `components/tests/ops-audit.test.js` …… **13 / 13** |

★★★道の 中で `delete` を 書いて いません。★日数が 2か所に 分かれます。
　★見張りは、★**台帳の 関数の 中の 日数**（`interval '90 days'`）と
　　`lib` の 数が 同じ かを、★毎回 数え直します。★写しません。

★★★消して よい 記録 と、★消しては いけない 記録 を 名前で 分けました。

```
消して よい …… ops_audit_log（90日）
★消しては いけない …… monka_read_log ／ org_post_perm_log ／
                        score_log ／ koen_kid_contact_reads
```

★★わけ …… ★「記録を 消す 仕組み」を 1つ 作ると、
　★次に 誰かが「同じ 仕組みで あれも」と 言います。★名前で 止めます。
