# 本番に当てた工事の一覧（2026-09-21）

★Opus から「`list_migrations` に S1・S2 が見当たらない」というお尋ねがありました。
その答えと、当てたものの一覧です。

## Q1 ── S2 は本番に当たっていますか

**当たっています。** 坂本さんのご承認（`S2_PRODUCTION_APPLY_APPROVED`）を
いただいてから当てました。いま数えた結果です。

| 見たこと | 数 |
|---|---|
| `open_monka_thread` | 1 |
| `monka_read_log` の列 | 11 |
| `monka_read_log_reason_kind_check` | 1 |
| `org_messages_select` に `monka_read` の枝（S1 で外したもの） | **無し** |

## Q2 ── なぜ `list_migrations` に出ないのですか

**`apply_migration` を使っていないからです。**

この品の工事は `tools/ask_ledger.py` から当てています。
この道具は Management API の `/v1/projects/{ref}/database/query` を叩きます。
**SQL をそのまま投げる口**です。`supabase_migrations.schema_migrations` に
行を足しません。

数えました。

| 見たこと | 数 |
|---|---|
| `supabase_migrations.schema_migrations` の行 | 31 |
| いちばん古い | 20260914042439 |
| いちばん新しい | 20260918132651 |
| リポジトリの `supabase/*.sql` | **294** |

★31行が覆っているのは 2026-09-14 から 09-18 までの4日間だけです。
それ以外の 260本あまりは、この表に1行もありません。
**この表は、この品の工事の記録ではありません。**

CLAUDE.md にこう書いてあります。

> Migrations are written out as files for the owner to paste into the SQL Editor
> …… **Do not treat `schema.sql` as the source of truth**

つまり、この品の記録は **リポジトリの `supabase/*.sql` と `docs/records/`** です。

★「`list_migrations` に無い」＝「当たっていない」ではありません。
★けれど、そう読めてしまうのは確かです。だからこの紙を書きました。

## Q3 ── 紙はリポジトリに写っていますか

写っています。3本とも push 済みです。

| 工事 | 紙 | 当てた先 |
|---|---|---|
| S1（門下を読む道を閉じる） | `supabase/migration_monka_read_close.sql` | test・本番 |
| S2（記録と開く道） | `supabase/migration_monka_read_log_s2.sql` | test・本番 |
| 裁定160（役職の履歴） | `supabase/migration_org_post_perm_log.sql` | test・本番 |

記録も同じ日付で残しています。

- `docs/records/2026-09-21-STEP0-門下を読む道を閉じました.md`
- `docs/records/2026-09-21-S2を本番に当てました.md`
- `docs/records/2026-09-21-S1の証明.md`
- `docs/records/2026-09-21-STEP2-過去の読み取りを調べました.md`

## 裁定160 を当てました（2026-09-21）

| 見たこと | 当てる前 | 当てたあと |
|---|---|---|
| `org_post_perm_log` | 0 | 1 |
| `log_org_post_perm` | 0 | 1 |
| `org_posts` の引き金 | 0 | 1 |
| 決まりの数 | ─ | 1（select だけ） |
| insert・update・delete の決まり | ─ | **0** |
| 表の行 | ─ | 0 |
| 関数の渡し先 | ─ | postgres と service_role のみ（anon・authenticated には渡していません） |

★**履歴の無い期間** …… 2026-09-11（役職を作った日）〜 2026-09-21（この工事を当てた日）。

## ★これから

本番に当てたら、この紙に1行足します。
`list_migrations` を見る方が同じ誤解をしないためです。
