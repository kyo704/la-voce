# 修正の記録 No.026 ── 生徒の教室の殻（3つの節）
全145行 / 末尾は「実機でお確かめください。」

- 日付 … 2026-09-15
- 出どころ … `docs/opus/未決機能の設計書_第3版_2026-09-14.md` §6
- 裁定 … Opus「次のレッスン → 近い行事 → 先生からの連絡。read-only」
- 裁定 … 坂本さん「「次のレッスン」は両方拾う」

## 1. 作ったもの

「きょう」の中に、3つの節を足しました。

```
きょう
  ├ ★次の レッスン       ← 足した
  ├ ★近い 行事           ← 足した
  ├ ★先生からの 連絡     ← 足した
  └ 今日の記録            ← もとから
```

- ★新しいタブを作っていません。`/house` も作っていません
- ★読むだけです。押せるものを1つも置いていません
- ★門の中の方だけに出ます。38名の画面は変わりません
- ★どの節も、1件も無ければ**節ごと出しません**
- ★取れなかったときは黙らず「いま 読めませんでした」と書きます

## 2. ★次のレッスン ── 両方拾う

裁定のとおり両方拾いました。調べると、**これ以外に正解がありません**でした。

`lessons` に生徒が届く道は2つあります。

| 決まり | 当たり方 |
|---|---|
| `Ops-visible lessons (org-based)` | `org_id is not null` かつ `auth.uid() = student_id` |
| `Teacher and student can view lessons` | `teacher_student_links` 経由（`link_id`）。org_id の条件なし |

★個人指導のレッスンは `student_id` を**持ちません**。

```js
.insert({ link_id: linkId, scheduled_at: scheduledAt, note: newLessonNote, created_by: userId })
```

`.eq("student_id", userId)` だけで引くと、個人指導の方に**1件も出ません**。
空の画面になり、★エラーも出ません。誰も気づきません。

★見張りが、この前提そのものも毎回確かめます。
将来 `student_id` も入れるようになったら落ちます。落ちて正しいです。

## 3. ★近い行事 ── 日づけのまま、出欠は集めない

裁定（2026-09-15・行事と時間割）のとおりです。

- ★`org_event_participants` に触れていません（no RSVP）
- ★添える1行「行事の 出欠は 集めません。知らせるだけです。」（見本のまま）
- ★日づけは日づけのまま。「あと◯日」と書いていません（no countdown）
- ★曜日×コマの型紙に重ねていません。日づけの並びです
  - `lib/myTimetable.js` は曜日×コマの型紙で、毎週繰り返します
  - 日づけを持つものをそこに重ねると、1度きりの行事が毎週出ます
- ★`previous_date` は「前はいつ」で、繰り返しではありません
  - 変わったときは「9月18日 から 変わりました」と言います
  - ★誰のせいとも言いません。事実だけです
- ★取り下げたもの（`withdrawn_at`）は出しません。行は消えていません

★`org_events` に繰り返しの列が無いことは、本番の列で確かめてあります（11列）。

## 4. ★先生からの連絡 ── v1では既読を書かない

★`org_message_reads` に**触れていません**。

★なぜか ── 「読んだか」を集め始めると、
★「読んでいない人」を数えられるようになります。
この家に、そういうものは置きません。

- ★「未読 2件」と数えません
- ★取り消されたもの（`withdrawn_at`）は出しません
  - 行は残っています（見本②「静かに 1行 残ります」）
  - ★v1 では、その1行をまだ出しません。読むだけの節だからです
- 新しい順に3つまで

## 5. ★`select("*")` にしていません

RLS は行を隠しますが、★列は隠しません。
`select("*")` は、出さない列も通信に載せます。

- `teacher_note` … 先生がご自分のために書いたもの。引いていません
- `student_notice` … この節では使いません。引いていません

★絞りも `.eq(...)` で書いていません。RLS に任せています。
書くと、いくつもの教室に通う方の分が落ちます。

## 6. ★見張りに止められたもの（★どれも本物）

| 何 | どう止まったか |
|---|---|
| `useEffect` を `layoutV2` の宣言より前に置いた | ★`no-tdz`。★lint も build も通っていた |
| 記録の器の中に節を置いた | `record-leak` |
| ★自分のコメントに器の名前を書き、器の頭が早まった | `record-leak` |
| ★ファイル全体を見て、関わりのないコードを落とした（★3度） | 較正 |
| ★目分量の窓（1400字）で切り出した | 較正 |
| ★`margin: i ? "8px 0 0",` と書いた（`:` が無い） | `next build` |

★3度目の「ファイル全体を見た」は、見張りの中にその旨を書き残しました。

## 7. 見張り

`components/tests/classroom-shell.test.js`（66本）。

1. レッスンを2つの道で引いていること
2. ★「個人指導は `student_id` を持たない」── 前提の確かめ
3. `select("*")` にしていないこと（★殻の中の全 select）
4. v1 では既読を書かないこと
5. 門の中だけに出していること
6. 出さないものを引いていないこと（N-1）
6-2. 近い行事（no RSVP／no countdown／曜日×コマに重ねない）
6-3. 先生からの連絡（★既読を書かない／未読と数えない）
7. ★実際に動かしてみる（レッスン・行事・連絡）
8. 1件も無ければ節ごと出さないこと

### 較正（★わざと壊しました）

| 壊しかた | 落ちた本数 |
|---|---|
| 個人指導の道を落とす | 5 |
| `select("*")` に戻す | 2 |
| 門の外にも出す | 1 |
| 済んだもの（held）も出す | 1 |
| `teacher_note` を引く | 1 |
| 個人指導にも `student_id` を入れる | 1 |
| 出欠の表に触る | 4 |
| 「あと◯日」と書く | 3 |
| 取り下げた行事も出す | 1 |
| 既読を書く | 2 |
| 取り消された連絡も出す | 2 |
| 「未読」と数える | 1 |
| 1件も無くても節を出す（各節） | 各1 |

## 8. たしかめ

- `npm run lint` … エラーなし
- `npm run build` … ✓ Compiled successfully
- 見張り … 通り307／落ち10（落ちた10本は、もとから落ちているものです）

コードに入れました。見張りは通りました。見た目は未確認です。
実機でお確かめください。
