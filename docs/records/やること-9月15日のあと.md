# ★9月15日の あとに する こと

★この 紙は 手で 書いて います（★控え）。

---

## ① AGENTS.md（★帳面の 根に 作る／既存の 決まりの 紙に 混ぜる）

★出どころ 坂本さん（2026-09-13）。★今日 くり返した まちがいを 止める ため。

★書く 中身
- 技術の 土台（★外の 下書きを 受け取る 前に 必ず 見る）
  - 言語 … plain JavaScript／JSX（★TypeScript では ない）
  - 見張り … 自作（`node components/tests/*.test.js` を 1本ずつ）
    ★Jest／Vitest の 形（describe／it／expect）は 動かない
  - Supabase … `@/lib/supabase/client` の `createClient()`
    ★`./supabaseClient` では ない
  - 配る 道 … Next.js App Router ＋ Vercel の Git 連携
    ★GitHub Actions 経由は 廃止済み
  - 生徒は enrollments、★職員は memberships（★別の 表）
  - 権限は `has_can(org_id, key)` が 正。
    ★`memberships.role`／`is_org_owner_or_admin` は 移行の 対象（§6-3）
- 外の 下書きを 受け取ったら
  1. 参照して いる ファイル名が 実在するか 確かめてから 着手
  2. 土台が ちがえば、★指示文の 言語を 優先して 直す
  3. すでに ある もの（★lib/exportData.js など）と 重なって いないか

## ② ARCHITECTURE.md（★帳面の 根に 作る）

- 権限の 2系統（★混同 注意）
  - 新 … `has_can(org_id, key)`。★org_posts.perms を 見る。★正は こちら
  - 旧 … `role IN ('owner','admin')`／`is_org_owner_or_admin`。★移行中
    ★一覧 docs/reports/2026-09-13-role-dependent-policies.md
- 生徒と 職員は 別の 表
  - 生徒 … enrollments（★status は active／left の 2値。★休会・招待中は 無い）
  - 職員 … memberships（★role は owner／admin／teacher／staff の 4値・NOT NULL）
  - 教員の 受け持ち … assignments
- 運営モードの 罠
  - OpsShell へ 直に 入ると `orgMembers` が 空に なり得る
    （★`fetchOrgDetail` は「教室の詳細」経由でしか 呼ばれない）
  - タブは `permsOfMember(opsMembership, ...)` を 通る。
    ★`opsMembership` が null だと 役割（role）に 落ちる

## ③ 外の 下書きを 先に 見る 道具

★出どころ 坂本さん（2026-09-13）。★中身の 案も いただいて います。
★見る もの … TypeScript の 書き方／Jest・Vitest の 書き方／
　`./supabaseClient` の 取り込み／★実在しない ファイル名

★★お決めが 要ります ── ★言語
- 頂いた 案は JavaScript（`tools/check_external_draft.js`）
- 2026-09-13 の お決めでは「★道具は Python・字下げ 2つ」
- ㋐ Python に 直す（`tools/check_external_draft.py`）★私の 推し
- ㋑ 指示の まま JavaScript

---

★★どれも 急ぎません。★9月15日の ものが 済んでから。
