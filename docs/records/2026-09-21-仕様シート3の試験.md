# 仕様シート③「役職を変えた記録」は守られています（2026-09-21）

裁定160 ALSO「仕様シート③が実装で本当に書かれているかを、試験で確かめる」。

★コードを読むだけで済ませていません。実際に役職を渡して、行が増えるかを見ました。

## 結果 ── 10 / 10

道具 `tools/spec3_prove.js`。実在の試しの利用者で、画面と同じ道（`/api/org/posts`）を通ります。

```
たろう（post あり）: 役職を渡す        → 期待 HTTP 200 → 結果 200
たろう: post_change_log の増え         → 期待 1行 → 結果 1行（0 → 1）
たろう: 誰が（changed_by）             → 期待 1行以上 → 結果 1行
たろう: 誰を（target_user_id）         → 期待 1行以上 → 結果 1行
たろう: いつ（changed_at）             → 期待 1行以上 → 結果 1行
たろう: 役職の名（to_post_name）       → 期待 1行以上 → 結果 1行
たろう: 役職を外す                     → 期待 HTTP 200 → 結果 200
たろう: もう1行                        → 期待 1行 → 結果 1行（1 → 2）
たろう: 何から外したか（from_post_name）→ 期待 1行以上 → 結果 1行
較正: 触らなければ増えない             → 期待 0行の増え → 結果 0行
```

## 裁定159 との違い（★書き分けます）

| | 門下を読む（`monka_read_log`） | 役職を変える（`post_change_log`） |
|---|---|---|
| 書く処理 | ★**無かった**（2026-09-21 に作った） | ★**もとからある**（`app/api/org/posts/route.js:72`） |
| 順序 | 記録が先。書けなければ返さない（fail closed） | 変更が先。記録は best effort |
| 書けないとき | 中身を返さない | 変更は止めない。ただし誤りは必ず書き出す |

★順序の違いは、源の註に理由が書いてあります。
「止めると、記録の都合で人の仕事が止まります」。
読むのは調べるためなので記録が先、変えるのは仕事なので変更が先、という書き分けです。

★**同じ型の穴ではありませんでした。**

## 途中で分かったこと

1回目 …… HTTP 500。手元に `SUPABASE_SERVICE_ROLE_KEY` がありませんでした。
2回目 …… HTTP 403。試しの学部長に `post`（ひとの役職を変える）がありませんでした。
3回目 …… HTTP 400。道具が `targetUser` で送っていました。道は `userId` です。
4回目 …… 10 / 10。

★3回とも、私の側の誤りでした。実装の欠けではありません。
★**500 を見た時点で「守られていない」と書かなかったのは正しかった**です。

## おまけ ── 裁定160 の引き金が、私の仕込みを捕まえました

仕込みで学部長に `post` を足したとき、`org_post_perm_log` に1行残りました。

```
op: update ／ post_name_at: 学部長 ／ added: ['post'] ／ removed: []
changed_by_kind: system
```

★画面を通していない変更（台帳に直に投げた `update`）も残ります。
★`auth.uid()` が無いので `system` と記されました。書き分けが効いています。

仕込みは戻しました。試しの台帳は原状です。
