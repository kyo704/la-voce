# 束2b ① を 本番に 当てました（2026-09-22）

★坂本さんの ご承認 …… 「束2b①_本番適用: 承認します」。

## 当てた あと（★本番の `pg_get_functiondef` を 読みました）

★`profiles_guard_server_only_columns` の 守る 列 …… **13列**。

```
is_admin / is_tester / cohort / teacher_beta_access /
deleted_at / reauth_at / is_internal / character_points_spent
  ★ここまで 8列 …… もとから
consent_health_data_at / reflux_care_consent_at /
guardian_consent_declared_at / is_under_18 / created_at
  ★ここから 5列 …… 2026-09-22 に 足しました
```

## ★危うく 誤って 報告する ところでした

★読み直す とき、★こう 書きました ──

```python
守 = re.findall(r"guarded := '([a-z_]+)'", src)
```

★★`is_under_18` は **数字** を 含みます。★`[a-z_]+` は 数字を 拾いません。
★★だから **12列** と 出ました。★「1列 入って いない」と 読める 数 です。

★★★数字を 拾う 形に 直して、★13列 だと 分かりました。

★★道具が 数え落として いる のか、★台帳が 入れ落として いる のか。
　★この 2つを 見分けずに 報告すると、★無い 不具合を 追う ことに なります。

## 影響

★足した 5列を 画面から 書く ところ …… **0か所**（★当てる 前に 数えました）。
★★できなく なる 人は 0人 です。

## まだ して いない こと

★`line_user_id` ／ `line_link_code` ／ `consent_health_data_withdrawn_at` ／ `consent_stats_use_at`。
★★この 4列は、★いま 画面が 書いて います。★先に サーバの 道を 作ります。
