# 「審査員として組まれている」── 正しい表・列は何か（2026-09-22）

★裁定163 §1 と 裁定164 W3 が、両方とも この 1点で 止まって います。
★本番を 読んで 確かめました。

## 一 ── 答え

★★**その表は ありません。** ★作る ことに なります。

探した ところ ──

| 探した もの | 結果 |
|---|---|
| 名前に `judge` を 含む 表 | `evaluation_judge_done` だけ |
| 名前に `saiten` を 含む 表 | ありません |
| 審査員を 並べた 列を 持つ 表 | ありません |

★`evaluation_judge_done` は **終えた 印** です。
　列 …… `org_id` / `event_id` / `judge_id` / `done_at`。
　決まりは `evaluation_judge_done_own`（ALL・`judge_id = auth.uid()`）。
　★★つまり **自分で 押せます**。★これを 割り当ての 証しに 使うと、
　　自分で 押して 自分を 審査員に できます。★裁定163 §1 の 案の ままでは 穴が 残ります。

## 二 ── いま 画面は どう 決めているか

`components/VocalTracker.jsx:17423`

```js
onGoSaiten={(canOps(gate, "saiten")
  || (orgAssignments[opsOrgId] || [])
    .some((a) => a && a.teacher_id === userId && !a.ended_at))
  ? … : undefined}
```

★**学校ごと** です。★**行事ごとでは ありません。**
　① その学校で `saiten`（採点）の できことを 持つ
　② その学校で 門下を 持つ 先生（`assignments.teacher_id` が 自分・`ended_at is null`）

★②が 要る わけ …… 学長・事務長に 門下は ありません。けれど 審査は します。

## 三 ── けれど 見本は 行事ごとに 決めて います

`docs/design/pack-final/00-動く見本-PC・iPad（運営）.html:3470-3472`

```
'<div class="card"><div class="h3">審査員</div>'
+(g.ju||[]).map(…)          ★その行事の 審査員の 一覧
+'<span class="btn g sm">＋ 審査員を 足す</span>'
```

★★見本には 「その行事の 審査員」を 足す 場所が あります。
★★いまの 台帳には、それを 入れる 場所が ありません。

★★★つまり ── **見本は 行事ごと。台帳は 学校ごと。画面は 学校ごと。**
　この 3つが ずれて います。

## 四 ── 本番の 数（2026-09-22）

| 見た こと | 数 |
|---|---|
| `evaluation_scores` | 0 |
| `evaluation_reviews` | 0 |
| `evaluation_items` | 0 |
| `evaluation_judge_done` | 0 |
| `saiten` を 持つ 人 | 0 |
| いきている `assignments` | 21 |
| `org_events` | 54 |

★★★採点は、本番で **一度も 使われて いません**。
★★だから ここを かたく 閉じても、★いま 困る 人は 0人 です。
　★（21人の 先生は 採点の 画面に 入れますが、★項目が 0 なので 点は 入れられません）

## 五 ── ★決めて いただく こと

★★私は 決めません。★2つ 出します。

### ㋐ 行事ごとの 表を 作る（★見本の とおり）

```
evaluation_judges
  org_id / event_id / judge_id / added_by / added_at
  主キー (event_id, judge_id)
  入れられるのは has_can(org_id,'saiten') を 持つ 人だけ
```

★`scores` / `reviews` / `judge_done` の 3つが、この 1つの 表を 見ます。
★★裁定164 が 言う「3つで 共通に 使う」は、これで 叶います。

　よい こと …… 見本と 合います。よその 行事に 点を 入れられません。
　★★困る こと …… 「＋ 審査員を 足す」の 画面が まだ ありません。
　　　作る まで、★誰も 点を 入れられません（★いまも 0件 なので、失う ものは ありません）。

### ㋑ 学校ごとで 閉じる（★画面と 同じ）

```
has_can(org_id,'saiten')
  or exists (assignments … teacher_id = auth.uid() and ended_at is null)
```

　よい こと …… 画面を 足さずに、いま 開いて いる 穴は 閉じます。
　★★困る こと …… 同じ 学校の 先生なら、★よその 行事にも 点を 入れられます。
　　　見本の「審査員を 足す」が 意味を 持ちません。

### ★私が お勧めする もの …… ㋐

★理由は 3つ です。
　1. 本番に 点が 0件 なので、★かたく 閉じても 誰も 困りません。
　2. あとから ㋑→㋐ に 移すと、★その あいだに 入った 行を どう するかを 決める ことに なります。
　3. 見本が もう 行事ごとに 書かれて います。★台帳を 見本に 合わせる ほうが、ずれが 減ります。

★★どちらでも、`evaluation_judge_done` は **証しに 使いません**。
　★自分で 押せる 印 だからです。

## 六 ── 決まれば すぐ できる こと

★どちらに 決まっても、次の 2つは 同じ です。

- `evaluation_scores_write` / `evaluation_reviews_write` の `with check` に、その条件を 足す
- `evaluation_judge_done` の insert に、同じ 条件を 足す（★裁定164 W3）
  ★★一意の 決まりは **もう あります**。`evaluation_judge_done_pkey PRIMARY KEY (event_id, judge_id)`。
  　★裁定164 W3 の うち、この 半分は すでに 済んで います。

★★在籍の 確かめ（`enrollments`）も 足すか どうかは、★上の 決めの あとに お尋ねします。
