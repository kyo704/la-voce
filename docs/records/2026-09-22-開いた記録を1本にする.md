# 開いた 記録を 1本に します（2026-09-22）

★台帳 08-14 が 決まりました（Opus・design-v21）。

## 決まった こと

- 正式な 1本は `monka_read_log`。
- `org_message_reads` は **廃めます**。併せ持ちません。
- 見え方 ── 運営（`monka_read`・`master`）は 全部。門下の 学生は「いつ・どの 門下」だけ。
  先生には この版では 出しません。

## 消した もの

| 何 | どこ | 呼ぶ ところ |
|---|---|---|
| `logRenrakuRead` | `components/VocalTracker.jsx` | ★2026-09-21 から 0か所 |

★消した わけ …… 同じ 約束に 表が 2つ あると、★片方が 古く なって 気づけません。
★書くのは `open_monka_thread` だけ です。★台帳の 中で 先に 1行 書きます。

## 移した もの

| 何 | 前 | いま |
|---|---|---|
| 学校ぜんぶの 一覧 | `org_message_reads` | `monka_read_log`（`org_id` で 絞る） |
| 1つの 門下の 分 | `org_message_reads.teacher_id` | `monka_read_log.target_monka_id` |
| 数 | `org_message_reads` | `monka_read_log` |
| 学生の 画面 | ありません でした | `get_monka_read_notices` |

★列は 名指し です（`READ_LOG_COLUMNS`）。★`select('*')` を 書いて いません。

## 開けるように しました

`lib/renraku.js` の `monkaReadOpen()` を `false` → `true` に しました。

★外す 条件は こう 書いて ありました ──
「中身を 返す 道が、先に 1行 書いて から 返す 形に なった日」。
★その日が 来ました（`open_monka_thread`・裁定159 S2・2026-09-21 に 13件 で 確かめ）。

★★本番の `monka_read` 保有者 …… **0名**。★画面が 変わる 人は いません。

## まだ して いない こと

★`org_message_reads` を drop する 移行。

★わけ …… `supabase/` の SQL に まだ 4本 残って います。
　`2026-09-10-連絡と、読んだ記録.sql` ／ `問い-決まりの無い書き込み.sql` ／
　`migration_small_three.sql` ／ `migration_leave_enrollment.sql`
★見張り（`components/tests/renraku.test.js:163`）も その SQL を 読んで います。

★when …… ①上の4本と 見張りを 直す ②権限変更の型で drop を 出す ③ご承認。

## 見た目

★コードに 入れました。★見張りは 通りました。★見た目は 未確認 です。
★実機で お確かめ ください。
