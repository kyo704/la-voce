# 修正の記録 No.014 ── 消した 曲が、読み直すと 戻る

★見つけた日 2026-09-14
★**直した日 2026-09-14**

## 落ち度は 2つ ありました

★① **台帳** … `repertoire_tessitura` に 消す 決まり（RLS の DELETE）が 無い。
★② **画面** … 何行 消えたかを 確かめて いない。204 を「通った」と 読む。

★★どちらも 直しました。★片側だけでは、★同じ 形が また 起きます（★No.002）。

## どうやって 見つけたか

★★**実際に 動かして** 見つけました。★字を 読んで いても 分かりません でした。
　★はじめの 見立て（一覧の しぼりに `!n.deleted_at` が 無い）は **誤り**でした。

★★道具 `tools/repro_repertoire_delete.py` が、★一度に これを 控えます ──
　★① 画面の 言づて（console）ぜんぶ
　★② 台帳との やりとり ぜんぶ（★送った 中身と、しくじった ときの 返り）
　★③ 10枚の 絵（★消す 前・消した 後・読み直した 後）

★★この 3つを いっしょに 見て、★はじめて 分かりました ──
　★「しくじりは 0件。★送った 中身も 正しい。★なのに 行が 残って いる」。

## 何が 起きて いたか

★★ノート → レパートリー で 曲を 消すと、★画面からは 消えます。
★★読み直すと、★戻って います。

## 実際に 動かして 見た こと

★★試し用の 企画（Supabase）に 3日ぶんの 記録と 曲を 置き、
　★実際に 消して、★読み直しました。

| | 曲の 数 |
|---|---|
| 消す 前 | 1 |
| 消した 後 | 0 |
| 読み直した 後 | **1** |

★★読み直した ときの 札は「記録 **0日**」でした（★前は 3日）。
　★★つまり **記録からは 外れて いた**のです。★名前だけが 残って いました。

## 台帳との やりとり（★ぜんぶ 控えました）

| 送った もの | 返り | 中身 |
|---|---|---|
| `POST /entries` ×3 | 200 | `activities:[{"items":[]}]`／`repertoire:null` |
| `DELETE /repertoire_tessitura` | **204** | — |
| `DELETE /role_master` | 204 | — |
| `DELETE /project_master` | 204 | — |
| `PATCH /notes` | 200 | `{"deleted_at":"…"}` |

★★しくじった やりとりは **0件** でした。★誤りの 言づても ありません。

## 読み直して、★台帳を 直に 見た

```
repertoire_tessitura … 「テスト曲A」の 行が ★残って いました
notes                … deleted_at が 入って いました（★正しい）
entries              … activities は [{"items":[]}]（★正しい）
role_master          … 空
project_master       … 空
```

## ★原因

★★`repertoire_tessitura` に、★**消す ための 決まり（RLS の DELETE ポリシー）が ありません**。

★★本人の 鍵で、★4つを 1つずつ 試しました。

| | 結果 |
|---|---|
| SELECT | 通る |
| INSERT | 通る |
| UPDATE | 通る |
| **DELETE** | **0行**（★HTTP 200／返り値 `[]`） |

★★RLS は、★決まりが 無い ときに **黙って 0行**に します。
　★★誤りを 返しません。★HTTP は 204 や 200 の ままです。
　★★だから 画面は「消した」と 思い込みます。

## ★これは 2つの 別々の 落ち度です

★① **台帳の 側** … 消す 決まりが 無い。
★② **画面の 側** … 何行 消えたかを **確かめて いない**。

★★①を 直しても、★②が 残れば 同じ ことが また 起きます。
　★★204 は「通った」では ありません。「誤りは 無い」だけ です。
　★★これは 名簿の ときと 同じ 形です（★No.002）。

## 直し方

### ① 台帳（★坂本さんが 流します）

`supabase/migration_repertoire_tessitura_delete_policy.sql`

★★同じ 形が ほかの 表にも 無いかを 数える 問いも 入れて あります。

### ② 画面（★直しました）

★★`handleDeleteRepertoire` に `.select("id")` を 付けました。
　★0行なら 止めます。★黙って 通しません。

★★役（`role_master`）と 企画（`project_master`）は、★0行でも 止めません。
　★★曲に 役が 無い ことは 普通です。★そこで 止めると 消せなく なります。
　★誤りが 返った ときだけ 止めます。

★★出す 言葉は **変えて いません**。
　★「消せませんでした。もう一度お試しください。」の ままです。
　★★中の 合図（`REPERTOIRE_DELETE_NO_ROWS`）は 画面に 出しません。

★★見張り `components/tests/delete-affected-rows.test.js`

## 本番

★★2026-09-14、★坂本さんが 本番でも 確かめ、★同じ 抜けが ありました。
　★★写し そこねでは ありません。★**本番にも あった 穴**です。
　★21行・2人。★決まりを 足すだけ なので、★中身は 触って いません。
　★`pg_policies` で、★DELETE が 並んだ ことを 確かめて います。

## 同じ 形が ほかに 無いか

★★消す 決まりの 無い 表が、★ほかに 22 あります。
　★★数えた 結果、★**同じ 形（③）は 0件** でした。
　★22の うち 10は 別の 消し方（印を つける 列）を 持ち、
　★12は 足すだけの 記録でした。
　★くわしくは `docs/reports/2026-09-14-missing-delete-policies.md`。
