# 修正の記録 No.019.5 ── 数える ために、値を 運ばない
全101行 / 末尾は「★★古い 道と 新しい 道が、★同じ 答えを 返す ことを、★実データで 確かめました。」

★決めた日 2026-09-14（★裁定 その61 ／ Opus・坂本さん 承認）
★入れた日 2026-09-14（★`732f25d` ／ 本番 `fc2c2cb`）

## ① 何を 直したか

| | 前 | 後 |
|---|---|---|
| `app/admin/page.js:102` | 全員の entries から **12列** | `admin_entry_stats()` が **数だけ** 返す |
| `app/api/character/unlock/route.js:55` | 本人の `select("*")` | `character_unlock_summary()` が **3つの 数だけ** |

★12列には、★`medication_tags`（お薬）・`mental_reason`（自由記述の 日記）・
★`voice_memo`（声の メモ）が 入って いました。
★★そして 値は ★1つも 使われて いません でした。
　★`typeof` と `.length` と `.trim()` ── ★「埋まって いるか」だけ です。

★2026-09-14、★本番の `x-vercel-id` を 測りました ──
　★静的 `hnd1`（東京）／★**関数 `iad1`（米国バージニア）**。
★★保管は 東京でも、★関数は 日本の 外で 動いて います。

## ★② 範囲の 訂正（★2026-09-14・同日）

★★はじめ、★私は こう 書きました ──

> ★全38人の 12列（服薬・気持ちの 日記・声の メモ）を、★管理画面を 開く たびに
> ★米国へ 運び、★埋まって いるかしか 見て いない。

★★これは **言いすぎ** です。★訂正します。

### ★調べた こと

| 調べた こと | 結果 |
|---|---|
| 門（`is_admin`）は いつからか | ★`bd10c1e` ── ★生まれた ときから あります |
| 門は 早い return か | ★はい。★`権限がありません` を 返して 止まります |
| 12列の 取得は いつ 入ったか | ★`3c8601d`（**2026-08-26**） |
| 門と 取得の 順番 | ★**8つの 版 すべて**で、★門が 先 |
| 紙（migration）が `is_admin` を 立てて いないか | ★立てて いません |
| いま `is_admin` が true の 方 | ★**48人中 0人**（★Opus・台帳で 確認） |

### ★`is_admin` が true だった ことは、★1度 あります

★`docs/lavoce-インシデント記録.md` の **009**（★2026-09-05）です。
★私が お渡しした 確かめの 手順が、★`begin … rollback` を 使って いました。
★★Supabase の SQL エディタは、★`rollback` を 効かせません でした。
　★運営者ご自身の `is_admin` が、★true の まま 残りました。

★★その ときも、★12列は 取りに 行って いません。★理由は 記録に あります ──

> ★`app/admin/page.js` は Server Component です。
> ★`"use client"` が ありません。`useEffect` も `setInterval` も
> ★`subscribe` も `router.refresh` も、★1つも ありません。
> ★つまり、★描いた ときに 1回だけ 取ります。★開いた ままでは 取り直しません。
> ★運営者が、★その タブを 押して いない ことを 確かめて くださいました。

★★`is_admin` が true だった のは 2026-09-05。★12列が 入ったのは 2026-08-26。
　★★時期は 重なって います。★けれど、★**描き直して いません**。

### ★★正しい 言い方

| | |
|---|---|
| ★経路 | **ありました**（2026-08-26 〜 2026-09-14） |
| ★通った 記録 | **1つも 見つかりません** |
| ★門 | ★8つの 版 すべてで、★取得より 前 |
| ★いま | `is_admin` は 48人中 0人 |

★★「★全38人の 記録が 米国へ 渡って いた」では ありません。
★★「★管理者が 管理画面を 開けば 渡る 形が、★19日間 あった」です。

### ★★それでも 確かめ切れて いない こと

★★`is_admin` が、★記録に 無い ところで true だった ことが ないか ──
　★これは **台帳からは 分かりません**。★列は いまの 値しか 持ちません。
★★確かめるなら、★Vercel の アクセスログで `/admin` を 2026-08-26 以降 数えます。
　★★急ぎません。★いまは 経路そのものが ありません。

★★私は「渡って いません」とは 書きません。
　★★読んだ 方が 確かめ ようの ない 言い方 だから です（★裁定・2026-09-03）。
　★書けるのは「★探したが、★通った 跡は 見つからなかった」まで です。

## ③ 直しは 戻しません

★★範囲が 小さかった ことと、★直しが 要らなかった ことは、★別です。

- ★値を 運ぶ 必要が、★はじめから ありません でした
- ★`unlock` の ほうは ★門が ありません。★ご本人が 押す たびに 動きます
- ★関数が 米国で 動く ことは、★2026-09-14 に 測って 分かった ばかり です

## ④ 確かめ（★2026-09-14・配信後）

| | |
|---|---|
| 本番の 版 | `fc2c2cb` |
| `admin_entry_stats()` の 9項目 | ★配信 前と **すべて 同じ** |
| 総数 | 102 → 104（★きょう 2人が 記録。★ふだんの 使い方） |
| ごほうび | ★増減 なし（`fieldKinds` は 18人 とも 17 以上・しきい値 10） |

★★古い 道と 新しい 道が、★同じ 答えを 返す ことを、★実データで 確かめました。
