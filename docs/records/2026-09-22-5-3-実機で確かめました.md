# 5-3 ── 連絡が 空でも「＋」が 出る（★実機で 確かめました・2026-09-22）

★実行ルート 5-3 の「済みの目安」を、★本物の ブラウザで 通しました。

★道具 …… `tools/renraku_plus_shot.py`（Python・Playwright）
★画像 …… `docs/design/compare/renraku/`

## 結果 …… **16 / 16**

| | 見た こと | 結果 |
|---|---|---|
| ① | 390px …… 書ける 役で 開く →「＋ おしらせを 書く」が **見えて 押せる** | ○ |
| ② | 1280px …… 同じ | ○ |
| ③ | ★較正 …… 書けない 役（`renraku_all` なし）には **出ない**（0個） | ○ |
| ④ | 連絡が 1件 ある 学校でも 出る | ○ |

★★連絡 0件・門下 0件 の 学校を、★試しの 台帳に 作りました
　（`★からっぽの 学校（5-3の ため）`・`supabase/seed_test_karano_gakko.sql`）。

## 通った 道

```
ログイン → きょう → ⚙（歯車） → 「◯◯ の 運営」 → 連絡
```

## ★つまずいた ところ（★書いて おきます）

1. ★`playwright`（Node）の chromium が 入って いません でした。
   ★★Python の ほうは 入って います。★道具を Python で 書きました
   　（★台帳の 決め …… `tools/` の 道具は Python）。

2. ★★★画面に 出る 字は「◯◯ **の 運営**」です。★`の` と `運営` の あいだに 空きが あります。
   ★もとの JSX は `{mm.org.name} の運営` です。★出る ときに 空きが 入ります。
   ★★「の運営」で 探して、★1度 見つかりません でした。
   ★★★見える 字で 押す、という 決めの **落とし穴** です。
   　★JSX の 字と、★画面に 出る 字が ちがう ことが あります。

3. ★`localhost:3000` に 別の ものが 動いて いました。★`3001` を 使って います。

## ★残って いる こと

★見本（`P_renraku`）と 並べた 画像は まだ です。★出た ことだけ 確かめました。
