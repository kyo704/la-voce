# STEP_0 ── 門下を読む道を閉じました（2026-09-21）

紙 `supabase/migration_monka_read_close.sql`（リポジトリに入っています）

## なぜ

画面は、`monka_read` を持つ方に、いちばん上の帯でこう言っています。

> あなたは、門下の やりとりを 確かめられる 役職です。見ると、記録に 残ります。ご本人にも 伝わります

見本③（2026-09-10 から）にも、同じ約束があります。

> 開いたことは 記録に残り、先生と学生の画面から 見られます。

ところが、`monka_read_log` に書く処理がどこにもありません。
画面からの挿入も、台帳の関数の中の挿入も、1つもありません（数えました）。
本番の `monka_read_log` は **0行** です。

★約束が偽のあいだ、読める状態を残しません。
★画面で隠すだけにしません。画面の門は門ではありません。

## 当てる前に数えたこと（本番）

| 見たこと | 数 |
|---|---|
| `monka_read` を持つ役職 | 0 |
| `monka_read` を持つ人 | 0 |
| `monka_read_log` の行 | 0 |

困る方はいません。先に閉じました。

## 何を外したか

`org_messages_select` の最後の枝1つだけです。

```
OR ((teacher_id IS NOT NULL) AND has_can(org_id, 'monka_read'::text))
```

残した枝は3つ。先生ご本人・門下の学生（在籍と宛先の絞り込みを含む）・
`renraku_all`（学校ぜんぶのお知らせ）。1文字も変えていません。

## 当てたあとに数えたこと（本番）

| 見たこと | 結果 |
|---|---|
| `monka_read` を含むか | いいえ |
| `renraku_all` を含むか | はい |
| `assignments` の枝 | あり |
| `enrollments` の枝 | あり |
| 先生ご本人の枝 | あり |
| `org_messages` の決まりの数 | 3（変わっていません） |

## 試しの台帳で押して確かめました

道具 `tools/monka_read_closed_check.js`（本人の鍵で引きます。管理の鍵は使いません）

たろうの役職に `monka_read` を足し、はなこ門下のやりとりを1件入れてから：

| 見たこと | 結果 |
|---|---|
| 門下の中身が 0行 | PASS |
| 名ざしで引いても 0行 | PASS |
| 較正：学校のお知らせは見える（2行） | PASS |

較正があるので「閉じ過ぎていない」ことも言えます。

## 画面

`monka_read` を持つ方の帯に、1行足しました。

> いまは 調整中の ため、門下の やりとりを 開けません。

わけを技術の言葉で書いていません。「記録の仕組みが未完成です」と書くと、
いま開けば残らない、と読めます。そう読んで試す方が出ます。

## 戻す条件

中身を返す道（RPC）の中で、台帳が先に `monka_read_log` へ1行書き、
書けたときだけ中身を返す形（fail closed）ができた日。
そのとき、紙の最後の1行を戻します。

## まだしていないこと

- STEP_1（記録を書く道と、理由の選択）
- STEP_2（過去に読み取りがあったかの調査と、修正の記録）
