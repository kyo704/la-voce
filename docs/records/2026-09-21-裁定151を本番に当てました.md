# 裁定151 の工事を本番に当てました（2026-09-21）

裁定149 の env「test で当ててから本番。本番に手で当てたら、その日のうちに
リポジトリへ写す」に従い、当てたことをここに残します。

紙 `supabase/migration_my_periods_org_id.sql`（リポジトリに入っています）

## 当てる前に数えたこと（本番）

| 見たこと | 数 |
|---|---|
| my_periods の行 | 22 |
| (user_id, ord) の重なり | 0 |
| has_can / get_teacher_periods | どちらも在ります |

重なりが 0 だったので、`my_periods_uniq` を外して部分一意索引 2本に
分け替えても、既にある行は 1行も弾かれません。

## 当てたあとに数えたこと（本番）

| 見たこと | 数 |
|---|---|
| my_periods の行 | 22（減っていません） |
| org_id が入った行 | 0（在籍1校の方が 0名のため） |
| my_periods_uniq_personal | 1 |
| my_periods_uniq_org | 1 |
| 古い my_periods_uniq | 0（外れました） |
| 引き金 | 1（trg_my_periods_org_stable） |
| 決まり（policy） | 4（select / insert / update / delete） |
| get_teacher_periods に `p.org_id = p_org_id` | 有り |

## 画面の側で変わること

`components/MyTimetable.jsx` は `org_id` を書きません。入れた行は null
── つまり「個人のコマ」になります。ご本人の画面はこれまでどおりです。

学校の側（日程を組む）は `get_teacher_periods` を通ります。いまは
org_id の入った行が 1行も無いので、**どの先生のコマも学校側には出ません**。
在籍2校以上の3名については台帳 08-11 に残しました。在籍1校の方は 0名です。

★取り上げてはいません。行は消えていません。ご本人の画面では見えます。

## まだしていないこと

`MyTimetable.jsx` に「このコマはどの学校のものか」を選ぶところがありません。
足すかどうかは、台帳 08-11 の引き金（ご本人の申し出、または
「コマが出ない」というお問い合わせ）が引かれたときに決めます。
