// ============================================================================
// 「1回だけ出す知らせ」を、1つの仕組みでまとめる
//
//   ★これまでは、知らせを1つ足すたびに profiles へ列を1本足していました。
//     occupation_notice_shown_at / age_question_shown_at / survey_day7_shown_at。
//     知らせが増えるたびに移行が要り、そのたびに
//     「コードは出たが SQL はまだ」の窓が開きます（2026-08-30 に2回）。
//
//   ★これからは user_notices に1行入れるだけです。列は増えません。
//
//   ★知らせの鍵は、ここが唯一の正です。文字列を画面に直接書かないこと。
//     書くと、綴り違いで「出したのに出続ける」が起きます。
// ============================================================================

/** 出しうる知らせの鍵。★増やすときは、ここに足してから使うこと。 */
export const NOTICE_KEYS = [
  // 初回のダッシュボードで1度だけ。文字の大きさを変えられることを伝える。
  "displayScaleHint",
  // ★★同意のお詫びと訂正（2026-09-03 確定・v3）。
  //   ★出どころ docs/lavoce-お知らせ画面-文面（2026-09-03確定-v3）.md
  //   ★文面は1文字も変えません。★段落も、そのままです。
  //   ★★A-2 の版を上げても、再同意は求めません。
  //     ★だから、別のお知らせは出しません。★同じ方を2回止めないこと。
  "consentApology2026"
];

/** 知らせの本文。★9言語にしていません。いまは日本語だけで出します。 */
export const NOTICE_TEXT = {
  displayScaleHint: "文字が小さければ、いつでも設定から変えられます",
  // ★お知らせ画面の見出し。★本文は NOTICE_PARAGRAPHS にあります。
  consentApology2026: "記録の同意について、お詫びと訂正"
};

/**
 * ★★お知らせの本文。★段落ごとに分けてあります。
 *
 *   ★出どころ docs/lavoce-お知らせ画面-文面（2026-09-03確定-v3）.md
 *   ★★1文字も変えないこと。★要約も、言い換えも、しないこと。
 *     ★確かめが、正の md と1行ずつ突き合わせます
 *     （components/tests/notice-screen.test.js）。
 *
 *   ★段落の切れ目は、★正の md の空行と同じです。
 */
export const NOTICE_PARAGRAPHS = {
  consentApology2026: [
    "体調の記録のうち、既往症（逆流性食道炎・声帯結節などの持病）に"
      + "関するものは、法律上「要配慮個人情報」にあたるものです。"
      + "お預かりするときには、その旨をはっきりお伝えする必要が"
      + "ありましたが、同意の画面にその記載がありませんでした。",
    "そのため、同意をもう一度お願いしています。"
      + "お聞きする内容は、これまでと変わりません。"
      + "書き方を、法律に合った形に直したものです。",
    // ★★「作り忘れです」に、禁じた言葉「忘れ」が入っています。
    //   ★禁じた理由は「まだ記録していません」のように、★利用者を急かすことでした。
    //   ★ここは★開発者が自分の落ち度を言っています。★急かしていません。
    //   ★確かめの MEASUREMENT_EXCEPTIONS に、理由つきで足してあります。
    //   ★★検査を消していません。
    "あわせて、同意の画面に「いつでも撤回できます」と"
      + "書いていましたが、撤回するためのボタンを作っていませんでした。"
      + "私（開発者）の作り忘れです。"
      + "9月3日に作りました。設定の画面から、いつでも撤回できます。",
    // ★★v2 から足した1段落は、これです（Opus・運営者の承認ずみ）。
    "あわせて、利用規約とプライバシーポリシーの書き方を、"
      + "より正確な形に直しました。お預かりする内容や、"
      + "先生から見えるものは、変わっていません。",
    // ★★この段落を落とさないこと。★いちばん心配されるところです。
    "記録が外部に渡ったということではありません。"
      + "説明の書き方が足りていなかった、ということです。"
  ]
};

export function isNoticeKey(key) {
  return NOTICE_KEYS.includes(key);
}

/**
 * その知らせを、いま出してよいか。
 *
 * @param {object} state  { [key]: shown_at } の形。読めていなければ null を渡す
 * @param {string} key
 * @returns {boolean}
 *
 * ★state が null（テーブルがまだ無い／読めなかった）のときは false。
 *   出しません。出してしまうと、既読にできず★毎回出続けます。
 *   「1回だけ」を守れないなら、出さないほうがましです。
 */
export function shouldShowNotice(state, key) {
  if (!isNoticeKey(key)) return false;
  if (!state) return false;
  return !state[key];
}

/**
 * 既読にした状態を作る。★元の state は変えません。
 * shown_at は呼ぶ側から渡します（この関数の中で時刻を作らない）。
 */
export function withNoticeShown(state, key, shownAt) {
  if (!isNoticeKey(key)) return state;
  return { ...(state || {}), [key]: shownAt };
}

/** 読み込んだ行の配列を、{ key: shown_at } の形にする。 */
export function noticeStateFromRows(rows) {
  const out = {};
  (rows || []).forEach((r) => {
    if (r && isNoticeKey(r.notice_key)) out[r.notice_key] = r.shown_at || true;
  });
  return out;
}
