// ============================================================================
// ★押したときの 名前 ── ★マスに 入る 字（★2026-09-25・C群）
//
//   ★見本 `SC['予定の名前']`（★design-v63）。★題は「押したときの 名前」です。
//
//   ★★★2つの 約束（★見本の `.note`・1文字も 変えないこと）──
//     ①「名前は おまけです。「予定」のままで かまいません。」
//     ②「この 名前は、あなたにしか 見えません。」
//
//   ★★★②は 決まりが 守って います（★2026-09-25・本番で 確かめました）──
//     ★`my_timetable` の 決まりは 本人だけ です。★事務にも 先生にも 道が ありません。
//     ★★`get_student_free_slots` が 事務に 返すのは **空きか どうか** だけ で、
//       ★`title` を 返しません。
//
//   ★★★①は「決めなくても よい」という 約束 です。
//     ★★だから **既定を 空に しません** ── ★「予定」を 入れて おきます。
//       ★空だと、★何か 決めないと 進めない ように 見えます。
//
//   ★★覚え場所は 端末 です（★見本の `S.bn`）。★台帳に 列が ありません。
//     ★★★「よく 使う 名前」を 数えません。★並べ替えません。
//       ★数えると、★使って いない 名前が 下に 落ちます。★選びにくく なります。
// ============================================================================

/** ★題と 戻り先（★見本）。 */
export const TITLE = "押したときの 名前";
export const BACK_TO = "自分の 予定";

/** ★上の 1行（★見本の `.usu`）。 */
export const LEAD = "ここで 決めた 名前が、マスを 押したときに 入ります。";

/** ★欄の 札。 */
export const FIELD_LABEL = "名前";

/** ★既定（★空に しません）。 */
export const DEFAULT_NAME = "予定";

/**
 * ★すぐ 押せる 名前。
 *
 *   ★★見本は 2つ 持って います ── ★学校の 予定 と 自分の 予定。
 *   ★★★自分の 予定の ほうに「バイト」が 入って います。
 *     ★学校の 側に 置くと、★事務に 出す 名前に なり得ます。★分けて あります。
 */
export const NAMES_SCHOOL = Object.freeze(["予定", "授業", "講義", "実技", "ゼミ"]);
export const NAMES_OWN = Object.freeze(["予定", "バイト", "通学", "用事", "からだの こと"]);
export function namesOf(kind) {
  return String(kind || "") === "自分" ? NAMES_OWN : NAMES_SCHOOL;
}

/** ★押しどころ。 */
export const BTN_OK = "これに する";

/** ★空の ときの 1行（★見本の `toast`）。 */
export const NAME_REQUIRED = "名前を 入れてください";

/** ★空では 決められません。 */
export function mayUse(name) {
  return String(name || "").trim().length > 0;
}

/** ★決めた あとの 1行（★見本の `toast`）。 */
export function doneWord(name) {
  return "「" + String(name || "") + "」に しました。マスを 押すだけです";
}

/** ★下の 2行（★見本の `.note`・1文字も 変えないこと）。 */
export const NOTE_LINES = Object.freeze([
  "名前は おまけです。「予定」のままで かまいません。",
  "この 名前は、あなたにしか 見えません。"
]);
