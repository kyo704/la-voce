// ============================================================================
// 先生とのつながりの記録（G4-26 / 作業指示-教室プラン D-3）
//
//   ★この1ファイルが持つ決めごと
//     ① 同意した文面の版（agreementVersion）
//     ② 積む行の形
//     ③ 「外した」を記録するときの書き方
//
//   ★何を記録しないか（Opus の裁定・2026-09-01）
//     ・org_id          … 教室ごとの共有への扉になるため、持たせません
//     ・共有範囲（scope）… 2026-09-01 に廃止した考え方です
//
//   ★このつながりは「記録を見せる」同意ではありません。
//     先生は、生徒の記録の中身を見られません。
//     記録した日も、件数も、記録があるかどうかも見られません（憲章 §10）。
//     同意しているのは、★レッスンの予定を一緒に見ることです。
// ============================================================================

/**
 * 同意した文面の版。
 *
 * ★画面の文言を変えたら、必ずここを上げてください。
 *   上げないと、「あの人は何に同意したのか」を後から答えられなくなります。
 *
 *   v1（2026-09-01）★消しません。この文に同意した方がいます。
 *     「つながると、この先生とレッスンの予定を共有できるようになります。
 *       あなたの記録の中身は、先生には見えません。」
 *
 *     ★この文は、いま見ると不正確でした（2026-09-03）。
 *       「記録の中身」の範囲が書かれておらず、読む人は
 *       「体調のこと全部」と受け取ります。
 *       ★ところが profiles の行（既往症・常用薬・health_notes ほか）は、
 *         ポリシーの誤りにより、つながっている相手から読める状態でした。
 *       ★entries と cycle_periods は渡っていません。守られていたのはそちらです。
 *
 *   v2（2026-09-03）★案A
 *     「つながると、この先生とレッスンの予定を一緒に見られるようになります。
 *
 *       先生に伝わるのは、あなたの表示名と職業だけです。
 *       日々の記録（体調・声・睡眠・食事・メモなど）を先生が見る仕組みは、
 *       ありません。周期の記録も同じです。
 *
 *       つながりの解除は、いつでもできます。」
 *
 *     ★変えた考え方
 *       ・「渡らないもの」より先に★「渡るもの」を書きます。
 *         読む人がまず知りたいのは、そちらです。
 *       ・「見えません」ではなく★「仕組みは、ありません」と書きます。
 *         ★約束は破れますが、無い仕組みは破れません。
 *       ・「一切」「決して」を使いません。★強すぎる言葉は、今日足をすくいました。
 *
 *     ★表示名と職業に触れているのが要点です。
 *       いまはそれが実際に渡ります（get_connected_names）。
 *       ★書かないと、また同じことになります。
 *
 *     ★この文の「仕組みは、ありません」は、いま事実です。
 *       get_student_entries は削除済み、entries の先生向けポリシーも削除済み。
 *       ★どちらかを戻すなら、この文も同時に変えなければなりません。
 *
 *   ★v1 → v2 で、再同意は求めません（2026-09-03・運営者の判断）。
 *     v2 は v1 より★狭く・正確になっただけで、
 *     利用者にとって不利になる変更ではないためです。
 *     ★A-2 の同意文を直したときと、同じ扱いです。
 *
 *   ★v1 より前は、共有範囲を9つのチェックで選ばせていました。
 *     その仕組みごと廃止したので、v1 は前の版の続きではありません。
 */
export const LINK_AGREEMENT_VERSION = "link-2026-09-03";

/**
 * ★この画面で使ってはいけない言い回し。
 *   検査（components/tests/link-consent-wording.test.js）が見ています。
 *
 *   ★理由：どれも「状態の断言」で、仕組みが変われば嘘になります。
 *     構造の記述（「仕組みが、ありません」）なら、変わったときに
 *     ★コードと文が同時にずれるので、気づけます。
 */
export const FORBIDDEN_LINK_PHRASES = [
  "見えません",   // ★状態の断言。今日これで誤りになりました
  "一切",         // 強すぎる
  "決して",       // 強すぎる
  "安全です",     // 読む人に確かめる手立てがない
  "守られています" // 同上
];

/** 誰が外したか。★片方が退会した場合は null にします（推測で埋めないこと）。 */
export const UNLINKED_BY = ["student", "teacher"];

/**
 * つながったときに積む行。
 *
 * ★unlinked_at は入れません。null が「いまも続いている」という意味です。
 */
export function buildLinkConsentRow({ studentId, teacherId, now }) {
  return {
    student_id: studentId,
    teacher_id: teacherId,
    linked_at: (now || new Date()).toISOString(),
    agreement_version: LINK_AGREEMENT_VERSION
  };
}

/**
 * 外したときに入れる値。
 *
 * ★行は消しません。上書きもしません。いま続いている行に、終わりの時刻を入れます。
 */
export function buildUnlinkPatch({ by, now }) {
  return {
    unlinked_at: (now || new Date()).toISOString(),
    unlinked_by: UNLINKED_BY.includes(by) ? by : null
  };
}

/**
 * いま続いているつながりの行を選ぶ（unlinked_at が null のもの）。
 * ★複数あってはいけませんが、あっても落ちないように、いちばん新しいものを返します。
 */
export function findOpenConsent(rows, teacherId) {
  const open = (rows || []).filter(
    (r) => r.teacher_id === teacherId && !r.unlinked_at
  );
  if (open.length === 0) return null;
  return open.sort((a, b) => String(b.linked_at).localeCompare(String(a.linked_at)))[0];
}
