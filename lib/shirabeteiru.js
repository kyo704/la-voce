// ============================================================================
// ★しらべている こと ── ★くらしと 声の 組（★2026-09-25・C群 束2）
//
//   ★見本 `SC['しらべていること']`（★design-v51・裁定197 で 直った あとの もの）。
//
//   ★★★変えて は いけない 約束 3つ（★裁定197 が 名指しで 守れ と 書いて います）──
//     ①「組を 作るのは あなたです。こちらから おすすめは しません。」
//     ②「機械が 組を 選ぶことは ありません。」
//     ③「書いていない 日は、組に しません。うめません。」
//   ★★①②は 同じ ことを 2度 言って います。★どちらも 残します ──
//     ★★①は 誰が 決めるか、★②は 機械が しない こと。★片方だけでは 弱く なります。
//   ★★③は 埋めない こと の 約束 です。★平均で 埋めると、★無い 日が 有る 日に 化けます。
//
//   ★★★時間差の 4つは `lib/lagChoice.js` の `LAGS` です（★裁定197）。
//     ★★ここで 並べません。★あちらが 1か所 です。
//     ★★見本は 裁定197 で こう 直りました ──
//       ★見出し「いつの くらしと くらべますか」
//       ★札「その日の朝 ／ 前の夜 ／ 前の日 ／ 2日まえ」
//     ★★★決め手 ──「『2日後』は 未来と 読めるが『2日まえ』は 読めない。
//       ★このアプリは 予測を しないため、★未来と 誤解される 表現は 避けるべき」
//
//   ★★★札の 並びは `LAGS` の 順 では なく、★見本の 順 です。
//     ★`LAGS` …… prevNight ／ prevDay ／ twoDaysAgo ／ sameMorning
//     ★見本 …… その日の朝 ／ 前の夜 ／ 前の日 ／ 2日まえ
//     ★★だから ここで 並べ替えます ── ★`PILL_ORDER`。
//       ★★字は `LAGS` から 取ります。★書き写しません。
//
//   ★★台帳（★2026-09-25・本番で 確かめました）──
//     ★読む: `entries`（★`entries_select_own`・ご本人だけ）
//     ★★時間差を しまう 列・表・関数は **1つも ありません**。★画面の 状態 だけ です。
//
//   ★★「無料では 1組までです」── ★無料の 上限 です。
//     ★★`lib/compareOrder.js` の `ORDER_MAX`（5）は 買った あとの 数 です。
//       ★★★別の 数 です。★混ぜません。
// ============================================================================
import { LAGS } from "@/lib/lagChoice";

/** ★題（★見本の `<h2>`）。 */
export const TITLE = "しらべている こと";

/** ★上の 2行（★見本の `.warn`・1文字も 変えないこと）。 */
export const HEAD_LINES = Object.freeze([
  "くらす ことと、声の ことを 組にします。",
  "組を 作るのは あなたです。こちらから おすすめは しません。"
]);

/** ★組の 見出し（★見本「組 1」）。★数は 組の 番号 です。 */
export function pairHead(n) {
  return "組 " + String(n);
}

/** ★行の 右（★見本 `<s>かえる ›</s>`）。★くらしの 側だけ に 出します（★下）。 */
export const ROW_ACTION = "かえる ›";

/**
 * ★★★声の 側は **1つ だけ** です ── ★`声の 出来`（`voice_quality`）。
 *
 *   ★見本は 2行目に「声の 調子」と 出し、★右に「かえる ›」を 置いて います。
 *   ★★★選べる ように 見えます が、★選ぶ ものが ありません。
 *     ★2026-09-11 の 決め（`lib/compareView.js` の 頭）──
 *       「★正しい 見本は 3択が 2つ です ── ★のどの 調子 → throat_condition ／
 *         ★声の 出来 → voice_quality。★のどの調子で 群を 分けると、
 *         ★図の 名前と 中身が ずれます」
 *     ★★★だから 声の 側は `voice_quality` 1つ に 決めて あります。
 *       ★のどの 調子は **わざと** 使って いません。
 *   ★★だから 2行目に「かえる ›」を 出しません ── ★押せない 札を 置きません。
 *   ★★字も 実装の 名 に します ──「声の 出来」。
 *     ★「声の 調子」は のどの 調子 と 混ざります。★混ざった ことが 一度 あります。
 */
export const VOICE_SIDE_LABEL = "声の 出来";
export const VOICE_SIDE_FIXED = true;

/** ★時間差の 見出し（★見本・裁定197 で 直った 字）。 */
export const LAG_HEAD = "いつの くらしと くらべますか";

/**
 * ★札の 並び（★見本の 順）。
 *
 *   ★★`LAGS` の 順 と ちがいます。★字は `LAGS` から 取ります。
 *   ★★知らない 鍵が `LAGS` に 増えた ときは、★後ろに 付けます ──
 *     ★★落としません。★落とすと、★選べない 時間差が できます。
 */
export const PILL_ORDER = Object.freeze(
  ["sameMorning", "prevNight", "prevDay", "twoDaysAgo"]);

export function lagPills() {
  const 並 = PILL_ORDER.map((k) => LAGS.find((l) => l.key === k)).filter(Boolean);
  const 残 = LAGS.filter((l) => !PILL_ORDER.includes(l.key));
  return [...並, ...残];
}

/** ★組を 足す（★見本の `.btn.g`）。 */
export const BTN_ADD = "＋ 組を 足す";

/**
 * ★無料の 上限（★見本「無料では 1組までです」）。
 *
 *   ★★`lib/compareOrder.js` の `ORDER_MAX`（5）とは 別の 数 です。
 *     ★あちらは 買った あとに 並べられる 数 です。
 */
export const FREE_PAIRS = 1;
export const FREE_LINE = "無料では 1組までです";

/** ★もう 足せない とき。★「買ってください」と 書きません。 */
export function mayAddPair(count, paid) {
  if (paid) return true;
  const n = Number(count);
  return Number.isFinite(n) && n < FREE_PAIRS;
}

/**
 * ★下の 3行（★見本の `.note`・1文字も 変えないこと）。
 *
 *   ★★★2行目・3行目は 裁定197 が 名指しで 守れ と 書いた 約束 です。
 */
export const NOTE_LINES = Object.freeze([
  "たくさん 選ぶほど、はっきり 言えるまでの 日数が 延びます。",
  "書いていない 日は、組に しません。うめません。",
  "機械が 組を 選ぶことは ありません。"
]);
