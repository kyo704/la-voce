// ============================================================================
// ★管理の 操作の 記録 ── 決めごと だけ（★2026-09-23）
//
//   ★★1つの 決めを、★2か所に 書かない こと。
//     ★★日数の 本当の 正は **台帳の 関数**（`purge_ops_audit_log()`）です。
//     ★★ここの 数は、★画面と 報告に 出す ための 写し です。
//     ★★★ずれたら 見張りが 止めます（`components/tests/ops-audit.test.js`）。
// ============================================================================

/** ★何日 残すか（★坂本さんの お決め・2026-09-23）。 */
export const OPS_AUDIT_RETENTION_DAYS = 90;

/**
 * ★消して よい 記録 と、★消しては いけない 記録。
 *
 *   ★★★混ぜません。★「記録を 消す 仕組み」を 1つ 作ると、
 *     ★★次に 誰かが「同じ 仕組みで あれも」と 言います。
 *   ★★消しては いけない ほうを、★名前で 書いて おきます。
 */
export const PURGEABLE = Object.freeze(["ops_audit_log"]);
export const NEVER_PURGE = Object.freeze([
  "monka_read_log",      // ★門下を 開いた 記録（★約束 P-0386「消せません」）
  "org_post_perm_log",   // ★役職の できことを 変えた 記録
  "score_log",           // ★確定の あとに 点を 直した 記録
  "koen_kid_contact_reads" // ★子どもの 緊急の 連絡先を 見た 記録
]);
