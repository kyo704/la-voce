// ============================================================================
// ★録画を 足す ── ★URL だけ（★2026-09-25・C群 束5）
//
//   ★見本 `SC['録画を足す']`（★design-v51・iPhone で 開く用）。
//
//   ★★★4つの 約束（★束5_約束の文・★消さないで と 書かれて います）──
//     ①「YouTube の 限定公開か 公開の URL だけです」
//     ②「URL だけ です」
//     ③「お答えは 記録しません」
//     ④「限定公開に しておけば、URL を 知っている 人だけが 見られます」
//
//   ★★②は 台帳で 守られて います（★2026-09-25・本番で 確かめました）──
//     ★`portfolio_recordings` は id, user_id, title, url, detail, sort_order, created_at。
//     ★★動画そのものを 置く 列が ありません。★置き場（Storage）も 使って いません。
//
//   ★★③も 台帳で 守られて います ── ★答えを しまう 列が ありません。
//     ★`lib/portfolio.js` の `mayAddRecording` は 受け取って 返すだけ です。
//     ★★★だから「記録しません」は ほんとう です。
//
//   ★★★問いと 許す ところは `lib/portfolio.js` が 持ちます ──
//     ★`RECORDING_ASKS`（4つ）／`mayAddRecording`／`ALLOWED_HOSTS`／`urlOk`／`hostOf`。
//     ★★ここで 作り直しません。★1つの 決めを 2か所に 置きません。
//     ★★2026-09-25、★あちらの 問いを 見本の 字に そろえました。
//
//   ★★★4つ とも「はい」でないと 足せません。
//     ★★押せない ままに しません ── ★押した ときに わけを 言います。
//       ★灰色の ボタンだけ だと、★なぜ 押せないか 分かりません。
// ============================================================================

/** ★題（★見本の `<h2>`）。 */
export const TITLE = "録画を 足す";

/** ★戻り先（★見本 `bk('曲')`）。 */
export const BACK_TO = "曲";

/** ★入れる ところの 見出し（★見本の `.h3`）。 */
export const F_URL = "YouTube の URL";
export const F_DATE = "日付";
export const F_PLACE = "場所";

/** ★URL の 下の 1行（★見本の `.usu`・1文字も 変えないこと）。 */
export const URL_NOTE =
  "YouTube の 限定公開か 公開の URL だけです。動画ファイルは 受け取りません";

/** ★問いの 見出し（★見本の `.sh3`）。 */
export const ASKS_HEAD = "載せる 前に";

/** ★答えた／答えて いない の 字（★見本 `はい` ／ `—`）。 */
export const YES = "はい";
export const NOT_YET = "—";

/** ★押しどころ（★見本の `.btn`）。 */
export const BTN_ADD = "足す";

/** ★4つ そろって いない ときの 1行（★見本の `toast`）。 */
export const NOT_ALL = "4つとも 確かめてから 足せます";

/** ★引く／書く 列 だけ。★動画は ありません。 */
export const COLS_RECORDING = "id, title, url, detail, sort_order";

/**
 * ★下の 4行（★見本の `.note`・1文字も 変えないこと）。
 *
 *   ★★2行目の「載せる 方が ご自分で 確かめます」は、★責任の 置き場 です。
 *     ★★こちらが 確かめた ことに しません ── ★確かめて いません。
 *   ★★3行目は 申し立てが あった ときの こと です。★消さないこと。
 *   ★★4行目が ④の 約束 です。
 */
export const NOTE_LINES = Object.freeze([
  "動画そのものは お預かりしません。URL だけ です。",
  "曲・演奏・写っている 人・会場の 権利は、載せる 方が ご自分で 確かめます。上の 4つは その 手がかりです。お答えは 記録しません。",
  "権利を お持ちの 方から 申し立てが あったときは、この 録画の 表示を 止めます。",
  "限定公開に しておけば、URL を 知っている 人だけが 見られます。"
]);
