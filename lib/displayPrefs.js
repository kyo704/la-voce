// ============================================================================
// 見やすさ（見やすさとかんたん表示.md）
//
// ★年齢で切り替えません。年齢を必須で聞きません。
//   40代でも老眼は始まります。20代でも弱視の人がいます。
//   80代でもスマホを速く使う人がいます。そして「あなたは高齢者ですね」と
//   機械に判定されるのは、不快です。
//   年齢は、必要な設定を推測するための代理変数にすぎません。
//   本人に直接、見え方を選んでもらえば足ります。
//
// ★「シニアモード」と画面に書きません。名前は「見やすさ」です。
//
// ★文字の大きさと「かんたん表示」は別の設定です。
//   大きい文字だけ欲しい人がいます。片方だけ変えられること。
//
// ★画面を2つ作りません。CSS変数で、1つの画面が伸び縮みします。
//   2つ作ると、片方だけ直す日が必ず来ます。
// ============================================================================

// ★★★2026-09-16、★3段 → 5段に しました（★見本 `SC['設定']` の 5つ）。
//   ★★両方向の 棚おろしが 見つけました ──
//     ★見本に あって 実装に 無い もの … 小さめ／大きめ／一番 大きい。
//   ★★字も 見本の とおりに しました（★「大きい」→「大きめ」）。
//   ★★`normal` は `data-scale` を 付けません（★既定）。
//     ★ほかの 4つは `app/globals.css` に 段が あります。
// ★★見本から ここまで（自動）ここから
//   ★このあいだは 手で 直さないで ください。★見本から 作られます。
//   ★作り直し …
//     node tools/mihon_extract.js 設定 && python3 tools/mihon_to_spec.py
//   ★出どころ … docs/design/pack-final/00-動く見本（さわれる・全画面）.html
//   ★★鍵（small／normal／…）は 実装の もの です。★見本に ありません。
//     ★保存に 使われるので、★字が 変わっても 鍵は 変えません。
export const SCALES = ["small", "normal", "large", "xlarge", "xxlarge"];
export const SCALE_LABELS = {"small": "小さめ", "normal": "ふつう", "large": "大きめ", "xlarge": "とても 大きい", "xxlarge": "一番 大きい"};
export const SCALE_SAMPLE_PX = Object.freeze({"small": 12.0, "normal": 13.5, "large": 15.5, "xlarge": 18.0, "xxlarge": 21.0});
// ★★見本から ここまで（自動）ここまで
export const DEFAULT_SCALE = "normal";

/** 設定画面に出す名前。★「シニア」「高齢者」と書かないこと。 */


/**
 * 見本の文字。★実寸で出すこと。
 * 「大きい」という言葉では伝わりません。選ぶ人が見て決められるように。
 */
export const SCALE_SAMPLE = "あいう";

/**
 * ★見本の 文字（★見本 `SC['設定']` のとおり ／ 2026-09-15・裁定 ㋙）。
 *
 *   ★★見本は、★**その人の 記録の 形**を 出して います ──
 *     「のどの 調子　◎よい」
 *     「昨夜の 睡眠　7時間30分」
 *   ★★「あいう」では、★自分の 画面が どう 見えるか 分かりません。
 *     ★★記録の 字で 見せる ほうが、★選べます。
 *   ★★`SCALE_SAMPLE`（★あいう）は 消して いません。
 *     ★門の 外（38名）の 画面が、★まだ それを 使って います。
 *
 *   ★★段は 3つの ままです（★裁定 ㋘ は 見送り）。
 *     ★見本は 5つ ですが、★`SCALES` を 増やすのは 別の 作業 です。
 */
export const SCALE_SAMPLE_LINES = Object.freeze([
  "のどの 調子　◎よい",
  "昨夜の 睡眠　7時間30分"
]);

/**
 * ★段ごとの、★見本の 文字の 大きさ（px）。
 *
 *   ★★見本の `[12,13.5,15.5,18,21][S.fs-1]` から、★いまある 3段ぶんを 取りました。
 *     ★`ふつう` … 13.5　★`大きい` … 15.5　★`とても大きい` … 18
 *   ★★5段に する ときは、★両端（12・21）を 足して ください。
 */


/**
 * ★文字の 大きさの 下に 添える 2行（★見本 `SC['設定']` の `.wl` ／ 2026-09-15）。
 *
 *   ★★どちらも **約束** です。★飾りでは ありません。
 *     ★1行目 … 大きくしても 押せる 大きさは 崩れない、という 約束
 *     ★2行目 … 先生・事務の 方も 同じ 設定で 働く、という 知らせ
 *   ★★44 という 数は `lib/uiKit.js` が 持って います。★ここでは 字だけ です。
 *     ★★数を 変える ときは、★両方を 見て ください。
 */
export const SCALE_NOTE = Object.freeze([
  "大きくしても、押せる大きさは 44px を 下回りません。",
  "運営モードにも 同じ設定が かかります（先生・事務の方も 使います）。"
]);

/** ★見本が 太字に して いる ところ（★`<b>運営モードにも 同じ設定が かかります</b>`）。 */
export const SCALE_NOTE_BOLD = "運営モードにも 同じ設定が かかります";

/**
 * ★設定の いちばん 下の 注記（★見本 `SC['設定']` の 最後の `.note`）。
 *
 *   ★★見本は **2行** です ──
 *     ①「記録の 催促は、お知らせに 入れません（設けていません）。」
 *     ②「羊の 動きは、酔う方の ために 止められます。」
 *
 *   ★★★2行目は、★いま 入れて いません（★2026-09-15）。
 *     ★★羊の 動きを 止める スイッチが、★アプリに **ありません**。
 *       ★見本の ⑦ は「新規機能・範囲の 外」と お決まり です。
 *     ★★無い ものの わけを 書くと、★**嘘に なります**。
 *       ★「止められます」と 読んだ 方が 探して、★見つかりません。
 *     ★★スイッチが 入った 日に、★ここへ 足して ください。
 */
export const SETTINGS_NOTE = Object.freeze([
  "記録の 催促は、お知らせに 入れません（設けていません）。"
]);

// ★★スイッチが 入った 日に 足す 1行は、★**字として 置きません**。
//   ★★2026-09-15、★`SETTINGS_NOTE_PENDING` として 置いて みました。★誤り です。
//     ★★どこからも 読まれません（★N-1 の 決まり ──
//       「作った ものは、必ず どこかで 読まれて いるか」）。
//     ★★そのうえ、★見本と 突き合わせる 道具が
//       ★「★アプリに ある」と 数えて しまいました。★出て いないのに。
//   ★★だから、★注記の 中に 書きます。★字としては 置きません。
//     ★足す ときの 文 ──「羊の 動きは、酔う方の ために 止められます。」
//     ★★その スイッチが できた 日に、★上の SETTINGS_NOTE へ 入れて ください。



export function normalizeScale(v) {
  return SCALES.includes(v) ? v : DEFAULT_SCALE;
}

/**
 * html 要素に付ける印。CSS 側の [data-scale="..."] がこれを見る。
 * ★ふつうのときは付けない（既定値を :root に置いてあるため）。
 */
export function scaleAttribute(profile) {
  const s = normalizeScale(profile && profile.display_scale);
  return s === DEFAULT_SCALE ? null : s;
}

/**
 * かんたん表示か。★文字の大きさとは独立して決まります。
 * 片方を選んだらもう片方も変わる、という作りにしないこと。
 */
export function isSimpleDisplay(profile) {
  return !!(profile && profile.simple_display);
}

/**
 * かんたん表示で減らすのは★選択肢であって、機能ではありません。
 * 消すのではなく、奥に置きます。押した先には全部あります。
 *
 * @returns ホームに出す二番目のボタンの上限
 */
export const SIMPLE_HOME_SECONDARY_MAX = 2;

/** かんたん表示のとき、この部品は使わない（§3-2） */
export const SIMPLE_FORBIDDEN_CONTROLS = ["slider", "toggle", "longPressDelete", "swipeOnly"];

/**
 * ★スライダーは、手が震える人には操作できません。
 *   そして正確な値を入れたい人にも向きません。両方に悪い部品です。
 *   かんたん表示では、5つの大きなボタンに置き換えます。
 */
export function controlFor(kind, profile) {
  if (!isSimpleDisplay(profile)) return kind;
  if (kind === "slider") return "buttons5";
  if (kind === "toggle") return "twoButtons";
  if (kind === "longPressDelete") return "deleteButton";
  if (kind === "swipeOnly") return "nextButton";
  return kind;
}

/**
 * ★消える通知を使わないこと（§4-1）。
 *   読み終わる前に消えます。そして「何か出たけど読めなかった」は不安を生みます。
 */
export const USE_DISAPPEARING_TOAST = false;

// ---------------------------------------------------------------------------
// 間違えても壊れない（§4-2）
// ---------------------------------------------------------------------------

/**
 * 保存のあと「取り消す」を出しておく時間。
 * ★消える通知（トースト）ではありません。画面の中に残し、自分で閉じます。
 *   読み終わる前に消えるのが、いちばん不安を生みます（§4-1）。
 */
export const UNDO_WINDOW_MS = 30000;

/**
 * 削除は2段階（§4-2）。「消しますか」→「消す」。
 * ★1回で消える削除を作らないこと。押し間違いは必ず起きます。
 */
export const DELETE_NEEDS_CONFIRM = true;

/**
 * ★エラーには、何をすればいいかを書くこと。
 *   ✗「エラーが発生しました」／「不明なエラー」
 *   ○「保存できませんでした。もう一度［保存］を押してください」
 *
 *   技術的な中身は console に出します。画面には出しません。
 *   利用者が読んで動けるのは、次にすることだけです。
 */
export const ACTIONABLE_ERROR_KEY = "errorSaveFailedRetry";

// ---------------------------------------------------------------------------
// ホーム画面に追加する案内（§6）
//
//   ★この層がいちばん脱落する場所です。
//     PWA は、ブラウザの共有メニューから「ホーム画面に追加」を押さないと
//     アプリになりません。この操作を知らない人が、大多数です。
//
//   ★「あとで」を押せるようにすること。
//     ブラウザのままでも全部使えます。そう書いてあること。
//     追加しないと使えない、と読ませないこと。
// ---------------------------------------------------------------------------

/** iPhone と Android で、案内を出し分ける（§6） */
export const INSTALL_STEPS = {
  ios: [
    "画面の下にある「共有」から開く",
    "出てきた一覧を下へたどり、「ホーム画面に追加」を押す",
    "右上の「追加」を押す",
    "羊のマークができたら、それを押して開く"
  ],
  android: [
    "画面の右上にある ⋮ のボタンを押す",
    "「ホーム画面に追加」または「アプリをインストール」を押す",
    "「追加」を押す",
    "羊のマークができたら、それを押して開く"
  ]
};

/** ★追加しなくても困らない、と必ず書く。 */
export const INSTALL_LATER_NOTE = "ブラウザのままでも、全部そのまま使えます。";
export const INSTALL_LATER_LABEL = "あとで";

/**
 * 案内を出してよいか。
 * ★もう追加済みの人には出さない。閉じた人にも、その後は出さない。
 * ★こちらから何度も勧めないこと（§7-9 と同じ考え方）。
 */
export function shouldShowInstallGuide({ installed, dismissed, platform }) {
  if (installed || dismissed) return false;
  return platform === "ios" || platform === "android";
}
